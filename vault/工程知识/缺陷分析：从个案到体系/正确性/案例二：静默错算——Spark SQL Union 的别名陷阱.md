---
title: 案例二：静默错算——Spark SQL Union 的别名陷阱
status: active
type: guide
updated: 2026-09-20
change_rate: low
confidence: high
review_after: 2027-03-20
tags:
  - data/sql
  - correctness
  - defect-analysis
sources:
  - "https://github.com/apache/spark/commit/d04c7884ca5190fec81a081102276e97197807fa"
  - "https://spark.apache.org/docs/latest/sql-ref-syntax-qry-select-setops.html"
---

# 案例二：静默错算——Spark SQL Union 的别名陷阱

> **要点**：Union 两侧同名列带别名时，优化器会删错别名，**静默返回错误的列值**——不崩溃、行数正常。它必须靠独立参照答案去抓，否则会随写入扩散成持久化的脏数据。

崩溃会喊，静默错算不会。Spark SQL 在特定条件下**静默返回错误的列值**——不报错、不告警、不抛异常，查询正常结束，只是答案错了。而错误结果可以随写入进入下游表，变成报表和决策的一部分。这是正确性缺陷的标准形态，也是八个家族里最难抓的一类。

## 要解决的问题：一个跑得通的 SQL 为什么算错数

**项目**：Apache Spark——面向大规模数据的批处理与 SQL 分析引擎，其 SQL 优化器在改写计划时会把同名列的别名处理错，静默返回错误的列值正是本篇的问题。

Spark SQL 的 Union 语义要求两侧按列位置对齐。当两侧存在**同名列且一侧带别名**时，优化器在改写查询计划的过程中会把这些别名处理错——最终返回的列值取错了来源。[上游 commit d04c7884](https://github.com/apache/spark/commit/d04c7884ca5190fec81a081102276e97197807fa) 修复了这个问题；在此之前，命中该条件的查询一直"正常"返回着错的数据。

## 机制：它是怎么发生的

要理解这个缺陷，得先看 Spark SQL 的执行模型：用户写的 SQL 不直接执行，而是先被翻译成逻辑计划，再由**优化器**做多轮改写——合并冗余节点、删除无用的 Project、化简别名。每轮改写都应该保持语义不变，这叫**计划改写的等价性**。

缺陷就藏在其中一轮改写里。Union 两侧的同名列在内部用别名区分归属，某个化简规则在删除"冗余"别名时，把不该删的那个也删了——两侧的列来源被错误地归并。从这一刻起，计划已经错了，但它看起来完全正常：有列名、有数据、有行数，只是**某些列的值来自错误的输入侧**。

三个因素叠加使它特别隐蔽：

用一个极简查询说明触发形态（两侧同名列各带别名）：

```sql
SELECT a.id FROM (SELECT x AS id FROM t1) a
UNION ALL
SELECT b.id FROM (SELECT x AS id FROM t2) b
-- 命中别名删除规则时，某一侧的 id 取值可能来自错误的输入侧
-- 不报错、行数正常，只有值不对
```

- **不崩溃**。类型系统能对上的错误取值，执行器一行不问照跑。
- **不确定**。是否触发取决于查询的写法——有没有同名列、有没有别名、优化器有没有走到那条规则。同一段代码换一种写法就没事。
- **会扩散**。`INSERT INTO ... SELECT` 一执行，错误值正式进入下游表，从"一次查询的问题"升级成"一份持久化的脏数据"。

## 为什么这一类危险

正确性缺陷的杀伤力用"出现多久"来度量，而不是"多严重"。崩溃类缺陷从出现到告警以分钟计；正确性缺陷从出现到被发现，可能隔一个季度——等下游对账发现数字不对，中间所有基于错误结果的决策都要回溯。

抓这类缺陷只有一条路：**你手里得有一个独立于被测代码的参照答案**。这道题的正确答案是可推导的（Union 的语义有明确的规范定义），测试可以构造两侧数据、断言精确输出。上游修复的提交里就带着这样的对照测试——没有参照答案，这个缺陷就算被撞见了也无法证明它是缺陷。

## 上游的修复

修复在优化器（`Optimizer.scala`）里：改写规则在删除别名前先检查列的归属，属于 Union 边界上用来区分两侧的别名不再被当作冗余删除。提交同时更新了优化器的对照测试套件，以及 TPC-DS 的计划稳定性文件——后者是 Spark 用来自证"计划改动不影响结果"的机制，修复改动计划，就要显式刷新基线并解释原因。

## 边界与教训

- **优化器缺陷的暴露依赖写法**，所以"我这里跑是对的"不构成反证。SQL 引擎的历史上，大量正确性缺陷的复现查询都极短——短到没人相信它会错。
- **不要用被测系统解释自己**。用 Spark 解释 Spark 的结果对不对，是循环论证；参照答案必须来自语义规范或独立实现。
- **写入数据库的错误是最贵的错误**。在写路径上给关键结果加校验（行数、聚合值、抽样比对），让静默错误至少在写入数据库那一刻多一道网。
- **计划改写类系统的测试基线机制值得学习**：把"计划长什么样"固化成可对照的基线，任何非预期改动都会被看见。

概念入口：一条查询在执行前经历了什么——[[工程知识/数据系统：在并发与故障中保存事实/查询与索引/一条SQL如何穿过优化器与存储引擎]]；列别名在改写过程中如何被解读——[[工程知识/软件构建：让变化可以理解、验证与交付/架构与代码/静态分析发现结构问题但不能证明行为正确]]。

相关：[[工程知识/缺陷分析：从个案到体系/缺陷分析的本质——期望与现实的偏差]] · [[工程知识/缺陷分析：从个案到体系/并发/案例三：并发两面——写回调卡死与心跳竞态]] · [[工程知识/缺陷分析：从个案到体系/缺陷分析：从个案到体系——导读与开源地图]]

相关变体：[[工程知识/缺陷分析：从个案到体系/正确性/案例十三：键序与列名——Spark SQL 又两种静默错算]]
