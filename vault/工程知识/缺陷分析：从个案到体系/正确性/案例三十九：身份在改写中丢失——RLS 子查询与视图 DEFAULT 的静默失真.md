---
title: 案例三十九：身份在改写中丢失——RLS 子查询与视图 DEFAULT 的静默失真
status: active
type: guide
updated: 2026-09-21
change_rate: low
confidence: high
review_after: 2027-09-21
tags:
  - database/transaction
  - correctness
  - defect-analysis
sources:
  - "https://github.com/postgres/postgres/commit/db4bc99948bf746c7035b49cc581eb1c44a30788"
  - "https://github.com/postgres/postgres/commit/0a08446308c8387144e69a99bf738e204f11fc70"
---

# 案例三十九：身份在改写中丢失——RLS 子查询与视图 DEFAULT 的静默失真

> **要点**：PostgreSQL 的两条病例——RLS 策略里的子查询被以错误的身份执行、可更新视图的 DEFAULT 写入错误的基表列——共享同一个不变量：**查询在被引擎改写时，附着在原始写法上的身份和列映射没有跟着传下去。**

## 案例背景（要解决的问题：身份在改写中静默丢失，机制是什么）

**项目**：PostgreSQL——开源关系型数据库。这一簇的修复全部来自它的上游，部分发行分支滞后未带上（[上游 commit db4bc999](https://github.com/postgres/postgres/commit/db4bc99948bf746c7035b49cc581eb1c44a30788) 与 [上游 commit 0a084463](https://github.com/postgres/postgres/commit/0a08446308c8387144e69a99bf738e204f11fc70)，P1，正确性类，已真实复现）。

SQL 引擎执行一条查询前要对它做大量**改写**：视图展开成子查询、规则重写、权限策略注入、DEFAULT 值映射到基表列。改写是引擎的日常——每一条改写都要携带两样易碎品：**身份**（这段表达式以谁的身份执行）和**映射**（这个列对应基表的哪一列）。两个病例各碎了一样。

## 病例一：RLS 子查询以调用者身份执行

行级安全（RLS）策略本身是一条表达式，可能内嵌子查询。通过视图访问基表时，策略表达式里的子查询应该以**视图属主**的身份执行（这是"视图代理权限"的语义），实际却以**调用者**的身份执行——安全边界从属主收紧到调用者，策略可能失效。

**为什么引擎会拿错身份**：策略表达式在改写阶段被注入查询树，注入时**没有人去更新表达式内部子查询的用户上下文**——改写只处理了"把策略挂上去"，没处理"策略内部的世界"。修复（[上游 commit db4bc999](https://github.com/postgres/postgres/commit/db4bc99948bf746c7035b49cc581eb1c44a30788)）在关闭基表前对策略条件与 WITH CHECK 选项各调用一次身份传播，把属主身份灌进子查询与 CTE。

## 病例二：可更新视图的 DEFAULT 写错列

多行 INSERT 走可更新视图时，VALUES 里某一行的 DEFAULT 应该映射到基表对应的列。旧实现按**位置列表**索引列映射——多行 VALUES 的位置索引对不上单行的假设时，DEFAULT 写入错误的基表列，**静默写错数据**。

**为什么是静默的**：写进去的值类型兼容、格式合法，任何约束都不拦。只有业务知道"这一列的默认值不对"——和 [案例二](./../正确性/案例二：静默错算——Spark SQL Union 的别名陷阱.md)的 Union 别名同构：**位置是一种隐含约定，改写层对它的假设一旦与实际行数或顺序不符，映射就错位**。修复（[上游 commit 0a084463](https://github.com/postgres/postgres/commit/0a08446308c8387144e69a99bf738e204f11fc70)）改为扫描目标列表中引用该关系层的实际列来构建映射，删除有假设的位置索引。

## 案例关联

两个病例并排放着看，不变量是一句话：**改写一条查询时，跟着原始写法走的隐式约定（执行身份、列位置）必须被显式地重新绑定——改写者只改了它关心的部分，易碎品留在原地。

改写时该随行的两件行李：

```text
改写：视图 V → 基表 T 的子查询
随行物品 1：执行身份 = 视图属主   ← RLS 子查询丢失的就是它
随行物品 2：列映射 = V.col → T.col ← DEFAULT 错位丢的就是它
改写只搬了查询树，两件行李留在原地址
```
** 与 [案例二](./../正确性/案例二：静默错算——Spark SQL Union 的别名陷阱.md)的 Spark SQL 别名删除完全同构：两个引擎、两种改写、同一个"改写时丢上下文"的模型。**任何做查询改写的系统（SQL 优化器、ORM 的查询翻译、规则引擎）都适用这条。**

## 上游怎么修

两处修复都在改写层补"传递"：身份传播函数覆盖策略表达式内的子查询与 CTE；列映射改为按实际 Var 引用构建。共同点是**不改变改写的目标行为，只把改写过程中丢掉的绑定补回来**——这也是改写类缺陷修复的标准姿势。

## 边界与教训

- **改写类的代码要维护一张"随行物品清单"**：身份、权限、列映射、排序语义、NULL 语义——每做一次转换，逐一核对是否跟着走了。
- **安全边界类缺陷优先看身份传播**。策略、视图、存储过程——每层封装都是一次身份可能失真的机会，测试要覆盖"通过封装访问"的路径，不能只测直连。
- **静默写错数据要评估存量**：DEFAULT 写错列写入的数据不会自己恢复，修复之后要对受影响窗口内的数据做清洗评估。

概念入口：认证与授权的分工——[[工程知识/后端系统：在并发、失败与变化中维持服务/服务设计/认证授权决定请求能看什么和做什么]]；一条查询在执行前经历什么——[[工程知识/数据系统：在并发与故障中保存事实/查询与索引/一条SQL如何穿过优化器与存储引擎]]；事务边界与业务用例的关系——[[工程知识/数据系统：在并发与故障中保存事实/事务与存储/事务边界应覆盖完整业务用例]]。

相关：[[工程知识/缺陷分析：从个案到体系/正确性/案例二：静默错算——Spark SQL Union 的别名陷阱]] · [[工程知识/缺陷分析：从个案到体系/正确性/案例十三：键序与列名——Spark SQL 又两种静默错算]] · [[工程知识/缺陷分析：从个案到体系/缺陷分析：从个案到体系——导读与开源地图]]

## 验证

1. 身份复现：以受限用户执行含安全条量子查询的语句，EXPLAIN 预期显示按调用者身份评估；切换 SECURITY DEFINER 后对比评估身份差异。
2. DEFAULT 列位复现：对改列序后的视图做 INSERT，核对写入行的列对位，预期修复后与业务预期一致。
3. 断言锚点：视图变更后必跑行为对比、RLS 视图的身份语义进评审清单。

