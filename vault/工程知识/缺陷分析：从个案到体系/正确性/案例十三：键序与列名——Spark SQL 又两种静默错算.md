---
title: 案例十三：键序与列名——Spark SQL 又两种静默错算
status: active
type: guide
updated: 2026-09-20
change_rate: low
confidence: high
review_after: 2027-03-20
tags:
  - defect-analysis
  - correctness
sources:
  - "https://github.com/apache/spark/commit/2a37f221631b11b40a8fa38535b6ed1f0d06e49d"
  - "https://github.com/apache/spark/commit/e8a578ac757b4e53072af1bec908f6a1ff8ba611"
---

# 案例十三：键序与列名——Spark SQL 又两种静默错算

> **一句话总结**：分组键顺序不一致时 cogroup 静默返回错误分组，分区列与数据列重名时过滤静默返回空结果——两种机制、同一个不变量：引擎对"哪份数据对应哪个名字"的改写没有经过独立参照验证。

## 案例背景

**项目**：Apache Spark——面向大规模数据的批处理与 SQL 分析引擎。

正确性缺陷的核心困难在 [案例二](./../正确性/案例二：静默错算——Spark SQL Union 的别名陷阱.md)已经说过：答案错了，执行器一句话都不说。这一篇补上同批扫到的两条 P0，机制与 Union 别名不同，暴露方式完全一样（均已在本环境真实复现）。

## 两种机制

**机制一：分组键顺序假设**（[上游 commit 2a37f221](https://github.com/apache/spark/commit/2a37f221631b11b40a8fa38535b6ed1f0d06e49d)，P0）。PySpark 的 cogroup 按键对两侧数据分组。两侧的分组键**顺序不一致**时，引擎没有做重对齐，直接按位置配对——分组结果张冠李戴，且重跑也无法恢复。用户拿到的是"格式完全正确"的错误输出。

**机制二：列名解析冲突**（[上游 commit e8a578ac](https://github.com/apache/spark/commit/e8a578ac757b4e53072af1bec908f6a1ff8ba611)，P0）。Parquet 表的分区列与数据列重名（忽略大小写）时，对分区列的过滤被解析到错误的列上，查询**静默返回空结果**——count 与 show 互相矛盾，同一个表两套答案。

## 最小对照

机制一用一对键序不同的 DataFrame 触发，修复前后跑同一份代码对照：

```python
left  = spark.createDataFrame([(1, "a"), (2, "b")], ["k", "lv"])
right = spark.createDataFrame([("b", 2), ("a", 1)], ["rv", "k"])  # 键列在第二位
for k, groups in left.cogroup(right).collect() if False else []:
    pass
# 修复前：按位置配对，(1,"a") 与 ("b",2) 被分进同一组
# 修复后：按列名对齐，k=1 与 k=2 各自成组
```

机制二的最小复现更短——建一张分区列与数据列重名的 Parquet 表，按分区列过滤：

```sql
-- 表内同时存在分区列 dt 与数据列 DT
SELECT count(*) FROM t WHERE dt = '2024-01-01';  -- 修复前返回 0
SELECT count(*) FROM t;                          -- 非空，与上一条矛盾
```

两条 count 互相矛盾，是这个缺陷最容易识别的外部特征——不需要读执行计划，只需把两条结果并排。

## 案例关联：三个机制，一个不变量

和 Union 别名陷阱并排放着看，去掉专有名词，三个机制的骨架是同一句话：**引擎对"哪份数据对应哪个名字"的映射做了一次自以为安全的改写，而等价性没有被独立参照验证。** 别名删除改的是映射，键序假设改的是映射，列名解析冲突还是映射——Spark SQL 的优化与解析层在不停地重写这层对应关系，每一次改写都是一个潜在的静默错算点。

这也解释了修复成本的结构：三处的修复都落在"补一个本该存在的对齐或校验"，几行代码；而发现它们的成本全在"构造独立参照"上——你要先知道正确答案长什么样，才能看出引擎答错了。

## 上游怎么修

两处修复都回到语义本身：键序不同就插入重新分区，让两侧按同一顺序分布；列名冲突时按明确的优先级解析并让过滤落到正确的列。修复不难，难的是有人先意识到"结果不对"这件事需要被报告。

## 边界与教训

- **"格式正确"不等于"内容正确"**。静默错算的输出看起来完全正常，唯一可疑之处是数值本身——对关键结果做聚合值校验，是唯一能抓住它的网。
- **凡是有"默认对齐/自动解析"的地方，都值得问一句**：两侧顺序真的一致吗？重名真的不存在吗？默认值就是缺陷的藏身处。
- **重跑无法恢复的错误结果，说明脏数据已经落盘**——这类缺陷的修复必须伴随对既有产出的清理评估。

概念入口：一条查询在执行前经历了什么——[[工程知识/数据系统：在并发与故障中保存事实/查询与索引/一条SQL如何穿过优化器与存储引擎]]；为什么结构检查证明不了行为正确——[[工程知识/软件构建：让变化可以理解、验证与交付/架构与代码/静态分析发现结构问题但不能证明行为正确]]。

相关：[[工程知识/缺陷分析：从个案到体系/正确性/案例二：静默错算——Spark SQL Union 的别名陷阱]] · [[工程知识/缺陷分析：从个案到体系/缺陷分析的本质——期望与现实的偏差]] · [[工程知识/缺陷分析：从个案到体系/缺陷分析：从个案到体系——导读与开源地图]]
