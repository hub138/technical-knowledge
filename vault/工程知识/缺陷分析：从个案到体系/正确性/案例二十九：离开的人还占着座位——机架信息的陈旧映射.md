---
title: 案例二十九：离开的人还占着座位——机架信息的陈旧映射
status: active
type: guide
updated: 2026-09-21
change_rate: low
confidence: high
review_after: 2027-03-21
tags:
  - defect-analysis
  - correctness
sources:
  - "https://github.com/apache/bookkeeper/commit/857959eb8cf3"
  - "https://github.com/apache/bookkeeper/commit/f507b1c54a3a"
---

# 案例二十九：离开的人还占着座位——机架信息的陈旧映射

> **一句话总结**：拓扑映射表在节点离开后没有移除对应条目，放置策略于是继续按已经不存在的节点做决策——容错域的计算建立在一份过期的世界地图上。

分布式系统要避免把副本放在同一个机架上，否则一个机架掉电就丢全部副本。这个判断依赖一份"哪个节点在哪个机架"的映射。这份映射不是静态配置，它随节点加入与离开变化。问题随之而来：**节点离开时，谁来把它从映射里删掉？** Apache BookKeeper 的这组病例（已在本环境真实复现）说明了漏掉这一步会发生什么。

## 案例背景

**项目**：Apache BookKeeper——为日志条目提供低延迟、可持久化写入的分布式日志存储服务，Apache Pulsar 的存储层。

BookKeeper 支持机架感知的放置策略（`RegionAwarePlacementPolicy`）。它在选择副本位置时，需要知道每个 bookie 节点属于哪个机架/区域，据此把副本分散到不同的容错域。

节点与机架的对应关系维护在一张映射表里（代码中是 `address2Region` 之类），由节点注册与发现流程维护。放置策略每次决策时读这张表。

这张表是放置决策的**唯一输入**。它错了，决策就一定错——而且不会有任何一方察觉，因为决策过程本身完全正常。

## 问题：两个不同层面的陈旧

**病例 A（[上游 commit 857959eb](https://github.com/apache/bookkeeper/commit/857959eb8cf3)，P1）**：bookie 离开时没有从 `address2Region` 中移除。一个已经不存在的节点，仍然在映射表里占着一个机架位置。

后果有两层。表层是决策失真：放置策略认为某个机架上有一个节点，实际那个节点已经不在了——它可能因此避开这个机架（浪费容量），也可能因此认为已经有足够分散度（实际没有）。深层是**容错保证失效**：分散副本的目的是防止单点故障，而基于陈旧拓扑算出的"已分散"，可能实际是"集中在剩下的少数机架上"。

**病例 B（[上游 commit f507b1c5](https://github.com/apache/bookkeeper/commit/f507b1c54a3a)，P1）**：区域感知放置策略里的磁盘权重不更新。节点上的磁盘使用情况变化后，权重仍是旧值，导致按权重分配的决策偏向错误的节点。

两个病例的层面不同——一个是成员关系，一个是容量权重——但结构完全一样：**决策输入是缓存的，而缓存的失效没有被完整覆盖**。

## 为什么是问题：容错保证是悄悄失效的

**系统不会报错。** 放置策略每次都在正常工作：读表、计算、返回一组位置。它没有任何依据去怀疑输入是旧的。返回的节点也确实存在、确实可用——只是分散度不对。

**它破坏的是一种概率性保证。** 机架感知不是"每次都必须分散"，而是"尽量分散以保证机架故障时数据不丢"。基于陈旧拓扑的决策，在多数时候仍能给出分散的结果（因为大部分映射还是对的），只在特定组合下才会把副本集中。这意味着：**系统在大部分时间看起来是安全的**，失效是间歇性的。

**失效只在真正发生故障时暴露。** 平时无法验证副本是否真的分散在不同机架——只有机架真的掉电、交换机真的故障，才会发现"三个副本原来都在同一个机架上"。而那时已经是数据不可用。

**它会随集群变动累积。** 每次节点离开都留下一条陈旧记录，集群运行越久、变动越多，映射表与实际拓扑的偏差越大。这是个单向恶化的过程，没有自愈机制。

**容量与正确性的影响难以分离。** 病例 B 的磁盘权重陈旧会导致某些节点被过度写入。运维看到的是"磁盘使用不均"，容易当成容量规划问题，而根因在决策输入。

## 本质：缓存的失效必须与源的变化同覆盖

去掉 BookKeeper 的所有专有名词，骨架是：

> **任何对外部状态的缓存，其失效路径必须覆盖源状态的所有变化方式；源有 N 种变化而缓存只有 M 种失效（M < N）时，缓存会单向地偏离真实状态，而依赖它的决策会在不知情的情况下基于错误输入运行。**

这条骨架里最值得注意的是"单向偏离"。缓存不会自己变好——没有失效路径的那些变化，会永久累积在偏差里。这与临时性的不一致不同：临时不一致会自愈，覆盖不全的偏差只会扩大。

第二个要点是**失效的触发方**。映射表的写入发生在"节点加入"路径上，这很自然——加入是一个明确事件。而失效需要发生在"节点离开"路径上，这条路径往往更隐蔽：节点可能是正常下线，也可能是崩溃、网络分区、被判定失联。**离开的判定方式有多少种，失效路径就要覆盖多少种**。少覆盖一种，就多一类陈旧。

第三个要点是**没有验证端**。缓存的正确性通常依赖"写入方与失效方都正确"，缺少一个独立的校验者。如果系统定期用真实拓扑去校验映射表，陈旧会被主动发现；没有这个校验，陈旧就一直存在到它造成后果。

## 比喻：座位表上有已经离职的人

公司有一张座位表，行政部门按它安排会议座位——规则是"同一个部门的人不要坐一起，免得部门临时有事全组缺席"。

有人离职了，但座位表没更新——离职流程由 HR 管，座位表由行政管，两边没有联动。座位表上，那个人的位置还在，部门标记也还在。

行政继续按座位表排座。她看到"技术部有 5 个人"，其中一个是已经离职的。实际到场的是 4 个。她按 5 个人的分布来安排，结果可能是：她以为技术部的人已经分散在三张桌子上了（表里显示的），实际只有两张桌子有人，而有两个人其实挨着坐。

平时看不出问题——会议照常开。只有当某张桌子那边临时出了状况（比如那片区域空调漏水），才会发现"技术部的人怎么都在那边"。

更麻烦的是，离职的人越多，座位表越不准。而没有任何一个环节会去核对"座位表上的人今天还在这家公司吗"。

修复不是"更仔细地排座"，而是**让离职这个事件一定触发座位表更新**，或者**排座前先点一次名**。

## 机制：加入有路径，离开没有

从上游提交改动的文件位置能定位到案发地：

```
bookkeeper-server/src/main/java/org/apache/bookkeeper/client/RegionAwarePlacementPolicy.java
bookkeeper-server/src/main/java/org/apache/bookkeeper/client/TopologyAwareEnsemblePlacementPolicy.java
```

拓扑映射的维护逻辑里，节点注册（加入）会写入映射；节点下线、失联、被移出集群等路径上，移除映射的动作缺失或不完整。

这类不对称有个典型成因：**加入是一个主动动作，离开是一个被动判定**。节点加入时，它主动上报自己的机架信息，写入路径清晰。节点离开时，没有节点会上报"我走了"——离开要由监控、心跳超时、或者运维操作来判定，而这些判定逻辑分散在不同模块里。每个模块都认为自己只负责判定，**没有人认为移除映射是自己的责任**。

病例 B 的权重不更新是同一结构：容量信息在写入时被记录，但容量变化（磁盘写入、删除、扩容）不会触发权重重算。同样是"变化发生了，没有对应的更新路径"。

上游修复的方向是补齐失效路径：在 bookie 离开的各个判定点上确保映射被清理；让磁盘权重在使用前或定期重算。

## 怎么发现：让缓存与源可以对账

**定期用源状态校验缓存。** 用真实的节点列表去比对映射表，报告"映射表里有但集群里没有"的项。这是最直接、最可靠的发现手段，而且能发现所有未被覆盖的失效路径——不需要预先知道缺口在哪。

**缓存条目加时间戳与来源标记。** 每个条目记录它最后一次被确认有效的时间。超过阈值未被确认的条目视为可疑。这让"陈旧"变成可度量的量。

**对账差异要告警，不只是记日志。** 拓扑陈旧平时无害，只在故障时致命。因此差异出现时就该告警，而不是等它造成后果。

**在测试中模拟节点离开的各种形态。** 正常下线、进程崩溃、网络分区、被判定失联——每种形态后检查映射表是否正确。只测正常下线的测试会漏掉大部分失效路径。

**把容错保证做成可验证的断言。** 副本放置后，主动验证"这些副本是否真的在不同机架"，不满足就记录。这让概率性保证变成可检查的。

## 上游怎么修

两处修复都是补齐缺失的更新：bookie 离开时从映射中移除，磁盘权重在使用时重算。

值得注意的是修复的**位置**：都在使用侧或离开判定侧，而不是在缓存本身加过期时间。加过期时间（TTL）是一种通用兜底，但对于拓扑信息，TTL 会带来新的问题——过期期间该用什么值？**对于"没有值就不能安全决策"的输入，正确的处理是"值必须是准确的，拿不到就拒绝决策或降级"，而不是"用旧值凑合"**。

这也提示了一个设计原则：**区分"旧值可用"与"旧值不可用"的缓存**。容量权重旧一点无妨（分配稍不均衡），拓扑成员旧一点有害（容错失效）。对后者，宁可保守。

## 边界与教训

- **缓存的失效路径要覆盖源的所有变化方式。** 缺一种，偏差就多一类。
- **加入是主动的，离开是被动的。** 离开的判定分散在多个模块，容易没人认领清理责任。
- **偏差是单向累积的。** 没有自愈机制，运行越久偏差越大。
- **概率性保证的失效只在故障时才暴露。** 平时无法验证，要主动做断言。
- **定期与源对账是最可靠的发现手段。** 它不需要预先知道缺口在哪里。
- **区分"旧值可用"与"旧值不可用"。** 拓扑这类影响安全保证的输入，宁可拒绝决策也不要用陈旧值。

概念入口：服务发现决定请求去向——[[工程知识/后端系统：在并发、失败与变化中维持服务/服务设计/服务发现与负载均衡决定请求落到哪里]]；复制与再平衡改变数据归属——[[工程知识/后端系统：在并发、失败与变化中维持服务/分布式可靠性/复制分片与再平衡改变数据归属]]。

相关：[[工程知识/缺陷分析：从个案到体系/配置/案例三十八：没有写下来的版本——默认值里的迁移债]] · [[工程知识/缺陷分析：从个案到体系/稳定性/案例三十六：写被自己的回调堵死——挂起的 add 回调]] · [[工程知识/缺陷分析：从个案到体系/缺陷分析：从个案到体系——导读与开源地图]]
