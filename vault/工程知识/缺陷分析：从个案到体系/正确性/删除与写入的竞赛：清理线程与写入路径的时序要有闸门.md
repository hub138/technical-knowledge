---
title: 删除与写入的竞赛：清理线程与写入路径的时序要有闸门
status: active
type: mechanism
updated: 2026-09-23
change_rate: low
confidence: high
review_after: 2029-09-23
tags:
  - data
  - correctness
  - defect-analysis
sources:
  - "https://bookkeeper.apache.org/"
  - "https://www.cs.cmu.edu/~dga/15-744/S07/papers/elasticsearch.pdf"
---

# 删除与写入的竞赛：清理线程与写入路径的时序要有闸门

> **要点**：删除（ledger 删除、目录清理）与写入（同对象的追加、复用）并发时，时序错位产生"删除后写入"与"写入后误删"两类数据事故——上游"write stuck due to pending add callback"、"data lost after writing and deleting"两条缺陷是同一竞赛的两面；闸门的设计原则是**状态机迁移点互斥**（删除只清"已完成写入"的对象，写入只接受"未进入删除流程"的对象），交接用原子状态位或租约，不用时间窗猜。

## 要解决的问题：删除与写入赛跑，谁该让谁

删除与写入竞赛的两种输法：**写入后误删**——写入线程刚完成对某文件/分段的追加，删除线程按"过期清单"把它清掉（清单是旧的，写入发生在清单生成之后）——数据丢；**删除后写入**——删除线程正在清理（文件已 unlink），写入线程拿着旧句柄或按旧路由继续追加——要么写进已删文件（数据进了黑洞），要么写失败但回调挂起（写入永久挂起，上游 pending add callback 的形态）。**根因同源**：两个线程对"对象处于什么状态"的认知不同步——写入方认为"存活"，删除方认为"应删除"，认知的仲裁缺失。本库"删除与写入的并发窗口"（复活句柄）讲过同族机制；本篇聚焦**清理流程的闸门设计**。

## 机制：闸门的三种实现与选择

**闸门一：状态机互斥（首选）**。对象的生命周期显式状态化：`WRITABLE → SEALED → DELETING → GONE`——写入路径只接受 WRITABLE（SEALED 后写入被拒并显式报错），删除流程只从 SEALED 进入 DELETING（WRITABLE 的对象不许进删除清单）。**状态迁移点是天然的互斥闸门**：SEALED 迁移（最后一条写入确认时）原子完成，之后写入自然停止，删除安全开始。**上游"data lost after writing and deleting"的修复方向**就是补齐这个中间态：原实现删除不检查写入是否完成，修复后删除前置检查 sealed 状态。

**闸门二：租约与引用计数（跨进程/跨组件）**。写入方与删除方分属不同进程（写入在 broker、清理在 GC 线程甚至独立服务）时，内存状态位不可用——**租约**：写入方持有对象的租约（有 TTL 的锁），删除前必须等到租约过期或显式释放；**引用计数**：每次写入路径的句柄获取计数 +1，结束 -1，删除在计数归零后进行。**租约的取舍**：TTL 要大于最大写入时长（TTL 内的误判 = 写入后误删复发），TTL 过长则删除延迟堆积——TTL 按"最大写入时长 × 安全系数"定，且写入方要定期续租（长写入）。

**闸门三：墓碑与两阶段删除（有历史包袱时）**。无法给对象加状态位时（格式固化、老版本共存），用墓碑记录：删除分两步——先写墓碑（"此对象将于 T 时刻删除"，写入路径见到墓碑即拒新写入），T 到后物理清理——**墓碑的宽限期**就是闸门（写入方有时间收尾），代价是删除延迟与墓碑存储。**两阶段的时间参数**：宽限期 > 写入方感知墓碑的传播时间（通知延迟 + 最大重试间隔），过短的宽限期让闸门形同虚设。

**pending add callback 的卡死面**：写入失败后回调挂起（未成功也未失败），上层等回调等到天荒地老——**回调的完备性**（成功、失败、超时三路都有终点，本库"异常路径上没有完成"的纪律）是闸门之外的配套：闸门防误删，回调完备性防卡死，**两条都齐写入路径才既有正确性又有活性**。超时是回调完备性的兜底：add 操作带超时，超时按失败回调处理并释放闸门资源（引用计数、租约）。

## 看完能判断：竞赛缺陷的现场判定

**症状"写入卡死"**：查三处——回调三路是否齐（超时路缺失是高嫌疑）、闸门资源是否泄漏（引用计数不减、租约不释放）、删除风暴是否在拖（清理线程大批量删除时 IO 饱和，写入的 fsync 排队——本库"清理限速"的同类核算）。**症状"数据丢但无报错"**：查删除清单的生成时点与写入完成时点（清单旧于写入即"写入后误删"），以及状态检查是否存在于删除路径（无检查 = 闸门缺失实锤）。**判定后的修复次序**：先补回调超时（止血活性）、再补删除前置检查（止血正确性）、最后上状态机或租约（根治）——三步对应三层数据事故的收敛。

## 边界

三条边界防止防线走形。

**闸门有成本，粒度要对**：对象级闸门（每 ledger 一个状态位）开销可忽略，操作级闸门（每次写入检查全局锁）会把写入吞吐拖垮——**闸门挂在生命周期节点上**（seal、delete 的迁移点），不挂在高频路径（每条写入）上。

**清理限速与闸门互补**：闸门解决"该不该删"，限速解决"删多快"——大批清理（GC 高峰）即使闸门齐备也会挤占前台 IO，限速让删除风暴不发生（本库"SST 文件回收的对账"的限速同源）。

**恢复流程是第三个竞赛方**：崩溃恢复时重建状态，重建过程中写入与删除都可能到来——恢复期的闸门策略（恢复完成前写入拒绝、删除挂起）要显式设计，**恢复期是竞赛规则最脆弱的时段**（状态未定，谁都不能信），本库"恢复与重平衡的可用性边界"的开关语义同源。

## 验证

可复现的验证路径：

1. 竞态复现：并发写入与删除的压力测试（对象生命周期交错），无闸门版本预期复现丢失或卡死，闸门版本干净；
2. 回调超时验证：注入写入目标不可用，预期超时路触发失败回调、闸门资源释放、无悬挂；
3. 租约验证：长写入持租约期间强制删除请求，预期删除等待或拒绝、写入完成后删除放行；
4. 断言锚点：**删除路径有状态前置检查、回调三路完备带超时、清理有限速**——三件齐备，删除与写入的竞赛有裁判，数据事故的两个方向都有闸门。

```text
状态断言 = 删除只清SEALED后的对象
完备断言 = 回调三路带超时兜底
限速断言 = 清理风暴不挤前台
```

删除与写入的并发正确性不靠运气靠闸门：状态机把"存活与已删除"的边界画清楚，租约让跨进程的认知同步，墓碑给历史包袱留过渡——三条实现按组件关系选，回调完备性与清理限速做配套，写入与删除的竞赛就有了不再输的规则。

## 相关

- [[工程知识/缺陷分析：从个案到体系/并发/删除与写入的并发窗口：复活句柄是静默丢数据的机器.md]] —— 同族机制的个案层
- [[工程知识/缺陷分析：从个案到体系/稳定性/案例三十六：写被自己的回调堵住——挂起的 add 回调.md]] —— 回调挂起的个案
- [[工程知识/缺陷分析：从个案到体系/稳定性/案例二十一：异常路径上没有完成——永不兑现的 future.md]] —— 回调完备性的纪律
- [[工程知识/缺陷分析：从个案到体系/硬件故障/空间回收要有对账表：引用登记让删除不漏不冤.md]] —— 清理侧的对账与登记
