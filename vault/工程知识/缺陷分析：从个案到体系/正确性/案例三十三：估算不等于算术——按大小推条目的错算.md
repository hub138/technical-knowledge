---
title: 估算不等于算术——按大小推条目的错算
status: active
type: guide
updated: 2026-09-21
change_rate: low
confidence: high
review_after: 2027-03-21
tags:
  - data/sql
  - correctness
  - defect-analysis
sources:
  - "https://github.com/apache/pulsar/commit/50754a6c1b0f"
  - "https://github.com/apache/pulsar/commit/bc3ea21385ef"
---

# 案例三十三：估算不等于算术——按大小推条目的错算

> **要点**：用总字节数除以平均条目大小来估算条目数，是一条在边界上必然失真的公式——它假设了平均值的稳定性，而边界情形恰恰是平均值不成立的地方。

估算器上有两类性质不同的缺陷，本篇讲的是其中关于计算本身的那一类：**估算公式在边界上失真**。它常常与读取竞态长在同一个组件上——因为估算器这种"不要求精确、被多处依赖"的组件，往往同时缺少严格的正确性审查和并发审查。

## 案例背景（要解决的问题见下节）

**项目**：Apache Pulsar——分布式的消息与流处理平台，其条目数估算器用总字节数推算条目数，推算过程中隐含的分布假设构成本篇的问题。

`EntryCountEstimator` 负责估算一段区间内的条目数量。精确计数需要遍历所有条目，代价太高；估算则是用已有的元数据（总字节数、已知条目数）推算。

`estimateEntryCountBySize` 是这类估算的典型实现：已知某段数据的总字节数，已知平均条目大小，两者相除得到条目数。

这条公式在**平均值稳定**时是准的。问题在于，它被调用的场景恰恰是平均值不稳定的场景。

## 问题：公式在边界上失真

上游报告的现象（[上游 commit 50754a6c](https://github.com/apache/pulsar/commit/50754a6c1b0f)，P1）是 `estimateEntryCountBySize` 存在问题。

按大小估算条目数，失真的来源有几种：

**平均值不可用。** 没有任何历史条目时，平均大小无从计算。此时代码需要一个兜底值（比如取 0、取 1、取某个常量）。这个兜底值不是估算，是猜测——它会在空账本或新账本上产生离谱的结果。

**边界除法。** 除数为零（总字节为 0、条目数为 0）时结果是除零异常或无穷。被除数为零时结果为零，而"零条目"与"不知道有多少条目"是两回事，返回值却相同。

**分布假设不成立。** 条目大小的实际分布可能高度不均——少量大条目与大量小条目混合。用平均值推算，对大条目段会高估、对小条目段会低估。估算误差不是随机抵消的，而是系统性偏向。

**整数溢出与截断。** 总字节数可能很大，与平均大小相乘或相除时可能越界。这与 [案例九](./../正确性/案例九：数字越过了边界——Redis 里三次截断导致的崩溃.md) 是同一族问题——数字在类型边界上失去意义。

## 为什么是问题：错误的估算会驱动错误的决策

**估算值被用于决策，不只是展示。** 如果只用于显示"大约有多少条"，错一点无妨。但它会被用于判断是否触发账本滚动、是否值得执行压缩、积压是否过大。错误的估算会导致：**该滚动时不滚动、不该压缩时压缩**。这些决策的代价远高于一个显示数字不准。

**它是静默的。** 估算返回 1000，实际是 50，没有任何一方会察觉。没有异常，没有校验——因为"估算"这个性质本身就豁免了精确性要求。**名字里的"估算"二字，让所有不合理的值都变得可被接受**。

**误差在边界上最大，而边界恰恰是需要正确决策的地方。** 新账本（无历史平均值）、空账本（零字节）、超大账本（溢出）——这些不是可以忽略的角落，而是系统状态转换的关键点。

**难以设定正确的期望。** 测试这类函数时，"正确值是多少"本身就不清楚。写测试的人只能断言"大概在这个范围"，而这样的断言几乎无法捕捉到系统性偏差。

**它与其他缺陷叠加。** 本组件同时存在读取竞态（见 [案例三十二](./../并发/案例三十二：取到了已经不存在的那个——估算器的读取竞态.md)）。估算值可能因为竞态返回缺失，也可能因为公式错误返回偏差——两者表现相似，排查时容易混淆。

## 本质：估算公式把假设藏在了实现里

把这条推算公式的隐含前提抽象出来，骨架是：

> **估算用一条公式替代真实的枚举，公式的每一步都引入了关于数据分布的假设；这些假设在典型场景下成立，在边界场景下失效——而失效时的输出与正常输出无法区分。**

这条骨架的关键在于**假设的隐藏性**。

精确计数不需要假设：数一遍就知道了。估算需要假设：用一部分信息推全体，推导规则就是假设。"条目大小差不多"是一个假设，"历史平均能代表当前"是另一个假设，"总量与均值相除有意义"是第三个。

这些假设**不写在任何地方**。它们隐含在 `totalSize / avgSize` 这一行代码里。写这行代码时，作者脑中的场景是"大量条目、大小相近"——在那个场景下公式是对的。后来这个函数被用在空账本上、被用在大小悬殊的混合负载上，假设不再成立，而代码没有任何变化，也没有任何地方表达了"我依赖这个假设"。

第二个要点是**估算豁免了正确性审查**。代码评审时，精确计算的函数会被仔细验算边界；估算函数往往被放过——"反正是估算"。结果是估算函数的边界处理通常比精确计算粗糙得多，而它却被用在同样重要的决策上。

第三个要点是**误差的方向性**。随机误差会互相抵消，系统性偏差不会。按平均值估算对不均匀分布的偏差是**朝一个方向的**——这意味着它不会"偶尔错"，而是"在这类输入上一直错"。

## 比喻：按平均身高估算一队人的总体重

要估算一队人的总体重，已知队里有 20 个人。用"平均体重 60 公斤 × 20 = 1200 公斤"。

这个方法的问题不在算术，在假设。如果这是一支篮球队，平均体重可能接近 90 公斤，估算值严重低于实际；如果是体操队，严重高于实际。**"平均 60 公斤"这个数字来自哪里，决定了对不对**——如果它来自全体人群的统计，那么用在这支特定队伍上就是错的。

还有两个更麻烦的情形。如果队伍刚组建，还没称过任何人，"平均体重"只能靠猜——这时公式给出的 1200 公斤与猜测的东西一样不可靠，但它看起来是一个计算出来的数字，比猜测更有说服力。

如果队伍只有一个人，或者零个人，公式会给出奇怪的结果：零人时算出 0 公斤（看起来对，但"零人"与"不知道有多少人"被混为一谈）；一人时用一个平均值代表他（而这一人的实际体重可能差得很远）。

关键教训：**一个算出来的数字，比一个承认不知道的答复更危险**——因为它会被当作事实使用，而它的不确定性没有随它一起传递。

## 机制：假设如何在边界上失效

估算逻辑集中在这一个类里：

```
managed-ledger/src/main/java/org/apache/bookkeeper/mledger/impl/EntryCountEstimator.java
```

按大小估算的典型实现路径上，以下几个环节各自引入假设：

**获取平均大小**：来自历史统计。历史为空时无值——这一步要么返回默认值（引入猜测），要么需要特殊处理（容易漏）。

**除法运算**：`totalSize / avgSize`。除数为零需要保护；结果为零时的语义（真的是零条目，还是算不出来）需要区分。

**结果的上下界**：估算值可能超出合理范围（比如超过账本理论最大条目数）。缺少 clamp 会让下游拿到荒谬的值。

**类型边界**：总量与均值的乘积可能溢出。这在长时间运行、大账本上才会触发，与 [案例九](./../正确性/案例九：数字越过了边界——Redis 里三次截断导致的崩溃.md) 的数字越界同族。

上游修复的方向是处理这些边界：为平均值不可用的情形提供明确语义（而不是静默用默认值），对除法与溢出做保护，对结果做合理性约束。

这里值得注意一个设计选择的机会：**估算函数可以返回一个区间而不是一个点**。"大约 800 到 1200 条"比"1000 条"更诚实，也更能让下游做出合理决策。代价是接口变复杂。多数实现选择返回点值，然后把这个点值当作事实使用——**这个选择才是问题的真正来源**。

## 怎么发现：把估算当成有前提的计算来审查

**把估算函数的每个假设写下来。** 评审时先列"这个函数依赖哪些关于数据的假设"，再逐个问"什么输入会让它不成立"。这个清单比读代码本身更能发现问题——因为假设不在代码里。

**专门测试边界输入。** 空输入、单元素输入、极端大小、极大总量。估算函数的边界测试比正常值测试更有价值，因为正常值上它通常是对的。

**验证估算值与真实值的偏差分布，而不只是平均偏差。** 跑一批真实数据，计算估算值与实际值的比值分布。如果偏差集中在某个方向（而不是围绕 1 波动），说明存在系统性偏差。

**给估算结果设合理性上下界并断言。** 结果超出理论可能范围时记录告警。这是发现公式错误最廉价的手段。

**区分"算出来是零"与"算不出来"。** 返回值里保留这个区分（比如用 `Optional` 或额外的标志位）。混在一起会让下游无法判断。

## 上游怎么修

修复在边界处理上：为平均值不可用的情形提供明确处理，对除法与溢出做保护，约束结果的合理范围。

值得注意的是修复保持了"估算"这个性质——没有改成精确计数。这是对的：精确计数的代价在热路径上不可接受，估算本身是合理的设计。**该修的不是"用估算"，而是"估算在边界上的行为未被定义"**。

验证的锚点：估算正确性要与三类边界输入对账——预写空账本、新账本与超大账本各自的返回值应有明确约束，实测返回零或常数猜测，与预期对不上。

## 边界与教训

- **估算的假设不写在代码里。** 评审时先列假设，再验算边界。
- **"反正是估算"让它逃过了边界审查。** 而它被用在同样重要的决策上。
- **系统性偏差不会互相抵消。** 偏差朝一个方向意味着在这类输入上一直错。
- **算出来的数字比"不知道"更危险。** 不确定性没有随结果一起传递。
- **区分"算出零"与"算不出来"。** 混在一起会让下游误判。
- **边界测试比正常值测试更有价值。** 估算在正常值上通常是对的。
- **考虑返回区间而非点值。** 点值会被当作事实使用，这是问题来源。

概念入口：算法复杂度连接规模与资源成本——[[工程知识/软件构建：让变化可以理解、验证与交付/算法与数据结构/算法复杂度连接规模与资源成本]]；测试策略按风险选择证据——[[工程知识/软件构建：让变化可以理解、验证与交付/测试与交付/测试策略从风险选择证据]]。

相关：[[工程知识/缺陷分析：从个案到体系/并发/案例三十二：取到了已经不存在的那个——估算器的读取竞态]] · [[工程知识/缺陷分析：从个案到体系/正确性/案例九：数字越过了边界——Redis 里三次截断导致的崩溃]] · [[工程知识/缺陷分析：从个案到体系/缺陷分析：从个案到体系——导读与开源地图]]

## 验证

1. 上游对照：到 Pulsar 的提交 50754a6c 里核对 `estimateEntryCountBySize` 的修复方式，确认偏差范围与兜底取值都做了约束。
2. 边界复现：分别构造空账本（总字节为零）、新账本（无历史平均值可算）、超大账本（总字节接近类型上界）三种输入，记录返回值是异常、是零还是常数猜测，修复前后逐项对照。
3. 分布核验：用少量大条目与大量小条目混合的数据复测，确认按平均大小推算的偏差方向与幅度，并确认偏差是系统性偏向。
4. 决策影响：把估算值接到滚动与压缩的判定逻辑上，确认修复前后触发时机的差异。
5. 断言锚点：边界输入的返回值逐项记录，偏差方向有实测数据，决策链路的触发条件与估算值对应。

