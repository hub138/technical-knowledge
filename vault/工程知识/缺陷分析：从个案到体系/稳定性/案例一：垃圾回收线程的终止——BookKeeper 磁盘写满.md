---
title: 案例一：垃圾回收线程的终止——BookKeeper 磁盘写满
status: active
type: guide
updated: 2026-09-20
change_rate: low
confidence: high
review_after: 2027-03-20
tags:
  - performance/storage
  - defect-analysis
sources:
  - "https://github.com/apache/bookkeeper/commit/35b8f137b49e730efc3c1c0d5e294d2ff827bd42"
  - "https://github.com/apache/bookkeeper/commit/7733db1c9a673371301d585e3d0e35c44c4110f9"
---

# 案例一：垃圾回收线程的终止——BookKeeper 磁盘写满

> **要点**：后台垃圾回收线程遇到未捕获异常会**静默终止**：不留日志、不触发告警，已删除数据的空间随之不再回收，直到磁盘写满停服。修复靠的不是消灭异常，而是让线程"不中断、喊得出"。

稳定类缺陷里有一种最阴险的形态：**后台线程的终止**。前台请求还在正常处理，监控一切发绿——直到某一天磁盘写满、写入失败，你回头看才发现，真正干活的后台线程早已终止了很久。这一篇解剖 BookKeeper 的两个真实病例，同一条垃圾回收线程，两种不同的终止方式。

## 要解决的问题：案例背景

（机制与本质见本节）

**项目**：Apache BookKeeper——为日志条目提供低延迟、可持久化写入的分布式日志存储服务，其后台那条负责回收空间的线程一旦异常退出，节点磁盘就再也无法腾出空间。

BookKeeper 是一种分布式日志存储，Bookie 节点负责存储条目。写入不断产生新的 entry log，删除操作留下的垃圾要靠后台的垃圾回收线程周期性回收——这条线程的健康，就是节点磁盘健康的全部。

- **病例 A（[上游 commit 35b8f137](https://github.com/apache/bookkeeper/commit/35b8f137b49e730efc3c1c0d5e294d2ff827bd42)，P0）**：垃圾回收与压缩周期遇到内存分配错误等未捕获异常时，线程整体停止且**不留任何日志**。已删除数据占用的空间从此不再回收，节点最终磁盘写满、写入失败。
- **病例 B（[上游 commit 7733db1c](https://github.com/apache/bookkeeper/commit/7733db1c9a673371301d585e3d0e35c44c4110f9)，P0）**：垃圾回收扫描到损坏的 entry log 文件时，抛出未捕获的内存溢出错误，回收线程终止——同样导致磁盘无法回收、最终停服。

## 机制：它是怎么发生的

两个病例的触发条件不同，失效机制相同。垃圾回收线程是一个**长生命周期的后台循环**：扫描 entry log、对照账本账目、删除无引用的数据、做压缩。它跑在后台，不服务任何用户请求——这恰恰是危险所在。

病例 A 的触发是一次内存分配失败。Java 里 `OutOfMemoryError` 和内存分配失败都属于 Error 而非 Exception，绝大多数 `try/catch` 根本不接它们。循环体里没有兜底，一次分配失败就顺着调用栈把整个线程打穿——线程静默退出，连一条日志都没留下。

病例 B 的触发是一个损坏的数据文件。扫描读到损坏的 entry log 头，处理路径上抛出 OOME，同样没有兜底，线程终止。

共同点：**周期性后台线程一旦没有最外层的异常兜底，任何一次未捕获的错误都会直接终止它**。而它终止之后，没有任何东西替它报警。

两个病例的共同案发地，从上游提交改动的文件就能看出来：

```
bookkeeper-server/src/main/java/org/apache/bookkeeper/bookie/GarbageCollectorThread.java
bookkeeper-server/src/main/java/org/apache/bookkeeper/bookie/DefaultEntryLogger.java
```

排查者面对的难题是：故障暴露点（磁盘写满）和故障发生点（线程终止）之间隔了几天甚至几周，没有任何日志桥梁——这正是后台线程与前台请求在可观测性上的本质差别。

## 为什么这一类危险

后台线程的终止是"慢性终止"。垃圾回收不发生→已删除数据占着空间→空间慢慢变少→某天写满。从线程终止到故障暴露，中间隔了几天甚至几周，这段间隔造成三个麻烦：

- **现场已毁**。等磁盘写满时排查，你看到的是"磁盘满了"，线程终止那一刻的调用栈早就找不回来。
- **归因遥远**。磁盘写满和"几周前一次内存分配失败"之间没有任何直接线索相连，靠监控告警几乎不可能走回到病因。
- **波及面大**。Bookie 写满会连锁触发集群的写入重试与恢复流程，一个节点的问题放大成一场集群事故。

## 上游的修复

两个病例的修复思路一致：**给循环体加上最外层的兜底，把"错误终止线程"改成"错误跳过本轮、记录日志、下轮继续"**。病例 B 的修复更细——捕获 OOME 后跳过损坏的那个日志文件，回收继续，空间释放能力保留。

修复没有试图消灭内存分配失败或损坏文件——那做不到。它消灭的是"一次错误升级为线程终止"这条传导路径。

验证的锚点：线程存活要与异常注入对账——预写回收周期内制造内存分配失败后该线程应继续存活并留下日志，实测线程退出且无任何记录，与预期对不上。

## 边界与教训

- **兜底不是吞异常**。修复里日志必须记下失败本身，否则线程虽然活着，你依然不知道它每轮都在失败——那只是把线程终止换成了慢性病。
- **给后台线程做"心跳"**。让线程每轮报告"我活着、上轮做了什么、失败了什么"，监控对着心跳告警，而不是对着磁盘剩余量告警——后者是最差的告警位置。后台线程终止了由谁发现，答案必须是线程自己的心跳，而不是它失职的后果。
- **家族聚集是线索**。同一条线程两周内被报了两个不同触发的终止病例——再扫出第三个终止触发时不用惊讶，直接检查循环体还有没有其他未兜底的路径。
- **磁盘写满这类症状是结果不是原因**，顺着结果往前找"谁该干活而没干活"。
- **线程活着不等于队列在收敛**。回收线程每轮正常运行，不代表它处理的每一项最终都会走完——待删集合里的账本可能因条件永不成立而被无限期跳过，堆积成幽灵。中间态必须有人负责收敛，见 [[工程知识/缺陷分析：从个案到体系/资源与容量/案例十七：删除列表里的幽灵——永不收敛的待删账本|案例十七·待删幽灵]]。
- **线程活着不等于行为正确**。本例修的是线程被异常终止，但回收线程的另一类故障来自它读到的输入本身不可信——回收依据 entry log 文件头自述的索引，索引写错时线程会正确地删掉仍在使用的真实数据。健壮性与输入校验是两件事，见 [[工程知识/缺陷分析：从个案到体系/正确性/案例十六：数据被自己人删掉——entry log 头里的假账|案例十六·假账索引]]。

概念入口：后台线程为什么会被一次内存分配失败终止——[[工程知识/性能工程：从用户等待到资源瓶颈/操作系统与运行时/垃圾回收与内存分配决定延迟尾部]]；后台任务的生命周期与清理责任归谁——[[工程知识/后端系统：在并发、失败与变化中维持服务/任务与并发/任务生命周期必须覆盖进程、日志、超时与清理]]。

相关：[[工程知识/缺陷分析：从个案到体系/缺陷分析的本质——期望与现实的偏差]] · [[工程知识/缺陷分析：从个案到体系/复现与回归/从复现到回归——让证据走完一圈]] · [[工程知识/缺陷分析：从个案到体系/缺陷分析：从个案到体系——导读与开源地图]]

相关：[[工程知识/缺陷分析：从个案到体系/稳定性/异常处理器的覆盖面：Error 逃逸终止周期任务]] —— 讲"Throwable 兜底 + 存活监控 + 按对象隔离"的通用防线，本病例修复动作背后的方法面；另有 [[工程知识/缺陷分析：从个案到体系/稳定性/类初始化的隐式输入：静态块里的环境读取]] 讲同批上游环境类故障的启动面，[[工程知识/缺陷分析：从个案到体系/配置/配置模板的段归属：选项写错段落，严格模式一票否决]] 讲配置资产导致的启动面故障

相关：[[工程知识/性能工程：从用户等待到资源瓶颈/存储与网络/磁盘空间管理三原则：配额水位与临时目录纪律]] —— 本病例是"写满后连锁反应"的实例解剖，防线篇讲配额、水位与临时目录的前置治理

## 验证

1. 上游对照：到 BookKeeper 的提交 35b8f137 与 7733db1c 里核对两处修复，确认改动位于 `GarbageCollectorThread.java` 与 `DefaultEntryLogger.java`，方向是给回收周期补上异常兜底与日志。
2. 线程终止复现（病例 A）：在回收周期执行中制造内存分配失败，修复前该线程整体退出且不留任何日志、已删除数据的空间不再回收，修复后异常被兜住、线程继续运行并留下记录。
3. 线程终止复现（病例 B）：让回收扫描读到一个损坏的 entry log 文件，修复前回收线程终止、磁盘占用持续上升直至写入失败，修复后损坏文件被跳过且不影响后续回收。
4. 观测核对：确认线程健康有独立的监控信号，磁盘占用的增长曲线能在写满之前被发现。
5. 断言锚点：回收线程的存活状态可观测，磁盘已删除空间的回收速率有基线，写满场景下写入失败的反馈路径确认可达。

## 收益与代价

按周期任务是否仍在运行来审查后台线程，换来的是兜底缺失、心跳缺失这两类问题在写代码时就能被指出，磁盘写满这类症状会先被追到"谁该干活而没干活"。代价是个案的完整分析要追到上游提交与修复取舍，告警位置还要从磁盘剩余量挪到线程心跳，改动范围超出单个案例本身。