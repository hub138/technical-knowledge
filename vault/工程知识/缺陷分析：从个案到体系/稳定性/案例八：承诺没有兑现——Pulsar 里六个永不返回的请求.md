---
title: 承诺没有兑现——Pulsar 里六个永不返回的请求
status: active
type: guide
updated: 2026-09-20
change_rate: low
confidence: high
review_after: 2027-03-20
tags:
  - "defect-analysis"
  - "software/resilience"
  - "pulsar"
sources:
  - "https://github.com/apache/pulsar/commit/102fdb3a09d883b2d3b41a2e83ebabcb53d995f1"
  - "https://github.com/apache/pulsar/commit/c8872591e2e87cb7f537d9978905e1fc3ba1b618"
  - "https://github.com/apache/pulsar/commit/dcbe23f94a83658649e4eb6111ce0f2e2f5e3cc0"
  - "https://github.com/apache/pulsar/commit/2b85b8752b5ae00fb197e19ccd0476335e5e860a"
  - "https://github.com/apache/pulsar/commit/cbeff98d513564418f4d800d04208d4206335c4e"
  - "https://github.com/apache/pulsar/commit/e15aedfcb3e393745a771fae7030a138e87d07fc"
---

# 案例八：承诺没有兑现——Pulsar 里六个永不返回的请求

> **要点**：一个异步调用一旦交给下游，就必须保证回调在每条路径上都被触发。Pulsar 的六个病例展示了这条保证是怎么在异常路径、资源回收和状态竞争的夹缝里失守的——请求不报错、不返回，只是永远悬着。

崩溃至少是诚实的：进程终止了，监控能看见，告警会响。有一类缺陷比崩溃更难缠——调用发出去之后，**既不成功也不失败，就是不返回**。客户端卡到超时，线程被占住，调用方以为请求还在路上。Apache Pulsar 的上游修复里可以找出六个这样的病例，分布在 broker、managed-ledger、客户端三条互不相邻的链路上，机制却高度同构。

## 六条悬空的路径

**病例 A · 独占生产者的排队窗口**（[上游 commit 102fdb3a](https://github.com/apache/pulsar/commit/102fdb3a09d883b2d3b41a2e83ebabcb53d995f1)，P1）。主题上最后一个共享生产者关闭时，队列里等待独占访问的生产者应当被提升接替。目标版本没有做这个提升，于是创建独占生产者的客户端一直等到超时。改动对应到 `AbstractTopic.java`——补上关闭时的提升逻辑。

**病例 B · 空条目的读取**（[上游 commit c8872591](https://github.com/apache/pulsar/commit/c8872591e2e87cb7f537d9978905e1fc3ba1b618)，P1）。managed-ledger 的游标向 BookKeeper 发起异步读，底层返回空结果时，读请求没有走到完成分支，`OpReadEntry` 的回调永远不触发。消费者收不到消息，也不报错，只能重连或重启。

**病例 C · 分层存储的读句柄被驱逐**（[上游 commit dcbe23f9](https://github.com/apache/pulsar/commit/dcbe23f94a83658649e4eb6111ce0f2e2f5e3cc0)，P2）。读取已卸载到对象存储的历史数据时，空闲驱逐逻辑恰好命中仍在进行的读取，把读句柄关掉。这次读取就此永久悬空，消费回溯和 seek 一起卡住。

**病例 D · 真实的错误被吞掉**（[上游 commit 2b85b875](https://github.com/apache/pulsar/commit/2b85b8752b5ae00fb197e19ccd0476335e5e860a)，P1）。事务元数据查询依赖底层统计信息，统计获取失败时，失败没有沿着 future 传回调用方，请求挂到超时。合入后同一查询立即以真实异常结束——调用方不只是不用干等，还能拿到失败原因去重试。

**病例 E · 元数据无响应时的阻塞调用**（[上游 commit cbeff98d](https://github.com/apache/pulsar/commit/cbeff98d513564418f4d800d04208d4206335c4e)，P2）。元数据存储不响应时，Bookie 注册、ledger 布局查询、副本管理这些阻塞调用没有超时边界，线程一直停在那里。改动给它加了超时上限。

**病例 F · 404 缺失**（[上游 commit e15aedfc](https://github.com/apache/pulsar/commit/e15aedfcb3e393745a771fae7030a138e87d07fc)，P1）。对不存在的非分区主题更新或删除属性，服务端没有返回主题不存在的 404，管理端只能等到客户端超时。这例最轻，也最能说明问题：**缺失的不是一个失败结果，而是一个应当快速返回的失败结果**。

## 要解决的问题：回调的承诺在哪里断掉

（机制与本质见本节）

把这六例并排看，共同结构很清楚：调用方持有一个 future 或回调，期待它在未来某个时刻被"完成"——成功、失败、异常，任何一种都算完成。悬空意味着**这条路径上没有任何代码去完成它**。

断点有三类，覆盖全部六例：

- **分支缺失**：代码处理了"有数据"和"出错"，漏掉了"数据为空""对象不存在"这些合法但没被写进分支的情况。病例 B 的空条目、病例 F 的不存在主题属于此类。
- **异常绕过完成路径**：异常在到达 future 的 `complete` 之前就被抛出或吞掉，调用方拿不到任何信号。病例 D 最典型——底层失败已经发生，只是没被传上来。
- **资源先于回调被回收**：句柄、连接、上下文在回调触发之前被关掉，回调的载体没了。病例 C 的读句柄驱逐是这一类。

病例 A 稍微特殊：它不是异常也不是竞态，而是**状态迁移漏了一个出口**——共享生产者关闭是一个明确的状态变化，却没有触发等待队列里的下一个状态。可以归到分支缺失：出口分支没写。

这三类断点的共同点是，它们在正常路径上都不可见。功能测试跑的是"有数据""正常返回""正常关闭"，恰好绕开了全部三类。

## 为什么这一类危险

**它伪装成慢，而不是错。** 请求不返回，监控看到的往往是延迟升高、队列堆积、线程数上涨，而不是错误率上升。对着错误率配置的告警一条都不会响。 这类缺陷给运维出的难题是：所有常规告警都哑着——错误率、异常数、崩溃计数全部为零，唯一的变化是延迟和堆积，而它们天然每天都在波动，从中认出"这一波不一样"需要基线意识。

**它会累积。** 崩溃会释放资源再重来，悬空的请求不会——每个悬空调用都占着一条线程、一个 future、一份连接资源。病例 E 里线程停在没有超时边界的调用上，累积到线程池耗尽，整个 broker 失去响应能力。故障从"这个请求卡了"演变成"这个节点不工作了"。

**超时掩盖了它，也治不好它。** 客户端超时能让调用方脱身，但服务端那条悬空的路径还在，资源也没释放。超时把"无限等待"变成"有限等待"，没有修复不返回本身。病例 E 的修复是在服务端加超时边界，而不是让客户端别等太久——这个区别决定了修复是治标还是治本。

## 上游怎么修

六个修复的方向一致：**枚举出每条会让回调不完成的路径，给它一个明确的终态**。空条目走完成分支并触发回调；读句柄的驱逐等进行中的读结束；异常沿 future 传回而不是吞掉；阻塞调用加上超时边界；不存在时返回 404；共享生产者关闭时提升等待者。

值得注意的是病例 C 的修法。它可以选择"驱逐时判断有没有在进行中的读"，也可以选择"让读句柄的生命周期覆盖整个读操作"。上游选了前者：**驱逐会等进行中的读结束后再释放句柄**。这是把资源回收的权力和进行中的操作对齐，而不是简单地给驱逐加锁。

## 同族现场

同一条"回调必须被完成"的保证，在不同的项目里以不同面目出现：

- **客户端故障切换的健康检查任务**（[上游 commit 8214bfde](https://github.com/apache/pulsar/commit/8214bfded94bdcff695c3b67eb2373345745c712)，P1）被异常打断后不再运行，集群故障后无法自动切回健康集群。周期任务被打断，本质上也是"承诺的下一次执行没有发生"。
- **去重主题的加载回放**（[上游 commit 0444514a](https://github.com/apache/pulsar/commit/0444514a3f30607f834fb0430a0dbd40ff5b9041)，P1）遇到读取或解析异常时不结束回放，加载请求一直等到超时。
- **栈溢出导致的订阅失败**（[上游 commit e98e8443](https://github.com/apache/pulsar/commit/e98e8443c526a025914e624686775867f7b28088)，P1）是这条保证的对立面：不是回调不触发，而是回调触发得太深——独占订阅在活跃消费者连接关闭过程中接受新消费者，沿同一线程无限递归。修复改成有次数上限的异步延迟重试。

验证的锚点：请求终局要与分支覆盖对账——预写空条目读取与请求不存在的主题都应返回明确结果，实测不返回也不报错，与预期对不上。

## 边界与教训

- **审查异步代码时，枚举的是"回调不完成的路径"，不是"可能出错的路径"。** 出错且被正确处理，不构成悬空；出错但没走到 `complete`，才构成。二者的代码形状完全不同。
- **空、不存在、零长度、已关闭，都是合法结果。** 它们不是异常，因此不会被 `catch` 兜住，却常常被漏写成分支。
- **资源回收的时机要对齐进行中的操作。** 关句柄、关连接、释放上下文之前，先确认没有未完成的操作依赖它。
- **服务端的超时边界是可用性设施，不是可选项。** 依赖客户端超时来兜底，等于把服务端的问题外包给调用方，而资源仍留在服务端。
- **把"不返回"当成一个独立的故障类别去监控。** 只盯错误率会漏掉它；pending 时间分布、future 未完成的存活时长、线程池占用趋势，才是这一类缺陷的报警位置。

概念入口：异步路径上的资源与生命周期责任——[[工程知识/后端系统：在并发、失败与变化中维持服务/任务与并发/任务生命周期必须覆盖进程、日志、超时与清理]]；可观测性为何对"静默"类失效——[[工程知识/性能工程：从用户等待到资源瓶颈/观测与诊断/性能问题定位]]。

相关：[[工程知识/缺陷分析：从个案到体系/稳定性/案例八：承诺没有兑现——Pulsar 里六个永不返回的请求]] · [[工程知识/缺陷分析：从个案到体系/正确性/案例九：数字越过了边界——Redis 里三次截断导致的崩溃]] · [[工程知识/缺陷分析：从个案到体系/稳定性/案例一：垃圾回收线程的终止——BookKeeper 磁盘写满]] · [[工程知识/缺陷分析：从个案到体系/缺陷分析五步法——从个案到通用模型]]

## 验证

1. 上游对照：到 Pulsar 的提交 102fdb3a、c8872591、dcbe23f9、2b85b875、cbeff98d 与 e15aedfc 里逐条核对六处修复，确认作者指派、空条目读取、读句柄驱逐、错误上抛、超时上限与 404 返回各自的改动位置。
2. 分支缺失复现：分别投递空条目与请求不存在的非分区主题，修复前对应的读请求与属性更新调用不返回也不报错，修复后空条目继续推进、不存在的主题返回 404。
3. 异常绕过复现：让底层统计信息获取失败，修复前失败不沿 future 传回、请求挂到超时，修复后同一查询立即以真实异常结束。
4. 资源回收复现：在读取已卸载数据的过程中让空闲驱逐命中仍在进行的读取，修复前该读取永久悬空、消费回溯与 seek 一起卡住。
5. 超时边界复现：让元数据存储不响应，修复前注册与布局查询的线程一直停住，修复后按超时上限结束。
6. 断言锚点：六条路径各自的修复前后行为留档，悬空请求的表现特征（不报错、不返回、占住调用方）被逐条确认。

## 收益与代价

按回调是否被完成来审查异步代码，换来的是空结果、对象不存在、句柄被回收这些合法分支会被逐个列出并给出终态，服务端的超时边界不再由客户端代偿。代价是六条链路的修复要分别追到上游提交与取舍理由，把不返回作为独立故障类别监控还要求补充 pending 时长与未完成存活时间这类指标。

