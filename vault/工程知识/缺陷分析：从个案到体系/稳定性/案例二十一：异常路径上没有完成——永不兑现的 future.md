---
title: 异常路径上没有完成——永不兑现的 future
status: active
type: guide
updated: 2026-09-21
change_rate: low
confidence: high
review_after: 2027-03-21
tags:
  - backend/concurrency
  - defect-analysis
sources:
  - "https://github.com/apache/pulsar/commit/c8872591e2e8"
  - "https://github.com/apache/pulsar/commit/0444514a3f30"
  - "https://github.com/apache/pulsar/commit/e15aedfcb3e3"
---

# 案例二十一：异常路径上没有完成——永不兑现的 future

> **要点**：异步方法在每个正常分支上都会兑现返回的 future，但异常分支常常漏掉这一步；调用方于是永久等待——没有超时、没有错误、没有日志。

异步编程有一条不成文的约定：返回一个 future，就要保证它在**每一条**路径上被兑现。这条约定在正常路径上几乎不会违反——那是开发者最先写、最先测的部分。问题出在异常路径上：那里往往只写了日志与返回，忘了调用方还在等。Apache Pulsar 的这组病例（已在本环境真实复现）是这条规律的集中展示。

## 案例背景（要解决的问题见下节）

**项目**：Apache Pulsar——分布式的消息与流处理平台，其 broker 的异步接口同时依赖回调与 CompletableFuture 兑现结果，异常路径上兑现不了正是本篇的问题。

Pulsar 的 broker 大量使用异步接口：读取条目、重放去重状态、更新主题属性、管理事务元数据。这些方法的共同形态是接受一个回调或返回一个 `CompletableFuture`，在操作完成时兑现。

这类接口的正确性要求有两半：**做对了要通知，做错了也要通知**。第一半通常由业务逻辑直接保证，第二半依赖错误处理代码——而后者往往是后来补上的，或者是从别处复制来的。

## 问题：三个病例，同一个缺口

**病例 A（[上游 commit c8872591](https://github.com/apache/pulsar/commit/c8872591e2e8)，P1）**：从 BookKeeper 异步读取条目时，如果读到的是空条目，`asyncReadEntries` 可能永不完成。空条目不是一个错误，但是一条没有被显式处理的分支——处理循环在遇到它时，既没有继续推进，也没有兑现 future。

**病例 B（[上游 commit 0444514a](https://github.com/apache/pulsar/commit/0444514a3f30)，P1）**：去重状态的重放过程在遇到某些异常时可能永不完成。异常被捕获并记录，但兑现 future 的那一步在异常分支上缺失。

**病例 C（[上游 commit e15aedfc](https://github.com/apache/pulsar/commit/e15aedfcb3e3)，P1）**：主题属性的更新与删除方法同样存在未完成的 future——部分返回路径上没有兑现。

三个病例分属不同模块、由不同人报告，缺口形态完全一致：**存在一条代码路径，它退出时没有兑现调用方持有的 future**。

## 为什么是问题：等待是永久的

**没有超时。** 调用方通常不为这类内部 future 设置超时——它信任被调用方会完成。future 不兑现时，等待方就一直等下去。这与网络调用不同：网络调用有超时兜底，内部 future 没有。

**没有错误。** 它不抛异常、不打错误日志。被调用方可能认为自己"处理完了"（异常已记录），调用方认为"还在进行中"。双方都正常，交易未完成。

**资源随之滞留。** 等待中的请求持有上下文对象、可能的网络连接与分配的内存。这些资源要等 future 兑现才释放。future 永不兑现，资源就永不释放——这是 [案例八](./../稳定性/案例八：承诺没有兑现——Pulsar 里六个永不返回的请求.md) 描述的悬空请求的一个具体成因。

**表现随积压放大。** 单个请求挂起不影响可用性；挂起累积到线程池容量后，新请求排队，整体吞吐下降，最终表现为"broker 变慢"或"部分主题无响应"。运维看到的症状与根因之间隔了好几层。

## 本质：承诺的兑现是全称命题

把这条 future 承诺抽象出来，骨架是：

> **"返回一个 future"是一条全称承诺：它必须在所有可能的退出路径上被兑现，包括每一条异常分支与边界分支。缺失任何一条，等待方就无法区分"尚未完成"与"永远不会完成"。**

这个病例的关键认知在于**"所有路径"这四个字如何被系统性遗漏**。

代码的书写顺序决定了注意力的分布。开发者先写正常路径，它的完成是自然的；再逐步添加错误处理，而每添加一条 `catch`，注意力都在"如何处理这个错误"上——记录、清理、重试——**而不是"调用方还在等"**。错误处理的意图与 future 兑现的意图在代码里是两个独立的动作，前者不会自动带来后者。

于是缺口呈现出可预测的分布：**越晚添加的错误分支，越可能缺少兑现**。这也解释了为什么这类缺陷常常成簇出现——同一个模块里，错误处理是分批补的，每批都可能漏。

还有一个加剧因素：**空分支**。病例 A 的空条目不是异常，而是一个"什么都不需要做"的分支。这类分支最容易被写成"什么都不做"——包括不兑现 future。异常分支至少有 `catch` 块提醒你写点什么，空分支连这个提醒都没有。

## 比喻：挂号后没人叫号，也没人说结束了

医院叫号系统，病人取号后等待。系统规则是：医生看完叫下一个，或护士通知"今天看不了了"。

某天，一个病人取了号，但负责他的医生临时被叫走，没有人接手这个号。系统里没有"这个号作废"的操作，也没有"改由其他医生接手"的操作——这个号就一直挂在屏幕上，状态是"等待中"。

病人不知道要等多久（没有超时），不知道出了问题（没有错误提示），也不知道该找谁（没有负责人）。他只能一直等。而候诊区座位有限，挂着的号越来越多，真正能看病的病人反而没座位了。

修复不是"让医生快点回来"，而是**在系统里补一条规则：任何号，要么被叫到，要么被显式结束**。没有第三种状态。

## 机制：缺失的那一行为什么看不见

从上游提交改动的位置能定位到 managed ledger 与 broker 的异步实现层：

```
managed-ledger/src/main/java/org/apache/pulsar/broker/service/.../ManagedCursorImpl.java
pulsar-broker/src/main/java/org/apache/pulsar/broker/service/.../ (dedup / topic properties)
```

典型的缺口形态：

```java
public void asyncReadEntries(..., ReadEntriesCallback cb, Object ctx) {
    // ... 正常路径
    if (entries.isEmpty()) {
        // 边界分支：既没有继续读，也没有调用 cb.readEntriesComplete(...)
        return;
    }
    // ...
}
```

或者：

```java
try {
    doSomething();
} catch (Exception e) {
    log.error("failed", e);
    return;              // future 从未被 complete / completeExceptionally
}
```

两段代码在语法与风格上都完全正常，静态检查也不会报警——没有一个规则说"catch 块里必须兑现 future"。这就是为什么这类缺陷能穿过代码评审：**缺的是一行，而且这行的缺失在局部代码里看不出来，必须对照调用方的等待才能发现**。

注意病例 A 的特殊性：空条目不是异常路径，而是**边界路径**。它的修复要求开发者意识到"读到空"也是一种需要终止等待的状态。这比异常分支更隐蔽——因为没有一个显式的 `catch` 提示你"这里需要收尾"。

上游修复的共同形态是：在缺失的分支上补上兑现动作（正常则完成，异常则 `completeExceptionally`）。代码量极小，通常是几行。

## 怎么发现：让"未完成"自己说话

**给所有内部 future 加超时。** 这是兜底网。超时不能修复缺陷，但能把"永久等待"变成"可观测的失败"，并且给出堆栈定位到具体位置。生产环境下，这条的价值最大。

**统计未完成的 future。** 维护一个"已创建未兑现"的计数与最长存活时长。健康系统里这个数应该稳定在低位且存活时间短；出现长时间存活的项，就是缺口。

**在测试里注入异常。** 单元测试通常只测正常路径。为异步接口写"每个异常分支都会兑现 future"的断言，是发现这类缺陷成本最低的方式。可以用字节码注入或 mock 强制抛出异常。

**代码评审时按"退出点"而不是按"功能"审查。** 列出方法的所有 `return`、`throw`、循环退出，逐个确认未来的兑现。这个方法笨，但有效——因为它把注意力从"这段代码做什么"转到"这段代码怎么结束"。

**寻找成簇信号。** 同一模块报出一个未完成的 future 时，主动检查同模块其他异步方法——缺口往往不止一处（本例三个病例分属不同模块，但每个模块内部都可能有多个）。

## 上游怎么修

三个病例的修复都是补上缺失的兑现动作：边界分支上正常完成，异常分支上以异常完成。没有重构，没有引入新机制。

值得注意的是修复的**方向**：不是让调用方加超时（那是防御），而是让被调用方履行约定（那是修复）。超时值得加，但如果只加超时而不补兑现，系统仍然在做无用功——每个请求都要等满超时才失败，吞吐不会恢复。

验证的锚点：请求终局要与异步返回对账——预写异步读取返回空条目时应继续推进或明确结束，实测该调用永不完成也不报错，与预期对不上。

## 边界与教训

- **返回 future 就意味着在每条路径上兑现它。** 包括异常分支与"什么都不需要做"的边界分支。
- **空分支比异常分支更危险。** 异常至少有 `catch` 提醒你收尾，空分支连提醒都没有。
- **超时是兜底不是修复。** 它把永久等待变成可观测失败，但不恢复正确行为。
- **未完成的 future 会滞留资源**，累积后表现为整体变慢，症状与根因隔了好几层。
- **成簇出现。** 发现一处，主动检查同模块其他异步方法。
- **评审时按退出点审查**，而不是按功能审查——前者能看到后者看不到的缺口。

概念入口：异步路径上的资源与生命周期责任——[[工程知识/后端系统：在并发、失败与变化中维持服务/任务与并发/任务生命周期必须覆盖进程、日志、超时与清理]]；调用语义与超时边界——[[工程知识/后端系统：在并发、失败与变化中维持服务/分布式可靠性/超时、重试与幂等共同定义调用语义]]。

相关：[[工程知识/缺陷分析：从个案到体系/稳定性/案例八：承诺没有兑现——Pulsar 里六个永不返回的请求]] · [[工程知识/缺陷分析：从个案到体系/并发/案例二十：关闭之后还有人在用——生命周期的时序竞态]] · [[工程知识/缺陷分析：从个案到体系/缺陷分析：从个案到体系——导读与开源地图]]

## 验证

1. 上游对照：到 Pulsar 的提交 c8872591、0444514a 与 e15aedfc 里核对三处修复，确认空条目读取、去重状态重放与主题属性更新三条路径的退出点都补上了兑现 future 的调用。
2. 分支复现（病例 A）：让 BookKeeper 异步读取返回空条目，修复前该调用永不完成、消费者收不到消息也不报错，修复后读请求继续推进或明确结束。
3. 异常路径复现（病例 B）：在去重状态重放过程中注入异常，修复前异常被记录但 future 不兑现、调用方永久等待，修复后调用方拿到失败结果。
4. 返回路径梳理：对每个返回 future 的异步方法，逐条列出正常与异常分支，确认每条分支都有兑现动作。
5. 断言锚点：三处断点各自的修复前后行为留档，等待中的请求所占资源随 future 兑现而释放这一结论有观测依据。

