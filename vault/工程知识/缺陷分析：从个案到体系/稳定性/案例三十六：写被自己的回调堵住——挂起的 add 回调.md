---
title: 案例三十六：写被自己的回调堵住——挂起的 add 回调
status: active
type: guide
updated: 2026-09-21
change_rate: low
confidence: high
review_after: 2027-03-21
tags:
  - backend/concurrency
  - defect-analysis
sources:
  - "https://github.com/apache/bookkeeper/commit/804b2b611cfe"
  - "https://github.com/apache/bookkeeper/commit/33a1985c95ea"
---

# 案例三十六：写被自己的回调堵住——挂起的 add 回调

> **要点**：写入路径用一个集合登记"已发出未回调"的请求；当同一个请求被多个线程重复登记或登记后回调丢失，集合永远不清空，写入方会一直认为还有请求未确认而停止推进。

一个用来做记账的结构，本身也会变成故障源。这类结构与它要防止的问题往往同名：防重复的结构自己产生了重复，防丢失的结构自己产生了丢失。Apache BookKeeper 的这个病例（已在本环境真实复现）属于后者——用来登记"未完成的写"的集合，自己让写永远无法完成。

## 案例背景（要解决的问题：集合再也不空了）

**项目**：Apache BookKeeper——为日志条目提供低延迟、可持久化写入的分布式日志存储服务，其异步写入用一份在途写登记表串起确认回调，回调自身也能把写路径堵住。

客户端写入一条条目（entry）是异步的：`asyncAddEntry` 发出后不阻塞，等副本确认后触发回调。在这段窗口内，这次写处在**已发出、未确认**的状态。

`LedgerHandle` 用一个名为 `pendingAddOps` 的集合登记这些在途的写。这个集合有两个用途：写入限流（在途数量达到上限时拒绝新写）与关闭时的完整性保证（关闭账本前必须等所有在途写确认）。

于是它承担了一个关键的不变量：**集合为空，才代表没有未完成的写**。写入方与关闭流程都依赖这个判断。

## 问题：集合再也不空了

上游报告的现象（[上游 commit 804b2b611c](https://github.com/apache/bookkeeper/commit/804b2b611cfe)，P0）是写入停顿——`Write stuck due to pending add callback by multiple threads`。

停顿的直接原因是 `pendingAddOps` 里残留了永远不会离开的项。写入方看到在途数量不为零，按设计拒绝或延迟新的写入；关闭流程看到集合非空，按设计继续等待。两边都在做正确的事，前提被破坏了。

残留的成因有两种可能形态，它们指向同一个结构问题。

**重复登记**：同一个写请求被多个线程登记进集合，但回调只会移除一次。集合里留下一个没有对应回调的幽灵项。

**回调丢失**：某条异常路径上，请求已登记但回调既没有成功触发也没有失败触发。这一项同样永远留下。

两种形态的后果完全一致：集合的非空不再等价于"有未完成的写"，而依赖它的所有判断随之失效。

## 为什么是问题：停顿比错误更难处理

**它不报错。** 没有异常堆栈，没有错误日志。系统只是在等待一个永远不会到达的回调。从监控看，写入吞吐掉到零，但没有失败计数——这是"健康地停住了"。

**它自我固化。** 一旦集合里有幽灵项，写入方就停止推进；停止推进意味着不会有新的回调产生，也就没有任何东西能把幽灵项清掉。状态机从"等待"进入了"死锁"，而这两者在日志上无法区分。

**影响是整条写入路径。** `pendingAddOps` 不是某次写的私有状态，而是账本级别的共享结构。一个幽灵项会让这个账本上的所有后续写入停住。对于以 BookKeeper 为存储层的 Pulsar，这直接表现为主题不可写。

**归因要跨线程。** 重复登记意味着至少两个线程参与了同一个请求的生命周期。单看任何一个线程的堆栈都是正常的——问题只存在于它们的交错顺序里。这类缺陷无法用单线程的代码阅读发现。

## 本质：记账结构与被记账对象必须同时生灭

把这份登记表的增删行为抽象出来，骨架是：

> **当一个集合的"为空"被赋予了业务含义（此处是"没有未完成的写"），那么集合中每一项的移除必须与它所代表的事件严格一一对应；多登记一次或少移除一次，都会让"为空"这个判断永久失真。**

这里的要害在于**一一对应**。这类记账结构通常有个隐含假设：登记与移除是配对的。这个假设在正常路径上成立，在以下三种情况下会破：

- 登记路径多于移除路径（某个分支登记了，但没有任何分支会移除它）。
- 移除依赖于某个可能不发生的事件（回调）。
- 同一个对象可以被登记多次，但移除是按对象而不是按次数。

第三种是本例的形态，也是最难被发现的一种——因为 `Set` 的语义天然允许"重复添加只保留一个"，而移除一次就移掉了。于是"登记两次、移除一次"的结果是：集合中不剩（看起来正常），或者剩下一个取决于实现细节。这类与容器语义纠缠的缺陷，往往要在具体的并发交错下才暴露。

更普遍地说，这是**状态与事件同步**的一个实例：集合的状态应当镜像外部事件的进度。当两者的同步只在部分路径上维护，状态就会漂移，而漂移后的状态会被当作真实状态使用。

## 比喻：取件柜的格子永远显示占用

快递柜有一排格子，每个格子有占用/空闲状态。快递员放件时把格子标为占用，取件人取走后标为空闲。管理员的规则是：**只有全部格子空闲，才能关柜门做维护**。

某天，一个快递员把同一件快递在两个格子里都登记了占用（一次是扫码入库，一次是手动补录）。取件人来取，系统只清掉了其中一个格子。另一个格子永远显示占用。

管理员每天来看，永远有格子占用，柜门永远关不上。他不会收到任何报错——柜子工作正常，只是永远有个格子非空。更麻烦的是，快递员看到"有格子占用"会以为柜子快满了，开始拒收新件。

问题不在取件人（他正常取了），不在管理员（他严格执行规则），在于**登记与清除不是一一对应的**，而系统把"全部空闲"当作了可信的判断依据。

## 机制：多线程如何制造重复登记

挂起的回调牵涉的两个类：

```
bookkeeper-server/src/main/java/org/apache/bookkeeper/client/LedgerHandle.java
bookkeeper-server/src/main/java/org/apache/bookkeeper/client/PendingAddOp.java
```

写入路径上，`PendingAddOp` 代表一次在途的写，它在创建后被加入 `pendingAddOps`，在回调完成后被移除。

多线程参与的场景出现在**错误恢复与重试**路径上。一次写在副本上超时或部分失败时，客户端可能需要重新发送或重新组织 ensemble。这时同一个逻辑写可能对应多次 `PendingAddOp` 的创建或重新登记，而移除逻辑是按"回调完成"触发的——它不感知"这个对象被登记了几次"。

于是出现这样的交错：线程 A 登记并等待；恢复逻辑在线程 B 上为同一个写再次登记；回调到达，移除一次；集合中残留一项，而这一项对应的写其实早已完成。

相关的另一条上游修复（[commit 33a1985c](https://github.com/apache/bookkeeper/commit/33a1985c95ea)）优化的是 `reorderReadSequence`——它检查整个 ensemble 而不是 WriteSet。这属于同一族的问题：**对集合的遍历与判断范围大于实际需要的范围**，导致本可以快速完成的判断必须等待更多条件。两者都在说同一件事——集合的范围与语义要对齐，过大或过小都会出错。

上游修复的方向是让登记与移除严格配对，并且让并发路径上的重复登记不可能发生（或可识别）。具体形态通常是：确保 `PendingAddOp` 的生命周期由单一线程或单一状态机管理，避免多线程各自登记。

## 怎么发现：质疑"为空"这个判断

**给在途集合加驻留时长监控。** 每一项在集合里待了多久，最老的那项待了多久。健康的在途集合，任何一项的驻留时长都有上界（等于一次往返的超时）。出现远超上界的老项，就是结构性卡住的直接证据。这比看集合大小更早给出信号。

**为登记与移除分别计数。** 两个计数器的差值就是集合应有的大小；实际大小与之不符，说明有重复登记或重复移除。这个校验成本极低，能在问题发生的瞬间给出信号。

**让等待有上限。** 关闭流程等待在途写清空时，如果等了远超预期的时间，应当中止并报告"有 N 项等待超过 T 秒"，而不是无限等待。这让死锁变成可观测的失败。

**在并发测试中交叉触发错误恢复。** 单纯并发写不会触发这类缺陷，必须让"写在途 + 错误恢复/重试"同时发生。测试要覆盖的是交错，不是负载。

**审查时列出所有登记点与所有移除点。** 逐个配对检查：每个登记点是否都有对应的移除路径？每个移除路径是否在异常分支上也成立？这个清单对于这类结构是最有效的审查工具。

## 上游怎么修

修复在生命周期的归属上：让一次写的登记与移除由明确的单一路径负责，避免多个线程各自为同一个逻辑写登记；同时对在途集合的操作加上并发保护，使重复登记在结构上不可能。

值得注意的是，修复没有选择"给等待加超时"这条路。超时能把死锁变成报错，但不能恢复正确的写入——幽灵项仍在，写入仍然是停的。真正的修复必须让记账结构与真实事件重新对齐。

## 边界与教训

- **"集合为空"是判断依据时，登记与移除必须严格一一对应。** 这条不成立的记账结构比没有记账更危险，因为它会给出错误的确定感。
- **防丢失的结构自己会产生丢失。** 用于保证完整性的辅助结构，自身也需要完整性保证。
- **等待与死锁在日志上无法区分。** 要靠驻留时长，而不是靠"有没有报错"。
- **等待必须有上限。** 无限等待让可恢复的故障变成永久停顿。
- **多线程各自登记同一个对象是设计缺陷的信号。** 生命周期应有单一归属。
- **并发测试要覆盖交错，不只是负载。** 错误恢复与正常路径同时进行时才会暴露这类问题。

概念入口：并发控制与完成条件的定义——[[工程知识/后端系统：在并发、失败与变化中维持服务/任务与并发/并发控制先定义共享状态和完成条件]]；任务生命周期与清理责任——[[工程知识/后端系统：在并发、失败与变化中维持服务/任务与并发/任务生命周期必须覆盖进程、日志、超时与清理]]。

相关：[[工程知识/缺陷分析：从个案到体系/稳定性/案例二十一：异常路径上没有完成——永不兑现的 future]] · [[工程知识/缺陷分析：从个案到体系/资源与容量/案例十七：删除列表里的幽灵——永不收敛的待删账本]] · [[工程知识/缺陷分析：从个案到体系/缺陷分析：从个案到体系——导读与开源地图]]

相关：[[工程知识/缺陷分析：从个案到体系/性能/判定范围的口径错位：按谁判，就在谁头上加成本]] —— 引用了本病例同一批上游修复的姊妹病例，讲判定范围与影响范围对齐的方法面

## 验证

1. 卡死复现：压测中注入回调处理线程的阻塞，预期复现集合只增不减、写入停顿；修复后（回调三路完备）预期超时路触发、集合回落。
2. 泄漏对账：运行 24 小时后对比登记集合的大小与实际活跃写数，预期两者对得上（无幽灵登记）。
3. 断言锚点：回调成功失败超时三路齐、记账与被记账对象同生灭。

