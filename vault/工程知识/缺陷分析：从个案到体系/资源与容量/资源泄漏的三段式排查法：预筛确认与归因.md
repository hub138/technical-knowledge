---
title: 资源泄漏的三段式排查法：预筛确认与归因
type: playbook
status: active
updated: 2026-09-22
review_after: 2027-09-22
change_rate: slow
confidence: high
tags:
  - defect-analysis
  - quality
  - operations
  - concurrency
sources:
  - "https://github.com/brendangregg/perf-tools"
  - "https://docs.oracle.com/en/java/javase/17/troubleshoot/diagnostic-tools.html"
  - "https://www.brendangregg.com/methodology.html"
---

## 要解决的问题

内存缓慢上涨、句柄数爬坡、连接数不降——曲线像泄漏，但"像"到"是"之间隔着三道证明：真泄漏还是正常缓存生长（要不要动）；泄漏的是什么（堆、堆外、fd、连接、线程）；漏在哪里（哪条代码路径没释放）。跳过证明直接重启，是这类问题的标准处置，也是标准复发方式。三段式排查法的本质：**把"资源上涨"从运维症状翻译成代码级缺陷证据，每一段都有明确的产出物与判定标准，段与段之间是过滤器——预筛排除假阳性，确认锁定泄漏物与速率，归因拿到可修的代码位置**。

## 机制

**第一段：预筛——这是泄漏吗**。资源上涨有三种无害解释要先排除。缓存生长：设计如此（LRU 未达上限前只涨不跌），查配置上限与命中率的联动（涨的同时命中率不掉、达上限后走平，是缓存；无上限配置且持续线性涨，是嫌疑）。启动期预热：冷启动阶段加载（JIT、连接池建连、页面缓存），预热后走平即正常。度量干扰：容器视角的 RSS 含共享页（page cache 记账），多进程共享内存被重复计量，看 smaps 的 PSS 才是真实占用。**预筛的判定标准：稳态负载下、排除设计性增长、度量口径正确（PSS 而非 RSS）之后仍单调上涨，才进第二段**。

**第二段：确认——漏的是什么、多快**。按资源类型走对应证据链。堆内（Java/Go/JS 运行时）：堆 dump 的对象直方图（jmap -histo、Go 的 pprof heap），找"数量增长且 Retained Size 占大头"的类型（容器类、线程局部、监听器）。堆外（native）：进程 RSS 减堆指标的差值走向、malloc 级观测（jemalloc 的 prof、glibc 的 mtrace），Direct ByteBuffer 类要查运行时的 direct memory 统计。fd：ls /proc/<pid>/fd 的分类统计（socket 还是文件还是 eventfd）、lsof 的句柄类型分布。连接：netstat/ss 的状态分布（ESTABLISHED 堆积是没关 CLOSE-WAIT 堆积是对端关了自己没关——CLOSE-WAIT 堆积直接指向代码没调 close）。**确认段的产出物：泄漏对象类型、泄漏速率（每小时涨多少）、以及一个可复现的观测窗口**（涨势与负载正相关还是恒定速率，两种形态指向不同根因：负载相关=请求路径泄漏，恒定=后台任务泄漏）。

**第三段：归因——谁在漏**。两条路。分配点归因（治本）：泄漏对象"谁创建"的证据——堆 dump 的引用链（GC roots 到泄漏对象的最短路径，直指持有者）、malloc profile 的分配栈、fd 的打开位置（/proc/<pid>/fdinfo 或 strace 抓 open）。关闭点缺失归因（对照）：泄漏资源"该谁关"——代码审查获取/释放的配对（异常路径、超时路径、取消路径是否都释放；try-with-resources / defer / finalizer 的覆盖面）。**两条路对照是归因的完备性检查：找到分配点（谁创建）与释放点（谁该关没关），两个证据对上，归因才算闭合**。

## 背景与代价

思想背景：资源泄漏排查的方法论沉淀在工具史里：Unix 的 lsof/strace（1980s-90s）覆盖 fd 与系统调用层；Java 的 jmap/jhat 到 MAT（Eclipse Memory Analyzer，2000s 中，支配树与泄漏怀疑报告自动化）；Go 的 pprof 把分配画像做进运行时标准件； Brendan Gregg 的 USE 方法（Utilization-Saturation-Errors，2012+）给出资源检查的通用框架。

最精巧的一笔是**三段之间的过滤器设计**：每一段的产出是下一段的输入约束（预筛排除假阳性省下 dump 的开销、确认锁定资源类型决定归因用哪条证据链、归因的两个证据闭合防止"改错地方"）。跳段的代价具体可感：不预筛直接 dump，100G 堆的 dump 抓一小时、分析一小时，结论是"缓存在长"（白干）；不确认类型直接看代码，堆外的泄漏改堆内逻辑（改错）。代价要摆明：**dump 类手段本身是侵入性的**（STW、磁盘写、敏感数据落盘，生产上要先问能不能对影子实例或复现环境做）；归因到"该关没关"的代码路径后，修复常牵扯生命周期设计（不是补一行 close 能了：连接池的归还语义、回调的注销时机、缓存的逐出钩子），修复成本在排查的十倍以上是常态——这也是为什么归因段的两个证据闭合值得花时间：改错地方的返工比排查本身更贵。

## 边界

- **泄漏与洪泛的区分**：曲线陡涨（分钟级翻倍）先怀疑突发负载或攻击（洪泛、爬虫、重试风暴），走限流与过载保护路径，不是排查法射程。排查法对付的是"慢性、单调、负载无关或弱相关"的上涨。
- **native 泄漏的观测要运维配合**：malloc profiling 需要 preload 库（jemalloc）或重启开选项（glibc mcheck），生产进程不能随便重启时，影子实例复现是替代路径。堆外泄漏是三段里证据最难拿的一段，预留时间。
- **"修复后验证"要有基线**：修复上线后同负载下重测泄漏速率（之前每小时涨 X，修复后走平才算修好）。只看"没告警了"不算证据——告警阈值以上的涨势可能仍在，只是还没到。
- **多语言混合进程（JNI、cgo、FFI）是归因地狱**：堆内工具看到 native 分配的黑盒，native 工具看到 VM 管理的内存乱飞。这类进程的泄漏排查预算按三倍估，优先在边界层（JNI 全局引用统计、cgo 的 pprof 标签）找接口泄漏。

## 验证

- **三段全流程实验**：写一个故意泄漏的程序（每请求创建一个不关闭的连接/不释放的 buffer），部署后跑三段：预筛（排除缓存：无上限配置、恒速上涨）→ 确认（/proc fd 分类：socket 持续涨）→ 归因（fdinfo/strace 定位到泄漏函数）。预期：三段各拿到对应证据，最终代码位置与实际埋的泄漏点一致。方法可用性的端到端验证。
- **假阳性预筛实验**：同样的观测窗口跑一个正常 LRU 缓存服务。预期：预筛段的上限配置与走平形态把它挡在第二段外，不触发 dump。过滤器有效性的证据。
- **CLOSE-WAIT 对照实验**：两个版本的服务端（正确 close vs 忘记 close），客户端主动断开。预期：忘记版的 ss 输出 CLOSE-WAIT 单调堆积，正确版无残留。连接泄漏的形态特征可复现。

## 相关

- [[工程知识/缺陷分析：从个案到体系/复现与回归/从复现到回归——让证据走完一圈.md]] —— 三段式的每段产出都是证据链一环，复现到回归的方法论母篇
- [[工程知识/后端系统：在并发、失败与变化中维持服务/服务设计/连接池的容量数学：池大小与排队等待的权衡.md]] —— 连接泄漏与池化设计互为因果：泄漏让池耗尽，池的指标是泄漏的第一观测面
- [[工程知识/性能工程：从用户等待到资源瓶颈/观测与诊断/性能问题定位.md]] —— 资源上涨是 USE 方法的 Utilization 维度，定位方法共用证据素养
