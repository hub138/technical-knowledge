---
title: 复用错误：同一对象重入使用路径，释放语义被重放
status: active
type: pattern
updated: 2026-09-23
change_rate: low
confidence: high
review_after: 2027-03-23
tags:
  - resource-capacity
  - defect-analysis
  - processes
  - operating-systems/fd
sources:
  - "https://github.com/apache/bookkeeper/commit/47c3fc37a0"
  - "https://docs.oracle.com/javase/8/docs/api/java/lang/AutoCloseable.html"
---

# 复用错误：同一对象重入使用路径，释放语义被重放

> **要点**：按"一次请求一个对象"设计的资源，被放进每次调用都执行的路径里反复新建；对象本身用完即弃没有错，错在新建与释放没有闭环——每调用一次，泄漏一份。

## 要解决的问题

客户端对象（数据库连接的轻量封装、RPC client、管理器句柄）有一个典型用法约定：创建成本高，按连接池或长生命周期持有。这个约定在代码里没有强制力——构造函数谁都能调。当一段按"工具函数"思路写的代码在每次调用里新建一个客户端、用完不关，约定就被打破了：每次调用泄漏一份线程、连接、文件句柄。

Apache BookKeeper 的一个真实缺陷（上游 commit 47c3fc37，已在本环境真实复现）就是这个形态：开启 HTTP 管理接口后，"删除账本"接口的每次调用内部新建一个 BookKeeper 客户端对象，用完从不关闭；每调用一次，客户端持有的线程池与网络连接就泄漏一份。运维批量清理账本时反复调用接口，线程与连接随调用次数累积，最终耗尽句柄，服务不可用，只能重启。同样的问题还出现在 DistributedLog 命名空间的流删除与枚举路径上——三处独立的代码，同一个疏漏。

单次调用视角下一切都对：对象创建成功、操作完成、接口返回成功。缺陷只累积在时间轴上，典型的高危点正是**管理接口、清理脚本、定时任务**这类低频但会被反复触发的路径。

## 机制：所有权没有随调用栈闭环

资源泄漏的本质可以用一句话刻画：**创建与释放的所有权没有闭环**。"每次调用新建一个客户端"的设计隐含所有权约定——调用栈负责释放，栈帧退出时资源归还。这个约定需要代码显式兑现（try-with-resources、defer、RAII），它不会自动发生。

三个因素让闭环特别容易断在管理路径上。

**工具函数的历史包袱**：管理接口常从一次性脚本演化而来，脚本语境里"进程跑完就退出，泄漏无所谓"成立；函数被搬进长生命周期服务后，语境变了，所有权代码没有跟着变。

**对象"看起来轻"**：一个客户端包装对象在 Java 堆上确实不大，泄漏的观感被堆内存曲线掩盖；真正累积的是它持有的原生资源——线程栈、socket、文件描述符，这些不归 GC 管，也不进堆直方图的前几名。

**触发频率低**：管理接口不是热路径，几百次调用才见分晓。压测覆盖不到，监控看到的是"缓慢上涨的线程数"，直到触顶。

三因素共同的解法方向只有一个：**把所有权写进结构，不靠作者记得**。

## 修复与防线

上游修复把三处泄漏点全部改成 try-with-resources——语句块结束即关闭，所有权随语法结构闭环。这个动作背后的防线值得逐条记下。

**语言结构优先**：能用 try-with-resources（Java）、`using`（C#）、RAII（C++）、`defer`（Go 的恢复路径）的结构化方案，就不用手写 finally。结构化方案把"忘记关闭"从可能变成语法错误级别的困难。

**新建客户端是告警级味道**：在长生命周期服务里，业务路径上出现"每次调用 new 一个重型客户端"直接按坏味道评审。判据：对象是否持有线程、连接、文件句柄中的任何一种——持有即重型，重型即需要明确的所有权归属（池、单例、或调用栈内闭环）。

**接口内部不藏所有权转移**：管理接口的实现如果需要临时客户端，所有权要写在方法签名能看出来的地方（返回的资源要关、传入的资源不关）。藏在实现里的所有权转移是最难审查的泄漏源。

**泄漏哨兵指标**：线程数、连接数、fd 数按"应随负载波动、不随调用次数单调"检查。管理接口的调用计数与资源占用放同一张图，两条线同步上涨就是泄漏直证。

## 边界

**try-with-resources 的嵌套释放顺序**：多个资源的关闭顺序与声明相反，依赖顺序的释放（先关会话再关连接）要检查声明顺序——结构化方案解决"忘记"，不自动解决"顺序"。

**关闭异常不能掩盖业务异常**：try-with-resources 里业务代码抛 A、关闭抛 B 时，B 作为 suppressed 附加在 A 上，主异常保留。自写 finally 时手动捕获关闭异常再抛业务的，别把 B 顶掉 A——诊断时业务异常才是主线。

**池化对象不适用本条**：从池里借的对象归还即可，"用完关闭"反而破坏池。防线判据按所有权归属走：池拥有的对象借还配对，调用栈拥有的对象创建释放配对。两套纪律别混用——对池化对象调 close 是常见的反向错误。

## 验证

可复现的验证路径：

1. 调用循环：对管理接口（或任意被怀疑的路径）连续调用 N 次（N 取 500 到 5000），每次调用前后采样线程数、连接数、fd 数；
2. 断言恒定：采样值在调用结束后回到基线，不随 N 单调增长；
3. 修复前基线：未修复版本跑同一循环，记录增长率作为缺陷证据；
4. 哨兵测试：把"调用 N 次资源恒定"写成回归测试，任何新引入的泄漏路径当场红。

```text
循环 N 次调用 → 线程/连接/fd 采样回到基线
哨兵 = 采样值不随调用次数单调上涨
```

fd 与线程采样用系统级命令（`ls /proc/<pid>/fd | wc -l`、线程 dump）就能完成，不需要任何插桩。预期与实际的对账就是验证本身：N 次调用后采样值回到基线，说明所有权闭环成立；对不上则按"创建点 - 释放点"的差集找缺口。防线的代价与收益同样直白：try-with-resources 是零运行时成本的结构约束，换来的是"忘记关闭"从线上事故降级为编译期习惯，泄漏哨兵多采几个数，换来资源曲线异常在触顶之前数天可见。

## 相关

- [[工程知识/缺陷分析：从个案到体系/资源与容量/资源泄漏的三段式排查法：预筛确认与归因]] —— 泄漏的通用排查路径
- [[工程知识/缺陷分析：从个案到体系/资源与容量/案例六：容量与泄漏——连接泄漏和 SST 堆积]] —— 同一上游缺陷的案例侧写
- [[工程知识/后端系统：在并发、失败与变化中维持服务/任务与并发/任务生命周期必须覆盖进程、日志、超时与清理]] —— 资源生命周期的框架性讨论
- [[工程知识/缺陷分析：从个案到体系/并发/案例二十：关闭之后还有人在用——生命周期的时序竞态]] —— 关闭时机的另一类错误
