---
title: 只增不减的索引：墓碑靠压实才能变成空间
status: active
type: pattern
updated: 2026-09-23
change_rate: low
confidence: high
review_after: 2027-03-23
tags:
  - resource-capacity
  - defect-analysis
  - operating-systems/fd
sources:
  - "https://github.com/apache/bookkeeper/commit/fbd33b56"
  - "https://github.com/facebook/rocksdb/wiki/Compaction"
---

# 只增不减的索引：墓碑靠压实才能变成空间

> **要点**：LSM 类存储的删除只是写入墓碑标记，磁盘空间要等压实（compaction）把墓碑与旧数据一起丢弃才真正释放——回收流程漏掉某个索引目录的压实，删除越多磁盘占用越大，且一切指标看起来正常。

## 要解决的问题

"删了数据，磁盘为什么没变少？"是 LSM 类存储（RocksDB 及其一切变体）最经典的疑问。答案写在这类存储的结构里：删除操作写入一条墓碑记录（tombstone，标记"此键已删"），被删的数据本体与墓碑都留在原处，等后台压实把同一键的旧值与墓碑一起物理丢弃，空间才回到文件系统。**删除是两段式的：逻辑删除立即生效，空间释放等待压实。**

两段式结构带来一个隐蔽的缺陷形态：回收编排漏掉某个子目录的压实。Apache BookKeeper 的一个真实病例（上游 commit fbd33b56，已在本环境真实复现）演示了完整链条：bookie 删除账本后，位置索引（locations 目录的 RocksDB 实例）里产生可回收的墓碑；后台垃圾回收只按周期压实 entry log 主数据，**从不压实位置索引**——墓碑永远躺在那里，SST 文件随删除次数单调累积，磁盘占用持续增长，恢复依赖运维手动调用接口触发压实。

这个缺陷的危险在于它的伪装性：垃圾回收在正常运转（主数据在回收）、删除在正常生效（读不到已删账本）、监控的磁盘曲线只涨不跌——涨速温和，像正常业务增长。直到某天磁盘写满，回看曲线才发现"增长与业务量无关，与累计删除量线性相关"。

## 机制：谁负责让墓碑兑现

把删除的完整生命周期排出来，缺口的位置看得非常清楚。

**写入侧**：正常写入产生 SST 文件，层层压实合并。这条路通常没问题是压实策略的默认配置与大量生产检验。

**删除侧**：删除写墓碑，墓碑本身是普通数据，走同样的写入与压实路径。压实遇到"墓碑 + 更老的数据"时同时丢弃——这是空间释放的兑现点。

**回收编排**：存储引擎往往有多个 RocksDB 实例或多个目录（主数据、索引、元数据），每个实例都需要压实。引擎自带的压实只管"自己这个实例的 SST 合并"，**实例之外没有谁统筹"每个存储区域都配置了压实且周期合理"**。BookKeeper 的病例里，locations 实例的压实压根没有编排进去——不是压实失败，是从来没人调用。

缺口就在编排层。每一层单看都合理：RocksDB 说"压实我负责，你得调用或配置触发"；垃圾回收说"我负责 entry log，那是我的职责清单"；配置说"压实参数有默认值，应该是开着的"——职责清单的空白处，墓碑永远不兑现。

## 为什么监控看不见

这类缺陷躲过监控的原因值得单独讲，因为它是资源类缺陷的通用伪装术。

**磁盘曲线与业务曲线同形**：删除量与业务量通常正相关，"磁盘随业务增长"看起来天经地义。区分二者需要把磁盘占用对"累计删除量"回归，而不是对"时间"或"写入量"——没有几个系统的监控做到这一步。

**回收指标显示正常**：垃圾回收的指标（回收了多少 entry log、压实执行了多少次）全部健康——它们度量的是"被编排的动作"，而缺陷恰恰是"有动作没被编排"。监控只能看见存在的东西，看不见缺席的调用。

**单机复现需要时间**：压测环境跑几小时，删除量不够多，增长看不出来。这个缺陷的时间常数以周计，普通测试窗口抓不住。

三条加起来，缺陷只在生产环境以月为尺度暴露，且暴露时的第一症状是磁盘告警，回溯定位要走"哪部分占用可回收"的下钻—— compaction 统计、每目录占用、删除量对照，一路查到"这个目录从来没有压实过"。

## 修复与防线

上游修复补上了缺席的编排：新增配置项控制位置索引的压实周期（带随机延迟打散），按周期调用压实清理 SST。动作很小，背后的防线体系值得展开。

**回收编排要覆盖每个存储区域**：列出存储引擎的全部数据目录与实例，逐项确认"压实方式、触发周期、可回收性"三项。清单化是对付"缺席调用"的唯一可靠手段——靠记忆，漏掉的正是记忆里没有的。

**墓碑兑现率作为指标**：统计"墓碑产生量"与"墓碑兑现量（压实实际丢弃）"，两条曲线长期背离就是压实缺席的直接证据。这条指标把"缺席"变成了可观测的东西，是三条防线里唯一能事前报警的。

**删除与释放的对账**：定期对账"逻辑删除量"与"磁盘释放量"，比值长期显著小于 1 说明存在只删不放的区域。对账思想与静默失真检测同源——不依赖报错信号，靠两个独立口径的数字互相印证。

**压实参数按区域设定**：写多读少的元数据索引、按删除频率波动的位置索引，压实周期应短于主数据——墓碑兑现越快，空间释放越及时。默认参数为通用负载设计，按负载特征调参不是优化，是正确性配置。

## 边界

三条边界防止防线用歪。

**压实有写放大代价**：压实是重写数据的操作，过于频繁的压实消耗 IO 与 CPU，反噬写入延迟。周期设定要在"空间释放及时性"与"压实 IO 开销"之间权衡，监控压实本身的耗时与排队——压实积压也是磁盘曲线异常的来源之一。

**压实不等于备份**：墓碑兑现后数据物理消失，"删错了靠压实找回"不存在。压实管空间，备份管后悔，两个机制别混为一谈。

**删除语义可能另有设计**：有的系统故意延迟释放（回收站、软删除窗口），那是产品语义，缺陷的判定标准是"编排声称要回收的区域实际没回收"。先读语义再判定，别把设计当缺陷，也别把缺陷当设计。

## 验证

可复现的验证路径：

1. 压测构造：在测试环境对 RocksDB 实例（或使用 LSM 的存储）执行大量写入-删除循环，删除键空间大于写入存活集；
2. 观察基线：记录 SST 总占用随删除量的增长曲线——无压实时持续增长，这是缺陷形态的证据；
3. 打开压实重跑：同一压测下，SST 占用回落到存活集量级并稳定；
4. 断言锚点：磁盘占用 ≈ 存活数据量 + 压实滞后窗口内的墓碑量，与累计删除总量无关。

```text
压测 = 写入-删除循环（删除量 >> 存活量）
空间断言 = 磁盘占用收敛到存活集量级，与累计删除量无关
指标断言 = 墓碑兑现率长期趋近 1
```

"磁盘占用 ⊥ 累计删除量"这条不变量适用于一切带墓碑机制的存储，值得成为容量巡检的标准判据。验证时预期与实际的对照规则要提前写明：打开压实后，SST 占用预期收敛到存活集量级，对不上（仍随删除量线性涨）即压实编排仍有缺席区域；墓碑兑现率预期趋近 1，对不上即压实周期跟不上删除速率，该调参了，缺陷不在这里。

## 相关

- [[工程知识/缺陷分析：从个案到体系/资源与容量/案例六：容量与泄漏——连接泄漏和 SST 堆积]] —— 同一存储引擎的 SST 堆积案例侧写
- [[工程知识/缺陷分析：从个案到体系/资源与容量/周期任务只进不出的集合：清理动作自己也要被清理]] —— 回收编排缺席的另一形态：重复执行而非从不执行
- [[工程知识/数据系统：在并发与故障中保存事实/事务与存储/存储引擎的两大路线：B树与LSM的写读取舍]] —— 墓碑与压实的机制基础
- [[工程知识/缺陷分析：从个案到体系/正确性/静默失真的检测靠对账与不变量而不是报错]] —— 删除与释放对账的方法论出处
- [[工程知识/缺陷分析：从个案到体系/资源与容量/回收要遍历每个候选：清理任务不能只认一条入口]] —— 同一缺陷的编排面方法：垃圾产生路径 × 回收入口的责任矩阵
