---
title: 周期任务只进不出的集合：清理动作自己也要被清理
status: active
type: pattern
updated: 2026-09-23
change_rate: low
confidence: high
review_after: 2027-03-23
tags:
  - resource-capacity
  - defect-analysis
  - concurrency
  - processes
sources:
  - "https://github.com/apache/bookkeeper/commit/0d31202037"
  - "https://github.com/apache/bookkeeper/commit/0d31202037933d1a0524b1c9ca45e09a44372cfa6"
---

# 周期任务只进不出的集合：清理动作自己也要被清理

> **一句话总结**：周期任务的工作集合登记了要清理的对象，清理完成后不把条目移走，下一次周期把全部历史重新清理一遍——工作量随历史单调增长，系统被自己积累的"已完成工作"拖慢。

## 要解决的问题

后台周期任务有一个通用形态：维护一个待处理集合，每个周期把集合里的项处理一遍（清理、同步、重试、上报）。这个形态隐含一条完成纪律：**处理成功的项要从集合里移走**。缺了这一步，集合只进不出，每个周期都把全部历史重新处理一遍，任务越来越慢，占用的内存越来越多，而系统没有任何报错——它在勤勤恳恳地重复劳动。

Apache BookKeeper 的一个真实病例（上游 commit 0d312020，已在本环境真实复现）是标准样本：存储节点删除账本后，把账本记进一个待删除集合，每个写缓存刷盘周期都执行一次"清理这些账本的索引并同步元数据"。清理逻辑没有把已删账本从集合移除，于是发生过一次删除之后，**每一次刷盘都在重新清理全部历史删除**。运行时间越长、累计删除越多，每次刷盘越慢，写延迟持续上升，集合占用的内存同步增长，重启才能清零。

## 机制：完成信号缺失时，周期语义放大一切

单次执行视角下，"处理完不移除"是个无害的小疏漏——工作照样完成了。周期语义把它放大成性能缺陷，中间的传导链值得完整看一遍。

**集合的语义漂移**：设计意图是"待处理队列"，实际行为变成"全部历史台账"。集合不再回答"现在有什么要做的"，改答"曾经有过什么"——两个语义的工作量差一个运行时长的数量级。

**每周期全量重放**：周期任务的执行成本从 O(当前待办) 退化为 O(累计历史)。退化没有阈值、没有报警，只有随时间的线性爬坡——最容易被误判成"数据量自然增长"。

**与热路径的耦合放大伤害**：这个集合的清理被挂在写缓存刷盘的路径上，重复清理直接拖慢每次刷盘，写入延迟随之上升。纯后台任务的重放只是浪费资源，挂在热路径上的重放直接变成用户可感知的延迟。

**内存随集合增长**：集合里的条目持有关联对象（账本元数据、索引引用），只进不出意味着内存占用单调上涨，最终成为资源缺陷而不只是性能缺陷。

## 为什么完成纪律总被漏掉

登记与处理写在同一个函数里，**移除却属于另一个时刻**——"确认成功"的时刻。代码结构在鼓励遗漏：处理循环的结尾没有天然的钩子提醒你"这里该移除了"，而失败路径（处理失败要保留、下次重试）的存在让移除逻辑必须带条件。带条件的收尾步骤是最容易被整体省略的——先省掉让功能先跑起来，回归时没有任何测试覆盖"处理过的项不再出现"，缺陷就定居了。

还有一个认知偏差在帮忙：**重复清理看起来无害**。幂等的清理动作重复执行，结果正确、数据无损，只是浪费。浪费不触发任何错误处理路径，于是没有任何信号。把"结果正确但成本失控"识别为缺陷，需要的视角是资源账本而不是正确性——这正是性能类缺陷与正确性缺陷的分界。

## 修复与防线

上游修复的动作极小：处理成功的条目从集合移除；集合为空时整个流程提前返回。极小的修复背后是三条可推广的防线。

**完成即移除**：处理成功的条目在同一事务语义下移出集合。移除与处理要么都发生要么都不发生，不允许"处理了但没记"的中间态。

**空集合快路径**：集合为空时提前返回，不做任何扫描。这条看似多余（空集合循环本来就不执行），实际上它声明了"无事可做是这个任务的常态"，让最常见情形的成本为零。

**工作量守恒监控**：给周期任务加一个自监控指标——单周期处理条数。健康值围绕"新增待办量"波动；持续等于累计历史总量、随时间线性上涨，就是重放缺陷的直接信号。这条监控在缺陷出现前就能报警，是三条防线里唯一"事前"的。

推广成清单：

- **设计周期任务先写三句话**：集合里是什么、处理成功后去哪、失败后去哪——三句话写不出来，完成纪律就没想清楚；
- **幂等不豁免完成纪律**：重复执行结果正确只是缺陷的遮羞布，成本账单独算；
- **挂在热路径上的后台工作要单独评审**：它的成本直接进用户延迟，预算与告警按热路径标准给。

## 边界

**失败保留是纪律的另一半**：移除只针对成功项，失败项必须留在集合里等待重试。省略失败判断的"一律移除"会把偶发失败变成永久丢失——集合清空了，工作没做完。完成纪律的完整形态是"成功移除、失败保留、（必要时）失败计数与退避"。

**集合要有界的另一面是拒新的**：如果集合无界增长来自生产速率超过消费速率（待清理的产生速度超过清理能力），移除纪律解决不了，那是容量问题，要背压或限流。完成纪律管"做过的别再做"，容量管理管"做不过来的别都接"，两条防线管两件事。

## 验证

可复现的验证路径：

1. 压测构造：周期性执行"登记-处理"循环，登记 N 项，处理完成后下一个周期开始计时；
2. 断言处理成本：第 2 个周期起，单周期处理条数应回到新增待办量级，与历史累计量无关；
3. 断言内存：集合长度在处理完成后回落，不随周期数单调增长；
4. 失败注入：让部分处理失败，断言失败项保留到下一周期、成功项按期移除。

```text
健康判据 = 单周期处理条数 ≈ 新增待办量（与累计历史无关）
内存判据 = 集合长度处理完成后回落
失败判据 = 失败项保留重试，成功项移除
```

工作量守恒判据写进后台任务的通用巡检项，这一类"只进不出"的缺陷在上线前就能拦下。防线的代价与收益算得清：完成即移除多写一行代码，换来任务成本与历史总量解耦；空集合快路径多一个判断，换来常态下零开销。预期与实际的对账是验证的核心——第 N 个周期的处理条数预期等于新增待办量，对不上就说明集合里混进了不该留的项。

## 相关

- [[工程知识/缺陷分析：从个案到体系/资源与容量/资源泄漏的三段式排查法：预筛确认与归因]] —— 内存单调增长的排查方法
- [[工程知识/缺陷分析：从个案到体系/资源与容量/案例十七：删除列表里的幽灵——永不收敛的待删账本]] —— 同一上游缺陷的案例侧写
- [[工程知识/后端系统：在并发、失败与变化中维持服务/任务与并发/任务生命周期必须覆盖进程、日志、超时与清理]] —— 周期任务的生命周期框架
- [[工程知识/性能工程：从用户等待到资源瓶颈/观测与诊断/系统指标与观测]] —— 工作量守恒指标的观测基础
