---
title: 案例六：容量与泄漏——连接泄漏和 SST 堆积
status: active
type: guide
updated: 2026-09-20
change_rate: low
confidence: high
review_after: 2027-03-20
tags:
  - defect-analysis
  - resource-capacity
sources:
  - "https://github.com/apache/bookkeeper/commit/47c3fc37a075b0bb991f8ecabb91415a04dcb1ec"
  - "https://github.com/apache/bookkeeper/commit/fbd33b56210bc17965c2d4bf0d835c44c5d4836f"
---

# 案例六：容量与泄漏——连接泄漏和 SST 堆积

> **一句话总结**：每次删除请求泄漏一个客户端、SST 文件从不自动清理——单日毫无异常，累积数月后致命。修法是把资源归还交给语言结构、把回收交给周期触发，而不是指望有人记得。

容量类缺陷是慢刀子：单次请求看不出任何问题，累积几个月之后一刀致命。这一篇的两个病例都来自 BookKeeper，都在**长期运行**后才暴露——一个是连接泄漏，一个是文件堆积。它们共享同一条底层规律：**资源被创造出来的速度正常，被释放的速度出了问题，或者根本没有释放这一说**。

## 病例一：管理接口泄漏客户端（P0）

**背景**：BookKeeper 的 server 提供一套 HTTP 管理端点，其中"删除账本"接口每收到一次请求，就新建一个 BookKeeper 客户端去执行删除。

**缺陷**：这个客户端**用完没有关闭**。每次删除请求泄漏一个客户端实例——每个实例背后是连接、线程、内存。单次请求毫无异样；长周期反复调用后，服务端连接被耗尽，服务停摆。

**机制**：资源的使用者（一次 HTTP 请求）和资源本身（客户端，带连接池和后台线程）**生命周期不匹配**。请求结束，使用者消失了，资源还活着——没有人拥有它的关闭职责。

**修复**（[上游 commit 47c3fc37](https://github.com/apache/bookkeeper/commit/47c3fc37a075b0bb991f8ecabb91415a04dcb1ec)）：改成 try-with-resources，让客户端**随请求结束自动释放**。修复没有加限流、没有加缓存，就是把"资源归还"这件事还给语言的结构去做——Java 的 try-with-resources 就是为这种场景设计的。

## 病例二：SST 文件堆积（P0）

**背景**：Bookie 启用 entry location 存储后，位置索引以 SST 文件形式写在 locations 目录。这些索引随数据的删除本应被回收。

**缺陷**：locations 目录的 SST 文件**没有自动清理的逻辑**——删除数据后索引文件留着，长期运行后占满磁盘，节点不可用。

**机制**：和病例一不同，这里不是"泄漏"，而是**清理逻辑缺失**。数据生命周期管理只写了"怎么写"，没写"怎么回收"——回收需要周期性触发机制，而那个机制没有被实现。

**修复**（[上游 commit fbd33b56](https://github.com/apache/bookkeeper/commit/fbd33b56210bc17965c2d4bf0d835c44c5d4836f)）：加入周期触发逻辑，运维无需手动介入，SST 文件自动回收。

## 为什么这一类危险

两个病例的死法一模一样，值得单独强调：

- **发作时间与引入时间完全脱钩**。泄漏从第一天就在发生，第 90 天才炸。等炸的时候，引入它的那次代码变更早被几百次提交埋住——归因难度是所有家族里最高的。
- **监控告警的位置总是太晚**。连接耗尽告警时，泄漏已经发生了几千次；磁盘 90% 告警时，能追的踪迹只剩"增长曲线从哪天开始抬头的"。
- **它惩罚的是"低频路径"**。两个病例都发生在管理/维护路径上——删除接口、位置索引回收。高频路径被反复测试，低频路径恰恰是资源管理最容易漏的地方。

## 上游的修复

两个修复都指向同一个设计原则：**资源的释放不靠人的自觉，靠结构**。try-with-resources 让"谁申请谁释放"由语言保证；周期触发逻辑让"该回收的会被回收"由系统保证。手工清理脚本可以救急，但把回收建立在"有人记得跑脚本"上，就是把缺陷延迟到下一次忘记。

## 边界与教训

- **审查资源管理代码时，先画生命周期**：谁创建、谁使用、谁负责关闭、异常路径上关不关。四个问题里答不出任何一个，就是缺陷。
- **低频路径要有专属测试**。管理接口、恢复流程、后台清理——这些路径的测试频率应该和它们的爆炸半径匹配，而不是和触发频率匹配。
- **增长曲线是最早的告警**。连接数、文件数、句柄数这些"只涨不跌"的指标，斜率突变就是泄漏的签名——监控它们的变化趋势，比监控绝对阈值早几周发现问题。

病例一的累积过程画出来就是一条只升不降的线：

```
第 1 天    删除接口请求 #1      → 客户端实例 #1 泄漏
第 N 天    累计泄漏 #N          → 连接池逐渐吃紧
第 M 天    连接耗尽             → 服务停摆
```

单看任何一天的监控都是正常的——这正是它靠绝对阈值抓不住的原因，也是"增长曲线是最早的告警"这条结论的依据。

- **修复之后要做长时间验证**。泄漏类缺陷的复现周期以天计，回归验证跑一遍不够，要挂上趋势观察确认曲线真的平了。

概念入口：SST 文件在 LSM 存储里扮演什么角色、为什么它会堆积——[[工程知识/数据系统：在并发与故障中保存事实/事务与存储/LSM树用写放大换取顺序写和可扩展性]]。

相关：[[工程知识/缺陷分析：从个案到体系/缺陷分析的本质——期望与现实的偏差]] · [[工程知识/缺陷分析：从个案到体系/稳定性/案例一：垃圾回收线程的死亡——BookKeeper 磁盘写满]] · [[工程知识/缺陷分析：从个案到体系/缺陷分析：从个案到体系——导读与开源地图]]

相关：[[工程知识/缺陷分析：从个案到体系/资源与容量/复用错误：同一对象重入使用路径，释放语义被重放]] —— 讲"所有权随调用栈闭环 + 泄漏哨兵指标"的通用纪律，是本病例修复动作背后的方法面

相关：[[工程知识/缺陷分析：从个案到体系/资源与容量/只增不减的索引：墓碑靠压实才能变成空间]] —— 讲"墓碑兑现依赖压实编排"的机制与磁盘占用不变量，本病例 SST 堆积的近亲形态

相关：[[工程知识/性能工程：从用户等待到资源瓶颈/存储与网络/连接池的验证要看池内水位：活跃数、等待数与超时率三个水位计.md]] —— 连接泄漏在运行时的水位形态：活跃数只涨不降、借还配对缺口

相关：[[工程知识/缺陷分析：从个案到体系/硬件故障/空间回收要有对账表：引用登记让删除不漏不冤.md]] —— SST 类文件回收的对账体系：泄漏量与丢失量双指标
