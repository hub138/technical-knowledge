---
title: 删除列表里的幽灵——永不收敛的待删账本
status: active
type: guide
updated: 2026-09-21
change_rate: low
confidence: high
review_after: 2027-03-21
tags:
  - defect-analysis
  - resource-capacity
sources:
  - "https://github.com/apache/bookkeeper/commit/0d31202037b8"
  - "https://github.com/apache/bookkeeper/commit/52d779ae4a6b6e1a4e2b3f4c5d6e7f8a9b0c1d2e"
---

# 案例十七：删除列表里的幽灵——永不收敛的待删账本

> **要点**：删除流程分步执行时，中间态需要有人负责推进到终态；当推进只在某一条路径上发生，走另一条路径进来的记录会永远停在中间态——它们占用空间、参与统计，却再也不会被清理。

删除一个东西，很少是一步完成的。分布式存储里尤其如此：要先标记、再确认没有读者、再回收空间、最后清掉元数据。这条链上每走一步，系统就处在一个**中间态**。中间态本身不是问题，**没有人为中间态的收敛负责**才是。Apache BookKeeper 的这个病例（已在本环境真实复现）展示了一条中间态如何变成永久状态。

## 要解决的问题：进了集合就再没出来

**项目**：Apache BookKeeper——为日志条目提供低延迟、可持久化写入的分布式日志存储服务，其账本删除要先放进待删集合再由回收线程清账，这个缓冲能否收敛构成本篇的问题。

账本（ledger）的删除在 BookKeeper 里不是单个动作。删除请求到达后，账本先被放进一个**待删除集合**（`pendingDeletedLedgers`），等待垃圾回收线程在下一轮扫描中真正执行空间回收；回收完成，元数据里的账本记录才被移除。

这个设计的用意是合理的：删除请求可能高频到达，而空间回收是重操作，攒一批批量处理更划算。待删集合就是这两者之间的缓冲。

缓冲带来一个隐含要求：**集合里的每一项最终都必须被取出并处理**。如果出现某条路径把账本放进集合、却没有对应的路径把它取出来，这些账本就变成了幽灵。

## 问题：进了集合就再没出来

上游报告的现象（[上游 commit 0d31202037](https://github.com/apache/bookkeeper/commit/0d31202037b8)，P1）是待删账本集合持续增长，且其中的账本迟迟不被清理。

后果有两层。表层是**空间不释放**：账本被用户删除了，用户认为空间已经归还，实际数据还躺在磁盘上。深层是**元数据膨胀**：待删集合本身要参与持久化与统计，它越长，扫描一轮的代价越大，形成正反馈——处理变慢，堆积更多。

这类问题的隐蔽之处在于，从单个账本的视角看，一切正常：删除命令成功返回，账本状态显示为已删除。要看出问题，必须从**集合的整体规模**这个视角看——而监控通常不采集这个量。

## 为什么是问题：慢性的、自我加速的

**它不报错。** 没有任何一条异常日志。删除接口返回成功，回收线程每轮照常运行、照常处理队列里的其他项。系统从内部视角看完全健康。

**它自我加速。** 待删集合变长，导致每轮扫描需要遍历更多元素、比对更多元数据，单轮耗时增加；耗时增加意味着单位时间内能处理的账本变少；处理变少又让堆积更快。这是一个没有自限性的正反馈，直到磁盘或内存先撑不住。

**它污染判断。** 运维看到磁盘空间没有如期释放，第一反应是回收线程挂了（例如 [案例一](./../稳定性/案例一：垃圾回收线程的终止——BookKeeper 磁盘写满.md)描述的那种终止）。按这个方向排查会完全走错——线程活得好好的，每轮都在跑，只是它处理的对象里有一批永远轮不到。

**它延迟暴露且归因困难。** 待删集合涨到引发故障，可能需要数周。而要定位到"某条入队路径缺少对应的出队处理"这个根因，得把多条代码路径逐一比对，看哪些路径进了集合、哪些路径会触发消费。

## 本质：中间态必须有人负责收敛

去掉所有专有名词，骨架是：

> **分步流程产生的每一个中间态，都必须存在一条保证把它推进到终态的路径；如果入队路径多于出队路径，多出来的那部分会永久停留在中间态。**

这句话里"保证"两个字是核心。不是"通常会有路径处理它"，而是**每一条入队路径都要有对应的出队保证**。缺一条，就缺一类。

可以把它看作一个守恒关系：进入中间态的速率与离开中间态的速率在稳态下必须相等。当离开的速率结构性低于进入的速率（而不是暂时性低于），系统里就会积累永久性的中间态。

这类缺陷与资源泄漏是近亲，但有个重要区别：泄漏通常是"拿了没还"，而这里是"标记了没做完"。泄漏的对象往往可以直接找到持有着；中间态的幽灵**没有任何一方认为自己在持有它**——入队方认为已经交出去了，处理方认为队列里没有它。

## 比喻：挂号单掉进了缝隙

医院分诊台发挂号单，病人拿着单子去候诊区等叫号。医生每看完一个，从候诊区叫下一个。

正常情况，每张单子都会走到被叫号这一步。但假设有一条特殊通道：某些病人从后门进来，分诊台给他们开了单子、让他们在候诊区等着，却**把单子记在了另一本本子上**——医生叫号用的本子里没有他们。

这些病人会一直等下去。他们确实在候诊区（占用座位），分诊台确实发了单子（流程合规），医生确实在按顺序叫号（每轮都在工作）。但因为叫号依据的那份名单里没有他们，永远不会轮到。

而且随着等待的人变多，候诊区越来越挤，医生从诊室走到候诊区的时间变长，叫号速度也跟着下降——越挤越慢，越慢越挤。

要发现问题，你不能问任何一个病人"你被服务了吗"（他们会说"我在等，正常的"），也不能问医生（"我一直在叫号"）。你只能去看**那两本名单是否对得上**。

## 机制：入队与出队不是对称的

待删账本的两侧代码，回收线程与元数据管理：

```
bookkeeper-server/src/main/java/org/apache/bookkeeper/bookie/GarbageCollectorThread.java
bookkeeper-server/src/main/java/org/apache/bookkeeper/meta/LedgerManager.java
```

待删账本集合由回收线程维护，账本元数据的读写经过 `LedgerManager`。

问题的结构是这样的：账本进入待删集合的路径不止一条，但**触发消费的条件只覆盖其中一部分**。某些情况下（例如账本在元数据中的状态已经处于特定终态、或删除请求来自某些特定调用方），账本被加入集合，却因为状态判断不成立而在后续扫描中被反复跳过。

于是出现一个静默的无限循环条件：

1. 账本 X 被加入 `pendingDeletedLedgers`。
2. 回收线程扫描到 X，检查其状态，判定"当前不满足回收条件"，跳过。
3. 下一轮再扫描到 X，状态没有任何变化（因为没有任何流程会去改变它），再次跳过。
4. 永远如此。

关键在第 2 步与第 3 步之间：**没有任何东西会改变 X 的状态**。跳过动作本身不产生副作用，状态机就此停住。这不是等待，这是死锁——但因为它表现为"每轮都在正常跳过"，看起来与"暂时不满足条件"毫无区别。

上游修复的核心是让**清理动作不再依赖那个永远不成立的条件**，或者为不满足条件的项提供一条明确的终结路径（记录并移出待删集合），而不是无限期重试。

## 怎么发现：盯着中间态的存量，而不是流程的存活

这类缺陷无法靠"流程是否运行"来判断，因为流程在运行。有效的观测点：

**监控中间态集合的存量与驻留时长分布。** 不看总量（总量上涨只是结果），看**最老的那一项待了多久**。一个健康的分步流程里，任何一项的驻留时长都应有上界。出现远超出上界的老项，就是结构性卡住的证据。

**对入队与出队分别计数。** 两个计数器长期不相等，差值即堆积速率。这比看集合大小更早给出信号，因为它在堆积还小的时候就能发现速率差。

**为跳过动作打点。** 回收线程每次跳过一个待删项，记录跳过原因与该项的驻留时长。同一个项被跳过 N 次以上，应当触发告警——重试是有意义的，无限重试不是。

**定期做入队/出队对账。** 主动构造一批删除，验证它们是否在预期时间内走完全流程。这是唯一能主动发现（而不是等故障暴露）的手段。

## 上游怎么修

修复方向是补齐缺失的收敛保证：让清理逻辑在处理待删项时不因为不满足某个条件就无限期挂起，而是要么处理、要么显式判定为无法处理并移出集合（同时记录），保证集合中的每一项都有终点。

这里的设计取舍值得注意：**"跳过并重试"在分布式系统里通常是正确的默认策略**（应对暂时性故障），但它只在"条件会自行变化"时成立。当条件不会自行变化时，跳过重试就退化成永久挂起。区分这两者的方法很简单——问一句：**这个条件由谁来改变？** 答不出具体的改变者，就不能用跳过重试。

## 边界与教训

- **中间态要有明确的收敛负责人。** 设计分步流程时，为每一个中间态写明"谁负责把它推进到下一步，超时怎么办"。
- **跳过重试的前提是条件会变。** 没有改变者的条件不适用重试策略，必须走终结路径。
- **监控存量与驻留时长，不只监控流程存活。** 流程活着但对象卡住，是最容易漏掉的一种故障形态。
- **入队路径与出队路径要成对审查。** 加一条新的入队路径时，同步确认出队侧是否已覆盖。
- **"每轮都在正常跳过"与"卡死"在日志上无法区分**，要靠计数与时长来分辨。

概念入口：后台任务的生命周期与清理责任——[[工程知识/后端系统：在并发、失败与变化中维持服务/任务与并发/任务生命周期必须覆盖进程、日志、超时与清理]]；重试在什么条件下才有意义——[[工程知识/后端系统：在并发、失败与变化中维持服务/分布式可靠性/超时、重试与幂等共同定义调用语义]]。

相关：[[工程知识/缺陷分析：从个案到体系/稳定性/案例一：垃圾回收线程的终止——BookKeeper 磁盘写满]] · [[工程知识/缺陷分析：从个案到体系/正确性/案例十六：数据被自己人删掉——entry log 头里的假账]] · [[工程知识/缺陷分析：从个案到体系/缺陷分析：从个案到体系——导读与开源地图]]

相关：[[工程知识/缺陷分析：从个案到体系/资源与容量/周期任务只进不出的集合：清理动作自己也要被清理]] —— 讲"完成即移除 + 空集合快路径 + 工作量守恒"的通用纪律，是本病例修复动作背后的方法面
