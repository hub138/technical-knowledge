---
title: 案例二十四：并行度被自己折叠——repartition 的 2 的幂倾斜
status: active
type: guide
updated: 2026-09-21
change_rate: low
confidence: high
review_after: 2027-03-21
tags:
  - defect-analysis
  - performance
sources:
  - "https://github.com/apache/spark/commit/18e83f9ab234e2e475842c70f191d90e4dd6e00d"
---

# 案例二十四：并行度被自己折叠——repartition 的 2 的幂倾斜

> **一句话总结**：df.repartition 的目标分区数恰好是 2 的幂时，哈希取模退化为位掩码，数据只落进少数分区——下游并行度减半、单分区负载成倍，性能缺陷里最冤的一种：**拖慢作业的是用户自己指定的那个"健康"数字**。

## 案例背景

**项目**：Apache Spark——repartition(n) 是最常用的数据重分布手段：按 key 哈希取模，把数据均匀散到 n 个分区，下游按分区并行。

"取模散列"的前提是**模数与散列值"配合得开"**。当 n 恰好是 2 的幂（128、256、1024……）时，Spark 内部对散列值的处理让取模退化成**位掩码**——只看散列值的最后几个二进制位。如果数据的 key 分布让这几个高位比特高度相关（比如 key 是递增 ID、时间戳前缀），散列值的最后几位就不再均匀——数据成片地涌进少数分区（[上游 commit 18e83f9a](https://github.com/apache/spark/commit/18e83f9ab234e2e475842c70f191d90e4dd6e00d)，P2，已真实复现）。

## 初见的两个疑问

1. **哈希散列不就是为了让数据均匀吗？** 哈希过的值怎么还会倾斜？
2. **2 的幂不是性能调优的经典数字吗？** 缓存、缓冲区全用 2 的幂，怎么到分区数就成了坑？

两个疑问的答案在"哈希之后还有一步"这个常被忽略的事实里。

## 机制：哈希之后，取模是第二次洗牌

很多人以为 repartition 的均匀性来自哈希函数本身。实际流程是两步：**第一步哈希**把任意 key 映射成一个大整数（这步确实近乎均匀）；**第二步取模** `hash % n` 决定落到哪个分区。均匀性由两步**共同**决定——第二步在 n 为 2 的幂时等价于"只保留散列值的最后 log2(n) 个二进制位"。

数学上这不改变均匀性**当且仅当**散列值的每个二进制位独立均匀。但真实数据的 key 往往有结构：连续 ID 的低位呈规律循环、时间戳的低几位随业务节拍波动。哈希函数负责把整体打散，**低位的局部规律是哈希打散不掉的**——n 为 2 的幂时低位单独决定分区，倾斜就这么发生。n 不是 2 的幂时，取模要混合所有位，局部规律被稀释，数据恢复均匀。

同一个 key 在两种 n 下的落点：

```text
hash(key) = 0x8A21C4（示意）
n=250：0x8A21C4 % 250 → 混合全部位 → 均匀
n=256：0x8A21C4 & 0xFF → 只看低 8 位（0xC4=196）→ 低位规律决定一切
```


这解释了缺陷的隐蔽性：**同一份数据，n=250 均匀，n=256 倾斜**——相邻的两个配置，一个正常一个灾难。用户怎么也想不通，自己只是把 250 调成了"更整"的 256。

## 案例关联

和 [案例七](./../兼容与升级/案例七：兼容与性能——滞后的依赖与放大的日志.md)的三个病例同族但机制不同：那三条是"该做的没做"（依赖没升级、重复登记、下推失效），这一条是"**做了一个带隐藏前提的优化**"。位掩码比取模快，这是做它的理由；隐藏前提（低位均匀）在真实数据上不成立，这是它的代价。[案例二](./../正确性/案例二：静默错算——Spark SQL Union 的别名陷阱.md)的别名删除也是同一形状：一次自以为安全的改写，前提在特定输入下不成立。**性能缺陷和安全缺陷一样，要审"优化的隐藏前提"，不是审"代码写没写错"。**

## 上游怎么修

修复落在 ShuffleExchangeExec.scala：当 n 为 2 的幂时不再走位掩码捷径，改用混合全部位的取模，让分区分配不依赖散列值的低位分布。修复同时更新了 Dataset 与 AQE 的测试套件——把"2 的幂分区数 + 结构化 key"固化为倾斜回归用例。

## 边界与教训

- **性能优化的隐藏前提要写进文档**。位掩码这个优化的前提是"散列低位均匀"，这个前提不写下来，没人知道 256 会出事。
- **"2 的幂"对缓冲区是对的，对哈希取模不一定对**——同为经典数字，适用场景不同，别互相类推。
- **倾斜的监控要看"分区内行数的最大/中位数之比"**，不是总吞吐。倾斜时总吞吐可能完全正常，比值才暴露真相。
- **数据有结构时，给哈希加盐**（比如把 key 拼一个随机后缀再散列）是应用层的自救手段——修复到来之前就能止血。

概念入口：取模与位运算的等价条件——[[工程知识/软件构建：让变化可以理解、验证与交付/算法与数据结构/算法复杂度连接规模与资源成本]]；倾斜如何破坏并行假设——[[工程知识/性能工程：从用户等待到资源瓶颈/处理器与内存/CPU性能来自有效执行与数据供给]]；调度对负载均衡的要求——[[工程知识/后端系统：在并发、失败与变化中维持服务/任务与并发/调度必须同时处理优先级、公平与背压]]。

相关：[[工程知识/缺陷分析：从个案到体系/兼容与升级/案例七：兼容与性能——滞后的依赖与放大的日志]] · [[工程知识/缺陷分析：从个案到体系/正确性/案例二：静默错算——Spark SQL Union 的别名陷阱]] · [[工程知识/缺陷分析：从个案到体系/缺陷分析：从个案到体系——导读与开源地图]]
