---
title: 案例三十二：取到了已经不存在的那个——估算器的读取竞态
status: active
type: guide
updated: 2026-09-21
change_rate: low
confidence: high
review_after: 2027-03-21
tags:
  - defect-analysis
  - concurrency
sources:
  - "https://github.com/apache/pulsar/commit/bc3ea21385ef"
  - "https://github.com/apache/pulsar/commit/50754a6c1b0f"
---

# 案例三十二：取到了已经不存在的那个——估算器的读取竞态

> **要点**：从并发容器里"先判断存在、再取值"不是原子操作；两步之间元素可能被移除，取值就会抛"没有这个元素"——检查通过不代表取得到。

并发容器提供了很多原子操作，但"检查然后取值"是两个操作。这个组合在并发下不成立，而它在代码里看起来天衣无缝。Apache Pulsar 的这个病例（已在本环境真实复现）是这条规律的一个干净样本——以及它为什么容易被误判为"不可能发生"。

## 案例背景（要解决的问题见下节）

**项目**：Apache Pulsar——分布式的消息与流处理平台，其账本条目数估算器在并发下必须返回一个数值，检查与取值之间的窗口构成本篇的问题。

`EntryCountEstimator` 用于估算一个账本或一段区间里有多少条目。这个估算用于多种目的：判断积压大小、决定是否触发滚动、估算读取成本。它不需要精确值——这是"估算"的意义——但它需要**不抛异常**。

估算器内部维护一个映射结构，记录每个账本的条目数与大小。随着账本的创建与删除，这个映射持续增减。映射由并发容器承载，支持并发的读写。

## 问题：检查与取值之间，元素消失了

上游报告的现象（[上游 commit bc3ea213](https://github.com/apache/pulsar/commit/bc3ea21385ef)，P1）是 `EntryCountEstimator` 中抛出 `NoSuchElementException`。

这个异常的来源是竞态：代码先检查某个键是否存在（或取得一个迭代器/视图），随后从中取值；两步之间，另一个线程删除了这个键。取值时发现元素不存在，容器按约定抛出 `NoSuchElementException`。

这类代码的典型形态：

```java
if (map.containsKey(key)) {
    return map.get(key);      // 两步之间元素可能被移除
}
```

或者从流/迭代器中取首个元素：

```java
return map.values().stream().findFirst().get();   // 集合可能已空
```

两段代码在无并发时完全正确，评审时也很容易放行——因为"先检查再取"是顺序编程里最基本的防护模式。**问题在于这个防护在并发下不成立：检查通过是过去的事实，取值是现在的动作，两者之间没有绑定。**

## 为什么是问题：它让估算功能整体失效

**异常发生在估算路径上，影响的是非核心功能。** 估算本身不是关键路径——消息投递不依赖它。这带来一个后果：这类问题容易被当作"偶发异常，重试就好"而忽略，但异常若发生在关键调用点（比如触发滚动的逻辑里），会导致该逻辑中断。

**它是概率性的，且概率随并发度上升。** 低负载下两步之间的窗口极小，几乎不发生；高负载下频繁出现。这让它在测试环境（低并发）完全不可见，在生产高峰暴露。

**异常类型具有误导性。** `NoSuchElementException` 听起来像"数据不存在"——一个业务意义上的空状态。排查时会去找"为什么这个账本没有记录"，而真实原因是"它有过记录，但在取值瞬间被删了"。**异常的名字把排查引向了错误的方向**。

**相关的另一条修复（[commit 50754a6c](https://github.com/apache/pulsar/commit/50754a6c1b0f)，P1）指向估算逻辑本身的边界问题。** 这与竞态是两类不同的缺陷，但都集中在同一个组件上——说明估算器这个"非关键但被多处依赖"的组件，容易同时积累多种问题。

## 本质：检查与使用的组合不是原子操作

把这段检查与使用的序列抽象出来，骨架是：

> **"先检查再使用"（check-then-act）在并发下不成立，因为检查所依据的状态在使用发生时可能已经改变；唯一可靠的做法是使用一个本身包含检查的原子操作，或者接受失败并处理它。**

这条规律的经典名称是 check-then-act 竞态，它有很多变体：

- `containsKey` 然后 `get` —— 本例的形态。
- `isEmpty` 然后 `getFirst` —— 空集取首个元素。
- `isConnected` 然后 `send` —— 检查连接状态后发送。
- `exists` 然后 `read` —— 文件存在性检查后读取（TOCTOU，安全检查里的经典问题）。

所有变体的共同结构是：**用一个过去的事实，为一个现在的动作做担保**。而并发下，过去的事实不为现在的动作担保任何事。

要强调一点：这类缺陷**不能靠"更快的检查"解决**。窗口再小也是窗口。也不能靠 `synchronized` 随手包一下解决——除非读与写两侧用同一把锁保护，否则加锁只覆盖了一侧，竞态依然存在。**修复必须让检查与使用成为一个不可分割的操作**，或者让使用本身能容忍缺失。

后一种思路在多数场景下更实用：`getOrDefault`、`computeIfAbsent`、`Optional.orElse` 这些 API 之所以更好，正是因为它们把"取不到"变成了正常返回值而不是异常。这不是"更优雅的写法"，而是**把竞态从需要避免的问题降级为可以处理的结果**。

## 比喻：看了一眼货架就去拿

超市货架上有一件商品。顾客在货架前看了一眼——在（检查通过），于是转身去拿（使用）。

就在他转身的两秒里，另一个顾客把那件商品拿走了。他伸手时货架空了。

"看了一眼"这个动作在他转身的那一刻是真实的，但它是**过去的事实**。它不能为"伸手能拿到"做担保。

有两个办法。一是**伸手就是检查**：不去"看一眼再拿"，而是直接伸手——拿到就有，拿不到就换一个。这对应 `getOrDefault` 这类原子 API，把检查与使用合并成一个动作。二是**接受可能拿不到**：伸手空了也不意外，本来就有这个可能，换了就是。

对应不上的是"看得更仔细一点"——无论看得多仔细，转身的两秒里发生的事都看不到。

## 机制：并发容器不保证组合操作的原子性

竞态窗口所在的这一个类：

```
managed-ledger/src/main/java/org/apache/bookkeeper/mledger/impl/EntryCountEstimator.java
```

映射由并发容器承载。这类容器的约定是：**单个方法调用是原子的**。`get` 是原子的，`containsKey` 是原子的，`remove` 是原子的。但它们的组合不是——容器没有任何机制知道你接下来要调用什么。

这里有一个常见的误解需要澄清：**并发容器保证的是内部结构的完整性，不是业务逻辑的原子性**。`ConcurrentHashMap` 保证 `get` 与 `remove` 交错时不会破坏内部桶结构，但它完全允许 `get` 返回一个"刚刚被 remove 的键"对应的 `null`。容器工作正常，业务语义失效。

这与 [案例十九](./案例十九：在回调里改自己——清理路径的重入与回收后复用.md) 与 [案例二十](./案例二十：关闭之后还有人在用——生命周期的时序竞态.md) 属于同一族——三者都在说：**并发原语提供的保证，与业务代码需要的保证，不是同一件事**。差别在于：案例十九是重入语义，案例二十是生命周期，本例是组合操作的原子性。

上游修复的方向是使用能容忍缺失的 API（避免"检查后取值"的两步结构），或者在取值失败时按"元素不存在"的业务语义处理，而不是让它抛出未捕获异常。

## 怎么发现：把"不可能"的异常当线索

**把 `NoSuchElementException`、`NullPointerException` 这类异常当作竞态信号，而不是数据问题。** 尤其当异常发生在"刚刚检查过存在"的路径上时，几乎可以确定是 check-then-act。这是最重要的一条——它决定了排查方向。

**在代码里搜索两步模式。** `containsKey` 后紧跟 `get`、`isEmpty` 后取元素、`isXxx` 后 `doXxx`。这些模式在并发上下文中都是嫌疑人。静态检查工具通常不报这类问题，需要靠模式匹配与人工审查。

**用并发压力测试暴露。** 让读写在同一批键上高频交错。把线程数调高、把检查与取值之间人为插入延迟（测试专用），能让窗口放大，把概率性变成必现。**在测试里故意放大竞态窗口，是发现这类缺陷最有效的手段**。

**记录异常发生时的并发上下文。** 异常日志里带上"当时有多少线程在这个结构上操作"，能帮助判断是否竞态。

**审查时对每个"检查"问一句：这个检查的结果在多长时间内有效？** 如果答案是"只在当前这一瞬间"，那么任何依赖它的后续操作都需要重新审视。

## 上游怎么修

修复在取值方式上：不再使用"检查后取值"的两步结构，改用能原子处理缺失的 API；对于确实可能缺失的情况，按业务语义返回默认值或跳过，而不是让异常向上传播。

值得注意的是修复没有加全局锁。加锁能解决问题，但会把一个高频读的路径串行化——对于估算器这种被多处调用、又允许不精确的组件，代价不划算。**选择合适的 API 比加锁更符合这里的场景**：估算允许不精确，那么"取不到就当它没有"是完全可接受的业务语义。

这提示了一个判断顺序：**先问业务能否容忍缺失，能容忍就用容忍缺失的 API；不能容忍才考虑加锁或用更强的原子结构**。跳过第一步直接加锁，是常见的过度设计。

验证的锚点：竞态窗口要与并发度扫描对账——预写并发为一时行为正常、并发提高后暴露缺失，实测单线程即失败，与预期对不上，说明另有原因。

## 边界与教训

- **check-then-act 在并发下不成立。** 过去的事实不为现在的动作担保。
- **并发容器保证结构完整，不保证业务原子。** 单个方法原子，组合不是。
- **`NoSuchElementException` 常常是竞态信号，不是数据缺失。** 排查方向不要被异常名误导。
- **优先用容忍缺失的 API，而不是先加锁。** 先问业务能否接受缺失。
- **测试里人为放大竞态窗口**，把概率性失败变成必现。
- **"检查"的有效期只有一瞬间。** 任何依赖它的后续操作都要重新审视。

概念入口：并发控制先定义共享状态与完成条件——[[工程知识/后端系统：在并发、失败与变化中维持服务/任务与并发/并发控制先定义共享状态和完成条件]]；内存模型决定并发读写可见性——[[工程知识/软件构建：让变化可以理解、验证与交付/语言与运行时/内存模型决定并发读写何时可见]]。

相关：[[工程知识/缺陷分析：从个案到体系/并发/案例三十一：回收之后还在飞——跨线程复用可回收对象]] · [[工程知识/缺陷分析：从个案到体系/并发/案例二十：关闭之后还有人在用——生命周期的时序竞态]] · [[工程知识/缺陷分析：从个案到体系/缺陷分析：从个案到体系——导读与开源地图]]

## 验证

1. 上游对照：到 Pulsar 的提交 bc3ea213 里核对修复方式，确认检查与取值被合并成一个原子操作。
2. 机制复现：让一个线程在映射中查询某账本的条目数，同时另一个线程删除该账本，提高并发度后修复前抛出 `NoSuchElementException`、修复后返回明确的缺失结果。
3. 边界核验：把并发度降到单线程时行为正确，确认症状只在检查与取值之间存在并发窗口时出现；核对 `containsKey` 加 `get` 与从流里 `findFirst().get()` 两种写法是否都存在同一问题。
4. 调用点核验：检查估算结果被哪些逻辑消费，确认异常发生在触发滚动的调用点时会中断该逻辑。
5. 断言锚点：复现脚本固定并发窗口、异常类型与调用位置有记录、修复后同场景不再抛出。

