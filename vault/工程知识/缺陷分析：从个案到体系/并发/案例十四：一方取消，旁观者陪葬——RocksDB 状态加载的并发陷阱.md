---
title: 案例十四：一方取消，旁观者陪葬——RocksDB 状态加载的并发陷阱
status: active
type: guide
updated: 2026-09-20
change_rate: low
confidence: high
review_after: 2027-03-20
tags:
  - defect-analysis
  - concurrency
sources:
  - "https://github.com/apache/spark/commit/f80559fc4605b3f3d9e8d7631a9a5c9e1439efe6"
---

# 案例十四：一方取消，旁观者陪葬——RocksDB 状态加载的并发陷阱

> **一句话总结**：两个任务并发加载同一个 RocksDB 状态存储，一方被取消时把共享的加载状态搅乱，另一方因线程状态异常跟着失败——取消操作也需要并发安全。

## 案例背景

**项目**：Apache Spark——其 Structured Streaming 用 RocksDB 做流式状态存储。

状态存储的首次加载是一个较重的初始化过程，Spark 允许多个任务并发触发它，共享加载结果。缺陷在于：当并发调用者中**一方被取消或中断**时，取消的清理动作污染了共享的加载状态，另一个无辜的调用者因线程状态异常直接失败（[上游 commit f80559fc](https://github.com/apache/spark/commit/f80559fc4605b3f3d9e8d7631a9a5c9e1439efe6)，P1，并发类，已真实复现）。

## 机制：取消也是一次并发写

大多数开发者把"取消"理解为"那个任务自己退出"。从并发视角看，取消是一次**对共享状态的写操作**：中断标志、线程状态、清理回调，全都在其他线程还在读取的状态空间里发生。加载流程走到一半，同伴的取消打断了它依赖的执行环境——失败的不是被取消的那个，而是还在正常执行的这一个。

这也是并发缺陷里最反直觉的一种：**受害者与肇事者是两个不同的任务**。排查时顺着失败者的堆栈走，看到的全是无辜代码；真正的时序交错发生在另一个人已经消失的执行流里。

## 案例关联

和 [案例三](./../并发/案例三：并发两面——写回调卡死与心跳竞态.md)的两个病例并排放着看，并发缺陷的出口集齐了三种：BookKeeper 客户端是**卡死**——等一个永远不会来的完成回调；Spark 心跳竞态是**崩溃**——计数被覆盖后踩中断言；这一条是**连坐**——一方的取消污染共享状态，旁观者陪葬。出口不同，不变量相同：**多个执行流交错的顺序是代码没有显式处理的隐藏输入。** 修复方向也一致——把"谁先谁后"从运气变成设计。

## 上游怎么修

修复的思路是给共享加载加**等待语义**：第二个调用者不再与取消动作并发竞争，而是等待第一个加载完成（或失败）后再行动。取消依旧合法，只是它的副作用被约束在自己身上，不再外溢给旁观者。

**代价是明确的**：等待语义让第二个调用者的启动延迟上升到与首次加载同量级——原先它是并发进入、可能更早拿到结果。这个取舍换来的是可靠性：流式作业里，一次状态加载失败意味着整个查询重启，代价远高于多等一次初始化。上游选择用延迟换确定性。

## 复现与验证

这一例已在本地环境真实复现，证据等级 L3。验证的关键是**控制时序**：让两个任务并发触发同一状态存储的首次加载，在其中一方进入加载中途时施加取消，观察另一方的结局。修复前，旁观者以线程状态异常失败，且失败堆栈不指向取消方；修复后，旁观者正常等到加载完成并拿到一致的状态。

复现的最小骨架如下，关键是让取消落在加载窗口内：

```scala
// 两个任务并发首次加载同一状态存储
val t1 = future { stateStore.load() }          // 进入加载中
val t2 = future { stateStore.load() }          // 共享同一份加载状态
t1.cancel()                                    // 取消落在加载中途
t2.await()                                     // 修复前：线程状态异常失败
```

回归用例要固定这个时序，而不是只测"两个任务并发加载"——后者在修复前后都可能通过，因为缺陷只在取消恰好落在加载窗口内时才触发。

## 边界与教训

- **取消路径要按并发代码审查**，而不是当成人畜无害的清理。它会读写共享状态，它也有自己的时序。
- **失败者的堆栈不指向肇事者**。并发连坐的归因要靠时序重构，不能只看失败现场。
- **"共享初始化"是并发问题的高发地**：谁触发、谁等待、一方失败时其他人怎么办——这三个问题在写第一行加载代码之前就该有答案。

概念入口：并发可见性由什么决定——[[工程知识/软件构建：让变化可以理解、验证与交付/语言与运行时/内存模型决定并发读写何时可见]]；设计并发逻辑前先回答哪两个问题——[[工程知识/后端系统：在并发、失败与变化中维持服务/任务与并发/并发控制先定义共享状态和完成条件]]。

相关：[[工程知识/缺陷分析：从个案到体系/并发/案例三：并发两面——写回调卡死与心跳竞态]] · [[工程知识/缺陷分析：从个案到体系/稳定性/案例十二：调度线程的永久等待——广播写锁不释放]] · [[工程知识/缺陷分析：从个案到体系/缺陷分析：从个案到体系——导读与开源地图]]
