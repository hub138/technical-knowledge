---
title: 空间回收要有对账表：引用登记让删除不漏不冤
status: active
type: pattern
updated: 2026-09-23
change_rate: low
confidence: high
review_after: 2029-09-23
tags:
  - hw
  - operations
  - defect-analysis
sources:
  - "https://bookkeeper.apache.org/"
  - "https://www.postgresql.org/docs/current/routine-vacuuming.html"
---

# 空间回收要有对账表：引用登记让删除不漏不冤

> **一句话总结**：存储的空间回收（GC）依赖"引用登记 → 活性判定 → 扫描删除 → 对账"四步——引用计数或登记表回答"这块数据还有人要吗"，扫描器只删"登记说不活跃"的；判定的错差有两个方向：漏删（空间泄漏，磁盘缓慢膨胀）与误删（活数据被回收，等价于数据丢失），对账表把两个方向的错差都变成可发现的数字。

## 要解决的问题

空间回收的判定错误是存储系统的隐性问题：**漏删**（已无引用的文件/版本没人回收，磁盘水位只涨不跌，排障时发现"很多文件不知道是谁的"）；**误删**（活性判定错差，引用还在的数据被当垃圾回收——最严重的一类数据丢失）。上游 BookKeeper 的复现记录里有一条"pendingDeletedLedgers 不移除 ledger 导致错误"（删除的中间状态管理出错：删除进行中的条目卡在登记表里，既没删净也没法重试）——**回收的中间状态与登记表的一致性是 GC 的真正难点**。本篇展开：**引用登记的形态、活性判定的窗口设计、对账的指标**。

## 机制：登记、判定、扫描与对账

**引用登记的两种形态**：引用计数（每个数据块的引用数，增删引用时同步更新——实时性好，计数错差不可恢复）、登记表（独立维护"谁引用什么"的清单，扫描器按清单判定——判定可重算，登记与数据的偏差可对账）。**长生命周期存储（日志段、SST、对象）常用登记表**：引用方（索引文件、manifest、活跃会话）登记在案，扫描器以登记表为权威——**计数型 GC 在崩溃恢复时计数可能错**，登记表可以按"重新扫描引用方"重建。

**活性判定的窗口设计**：删除不是瞬时的——业务标记删除（逻辑删）→ 等待窗口（在途读与副本同步完成）→ 物理回收。**窗口的作用是防"删除竞态"**：读方刚拿到引用、写方标记删除、GC 立即回收——读失败。窗口时长按最坏在途时间定（读超时 + 副本同步时间），**窗口内的条目状态机**（pending-delete → deleted）要持久化：上游"pendingDeletedLedgers 不移除"的缺陷正是这个状态机的实现错误——中间状态的条目被错误处理，删除流程卡住。

**扫描删除的断点续跑**：扫描器按批次处理（扫一批、判一批、删一批），每批的进度持久化——中断后从断点继续，不重复不遗漏；**扫描的限速**与业务 IO 共享带宽（上一批数据修复篇的限速原则同源）。

**对账的两个指标**：应删未删（登记表标记已删但文件还在——泄漏量）、应存被删（业务引用存在但文件缺失——丢失量，P0 级告警）——**对账任务按天跑**，两个指标进趋势监控：泄漏量持续增长说明扫描器或状态机有 bug，丢失量非零直接进入数据修复分级流程。

## 看完能判断：GC 的失效模式与防御

**三类失效模式**：状态机错误（中间状态卡住或跳变，上游复现的 pendingDeletedLedgers 类）、登记与数据不同步（登记更新了数据没删/数据删了登记没更新——崩溃窗口内两个动作不原子）、扫描器误判（活性判据错：把"暂时无引用"当"永久无引用"）——**防御的共同点是"判定可重算"**：登记表可重建、扫描可重跑、删除可重试，任何一步出错都能从另一步纠正。

**误删的最后防线：回收延迟与软删除**：物理删除前数据进"回收站"状态（保留 N 天才真正抹除）——**误删在 N 天内可恢复**，代价是空间回收延迟 N 天；按存储价值分层配置（热数据长保留、临时区零保留）。

**GC 与备份的关系**：GC 删除的数据在备份里还有吗——**备份链与 GC 的交互要显式设计**（备份周期长于 GC 窗口时，备份里存在已被业务删除的数据，恢复时多出来的数据要不要、过期的怎么处理），两个系统的保留策略对齐评审。

## 边界

三条边界防止防线走形。

**引用计数不用于长生命周期存储**：计数型 GC 适合进程内短生命周期对象（内存管理），跨进程、跨机器的存储引用用登记表——**计数的错差不可恢复**（漏计一次引用就永久泄漏或误删），登记表可重建。

**GC 窗口与读超时的联动**：删除窗口短于业务读超时，窗口保护失效（读方超时重试时数据已被回收）——**窗口时长 ≥ 读超时 + 副本同步**，两个参数在配置里显式联动。

**扫描器自身要有监控**：扫描进度、批次速率、异常退出次数——扫描器静默失败时泄漏量无声增长（对账指标发现时已经堆了很久），**扫描器的存活与进度本身是监控对象**。

## 验证

可复现的验证路径：

1. 漏删复现：制造登记与数据不同步（手工建孤儿文件），预期对账任务按天检出、泄漏量入趋势图；
2. 竞态复现：读方持有引用的同时触发删除 + GC，预期窗口保护下读不失败；缩短窗口后复现读失败——验证窗口时长公式的正确性；
3. 断点续跑验证：扫描中断（kill）后重启，预期从断点继续、对账无缺口不重复；
4. 断言锚点：**登记表为判定权威且可重建、中间状态持久化、应删/应存两个对账指标有监控与告警、软删除保留期按层配置**——四件齐备，空间回收从"后台跑着的黑盒"变成"对账数字可查的工程流程"。

```text
登记断言 = 引用清单可重建可重算
窗口断言 = 删除窗口覆盖最坏在途
对账断言 = 泄漏与丢失两个指标在管
```

空间回收的可靠性不在 GC 算法本身，在它周围的三件套：可重建的登记表、覆盖在途的删除窗口、双方向的对账指标——三件配齐，"磁盘怎么越用越满"和"数据怎么没了"两个悬案都变成有数字可查的对账问题。

## 相关

- [[工程知识/数据系统：在并发与故障中保存事实/可靠性与运维/数据修复要按失效半径分层：行级订正、分区重建、全量重导.md]] —— 对账发现丢失后的修复流程
- [[工程知识/数据系统：在并发与故障中保存事实/事务与存储/LSM树用写放大换取顺序写和可扩展性.md]] —— compaction 即 LSM 的空间回收
- [[工程知识/性能工程：从用户等待到资源瓶颈/存储与网络/临时文件要先写进命名约定：可清理性是写入路径的设计要求.md]] —— 临时文件清理与 GC 同属空间治理，共享命名与登记原则
- [[工程知识/数据系统：在并发与故障中保存事实/可靠性与运维/存储的静默损坏防线：校验和、副本比对与定期扫描各拦一层.md]] —— 扫描体系的一致性与回收共用限速原则
