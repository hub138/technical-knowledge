---
title: 投毒不是写错，是写给你看——依赖投毒的三个真实剧本
type: guide
status: active
updated: 2026-09-22
review_after: 2027-09-20
change_rate: low
confidence: high
tags:
  - software/dependencies
  - security
  - supply-chain
  - defect-analysis
sources:
  - "https://nvd.nist.gov/vuln/detail/CVE-2024-3094"
  - "https://www.openwall.com/lists/oss-security/2024/03/30/4"
  - "https://github.com/advisories"
---

# 案例四十四：投毒不是写错，是写给你看——依赖投毒的三个真实剧本

> **要点**：案例五讲的是"依赖带伤"（被动），这篇讲的是"依赖被人下了毒"（主动）。被动漏洞等你升级就关闭，主动投毒不等你做错任何事——攻击面不在你的代码里，在你拉包的那条链路上。三个公开真实剧本（PyPI typosquat、GitHub Actions 投毒、xz 后门）给出三种不同的攻击面与对应的防线。

## 要解决的问题：为什么被动漏洞与主动投毒必须分开学

案例五里带 CVE 的依赖与主动投毒有一个根本区别：**时间维度反转**。被动漏洞是"已存在的伤口"，扫描器能看见它，升级能关闭它，漏洞公开到被利用之间的时间差是你买的保险。主动投毒反过来：**它在你的扫描器看见它之前就在等你拉取**，你拉一次就中一次，零交互、零利用成本。

这个区别决定了防线不同。防被动漏洞靠"例行扫描+升级纪律"，防主动投毒靠**拉取环节的来源验证**——而这两个防线在你的依赖治理里通常混在一起，前者有扫描器，后者常常是裸奔。

## 病例一：typosquat 与抢注包（P1）

### 病例

2023 年 PyPI 出现大量与热门包一字之差的抢注包（如 `colourfool` 冒充 `colourfool` 之外的更多真实案例包括 `request` 冒充 `requests`、`crypt` 冒充 `crypto` 系列）。开发者打错一个字母、`pip install` 随即执行投毒脚本。这个剧本的攻击面是**人类输入的不可靠性**：包管理器不做包名的模糊匹配，注册门槛低，抢注几乎零成本。漏洞窗口以小时计：抢注包存活到被 PyPI 下架为止。

### 机制

typosquat 的杀伤链条：注册与热门包一字之差的包名 → 开发者手滑装上 → install 钩子执行任意代码 → 窃取凭证/挖矿/横向移动。三个环节每个都不高明，合在一起形成一条**零交互攻击链**。

### 教训

- **锁定精确包名**：requirements.txt / package.json 里的包名是唯一真源，人的输入不可信。CI 里直接引用 lock 文件（pip freeze / package-lock.json），不用人手敲包名。
- **安装前验证内容**：pinned hash（`pip install --require-hashes`、npm 的 integrity 字段）让"包名对但内容被人换过"直接失败。hash 锁下 typosquat 无处藏身：包名错了装不上，包名对但内容换过 hash 对不上，两条路都被封闭。

## 病例二：构建基础设施投毒（P1）

### 病例

近年 GitHub Actions 生态出现三种公开的投毒剧本：
1. **action 仓库被夺舍**：热门 action 的维护者账号被盗（或放弃维护被接管），旧 tag 被改指向恶意 commit。你的 workflow 引用 `action@v1`，拉到的是被篡改的 v1。不可变引用保护不了你——tag 是可变的，它只是指针。
2. **self-hosted runner 投毒**：投毒者向公共仓库提恶意 PR，触发配置不当的 self-hosted runner 执行任意代码。这个剧本的攻击面是**基础设施信任的错配**：你以为 runner 是可信执行环境，它实际暴露着你的 CI 凭证（GITHUB_TOKEN、缓存凭证、自签证书）。
3. **tj-actions/changed-files 事件（2025）**：攻击者取得维护者 token 后改写了历史 tag，所有引用旧 tag 的仓库在下次 CI 运行时拉到恶意版本，泄漏的凭证从 runner 传播到所有下游仓库。

### 机制

构建基础设施投毒的共同病根：**CI 凭证的高权限与 action 来源验证的缺失形成攻击闭环**。runner 持有的 GITHUB_TOKEN 能写仓库、能拉私有镜像；action 的引用基于 tag/sha，tag 可变、sha 引用很少人遵守。攻击者只要改一个 tag，成千上万个下游仓库的 CI 在下一次运行中变成恶意代码的执行环境——**投毒的成本是一次 commit，传播的代价由所有下游承担**。

### 教训

- **引用 commit SHA 而非 tag**：`actions/checkout@<full-sha>`，sha 不可变，tag 改写攻击对 sha 引用无效。
- **最小化 runner 凭证**：GITHUB_TOKEN 权限最小化（只读仓够用就别给写权限）、per-job 的 OIDC 短期凭证替代长效 PAT。
- **runner 隔离**：self-hosted runner 不跑公共仓库的 PR（公共 PR 可以提取凭证），或者至少凭证隔离 + 一次性环境（每次 job 后销毁容器）。
- **审计 workflow 日志里的出站请求**：凭证外传必有网络痕迹，对 CI 子网做出站监控是事后的最后防线。

## 病例三：xz 后门（P0）

### 病例

2024 年 3 月，xz 5.6.0/5.6.1 被发现携带后门（CVE-2024-3094）。投毒者以 "Jia Tan" 身份潜伏两年多：先通过持续贡献建立信任、成为共同维护者、接手 release 权限，然后在自己的构建机上产出带后门的发布 tarball（后门不在上游 git 仓库里），后门在特定条件下（systemd 系统、链接了 liblzma 的 sshd 路径）能让攻击者远程执行命令。被 Andres Freund 在排查 sshd 性能异常时偶然发现，距离进入 Debian unstable 只差几周。

### 机制

xz 后门是依赖投毒的"完全体"，它演示了两个新东西：

1. **社会工程学的时间尺度**。攻击者没有攻破任何系统，他"成为"了系统的一部分——两年多的时间里贡献代码、建立信任、获取 release 权限。你的依赖治理里没有任何防线针对"维护者本身就是攻击者"这个场景。
2. **构建产物的来源欺骗**。后门不在 git 仓库里（上游 commit 历史干净），只在发布 tarball 里（构建脚本在维护者本机注入）。"从源码构建"这个防线要求"源码"与"产物"来自同一来源，而这里发布产物与公开源码在构建环节被分离。

### 教训

- **可复现构建（reproducible builds）**是唯一硬防线：任何人能从同一源码构建出 bit-for-bit 相同的产物，则"构建者私自注入"在数学上无处藏身。xz 事件中发布 tarball 与官方构建产物的差异正是暴露线索之一。
- **警惕维护者变更**：一个项目突然被无名新维护者接手、release 节奏突变、贡献者集中度异常（一人包办全部关键 commit），都是社会工程学的信号。OpenSSF scorecards 把这些纳入评分。
- **最小化安装面**：xz 这种"无处不在的基础库"被后门意味着几亿设备的潜在暴露。基础库的依赖治理要升格：不只是"我们用了什么"，还要"我们的基础库被谁维护、维护者怎么产生的、release 怎么构建的"。

## 防线总结：拉取环节的来源验证矩阵

| 剧本 | 攻击面 | 防线 |
| --- | --- | --- |
| typosquat | 包名输入错误 | lock 文件锁定精确包名 + pinned hash |
| action tag 改写 | tag 指针可变 | 引用 commit SHA |
| runner 凭证外泄 | CI 凭证高权限 | 最小权限 token + OIDC 短期凭证 + 一次性 runner |
| 维护者夺舍 | 社会工程学 | 维护者变更审计 + scorecards |
| 构建产物分离 | tarball 与源码不一致 | 可复现构建 |

## 边界

- 与案例五互为反面：案例五是"依赖带伤"（被动、可扫描、可升级关闭），本案例是"依赖被下毒"（主动、拉取即中、来源验证是唯一防线）。
- 与案例二十六正交：案例二十六讲权限模型内的越权（门禁内绕行），本案例讲权限模型外的人（维护者本身就是攻击者）。
- 软件构建域的理论篇（见 [[工程知识/软件构建：让变化可以理解、验证与交付/开发工具/依赖供应链需要锁定来源与构建输入]]）已给出锁源框架；本案例补上"为什么"的三个公开实例与每种的触发条件。
- 三个剧本都基于公开事件（PyPI typosquat、GitHub Actions 事件、xz 后门），内部细节以公开报告为准，不引入任何非公开案例。

## 验证

最小验证：在你自己的仓库跑一次"来源验证三件套"——① lock 文件里的包名与官方注册名逐一比对（找 typosquat）；② GitHub Actions 引用里 tag 引用改 full commit SHA（找 tag 可变风险）；③ 挑一个你依赖的基础库，读它最近一次 release 的维护者与构建流程（找维护者变更信号）。三步全过说明你的拉取面至少对这三类剧本有了最低防线。

关系：[[工程知识/缺陷分析：从个案到体系/安全/案例五：安全两例——认证绕过与带漏洞依赖]] · [[工程知识/缺陷分析：从个案到体系/安全/案例二十六：公告栏没查门禁——Redis 的 ACL 键名泄露与解析崩溃]] · [[工程知识/软件构建：让变化可以理解、验证与交付/开发工具/依赖供应链需要锁定来源与构建输入]] · [[工程知识/软件构建：让变化可以理解、验证与交付/开发工具/维护者信任是供应链信任的地基：贡献者治理与维护者变更审计]] · [[工程知识/缺陷分析：从个案到体系/兼容与升级/案例四十三：升级把前提带走了——依赖行为变更引发的下游失效]]
