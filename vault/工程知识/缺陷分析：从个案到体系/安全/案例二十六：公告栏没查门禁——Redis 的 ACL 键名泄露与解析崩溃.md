---
title: 案例二十六：公告栏没查门禁——Redis 的 ACL 键名泄露与解析崩溃
status: active
type: guide
updated: 2026-09-21
change_rate: low
confidence: high
review_after: 2027-09-21
tags:
  - defect-analysis
  - security
sources:
  - "https://github.com/redis/redis/commit/3869512c920d4e865b54384bce5fcb6a4e06ae0d"
  - "https://github.com/redis/redis/commit/4a95b3005a140165bbb9df373ba61f775c936554"
---

# 案例二十六：公告栏没查门禁——Redis 的 ACL 键名泄露与解析崩溃

> **一句话总结**：Redis 的两个 ACL 病例——客户端缓存广播失效通知时没按权限过滤，低权限账号收到了无权读取的键名（元数据泄露）；ACL SETUSER 收下裸 `%` 模式后，一条查询命令能让整个实例崩溃。门禁没坏，坏的是没走门禁的那条公告栏。

## 案例背景

**项目**：Redis——内存键值存储，ACL（Access Control List）负责"谁能对哪些键做什么"。

两个病例同属 ACL 模块（[上游 commit 3869512c](https://github.com/redis/redis/commit/3869512c920d4e865b54384bce5fcb6a4e06ae0d) 与 [上游 commit 4a95b300](https://github.com/redis/redis/commit/4a95b3005a140165bbb9df373ba61f775c936554)，均 P0，均已真实复现），但坏的位置完全不同——这个对比本身就是本篇的教学价值。

## 初见的三个疑问

1. **"收到自己无权访问的键名"——能收到，难道不是能访问吗？** "收到"这个词很怪。
2. **ACL 被绕过了？** 权限系统被绕过是大事，修复为什么只改了推送函数？
3. **一个查询命令怎么能弄崩整个实例？** ACL LIST 只是读配置，读能崩？

## 比喻：门禁与公告栏

把 Redis 想成一栋公寓：键是房间，键值是房间里的东西，**键名是门牌号**，ACL 是门禁卡，服务端是物业。

受限账号的门禁卡只能刷开 A 栋。**门禁是好使的**——它去刷 B 栋 202，照样拒绝。但物业有个大屏公告栏，滚动播放"某房间有变动"。推送失效通知的代码往大屏上贴公告之前，**没有查一遍"看到这条公告的人有没有资格知道这个房间"**。于是低权限账号在公告栏上看到了 B 栋 202 这五个字。

结果：它**进不去** B 栋 202（门禁还在拦，GET 返回 NOPERM），但**它知道了这栋楼里有这么个房间**。泄露的是门牌号，不是房间里的东西——**元数据泄露，不是越权访问**。

## 机制：推送路径上没有"当事人"

病例一的根因（[上游 commit 3869512c](https://github.com/redis/redis/commit/3869512c920d4e865b54384bce5fcb6a4e06ae0d)）：命令路径上有"当前用户是谁"（每个命令都过 ACL 检查），但**客户端缓存广播失效通知时，推送路径上根本没有"当事人"这个上下文**——代码只排除了发起修改的那个连接，没想过还要按订阅者的权限逐键过滤。修复把订阅者身份显式传进推送函数，逐键过滤。

病例二（[上游 commit 4a95b300](https://github.com/redis/redis/commit/4a95b3005a140165bbb9df373ba61f775c936554)）更简单：ACL SETUSER 收下一个裸 `%`（权限模式语法错了）时，解析缺少权限位校验——错误配置被存了进去。之后任何一次 ACL LIST / GETUSER / SAVE 触发解析，实例 panic，**全部客户端断连**。修复是拒绝非法模式并返回明确错误。

两条病例的对照放在一起看：

```text
病例一（键名泄露）：
  订阅者权限 = 只读 A 栋
  推送广播 = "B栋202 有变动"
  结果：订阅者看见了 B 栋202 的门牌号 → 元数据泄露

病例二（解析崩溃）：
  ACL SETUSER demo %     ← 裸 %，权限位缺失
  结果：错误配置入库 → 下一次 ACL LIST 触发解析 → 实例 panic

修复：病例一补逐键过滤；病例二当场拒绝非法模式
```

这两条给运维出的难题是：泄露的那条**没有任何报错**——门禁在正常工作，告警无事可做；崩溃的那条**倒下得毫无征兆**——一条只读命令，事后日志里也找不到"为什么是它"。要解决的问题因此很明确：把"权限在主路径校验过"升级为"每条接触数据的路径都校验"，把"非法输入"在写入的第一秒拒绝。修复的收益也在这里：前者保住共享实例的隔离边界，后者消除一颗定时炸弹。

## 案例关联

两条合在一起看，ACL 病例的分布规律是：**主路径上的检查最完善，围绕主路径的旁支（推送、解析、展示）逐层失守**。推送路径缺过滤是"该调的没调"，解析缺校验是"该拒的没收"——[案例五](./../安全/案例五：安全两例——认证绕过与带漏洞依赖.md)的认证绕过也是"机制在但没被调到"。安全缺陷的三连问由此成立：**检查在哪？谁没调它？哪条路径绕开了它？**

## 上游怎么修

- 病例一：推送函数签名增加订阅者身份，失效通知按订阅者当前的 ACL 权限逐键过滤；身份变更（re-AUTH / SETUSER）前后分别按新旧身份冲刷，避免边界遗漏。
- 病例二：ACL 模式解析增加权限位校验，裸 `%` 在 SETUSER 当场被拒绝。

## 边界与教训

- **"推送 / 订阅 / 回调 / 广播"这些字眼出现时，条件反射问一句**：推送内容按接收方的权限过滤了吗？命令路径有 ACL 不代表推送路径有。
- **泄露分级要分清**：键名泄露不致命（值读不到），但键名本身就是情报（`user:idcard:xxx` 这类命名），且可被反复枚举——该修。
- **解析层的输入校验是第一道门**：非法输入在写入时拒绝，比让它潜伏到某次读取时崩溃便宜一百倍。
- **审安全修复时，看签名变化**：推送函数多了一个身份参数，往往就是"补当事人"的痕迹。

概念入口：认证与授权的分工——[[工程知识/后端系统：在并发、失败与变化中维持服务/服务设计/认证授权决定请求能看什么和做什么]]；泄露分级与最小权限——[[工程知识/缺陷分析：从个案到体系/安全/案例五：安全两例——认证绕过与带漏洞依赖]]。

相关：[[工程知识/缺陷分析：从个案到体系/安全/案例五：安全两例——认证绕过与带漏洞依赖]] · [[工程知识/缺陷分析：从个案到体系/缺陷分析的本质——期望与现实的偏差]] · [[工程知识/缺陷分析：从个案到体系/缺陷分析：从个案到体系——导读与开源地图]]
