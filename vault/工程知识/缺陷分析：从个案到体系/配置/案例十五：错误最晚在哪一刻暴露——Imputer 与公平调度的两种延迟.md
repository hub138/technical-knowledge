---
title: 案例十五：错误最晚在哪一刻暴露——Imputer 与公平调度的两种延迟
status: active
type: guide
updated: 2026-09-20
change_rate: low
confidence: high
review_after: 2027-03-20
tags:
  - defect-analysis
  - configuration
sources:
  - "https://github.com/apache/spark/commit/2b54b48cd852f93e8cf24397df6f3ec5b755233e"
  - "https://github.com/apache/spark/commit/35023e81e0b0d1c64e2db0b1a3ba30c29e1fbd4c"
---

# 案例十五：错误最晚在哪一刻暴露——Imputer 与公平调度的两种延迟

> **一句话总结**：配置类缺陷的两种延迟形态——错误延迟暴露（拟合通过、保存才炸）与配置静默不生效（启用了等于没启用）——共享同一条不变量：配置的价值在四级链路贯通的那一刻才存在，断在哪一级都等于没配。

## 案例背景

**项目**：Apache Spark——其 ML 库与 YARN 资源调度是配置问题的高发地。

[案例四](./../配置/案例四：配置不生效——recover 速率与重复日志.md)讲过"声明—解析—传递—执行"四级链路：断在哪一级，配置都等于没配。这一篇补两个 Spark 的变体（均已真实复现），它们展示了同一条不变量的另外两种断裂方式。

两个变体要解决的**同一个难题**是：配置的错误如果没有在配置当下暴露，就会沿着流水线向下漂移，等到暴露时，用户已经为这个错误付出了完整的计算代价。识别延迟暴露的形态，才能把校验点前移到代价最低的位置。

## 两种延迟

**错误延迟暴露**（[上游 commit 2b54b48c](https://github.com/apache/spark/commit/2b54b48cd852f93e8cf24397df6f3ec5b755233e)，P2）。Spark ML 的 Imputer 在输入列集合为空时不做校验，拟合阶段一切正常，直到**保存模型**时才因空 schema 失败。同一个错误，暴露时刻从"配置时"推迟到了"训练完成后"——用户要重新配置列，再把整轮训练重跑一遍。

**配置静默不生效**（[上游 commit 35023e81](https://github.com/apache/spark/commit/35023e81e0b0d1c64e2db0b1a3ba30c29e1fbd4c)，P2）。用户启用应用内公平调度，却不提供调度池配置文件——作业实际仍按先到先服务排队，多个作业无法按权重共享资源。配置被接受了、被记住了，就是**没有产生任何效果**，也没有任何提示。

## 最小对照

延迟暴露的最小复现只需三行：

```python
imputer = Imputer(inputCols=[], outputCols=[])   # 空列集合，配置阶段不报错
model = imputer.fit(df)                          # 拟合通过
model.save(path)                                 # 直到这里才失败
```

把 `inputCols` 留空是用户的配置错误，但错误的第一现场在 `save`，此时整轮训练已经跑完。

静默不生效那一例的对照是配置与观测的对照：

```bash
spark-submit --conf spark.scheduler.mode=FAIR app.jar   # 未提供 fairscheduler.xml
```

配置被接受，作业按 FIFO 排队。验证方法是看调度池视图里是否出现多个权重池——没有出现，就说明这条配置没有贯通。

## 案例关联

和 [案例四](./../配置/案例四：配置不生效——recover 速率与重复日志.md)的主病例（recover 限速参数在传递链路上丢失）并排放着看，不变量完全一致：**配置的价值在"声明—解析—传递—执行"四级链路贯通的那一刻才存在，断在哪一级、或验证缺位，都等于没配。** 差别只在断裂的位置和暴露的时机——Imputer 断在"校验缺位"，公平调度断在"传递不达"，recover 断在"参数传递"。而错误暴露得越晚，重跑的代价越大：配置时发现改一行参数，训练完才发现要重跑整轮训练。

## 上游怎么修

两条修复方向一致——**把错误推到最早能发现的时刻**。Imputer 的修复在拟合入口加参数校验，空输入列当场给出明确错误；公平调度的修复让缺省配置真正生效，而不是静默退回旧行为。前者是"启动时校验"，后者是"缺省即正确"，都是把验证前移。

## 边界与教训

- **每个配置项都值得问一句**：如果这个值是空的/错的/缺失的，系统会在哪一步、以什么方式发现？答不出"最早的那一步"，就是潜在的延迟暴露。
- **静默退回默认行为比报错更危险**——用户以为配置生效了，系统按另一套规则在跑，两边还都对得上日志。
- **校验前移的成本极低**：一个 if 加一条明确报错，换来的是"配置错误活不过第一秒"。

概念入口：配置与可操作性的行为对齐逻辑已在 [案例四](./../配置/案例四：配置不生效——recover 速率与重复日志.md)文内讲清；启动自检的价值见 [[工程知识/缺陷分析：从个案到体系/缺陷分析五步法——从个案到通用模型]]。

相关：[[工程知识/缺陷分析：从个案到体系/配置/案例四：配置不生效——recover 速率与重复日志]] · [[工程知识/缺陷分析：从个案到体系/缺陷分析五步法——从个案到通用模型]] · [[工程知识/缺陷分析：从个案到体系/缺陷分析：从个案到体系——导读与开源地图]]
