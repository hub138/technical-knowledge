---
title: 没有写下来的版本——默认值里的迁移债
status: active
type: guide
updated: 2026-09-21
change_rate: low
confidence: high
review_after: 2027-03-21
tags:
  - backend/config
  - defect-analysis
sources:
  - "https://github.com/apache/bookkeeper/commit/b00d2e0cd4a1"
  - "https://github.com/apache/bookkeeper/commit/697ff5702394"
---

# 案例三十八：没有写下来的版本——默认值里的迁移债

> **要点**：数据格式的版本如果不显式写入存储，读的时候就只能靠默认值猜——猜对了能跑，猜错了要么拒绝打开、要么静默地按错误格式解读。

一个数据文件被写下来时，除了内容本身，还有一件事必须一起写下来：**它是用什么格式写的**。这件事听起来理所当然，但它经常被漏掉——因为在最初的版本里，只有一种格式，不需要区分。Apache BookKeeper 的这组病例（已在本环境真实复现）说明了漏掉它的代价，以及为什么这类缺陷总是在很久以后才爆发。

## 案例背景（要解决的问题见下节）

**项目**：Apache BookKeeper——为日志条目提供低延迟、可持久化写入的分布式日志存储服务，其元数据用 RocksDB 存储，数据库格式版本是否被显式写入构成本篇的问题。

BookKeeper 用 RocksDB 存储账本元数据。RocksDB 有自己的数据格式，由 `format_version` 控制：不同版本使用不同的 SST 文件结构，高版本 RocksDB 可以读低版本格式，反过来不行（低版本读高版本会拒绝打开）。

这个版本号应当被**显式写入数据库**。它的作用不是给 RocksDB 自己看，而是给"将来某个不知道当时使用了什么配置的人"看。

## 问题：版本号没有被记录下来

上游报告的现象（[上游 commit b00d2e0cd4](https://github.com/apache/bookkeeper/commit/b00d2e0cd4a1)，P1）是：并非所有 RocksDB 实例都指定了 `formatVersion`。

相关的另一条（[commit 697ff570](https://github.com/apache/bookkeeper/commit/697ff5702394)，P1）把默认 `format_version` 设为 5。

这两条合起来暴露了一个完整的问题链：

**第一环：版本未显式指定时，实际使用的版本由 RocksDB 库自身的默认值决定。** 这个默认值随 RocksDB 版本变化——换一个 RocksDB 依赖，写出的文件版本就变了，而 BookKeeper 侧完全没有记录这件事。

**第二环：换环境时，读方与写方的假设不一致。** 在一台机器上用高版本 RocksDB 写出的数据库，复制到另一台装着低版本 RocksDB 的机器上，会直接拒绝打开。这是一个"能明确报错"的失败，算是幸运的情形。

**第三环：真正危险的是静默情形。** 如果格式差异不改变文件能否打开，而只改变某些结构的解释方式，那么读方会正常打开、正常返回，只是返回的内容是错的。这类失败没有信号。

## 为什么是问题：它跨越时间与机器

**缺陷的引入与爆发相隔很远。** 写出文件的那一刻，一切正常——文件能被读、能被用。问题在数月后的一次迁移、一次升级、一次跨机器复制中爆发。到那时，写文件的代码可能已经改过好几轮，没人记得当时的默认配置是什么。

**现场缺失关键信息。** 排查时最需要知道"这个文件是用什么格式写的"，而这恰恰是没被记录下来的那一项。只能从间接证据推断：文件的二进制特征、写入时间、当时的部署版本。这让排查成本极高。

**它在测试环境不可复现。** 测试环境的 RocksDB 版本固定，写与读用同一个版本，永远一致。这个缺陷只在"写入环境与读取环境不同"时成立——而这正是生产迁移、灾备恢复、版本回滚这些真实场景的常态。

**影响面是全部存量数据。** 与"某次操作出错"不同，格式问题影响的是已经写下的所有数据。修复不能只改代码，还要处理存量——要么转换，要么保证兼容。这是它比一般缺陷难处理得多的原因。

**它有两种失败模式，一种响亮一种安静。** 拒绝打开会让服务起不来，立刻被发现；静默错读会让服务"正常"运行，直到有人发现数据不对。后者是真正需要担心的。

## 本质：格式是约定，约定必须随数据一起持久化

把这份格式约定的存放位置抽象出来，骨架是：

> **任何对数据的解释都依赖一组隐含约定（格式、编码、字节序、单位）；这些约定如果只存在于写入方的运行时环境里，而没有随数据一起持久化，那么读取方只能靠环境默认值重建它们——环境一变，约定就失效。**

这个规律的适用范围远大于文件格式。同一条骨架出现在很多地方：

- 序列化数据没有写 schema 版本 → 反序列化方按当前 schema 解释旧数据。
- 时间戳没有写时区 → 读取方按本地时区解释。
- 数值没有写单位 → 读取方按自己假定的单位使用（这与 [案例九](./../正确性/案例九：数字越过了边界——Redis 里三次截断导致的崩溃.md) 里数字越过类型边界是同一族——数字的含义与它的表示分离了）。
- 配置文件没有写版本 → 升级后的程序按新语义解释旧配置。

共同点是：**信息在写入时是"显然的"，因为写方知道上下文；在读取时这个上下文已经消失，而数据本身不足以重建它。**

要强调的是"随数据一起"这几个字。把版本写在文档里、写在代码注释里、写在部署手册里，都不算——它们都不随数据走。只有写进数据本身（文件头、记录前缀、元数据字段），才能保证读取方拿得到。

## 比喻：一份没有标注单位的体检报告

医院出具体检报告，上面写着"血糖：5.6"。开具的医生知道他用的是 mmol/L，因为医院一直用这个单位。报告上没有写单位。

几个月后，病人换了个城市看病。新医生看到"5.6"，按自己习惯的 mg/dL 理解——在那个单位下，5.6 是极低的危险值。于是开出了完全错误的处理。

两种失败的严重性不同。如果新医生觉得"这个数值不合理，报告可能有问题"，他会去核实——这是"拒绝打开"式的响亮失败。如果他直接采信并据此处理——这就是静默失败，后果严重得多。

问题不在任何一位医生（各自都在按自己的常识工作），在于**报告上没有写单位**。而"单位"这种东西，在开具的那一刻太显然了，显然到根本不会有人想到要写下来。

正确的做法是在报告模板里把单位变成必填项——不是为了当下，是为了那份报告离开开具环境之后仍然可读。

## 机制：默认值如何替代了显式记录

默认版本在本项目的出现位置只有一处：

```
bookkeeper-server/src/main/java/org/apache/bookkeeper/bookie/storage/ldb/KeyValueStorageRocksDB.java
```

创建 RocksDB 实例时，`formatVersion` 应当通过 `CFOptions` 显式设置。未设置时，实际生效的是 RocksDB 库自身的默认值。

关键的结构问题在于：**未设置与"设置为当前默认"在代码上无法区分**。代码里没有这一行，与"写这一行、值等于库默认"是等价的。这让评审与静态检查都无能为力——没有任何规则会说"这里少了一行"，因为少这一行是合法的。

于是缺陷以一种特殊形态存在：**它不是错误的配置，而是缺失的配置**。而"缺失"在配置系统里通常有明确的含义——使用默认值。这个机制本身是合理的（配置就该有默认值），问题出在**这个特定配置项不该有默认值，或者它的默认值不该由外部库决定**。

上游的修复正是针对这一点：显式指定 `format_version`，并把默认值固定为 5（固定写在 BookKeeper 侧，而不是依赖 RocksDB 库）。这样无论 RocksDB 依赖怎么升级，BookKeeper 写出的格式版本都是确定的、可预测的。

这里能看到一个值得记住的修复思路：**对于影响数据持久形态的配置，默认值应当固定在本项目里，而不是继承外部库的默认值。** 外部库的默认值会随依赖升级而变，而数据不会跟着变——两者的错配就是这类缺陷的来源。

## 怎么发现：找出"离开环境就失效"的假设

**列出所有隐含的格式假设，逐个问：数据里有没有记录它？** 格式版本、编码、字节序、时区、单位、schema 版本。任何"读方需要知道但数据里没有"的项，都是候选人。

**做跨环境读取测试。** 在不同的依赖版本、不同的机器、不同的 locale 下读取同一份数据。这类测试不需要复杂，只需要环境真的不同——而测试环境往往做不到这一点，所以要刻意在 CI 里准备"旧环境"或"另一环境"。

**用旧数据测试新代码。** 保留一批历史数据样本，每次升级后读取它们。这能直接验证兼容性，而不是假设兼容。

**给数据加自描述头并强制校验。** 打开文件/记录时先读自描述信息，不匹配就报错。这让"静默错读"变成"明确拒绝"——把最坏的失败模式消除掉。

**审查配置项时区分两类默认值。** 一类是行为调优参数（有默认值完全合理），一类是决定数据持久形态的参数（不应继承外部默认值）。后者应当明确写下并加注释说明为什么是这个值。

## 上游怎么修

两处修复配合完成：一处确保所有 RocksDB 实例都显式指定 `formatVersion`，另一处把默认 `format_version` 固定为 5。

合起来的效果是：BookKeeper 写出的元数据格式不再依赖 RocksDB 库的版本，而是由 BookKeeper 自己决定。这为后续升级 RocksDB 扫清了障碍——格式版本不变，兼容性就可控。

值得注意的是，修复没有去处理存量数据的转换。这说明在这个具体情形下，固定版本后新旧格式可以共存（RocksDB 高版本能读低版本）。**不是所有这类缺陷都需要迁移存量**——是否需要，取决于新旧格式之间是否存在单向不兼容。

验证的锚点：版本兼容要与显式记录对账——预写跨版本打开时行为应明确（拒绝或正常），实测低版本静默读出错乱结果，与预期对不上。

## 边界与教训

- **格式约定必须随数据持久化。** 写在文档、注释、手册里都不算，它们不随数据走。
- **缺失的配置与错误的配置一样危险。** 前者更隐蔽，因为代码里什么都没有。
- **决定数据形态的配置，默认值要固定在本项目。** 继承外部库的默认值等于把数据格式的控制权交给了依赖升级。
- **引入与爆发可能相隔数月。** 排查时要问"这个文件写下来的时候，环境是什么样的"。
- **静默错读比拒绝打开危险得多。** 优先消除静默失败：加自描述头并强制校验。
- **跨环境测试是唯一的网。** 读写环境相同的测试永远发现不了这类问题。

概念入口：API 与 Schema 演进的兼容要求——[[工程知识/软件构建：让变化可以理解、验证与交付/架构与代码/API与Schema演进必须兼容新旧消费者]]；依赖锁定与构建输入——[[工程知识/软件构建：让变化可以理解、验证与交付/开发工具/依赖供应链需要锁定来源与构建输入]]。

相关：[[工程知识/缺陷分析：从个案到体系/正确性/案例九：数字越过了边界——Redis 里三次截断导致的崩溃]] · [[工程知识/缺陷分析：从个案到体系/性能/案例三十七：锁自己变成了瓶颈——争用背后的临界区错配]] · [[工程知识/缺陷分析：从个案到体系/缺陷分析：从个案到体系——导读与开源地图]]

相关：[[工程知识/缺陷分析：从个案到体系/配置/默认值不进账本：建库参数要写进数据目录，读库时对账]] —— 本对病例的方法面：建库参数写进数据目录自描述，读库时对账

## 验证

1. 上游对照：到 BookKeeper 的提交 b00d2e0c 与 697ff570 里核对两处改动，确认一处让所有 RocksDB 实例显式指定 `formatVersion`、一处把默认版本设为 5。
2. 跨版本读取复现：用较高版本的 RocksDB 写出数据库，再用较低版本的 RocksDB 打开，确认低版本直接拒绝打开这一可报错的情形；核对版本号未被记录时读方只能靠默认值推断。
3. 静默失真核验：构造格式差异不改变能否打开、只改变部分结构解释方式的场景，确认读方正常打开并返回内容，而内容是错的，且没有任何信号。
4. 存量核对：统计现有的数据库文件中显式记录格式版本的比例，对未记录的逐个确认其实际格式。
5. 断言锚点：每个实例的格式版本显式写入并可读回，跨版本读写的两种结果（拒绝打开、静默失真）分别有复现记录。

