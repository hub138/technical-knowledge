---
title: Scaling Law 把训练预算分配成可测量假设
type: concept
status: active
updated: 2026-09-25
review_after: 2027-03-03
change_rate: medium
confidence: high
tags:
  - ai/training
  - ai/scaling
  - ai/capacity
sources:
  - "https://arxiv.org/abs/2001.08361"
  - "https://arxiv.org/abs/2203.15556"
editorial_pass: 1
editorial_at: 2026-09-25
editorial_by: agent-A
editorial_note: "去味四处（抽象主语、三处无来源数值补基准），命中清零，essence 信号词保留"
---

# Scaling Law 把训练预算分配成可测量假设

Scaling law 解决的是训练预算有限时，参数、数据和计算量如何共同分配的问题。 Hoffmann 等人给出的趋势模型覆盖的是受控实验内的损失曲线，其本质是拟合出的幂律，不承诺下游事实性、安全性、工具使用或线上延迟；外推前必须确认数据质量、架构和任务分布没有改变结论。

Scaling law 观察模型规模、训练 token 和计算量与验证损失之间的统计关系，帮助判断有限算力应该增加参数、数据还是训练步数。它是预算分配模型，不是下游质量、事实性、推理成本和产品价值的保证。

## 为什么会改变结论

早期实验可能在固定 token 下扩大模型；Chinchilla 说明在计算预算固定时，模型和训练 token 需要共同增长。数据质量、去重、配比、优化器、上下文长度、架构和后训练会改变曲线，不能把一个公开模型的拟合系数直接搬到自己的领域。

## 工程用法

1. 先定义可测目标：预训练 loss、目标任务质量、训练/推理成本和硬件约束。
2. 用多个小规模配置拟合趋势，保留数据分布和训练配方。
3. 检查拐点、过拟合、污染、能力突变和后训练收益。
4. 用独立任务、长尾、安全和线上负载验证放大后的结论。

验证的锚点：外推要与小规模实测对账——预写用百分之一预算拟合的曲线预测大模型损失的偏差范围，超出偏差范围就说明该曲线不能用于外推。

## 边界

更大的模型可能提升能力却超出延迟、显存或预算；更多数据可能增加重复、版权和噪声；蒸馏、MoE、量化和检索会改变参数与计算的关系。任何 scaling 结论都要带实验范围和不确定性。

最小实验：固定数据配方与评测集，训练至少三档模型规模或三档有效 token，记录 FLOPs、验证损失、目标任务质量和 wall-clock，再用未参与拟合的保留集检验外推。若收益只出现在训练分布或单一 benchmark，结论应标为分布内趋势。

关系：[[工程知识/AI 系统工程：从模型能力到生产能力/模型基础与训练/预训练数据决定模型能力上限]] · [[工程知识/AI 系统工程：从模型能力到生产能力/推理服务与平台/推理服务的核心矛盾是延迟、吞吐、显存与质量]]

## 量化锚点：三参数的预算表

Chinchilla 型算力分配的实用账：算力 C ≈ 6ND（参数量乘 token 数），最优配比 D/N ≈ 20（每参数约 20 token）。10^23 FLOPs 预算的两种分法：70B 参数 × 2T token，或 40B × 4T token——后者按 Chinchilla 论文的实测损失更低。每条 isoFLOP 曲线的谷底落在哪个参数规模，论文原图一眼说清：

![Chinchilla 论文图 3：同一批 isoFLOP 预算（横轴模型参数量，纵轴验证损失）下，每条曲线的谷底随预算增大向更大参数规模移动，谷底连线给出「每参数约 20 token」的最优配比；按谷底分配，40B×4T 的损失低于 70B×2T](/static/figures/isoflop-7.svg)

来源：Hoffmann et al.，[Training Compute-Optimal Large Language Models](https://arxiv.org/abs/2203.15556)（Figure 3）。谷底就是「这笔预算该买多大模型」的答案：预算翻倍谷底右移，配比保持约 20 token/参数（该数值来自 Chinchilla 论文 Figure 3 的等算力曲线族）；推理成本再把账改写——训练省下的参数量在推理期每次请求都要付（参数量正比推理算力），长期高流量的服务朝"训练欠拟合、推理省钱"方向移动，这是 Llama 系列长期训练的经济学：参数量低于 Chinchilla 最优解，token 数按其配比准则（约 20 token/参数）的数倍投入，把成本从训练端移到推理端省回来。

## 案例回流：假设先行的衔接

Scaling Law 的工程用法与 [[工程知识/AI 系统工程：从模型能力到生产能力/质量与运营/Agent评估系统的设计约定]] 的评估约定同构：都要求"先定义可测量的判据，再花大钱"。训练侧的判据是损失曲线的外推拟合（用 1% 算力的小模型拟合幂律，外推大模型损失，外推误差控制在个位数百分比），评估侧的判据是测试集对分布的代表性与 leakage 检查（[[工程知识/AI 系统工程：从模型能力到生产能力/数据与MLOps/训练数据治理：来源、许可与污染决定语料的长期价值]] 的 n-gram 污染检测直接可用）——两条线共用一个纪律：大额支出前的假设必须便宜地验证过。

## 可运行示例与案例回流：预算分配的账

可运行示例：一份训练预算（10^23 FLOPs）怎么变成假设检验：

```text
Chinchilla 型假设（公开结论）：token 数 ≈ 20 × 参数数 时单位算力损失最低
  10^23 FLOPs ≈ 6ND（训练 FLOPs 估式）
  → N ≈ 6e10 / (20) ... 具体解：N ≈ 50B 参数、D ≈ 1T tokens 附近

假设的验证路径（小模型外推）：
  用 1/100 的预算训一族小模型（N=70M/160M/400M/1B，D 按 20N 同步放大）
  拟合 L(N, D) = A/N^α + B/D^β + E 的幂律参数
  用拟合曲线预测 50B 模型的损失 → 真训 50B 验证预测偏差
  偏差小于 3%（小模型族到中型模型的常见实测区间）→ 曲线可信，后续预算分配按曲线走；偏差大 → 换假设重新拟合

数据约束的修正：高质量 token 不够 1T 时，
  曲线从"算力最优"切到"数据受限"分支（多训 epoch 的边际收益递减），
  预算分配随之改变：加数据工程权重，砍参数规模
```

案例回流：Scaling Law 用错的实录形态——把幂律拟合外推三个数量级以上（小模型 1B 的曲线直接外推到 1T），外推超出拟合域的预测偏差不可控，公开复现的经验是外推每多一个数量级、预测误差放大数倍；数据配比变化后不重拟合（换了数据管线还用旧曲线），预测整体失真。同源纪律见 [[工程知识/性能工程：从用户等待到资源瓶颈/观测与诊断/基准测试必须先定义负载和测量误差.md]]——假设要可证伪、拟合要报误差、外推要声明边界，两个域用的是同一条测量纪律。

## 验证

1. 反向验证：先用约百分之一的预算训练一族小模型拟合幂律参数，用曲线预测大模型损失，再实训比对偏差，实测与曲线预期对不上时禁止外推，偏差在百分之三以内方可外推。
2. 外推边界：确认预测没有超出拟合域一个数量级以上；数据配比变化后重新拟合。
3. 数据受限分支：高质量 token 不足时切换到数据受限分支，确认预算分配随之降低参数规模、提高数据工程权重。

## 要解决的问题

训练预算有限，参数规模、数据量与计算量三者之间怎样分配缺少依据，凭经验堆参数或堆数据都会在预算耗尽之后才发现方向偏离。本篇回答：趋势模型在什么条件下成立、外推之前要确认哪些前提没有变化、它对下游事实性与安全性的结论边界在哪里，以及怎样把预算分配写成可测量的假设。

