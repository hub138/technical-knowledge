---
title: LoRA与参数高效微调：改行为不改基座
type: concept
status: active
updated: 2026-09-22
review_after: 2027-03-22
change_rate: medium
confidence: high
tags:
  - ai/adaptation
  - ai/training
  - ai/capacity
sources:
  - "https://arxiv.org/abs/2106.09685"
  - "https://arxiv.org/abs/2305.18290"
  - "https://huggingface.co/docs/peft"
---

# LoRA与参数高效微调：改行为不改基座

给开源模型加领域能力，全参数微调要动全部权重、显存动辄数百 GB；而多数行为变化其实集中在低维子空间——LoRA 冻结基座、只注入低秩矩阵，让可训练参数降到千分位，单卡可调 7B 模型。要解决的问题：用最小可训练参数获得目标行为改变，同时保留基座能力、可回滚、可合并部署。

本质是低秩假设：微调引起的权重变化量 ΔW 近似低秩，用两个小矩阵的乘积 B·A（rank r 远小于原维度）去拟合它。训练时只更新 B 和 A，前向时 ΔW 加到原权重上；部署时可把 B·A 合并进 W，推理零额外开销。

## 机制：分解、缩放与合并

| 组件 | 作用 | 工程含义 |
| --- | --- | --- |
| rank r | 低秩近似维度 | 越大拟合能力越强、参数越多；常见 8-64 |
| alpha α | 缩放系数，实际缩放为 α/r | 控制适配强度，稳定训练 |
| target modules | 注入位置（q/k/v/o、FFN） | 决定改变哪类行为；全投影层覆盖最广 |
| 合并 | W' = W + B·A | 部署时合并，推理延迟不变 |
| 量化底座 QLoRA | 4-bit 基座 + LoRA | 显存再降一个量级，但训练与部署精度要分开管理 |

训练数据形态决定行为改变的类型：指令对改变输出格式与风格，领域语料改变知识分布（但知识注入效果有限，见边界），偏好对改变取舍方向。LoRA 是优化器不是数据策略，数据质量上限依然是效果上限。

## 与相邻方案的边界

| 方案 | 参数量 | 改知识 | 改行为 | 回滚 |
| --- | --- | --- | --- | --- |
| 全参数微调 | 100% | 可能 | 深 | 需另存整份权重 |
| LoRA/QLoRA | 0.1-1% | 弱（分布内） | 强 | 删适配器即回滚 |
| Prompt/上下文 | 0 | 无（每次注入） | 浅 | 天然可回滚 |
| RAG | 0 | 无（证据注入） | 无 | 删索引即回滚 |

判断顺序：知识型需求先试 RAG，行为/格式/风格型需求用 LoRA，需要改变推理方式或深度能力时才考虑全参数。混用时要意识到 RAG 改变每次输入、LoRA 改变权重分布，两者叠加的效果不是简单相加，需用同一评测集分别测。

## 边界

- 低秩假设对"学新事实"较弱：注入知识的检索式访问仍应交给 RAG，LoRA 更适合改"怎么回答"而非"知道什么"。
- rank 不是越大越好：r 过大易过拟合小数据集，且参数收益递减；从 r=8 起步翻倍实验比直接上 r=256 更省。
- 合并是单向门：合并进基座后无法再分离出单个可回滚的 LoRA，多适配器并存场景（每租户一个适配器）要保留分离形态，牺牲一点延迟换隔离。
- 量化底座上的 LoRA 合并回高精度基座时可能有精度差，部署前用同一评测集对比合并前后。

## 可运行示例：rank 的账直接算

LoRA 的参数节省与 rank 的关系，一行的账：

```python
# 70B 模型，全参微调：可训练参数 = 70e9
# LoRA 只在注意力与 MLP 的权重矩阵旁挂低秩分解：
#   W' = W + (alpha/r) * A @ B，其中 A: r×d，B: d×r
# r=16, d=8192 的一个矩阵：A+B = 2*16*8192 = 262,144 参数（原矩阵 67,108,864 的 0.4%）
# 全模型 70B 参数下，r=16 挂满常见层：可训练参数约 2e8（0.3%）
# 显存账：优化器状态（Adam 的 m、v 两份）只对可训练参数保留
#   全参：70B × 3 份 × 2 字节 ≈ 420GB；LoRA：0.3% × 420GB ≈ 1.2GB
```

跑一遍的收获：LoRA 省的不是模型权重那份显存（权重要全量加载、冻结），是优化器状态与梯度那份——这就是 70B 模型能在单卡 80GB 上微调的数学。rank 的选择：8-32 覆盖大多数适配任务，rank 翻倍的效果提升递减，任务与基座差异越大 rank 需求越高。

## 量化锚点与案例回流

效果与成本的公开量级：任务精度上 LoRA（r=16）与全参微调的差距通常在 1-2 个点内（适配类任务），推理成本零增加（合并后 W' 与原矩阵同形）；训练显存降一个量级（70B 模型全参需 8×A100，LoRA 单卡可跑），训练时长降 60-80%。失效形态的量化：能力回退与 rank 正相关（rank 越大越可能覆盖基座的原有能力分布），公共基准掉分超 3 个点就该降 rank 或混入通用数据。与 [[工程知识/AI 系统工程：从模型能力到生产能力/模型基础与训练/后训练对齐与微调改变行为边界]] 的衔接：LoRA 是对齐管线里控制遗忘的杠杆；与 [[工程知识/AI 系统工程：从模型能力到生产能力/模型基础与训练/分布式训练与并行策略如何切分模型]] 的衔接：显存账里的优化器状态份额（Adam 三份状态）正是 ZeRO 分级切分的对象——两篇的账本是同一本。

## 验证

验证的锚点：微调效果要与三版本对照对账——预写目标行为提升、基座回归不降超阈值、合并前后差异在噪声内，任一项与预期对不上就不算改动成功。

最小验证：固定评测集（目标行为集 + 基座能力回归集各一），对比基座、LoRA 适配后、合并后三个版本；断言目标行为提升、基座回归不降超过阈值、合并前后差异在噪声内。再测回滚路径：卸载适配器后输出与原基座逐 token 一致。

关系：[[工程知识/AI 系统工程：从模型能力到生产能力/模型基础与训练/后训练对齐与微调改变行为边界]] · [[工程知识/AI 系统工程：从模型能力到生产能力/生态与选型/精调与RAG的边界：什么时候微调更划算]] · [[工程知识/AI 系统工程：从模型能力到生产能力/模型基础与训练/预训练数据决定模型能力上限]]
