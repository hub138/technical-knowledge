---
title: 解码采样决定生成行为而不是知识本身
type: concept
status: active
updated: 2026-09-03
review_after: 2027-03-03
change_rate: medium
confidence: high
tags:
  - ai/inference
  - ai/generation
sources:
  - "https://arxiv.org/abs/1904.09751"
  - "https://platform.openai.com/docs/guides/text"
---

# 解码采样决定生成行为而不是知识本身

解码要解决的是模型给出概率分布后如何选择具体输出轨迹。它改变多样性、稳定性、长度和风险，不会增加模型未学到的事实；最小实验是固定 prompt 和模型，对同一问题集比较 greedy、低温和高温的格式通过率、引用支持率、重复率与成本。
模型每一步给出下一个 token 的概率分布，解码策略决定如何从分布中选取 token。温度、top-p、top-k、重复惩罚、停止条件和结构化约束会改变多样性、稳定性、长度和错误形式，但不能把缺失的事实变成可靠知识。

## 常见策略

| 策略 | 机制 | 适用倾向 | 风险 |
| --- | --- | --- | --- |
| greedy | 每步选最大概率 | 稳定格式、确定性任务 | 可能重复或陷入局部高概率 |
| temperature | 重塑 logits 分布 | 控制随机性 | 温度越高不等于创造力越有用 |
| top-p/top-k | 只在候选子集采样 | 开放式生成 | 截断会改变尾部概率质量 |
| beam search | 保留多条高分路径 | 某些序列生成任务 | 对开放式对话常变得保守、重复 |
| grammar/JSON schema | 将非法 token 路径屏蔽 | 结构化输出、工具参数 | 只保证语法，不保证业务语义 |

## 工程上最容易忽略的边界

- 同一 prompt 和模型，采样参数变化就可能产生不同工具调用和安全行为。
- `max_tokens`、stop sequence 和截断会改变完成条件；“返回 200”不代表任务完成。
- 结构化约束可能让模型在无法满足语义时输出形式正确的错误值。
- 流式输出中途断开、重试或重复提交时，调用方必须用幂等和状态机处理。

## 量化锚点：温度与 top-p 的行为账

同一模型的采样参数账：temperature=0（贪心）输出确定但重复风险高（循环生成的概率随长度上升）；temperature=0.7 + top_p=0.9 是对话类服务的常见起点（多样性足够、退化率低）；代码生成类任务取更低温度（0-0.3，语法错误的代价高）。参数的影响可量化对比：公开基准上同一模型温度 0 与 0.8 的 pass@1 差通常在 1-5 个点，pass@k（k=10）差距反向（高温多采样命中率高）——按任务的验收指标（单次正确还是多次尝试内正确）选温度，这是解码参数唯一可靠的选型依据。

## 案例回流：输出确定性的衔接

采样参数的工程纪律接两篇：[[工程知识/AI 系统工程：从模型能力到生产能力/质量与运营/可靠生成需要结构、证据、拒答与回归]] 的回归测试要求输出可复现——temperature>0 时同一 prompt 两次输出不同，回归判据要从"输出相等"改成"结构相等 + 断言点成立"，temperature=0 也要固定种子与 batch（数值非确定性仍在）；[[工程知识/AI 系统工程：从模型能力到生产能力/推理服务与平台/批处理、KV缓存、量化与并行如何改变服务容量]] 的 batching 会引入浮点非确定性——采样参数的"确定性承诺"要连着基础设施一起核对。

## 可运行示例与案例回流：采样改变行为的实录

```python
# 知识固定、行为可调的直接实验（通用示例）
prompt = "列出这个项目的三个风险"

# 贪心解码（temperature=0）：跑三次，输出完全相同
#   ["进度风险", "质量风险", "成本风险"] —— 每次都一样

# temperature=0.8：跑三次，三种组合
#   ["进度风险", "人员流动风险", "依赖延迟风险"]
#   ["质量风险", "成本风险", "进度风险"]
#   ["依赖延迟风险", "合规风险", "文档缺失风险"]
#   —— 模型"知道"的风险集合不变，采样决定每次亮出哪些
# 温度调的是分布形状：低温削峰（只出最可能的），高温摊平（长尾也有机会）
```

案例回流：把"换参数"当"换能力"的实录——评测发现模型"不会"列风险，加 few-shot、换提示词轮番折腾后才发现采样温度设了 1.3（长尾噪声盖过了主分布），回落到 0.3 后"不会"变"很稳"：先查解码配置，再动提示词与数据，排查顺序错了一半的力气白花。相反方向的实录：创意场景设了贪心，用户发现"每次生成的文案一模一样"，这不是能力问题，是控制面配错了任务。知识层与解码层的分界实验是同一结论的另一半：模型不知道的事实，任何采样参数都变不出来——参数调分布，数据与检索改知识（与 [[工程知识/AI 系统工程：从模型能力到生产能力/知识与检索/RAG的核心是选择可引用证据.md]] 的证据供给衔接）。

## 验证方法

在固定模型快照、prompt、工具 schema 和输入集下，分开测确定性、格式通过率、事实/引用、拒答、工具参数正确率、平均/长尾 token 和成本。生产任务按类型设定不同解码策略，不要用一个全局温度掩盖任务差异。

关系：[[工程知识/AI 系统工程：从模型能力到生产能力/模型与上下文/模型提出候选，系统定义正确性]] · [[工程知识/AI 系统工程：从模型能力到生产能力/推理服务与平台/推理服务的核心矛盾是延迟、吞吐、显存与质量]]

## 验证

1. 固定变量：在固定模型快照、提示词、工具 schema 与输入集的前置条件下比较不同采样配置，确认输出差异来自参数而不是其他变动。
2. 分工测试：知识类问题在调整温度前后答案事实不变，格式类问题随结构化约束改善，确认两类属性没有被混在一起调。
3. 任务分档：按任务类型分别统计确定性、格式通过率、引用支持率与平均、长尾 token，确认没有用同一个全局温度掩盖任务差异。

## 效果与代价

按任务类型分别设定采样策略，换来的是格式通过率、引用支持与成本之间可以做有依据的取舍，同一提示词在不同请求上的输出差异有了可解释的来源。代价是参数与业务语义的边界需要单独守住，温度提高不等于知识增加，结构化约束只能保证形状，回归判据还要从输出相等改成结构与断言成立。

## 要解决的问题

同一段提示词在不同请求里得到风格差异很大的输出，或者输出稳定但内容与其他事实对不上，团队改动提示词之后现象随机变化。本篇回答：采样参数改变的是输出的哪一类属性、它会带来哪些副作用、它与模型掌握的知识之间是什么关系，以及比较不同采样配置时应当固定哪些变量。

