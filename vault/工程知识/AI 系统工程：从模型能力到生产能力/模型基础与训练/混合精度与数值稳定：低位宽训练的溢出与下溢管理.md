---
title: 混合精度与数值稳定：低位宽训练的溢出与下溢管理
type: concept
status: active
updated: 2026-09-22
review_after: 2027-03-22
change_rate: medium
confidence: high
tags:
  - ai/training
  - ai/numerics
  - reliability
  - correctness
sources:
  - "https://arxiv.org/abs/1710.03740"
  - "https://developer.nvidia.com/blog/mixed-precision-training-deep-neural-networks/"
---

# 混合精度与数值稳定：低位宽训练的溢出与溢下管理

FP16 训练跑到第 3000 步 loss 突然变 NaN，FP8 更激进省显存但对算子的要求更高——混合精度训练省下的显存与算力，是用数值风险换来的。要解决的问题：理解每个精度档位的风险来源（溢出、下溢、精度损失），配好对应的防护（loss scaling、敏感算子 FP32 化、梯度统计），让低位宽训练既快又不悄悄劣化质量。

本质是动态范围与精度的交换。FP32 有 8 位指数、23 位尾数，动态范围约 1e38；FP16 指数只有 5 位，上限约 65504、下限约 6e-8；BF16 指数位与 FP32 相同（8 位），动态范围一样但尾数只有 7 位——精度低但不容易溢出。训练中梯度常比权重小几个数量级，FP16 下大量梯度下溢成零，loss scaling 就是给 loss 乘一个大系数把梯度拉回可表示区间，再在更新前除回去。

## 机制：三件防护与精度档位

| 防护 | 防什么 | 怎么做 |
| --- | --- | --- |
| loss scaling | 梯度下溢 | loss × s 再反传，权重更新前 unscale；s 动态调整（溢出则减半） |
| 敏感算子 FP32 化 | 归一化、softmax、reduction 的精度塌陷 | 这些层强制 FP32 计算，只对 conv/matmul 用低精度 |
| 梯度统计与跳批 | 溢出批次混入更新 | 监控 inf/nan 梯度比例，溢出批跳过而非写入 NaN |

档位选择的起点：有 BF16 硬件（Ampere 及以后）优先 BF16，动态范围大、不需要 loss scaling，是省心的默认；FP16 是老硬件的过渡档，必须配 loss scaling；FP8 只适合对成本极敏感且有成熟框架支持的场景，需要 per-tensor scaling 与更细的算子检查。推理与训练不同：推理可以更激进地低精度（权重与激活的分布固定），训练必须保守（分布每步都在变）。

## 失效形态

| 失效 | 机制 | 信号 |
| --- | --- | --- |
| loss 变 NaN | 梯度溢出或下溢后写入 inf/nan | loss 曲线某步后恒 NaN，重启复现 |
| 质量悄悄降 | 累积精度损失，loss 看着正常但最终指标差 | 与 FP32 基线对比最终任务质量差 1%+ |
| scaling 失调 | scale 太小梯度仍下溢，太大频繁溢出跳批 | 溢出率 >0.1% 或 scale 频繁震荡 |
| 算子不兼容 | 某些 kernel 无低精度实现，静默回退 FP32 | 实测加速比低于理论值 |
| 数值噪声放大 | 低精度噪声与学习率、正则化交互 | 小数据集上过拟合行为变化 |

## 边界

- 加速比承诺要按你的模型实测：注意力与 GEMM 密集的模型收益大，大量小算子与 reduction 的模型收益小，框架文档的 2-3x 是理想情形。
- BF16 并非万能答案：尾数少意味着精度低，某些对精度敏感的任务（数值回归、精确定位）BF16 与 FP32 有可测差距，要对比后决定。
- 混合精度的"混合"策略因框架而异：哪些算子自动 FP32 化、autocast 的边界在哪，换框架要重新验证，不能沿用结论。
- 与量化的边界：训练用混合精度是"计算档位"，部署用量化是"存储与带宽档位"，两者独立决策、独立验证，中间的精度一致性要端到端测。

## 可运行示例与案例回流：损失缩放的账

可运行示例：fp16 训练的动态范围问题与损失缩放的解法：

```python
scaler = GradScaler(init_scale=2**16)
with autocast(dtype=torch.float16):        # 前向用 fp16（快 2-3 倍，省一半显存）
    loss = model(input)
loss_scaled = scaler.scale(loss)           # 损失放大 65536 倍
loss_scaled.backward()                     # 梯度带着缩放走，避开 fp16 下溢区
scaler.step(optimizer)                     # 检查有无 inf/nan
scaler.update()                            # 有溢出 → 缩小 scale 并跳过本步

# fp16 的短板：动态范围约 6e-5 到 6e4
#   梯度值集中在 1e-7 量级 → 直接存 fp16 全部下溢成 0 → 训练静默失效
#   缩放 2^16 后梯度回到可表示区间；权重更新仍用 fp32 主副本
# bf16 换指数位换尾数位：范围与 fp32 相同（不会溢出/下溢），
#   精度降到 8 位尾数 → 无需缩放，代价是累积误差更粗
```

案例回流：数值失效的实录形态——loss 变 nan 不是"训练不稳定"这种模糊话术，来源高度收敛：梯度溢出（scale 太大）、学习率过大致权重爆炸、数据里有 inf（未归一化的特征）；诊断顺序是先看 scale 曲线（持续下跌说明在频繁跳步）、再查数据管线、最后才是调学习率。静默失效比崩溃更危险的实录见 [[工程知识/AI 系统工程：从模型能力到生产能力/模型基础与训练/后训练对齐与微调改变行为边界.md]] 的评测门禁——梯度下溢成 0 时训练照常跑、loss 照常降（在缩放的损失上看不出来），只有评测指标不动才暴露，这也是混合精度训练必须带指标看板的理由。

## 验证

最小验证：同一配置跑 FP32 与 BF16 各一份，对比三件事——loss 曲线形态一致、最终任务质量差距 <0.5%、吞吐提升符合预期；再跑 FP16 + loss scaling 变体，监控溢出率与 scale 轨迹，断言溢出率 <0.01% 且 scale 稳定。发现 NaN 时先做二分定位：固定 seed 逐步回退精度档位，找到第一个出问题的算子再决定单点 FP32 化。

关系：[[工程知识/AI 系统工程：从模型能力到生产能力/模型基础与训练/分布式训练与并行策略如何切分模型]] · [[工程知识/AI 系统工程：从模型能力到生产能力/推理服务与平台/模型量化的精度经济学：位宽精度与显存的三角]] · [[工程知识/AI 系统工程：从模型能力到生产能力/模型基础与训练/LoRA与参数高效微调：改行为不改基座]]
