---
title: Transformer 如何把序列建模成可扩展计算
type: concept
status: active
updated: 2026-09-03
review_after: 2027-09-03
change_rate: low
confidence: high
tags:
  - ai/fundamentals
  - ai/models
  - deep-learning
sources:
  - "https://arxiv.org/abs/1706.03762"
  - "https://nlp.seas.harvard.edu/annotated-transformer/"
---

# Transformer 如何把序列建模成可扩展计算

Transformer 解决的是序列关系建模中“逐步处理难以并行、局部状态难以跨距离传递”的问题。它用全局注意力交换位置间的信息，用逐位置前馈网络变换表示；训练阶段适合矩阵并行，生成阶段却受 KV cache、显存和逐 token 依赖限制。

Transformer 的关键不是“有注意力”这一个组件，而是把序列中每个位置的表示，通过可并行的全局内容寻址和逐位置变换反复更新。训练阶段可以并行处理整段序列；自回归生成时则用 KV cache 保存已经计算过的键和值，把每一步的代价从重复处理整个前缀降下来。

## 一层在做什么

给输入表示 `X`，多头自注意力先投影出 `Q、K、V`：

```text
Attention(Q,K,V) = softmax(QKᵀ / √dₖ + mask) V
```

- `Q` 表示当前位置要寻找什么；`K` 表示每个位置能被什么匹配；`V` 是被取回的内容。
- 缩放项避免内积随维度增大而让 softmax 饱和。
- causal mask 让位置只能看见自己和过去，保证训练目标与生成时的信息边界一致。
- 多头让模型在不同子空间同时学习指代、局部模式、长距离关系等关联。

注意力输出经过残差连接、归一化和前馈网络。前馈网络对每个位置独立应用非线性变换，注意力负责位置之间的信息交换；两者不是互相替代的“两个模型”。

## 为什么它能扩展

训练时一批 token 的矩阵乘法适合 GPU/TPU，序列内没有逐 token 的控制依赖。代价主要来自注意力的序列长度平方项、层数、隐藏维度和激活/通信；长上下文并不是免费扩大记忆。

生成时，每一步的新 query 需要读取历史 `K/V`。KV cache 避免重算历史，但会按层、头、序列长度占用显存；这就是上下文窗口、并发、显存和 TTFT/ITL 相互制约的来源，连接 [[工程知识/AI 系统工程：从模型能力到生产能力/推理服务与平台/批处理、KV缓存、量化与并行如何改变服务容量]]。

## 它不保证什么

- 注意力权重不是可靠的因果解释，也不证明模型“理解”了被关注内容。
- 参数量和上下文长度不等于事实准确性；知识是否新鲜由数据、RAG 和工具决定。
- 训练可并行不代表部署可无限扩展；通信、显存带宽、KV cache 和调度可能成为瓶颈。
- 结构化输出、工具调用和安全行为是后训练与运行时共同提供的约定，不是 Transformer 原生保证。

## 最小验证

学习或比较模型时固定 tokenizer、上下文长度、精度和 batch，分别测：

1. 训练/推理显存随序列长度和并发的变化；
2. prefill 与 decode 的计算、带宽和通信占用；
3. causal mask、padding、位置编码变化对困惑度和下游任务的影响；
4. 长上下文任务中有效信息利用率，而非只看最大窗口声明。

最小验证：固定 tokenizer、层数、隐藏维度和硬件，只改变序列长度或并发，分别测 prefill、decode、KV cache 和任务准确率。这样可以把“架构计算复杂度”与“运行时容量瓶颈”区分开。

## 关系

Transformer 是模型计算骨架；tokenizer 决定输入如何切分，预训练决定参数中的通用模式，后训练改变行为，解码决定输出轨迹，推理运行时决定有限硬件上的服务容量。

关系：[[工程知识/AI 系统工程：从模型能力到生产能力/模型基础与训练/Token、Embedding与表示学习连接数据和模型]] · [[工程知识/AI 系统工程：从模型能力到生产能力/推理服务与平台/批处理、KV缓存、量化与并行如何改变服务容量]] · [[工程知识/AI 系统工程：从模型能力到生产能力/模型基础与训练/位置编码和长上下文限制有效利用]]

- [[工程知识/AI 系统工程：从模型能力到生产能力/模型基础与训练/MoE用稀疏激活换取参数容量和通信复杂度]] —— 用稀疏前馈替换本页的稠密前馈层
- [[工程知识/AI 系统工程：从模型能力到生产能力/推理服务与平台/Prefill与Decode决定LLM服务如何调度]] —— 注意力在两阶段的开销差异
- [[工程知识/AI 系统工程：从模型能力到生产能力/模型基础与训练/Scaling Law把训练预算分配成可测量假设]] —— 计算量与参数量的分配依据

## 验证

1. 复杂度核对：把序列长度加倍，确认训练阶段的注意力开销按平方增长，生成阶段每步的键值缓存占用按长度线性增长。
2. 阶段对照：同一模型在预填充与逐 token 生成两个阶段分别测吞吐与显存，确认瓶颈位置不同，与理论预期对得上。
3. 替代结构对照：把稠密前馈换成稀疏激活后，确认参数量上升而单 token 计算量不随参数等比上升。

## 机制与本质

Transformer 的机制层：序列中任意两个位置之间的关系通过查询与键的内积直接计算，不再依赖逐步传递的隐状态，因此同一批 token 的表示可以在矩阵运算中同时更新。注意力带来序列长度的平方项开销，生成阶段的逐 token 依赖又把历史键值固定在显存里，训练阶段的可并行程度与部署时的容量上限由此分开。

## 要解决的问题

按顺序逐步处理的序列模型难以并行训练，相隔很远的两个位置之间的信息也难以传递。本篇回答：注意力机制怎样在位置之间交换信息、逐位置前馈网络完成什么变换、训练阶段与生成阶段各自的资源限制在哪里，以及这些限制怎样决定服务侧的容量表现。

