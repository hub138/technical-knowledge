---
title: 后训练对齐与微调改变行为边界
type: concept
status: active
updated: 2026-09-03
review_after: 2027-03-03
change_rate: medium
confidence: high
tags:
  - ai/training
  - ai/alignment
  - ai/evaluation
sources:
  - "https://arxiv.org/abs/2203.02155"
  - "https://arxiv.org/abs/2305.18290"
  - "https://arxiv.org/abs/2305.18654"
---

# 后训练对齐与微调改变行为边界

后训练解决的是预训练模型“会生成但不一定按任务格式、偏好、安全和工具约定行动”的问题。它改变行为分布，不是可靠写入新事实；最小对照应固定基础模型、prompt 和解码，只替换后训练版本，检查格式、工具参数、拒答和旧能力回归。
预训练形成通用表示和预测能力，后训练在较小但更有意图的数据上塑造“如何回答、何时拒绝、如何调用工具、输出什么格式”。监督微调、偏好优化和强化学习改变的是输入到输出的行为分布，不应被误解为可靠地写入一组新事实。

## 典型链路

1. **监督微调（SFT）**：用高质量示范把模型推向目标格式、任务和语气。
2. **偏好数据**：收集两个回答的相对偏好，训练奖励模型或直接优化偏好目标。
3. **RLHF/DPO 等优化**：提高偏好目标，但可能过拟合标注者、奖励模型或短期风格。
4. **安全与能力回归**：检查拒答边界、工具行为、事实、代码和长上下文是否互相牺牲。

## 为什么会发生能力退化

微调数据过窄会造成灾难性遗忘、格式过拟合和分布外性能下降；偏好目标可能奖励“听起来正确”而不是“证据充分”。模型更服从指令也可能更容易被恶意指令驱动。参数更新、LoRA、蒸馏和系统 prompt 的影响范围不同，必须分别记录。

## 什么时候不用微调

- 事实经常变化或必须引用：优先 RAG/权威工具。
- 只是临时格式或少量示例：优先 prompt 和结构化输出。
- 需要实时业务状态或副作用：使用受控 API/工具。
- 需要稳定风格、术语或重复行为，且有足够代表性数据：才评估微调。

## 评测门禁

微调前冻结基线、数据和环境；按任务切片比较质量、拒答、越权、引用、延迟、token、显存和成本。至少包含未见领域、冲突指令、长输入、工具错误和恢复样本。只有当目标切片收益超过回归和运维成本时，才进入灰度。

关系：[[工程知识/AI 系统工程：从模型能力到生产能力/模型与上下文/预训练、后训练、RAG与工具改变不同层次]] · [[工程知识/AI 系统工程：从模型能力到生产能力/质量与运营/Agent评测必须覆盖轨迹而不只看最终答案]] · [[工程知识/AI 系统工程：从模型能力到生产能力/模型基础与训练/蒸馏与模型合并：用小模型承接已验证的能力]]

## 可运行示例：SFT 与 DPO 的最小差异

同一个偏好数据（好答案/坏答案对），监督微调与直接偏好优化的损失形态：

```python
# SFT：只学好答案（坏答案不进损失）
loss_sft = -log p_model(good_response | prompt)

# DPO：好答案与坏答案的相对概率差（免奖励模型）
beta = 0.1
loss_dpo = -log sigmoid(beta * (
    log p_model(good | prompt) - log p_ref(good | prompt)
  - (log p_model(bad  | prompt) - log p_ref(bad  | prompt))
))
# p_ref 是冻结的基座：对齐学的是"相对基座的偏移"，不是绝对概率
```

跑一遍的收获：DPO 的梯度只在"好答案与坏答案的似然差"上发力，beta 控制偏移幅度（太大偏离基座失通用能力，太小学不动）。SFT 会连坏答案的分布一起挪（只在好样本上训练也会压低其余输出的概率），DPO 显式推开坏答案——这就是同一份数据两种损失的行为差异来源。

## 量化锚点与案例回流

对齐的成本账：RLHF 三阶段（SFT → 奖励模型训练 → PPO）里奖励模型训练与 PPO 各要独立算力，DPO 砍掉两段（免奖励模型、免在线采样），成本约为 RLHF 管线的三分之一，公开对齐基准上两者差距在个位数百分点。能力退化的量级：全参微调 10 万条窄域数据后，通用基准掉 5-15 个点是常态（灾难性遗忘），LoRA 与混入通用数据是两条止损线。与 [[工程知识/AI 系统工程：从模型能力到生产能力/模型基础与训练/LoRA与参数高效微调：改行为不改基座]] 的衔接：对齐改的是行为分布，微调改的是任务边界，两者都要过 [[工程知识/AI 系统工程：从模型能力到生产能力/质量与运营/离线评测与在线评测回答不同问题]] 的评测门禁——行为改变没有"看起来变好了"，只有指标说话。

## 验证

1. 行为对照：同一数据分别按 SFT 与 DPO 训练，比较输出的格式遵从、拒答边界与工具调用方式，确认偏移幅度随 beta 变化。
2. 通用能力回归：在窄域数据微调前后各跑一次通用基准，记录退化幅度，确认 LoRA 或混入通用数据把退化控制在可接受范围。
3. 分层归因：微调后知识类问题的正确率不因训练而上升，行为类问题按预期改变，确认改动对应到正确的层。

## 机制与本质

后训练的机制层：预训练形成的是输入到输出的概率分布，后训练在较小规模、带有明确意图的数据上继续调整这一分布，使回答格式、拒答边界与工具调用方式发生偏移。分布偏移改变的是行为选择，参数中的事实内容并不会因为微调而变得可靠，因此行为改动与知识更新需要分别用不同的手段承担。

## 要解决的问题

预训练得到的模型能够生成内容，却不按任务要求的格式输出、不遵循工具参数约定、在应当拒答的位置照常作答。本篇回答：后训练改变的是哪一类行为、它能写入什么与不能写入什么、行为改变怎样与事实更新区分开，以及验证一组改动需要固定哪些变量。

