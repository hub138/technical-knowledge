---
title: Token、Embedding 与表示学习连接数据和模型
type: concept
status: active
updated: 2026-09-15
review_after: 2027-03-15
change_rate: stable
confidence: high
tags:
  - ai/fundamentals
  - ai/embeddings
  - information-retrieval
sources:
  - "https://arxiv.org/abs/1508.07909"
  - "https://arxiv.org/abs/1908.10084"
  - "https://www.sbert.net/"
---

# Token、Embedding 与表示学习连接数据和模型

模型看不见"文字"，只看见**数字**。这一页讲三件事：文本怎么变成数字（token）、对象怎么变成可比较的向量（embedding）、以及这些向量里的结构从哪来（表示学习）。

这三个概念常被混为一谈，导致对上下文容量、token 成本、索引内存、RAG 召回质量的判断同时出错。理解它们的差别，是估算成本、设计检索、排查"为什么搜不到"的前提。

## 背景：一个"搜不到"的 bug

一个内部问答系统，用户搜「怎么申请生产环境权限」，返回的全是"开发环境权限"的文档。

排查过程：

1. 先看召回：文档确实在库里，但**余弦相似度排在第 47 位**
2. 看向量：「怎么申请生产环境权限」和「生产环境权限申请流程」的相似度只有 **0.61**
3. 而「怎么申请开发环境权限」是 **0.78** —— 因为"怎么申请...权限"这几个字几乎一样

**原因：embedding 模型对"生产"和"开发"这两个词的区分度不够**——它们在向量空间里太近了。

这个 bug 引出三个层面的问题：

| 层面 | 问题 |
| --- | --- |
| Token 层 | 中文是怎么被切开的？"生产环境"是一个 token 还是三个？ |
| Embedding 层 | 相似度 0.61 意味着什么？为什么语义不同的句子分数很高？ |
| 表示学习层 | 向量空间的结构从哪来？为什么"生产"和"开发"会靠得近？ |

## 从 0 开始：Token

### 模型只认识数字

```
"今天天气不错"
    ↓ tokenizer
[32046, 15823, 8947]        ← 三个 token id
    ↓ 输入层查表
[[0.12, -0.4, ...], ...]     ← 三个向量（每个 4096 维）
```

**Token 是模型处理文本的最小单位。** 它不等于"字"，也不等于"词"。

### 切分规则：子词（Subword）

现代 tokenizer 用 BPE/Unigram 算法，把文本切成**子词**：

```
英文："unbelievable" → ["un", "believ", "able"]     3 个 token
中文："生产环境权限" → ["生产", "环境", "权限"]        3 个 token（常见情况）
代码："getUserById" → ["get", "User", "By", "Id"]    4 个 token
数字："1234567"     → ["123", "456", "7"]           3 个 token（不是 1 个！）
```

**关键：切分规则是训练出来的，不是按语言学规则。** 不同的模型 tokenizer 不同，**同一个文本的 token 数可以差 2 倍**。

### 这对工程意味着什么

**1. 成本直接由 token 数决定**

```
输入 100 万 token × 单价 = 成本
```

中文通常 1 个字 ≈ 0.6-1 个 token（取决于模型和内容）。**不要按"字数"估算成本，要按 token 数。**

**2. 上下文窗口是 token 数，不是字数**

"支持 200K 上下文" = 20 万个 token ≈ **15 万汉字**（按 1.3 token/字算）。不是 20 万字。

**3. 代码和数字的 token 密度很高**

```python
# 这行看起来很短，但可能是 15 个 token
df.loc[df['user_id']==8821, 'amount'].sum()
```

**代码比自然语言"费" token**，这是代码 Agent 成本高的原因之一。

**4. 换模型要重新测**

同样是这份文档：
- GPT 系 tokenizer：12,400 token
- Claude 系 tokenizer：11,800 token
- 某个开源模型：18,200 token

**不要沿用旧的估算。** 用目标模型的 tokenizer 实测。

### 一个反直觉的坑：数字

```
"1000" 和 "1001" 在模型眼里可能差别很大
因为它们被切成不同的子词组合
```

这解释了为什么**大模型做算术容易错**——它看到的是 token，不是数值。

## 从 0 开始：Embedding

### 是什么

**Embedding = 把一个对象（文本/图片/代码）映射成一个固定长度的向量**，使得"语义相近"的对象在向量空间里距离近。

```
"如何申请生产权限"  → [0.21, -0.33, 0.87, ...]   (1024 维)
"生产权限申请流程"  → [0.19, -0.31, 0.85, ...]   ← 很接近
"今天午餐吃什么"    → [-0.72, 0.44, -0.11, ...]  ← 很远
```

### 相似度怎么算

**余弦相似度**（最常用）：两个向量的夹角。

```python
similarity = cos(θ) = (q · x) / (|q| × |x|)
```

取值范围 -1 到 1（归一化后通常 0 到 1）。

| 分数 | 含义（通用模型，仅供参考） |
| --- | --- |
| > 0.85 | 几乎同义 |
| 0.7-0.85 | 语义相关 |
| 0.5-0.7 | 主题相关但细节不同 |
| < 0.5 | 基本无关 |

**重要：绝对分数没有意义，只有相对排序有意义。** 不同模型的分数分布完全不同——**不要用固定阈值判断"是否相关"**，要用 top-k 排序。

### Embedding 不是什么

这是最容易出错的认知：

| 误解 | 事实 |
| --- | --- |
| "相似度 0.9 就是正确答案" | ❌ 只说明**表述相近**，不说明事实正确 |
| "向量库里能查到就能回答" | ❌ 查到的是**候选**，还要读原文验证 |
| "embedding 理解了语义" | ❌ 它学到的是**训练目标下的统计规律** |
| "可以用它判断权限" | ❌ 向量相近不代表权限相同（**这是安全漏洞**） |

**最危险的误解是最后一条**：用"向量相似度"做权限控制或内容过滤，会被绕过。

### 三种常被混淆的"向量"

| 类型 | 产生位置 | 用途 | 特点 |
| --- | --- | --- | --- |
| **token embedding** | 模型输入层 | 给 Transformer 提供初始表示 | **上下文无关**："苹果"在"吃苹果"和"苹果公司"里是同一个向量 |
| **hidden state** | 模型中间层 | 可用于分类、特征提取 | **上下文相关**：同一个词在不同句子里向量不同 |
| **句子/文档 embedding** | 专门的编码器或池化 | 检索、聚类、去重 | 为"整段语义相似度"优化 |

**关键区别**：

- 拿**输入层的 token embedding** 做检索 → 效果很差（上下文无关）
- 拿**生成模型的 hidden state** 做检索 → 通常不如专门的 embedding 模型
- **专门的 embedding 模型**（如 bge、gte、E5）在检索任务上明显更好

## 从 0 开始：表示学习

### 向量空间的结构从哪来

**不是天然的，是训练出来的。** 模型通过训练任务，学会把"相似的东西"放近。

对比学习（Contrastive Learning）的典型做法：

```
正样本：「如何申请权限」↔「权限申请流程」  → 拉近
负样本：「如何申请权限」↔「今天天气不错」  → 推远
```

训练数据决定了"什么算相似"：

| 训练数据 | 学到的"相似" |
| --- | --- |
| 通用网页文本 | 主题相似 |
| 问答对 | 问题和能回答它的段落相似 |
| 代码 + 注释 | 代码和描述它的文字相似 |
| 同义句对 | 语义相同 |

**这解释了开篇那个 bug**：通用模型在训练数据里，"生产环境"和"开发环境"经常出现在相似的句子结构中，所以它们的向量很近——但业务上它们完全不同。

### 领域偏移

**通用 embedding 模型在专业领域表现会明显下降。**

实测常见现象：

| 场景 | 通用模型表现 |
| --- | --- |
| 通用百科问答 | 好 |
| 内部业务术语 | 差（"开单" vs "下单" vs "建单" 分不清） |
| 代码检索 | 差（需要代码专用模型） |
| 多语言混合 | 中等（跨语言能力依赖训练数据） |

**解决**：领域微调，或者用更好的检索策略（混合检索 + 重排）。

## 生产怎么用

### 检索链路的完整形态

```
查询
 ├─ 关键词检索（BM25）    ← 精确匹配："生产环境权限"
 ├─ 向量检索（embedding） ← 语义匹配："怎么申请正式环境的权限"
 └─ 融合（RRF）→ 重排（cross-encoder）→ Top K
                                          ↓
                              读原文验证 → 生成答案
```

**单用向量检索是最常见的错误**——它擅长语义，不擅长精确匹配（ID、报错码、专有名词）。

详细见 [[工程知识/AI 系统工程：从模型能力到生产能力/知识与检索/RAG的核心是选择可引用证据]]。

### 向量库的容量估算

```
内存 ≈ 向量数 × 维度 × 每维字节数 × 索引开销
```

| 配置项 | 例子 |
| --- | --- |
| 100 万条 × 1024 维 × float32 | 4 GB（原始向量） |
| 加 HNSW 索引开销 | 约 1.5-2 倍 → 6-8 GB |
| 量化（int8 / 二值） | 可降到 1/4 - 1/32，但有精度损失 |

**维度不是越大越好**：1024 维够用，**更高维度增加内存和计算，召回提升有限**。

### 版本管理（最容易漏的工程问题）

**换 embedding 模型 = 整个向量空间变了，旧向量全部失效。**

```
v1 模型的向量：A(0.2, 0.3), B(0.5, 0.1)
v2 模型的向量：A(0.8, -0.1), B(0.1, 0.9)   ← 同一个文档，完全不同的坐标
```

**新旧向量混在一起，检索结果无法解释。**

必须做的：

1. **向量里记录模型版本**
2. **换模型时全量重建**（不是增量）
3. **重建期间双写或双索引**，切换时用开关控制
4. **重建是耗时操作**（百万级可能几小时），要提前规划

## 验证清单

- **token 数用目标模型的 tokenizer 实测**，不要按字数估算
- **按内容类型分桶测**：中文、代码、数字、混合语言的 token 密度差异很大
- **检索质量用 Recall@k 衡量**，不是看向量分布图
- **构造难例测试**：否定（"不含 X"）、近义词、专有名词、多语言
- **相似度阈值不要跨模型复用**
- **删除/撤权的文档要验证真的检索不到**（向量库删除可能不彻底）

## 常见错误

**错误 1：按字数估算 token 和成本**

中文 1 字 ≈ 0.6-1.5 token，代码更高。**必须实测。**

**错误 2：用输入层 embedding 做检索**

上下文无关，效果差。**用专门的 embedding 模型。**

**错误 3：用固定相似度阈值过滤**

不同模型分数分布不同。**用 top-k 排序，不用绝对阈值。**

**错误 4：换 embedding 模型不重建索引**

新旧向量混用 → 召回结果混乱。**必须全量重建。**

**错误 5：以为向量相似 = 语义相同 = 可以回答**

相似只是候选。**必须读原文，检查权限，再生成答案。**

**错误 6：用 embedding 做权限控制**

"这段内容和那段内容向量相近，所以可见性相同" —— **这是安全漏洞，绝对不能这么做。**

关系：[[工程知识/AI 系统工程：从模型能力到生产能力/知识与检索/RAG数据管道从文档到可引用证据]] · [[工程知识/AI 系统工程：从模型能力到生产能力/知识与检索/向量数据库不是RAG本身]] · [[工程知识/AI 系统工程：从模型能力到生产能力/知识与检索/RAG的核心是选择可引用证据]]
