---
title: GPU执行模型与显存层级：SIMT与合并访存
type: concept
status: active
updated: 2026-09-22
review_after: 2027-09-22
change_rate: low
confidence: high
tags:
  - hardware
  - ai/training
  - performance/gpu
sources:
  - "https://docs.nvidia.com/cuda/"
  - "https://developer.nvidia.com/blog/understanding-the-gpu-memory-hierarchy/"
---

# GPU执行模型与显存层级：SIMT与合并访存

## 要解决的问题

GPU 执行模型要解决的问题，是**用一条指令驱动成千上万的计算单元**，同时不让访存拖住计算单元。CPU 的优化方向是单指令流的水深（乱序、预测、大缓存，见 [[工程知识/软件构建：让变化可以理解、验证与交付/语言与运行时/CPU微架构与流水线：分支预测乱序执行对程序员的可见影响.md]]）；GPU 的方向相反：单核简单，核数海量（数千到上万），靠并行宽度换吞吐。执行模型是 SIMT（single instruction, multiple threads）：一批线程（warp，32 个）锁步执行同一条指令，走不同路径的分支会分裂执行。这个模型的两个可见后果：分支发散吃掉并行度、访存模式决定吞吐大半。GPU性能取决于计算、访存、并行与通信（见 [[工程知识/性能工程：从用户等待到资源瓶颈/处理器与内存/GPU性能取决于计算、访存、并行与通信.md]]）写的是怎么判断瓶颈，本文写模型本身。

## 机制

**warp 与分支发散**。调度单位是 warp（32 线程锁步）。warp 内所有线程执行同一条指令；if-else 分支让 warp 分裂：先执行 then 分支的线程（其余空闲），再执行 else 分支（先前那批空闲）。两个分支都执行完才汇合，warp 的有效吞吐减半。发散是 SIMT 的税：控制流越复杂（每线程走不同路径），浪费越多。GPU 友好的代码要"全体一致"（warp 内线程尽量走同一路径），排序、分桶、直方图类算法的 GPU 版本都要围绕"消除发散"重构。

**显存层级与合并访存**。GPU 的存储是金字塔：寄存器（每线程私有，最快）→ 共享内存/L1（每 SM 共享，快）→ L2（全芯片）→ 显存 HBM（慢一个数量级以上，但带宽极高）。合并访存（coalesced access）：warp 的 32 个线程访问连续地址（如 32 个 float 正好 128B 一段），硬件合成一次显存事务；地址散乱则每个线程一次事务，访存吞吐塌方。**访存模式是 GPU 编程的第一定律**：矩阵乘的分块（tiling）、共享内存的显式搬运，全是把"散乱访存"重写成"连续访存 + 局部复用"。

**kernel 与主机-设备分工**。GPU 代码（kernel）从 CPU 启动，数据经 PCIe/NVLink 在主机内存与显存间搬运。kernel 内部：grid（全部线程）→ block（每 SM 一个或多个）→ warp（调度单位）→ thread。内存拷贝与 kernel 启动是异步的（流 stream），CPU 发起后可继续干活——但拷贝与计算的流水线重叠要显式设计，否则 GPU 在等数据、CPU 在等 GPU，两边空转。prefill/decode 的性能差异（LLM 场景）根源在这：prefill 是大 batch 矩阵计算（并行宽度大、计算密度高），decode 是逐步小计算反复读权重与 KV cache（访存密集、并行宽度小），两者对 GPU 的压榨方式完全不同。

## 背景与代价

思想背景：SIMT 继承自 SIMD（向量机的思想，1960s 的 ILLIAC IV 到 Cray），创新在"每线程有自己的寄存器组与程序计数器表象"——编程模型看起来是标量线程（写 C 的 for 循环），硬件执行是锁步批量，抽象层把向量化的苦活藏了。GPGPU（2006 CUDA）把"图形卡"变成"通用并行处理器"是关键转折：之前的 GPU 编程要借图形 API（把数据伪装成纹理），CUDA 给了 C 的直路。

最精巧的一笔是**warp 的"表象标量、执行向量"**。程序员写的是每线程一份的标量代码，硬件按 warp 批量执行，同一个程序既是"一万个线程"又是"三百多个 warp 指令流"。编译器与硬件一起维持这个假象（分歧时的锁步分裂、汇合，都是自动的）。代价要摆明：这个模型的收益前提是**问题本身有海量数据并行**（图像像素、矩阵元素、token 序列），串行依赖强的任务（图算法的指针追逐、数据库的逐行处理）在 GPU 上水土不服，发散与访存散乱会把并行红利吃光。选型判据：计算密度（计算量/访存量）高的问题才值得上 GPU。

## 边界

- **利用率指标会骗人**。SM 利用率高可能是在空转的发散分支上打转；显存带宽高可能全是未合并的散乱事务。判断瓶颈要 warp efficiency（有效 warp 占比）+ 内存事务数（l1tex__t_sectors 之类计数器）联合看，单指标都片面，方法论见 [[工程知识/性能工程：从用户等待到资源瓶颈/处理器与内存/GPU性能取决于计算、访存、并行与通信.md]]。
- **共享内存与寄存器是零和资源**。每 SM 的共享内存与寄存器文件总量固定，kernel 用得多则同时驻留的 block 数少（occupancy 下降）。最大化 occupancy 与最大化每 block 资源是两个方向相反的目标，最优解通常在中间，且依赖负载形态，没有普适配置。
- **主机-设备传输常是隐藏瓶颈**。PCIe 带宽（几十 GB/s）远低于显存带宽（TB/s 级），数据搬进来搬出去的时间可能超过计算时间。"计算搬不动的数据、搬运便宜的计算"是反模式，修法是让数据尽量留在显存（多 kernel 串联、避免中间结果回传主机）。
- **HBM 的带宽红利依赖访问模式**。HBM 的带宽靠极宽的并行通道（数千 bit 位宽），合并访存才能喂满；散乱访问让通道闲置。内存层级优化的思路（连续访问 + 局部复用）与 CPU 的缓存优化同源，见 CPU 缓存与分支决定有效执行时间（见 [[工程知识/性能工程：从用户等待到资源瓶颈/处理器与内存/CPU缓存与分支决定有效执行时间.md]]）。

## 验证

- **合并访存实测**：同一 kernel 两种写法（线程 i 访问 data[i] vs data[i*stride]），stride=32 时 32 个线程打 32 个不同内存事务段。预期：连续访问的 kernel 耗时数倍快于 stride 访问，profiler 的内存事务数差一个数量级。数字对不上先查编译器是否已优化（常量折叠、向量化改写）。
- **分支发散实测**：kernel 内 if (threadIdx.x % 2) 分支 vs 无分支版本，预期：发散版耗时约两倍（warp 内一半线程总在空转）。把 if 换成位运算消除分支，预期耗时回落。这是 SIMT 发散税的最小实验。
- **occupancy 与资源实验**：调节 kernel 的每 block 共享内存用量，观察 occupancy 与吞吐的关系，预期：不是单调——共享内存加大（每 block 用途大）先升后降（驻留 block 数减少），拐点位置因 kernel 而异。单点最优不等于全局最优，要扫参数。

## 相关

- [[工程知识/性能工程：从用户等待到资源瓶颈/处理器与内存/GPU性能取决于计算、访存、并行与通信.md]] —— 瓶颈判断的方法论与 LLM 场景
- [[工程知识/软件构建：让变化可以理解、验证与交付/语言与运行时/CPU微架构与流水线：分支预测乱序执行对程序员的可见影响.md]] —— CPU 与 GPU 两条优化路线的对照
- [[工程知识/性能工程：从用户等待到资源瓶颈/处理器与内存/CPU缓存与分支决定有效执行时间.md]] —— 同源的内存层级优化思想
- [[工程知识/数据系统：在并发与故障中保存事实/查询与索引/列存与向量化执行：OLAP引擎快的两个来源.md]] —— SIMD 思想在 GPU 的极致形态
- [[工程知识/AI 系统工程：从模型能力到生产能力/推理服务与平台/模型量化的精度经济学：位宽精度与显存的三角.md]] —— 显存带宽的物理约束
- [[工程知识/AI 系统工程：从模型能力到生产能力/模型基础与训练/投机采样与推测解码：小模型带路大模型确认.md]] —— decode 阶段的算术强度分析
- [[工程知识/AI 系统工程：从模型能力到生产能力/模型基础与训练/MoE路由的工程问题：负载不均与显存碎片.md]] —— 显存与带宽的物理层级
