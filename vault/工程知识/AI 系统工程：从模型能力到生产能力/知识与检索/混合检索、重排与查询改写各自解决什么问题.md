---
title: 混合检索、重排与查询改写各自解决什么问题
type: concept
status: active
updated: 2026-09-03
review_after: 2026-12-03
change_rate: medium
confidence: high
tags:
  - ai/rag
  - information-retrieval
  - search
sources:
  - "https://www.elastic.co/guide/en/elasticsearch/reference/current/rrf.html"
  - "https://arxiv.org/abs/2004.12832"
  - "https://dl.acm.org/doi/10.1145/1571941.1572114"
  - "https://docs.opensearch.org/latest/vector-search/ai-search/hybrid-search/rrf/"
  - "https://huggingface.co/blog/train-reranker"
---

# 混合检索、重排与查询改写各自解决什么问题

这些技术分别修复召回、排序和问题表达的不同失败层：倒排/向量负责找到候选，重排负责在候选中比较相关性，改写负责把省略或多轮问题变成可检索表达。最小对照是在同一查询集分别关闭一个组件，记录 Recall@k、nDCG、引用覆盖、延迟和成本，避免把堆组件误认为提升。
RAG 检索失败不是一个单一问题：关键词检索擅长精确术语和稀有标识，向量检索擅长语义近似，重排擅长在较小候选集中精细比较，查询改写擅长弥补用户问题表达不完整。把所有技术都堆上去会增加延迟和不可解释性，必须先判断失败发生在哪一层。

## 四层职责

| 层 | 解决 | 不解决 |
| --- | --- | --- |
| 词法/BM25 | 专名、错误码、版本号、精确短语 | 同义改写、隐含意图 |
| 向量召回 | 语义近似、自然语言表达差异 | 稀有 token、权限和事实正确 |
| 融合（如 RRF） | 合并不同召回器的候选排名 | 候选集合本身没有目标文档 |
| Cross-encoder/重排 | 用 query 与文档联合编码精排 | 大规模全库搜索、过期/越权文档 |

查询改写、分解、扩展或假设性答案可以提高召回，但会引入模型幻觉、额外循环和成本。每个改写都应保留原问题、改写文本、召回来源和最终证据，便于判断收益是否来自真实覆盖。

Cross-Encoder 应放在候选召回和权限/版本过滤之后，只对 top-N 候选做联合编码；它通常比双编码器更能判断 query 与段落的细粒度关系，但延迟随候选数量增长，分数也不能跨模型直接比较。候选规模、批处理、领域迁移和 p95 延迟必须在同一评测集上做对照。

## 诊断顺序

1. 先用标注查询判断目标文档是否存在、是否有权限、是否在当前索引版本。
2. 比较 BM25、向量和融合的 Recall@k；若候选没有目标，先修切分、embedding、过滤或查询。
3. 若目标在候选但排名靠后，再评估重排模型和候选数量。
4. 最后才引入查询改写/多跳；同时记录额外 TTFT、token、失败循环和引用质量。

## 生产边界

任何检索器都不能绕过 ACL、版本和删除过滤。分数不能跨模型、跨索引版本直接比较；阈值需要按领域和查询类型校准。低置信度或证据冲突时，系统应拒答、请求澄清或交给人工，而不是用更多生成掩盖召回失败。

关系：[[工程知识/AI 系统工程：从模型能力到生产能力/知识与检索/RAG数据管道从文档到可引用证据]] · [[工程知识/AI 系统工程：从模型能力到生产能力/知识与检索/检索从一次查询演进为有状态的证据获取]] · [[工程知识/AI 系统工程：从模型能力到生产能力/质量与运营/RAG评测要分离检索质量与生成质量]]

## 可运行示例：同一查询在四层的不同命运

查询"如何回滚已发布的事务性消息"，看每层各自贡献什么：

```python
query = "如何回滚已发布的事务性消息"
# 第 1 层 查询改写：LLM 改写成检索友好形态
#   "事务性消息 回滚 补偿 逆向消息 撤回" （原查询里"回滚"在消息域叫"补偿/撤回"）
#   → 贡献：词汇鸿沟（用户词 vs 文档词）补齐

# 第 2 层 召回（混合）：
#   向量通道 top-50：语义近邻（"消息补偿机制设计""发件箱模式与回滚"）
#   关键词通道 top-50：精确命中（含 "事务性消息" 的规范文档、API 参考）
#   RRF 融合：d = Σ 1/(60+rank)，两通道都排前的文档浮上来
#   → 贡献：向量管"说得不同但意思近"，关键词管"术语必须精确"

# 第 3 层 重排（cross-encoder）：对融合后 top-100 逐对打分，取 top-10
#   → 贡献：双塔/向量是"先编码后交互"（快但粗），重排是"逐对交互"（慢但准）
#   典型：召回 50 名之外的正确文档被重排拉回前 3

# 第 4 层 生成：top-10 进上下文
```

跑一遍的收获：四层的失败模式互不相同（改写失去原意、召回漏、重排慢、上下文超限），诊断先分层再定位——跳过第 2 层直接调重排，是检索优化最常见的力气错花。

## 量化锚点与案例回流

各层的收益量级（公开评测的典型区间）：纯向量换成混合召回，召回率提升 10-20 个点（术语精确匹配的场景更大）；加 cross-encoder 重排，top-3 命中率再提 5-15 个点；查询改写在词汇鸿沟大的域（专业术语、跨语言）提升 10-30 个点，在口语化问答域收益小。成本账同样分层：改写每查询多 1 次 LLM 调用（百毫秒级），重排 top-100 多 100 次 cross-encoder 前向（十毫秒到百毫秒级）——按延迟预算倒推每层开不开。与 [[工程知识/AI 系统工程：从模型能力到生产能力/知识与检索/向量数据库不是RAG本身]] 的衔接：索引选型只是第 2 层内部的一个变量；与 [[工程知识/AI 系统工程：从模型能力到生产能力/知识与检索/GraphRAG适合关系与全局问题但成本更高]] 的分界：本篇四层管局部问题，全局问题走图谱路线。

验证的锚点：各层收益要与单组件开关对账——逐层关闭混合召回、重排与查询改写，预写指标变化应能归到被打开的那一层，归不上就说明该层没有独立贡献。

## 验证

1. 单组件对照：在同一查询集分别关闭混合召回、重排与查询改写，记录每层的 Recall@k、nDCG、引用覆盖、延迟与成本，提升对应到预期的那一层才说明归因成立。
2. 分层定位：对失败查询先判断问题位于候选遗漏、候选排序还是问题表达哪一层，确认改动只针对该层。
3. 上限核对：目标文档不在候选集时确认重排无法补回，据此回到召回层处理。

## 机制与本质

混合检索与重排的机制层：检索失败分别发生在候选遗漏、候选排序与问题表达三个位置，词法检索依据精确 token 匹配、向量检索依据表示空间中的邻近、重排依据查询与段落的联合编码，各自能修复的范围互不重叠。在候选集合里没有目标文档的情况下，重排只能重新安排已有的候选，无法补回缺失的材料。

## 要解决的问题

回答质量不佳时把所有检索手段一起加上，延迟与成本上升而质量没有变化，也无法判断究竟缺哪一环。本篇回答：候选生成、相关性排序与问题表达各自修复的是检索链路的哪一段失败、各项在什么条件下才有收益、关闭其中一个组件时应当观察哪些指标，以及怎样避免把组件的堆叠当成质量的提升。

