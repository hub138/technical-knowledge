---
title: RAG 数据管道从文档到可引用证据
type: playbook
status: active
updated: 2026-09-03
review_after: 2026-12-03
change_rate: medium
confidence: high
tags:
  - ai/rag
  - ai/data
  - information-retrieval
  - ai/operations
sources:
  - "https://arxiv.org/abs/2005.11401"
  - "https://docs.llamaindex.ai/en/stable/module_guides/loading/"
  - "https://www.elastic.co/guide/en/elasticsearch/reference/current/index-modules-similarity.html"
  - "https://iceberg.apache.org/"
  - "https://delta.io/"
  - "https://hudi.apache.org/"
---

# RAG 数据管道从文档到可引用证据

它解决的是原始文档不能稳定检索、授权和引用的问题。核心机制是把来源解析成带结构、版本、ACL 和位置的知识单元，再建立可重建索引；最小例子是修改一条政策并撤权，验证旧 chunk 不再被召回、引用能回到新版本原文。
## 核心拆解

| 维度 | 回答 |
| --- | --- |
| 要解决的问题 | 原始文档不能直接被稳定检索和引用；解析丢结构、版本残留或权限遗漏会让模型获得看似相关却错误的上下文。 |
| 本质 | 把来源转换成带结构、版本、权限和位置的证据单元，并让它们可以被召回、过滤、引用和删除。 |
| 做法 | 发现来源，解析并保留标题/表格/代码，按语义切分，写入 ACL 和版本元数据，再建立词法、向量或结构索引。 |
| 效果与代价 | 证据可追溯、可更新、可按权限过滤，回答更容易审计；代价是解析、重建、存储和索引发布流程变重。 |
| 边界 | Embedding 或切块优化不能修复错误来源、实时状态和业务计算；索引命中也不等于模型实际使用并正确引用。 |
| 验证 | 抽样回到原文核对结构和位置，分别测召回、过滤、重排、引用支持、删除传播和生成拒答。 |

RAG 的质量上限由“哪些内容被正确地变成可检索、可授权、可引用的证据”决定。把文件切成几段再做向量搜索只是其中一步；解析错误、层级丢失、权限遗漏、旧版本残留和引用无法回指，都会让生成模型在看似有上下文的情况下得出错误答案。

## 完整管道

```text
来源发现 -> 解析/OCR -> 结构恢复 -> 切分
       -> 元数据/ACL -> embedding + 词法索引
       -> 版本化写入 -> 查询召回/过滤/重排
       -> 证据组装 -> 生成/引用 -> 反馈与重建
```

每个 chunk 至少需要稳定的 `document_id、version、source_url、section、offset、acl、updated_at`。表格、代码、列表、标题和脚注应保留结构；如果只能引用 chunk 文本却无法回到原文位置，系统难以审计。

## 更新与删除语义

用 `source_updated_at -> indexed_at -> query_visible_at` 描述新鲜度。更新不能只追加新 chunk：旧版本何时不可见、删除/撤权多久传播、embedding 变更是否需要重建，都必须有明确状态。缓存键要包含租户、权限版本、文档版本和必要的时间切片。

来源规模较大时，可以用数据库快照、对象存储清单或 Iceberg/Delta/Hudi 一类开放表格式表达一批文件的 schema、版本和提交边界，再把 `source_version -> index_version -> evidence_id` 写入索引元数据。它们解决来源集合的增量、删除和回溯，不负责 BM25、ANN 或重排；来源快照更新后仍要验证检索索引已经同步。

## 解析和切分的取舍

- 固定 token 长度简单，但可能切断定义、表格和因果关系。
- 按标题/段落递归切分保留语义层级，但会产生长度不均和长段落。
- 父子 chunk、滑窗和摘要可改善召回，却增加重复、token 和引用合并成本。
- OCR、HTML 清洗和代码解析都可能静默丢失内容；必须保留原文哈希和解析告警。

## 发布门禁

发布一个索引版本前，用代表性查询检查召回、权限、版本、引用位置、删除传播、重复文档和跨租户隔离；再测生成是否只使用证据、是否正确拒答和是否能解释证据不足。RAG 的索引版本和生成模型版本要一起进入追踪，不能只记录最终回答。

关系：[[工程知识/AI 系统工程：从模型能力到生产能力/知识与检索/RAG的核心是选择可引用证据]] · [[工程知识/AI 系统工程：从模型能力到生产能力/知识与检索/混合检索、重排与查询改写各自解决什么问题]] · [[工程知识/AI 系统工程：从模型能力到生产能力/知识与检索/检索从一次查询演进为有状态的证据获取]] · [[工程知识/AI 系统工程：从模型能力到生产能力/知识与检索/多模态RAG：图表、PDF与音频的证据不能只靠文本化]]
