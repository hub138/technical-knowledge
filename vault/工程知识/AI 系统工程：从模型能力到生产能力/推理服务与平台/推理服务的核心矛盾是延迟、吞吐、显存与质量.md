---
title: 推理服务的核心矛盾是延迟、吞吐、显存与质量
status: active
type: concept
updated: 2026-09-03
change_rate: medium
confidence: high
review_after: 2027-03-03
tags:
  - ai/serving
  - performance
sources:
  - "https://arxiv.org/abs/2309.06180"
  - "https://docs.vllm.ai/en/latest/serving/metrics.html"
  - "https://nvidia.github.io/TensorRT-LLM/performance/perf-overview.html"
---

# 推理服务的核心矛盾是延迟、吞吐、显存与质量

推理服务要解决的是有限 GPU/CPU 资源如何同时满足交互延迟、并发吞吐、上下文长度和输出质量。任何优化只改善其中一项都可能把压力转移到队列、KV cache、网络或质量回归；因此容量结论必须绑定具体流量分布和 SLO。
LLM 服务不是普通的一次前向计算。请求长度不同、输出长度未知，prefill 与 decode 的资源特征不同，KV cache 又让请求状态长期占用显存。优化任何单项指标，都可能把代价转移到另一项。

## 先定义用户真正感受到的指标

设请求到达时间为 t0，首个输出 token 到达为 t1，最后一个 token 到达为 tn：

- **TTFT**（Time to First Token）：t1 - t0。由排队、输入处理、prefill 和网络共同决定。
- **TPOT**（Time per Output Token）：通常指首 token 之后，后续 token 的平均间隔。
- **ITL**（Inter-token Latency）：相邻输出 token 的间隔分布，比平均 TPOT 更能暴露抖动。
- **端到端延迟**：tn - t0，同时受输入长度、输出长度和调度影响。
- **吞吐**：每秒请求或 token；必须注明输入/输出长度分布和并发，否则不可比较。

平均值会掩盖排队和长尾。生产 SLO 至少看 p50/p95/p99 TTFT、ITL、端到端延迟、错误率、每请求 token/成本与质量指标。

## 容量从哪里消耗

~~~mermaid
flowchart LR
    Q[等待队列] --> P[Prefill<br/>处理全部输入]
    P --> K[(KV Cache)]
    K --> D[Decode<br/>逐 token 生成]
    D --> O[流式输出]
    P --> G[GPU 计算与显存带宽]
    D --> G
~~~

显存粗略由四部分构成：

```text
模型权重 + KV cache + 激活/临时工作区 + 运行时预留
```

权重量化主要减少第一项；PagedAttention、分页和回收策略主要改善 KV cache 的碎片与利用率；算子融合和 CUDA Graph 主要减少执行开销。它们不会自动减少输出 token，也不会自动保持质量。

## 四组核心权衡

| 优化动作 | 可能收益 | 常见代价 |
| --- | --- | --- |
| 增大批次 | 提高 GPU 利用率和吞吐 | 排队、TTFT 和单请求长尾上升 |
| 增加并发序列 | 隐藏执行空隙 | KV cache 压力、抢占与重算上升 |
| 权重/KV 量化 | 降显存、提高带宽效率 | 精度变化、校准和内核支持约束 |
| 张量/流水线并行 | 容纳更大模型或提高吞吐 | 跨卡通信、气泡和部署复杂度 |
| speculative decoding | 减少大模型 decode 步数 | 草稿模型成本、接受率不稳定 |
| prefill/decode 分离 | 各自独立扩缩和调度 | KV 传输、网络与队列复杂度 |

## 容量规划不能只看 GPU 利用率

高 GPU 利用率可能意味着系统高效，也可能意味着队列已经失控；低利用率可能来自流量不足、CPU/tokenizer 瓶颈、网络阻塞或小批次。容量实验必须固定：

- 模型与精度；
- 输入、输出长度分布；
- 并发和到达模式；
- 硬件、拓扑与运行时版本；
- 质量门槛与采样参数；
- 预热、缓存状态和测试持续时间。

寻找“最大吞吐”时，还要满足既定 TTFT/ITL SLO；超过这个边界的吞吐没有用户价值。

## 定位顺序

1. 从端到端 trace 分开客户端、网关、队列、tokenization、prefill、decode 和流式发送。
2. 看请求长度、batch 大小、running/waiting 数、KV cache 使用率与抢占。
3. 再看 GPU kernel、算力、显存带宽、互联、CPU 和网络。
4. 只改变一个变量重跑代表性负载，并同时比较质量和成本。

相关：[[工程知识/AI 系统工程：从模型能力到生产能力/推理服务与平台/Prefill与Decode决定LLM服务如何调度]] · [[工程知识/性能工程：从用户等待到资源瓶颈/观测与诊断/性能问题定位]] · [[工程知识/AI 系统工程：从模型能力到生产能力/推理服务与平台/FlashAttention是IO感知的精确算法，不是近似]]

## 验证

1. 指标分测：在给定流量分布下分别测 TTFT、TPOT 与相邻 token 间隔分布，确认结论绑定到具体 SLO 而不是单次吞吐数字。
2. 压力转移核对：调大批处理之后确认显存占用与排队延迟随之上升，优化项没有把代价转移到别的指标。
3. 长度分档：按输入与输出长度分档统计延迟与显存，确认长上下文请求的 KV cache 占用与预期一致。

## 机制与本质

推理服务的机制层：输入长度各不相同、输出长度事先未知，prefill 需要整段输入的计算、decode 逐 token 生成，KV cache 又把每个请求的状态长期固定在显存里。显存一旦接近上限，批次大小、并发序列与上下文长度就只能此消彼长；延迟、吞吐、显存与质量之间的制约来自这套结构，而不是某个参数的取值。

## 要解决的问题

调大批次能提升吞吐却让交互请求的等待变长，拉长上下文能提升回答质量却把显存占满，任何一项指标的改善都会把压力转移到别处。本篇回答：延迟、吞吐、显存与质量之间怎样相互制约、容量结论为什么要绑定具体流量分布与目标、四项指标里哪一项在给定场景下是约束条件。

