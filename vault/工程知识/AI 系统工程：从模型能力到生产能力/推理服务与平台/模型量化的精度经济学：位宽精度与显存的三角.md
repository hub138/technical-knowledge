---
title: 模型量化的精度经济学：位宽精度与显存的三角
type: concept
status: active
updated: 2026-09-22
review_after: 2027-09-22
change_rate: medium
confidence: high
tags:
  - ai/inference
  - quantization
  - performance
sources:
  - "https://arxiv.org/abs/2208.07339"
  - "https://github.com/ggerganov/llama.cpp"
  - "https://docs.nvidia.com/cuda/pdf/CUDA_C_Programming_Guide.pdf"
---

# 模型量化的精度经济学：位宽精度与显存的三角

## 要解决的问题

推理服务的显存账是三元方程：模型权重、KV 缓存、激活值共享一块 GPU 显存（显存层级的物理约束见 [[工程知识/AI 系统工程：从模型能力到生产能力/模型基础与训练/GPU执行模型与显存层级：SIMT与合并访存.md]]）。权重占大头且只读，量化压缩权重是最高杠杆的降本手段。但位宽压缩有代价：INT8/INT4 的表示误差让模型输出偏离 FP16 基线，部分任务（数学、代码）比闲聊敏感得多。量化要解决的问题是**找到位宽、精度损失、显存收益的三角平衡点**，让每 bit 的压缩都换到可接受的质量损失。这是推理服务成本模型（见 [[工程知识/AI 系统工程：从模型能力到生产能力/推理服务与平台/推理服务的单位请求成本从token定价推出来.md]]）里最硬的一个杠杆。

## 机制

**量化的数学：线性映射与反量化**。FP32/FP16 权重映射到低 bit 整数：scale×q+b（q 为 INT4/INT8 整数，scale/b 为该组的缩放与零点）。存储从 16 bit/参数降到 4-8 bit/参数，7B 模型权重从 14GB 到 3.5-7GB。推理时反量化回浮点计算（或直接整数算术，INT8 Tensor Core）。误差来源：量化噪声（映射不可逆，位于两个整数之间的值被迫取整）、离群值难题（权重分布长尾，少数大值撑大 scale 压垮多数小值的分辨率）。

**三条路线**。
- 训练后量化（PTQ）：训完直接压。简单但精度损失不可控，大模型对它意外友好（涌现的离群值通道可以被平滑迁移，如 SmoothQuant）。
- 量化感知训练（QAT）：训练时模拟量化噪声，精度最好但要重训，成本高，只用于极端压缩（2-3 bit）。
- 混合精度：敏感层（首尾层、attention 输出投影）保 FP16，其余量化。精度损失曲线的非均匀分布决定了“哪里该花 bit”。

**KV 缓存与权重的分账**。量化权重省的是静态占用（一次性的），KV 缓存随 batch 与上下文长度线性涨（KV 缓存的显存账见 [[工程知识/AI 系统工程：从模型能力到生产能力/模型与上下文/KV缓存与上下文长度的显存账：为什么长上下文贵.md]]）。长上下文服务里 KV 可能反超权重成为大头，只压权重不压 KV 的显存收益封顶。KV 量化（FP8/INT4 KV）是长上下文服务的第二杠杆，但 KV 误差直接进注意力计算，比权重量化更伤精度，要按任务实测。

**算力代际的红利**。硬件整数吞吐与显存带宽决定量化的实际收益：INT8 Tensor Core 吞吐是 FP16 的 2 倍（代际相关），显存带宽受限场景（decode 阶段，见批处理与容量（见 [[工程知识/AI 系统工程：从模型能力到生产能力/推理服务与平台/批处理、KV缓存、量化与并行如何改变服务容量.md]]））里，权重带宽需求减半直接近线性提速 decode。量化的收益结构是三维的：显存（能装更大模型或更大 batch）、带宽（decode 提速）、算力（INT8 单位算力更便宜）。

## 背景与代价

思想背景：量化与信号处理的量化噪声理论同源（1950s 的 PCM 编码），神经网络量化研究在 2015 前后因移动端部署兴起（TensorFlow Lite、INT8 推理）。大模型时代转折点是 LLaMA 开源后的社区量化生态（llama.cpp 的 GGUF、GPTQ、AWQ），把 PTQ 从论文技术变成一键操作。AWQ（激活感知量化）的核心洞察：1% 的显著通道决定量化误差，保护它们（按激活分布缩放权重）比全模型 QAT 便宜几个数量级。

最精巧的一笔是**“离群值通道的激活感知保护”**。权重分布的长尾离群值撑大 scale 是精度损失的主因，AWQ 观察到离群值在激活侧（少数通道的激活值系统性超出正常范围），于是把激活的幅度迁移到权重侧（通道等比缩放，数学上等价），让权重分布回到可量化范围。不重训、不微调，一次前向统计就完成。这一笔把 PTQ 的精度从“能跑”拉到“接近 FP16”。代价要摆明：位宽压缩比是不可绕开的物理限制，4 bit 以下（3/2 bit）的精度损失在数学与代码任务上断崖，闲聊与摘要感知不到——质量评测必须覆盖任务谱，单一 benchmark 覆盖不了；量化的“免费午餐”只到 4 bit，再往下要 QAT 与极端技巧，工程性价比急剧下降。服务侧的隐性代价：量化模型与 FP16 基线的输出分布不同（轻微但系统），下游的输出解析、评测基线都要重新校准，混布量化与非量化实例的 A/B 结论会带噪声。

## 边界

- **精度损失是任务依赖的，没有普适安全位宽**。Perplexity 掉 2% 不代表数学正确率不掉 20%。上线路径：目标任务的评测集（构建方法见 [[工程知识/AI 系统工程：从模型能力到生产能力/质量与运营/评测集的构建与维护：从场景采样到难度分层.md]]）上量化前后对照，给“可接受损失”定业务阈值，再看显存收益值不值。
- **W4A16 与 W8A8 是不同物种**。压权重量化（W4A16：省显存与带宽，计算仍 FP16）与全整数流水线（W8A8：权重激活都 INT8，吃 Tensor Core 红利）服务不同目标：前者是消费级卡跑大模型，后者是数据中心吞吐。选型时先分清目标，别被“量化”一个词混过去。
- **动态量化与静态量化的精度/开销分界**。动态量化（推理时实时算激活 scale）精度好但有额外开销；静态量化（校准集预先定 scale）快但分布漂移（输入域变化后 scale 失配）会静默掉精度。输入分布稳定的服务用静态，开放域用动态或定期重校准。
- **量化与并发推理的交互**。Continuous batching 的吞吐收益依赖大 batch，而 batch 涨激活显存涨——压权重省的空间被激活吃回一部分。容量规划要按“权重+KV+激活峰值”三维算，见批处理与容量的容量模型（上文链接）。

## 验证

- **精度对照实验**：同一模型 FP16 vs INT8 vs INT4（GPTQ/AWQ 各一），在数学、代码、闲聊三类评测集跑对照。预期：闲聊差异小（<2%），数学与代码在 INT4 出现可测损失（5-15%），AWQ 普遍好于朴素 PTQ。任务谱的差异化损失是“无普适安全位宽”的直接证据。
- **显存与吞吐实测**：7B/13B 模型在不同位宽下的显存占用、decode 吞吐、TTFT。预期：显存近线性于位宽（14GB→7GB→3.5GB）；decode 吞吐在带宽受限区随位宽近线性提升。容量收益的实测数字是部署决策依据。
- **离群值可视化**：抽取权重与激活的分布（直方图/分位数），预期：激活侧少数通道值域远超其余（离群值通道存在），AWQ 前后权重分布对比（缩放后尾部收敛）。分布图是激活感知量化机制的直观证据。

## 相关

- [[工程知识/AI 系统工程：从模型能力到生产能力/推理服务与平台/推理服务的单位请求成本从token定价推出来.md]] —— 量化在成本模型中的杠杆位置
- [[工程知识/AI 系统工程：从模型能力到生产能力/模型基础与训练/GPU执行模型与显存层级：SIMT与合并访存.md]] —— 显存带宽的物理约束
- [[工程知识/AI 系统工程：从模型能力到生产能力/推理服务与平台/批处理、KV缓存、量化与并行如何改变服务容量.md]] —— 量化与批处理的容量交互
- [[工程知识/AI 系统工程：从模型能力到生产能力/模型基础与训练/投机采样与推测解码：小模型带路大模型确认.md]] —— 另一维度的降本杠杆
- [[工程知识/AI 系统工程：从模型能力到生产能力/推理服务与平台/模型路由与级联：小模型过滤大模型兜底的省钱结构.md]] —— 单位成本侧的降本杠杆
- [[工程知识/AI 系统工程：从模型能力到生产能力/模型基础与训练/混合精度与数值稳定：低位宽训练的溢出与下溢管理.md]] —— 训练侧的低位宽：溢出与下溢如何决定量化能压到几位