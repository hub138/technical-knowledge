---
title: 评测集的构建与维护决定所有评测的地基
type: concept
status: active
updated: 2026-09-21
review_after: 2027-03-21
change_rate: medium
confidence: high
tags:
  - ai/evaluation
  - dataset
  - mlops
sources:
  - "https://arxiv.org/abs/2104.14337"
  - "https://huggingface.co/docs/datasets/"
  - "https://paperswithcode.com/datasets"
---

# 评测集的构建与维护决定所有评测的地基

Agent 评测（见 [[工程知识/AI 系统工程：从模型能力到生产能力/质量与运营/Agent评测必须覆盖轨迹而不只看最终答案]]）、RAG 评测（见 [[工程知识/AI 系统工程：从模型能力到生产能力/质量与运营/RAG评测要分离检索质量与生成质量]]）都消费评测集，但评测集本身怎么建、怎么不腐烂，是所有评测要先解决的问题。地基烂了，上面的评测分数全部失效——而且失效方式很隐蔽：分数照常输出、趋势照常画，只是不再代表真实能力。

评测集的核心机制是**抽样代表性 + 判分客观性**，两者都随时间衰减：模型能力变化让旧题目失去区分度（天花板效应），业务分布漂移让旧样本不再代表当前流量，标注标准不一致让分数不可比。所以评测集的属性里"维护"与"构建"同权重。

## 构建的四个决策

| 决策 | 选项 | 代价 |
| --- | --- | --- |
| 来源 | 真实流量抽样 / 人工构造 / 公开集改造 | 真实流量最有代表性但含隐私；人工构造可控但分布偏窄 |
| 规模 | 覆盖意图 × 难度 × 长度的组合 | 组合爆炸，每格都要有样本 |
| 判分 | 自动指标 / 规则断言 / 人工 / LLM 判分 | 自动便宜但偏（准确率≠有用）；人工准但贵且标准漂移 |
| 防泄漏 | 题目是否进过训练数据 | 泄漏题测的是记忆，分数虚高 |

防泄漏是 AI 评测特有的一环：评测题进过预训练或后训练数据，模型"背过答案"，分数测的是记忆而非能力。公开基准（MMLU 类）全量公开、必然被训练集吸收，所以公开榜分数与自建集分数要分开看——自建集的价值就在**没有进过任何训练管道**。

分布设计上，分层比规模重要：1000 题全来自简单意图，区分度低于 300 题覆盖"简单 50% / 中等 30% / 困难 20%"。分层维度至少包含意图类别、输入长度、语言、是否带干扰项。

## 判分的客观性设计

判分是评测集腐烂最快的部分。三种判分方式的稳定性排序：**规则断言 > 参考答案比对 > LLM 判分**。LLM 判分有位置偏差与长度偏差（倾向给长答案高分），使用时必须固定判分模型版本与提示词，并把判分模型本身的判定一致率（同题重判两次，结果一致的比例）测出来——一致率低于 95% 的 LLM 判分不能用于回归门禁。

规则断言的写法来自业务不变量：输出 JSON 必须能解析、引用的文件必须存在、命令的退出码必须为 0、金额计算必须等于期望值。这与缺陷分析里的不变量思想同源（见 [[工程知识/缺陷分析：从个案到体系/正确性/静默失真的检测靠对账与不变量而不是报错]]）：把"好"定义成可检查的谓词，而不是靠印象打分。

## 维护的三个动作

1. **版本化**：评测集与模型版本分开版本化，每次评测记录"模型版本 × 评测集版本"对，分数才可比。数据集的版本治理与模型版本同构，见 [[工程知识/AI 系统工程：从模型能力到生产能力/数据与MLOps/数据集、特征与模型版本必须可追溯]]。
2. **难度复核**：定期跑头部模型，全对的那部分题目已无区分度，归档并补充更难样本。区分度按"通过率 × (1-通过率)"算，接近 0 的题（全对或全错）没有信息量。
3. **污染扫描**：新模型上线前，用评测题在模型上做成员推断式抽查（题目原文让模型续写，能续写出后文即疑似泄漏），疑似题移出或改写。

## 边界

评测集分数天然测"分布内"能力，分布外的泛化只能靠新场景的持续抽样发现——所以评测集要有"未知区"：定期放入少量真实新流量样本，人工判分后观察分数，这是发现能力盲区的唯一途径。

评测集不能证明"没有问题"，只能证明"测过的地方没问题"。把评测分数当成全面能力证明，与把单测覆盖率当质量证明是同一种错误。

## 验证

评测集健康度的一次性体检：随机抽 50 题，检查分层是否仍然覆盖当前业务分布、判分标准是否与一年前一致、是否存在全对题目。通过率 100% 的题目占比超过 20% 即该扩难度。

判分稳定性实验：同一批题让 LLM 判分模型判两次（顺序打乱），统计判定一致率。低于 95% 则改用规则断言或引入人工校准集。

## 相关

关系：[[工程知识/AI 系统工程：从模型能力到生产能力/质量与运营/Agent评测必须覆盖轨迹而不只看最终答案]] · [[工程知识/AI 系统工程：从模型能力到生产能力/质量与运营/RAG评测要分离检索质量与生成质量]] · [[工程知识/AI 系统工程：从模型能力到生产能力/数据与MLOps/数据集、特征与模型版本必须可追溯]]

- [[工程知识/AI 系统工程：从模型能力到生产能力/质量与运营/Agent评估系统的设计契约]] —— 消费评测集的评估系统契约
- [[工程知识/AI 系统工程：从模型能力到生产能力/安全与治理/模型与数据供应链需要可验证来源]] —— 防泄漏的数据侧防线
- [[工程知识/缺陷分析：从个案到体系/正确性/静默失真的检测靠对账与不变量而不是报错]] —— 规则断言的不变量来源
- [[工程知识/AI 系统工程：从模型能力到生产能力/质量与运营/提示词是代码要版本化和回归测试]] —— 评测集上的回归测试实践
- [[工程知识/AI 系统工程：从模型能力到生产能力/质量与运营/离线评测与在线评测回答不同问题]] —— 评测集在离线侧的角色与在线侧的边界
