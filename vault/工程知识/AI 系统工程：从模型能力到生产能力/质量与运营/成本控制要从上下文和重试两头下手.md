---
title: 成本控制要从上下文和重试两头下手
type: principle
status: active
updated: 2026-09-16
review_after: 2027-01-15
change_rate: high
confidence: high
tags:
  - "agent-engineering/operations"
  - "agent/optimization"
  - "learning"
sources:
  - "https://www.anthropic.com/news/anthropic-model-context-protocol"
  - "https://docs.anthropic.com/en/docs/build-with-claude/prompt-caching"
---

# 成本控制要从上下文和重试两头下手

成本控制解决的是演示阶段看似便宜的 Agent，在真实长任务、失败重试和重复上下文下突然失去预算边界的问题。先测量每步输入、输出、缓存命中和重试，再用上下文选择、稳定前缀缓存、只读结果复用和可计算预算减少无效调用；收益是能控制 P95 和最坏成本，代价是缓存失效、模型路由和质量回归都必须被持续评测。最小验证是在同一批真实任务上比较优化前后的完成率、P95 成本和 P95 延迟，而不是只报一次平均 token。

## 背景：那个 POC 很成功、上线后账单失控的 Agent

内部 POC 阶段，单次任务成本 0.15 美元，每天 20 次，月成本 90 美元——完全可接受。

上线后：

- 单次任务平均成本涨到 **1.8 美元**（12 倍）
- 月成本 **3.2 万元**

为什么涨 12 倍？三个原因叠加：

1. **上下文变长**：真实任务的对话轮次是 POC 的 4 倍，而每轮都带上完整的系统提示 + 20 个工具定义 + 历史
2. **重试变多**：真实环境有更多失败，每次失败都重跑整条链路
3. **长尾任务**：5% 的任务消耗了 40% 的成本——那些陷入循环的任务

**POC 的成本不能外推到生产**，因为长度分布完全不一样。

## 方案：成本 = 单次 token × 调用次数

```
总成本 = Σ(每次调用的输入 token + 输出 token) × 单价
```

要降成本，只有三个杠杆：**减少每次的输入、减少调用次数、用更便宜的模型。**

### 杠杆 1：减少每次调用的输入（效果最大）

每次调用都要重新发送系统提示 + 工具定义 + 历史。这部分在很多调用里是**完全重复的**。

**Prompt Caching**：把不变的前缀缓存起来，后续调用只付 10% 的价格。

```
系统提示 + 工具定义（8000 token，缓存）
+ 对话历史（增长中，部分可缓存）
+ 本轮新内容
```

要求：**把稳定的内容放在前面**，变化的内容放在后面。缓存是按前缀匹配的，顺序变了缓存就失效。

**其他减少输入的方法**：

| 方法 | 效果 | 代价 |
| --- | --- | --- |
| 精简系统提示 | 直接减少每次输入 | 可能损失质量，要测 |
| 工具分组加载 | 20 个工具 → 只注入 5 个 | 需要路由逻辑 |
| 对话历史压缩 | 见上下文工程 | 可能丢信息 |
| **引用代替全文** | 大文件内容用路径引用 | 需要时能展开 |

### 杠杆 2：减少调用次数

每次调用都有固定开销（系统提示 + 工具定义）。**减少轮次比减少每轮 token 更有效。**

- **一次提取多个字段**，而不是每个字段调一次
- **合并独立操作**：批量读文件而不是一个个读
- **允许并行**：独立的工具调用放在同一轮（多数 API 支持一次返回多个 tool_use）
- **终止循环**：见 [[工程知识/AI 系统工程：从模型能力到生产能力/Agent与工作流/模型负责判断，运行时负责执行语义]]——**循环是最大的成本黑洞**

### 杠杆 3：按难度选模型

不是每步都需要最强的模型：

| 步骤类型 | 模型选择 |
| --- | --- |
| 分类、路由、格式判断 | **小模型/便宜模型** |
| 信息提取（结构化） | 小模型通常够用 |
| 复杂推理、规划、写代码 | 强模型 |
| 总结、润色 | 小模型 |

**路由模式**：先用一个便宜模型判断任务难度，难的给强模型。

**实际效果**：混合策略常能省 60-80% 的成本，质量下降很小——因为大部分步骤并不需要最强模型。

### 杠杆 4：设预算硬约束

```go
type Budget struct {
    MaxCostPerTask   float64   // 单任务上限
    MaxCostPerSession float64  // 单会话上限
    MaxCostPerDay    float64   // 日上限
}
```

**超限怎么办**（不是简单报错）：

1. 降级到更便宜的模型继续
2. 压缩上下文后继续
3. 停止并转人工

**在接近预算时提前告警**（比如 80%），让上层有机会决策。

## 效果与代价

**收益**

- **成本可预测**：有了预算约束，最坏情况已知
- **成本常能降一个量级**：缓存 + 减少轮次 + 模型路由的组合效果显著
- **倒逼设计优化**：看成本分布能发现设计问题（比如某个工具返回过大）

**代价**

- **缓存需要精心设计**：前缀必须稳定。哪怕在前面加个时间戳，缓存就全失效了
- **模型路由增加复杂度**：要判断什么时候用大模型，且可能误判
- **降级影响质量**：超预算时降级，任务可能失败。**要权衡是省钱还是保质量**
- **监控成本本身有成本**：记录每次调用的 token 和费用，本身也是工程量

## 从什么地方做

**第一步：先测量，别猜**

记录每次调用的：

```json
{"task_id": "...", "step": 7, "model": "...",
 "input_tokens": 12400, "output_tokens": 380,
 "cached_tokens": 8200, "cost": 0.042, "latency_ms": 1820}
```

**没有这个数据，所有优化都是瞎猜。**

**第二步：看成本分布，找长尾**

按 task_id 聚合，排序。**通常会发现 5% 的任务占 40%+ 的成本。** 先优化这些（多半是循环或超长上下文）。

**第三步：上 Prompt Caching**

把系统提示和工具定义固定在前，确保前缀稳定。**这是投入产出比最高的一步**，改动小、效果大。

**第四步：找出"重复输入"**

系统提示 + 工具定义 × 调用次数 = 多少？**如果这个数字超过总成本的 50%，说明轮次太多。**

**第五步：加预算约束和告警**

单任务上限 + 日上限 + 80% 预警。**上线前必须有，否则一次失控就能吃掉一个月预算。**

## 常见错误

**错误 1：只看平均成本**

平均值掩盖长尾。**要看 P95 和最大值**——失控的往往是少数任务。

**错误 2：在系统提示里放动态内容**

"今天是 2026-09-15" 放在提示词开头 → 每天缓存失效一次。如果放在结尾，只影响最后一段。

**错误 3：为了省钱过度压缩上下文**

把关键信息也压掉了，导致模型判断错误，反而要更多轮次重试。**省了 token，亏了轮次。**

**错误 4：没有预算就上线**

POC 阶段没设上限，上线后一个无限循环任务烧掉几千美元。**预算约束是上线的前提。**

**错误 5：忽略输出 token 的单价**

输出通常比输入贵 3-5 倍。**让模型少说废话**（"不要解释你的推理过程，直接输出结果"）能省不少。

相关：[[工程知识/AI 系统工程：从模型能力到生产能力/模型与上下文/上下文工程是在有限预算内构造决策现场]] · [[工程知识/AI 系统工程：从模型能力到生产能力/Agent与工作流/模型负责判断，运行时负责执行语义]]
- [[工程知识/AI 系统工程：从模型能力到生产能力/质量与运营/评测集的构建与维护：从场景采样到难度分层.md]] —— 评测与成本的预算平衡

## 验证

1. 分布对照：在同一批真实任务上比较优化前后的完成率、P95 成本与 P95 延迟，确认结论不来自单次平均 token。
2. 逐项记账：统计每一步的输入、输出、缓存命中与重试次数，确认成本下降落在预期的项上。
3. 质量回归：优化后重跑质量评测，确认完成率没有因为缓存失效或模型路由而下降。

## 机制与本质

成本控制的机制层：每次调用的费用由送入的 token、产生的 token 与调用次数共同决定，稳定的系统提示与工具定义在每一轮重复发送，失败重试又把整条链路重放一次。上下文的选择决定单次开销，步骤数与重试策略决定次数，两者的乘积在长任务里随轮次线性累积。

## 要解决的问题

演示阶段成本可控的 Agent 进入长任务之后，输入重复累积、失败重试叠加、上下文不断增长，花费在预算之外，且无法预估最坏情况。本篇回答：每一步的输入输出与缓存命中与重试怎样分别测量、上下文的取舍与后端结果的复用各自能压缩哪一部分开销、预算怎样写成可计算的表达式，以及优化之后质量回归用什么来盯。

