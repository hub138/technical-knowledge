---
title: 投递指针的选举与失效：连接注册表的四个健壮性缺陷
status: active
type: principle
updated: 2026-09-23
change_rate: medium
confidence: high
review_after: 2028-03-23
tags:
  - agent-engineering/operations
  - agent/reliability
  - distributed-systems/reliability
sources:
  - "https://aws.amazon.com/builders-library/timeouts-retries-and-backoff-with-jitter/"
  - "https://sre.google/sre-book/handling-overload/"
---

# 投递指针的选举与失效：连接注册表的四个健壮性缺陷

凡是「把请求转发给对端去执行」的机制，都维护着一个指向当前对端的**投递指针**——一个「谁现在负责应答」的引用。AI 客户端把审批、选项题、文件读写委托给替身窗口，消息中间件把消息投给消费者，网关把连接迁移给新后端，都是这个模式。指针本身可靠不可靠，决定了整个转发链路会不会无声卡死。

指针有四个常见的健壮性缺陷，任何一个都足以让后续所有投递石沉大海，而且失败往往静默到无法察觉。它们不是某个产品的偶发 bug，而是「选举 + 转发」这类结构的通病：**谁当选、死了谁清理、投空了报不报、等多久算超时**，四问答错一题就是永久挂死。

## 要解决的问题

一个「转发给对端」的系统，最安静的死法是：指针指向了一个已经死掉的连接，但没人知道，于是每一次投递都发往尸体，调用方无限等待，日志里只有一条 warn。用户看到的只是一个永远转圈的界面。

这类故障特别难查，因为：

- 它不报错——投递「发出去了」，只是没人应答。
- 它不自愈——指针不会自己发现指错了人。
- 它影响面大——指针一旦错，之后**每一类**投递全废，不止一种请求。

## 四个缺陷

| # | 缺陷 | 后果 | 应有的行为 |
| --- | --- | --- | --- |
| 1 | 收件人指针「最后登记者当选」，且不归还 | 任何第三方连接挂接都会架空原窗口 | 指针有归属策略；原窗口活动时归还给它 |
| 2 | 连接死亡后指针不回收 | 后续所有投递指向尸体 | 连接关闭时清理指针，回退到存活的上一任 |
| 3 | 投递失败静默丢弃（只记 warn 日志） | 工具调用无限等待，用户只看到卡 | 当场报错给调用方：「投递失败：客户端已断开」 |
| 4 | 投递无超时 | 卡死没有兜底 | 可配置超时，超时后调用失败并说明原因 |

### 缺陷 1：最后登记者当选，且不归还

选举规则是「谁最后 connect 上来，指针就指向谁」，而且一旦指过去就不回来了。后果是任何一个后来者——哪怕是一个连初始化握手都没做的裸连接——只要建立连接，就把真正在干活的原窗口架空了。所有本该发给原窗口的投递，全被这个冒名者截走。

比「先到先得」更危险的是，这里连「做过 initialize」都不要求，**connect 本身就足以当选**。归属策略缺失让选举退化成了纯粹的时序竞争。

正确做法：指针选举要有归属判据（是不是真正的服务窗口、有没有完成握手），并且**原窗口重新活跃时要归还**指针，而不是谁后连谁赢。

### 缺陷 2：连接死亡后指针不回收

连接关闭时不清理指针。指针继续指着那个已经关闭的连接（尸体），之后每一次投递都发往它，全部落空。这是缺陷 1 的慢性版本：不是被别人抢走，而是主人死了没人收尸。

正确做法：连接关闭事件必须触发指针清理，回退到上一个仍存活的连接；没有存活连接时指针置空，投递直接失败而不是发给尸体。健康检查只能提供有时效的失败证据（见 [[工程知识/后端系统：在并发、失败与变化中维持服务/分布式可靠性/健康检查只能提供有时效的失败证据]]），所以清理要靠连接关闭这个确定性事件，而不是定期探活。

### 缺陷 3：投递失败静默丢弃

这是四个里最毒的一个。投递发出去、对端不应答或已断开，系统不报错，只在日志里记一条 warn，然后调用方就一直等。**静默丢弃把「明确的失败」变成了「无限的等待」**——调用方无从知道自己已经被放弃，也无法重试或改道。

它的杀伤力在于影响面：文件读写的执行委托（fs/read_text_file、fs/write_text_file）和审批询问走的是**同一条投递指针**。所以指针死了，不止审批丢，连读写工具本身都直接挂死——命令执行不受影响（后台本地跑，不走窗口），表现为「读写全挂、命令正常」这种诡异的部分失效。

正确做法：投递失败必须**当场报错给调用方**，明确说「投递失败：客户端已断开」，让调用方决定重试、降级还是终止。绝不允许吞掉失败只记日志。

### 缺陷 4：投递无超时

即使前三个缺陷都在，只要没有超时，一次投空仍然能让调用永久挂住。超时是最后一道兜底：给每次投递一个明确的死期，到点就判定失败并说明原因。

这对应 [[工程知识/后端系统：在并发、失败与变化中维持服务/分布式可靠性/超时的层次设计：连接、读写与整链路的预算分配]]——超时必须是可配置的、分层的（连接、读写、整链路各有预算），而且超时要真正触发失败，不能形同虚设。

## 四者的关系

它们是一条防线上的四层，缺一层就多一类卡死：

```text
投递指针的生命周期
├── 选举（缺陷 1）：谁该被指 —— 指错了人，后面全错
├── 回收（缺陷 2）：死了谁清理 —— 不清理，指向尸体
├── 报告（缺陷 3）：投空了报不报 —— 不报，调用方蒙在鼓里
└── 超时（缺陷 4）：等多久算完 —— 不限时，永久挂死
```

其中缺陷 3、4 是**止血层**：即使选举和回收一时修不好，只要投递失败能报错、有超时兜底，至少不会永久卡死，用户能看到失败而不是一个转圈。所以修复优先级上，3、4 先上，1、2 随后根治。

## 效果与代价

**收益**

- 投递落空时立即失败并说明原因，不再无声卡死。
- 指针随连接生命周期正确回收，不会长期指向尸体。
- 超时兜底让最坏情况也有界，便于定位和恢复。

**代价与边界**

- **报错会改变调用方行为**：以前挂着等，现在会失败。调用方必须有处理失败的分支，否则只是把「卡死」换成「崩在上游」。因此报错要和幂等、重试策略一起设计（见 [[工程知识/AI 系统工程：从模型能力到生产能力/质量与运营/失败要分类才能对症下药]]）。
- **回收要绑定确定性事件**：靠探活回收会有时效盲区，靠连接关闭事件回收才干净。
- **归属策略可能误伤**：判断「谁是真正的服务窗口」要有可靠判据，判据太松等于没判。

## 从什么地方做

1. **先加超时和报错（缺陷 3、4）**：这是止血，改动小、收益立竿见影。给投递设可配置超时，失败就地抛错给调用方。
2. **再补回收（缺陷 2）**：在连接关闭事件上清理指针，回退到存活上一任。这一步消除「指向尸体」的慢性故障。
3. **最后改选举（缺陷 1）**：给指针加归属判据和归还逻辑。这步最复杂，但前两步做完后，即使选举偶尔失手，也不会再永久卡死。
4. **用故障注入验证**：分别模拟「冒名连接挂接」「对端中途断开」「投递时对端已死」「对端永不响应」四种情况，检查是否都能在规定时间内失败并给出可读原因。

相关：[[工程知识/后端系统：在并发、失败与变化中维持服务/分布式可靠性/超时的层次设计：连接、读写与整链路的预算分配]] · [[工程知识/后端系统：在并发、失败与变化中维持服务/分布式可靠性/健康检查只能提供有时效的失败证据]]

相关（同类模式）：[[工程知识/AI 系统工程：从模型能力到生产能力/质量与运营/AI 客户端会话需要守护：审批、委托与断线的接管层]] —— 投递指针正是守护所接管的那条通道

相关（同层机制）：[[工程知识/后端系统：在并发、失败与变化中维持服务/服务设计/一路会话只能有一个回复归属：多客户端接入时的所有者绑定]] —— 指针槽位为什么只有一个位置，以及能力声明如何参与归属判定
[[工程知识/缺陷分析：从个案到体系/稳定性/订阅者断线之后路由指向谁：请求被静默丢弃的识别]] —— 指针指向已删除的连接之后，请求在传递中被丢弃的识别方法

相关（同一条通道的两端）：[[工程知识/AI 系统工程：从模型能力到生产能力/生态与选型/编辑器与编码代理的通信通道：能力协商与连接归属]] —— 交互协议这条通道的连接归属，与投递指针共用同一套槽位规则
