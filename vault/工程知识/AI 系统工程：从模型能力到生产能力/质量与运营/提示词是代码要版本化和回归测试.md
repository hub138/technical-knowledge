---
title: 提示词是代码，要版本化和回归测试
type: principle
status: active
updated: 2026-09-16
review_after: 2027-01-15
change_rate: high
confidence: high
tags:
  - agent/prompt
  - agent/testing
  - skill/mastery
sources:
  - "https://www.anthropic.com/research/prompt-engineering-overview"
  - "https://www.promptfoo.dev/docs/intro/"
---

# 提示词是代码，要版本化和回归测试

提示词版本化解决的是改一句规则修复一个案例，却在其他任务中静默改变模型行为而无人察觉的问题。把提示词、工具描述和示例放入可审查版本库，并用来自真实失败的评测集比较改动前后结果；收益是可以追溯、回滚和阻止回归，代价是评测集需要维护且不能只针对已知样本刷分。最小验证是在一次提示词变更的 CI 中同时运行目标坏样本、稳定样本和保留集，并记录每个切片的变化。

## 背景：改好了一个 case，弄坏了三个

一个 Agent 的生产环境出了个问题：某类输入会输出格式错误的 JSON。工程师在系统提示里加了一段：

```
重要：输出必须是合法 JSON，不要包含任何解释性文字。
```

这个 case 修好了。但三天后发现：

- 代码解释能力下降了（因为"不要解释"被模型理解得太宽）
- 某些任务开始输出过于简略
- 之前调好的一个边界 case 又坏了

**没有测试，所以不知道改坏了什么。** 而这个提示词已经在生产跑了三天。

## 方案：把提示词当代码管理

### 1. 版本化

提示词必须**存在文件里、进 git**，不能：

- 硬编码在代码里（改一次要发版）
- 存在数据库/配置中心（没有 diff 和 review）
- 存在某人的草稿里（无法追溯）

```
prompts/
  system.md
  tools/
    search.md
  tasks/
    bug_fix.md
    code_review.md
```

**好处**：能看到"谁在什么时候为什么改了哪句话"，能回滚。

### 2. 建评测集（回归测试的前提）

和 [[工程知识/AI 系统工程：从模型能力到生产能力/质量与运营/Agent评测必须覆盖轨迹而不只看最终答案]] 一样，但**针对提示词**：

```yaml
- id: json_output_001
  input: "提取这封邮件里的人物信息"
  expect:
    output_is_valid_json: true
    has_fields: ["name", "email"]

- id: explain_code_001
  input: "这段代码做什么？"
  expect:
    min_length: 100          # 不能被"不要解释"影响
    mentions: ["循环", "边界"]

- id: edge_case_042
  input: "<之前坏过的那个 case>"
  expect:
    output_is_valid_json: true
```

**关键：每个曾经出过的问题都固化成一条用例。** 这样改提示词时，之前修过的不会被改坏。

### 3. 改动前必须跑全量评测

```
改提示词 → 跑评测集 → 对比通过率 → 合并或回退
```

对比的是**所有用例**，不只是你关心的那个。**这正是前面那个案例缺失的：改之前不知道会弄坏什么。**

### 4. 提示词的结构化组织

长提示词要有结构，且各部分职责清晰：

```markdown
# 角色
你是代码审查助手。

# 任务
审查给定的代码变更，指出问题。

# 输出格式
<必须严格遵守>
输出 JSON：{"issues": [{"severity": "...", "line": 0, "desc": "..."}]}
</必须严格遵守>

# 约束
- 只报告真实问题，不要为了凑数编造
- 严重程度只能从 critical/major/minor 中选
- 不确定的标记为 uncertain，不要猜测

# 示例
输入：...
输出：...
```

**几个原则**：

| 原则 | 原因 |
| --- | --- |
| 关键约束放开头和结尾 | 中间位置注意力最弱 |
| 输出格式用明确分隔符包裹 | 让它和普通指令区分开 |
| 给 1-2 个示例 | 比描述有效得多 |
| 约束要可验证 | "简洁"不可验证，"不超过 100 字"可验证 |

### 5. 用测试代替"感觉更好"

改提示词后不要凭感觉判断。**跑评测集看数字**：

```
改动前：通过 78/100
改动后：通过 82/100，但 explain_code 类的平均长度从 180 降到 60
       → 通过率涨了，但副作用是解释变短
       → 判断：可接受吗？
```

**数字让取舍可讨论。**

## 效果与代价

**收益**

- **改坏能被发现**：评测集拦住回归
- **可追溯**：知道每句话为什么在那里（看 git blame 和对应的 issue）
- **可协作**：多人改同一个提示词不会互相覆盖
- **可回滚**：出问题能快速回到上一版

**代价**

- **建评测集有成本**：100 条用例 + 判据是实打实的工作
- **评测集可能过拟合**：为刷分优化，实际效果没提升。**留 holdout 集**
- **部分质量难自动化**：文风、有用性这类需要人判断
- **提示词变化的影响面广**：一句话可能影响所有任务，评测要覆盖全面

## 从什么地方做

**第一步：先把提示词从代码里抽出来**

存成 `.md` 文件，进 git。**这一步没有门槛，立刻能做。**

**第二步：建 20 条评测用例**

来自真实的失败案例。**不用 100 条，20 条已经能拦住大部分回归。**

**第三步：给每条写判据**

优先级：**可执行断言 > 结构化字段检查 > 模型评分 > 人工**。

```python
# 最好：可执行
assert json.loads(output)["issues"]

# 其次：字段检查
assert len(output.split()) < 200

# 再次：模型评分（要外部锚点）
assert judge(output, rubric) > 0.8
```

**第四步：每次改动都跑**

接入 CI。**改了 prompts/ 目录就触发评测**，通过率下降就阻止合并。

**第五步：把每次线上问题变成新用例**

修复一个线上问题时，先写一条用例复现它，再改提示词。**这样问题不会复发。**

## 常见错误

**错误 1：提示词硬编码在代码里**

改一句话要发版，导致没人敢改，或者偷偷改测试环境。**抽到文件。**

**错误 2：改完只看几个例子**

"我试了 3 个 case 都好了" —— 可能弄坏了 30 个。**必须跑全量。**

**错误 3：提示词无限增长**

每次出问题就加一段"重要：不要...", 半年后 5000 字，模型开始忽略中间的。**定期精简，合并重复约束。**

**错误 4：约束之间互相冲突**

"输出要详细" + "不要超过 100 字" —— 模型只能猜。**每加一条约束，检查是否和已有的冲突。**

**错误 5：没有 holdout 集**

所有用例都用来调优 → 过拟合。**留 20% 从不看。**

**错误 6：不同任务的提示词复制粘贴**

`bug_fix.md` 和 `code_review.md` 有 80% 重复内容，改一处忘另一处。**用共享片段（include/partial）。**

相关：[[工程知识/AI 系统工程：从模型能力到生产能力/质量与运营/Agent评测必须覆盖轨迹而不只看最终答案]] · [[工程知识/AI 系统工程：从模型能力到生产能力/模型与上下文/上下文工程是在有限预算内构造决策现场]]

相关（约定层）：[[工程知识/AI 系统工程：从模型能力到生产能力/质量与运营/Agent评估系统的设计约定]] —— 回归证据如何与任务版本绑定

验证的锚点：提示词改动要与切片对照对账——预写修好目标坏样本的同时保留集不应劣化，只盯目标样本而放过保留集劣化，与预期对不上。

## 验证

1. 切片对照：在一次提示词变更的流水线里同时运行目标坏样本、稳定样本与保留集，确认三类样本各自的前后变化都被记录。
2. 回归拦截：构造一次修好目标案例却损坏其他任务的改动，确认门禁以保留集的变化拦下这次提交。
3. 版本可回退：确认提示词、工具描述与示例都在版本库中，改动可追溯、可还原。

## 机制与本质

提示词的机制层：提示词、工具描述与示例共同构成模型每次决策的输入，改动其中一句会同时影响所有经过这段输入的任务。影响范围由输入决定而不由改动者的意图决定，因此一次改动是否只修好目标案例，只有在一组覆盖不同任务的样本上重新运行才能看出来。

## 要解决的问题

为了修一个案例改动了一句规则，其他任务上的模型行为随之改变，且没有人在改动时察觉，直到线上表现出现偏差才回溯。本篇回答：提示词、工具描述与示例怎样纳入可审查的版本记录、评测集从哪里来、一次改动需要同时观察哪些切片，以及怎样避免评测集只针对已知样本调优。

