---
title: Agent评估系统的设计约定
type: design
status: active
updated: 2026-09-06
review_after: 2026-12-04
change_rate: medium
confidence: high
tags:
  - ai/agent
  - ai/evaluation
  - ai/observability
  - engineering/evidence
sources:
  - "[[工程知识/AI 系统工程：从模型能力到生产能力/质量与运营/Agent评测必须覆盖轨迹而不只看最终答案]]"
  - "[[工程知识/AI 系统工程：从模型能力到生产能力/安全与治理/执行证据必须独立于AI结论]]"
  - "https://github.com/microsoft/AgentRx"
  - "https://github.com/alphadl/AdaRubrics"
  - "https://cheatsheetseries.owasp.org/cheatsheets/AI_Agent_Security_Cheat_Sheet.html"
---

# Agent评估系统的设计约定

Agent 评估不是给回答打一个漂亮分数，而是回答一个可复核的问题：**在给定任务、环境、权限和预算下，Agent 是否完成了目标，为什么成功或失败，哪一个最小改动值得验证？**

## 要解决的问题：评估的责任边界在哪

模型负责理解项目、选择工具、提出方案、分析失败和生成改进候选。评估运行时负责独立保存任务输入、命令、退出码、stdout/stderr、进程关系、文件差异、测试结果、产物和时间线。模型可以解释证据，不能创造证据。

本页解决的核心问题是：如何把概率性的 Agent 判断变成可复核、可比较、可回归的工程结论。核心机制是把模型结论与独立运行证据分层，再用任务约定把二者绑定到同一项目版本和执行代次；实施时先采集不可篡改轨迹，再生成维度化评估，效果是成功事实不会被后续失败覆盖，代价是需要存储、清理和隐私治理；边界是评估系统不能替业务定义正确性，必须由任务夹具或人工给出验收标准；验证看证据链是否完整、重放是否一致，以及改动在保留集和故障样本上的回归结果。

因此，下面两类事实必须分开：

| 层 | 允许回答 | 不能回答 |
| --- | --- | --- |
| Agent 结论 | “我判断这个测试不适用”“建议切换依赖源” | “命令已经执行”“代码已经覆盖” |
| 运行时证据 | 命令是否存在、退出码、日志、产物、提交和任务代际 | 业务上哪个方案更有价值 |

## 最小输入约定

- **任务**：目标、输入、成功条件、失败条件、允许副作用、预算和截止时间。
- **项目画像**：仓库、提交、构建系统、语言/运行时、依赖源、环境变量和凭据范围。
- **隔离上下文**：独立 worktree、运行目录、进程组、缓存命名空间和任务 ID。
- **评估口径**：任务类型与该类型专属的判定维度；不要把检索、代码修复和业务操作用同一张表。

## 轨迹与证据包

每个动作都要能回到以下链路：

```text
任务约定 → 模型计划 → 实际命令 → 进程/日志 → 退出码 → 文件或外部状态 → 测试/复现 → 评估判断
```

证据包至少包含：

1. `task_id`、项目提交、revision 和任务代际；
2. 实际执行的 argv（凭据脱敏）、cwd、环境摘要和开始/结束时间；
3. stdout、stderr、退出码、超时、子进程和资源回收结果；
4. 代码 diff、构建/测试报告、数据库或 API 状态、生成文件和引用来源；
5. 失败分类：编译、依赖/网络、权限、资源、产品未触发、断言不成立或观察缺失；
6. 结论、置信度、支持它的证据、反例、成本和下一次回归任务。

## 成功事实不可覆盖

一次重试失败不能抹掉此前已经确认的复现或成功。状态应当是 append-only 事件流，报告读取经过校验的投影；同一个任务的结果按 `task_id + project_revision + attempt` 绑定。旧任务不能清理仍被新任务引用的 worktree、进程或缓存。

## 按任务选择维度

| 任务 | 必须看 | 常见误判 |
| --- | --- | --- |
| 功能交付 | 验收条件、diff、构建、测试、产物 | 只看模型总结或单个命令成功 |
| 缺陷修复 | 先复现、根因、最小改动、回归 | 测试变绿但没有证明原缺陷被触发 |
| RAG/问答 | 召回、重排、引用、拒答、时效、权限、生成质量 | 只看最终答案相似度 |
| 业务操作 | 工具授权、审批、幂等、副作用、恢复 | 把工具调用记录当作业务状态已改变 |
| 多 Agent 协作 | 委托、所有权、交接、协议、跨系统失败 | 只统计最终文本，不看责任链 |
| 长期运行 | 成本、延迟、错误率、漂移、告警、回滚、人工接管 | 用一次离线样本推断线上 SLO |

## 与其他质量知识的关系

本页是评估系统的约定层，规定输入、证据、状态和改进报告如何绑定；它不替代具体评测方法。[[工程知识/AI 系统工程：从模型能力到生产能力/质量与运营/Agent评测必须覆盖轨迹而不只看最终答案]] 负责把最终结果与中间轨迹分开，回答“是否以允许的方式完成”；[[工程知识/AI 系统工程：从模型能力到生产能力/质量与运营/RAG评测要分离检索质量与生成质量]] 负责把召回、排序、引用和生成分别度量，回答“证据链的哪一段失效”；[[工程知识/AI 系统工程：从模型能力到生产能力/质量与运营/可观测性必须能重建一次决策]] 负责让评估输入可被重放，回答“这次分数能否回到真实运行”。三者共同形成：**约定定义要保存什么，轨迹说明发生了什么，专项指标解释为什么变化**。

如果一个评估结果无法定位到任务夹具、代码/模型/工具版本和原始证据，它只能作为探索性反馈，不能进入发布门禁或知识库结论。

## 改进报告格式

每条建议都要写清：**观察到的问题 → 作用机制 → 最小改动 → 预期改变的指标 → 反例/风险 → 成本 → 回归任务 → 置信度**。论文、基准或 README 只支持“值得尝试”；只有目标系统的重复运行和真实产物才能支持“当前有效”。

## 实现顺序

先实现任务夹具和不可篡改 Trace，再实现 worktree/进程/凭据隔离与证据投影；然后接入模型评审、维度化报告和图谱。`packages/agent-foundation` 提供与模型和框架无关的进程监督、JSONL transcript、RAG 上下文、记忆窗口、checkpoint 和恢复原语；它不替应用决定正确性。

## 复查触发器

当 Agent 框架、工具协议、评估 API、任务夹具格式、模型 tracing、供应商安全策略或部署边界变化时复查。若只改变页面样式，不改变证据约定，无需重写本页。

相关（学习侧）：[[工程知识/AI 系统工程：从模型能力到生产能力/质量与运营/学习活动需要结果、轨迹与迁移证据]] —— 评估结论如何转化为可迁移的学习证据