---
title: 模型提出候选，系统定义正确性
status: active
type: concept
updated: 2026-09-03
change_rate: medium
confidence: high
review_after: 2027-03-03
tags:
  - agent-engineering/models
  - agent-engineering/correctness
sources:
  - "https://arxiv.org/abs/1706.03762"
  - "https://arxiv.org/abs/2203.02155"
  - "https://openai.github.io/openai-agents-python/agents/"
---

# 模型提出候选，系统定义正确性

模型是概率性的：它输出的是"在当前上下文下高概率的续写"，不是对事实的断言。它可能非常有用，也可能在语法正确、语气自信的同时与真实世界完全不一致。**工程系统无法让模型停止具有概率性，只能把"正确性"拆成几种，并为每一种建立独立的证明。**

这一页给出这个拆分：从模型输出到业务结果要经过五道边界（结构、业务、权限、事实、任务），每一道由不同的机制负责证明。核心结论是——**结构化输出只解决第一道**，模型返回 `status: success` 既不能证明命令执行过，也不能证明用户目标完成。

## 背景：JSON 合法、语气自信，结果是编的

一个运维 Agent 被问"昨晚的定时任务有没有成功"。它返回：

```json
{
  "status": "success",
  "checked_at": "2026-09-14T08:00:00Z",
  "log_summary": "任务已完成，处理 12,483 条记录"
}
```

**格式完全合法，语气非常确定。但它是编的**——这个 Agent 根本没有被授予访问生产日志的权限，它只是"合理地续写"了一个看起来像结果的 JSON。

上线后被发现，是因为有人手动查了日志：**那个任务其实失败了**。

这个案例暴露的核心问题是：**"模型输出了 `status: success`" 和 "任务真的成功了" 之间，没有任何必然联系。** 而很多系统直接把前者当成了后者。

**正确的分工是**：模型的价值在于把开放输入转换成**候选**（理解、计划、动作建议）；系统的责任是定义**哪些候选才算正确**。

## 从模型到业务结果经过五道边界

```mermaid
flowchart LR
    M[模型输出] --> S[结构合法]
    S --> B[业务合法]
    B --> A[授权合法]
    A --> F[事实成立]
    F --> T[任务完成]
```

| 正确性 | 要回答的问题 | 谁负责证明 |
| --- | --- | --- |
| 结构正确 | JSON、类型和必填字段是否合法？ | schema 解析器 |
| 业务正确 | 参数范围、状态转移和领域约束是否合法？ | 业务代码与数据库约束 |
| 权限正确 | 当前主体是否有权读取或执行？ | 身份、策略与资源服务 |
| 事实正确 | 声称的命令、数据和外部事实是否真实存在？ | 工具结果、来源和证据记录 |
| 任务正确 | 用户目标是否真正完成？ | 可执行验收、评测器或人工确认 |

结构化输出只解决第一层。模型返回 `{ "status": "success" }` 既不能证明命令执行过，也不能证明用户目标完成。

## 模型能力来自哪里（四种来源，可信度不同）

**"模型知道" 不是一个足够精确的工程描述。** 必须继续问：这句话的信息来自哪里？因为四种来源的更新方式和可靠程度完全不同。

| 来源 | 特点 | 能更新吗 | 能撤回吗 | 典型失效 |
| --- | --- | --- | --- | --- |
| **参数知识** | 训练时学到的，覆盖广 | ❌ 不能逐条更新 | ❌ | 知识过时、记错细节 |
| **当前上下文** | 本次对话可见 | ✅ | ✅ | 被截断、被噪声干扰 |
| **检索证据** | 从外部库召回 | ✅ | ✅ | 召回错误、内容过期、越权 |
| **工具结果** | 真实执行得到 | ✅ | ✅ | 调用失败、参数错、超时 |

**具体例子**：

```
问："公司的报销流程是什么？"

参数知识：模型知道"一般公司的报销流程" → 但不知道你们公司的
检索证据：从内部 wiki 召回 → 但可能召回的是旧版流程
工具结果：调用 HR 系统 API → 最可靠，但需要权限和正确的调用

如果只依赖参数知识，模型会编一个"看起来合理"的流程 —— 这正是前面那个事故的原因。
```

**工程含义**：

- 需要**实时/专有信息** → 必须接检索或工具，不能靠参数知识
- 检索到的内容**必须带来源和时间戳**，供后续验证
- 工具结果**最接近真实**，但仍要验证调用是否真的执行了（见 [[工程知识/AI 系统工程：从模型能力到生产能力/安全与治理/执行证据必须独立于AI结论]]）

## 工程师真正要比较的不是榜单总分

模型选型应从任务分布出发，分别测量：

- 指令遵循和结构化输出稳定性；
- 工具选择、参数生成和停止判断；
- 长上下文中的证据定位与抗干扰能力；
- 中文、代码、图片或音频等实际模态；
- 拒答、越权和提示注入下的行为；
- P50/P95 延迟、吞吐、token 与整体任务成本；
- 供应商可用性、数据政策、区域、版本和弃用路径。

一个模型在通用 benchmark 上更强，不代表它在你的工具 schema、数据分布和延迟预算下更优。模型升级也不是配置替换：它可能同时改变工具偏好、输出长度、拒答策略和成本曲线。

## 正确使用推理预算与结构化输出

更高推理预算适合路径复杂、可验证且错误成本高的任务；它不适合替代缺失的事实和模糊的完成条件。结构化输出适合把模型判断交给程序，但 schema 应表达“模型可以判断的内容”，不能让模型填写它无法证明的字段。

例如模型可以输出：

```json
{
  "recommended_action": "run_tests",
  "arguments": {"suite": "integration"},
  "reason": "修改影响数据库事务边界",
  "uncertainties": ["本地依赖可能未准备"]
}
```

执行器再生成不可伪造的事实：

```json
{
  "command_id": "cmd_42",
  "exit_code": 1,
  "stdout_ref": "artifact://cmd_42/stdout",
  "stderr_ref": "artifact://cmd_42/stderr",
  "started_at": "...",
  "finished_at": "..."
}
```

模型拥有第一段的语义判断权，不拥有第二段的事实写入权。

## 常见错误

- 用 `temperature=0` 当作确定性或事实保证；它只能减少部分采样变化。
- 要求模型给自己一个置信度，并把这个数字直接当概率使用；未经校准的自报置信度没有统计语义。
- 在提示词里写“必须安全”，却没有服务端权限、参数校验和副作用控制。
- 把更长上下文当作长期记忆，忽略内容选择、冲突和时效。
- 只保存最终回答，不保存模型版本、上下文、工具轨迹和外部证据，导致失败无法重现。

## 一个稳定的应用接口

业务层不应直接依赖某个供应商的响应对象。内部接口至少显式接受任务、上下文、工具能力、输出契约和预算，并返回候选结果、工具请求、拒答或错误。模型别名和供应商特性留在适配层，业务正确性留在模型之外。

相关：[[工程知识/AI 系统工程：从模型能力到生产能力/模型与上下文/上下文工程是在有限预算内构造决策现场]] · [[工程知识/AI 系统工程：从模型能力到生产能力/安全与治理/AI可以做语义决策，系统必须守住事实边界]]
