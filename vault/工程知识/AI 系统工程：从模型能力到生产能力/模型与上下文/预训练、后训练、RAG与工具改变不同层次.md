---
title: 预训练、后训练、RAG 与工具改变不同层次
status: active
type: concept
updated: 2026-09-03
change_rate: medium
confidence: high
review_after: 2027-03-03
tags:
  - ai/models
  - ai/adaptation
sources:
  - "https://arxiv.org/abs/1706.03762"
  - "https://arxiv.org/abs/2203.02155"
  - "https://arxiv.org/abs/2005.11401"
---

# 预训练、后训练、RAG 与工具改变不同层次

模型、prompt、RAG、微调和工具不是同一条升级轴。正确选择取决于要改变的是参数中的能力、一次调用看到的信息、稳定行为，还是外部世界的真实状态。

## 模型怎样产生输出

输入先被 tokenizer 切成 token，再映射为向量。Transformer 通过注意力和前馈网络混合上下文，最终产生下一个 token 的概率分布；解码策略从分布中选择输出。这个过程解释了三条工程事实：

- 模型输出天然是条件概率结果，不是事实证明；
- 上下文长度会增加注意力计算、KV cache 占用和服务延迟；
- 温度和采样改变输出分布，不能把未知事实变成已知事实。

模型参数保存的是从训练数据中压缩出的统计规律。它能概括、组合和推断，却不能像数据库记录一样逐条查询、更新、授权和删除。

## 能力形成的阶段

~~~mermaid
flowchart LR
    P[预训练<br/>通用模式与知识] --> S[监督微调<br/>任务形式与示例]
    S --> A[偏好与安全对齐<br/>行为倾向]
    A --> I[推理时增强]
    C[上下文] --> I
    R[RAG] --> I
    T[工具] --> I
    I --> O[可验证输出或动作]
~~~

### 预训练

大规模自监督训练让模型学习语言、代码、图像与世界模式。它决定广度和基础能力，但知识有训练时间边界，来源也难逐条追踪。为了更新一个业务事实重新预训练，成本和控制粒度都不合理。

### 后训练

监督微调、偏好优化和安全训练改变模型如何响应：遵循指令、形成稳定格式、拒绝某些请求或采用特定表达。后训练适合塑造反复出现的行为，不适合承载需要实时更新、精确撤回或按用户授权的事实。

微调前要先排除三个更常见的问题：输入信息不足、任务定义含糊、输出没有确定性验证。只有在固定评测集上，微调相对 prompt/RAG 有稳定收益，且推理成本、数据治理和升级成本可接受时才值得采用。

### 推理时增强

| 机制 | 把什么带进本次决策 | 最适合 | 主要风险 |
| --- | --- | --- | --- |
| 指令与示例 | 任务、约束和少量范例 | 格式、风格、临时规则 | 冲突、注入、上下文噪声 |
| RAG | 可更新、可引用的外部证据 | 文档问答、知识密集任务 | 漏召回、过期、ACL、错误引用 |
| 工具 | 实时查询或真实动作 | 交易、搜索、执行、计算 | 越权、重复副作用、不可用 |
| 记忆 | 跨步骤或跨会话状态 | 个性化、长任务 | 污染、过期、隐私和删除 |

## 选择机制的判断顺序

1. 任务是否可以由普通程序可靠完成？可以时，模型只处理难以枚举的语义部分。
2. 缺失的是实时事实吗？用权威 API 或数据库工具；需要从非结构化语料找证据时再用 RAG。
3. 缺失的是当前任务背景吗？优化上下文选择、压缩和结构。
4. 缺失的是稳定行为吗？先用 prompt 和 schema 建立基线，再评估微调。
5. 缺失的是多步骤适应性吗？把模型放入有状态工作流，不要把更多推理轮次误当成能力本身。

## 质量必须按层定位

模型升级后答案变差，可能是能力回归；RAG 接入后仍答错，可能是数据缺失、召回失败、排序失败或证据未被采用；工具调用成功却业务错误，可能是授权或幂等问题。把所有失败都记为“模型幻觉”，就无法形成可修复的系统。

因此每次运行都应记录模型快照、prompt、检索索引、工具 schema、代码 revision 和评价结果。具体评测见 [[工程知识/AI 系统工程：从模型能力到生产能力/质量与运营/Agent评测必须覆盖轨迹而不只看最终答案]]。

相关：[[工程知识/AI 系统工程：从模型能力到生产能力/模型与上下文/模型提出候选，系统定义正确性]] · [[工程知识/AI 系统工程：从模型能力到生产能力/知识与检索/RAG的核心是选择可引用证据]]

相关（结构基础）：[[工程知识/AI 系统工程：从模型能力到生产能力/模型基础与训练/Transformer如何把序列建模成可扩展计算]] —— 注意力机制为何能并行建模序列

## 可运行示例与案例回流：四层杠杆的实录

```text
同一个"模型答不对我们的产品规格"问题，四层杠杆的介入点：

预训练（改权重，最重杠杆）：
  把产品文档混进预训练语料 → 模型"记住"规格
  成本：重新预训练（百万美元级）→ 只值得放高频核心知识

后训练（改行为分布）：
  SFT/DPO 教会"回答规格问题时引用文档"的行为
  成本：万级样本 + 数千卡时 → 改行为不改知识

RAG（改上下文供给）：
  规格文档入库，回答时检索注入 → 知识随文档更新（天级生效）
  成本：一条数据管线 → 知识新鲜度要求高的场景首选

工具（改能力边界）：
  模型不确定时调"查规格"接口 → 让系统替模型拿事实
  成本：一个工具接口 → 把"知道"外包给可验证的数据源
```

案例回流：杠杆选错的实录——产品知识更新每周一次的团队选择 SFT 灌知识，每次更新重训一轮（成本与延迟都不成立），换 RAG 后更新只是重新入库（分钟级）；反过来，客服语气风格用 RAG 硬塞示例文档，效果忽好忽坏，换后训练（几百条偏好对）行为才稳定——知识新鲜选 RAG、行为稳定选后训练，两层管的事不同。四层的成本阶梯是判据的量化面：层越深（越靠近权重）改动的成本越高、生效越慢，先用浅层杠杆解决不了的才向深层升级（与 [[工程知识/软件构建：让变化可以理解、验证与交付/架构与代码/购买与自建的决策框架：每条约束都摆上桌面.md]] 的决策框架同构——每条约束都上桌面对比）。

## 验证

1. 杠杆归因：把同一批失败样本分别用提示调整、检索补充、后训练与工具接入试验，确认改善出现在预期的那一层，其余层表现不变。
2. 成本对照：记录每一层变更的实施周期与推理开销，确认所选层是能解决问题的最浅一层。
3. 边界核对：知识类失败在该层得到修复，行为类失败没有被顺手改掉。

## 要解决的问题

模型答不好的时候，团队容易把所有问题都归到同一种手段上：继续微调、继续加检索、继续加工具，改动之后效果没有变化，也说不清问题出在哪一层。本篇回答：参数中的能力、单次调用看到的信息、稳定的行为习惯、外部世界的真实状态各自由什么手段改变，以及一项需求应该由哪一层承担。

