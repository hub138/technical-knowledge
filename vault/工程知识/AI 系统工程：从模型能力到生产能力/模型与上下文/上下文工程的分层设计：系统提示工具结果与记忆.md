---
title: 上下文工程的分层设计：系统提示工具结果与记忆
type: concept
status: active
updated: 2026-09-22
review_after: 2027-09-22
change_rate: medium
confidence: high
tags:
  - "agent-engineering/context"
  - "ai/models"
  - "agent-engineering/context"
sources:
  - "https://docs.anthropic.com/en/docs/build-with-claude/prompt-engineering/overview"
  - "https://arxiv.org/abs/2307.03172"
  - "https://arxiv.org/abs/2310.08560"
  - "https://platform.openai.com/docs/guides/prompt-engineering"
---

# 上下文工程的分层设计：系统提示工具结果与记忆

## 要解决的问题

Agent 的上下文窗口是一个**竞争性资源**：系统提示、工具定义、检索片段、工具返回结果、历史对话、记忆，全部塞进同一条有限窗口（显存与注意力的双重约束，见 KV 缓存显存账（见 [[工程知识/AI 系统工程：从模型能力到生产能力/模型与上下文/KV缓存与上下文长度的显存账：为什么长上下文贵.md]]））。每层都想多占，但窗口满 = 系统退化（注意力稀释、关键指令被挤出、成本线性涨）。上下文工程要解决的问题是：**把窗口当作有预算的分层系统来设计**，每层内容的进出、优先级、压缩都显式管理，而不是“塞满为止”。这是上下文工程是在有限预算内构造决策现场（见 [[工程知识/AI 系统工程：从模型能力到生产能力/模型与上下文/上下文工程是在有限预算内构造决策现场.md]]）的结构化展开。

## 机制

**分层模型**。把上下文按生命周期与变化率分层：
- 系统层（系统提示）：全程驻留，变化率最低。任务定义、行为约束、输出格式。占预算的 5-10%。
- 工具层（工具定义）：全程驻留，按 Agent 能力集静态定义。工具多的系统这层能占 20-40%（schema 膨胀），要控制（工具分组按需加载）。
- 检索层（RAG 片段）：任务相关时进入，完成后释放。变化率高。占 30-50%，质量决定效果。
- 轨迹层（历史对话与工具调用）：随任务增长，是预算的动态大头。要压缩（见后文）。
- 记忆层（跨会话知识）：受治理的状态（见 [[工程知识/AI 系统工程：从模型能力到生产能力/模型与上下文/记忆是受治理的状态，不是更长的聊天记录.md]]），选择性注入。占 5-15%。

**预算表与准入控制**。每层设配额（字符或 token 数），内容超出触发该层的压缩或拒绝。准入优先级（谁先被挤出去）：轨迹层最老的部分 > 低相关检索片段 > 工具层未用过的定义 > 系统层（永不被挤）。这个优先级是设计决策，不是默认行为——不设计就是隐式的“最新优先”，关键系统提示可能被长工具结果挤掉。

**轨迹压缩的四种手段**。
- 截断（最粗暴）：保留最近 N 轮。丢早期约束与目标。
- 摘要（最常用）：旧轨迹摘要成任务状态。摘要损失细节（工具调用的精确参数与结果），要在摘要里保留关键 ID 与结论。
- 外置（最结构化）：轨迹状态外置到文件/数据库，上下文只留指针与摘要。Agent 重启可恢复（状态的持久化），工具结果按需重读。
- 选择性保留（最精细）：按信息价值保留（未完成的任务、未确认的假设、用户明确要求记住的）。

**工具结果的管理**。工具返回经常是预算杀手（一个目录列表 10KB、一个网页抓取 50KB）。分层处理：原始结果外置（文件/缓存），上下文只留结构化摘要（关键字段 + 指针）；大结果按需重读（工具结果二次读取工具）。工具结果的 schema 设计要面向上下文预算（返回摘要 + 全量的两级 API）。

## 背景与代价

思想背景：上下文工程一词 2024 年才流行（Anthropic 的构建有效 Agent 指南、各家 Agent 框架的文档），但思想源头是操作系统的内存管理：分页、交换、优先级、准入控制。把窗口当内存管，把每层内容当进程管——生命周期不同（驻留 vs 换出）、优先级不同（系统级 vs 用户级）、管理动作不同（压缩 vs 释放）。这个类比不是装饰，它直接给出设计动作：预算表 = 内存配额，轨迹压缩 = 页面交换，检索准入 = 缓存替换（LRU 类），记忆治理 = 长期存储。Claude Code 的上下文管理（compact 命令、系统提示驻留）是产品化代表。

最精巧的一笔是**“轨迹外置把上下文从存储变成缓存”**。轨迹不外置时，上下文既是存储（任务状态全在这里）又是缓存（窗口有限），两个角色冲突（要完整就要大窗口）。外置后，上下文只当缓存（最近与最相关的部分），存储在外部（文件/数据库/记忆系统），Agent 通过工具重读。这一笔解开了长任务（数百轮）与有限窗口的矛盾：任务可以无限长，窗口只装当下需要的切片。代价要摆明：分层与压缩都是工程复杂度——预算表要按任务调（不同任务的层配比不同）；摘要压缩有损（关键信息可能在摘要中丢失，引发 Agent 行为漂移），压缩策略要评测（压缩前后任务成功率对比，见评测集篇（见 [[工程知识/AI 系统工程：从模型能力到生产能力/质量与运营/评测集的构建与维护：从场景采样到难度分层.md]]））；外置引入一致性负担（外部状态与上下文摘要的同步，脱同步时 Agent 决策基于过时状态）；工具两级 API 的开发成本（每个工具都要摘要版）。上下文工程不是一次性设计，是随任务演化的运营对象。

外置之后的整体结构，论文里画得比文字直白：处理器的窗口、外部的存储、连接两者的换页通道各就各位。

![MemGPT 分层内存系统：固定上下文 LLM 处理器，主上下文当缓存，外部上下文当存储](https://arxiv.org/html/2310.08560v2/memgpt_system_flow_2.svg)

图取自 [MemGPT: Towards LLMs as Operating Systems（arXiv:2310.08560）](https://arxiv.org/abs/2310.08560) 论文 Figure 3：LLM 处理器只握固定窗口（主上下文），任务状态住在外部存储（外部上下文），通过搜索与换页按需调进窗口——轨迹外置把上下文从存储变成缓存的教科书结构。

## 边界

- **系统提示不是越多越好**。指令密度过高（几十条规则）模型的遵循率下降（注意力稀释，指令间的相互冲突）。系统层要克制：任务定义 + 少量硬约束 + 格式要求，其余放工具层或检索层按需注入。指令遵循率的实测（系统提示加减指令的对照实验）比拍脑袋靠谱。
- **工具数量失控是 Agent 退化主因之一**。工具定义占预算 + 工具选择难度随数量上升（模型在 50 个工具里选对的准确率显著低于 10 个）。工具分组（按场景打包按需加载）与工具合并（功能重叠的合并）是两层治理。
- **记忆注入要按相关性过滤**。记忆全量注入（把记忆当聊天记录塞）挤占预算且引入噪声（无关记忆干扰判断）。相关性检索（当前任务与记忆条目的匹配）+ 治理（过期记忆清理，见记忆是受治理的状态（上文链接））是记忆层的准入控制。
- **长上下文模型不豁免这些工程**。128k 窗口降低了“装不下”的压力，但注意力稀释（中间内容被忽视的 lost in the middle 现象）、成本线性涨、KV 显存（见 KV 显存账（上文链接））三个约束仍在。窗口变大改变的是预算数字，不改变分层设计必要性。

## 验证

- **预算占用画像**：真实 Agent 任务跑 50 个，统计每层的 token 占比分布。预期：轨迹层随轮次增长成大头（不管理时 60%+），系统+工具层被挤压。画像数据是预算表设计的依据。
- **压缩策略对照**：截断/摘要/外置三策略跑同一长任务集，对比任务成功率与 token 成本。预期：外置成功率高（细节可重读）、截断最差（丢早期约束）、摘要居中。三策略的曲线是选型依据。
- **lost in the middle 复现**：关键信息放上下文头部/中部/尾部三组对照，测试模型检索准确性。预期：中部最低（U 形曲线）。位置效应的实证是分层摆放（重要内容头尾放）的依据。

## 相关

- [[工程知识/AI 系统工程：从模型能力到生产能力/模型与上下文/上下文工程是在有限预算内构造决策现场.md]] —— 预算思想的原始表述
- [[工程知识/AI 系统工程：从模型能力到生产能力/模型与上下文/记忆是受治理的状态，不是更长的聊天记录.md]] —— 记忆层的治理展开
- [[工程知识/AI 系统工程：从模型能力到生产能力/质量与运营/评测集的构建与维护：从场景采样到难度分层.md]] —— 压缩策略的评测方法
- [[工程知识/AI 系统工程：从模型能力到生产能力/生态与选型/精调与RAG的边界：什么时候微调更划算.md]] —— RAG 片段的上下文预算管理
