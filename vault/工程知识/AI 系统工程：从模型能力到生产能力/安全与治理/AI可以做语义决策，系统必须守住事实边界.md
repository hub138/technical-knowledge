---
title: AI 可以做语义决策，系统必须守住事实边界
status: active
type: principle
updated: 2026-09-03
change_rate: medium
confidence: high
review_after: 2027-03-03
tags:
  - "agent-engineering/correctness"
  - "agent/reliability"
sources:
  - "实践证据档案（已脱敏）"
  - "https://www.anthropic.com/research/trustworthy-agents"
---

# AI 可以做语义决策，系统必须守住事实边界

最能发挥模型能力的设计，既不应把规则全部固定写下，也不应让模型同时担任执行者和裁判。合理边界是：**AI 负责理解未知环境、提出方案和解释证据；确定性系统负责隔离、执行、记录和维护事实一致性。**

## 要解决的问题：语义判断与事实边界为什么必须分层

语义判断的输入空间巨大，而且会随项目、语言和环境变化。为每个仓库硬编码“应该用 Maven 还是 Gradle”“哪条测试适用”既脆弱又昂贵，模型更适合根据上下文判断。执行事实则相反：命令是否存在、退出码是什么、写入绑定哪个 revision，这些必须有唯一、不可伪造的答案，不能由语言模型自由生成。

```mermaid
flowchart LR
    C[完整上下文] --> A[AI 理解与决策]
    A --> P[结构化动作提议]
    P --> G[权限、预算与事实门禁]
    G --> X[隔离执行]
    X --> E[事件、日志与产物]
    E --> A
    E --> V[独立验收]
    V --> R[最终结论]
```

## AI 负责开放语义

AI 可以动态完成：识别语言和构建系统、探测本地工具和缓存、选择依赖策略、寻找与迁移测试、判断 API 差异、提出最小复现、分析编译和运行失败、决定下一步试验，以及判断当前证据最支持哪个假设。

这些决定应输出成结构化提议，并附带理由、不确定点和预期观察。AI 可以不断修改提议，但不能回写执行事实。

## 系统负责不可委托的事实

系统至少保证：

- 每个任务绑定明确的主体、目标资源、revision、工作目录和任务代际；
- 命令、子进程、超时、退出码、stdout、stderr 和产物真实保存；
- 写操作经过 schema、业务、权限、副作用和预算检查；
- 旧任务不能清理或覆盖仍被新任务使用的资源；
- 后一次失败不能抹掉已经确认的成功步骤；
- 报告只能读取事件投影，不能接受模型自报的执行记录；
- 任务结果由独立验收条件判定，而不是由回答措辞判定；
- 凭据、网络、文件和进程权限限制在任务所需范围内。

这些约束不替 AI 做业务判断，只防止模型的文字结论冒充现实。

## 三类数据不可混写

| 数据 | 示例 | 写入者 |
| --- | --- | --- |
| 提议 | “应运行集成测试，因为事务边界改变” | AI |
| 事实 | 命令、环境、退出码、日志引用、文件哈希 | 执行器 |
| 判定 | 验收是否满足、风险是否可接受、是否可发布 | 验证器或授权人 |

提议可以被事实否定，事实可以被重新解释，判定可以因新证据而更新；但三者必须保留来源和时间，不能压扁成一个 `status: success`。

## 一个能抵抗假成功的状态模型

```yaml
claim:
  kind: tests_passed
  proposed_by: model_turn_8
evidence:
  command_id: cmd_17
  revision: 4f8c...
  exit_code: 0
  artifact: artifact://cmd_17/test-report.xml
verification:
  rule: required_suite_completed
  result: passed
```

如果只运行了 `mvn validate`，证据就不能支持“测试通过”；如果测试因依赖下载失败没有启动，结果是环境阻断，不是“测试不适用”；如果旧任务的成功证据绑定旧 revision，它不能被复用到新 revision。

## AI 也不能独自评价 AI

另一模型可以帮助判断文本质量、归类开放式失败和发现异常轨迹，但评价仍需外部锚点：参考答案、工具事实、业务规则、人工标注或任务环境。没有锚点的自我反思适合产生下一版候选，不适合作为成功证明。

## 判断边界是否放对了

问两个问题：

1. 这个决定是否依赖开放语义、未知环境或复杂权衡？如果是，优先让 AI 判断。
2. 这个结论是否应该只有一个可审计的真实答案？如果是，必须由系统记录或验证。

例如“哪个测试最能覆盖修改”属于语义判断；“该测试是否在目标 revision 上以退出码 0 完成”属于事实。边界放对后，AI 可以拥有很高行动自由，同时不能把合理叙述直接变成系统成功。

相关：[[工程知识/AI 系统工程：从模型能力到生产能力/Agent与工作流/模型负责判断，运行时负责执行语义]] · [[工程知识/AI 系统工程：从模型能力到生产能力/质量与运营/Agent评测必须覆盖轨迹而不只看最终答案]]
