---
title: 沙箱限制能力，策略限制行为
status: active
type: concept
updated: 2026-09-03
change_rate: medium
confidence: high
review_after: 2027-03-03
tags:
  - agent-engineering/security
  - agent-engineering/sandbox
sources:
  - "https://www.anthropic.com/research/trustworthy-agents"
  - "https://modelcontextprotocol.io/specification/"
---

# 沙箱限制能力，策略限制行为

沙箱解决的是进程能访问什么，策略解决的是在允许范围内什么动作仍不应执行。二者结合才能把高权限 AI 约束在任务目录、资源额度、网络出口和工具白名单内；验证要尝试越界路径、恶意结果、超时和子进程逃逸，而不是只检查正常命令。
Agent 安全不能只靠提示词拒绝危险请求。提示词影响模型行为，沙箱决定进程实际能触达什么，策略决定某个主体在当前任务中允许做什么。三者分别处理不同风险，缺一不可。

## 三层边界

| 层 | 回答的问题 | 典型机制 |
| --- | --- | --- |
| 模型行为 | 模型应该提议什么？ | 指令、训练、输入输出 guardrail |
| 授权策略 | 当前主体可以做什么？ | IAM、RBAC/ABAC、审批、风险规则 |
| 能力隔离 | 即使出错，进程最多能影响什么？ | 容器、VM、worktree、文件/网络/进程隔离 |

模型拒答被绕过时，策略仍应拒绝越权动作；策略配置错误时，沙箱仍应限制爆炸半径。

## 最小权限应随任务动态生成

不要给一个“全能 Agent”永久访问所有仓库、数据库和凭据。每次任务根据主体、目标和阶段生成能力集：只读调查阶段没有写权限；修改阶段只写独立工作区；发布阶段需要单独审批。凭据按资源和动作短时签发，不把主密钥放进通用环境变量或上下文。

## 提示注入是信任边界问题

检索文档、网页、issue、代码注释和工具结果都可能包含指令性文本。模型可能无法稳定区分“需要分析的数据”和“应该服从的指令”，因此系统必须：

- 明确标记来源，禁止外部内容改变权限和工具集合；
- 在执行前重新验证主体、资源和动作；
- 限制工具链组合，防止低风险读工具与高风险写工具形成数据外泄路径；
- 对敏感数据做最小化、脱敏和输出检查；
- 用恶意文档、工具投毒和间接注入建立回归集。

## 沙箱要隔离哪些对象

至少考虑文件系统、进程树、网络目标、凭据、系统调用、CPU/内存/磁盘、运行时间和其他任务资源。代码 Agent 还需要独立 worktree 或副本，不能让一个任务的清理操作删除另一个任务正在使用的 Git 元数据。

高权限不等于宿主机裸权限。更合理的是“任务沙箱内可执行任意项目命令，沙箱外不可触达系统关键目录、其他任务和未授权凭据”。

## 人工审批应绑定事实

审批页面需要展示动作、资源、关键参数、来源证据、预期副作用和回滚方式。批准结果绑定参数哈希、任务代际和有效期；模型修改参数或目标后必须重新审批。笼统的“允许 Agent 继续”会把控制权重新交回模型。

## 验证

安全测试不止测明显恶意输入，还要覆盖合法目标下的越权路径、跨租户检索、工具组合外泄、旧任务凭据复用、符号链接逃逸、子进程残留、DNS/重定向绕过和审批后参数替换。只有真实隔离和策略执行结果才能证明边界存在。

相关：[[工程知识/AI 系统工程：从模型能力到生产能力/Agent与工作流/工具是受约束的能力，不是提示词里的函数名]] · [[工程知识/AI 系统工程：从模型能力到生产能力/安全与治理/AI可以做语义决策，系统必须守住事实边界]]
