---
title: 超时的层次设计：连接、读写与整链路的预算分配
type: playbook
status: active
updated: 2026-09-22
review_after: 2027-09-22
change_rate: low
confidence: high
tags:
  - distributed-systems
  - distributed/communication
  - resilience
sources:
  - "https://aws.amazon.com/builders-library/timeouts-retries-and-backoff-with-jitter/"
  - "https://sre.google/sre-book/handling-overload/"
---

# 超时的层次设计：连接、读写与整链路的预算分配

## 要解决的问题

超时要解决的问题，是给每个远程调用一个明确的死期。没有超时（或超时设成默认的 30 秒、60 秒），一个慢依赖就能拖住整条请求链：线程被占着、连接被占着、上游也在等。超时、重试与幂等共同定义调用语义（见 [[工程知识/后端系统：在并发、失败与变化中维持服务/分布式可靠性/超时、重试与幂等共同定义调用语义.md]]），本文把"超时"一角展开成分层的预算分配。

## 三个层次：连接、请求、链路

**连接建立超时**（connect timeout）：TCP 握手完成的期限。典型 1-3 秒——握手是轻操作，超过 3 秒说明对端或网络已异常，等下去没有意义。

**读写超时**（read/write timeout）：连接建立后，等待响应字节的期限。这是最容易设错的一层：它应该按该接口的**正常响应时间的分布**设（P99 的 1.5-2 倍），不是按"能容忍多久"设。设短了误杀慢请求，设长了失去保护意义。读超时与写超时可以分开（流式响应的读超时要显著放宽）。

**整链路超时预算**（deadline propagation / budget）：入口请求的总期限（如 1 秒），沿调用链逐层往下分配。A 调 B 调 C，A 的预算 1s，给 B 800ms，B 给 C 500ms。每一层的超时不能超过它拿到的预算。没有预算传递时，底层超时比顶层还长，顶层超时了底层还在跑——线程、连接、下游资源全部白烧，这是分布式系统里最常见的隐性浪费。

三层的本质：**连接超时守门（异常快速发现），读写超时定速（按分布设，不按耐受设），链路预算定量（总量守恒）**。

## 背景与代价

分层超时的思想背景，是把"慢"当成一种可用性威胁而不是性能小节的认知转变。替代的是两种旧做法：一是不设超时（或超长默认），全靠客户端自己放弃；二是所有调用一个全局超时值（忽略接口响应分布差异，慢接口被误杀、快接口失去保护）。

最精巧的一笔是**预算的显式传递**。gRPC 的 deadline propagation 是代表实现：每个请求带自己的剩余时间，下游服务读到"还剩多少预算"再决定自己的动作（够就做，不够就快速拒绝）。这把超时从"每层各自设防"变成"整条链共享一份预算"，避免了层间不协调导致的资源白烧。代价：预算传递要求整条链路上的框架都支持（老框架链里传递会断），且每层要显式做预算分配决策——这是一个持续的纪律成本，不是一次性配置。

## 边界

- **超时是重试的前置**。没有明确超时的重试等于盲发。重试的完整纪律见 [[工程知识/后端系统：在并发、失败与变化中维持服务/分布式可靠性/重试的正确姿势：退避、抖动与重试预算.md]]。
- **超时后要主动取消下游**。客户端超时放弃后，服务端若不知情会继续算完。HTTP/2 的 RST_STREAM、gRPC 的 cancellation 是传递取消的标准机制；没有取消机制时，超时只救了客户端，没救服务端。
- **流式与长任务要换模型**。读超时对流式响应（SSE、日志拉取）不适用，要改成心跳 + 活性检测。批处理任务的长超时（小时级）要配合进度回报与检查点，不能简单放大读超时值。
- **重试会吃掉预算**。A 给 B 800ms 预算，B 重试两次意味着每次只有 ~400ms。预算分配要预先扣除重试放大系数。

## 验证

- **预算守恒验证**：整链注入底层慢 500ms 的故障，预期：底层在预算耗尽处被取消（trace 里能看到 cancellation 传播），顶层总耗时不超过入口预算。对不上就停下查：预算是否真的传到最底层、取消是否传播。
- **超时分布验证**：任选一个线上接口，拉它的 P50/P99 响应时间，预期读超时设置落在 P99 的 1.5-2 倍区间。对不上说明超时是拍出来的，要按分布重设。
- **白烧检测**：对比客户端超时率与服务端完成率，预期服务端完成率 ≥ 客户端超时率（服务端不该在客户端放弃后大量跑完）。
- **整链预算对不上时**：把每层的实际超时配置拉出来加总，与入口预算对比，层和超出即预算失衡的直接证据。

## 相关

- [[工程知识/后端系统：在并发、失败与变化中维持服务/分布式可靠性/超时、重试与幂等共同定义调用语义.md]] —— 超时角的总纲
- [[工程知识/后端系统：在并发、失败与变化中维持服务/分布式可靠性/重试的正确姿势：退避、抖动与重试预算.md]] —— 超时之后的动作纪律
- [[工程知识/后端系统：在并发、失败与变化中维持服务/服务设计/RPC框架的通用骨架：序列化连接管理与超时传递.md]] —— 超时传递的完整纪律
- [[工程知识/后端系统：在并发、失败与变化中维持服务/分布式可靠性/电源与时钟：UTC与单调钟的工程含义.md]] —— 超时预算的地基是单调钟

相关（等待没有上界时的形态）：[[工程知识/缺陷分析：从个案到体系/稳定性/订阅者断线之后路由指向谁：请求被静默丢弃的识别]] —— 请求被静默丢弃之后，调用方在没有超时的情况下会无限等待
