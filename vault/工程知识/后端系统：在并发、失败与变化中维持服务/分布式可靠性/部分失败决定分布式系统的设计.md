---
title: 部分失败决定分布式系统的设计
type: concept
status: active
updated: 2026-09-16
review_after: 2027-02-28
change_rate: stable
confidence: high
sources:
  - "https://raft.github.io/raft.pdf"
  - "https://www.microsoft.com/en-us/research/publication/time-clocks-and-the-ordering-of-events-in-a-distributed-system/"
  - "[[工程知识/后端系统：在并发、失败与变化中维持服务/后端系统：在并发、失败与变化中维持服务]]"
tags:
  - distributed/systems
  - reliability
  - consistency
---

# 部分失败决定分布式系统的设计

分布式系统的难点来自部分失败：节点、网络、时钟、存储和依赖可能只失败一部分。设计时先声明一致性、可用性、延迟和恢复目标，再选择协议和组件。

一次请求可能已经写入事实源，却在返回客户端前遇到网络故障；一个副本可能健康但没有最新日志；控制面可能认为实例就绪而业务依赖已不可用。把这些状态都压成“成功/失败”会让重试、补偿和告警做出错误动作，所以系统必须保留未知状态和真实证据。

## 四个不变问题

| 问题 | 必须回答 |
| --- | --- |
| 状态放在哪里 | 所有权、持久化、复制、缓存、版本和恢复 |
| 消息如何到达 | 超时、重试、顺序、重复、背压和投递语义 |
| 失败如何收敛 | 幂等、补偿、隔离、降级、人工接管和回放 |
| 变更如何兼容 | schema、协议、数据迁移、灰度和回滚 |

## 从二态变成三态

单机函数常被简化成“成功或失败”，跨网络调用却还有第三种结果：**未知**。超时只说明调用方没在期限内拿到响应，服务端可能没收到、仍在执行，或已经提交但响应丢失。把未知误判为失败会重复扣款，把未知误判为成功会漏掉补偿。

因此每个跨边界动作都要有稳定身份和可查询状态：请求用幂等键，任务用 task/attempt，消息用 event id，写入用版本。状态不能只存在调用进程内存里；进程退出后要能从事实源、事件日志或检查点重建，并清楚区分已确认成功、已确认失败和待对账。

## 一致性与复制

- **线性一致性**要求操作表现得像在单一副本上按实时顺序执行，代价通常是更高的协调和故障敏感性。
- **最终一致性**允许副本暂时不同，但必须定义收敛条件、冲突解决、读己之写和过期行为。
- **增量同步**依赖可定位的版本、日志位点或变更时间；断线后要检测日志是否仍可用，必要时回退到全量重建。
- **缓存和索引是派生状态**：写入顺序、失效、重建和删除必须有明确策略，不能把“缓存命中”当成一致性证明。

## 消息、重试与幂等

一次网络调用可能已经在服务端成功，但客户端没有收到响应，因此“只重试失败请求”并不等于不重复。

- 为业务操作生成幂等键，并在事实源或去重存储中记录处理结果。
- 重试只针对可恢复错误，设置次数、退避、抖动和总截止时间；不要让多层重试相乘。
- 明确 at-most-once、at-least-once 或更强语义的适用范围；exactly-once 通常是受限边界内的处理效果，不是全链路魔法。
- 队列要有容量上限、背压、积压告警、死信和重放流程；消费者必须能安全处理重复和乱序。

## 跨服务事务

- **本地事务 + 可靠事件**：先提交事实源，再发布可重试事件，适合多数异步流程；需要 outbox、幂等消费者和失败补偿。
- **Saga**：把长事务拆成可独立提交的步骤，为每一步定义补偿动作；补偿不是数据库回滚，必须考虑外部副作用和人工处理（见 [[工程知识/后端系统：在并发、失败与变化中维持服务/分布式可靠性/Saga把长事务拆成补偿链，代价是失去隔离性]]）。
- **TCC**：Try 预留、Confirm 提交、Cancel 释放，业务侵入较强；三阶段都必须幂等，并处理空回滚、悬挂和超时。
- **两阶段提交/共识提交**：适用边界窄，协调者和参与者故障会增加阻塞与运维复杂度，不能因为“强一致”就默认采用。

## 共识与成员发现的边界

| 机制 | 解决的问题 | 不解决的问题 |
| --- | --- | --- |
| Raft/Paxos 类共识 | 在故障模型内让副本对日志/状态顺序达成一致 | 业务事务、跨系统副作用和网络永不分区 |
| Gossip/成员传播 | 传播成员、健康和部分元数据，最终收敛 | 线性一致的写入顺序和唯一提交者 |
| 负载均衡/服务发现 | 找到可接收流量的实例 | 业务幂等、数据一致性和故障后补偿 |

不要用“集群节点数建议”“收敛秒数”或“吞吐倍数”作为脱离实现的通用结论；这些取决于网络、存储、故障模型、负载和实现版本。

## 设计检查表

- [ ] 写出数据所有权、读写路径和一致性承诺。
- [ ] 为每个外部调用写超时、取消、重试、幂等和降级路径。
- [ ] 为消息写重复、乱序、积压、死信、回放和 schema 兼容策略。
- [ ] 为跨服务流程写补偿动作、人工接管和不可逆副作用。
- [ ] 为发布和迁移准备向前/向后兼容、灰度、观测门禁和回滚方案。
- [ ] 通过故障注入验证节点、网络、依赖、存储和控制面的部分失败。

最小实验：在“提交前、提交后响应前、事件发布后”三个位置分别断开网络或杀死进程；重复客户端请求并观察事实源、事件、缓存和报告。验收标准是业务效果只发生一次、未知状态可查询、失败不会覆盖已确认成功，而不是所有步骤都返回 200。

## 关联

- 总览：[[工程知识/后端系统：在并发、失败与变化中维持服务/后端系统：在并发、失败与变化中维持服务]]
- 入口和服务发现：[[工程知识/后端系统：在并发、失败与变化中维持服务/服务设计/流量入口只负责路由与连接，不负责业务正确性]]、[[工程知识/后端系统：在并发、失败与变化中维持服务/基础设施/基础设施选型先比较责任边界与故障模型]]
- 数据与索引：[[工程知识/数据系统：在并发与故障中保存事实/数据系统：在并发与故障中保存事实]]
- 发布与测试：[[工程知识/软件构建：让变化可以理解、验证与交付/测试与交付/测试策略从风险选择证据]]
