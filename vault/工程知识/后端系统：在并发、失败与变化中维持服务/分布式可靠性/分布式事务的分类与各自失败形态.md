---
title: 分布式事务的分类与各自失败形态
type: concept
status: active
updated: 2026-09-22
confidence: high
change_rate: low
review_after: 2029-09-22
tags:
  - distributed-systems
  - correctness
  - consensus
  - reliability
sources:
  - "https://www.cs.cornell.edu/andru/papers/dsproperties.pdf"
  - "https://azure.microsoft.com/en-us/blog/azure-storage-data-distributed-storage-and-strong-consistency/"
  - "https://jepsen.io/analyses/cockroachdb-1.1"
---

# 分布式事务的分类与各自失败形态

单机事务失败就回滚，机器原子性由存储引擎保证。分布式事务把一次业务写入拆到多台机器，失败形态立刻多出几种：提交到一半网络断了、协调者宕在决定前后、参与者准备好但永远等不到裁决。要解决的问题：把"分布式事务会怎么坏"按类列清楚，每类失败对应哪个组件的哪种状态，修复路径才有的放矢。

## 机制：四类事务与失败形态表

| 事务类 | 协调结构 | 独有失败形态 | 恢复路径 |
| --- | --- | --- | --- |
| 2PC（两阶段提交） | 单协调者 | 协调者宕在 prepare 后：参与者锁资源无限等待（阻塞） | 协调者恢复后重放日志 |
| 3PC | 协调者 + 超时推进 | 网络分区下仍可能不一致（非解决阻塞的银弹） | 不一致需人工仲裁 |
| Saga | 无协调者，补偿链 | 补偿自身失败：链停在中间态 | 补偿的重试与死信队列 |
| Percolator 风格 | 主锁 + 冲突队列 | 锁未释放拖住后续事务（长尾延迟） | lockGC 定时清理僵尸锁 |

2PC 的阻塞失败是经典考点，工程上的实际答案不是 3PC 而是共识：把协调者换成 Paxos/Raft 组（CockroachDB、Spanner 的做法），协调者自身高可用，阻塞窗口从"协调者恢复为止"缩到"共识组多数派存活"。Jepsen 对 CockroachDB 1.1 的分析报告展示了真实实现里因果一致性与事务异常的边界，是验证断言时的一手对照。收益是跨机原子性与高可用协调；代价是每类方案各自的失败窗口与复杂度。

## 验证

```bash
# 用 Jepsen 的开源套件对支持分布式事务的数据库做故障注入
# （jepsen: https://github.com/jepsen-io/jepsen）
# 典型 nemesis：partition、kill、clock-skew
docker run -it jepsen/jepsen
# 对照检查：事务历史是否有内部一致性（奇怪读、丢失更新）
```
预期：正常网络下 2PC 事务全部提交或全部回滚；注入分区后，正确实现表现为事务卡住或拒绝（而非既提交又回滚）。Azure Storage 的论文给出了生产级分布式强一致事务的实现细节，包含故障注入下的不变量设计。数字对不上（出现脏读或丢失更新）说明实现存在正确性缺陷，这是 Jepsen 报告反复证实过的问题类别。

## 边界

本篇讲分布式事务的故障形态分类。单机事务的隔离与原子性机制见 [[工程知识/数据系统：在并发与故障中保存事实/事务与存储/事务隔离决定并发读写能观察到什么|事务隔离决定并发读写能观察到什么]]。共识怎么给协调者提供高可用见 [[工程知识/后端系统：在并发、失败与变化中维持服务/分布式可靠性/Raft选举与日志复制把共识落到实现|Raft选举与日志复制把共识落到实现]]。Saga 补偿链的隔离性代价见 [[工程知识/后端系统：在并发、失败与变化中维持服务/分布式可靠性/Saga把长事务拆成补偿链，代价是失去隔离性|Saga把长事务拆成补偿链]]。部分失败的总纲见 [[工程知识/后端系统：在并发、失败与变化中维持服务/分布式可靠性/部分失败决定分布式系统的设计|部分失败决定分布式系统的设计]]。

## 相关

- [[工程知识/后端系统：在并发、失败与变化中维持服务/分布式可靠性/Raft选举与日志复制把共识落到实现]]——阻塞失败的现代答案
- [[工程知识/后端系统：在并发、失败与变化中维持服务/分布式可靠性/Saga把长事务拆成补偿链，代价是失去隔离性]]——补偿链的失败形态
