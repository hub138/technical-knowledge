---
title: 一致性模型与共识解决不同问题
type: concept
status: active
updated: 2026-09-15
review_after: 2027-03-15
change_rate: stable
confidence: high
tags:
  - distributed-systems/consistency
  - distributed-systems/consensus
  - correctness
sources:
  - "https://raft.github.io/raft.pdf"
  - "https://etcd.io/docs/v3.5/learning/api_guarantees/"
  - "https://lamport.azurewebsites.net/pubs/lamport-paxos.pdf"
  - "https://jepsen.io/consistency"
---

# 一致性模型与共识解决不同问题

"一致性"这个词在日常讨论里至少混着三件不同的事：**读到的值是不是新的**（副本之间的新鲜度）、**并发写入会不会互相踩**（事务之间的隔离）、**多个节点能不能对顺序达成唯一决定**（共识）。三者需要不同的解法，混为一谈就会开错药方。

这一页把它们拆开，各自讲清楚：一致性模型有哪些、分别承诺什么；线性一致性和可串行化为什么是两回事；Raft 这类共识算法到底解决什么、不解决什么；以及生产环境里遇到"读到旧值""超卖""跨服务对不上账"时，分别该用什么手段。

默认建议是：**不要一上来就上共识**。多数所谓的一致性问题，用条件更新、写后读一致性、对账就能解决，代价低一个数量级。

## 背景：库存显示还有 1 件，两个人都买到了

库存只剩 1 件。用户 A 和用户 B 同时下单，都看到"还剩 1 件"，都下单成功。

**结果：超卖，库存变成 -1。**

排查时发现三个不同的问题被混在一起讨论：

1. **读到旧数据**：主库已经扣减，但从库还没同步，B 读到的是旧值
2. **写入没互斥**：两个扣减操作都基于"库存 = 1"做判断，都通过了
3. **副本顺序不一致**：两个从库应用更新的顺序不同，最终值不一样

这三个问题分别属于**一致性模型**、**事务隔离**、**共识**——是不同的东西，需要不同的解法。把它们统称为"一致性问题"，就会开错药方。

## 从 0 开始：一致性到底在说什么

### 一句话定义

**一致性模型 = 一个系统在有多个副本、多个客户端并发读写时，承诺"看起来像什么"。**

最强的承诺是：**"看起来只有一个副本，且操作按真实时间顺序发生"**（线性一致性）。
最弱的承诺是：**"最终大家会一样，但中间可能看到任何东西"**（最终一致性）。

### 用一个具体场景区分四种模型

假设：主库写入 `x = 1`，然后客户端 A 读，客户端 B 读。

**线性一致性（Linearizability）** —— 最强

```
t=0    主库写入 x=1，返回成功
t=1    A 读 → 一定是 1
t=2    B 读 → 一定是 1
```

写入返回成功后，**之后的所有读都必须看到新值**。就像只有一个副本。

**顺序一致性（Sequential Consistency）**

```
A 写 x=1，A 写 x=2
B 读 → 看到 2
B 读 → 一定不会看到 1（不能倒退）
```

所有操作排成一个全局顺序，且**每个客户端自己的操作顺序被保持**。但不要求这个顺序符合真实时间。

**因果一致性（Causal Consistency）**

```
A 发帖，B 回复了 A 的帖
C 读 → 不能只看到回复而看不到原帖（有因果，顺序不能颠倒）
```

只有**有因果关系**的操作保证顺序。并发的操作，不同副本可以有不同的顺序。

**最终一致性（Eventual Consistency）**

```
主库写入 x=1
t=0    B 读 → 可能是 0（还没同步）
t=500ms B 读 → 是 1
```

没有新写入后副本最终收敛，但**不承诺什么时候、以什么顺序**。

### 一张表

| 模型 | 承诺 | 典型实现 | 延迟 |
| --- | --- | --- | --- |
| 线性一致 | 像单副本，尊重真实时间 | etcd/ZooKeeper（走 Raft）、单主同步复制 | 高（要等多数派） |
| 顺序一致 | 有全局顺序，不管真实时间 | 较少单独提供 | 中 |
| 因果一致 | 有因果的不乱序 | 部分 Geo 数据库（如 CockroachDB 的部分场景） | 中 |
| 最终一致 | 最终收敛 | MySQL 异步主从、DNS、CDN | 低 |

## 最常见的混淆：一致性 ≠ 隔离级别

这是面试和工程里都最容易搞混的一对：**线性一致性（Linearizability）vs 可串行化（Serializability）**。

| | 管什么 | 针对什么 |
| --- | --- | --- |
| **线性一致性** | 单个对象、读写的新鲜度 | 副本之间 |
| **可串行化** | 多个对象、事务之间的顺序 | 并发事务 |

**例子区分**：

```
库存 = 1，两个事务同时扣减

可串行化保证了什么：两个事务等价于串行执行
  → T1: 读 1，写 0
  → T2: 读 0，拒绝（或读到 1 也写 0）
  结果：不会超卖 ✓

线性一致性保证了什么：读操作能看到最新的写
  → 但如果不加事务，两个读都读到 1，都写 0
  → 仍然超卖 ✗（因为线性一致不保证"读-改-写"原子）
```

**结论**：

- 防止超卖需要**可串行化（或行锁/原子操作）**，不是线性一致性
- 防止"读到旧值"需要**线性一致性（或读主库）**，不是可串行化
- 两者都想要 → **严格可串行化（Strict Serializability）**，代价最高

## 共识：解决"多个节点对顺序达成一致"

### 共识解决什么

Raft/Paxos 解决的问题很具体：**在部分节点故障、网络分区的情况下，让多个节点对"一串值的顺序"达成一致，并且这个决定不可撤销。**

它的核心机制是**多数派（Quorum）**：

```
3 节点：需要 2 个同意
5 节点：需要 3 个同意
```

**为什么多数派能工作**：任何两个多数派一定有交集。所以新 leader 一定能从交集里看到上一个任期的决定。

### 共识不解决什么

| 误解 | 实际情况 |
| --- | --- |
| "用了 etcd，业务数据就强一致了" | etcd 只保证**它存的那份数据**一致。你的业务数据库的一致性要自己保证 |
| "共识能让并发写入不冲突" | 共识只决定**顺序**，不判断**业务上是否允许**。两个扣减都会被提交，只是有先后 |
| "共识能保证读到最新" | 从 follower 读可能 stale。**要线性一致读必须走 leader 或用 ReadIndex/Lease Read** |

### Raft 的三个关键代价

理解这些才能判断该不该上共识：

1. **每次写入要等多数派 fsync** → 写入延迟 = 网络 RTT + 磁盘 fsync（通常 2-10ms）
2. **多数派不可用就拒绝写入** → 5 节点挂 3 个就不可写（这是**正确的**设计，不是缺点）
3. **Leader 切换有不可用窗口** → 选举期间（通常 1-10 秒）不可写

## 生产怎么用

### 场景 1：读写分离导致的 stale read（最常见的问题）

```
写 → 主库
读 → 从库（异步复制，可能有 50-500ms 延迟）

用户改完昵称，立刻刷新 → 读到旧值
```

**四种解法**：

| 解法 | 做法 | 适用 |
| --- | --- | --- |
| **读主库** | 写后立即读走主库 | 简单，但主库压力大 |
| **写后读一致性（Read-your-writes）** | 客户端记住自己的写入版本（如 LSN/binlog 位点），读时要求从库至少追上该版本 | 推荐，通用的做法 |
| **延迟读主** | 写入后 1 秒内读主库 | 粗暴但有效 |
| **会话一致性** | 同一会话内保证单调读 | 折中 |

**生产建议**：用**写后读一致性**。MySQL 可以用 `GTID_WAIT()` 或等待从库追上 binlog 位点；PostgreSQL 类似。

### 场景 2：防止超卖（需要原子性，不是一致性）

```sql
-- 错误：先查后写，两个事务都读到 1
SELECT qty FROM stock WHERE id=?;  -- 1
UPDATE stock SET qty = qty - 1 WHERE id=?;

-- 正确：条件更新，让数据库保证原子
UPDATE stock SET qty = qty - 1
WHERE id = ? AND qty >= 1;
-- 影响行数 = 0 就是没抢到
```

**关键**：把"检查 + 修改"放进**一条 SQL**，让行锁保证原子。这一条 SQL 就够了，不需要分布式锁、不需要共识。

### 场景 3：什么时候真的需要共识（etcd/ZooKeeper）

| 场景 | 需要共识吗 |
| --- | --- |
| 选主（哪个实例是 leader） | **需要** |
| 分布式锁（正确性要求高） | **需要** |
| 服务发现（有哪些实例） | 需要（但要接受短暂 stale） |
| 配置管理（少量、强一致） | 需要 |
| 业务数据（订单、库存） | **不需要** —— 用数据库事务 |
| 缓存 | 不需要 |

**判断标准**：需要"多个节点对同一件事达成唯一决定，且不可撤销" → 用共识。其他情况用数据库事务就够了。

### 场景 4：跨服务的数据一致性

不要追求跨服务的强一致（2PC 代价高且脆弱）。用：

- **Saga**：拆成本地事务 + 补偿
- **Outbox**：保证"写库"和"发消息"原子
- **对账**：最终一致 + 定期对账兜底

**对账是被低估的兜底手段**：接受短暂不一致，但每天跑一次对账发现并修复。这比追求强一致便宜得多。

## 效果与代价

| 选择 | 延迟 | 可用性 | 复杂度 |
| --- | --- | --- | --- |
| 最终一致 + 读写分离 | 低 | 高 | 低（但要处理 stale） |
| 读主库 | 中 | 中 | 低 |
| 因果一致 | 中 | 中高 | 高 |
| 线性一致（共识） | 高（+RTT+fsync） | 低（多数派不可用就拒绝） | 高 |

**默认建议**：从最终一致开始，用**写后读一致性**解决用户可见的 stale 问题。**只在真的需要"唯一决定"时才上共识。**

## 从什么地方做

**第一步：把"一致性问题"拆成具体是哪一类**

- 读到旧值 → 一致性模型问题
- 超卖/重复扣 → 事务隔离/原子性问题
- 多个节点要选主 → 共识问题
- 副本数据不同 → 复制/冲突解决问题

**用不同的解法，不要统称为"一致性"。**

**第二步：明确每个接口的读写要求**

对每个 API 问："允许读到多久之前的数据？"

- 用户信息：写后必须立即可见 → 读主或写后读
- 商品列表：允许 1 秒延迟 → 读从库
- 报表：允许 5 分钟 → 随便读

**第三步：用条件更新替代"先查后写"**

搜代码里所有 `SELECT ... 然后 UPDATE` 的地方。**这是超卖和并发 bug 的主要来源。**

**第四步：只在需要"唯一决定"时引入 etcd/ZooKeeper**

选主、分布式锁、服务注册。**业务数据一致性用数据库事务，不要用 etcd。**

**第五步：加对账**

对于资金、库存这类，**每天对账**。这是最后一道防线，能发现所有你没想到的一致性问题。

## 验证：怎么知道系统实际提供什么保证

不要相信文档，要测。

**最小验证**（推荐都做一次）：

```
1. 起 3 节点存储（比如 etcd 或主从 MySQL）
2. 并发写入同一个 key，记录每次写入的时间戳和值
3. 同时从所有节点读
4. 注入网络分区（iptables 或 tc）
5. 分区期间继续读写，记录：哪些写被接受、读到什么版本
6. 恢复分区，检查：有没有丢写、有没有冲突、最终值是什么
```

**会发现的真相**：很多号称"强一致"的系统，在默认配置下提供的是最终一致（比如 MySQL 默认的异步复制、etcd 的 follower 读）。

## 常见错误

**错误 1：以为用了共识就等于业务强一致**

etcd 保证它自己的数据一致。**你的订单表的一致性，etcd 管不到。**

**错误 2：混淆线性一致和可串行化**

以为"线性一致"能防超卖 —— 不能，防超卖需要事务隔离或原子操作。

**错误 3：从 follower 读却期望线性一致**

etcd 默认从 leader 读才是线性一致。**从 follower 读要用 `linearizable` 选项或 ReadIndex。**

**错误 4：用 2PC 追求跨服务强一致**

2PC 在协调者故障时会阻塞，且性能差。**用 Saga + 对账。**

**错误 5：认为 CAP 是"三选二"的产品标签**

CAP 只在**网络分区发生时**才需要在 C 和 A 之间选。**平时系统两者都能给。** 真正的取舍是延迟和一致性（PACELC）。

**错误 6：不测就假设**

默认配置往往是最弱的一致性。**测一次就知道实际保证是什么。**

关系：[[工程知识/后端系统：在并发、失败与变化中维持服务/分布式可靠性/部分失败决定分布式系统的设计]] · [[工程知识/后端系统：在并发、失败与变化中维持服务/分布式可靠性/Raft选举与日志复制把共识落到实现]] · [[工程知识/后端系统：在并发、失败与变化中维持服务/分布式可靠性/消息投递与业务提交需要显式的一致性边界]]
