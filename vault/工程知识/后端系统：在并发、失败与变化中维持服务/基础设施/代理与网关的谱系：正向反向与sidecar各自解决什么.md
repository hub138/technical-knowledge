---
title: 代理与网关的谱系：正向反向与sidecar各自解决什么
type: concept
status: active
updated: 2026-09-22
review_after: 2027-09-22
change_rate: low
confidence: high
tags:
  - "backend/networking"
  - "networking"
  - "backend/infrastructure"
sources:
  - "https://docs.envoyproxy.io/envoy/latest/intro/what_is_envoy"
  - "https://www.envoyproxy.io/docs/envoy/latest/configuration/overview"
---

# 代理与网关的谱系：正向反向与sidecar各自解决什么

## 要解决的问题

代理要解决的问题，是**在通信路径上插入一个可控的中间人**：流量要经过我，我就能观测、改写、路由、熔断它。这个"可控中间人"是网络治理的最小单元——所有流量治理（负载均衡、灰度、熔断、加密、审计）都建立在"流量先过我手"这个前提上。谱系的存在是因为中间人插的位置不同，解决的问题不同：正向代理替客户端出头，反向代理替服务端挡门，sidecar 替单个应用包一层。位置决定职责，这是本谱系的组织原则。

## 谱系三形态

**正向代理（forward proxy）**：插在客户端侧。客户端知道代理存在（显式配置或系统代理），代理替客户端访问外部。解决客户端侧问题：出口管控（企业内网上网审计）、地域突破（跨境加速）、隐私（隐藏客户端 IP）。HTTP 语义的分层（见 [[工程知识/后端系统：在并发、失败与变化中维持服务/服务设计/HTTP语义的分层：方法、状态码与头部的约定角色.md]]）在正向代理处有一次"再解释"——代理改写 Host 头、追加 X-Forwarded-For，约定被中间件重新包装。

**反向代理（reverse proxy）**：插在服务端侧。客户端不知道代理存在（域名解析到代理，代理背后是真服务集群）。解决服务端侧问题：负载均衡、TLS 终止（后端只管明文）、缓存加速、限流熔断。Nginx/HAProxy 的主战场。服务发现与负载均衡决定请求发往哪里（见 [[工程知识/后端系统：在并发、失败与变化中维持服务/服务设计/服务发现与负载均衡决定请求发往哪里.md]]）的"决定"动作大多由反向代理执行。

**sidecar 代理**：插在每个应用实例旁（同机/同 Pod）。应用只看到 localhost 的一个代理，所有进出流量经它。解决应用级问题：服务间 mTLS（应用零改造）、按实例的精细观测、渐进式流量切分。Envoy 是事实标准。容器隔离资源边界与镜像身份（见 [[工程知识/后端系统：在并发、失败与变化中维持服务/基础设施/容器隔离资源边界与镜像身份.md]]）给了 sidecar 部署形态——同 Pod 共享网络命名空间，sidecar 与应用的生命周期一致。

三形态的统一本质：**都是"流量必经点"的实现，差别只在插的位置与治理半径**。正向代理治理出向流量（半径=客户端的对外行为），反向代理治理入向流量（半径=一个集群的入口），sidecar 治理服务间流量（半径=单个实例的所有进出）。

反向代理的形态一看就懂——客户端只认识 example.com，不知道背后被代理挡下的服务器集群：

![反向代理：客户端与 example.com 代理通信，代理把请求转给内部服务器](https://upload.wikimedia.org/wikipedia/commons/thumb/6/67/Reverse_proxy_h2g2bob.svg/1280px-Reverse_proxy_h2g2bob.svg.png)

*图源：Wikimedia Commons「Reverse proxy h2g2bob」，作者 h2g2bob，许可 [CC0](https://commons.wikimedia.org/wiki/File:Reverse_proxy_h2g2bob.svg)*

## 背景与代价

思想背景：代理概念的祖先是电话接线员——所有通话经过总机，总机可以监听、转接、优先级排序。HTTP 代理的标准化（1996 年代）最早解决的是企业出口管控。反向代理随 Web 规模化兴起（90 年代末的负载均衡需求）。sidecar 是微服务时代的发明（2016 年 Envoy + 2017 年 Istio 把"每实例一个代理"制度化），它把反向代理的能力下放到实例级。

最精巧的一笔是**sidecar 用部署形态换治理粒度**。反向代理是集群共享的，它能做"这个集群的流量规则"；sidecar 是实例私有的，它能做"这个实例的出向流量在目标故障时快速失败"。治理粒度从集群细到实例，代价是资源与运维的乘法：1000 实例 = 1000 个 sidecar 进程（内存、升级、版本管理都是 ×1000）。服务网格的"资源开销是否值得"争议全部源于这个乘法，这也是近年 sidecar-less（Cilium eBPF 路线、Ambient Mesh）兴起的动机。

## 边界

- **代理链会层层改写约定**。正向代理改 Host、反向代理加 X-Forwarded-For、CDN 注入 Via——请求到达源站时的"真相"是所有中间件的累积改写。排障时要逐跳核对头部，不能拿源站看到的请求推断客户端行为。
- **透明代理不等于无感**。运营商级的透明拦截（TCP 分流)对应用是黑盒，应用无法区分"网络慢"与"中间人在延迟我的包"。排障方法见 [[工程知识/性能工程：从用户等待到资源瓶颈/存储与网络/端到端网络延迟来自排队、协议、传输与处理.md]]。
- **网关与代理的边界在职责不在名字**。API 网关（路由+鉴权+限流+协议转换）是反向代理加了业务语义，两者常共用实现（Nginx 做网关、Envoy 做网关）。选型的分界是"要不要承载业务逻辑"——纯流量治理用代理，业务语义重的用网关，界线在组织架构里比在技术里更清晰。
- **sidecar 的升级独立性是双刃剑**。应用与 sidecar 可以独立升级（治理能力演进不重启发应用），但版本组合矩阵膨胀（应用 v1/v2 × sidecar v1/v2 = 4 种组合），回归测试矩阵随之膨胀。

## 验证

- **路径观测**：对一条真实链路（客户端 → 正向代理 → 反向代理 → 源站）tcpdump 逐跳抓包，预期每跳的头部改写累积可见（X-Forwarded-For 逐级追加）。对不上就停下查：哪一跳丢了转发头。
- **治理生效验证**：在反向代理配置一条限流规则，压测预期 429 在代理层返回（不到源站）。若限流"生效"但源站也收到超额流量，说明有旁路（客户端直连、缓存命中路径绕过代理）。
- **sidecar 故障演练**：kill sidecar 进程，预期应用流量快速失败（治理失效但通信不可用）——sidecar 是单点，它的可用性就是实例的可用性。若流量仍通（应用有直连 fallback），治理规则对该路径失效，这要进设计文档。

## 可运行示例与案例回流：谱系的账

```text
三类代理的职责推演账（通用形态）：

  正向代理：代表客户端出去（访问控制、缓存、匿名）
    放错了的账：把正向代理当反向代理部署 → 内部服务暴露在公网
  反向代理：代表服务端接客（负载均衡、TLS 终结、静态缓存、限流）
    单点账：反向代理是全站入口，挂了 = 全站挂 → 双活 + 健康检查是底线
  sidecar：代表单个实例收发（mTLS、重试、熔断的进程级外置）
    密度账：每实例一个 sidecar，千实例 = 千份内存税（上批网格篇的账）

  演进账：功能先在应用库（重试熔断 SDK）→ 量大后外置到 sidecar/网关统一治理
    每次外置 = 换一次故障域（SDK 故障变进程故障、网关故障变全站故障）
```

案例回流：代理放反的实录——运维把带公网 IP 的正向代理配进生产网段，内部服务被扫到直接暴露（代理方向是安全边界，配错即开门）；按方向 + 网段双重校验后杜绝。网关单点的实录——反向代理单实例承载全站，升级重启窗口全站不可用 40 秒；双实例 + 探活摘流后发布无感。SDK 外置过头的实录——单个请求的重试策略需要在业务层感知上下文（哪些错误可重试依赖业务语义），外置到 sidecar 后策略僵化，失败率反升（重试语义与业务强耦合时留在应用内，通用通信治理才外置）。sidecar 治理的展开账见 [[工程知识/后端系统：在并发、失败与变化中维持服务/基础设施/服务网格控制面与数据面：mTLS与流量治理.md]]、网格的能力边界见 [[工程知识/后端系统：在并发、失败与变化中维持服务/基础设施/服务网格统一通信治理但不负责业务正确性.md]]、入口分流的层级观见 [[工程知识/后端系统：在并发、失败与变化中维持服务/服务设计/服务发现与负载均衡决定请求发往哪里.md]]。

## 相关

- [[工程知识/后端系统：在并发、失败与变化中维持服务/服务设计/服务发现与负载均衡决定请求发往哪里.md]] —— 反向代理执行的"决定"动作
- [[工程知识/后端系统：在并发、失败与变化中维持服务/服务设计/HTTP语义的分层：方法、状态码与头部的约定角色.md]] —— 代理改写的约定层
- [[工程知识/后端系统：在并发、失败与变化中维持服务/基础设施/容器隔离资源边界与镜像身份.md]] —— sidecar 的部署形态基础
- [[工程知识/后端系统：在并发、失败与变化中维持服务/服务设计/RPC框架的通用骨架：序列化连接管理与超时传递.md]] —— RPC 流量过 sidecar 的治理插入点
- [[工程知识/后端系统：在并发、失败与变化中维持服务/基础设施/网络命名空间与容器网络：vethbridge与overlay.md]] —— sidecar 共享 netns 的部署基础
- [[工程知识/后端系统：在并发、失败与变化中维持服务/基础设施/CDN的回源与边缘缓存：静态加速背后的机制.md]] —— CDN 节点本质是反向代理集群
- [[工程知识/后端系统：在并发、失败与变化中维持服务/基础设施/服务网格控制面与数据面：mTLS与流量治理.md]] —— sidecar 形态与治理半径
