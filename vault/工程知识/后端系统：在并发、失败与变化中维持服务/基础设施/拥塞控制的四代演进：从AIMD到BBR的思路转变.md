---
title: 拥塞控制的四代演进：从AIMD到BBR的思路转变
type: evolution
status: active
updated: 2026-09-22
review_after: 2027-09-22
change_rate: low
confidence: high
tags:
  - networking
  - tcp
  - performance/network
sources:
  - "https://www.rfc-editor.org/rfc/rfc5681"
  - "https://research.google/pubs/pub45641/"
---

# 拥塞控制的四代演进：从AIMD到BBR的思路转变

## 要解决的问题

拥塞控制要解决的问题，是防止发送方把网络压垮。1986 年互联网第一次拥塞崩溃事件（LBL 到 UC Berkeley 的吞吐从 32 Kbps 掉到 40 bps，降了千倍）证明了:流量控制（接收方能力协商）管不了网络本身,一个共享网络里所有发送方都抢着发,大家一起死。拥塞控制与流量控制是两个机制：流量控制保护接收方,拥塞控制保护网络。TCP连接建立与断开的状态机（见 [[工程知识/后端系统：在并发、失败与变化中维持服务/基础设施/TCP连接建立与断开的状态机：握手挥手与异常路径.md]]）管单条连接的生死，拥塞控制管整条网络的生死。

## 四代思路

**第一代：Tahoe/Reno 的 AIMD（1988）**。思路是"把丢包当拥塞信号"。每 RTT 加一个 MSS（加性增），丢包时窗口减半（乘性减），AIMD 收敛到公平共享。慢启动是它的加速机制（指数增长到阈值）。代价：网络的轻微丢包（无线网络天然 1% 丢包）会被误判为拥塞，窗口反复减半，高延迟长肥网络（LFN）上吞吐惨不忍睹。

**第二代：CUBIC（2008 Linux 默认）**。思路不变（丢包驱动），只改窗口调整曲线（立方函数），在窗口回退后更快爬回原点，适配高带宽长 RTT 网络。它解决了"减半后爬回太慢"，但丢包误判的根本问题原封未动。

**第三代：基于时延的 Vegas/BBR 前身**。思路转向"把时延当拥塞信号"：RTT 上升 = 队列在堆积 = 网络接近饱和。Vegas 用 RTT 与基线的偏差调整窗口。代价：与丢包驱动算法混跑时,守规矩的时延算法会被激进算法挤掉带宽（"收敛性差"的批评源于此）。

**第四代：BBR（2016 Google）**。思路彻底换：不再等信号，主动测量。周期性探测瓶颈带宽（BtlBw）与最小 RTT，发送速率压在 BDP（带宽×时延积）上，让数据恰好填满管道、不堆积队列。丢包不再是输入信号（BBR 可以在 1% 丢包链路上跑到满速，实测论文数据：与 CUBIC 相比吞吐提升可达两个数量级）。Google 自己的骨干网与 YouTube 全量部署是最大规模验证。

四代的本质转变：**从"事件驱动"（丢包了才反应）到"模型驱动"（持续测量网络模型,按模型发送）**。BBR 之前的算法都在回答"出现了拥塞信号怎么办"，BBR 回答"网络现在是什么样子,我该发多快"。

## 背景与代价

思想背景：1988 年 Van Jacobson 的论文是整个领域的开山之作，他证明了拥塞崩溃源于"发送方对网络状态的无知 + 全体同步反应"。AIMD 的乘性减设计精巧在：减半是全局可收敛的——所有流同步减半后公平分享带宽,加性增让它们各自慢慢扩张。这是没有中央协调的分布式收敛设计，思想源头是控制论里的稳定性分析。

BBR 的精巧在于把"管道"隐喻变成了可操作的模型：BDP 是管道容积，测量它需要分别探测（带宽探测会拉高 RTT、RTT 探测要限速），两者不能同时测准，所以 BBR 周期性交替探测。代价要摆明：BBR 的公平性争议持续存在（与 CUBIC 混跑时 BBR 有时不让路），单流测量不能完全替代拥塞信号，BBRv2/v3 在补这个课。

## 边界

- **四代算法今天都在跑**。Linux 默认 CUBIC，Google 系与部分 CDN 用 BBR，数据中心内部常用 DCTCP（ECN 信号驱动，比四代更早用显式信号）。选型是部署位置问题：跨公网长肥管道 BBR 优势大，数据中心内 ECN 更精确。
- **拥塞控制在应用层的影子**：HTTP/2 的流控窗口、QUIC 的独立流控，都是同一问题的应用层重述。QUIC 把传输握手与加密合流（见 [[工程知识/性能工程：从用户等待到资源瓶颈/存储与网络/QUIC把传输握手与加密合流到用户态.md]]）的同时把拥塞控制可插拔化，同一问题的多代答案在同一个新协议里共存。
- **慢启动与拥塞避免的切换阈值 ssthresh 没有四代演进**：它是第一代遗产,现在仍是默认行为的一部分。演进不是全体换代，是局部替换。

## 验证

- **问题场景**：跨地域传输大文件吞吐远低于带宽上限，`ss -ti` 看拥塞窗口与 RTT，若窗口反复减半（retrans 计数涨），是丢包驱动算法在弱网下的典型表现，换 BBR 是对症药。
- **验证**：两台云主机间 iperf3 对比 CUBIC 与 BBR（`sysctl net.ipv4.tcp_congestion_control` 切换），预期弱网（tc netem 注入 1% 丢包）下 BBR 吞吐显著高于 CUBIC，零丢包时两者接近。对不上就停下查：netem 是否真的注入了丢包、队列规则是否生效。
- **收益核实**：YouTube 前端 BBR 全量后 Google 报告的平均吞吐提升（BBR 论文数据）可作为量级参考，但自己场景要自己测：不同 RTT、丢包率、队列深度下四代算法表现不同，拿论文数字直接当预期收益是误用。

## 相关

- [[工程知识/后端系统：在并发、失败与变化中维持服务/基础设施/TCP连接建立与断开的状态机：握手挥手与异常路径.md]] —— 单连接生死的协议机制
- [[工程知识/性能工程：从用户等待到资源瓶颈/存储与网络/QUIC把传输握手与加密合流到用户态.md]] —— 传输层新协议对拥塞控制的重新装载
- [[工程知识/性能工程：从用户等待到资源瓶颈/存储与网络/端到端网络延迟来自排队、协议、传输与处理]] —— RTT 的构成与队列堆积的延迟侧
- [[工程知识/后端系统：在并发、失败与变化中维持服务/基础设施/socket读写缓冲区：拥塞窗口与应用层背压的连接点.md]] —— cwnd 水阀的完整故事
