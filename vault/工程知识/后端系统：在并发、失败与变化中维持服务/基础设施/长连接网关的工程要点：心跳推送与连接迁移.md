---
title: 长连接网关的工程要点：心跳推送与连接迁移
type: concept
status: active
updated: 2026-09-22
review_after: 2027-09-22
change_rate: low
confidence: high
tags:
  - networking
  - infrastructure
  - backend/networking
sources:
  - "https://www.rfc-editor.org/rfc/rfc6455"
  - "https://datatracker.ietf.org/doc/html/rfc9000"
---

# 长连接网关的工程要点：心跳推送与连接迁移

## 要解决的问题

长连接网关要解决的问题，是让服务端能把消息推给客户端，且连接本身不能悄无声息地死掉。短连接（HTTP 一问一答）里客户端主动、服务端被动，推送只能靠轮询（延迟高、空转浪费）。长连接（WebSocket、SSE、QUIC stream）反转了这一点：连接保持打开，服务端随时可推。但"保持打开"在真实的网络里是个工程谎言——NAT 会超时回收映射、中间设备会掐空闲连接、进程重启会断掉所有在线连接。心跳、推送、连接迁移，就是围绕这个谎言的三个补救件。

## 三个补救件（机制）

**心跳：连接死没死要主动探测**。TCP keepalive 默认两小时才起探测，对推送场景形同虚设，所以应用层自己跳：WebSocket 的 ping/pong 帧、SSE 的注释行、自定义业务心跳。心跳解决两个问题：一是穿透 NAT（中间设备看到流量就续租映射表项），二是死连接检测（对端假死时，写超时或读超时暴露它）。心跳间隔是权衡：太密浪费电（移动端）与带宽，太疏死连接滞留占着连接表。工程常见值落在 30s-60s，但要按客户端网络画像分档。

**推送：协议形态决定语义边界**。三条路各有代价：长轮询（客户端发起、服务端 hold 住直到有消息）复用 HTTP 基建但每个消息一次往返；SSE（服务端单向流）只能下行、断线重连靠浏览器内置 EventSource；WebSocket（双向帧协议，RFC 6455）全双工但要自己处理握手升级、帧分片、关闭语义。选型判据是消息方向与频率：纯下行通知用 SSE 最省，双向高频交互才上 WebSocket。

**连接迁移：网关重启不能变成全量断线**。长连接网关是有状态节点（连接粘在某台机器上），重启、发布、宕机都是断连事件。百万在线的网关重启，百万客户端同时重连，重连风暴会打垮后端。对策分两类：迁移（把连接状态挪走，热升级/连接接管）与错峰（重连退避，客户端按 jitter 分散重试，见 [[工程知识/后端系统：在并发、失败与变化中维持服务/分布式可靠性/重试的正确姿势：退避、抖动与重试预算.md]]）。QUIC 在协议层给了新选项：连接迁移靠 connection ID 而非四元组标识连接，IP 变了连接不断，见 [[工程知识/性能工程：从用户等待到资源瓶颈/存储与网络/QUIC把传输握手与加密合流到用户态.md]]。

## 背景与代价

思想背景：推送需求的祖先是桌面时代的 IM（ICQ/MSN 的服务器长期挂着连接），那时连接数以千计、单机 select 就够。移动互联网把在线规模推到百万级，epoll/kqueue 的单机百万连接（C10K 到 C1M）成为网关的基本功。WebSocket 标准化（2011 年 RFC 6455）把浏览器纳入长连接生态，这之前浏览器推送靠 Comet 的 hack（隐藏 iframe 长轮询）。

最精巧的一笔是**心跳把"连接可用"变成显式契约**。TCP 报文里没有"我活着"的声明，keepalive 又太钝，应用层心跳等于双方约定：N 秒内没有流量就互相宣判死亡。这个契约让死连接的清理从"猜"变成"测"。代价要摆明：长连接网关的资源模型与请求-响应服务完全不同——内存按在线数线性涨（每连接缓冲区）、CPU 由消息推送量驱动而非请求数、发布节奏被连接粘性绑架（滚动发布一轮要等连接自然耗散或主动迁移）。容量估算的通用骨架（见 [[工程知识/后端系统：在并发、失败与变化中维持服务/服务设计/容量估算的通用骨架：从峰值QPS到机器数的推导演练.md]]）里"资源跟着什么涨"这一问，长连接场景的答案与在线数挂钩，不与 QPS 挂钩。

## 边界

- **心跳检测不到半死状态**。写成功（进了内核缓冲区）不代表对端收到，对端假死但内核还在收的场景，只有业务层往返（req-res 配对）才暴露。关键路径的活跃检测要用业务心跳，链路心跳只够清垃圾连接。
- **代理链路会吃掉心跳语义**。过 L7 代理的 WebSocket，代理自己也发 ping（透传或拦截），客户端看到的 pong 可能来自代理而非源站。排查"连接活着但消息不到"时先查中间层，代理与网关的谱系（见 [[工程知识/后端系统：在并发、失败与变化中维持服务/基础设施/代理与网关的谱系：正向反向与sidecar各自解决什么.md]]）里每层都可能改写行为。
- **连接数上限在文件描述符与内存，不在 CPU**。百万连接的网关先撞 ulimit、再撞每连接缓冲区的内存墙，CPU 反而闲。调优顺序：fd 限制 → 内存画像 → 才轮到 CPU。
- **迁移不是免费午餐**。连接状态接管要求状态外置（会话表挪到共享存储或对端），每次消息多一跳查表。迁移收益（发布不断线）与常态代价（每消息多一跳）要在在线规模大时才合算，小规模错峰重连更简单。

## 验证

- **死连接清理验证**：拔掉客户端网线（不是杀进程），预期：网关在心跳超时窗口内检测到并回收连接，连接表数量回落。若连接滞留超过 3 倍心跳周期，查心跳实现是否真的绑定了读超时。
- **重连风暴演练**：kill 网关主进程，预期：客户端按 jitter 退避分散重连，后端 QPS 曲线是缓坡不是尖刺；新网关节点在线数在退避窗口内爬回基线。尖刺出现说明客户端退避没加抖动，全员同一毫秒冲进来。
- **容量画像验证**：按目标在线数压测（如 50 万连接、每连接 4KB 缓冲），预期：内存占用 ≈ 在线数 × 每连接开销（缓冲+会话结构），实测对不上就逐项查每连接真实内存（ss -m 看每 socket 的内存分配）。

## 相关

- [[工程知识/后端系统：在并发、失败与变化中维持服务/基础设施/TCP连接建立与断开的状态机：握手挥手与异常路径.md]] —— 长连接赖以存活的传输层状态机
- [[工程知识/性能工程：从用户等待到资源瓶颈/存储与网络/QUIC把传输握手与加密合流到用户态.md]] —— 协议层连接迁移的原生支持
- [[工程知识/后端系统：在并发、失败与变化中维持服务/分布式可靠性/重试的正确姿势：退避、抖动与重试预算.md]] —— 重连风暴的退避纪律
- [[工程知识/后端系统：在并发、失败与变化中维持服务/基础设施/QUIC之后的传输层：HTTP3与连接迁移的工程红利.md]] —— 应用层连接迁移与协议层迁移的对照
- [[工程知识/后端系统：在并发、失败与变化中维持服务/基础设施/socket读写缓冲区：拥塞窗口与应用层背压的连接点.md]] —— 缓冲区事故的重灾区场景
