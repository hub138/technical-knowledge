---
title: 容量规划与压测要用同一个负载模型说话
type: concept
status: active
updated: 2026-09-21
review_after: 2027-03-21
change_rate: medium
confidence: high
tags:
  - backend/scalability
  - capacity
sources:
  - "https://www.usenix.org/system/files/conference/hotcloud14/hotcloud14-wang.pdf"
  - "https://www.brendangregg.com/usemethod.html"
---

# 容量规划与压测要用同一个负载模型说话

容量规划（"要多少台机器"）与压测（"现在这套能扛多少"）经常各说各话：规划用峰值 QPS 估，压测用固定并发压，两边对不上号，结果要么过度采购、要么大促当天崩。问题的根不在工具，在**两边没有共用同一个负载模型**——负载模型把"用户行为"翻译成请求序列的统计描述（到达率、请求构成、长度分布、突发形态），规划与压测都应该是这个模型的两个消费者。

排队论给出框架：系统在利用率 ρ 上升时，等待时间按非线性增长（M/M/1 模型里 W = ρ/(1-ρ) × 服务时间），**ρ 越接近 1，延迟对负载增量越敏感**。80% 利用率时排队延迟是服务时间的 4 倍，90% 时是 9 倍。这解释了容量规划的黄金数字：留 20-30% 余量不是经验主义，是排队曲线在尾部的陡峭。

## 要解决的问题：负载模型的四个要素

| 要素 | 问的问题 | 压测常见错误 |
| --- | --- | --- |
| 到达率 | 单位时间来多少请求 | 只设并发数不设速率，测的是吞吐上限不是延迟形态 |
| 请求构成 | 各类请求占比 | 全用同一接口压，混合负载下的资源竞争看不到 |
| 长度分布 | 请求处理时长的分布 | 用平均时长代替分布，长尾请求被藏住 |
| 突发形态 | 峰值/均值的比例与持续时间 | 只压平稳负载，突发排队雪崩测不到 |

到达率与并发数的区别是压测的第一坑：并发 100 的固定压测让系统打满，测出的是**饱和吞吐**；按到达率（每秒 50 个新请求）压，测出的是**目标负载下的延迟形态**。容量决策需要后者——大促不是把系统打满，是在预估到达率下延迟是否达标。

## 规划的推导链

容量从需求推到机器数：`机器数 = 目标到达率 × 平均服务时间 × (1 + 余量) / 单机有效并发`。链条上每个量都要实测，拍出来的数值撑不住容量决策：服务时间从线上剖析或日志来，单机有效并发从阶梯压测来（逐级加压，观察延迟拐点），余量按排队曲线与服务等级目标定（P99 要求严的，余量留 30%+）。

拐点压测的读法：以到达率为横轴、P99 延迟为纵轴画曲线，曲线的膝盖（延迟开始陡升的利用率）就是单机的容量点。膝盖位置由资源瓶颈决定——CPU 型服务在 70-80%，锁竞争型可能 50% 就拐（见 [[工程知识/缺陷分析：从个案到体系/性能/案例三十七：锁自己变成了瓶颈——争用背后的临界区错配]]）。**拐点早于 CPU 打满，说明瓶颈在锁或连接池，加机器不如修瓶颈**——这是压测最值钱的产出。

## 压测的有效性前提

压测结果可信的前提是**环境与线上的关键相似性**：数据规模（百万行的表与十亿行的表执行计划不同）、缓存热度（冷启动与稳态差一个量级）、依赖行为（下游的真实延迟 vs mock 的固定 10ms）。数据规模的影响见 [[工程知识/数据系统：在并发与故障中保存事实/可靠性与运维/数据库基准必须描述负载与资源边界]] 的基准边界要求，测量的误差控制见 [[工程知识/性能工程：从用户等待到资源瓶颈/观测与诊断/基准测试必须先定义负载和测量误差]]。

mock 依赖的压测要标注置信边界：mock 的下游不会像真下游那样在压力下劣化，压出的容量是"下游健康时"的容量。线上真实故障时的容量更小，这一差额要靠降级预案补（见 [[工程知识/后端系统：在并发、失败与变化中维持服务/任务与并发/限流熔断与隔离控制故障半径]]）。

## 边界

排队模型的前提是到达接近泊松、服务时间指数分布，真实流量（整点脉冲、缓存命中突发）偏离假设时，**曲线形态仍可用而精确数值不可用**——所以容量规划结论要配一次真实形态的回放压测（录制线上流量回放）做校准。

容量是动态的：每次发布改变服务时间，每次缓存策略调整改变到达构成。容量规划不是一次性项目，是持续过程——每次大版本后重跑阶梯压测，容量数字与版本一起管理，这与性能回归防护的调优闭环同构（见 [[工程知识/性能工程：从用户等待到资源瓶颈/性能工程：从用户等待到资源瓶颈]] 的持续测量原则）。

## 验证

阶梯压测：从目标负载的 50% 逐级加到 150%，每级稳定 10 分钟，记录吞吐、P50/P99、错误率、资源利用率四条曲线。找到延迟拐点与瓶颈资源。验证判据：拐点可复现（两次压测拐点位置一致），瓶颈资源可解释（拐点处某资源达到阈值）。

校准验证：用压测预测的容量与下次真实峰值的实际表现对比，偏差超过 30% 回查负载模型哪个要素失真。

## 相关

关系：[[工程知识/性能工程：从用户等待到资源瓶颈/观测与诊断/基准测试必须先定义负载和测量误差]] · [[工程知识/数据系统：在并发与故障中保存事实/可靠性与运维/数据库基准必须描述负载与资源边界]] · [[工程知识/后端系统：在并发、失败与变化中维持服务/任务与并发/限流熔断与隔离控制故障半径]]

- [[工程知识/缺陷分析：从个案到体系/性能/案例三十七：锁自己变成了瓶颈——争用背后的临界区错配]] —— 拐点早于资源打满的机制实例
- [[工程知识/性能工程：从用户等待到资源瓶颈/存储与网络/端到端网络延迟来自排队、协议、传输与处理]] —— 排队论的延迟侧展开
- [[工程知识/后端系统：在并发、失败与变化中维持服务/基础设施/Kubernetes管理资源状态，不管理业务正确性]] —— 容量的调度层实现
- [[工程知识/性能工程：从用户等待到资源瓶颈/性能工程：从用户等待到资源瓶颈]] —— 容量数字的持续复核

- [[工程知识/后端系统：在并发、失败与变化中维持服务/服务设计/容量估算的通用骨架：从峰值QPS到机器数的推导演练.md]] —— 容量数字的推导侧起点：从峰值QPS到机器数的六步估算链
