---
title: CDN的回源与边缘缓存：静态加速背后的机制
type: concept
status: active
updated: 2026-09-22
review_after: 2027-09-22
change_rate: low
confidence: high
tags:
  - networking
  - web/http
  - infrastructure
sources:
  - "https://www.rfc-editor.org/rfc/rfc9111"
  - "https://www.rfc-editor.org/rfc/rfc9110"
---

# CDN的回源与边缘缓存：静态加速背后的机制

## 要解决的问题

CDN 要解决的问题，是把内容搬到离用户近的地方。源站在单点（一个机房），全球用户都从这点拉内容：距离远的用户吃跨洋 RTT（100-300ms 起步），热点内容反复回源打爆源站带宽。CDN 的答案是在全球部署边缘节点（POP，point of presence），用户的请求被 DNS 调度到最近的 POP，内容缓存在边缘，命中就地返回，未命中才回源拉一次。本质是"读多写少的内容，按地理位置复制一份"。它是缓存改变读写路径（见 [[工程知识/后端系统：在并发、失败与变化中维持服务/服务设计/缓存改变读写路径、一致性与故障模式.md]]）在"以地理距离为轴"上的实例。

## 命中、回源与新鲜度（机制）

**请求路由：DNS 与调度系统**。用户查 cdn.example.com，CDN 的权威 DNS（GSLB）按用户 IP 地理位置、节点负载、节点健康返回最近的 POP 地址。这层调度就是 DNS 解析全链路（见 [[工程知识/后端系统：在并发、失败与变化中维持服务/基础设施/DNS解析的全链路：递归缓存与失效传播.md]]）的 TTL 机制在运营层的使用：调度决策的生效速度受 DNS TTL 拖累（改调度要等 TTL 过期），故障摘除节点也慢半拍。

**命中路径：边缘就地返回**。POP 的缓存服务器收到请求，内容在缓存且新鲜，直接返回（不发 x-cache: MISS 的回源流量）。命中路径的延迟构成：用户到 POP 的 RTT（同城几毫秒）+ POP 处理时间，跨洋 RTT 从链路里消失。这是 CDN 加速的主体收益。

**回源路径：miss 时的拉取链**。内容不在缓存或已过期，POP 向源站（或上层 CDN 节点，多级回源）拉取。回源是 CDN 的昂贵的路径：一次跨洋 RTT + 源站处理 + 回传。回源风暴（热点内容过期，百万用户同时 miss，同时回源）是 CDN 的经典故障模式，防护靠请求合并（同一内容的并发回源合并成一次，其余请求等结果，术语 request coalescing）。

**新鲜度：HTTP 缓存语义的执行**。RFC 9111 定义了浏览器与代理共享的缓存语义，CDN 是其中的代理缓存角色：源站用 Cache-Control（max-age、s-maxage）声明内容能缓存多久，CDN 按声明执行。静态资源（带指纹的 JS/CSS，文件名含 hash）可以 max-age 一年（内容变了 URL 变，旧 URL 永不失效）；动态内容（API 响应）要么短 max-age 要么 no-store。HTTP 语义的分层（见 [[工程知识/后端系统：在并发、失败与变化中维持服务/服务设计/HTTP语义的分层：方法、状态码与头部的契约角色.md]]）里 Cache-Control 是其中最容易被忽视、又最影响 CDN 行为的头。

## 背景与代价

思想背景：CDN 的原型是 1998 年 Akamai 创立时的洞察（MIT 的分布式系统研究）：互联网的拓扑决定了距离即延迟，内容不动用户动，不如内容主动搬到用户旁边。第一代 CDN 服务静态文件下载；流媒体时代（2000s）推了大带宽节点；动态内容时代（2010s）扩展到动态加速（TCP 优化、路由优化、只缓存可缓存的边缘部分）；现在边缘计算（Cloudflare Workers、边缘函数）把"算"也搬过去。

最精巧的一笔是**"缓存键 + 新鲜度声明"把失效责任交还给内容作者**。CDN 不猜内容多久过期，源站的 Cache-Control 说多久就多久；内容作者用"文件名带 hash"的构建产物把失效变成"永不失效的新 URL"。这个组合让 CDN 可以大规模缓存而不用担心失效协议（没有主动失效推送，靠 TTL 自然过期 + URL 换代）。代价要摆明：这套机制只对"URL 可变"的内容优雅，不可变的 URL（首页 HTML、API 响应）的缓存失效仍是难题——短 TTL 意味着频繁回源，主动失效（CDN API 清缓存）又引入新的运维接口与故障面。CDN 加速的越彻底，"内容改了但边缘还是旧的"这类不一致事故的排查越困难（多级缓存、多 POP、各自 TTL）。

## 边界

- **CDN 只加速可缓存的部分**。HTML 里嵌的动态数据、个性化内容（登录态、地域价）不能边缘缓存，回源仍是延迟主体。动态加速（链路优化）收益有限（省的是握手与拥塞窗口爬坡，跨洋 RTT 本身省不掉）。
- **回源率是 CDN 的核心健康指标**。命中率 99% 意味着 1% 的请求吃全价回源，这 1% 的延迟分布（P99）与容量（回源带宽）要单独监控。命中率突降（内容改版、缓存键设计错误、query string 进了缓存键）是最常见的事故信号。
- **多级缓存的失效传播不保证即时**。CDN 上层节点、POP、浏览器各自缓存，清边缘缓存不等于清浏览器缓存，Cache-Control 与 purge API 管不到浏览器层。发布后"我这里还是旧页面"的工单，逐层清缓存是标准处置。
- **CDN 是信任边界也是攻击面**。边缘节点替源站挡 DDoS（吸收带宽攻击是 CDN 的增值能力），但源站若不限制"只有 CDN 能回源"（防火墙白名单或 CDN 回源鉴权头），攻击者绕过 CDN 直打源站 IP，防护归零。源站 IP 保密是这条防线的一部分。

## 验证

- **命中路径验证**：同一 URL 连续两次 curl，看响应头 x-cache（或 age 头），预期：首次 MISS（回源）、二次 HIT。HIT 的 age 头随时间增长（缓存了多久）。如果二次仍 MISS，查缓存键是否被 query string 或 cookie 撕裂（cache key 把不需要的变量算进去了）。
- **调度就近验证**：从不同地域的机器（或代理出口）dig CDN 域名，预期解析到的 POP IP 不同且就近（用 IP 归属地查）。全部解析到同一 IP 说明 GSLB 没按地理调度，查 DNS 配置。
- **回源合并验证**：对同一 URL 并发发起 100 个请求（缓存过期后），源站访问日志预期只有 1-2 次回源（合并生效），其余在边缘等待后拿到结果。源站日志 100 条说明合并没开，回源风暴裸奔。

## 相关

- [[工程知识/后端系统：在并发、失败与变化中维持服务/服务设计/缓存改变读写路径、一致性与故障模式.md]] —— CDN 是缓存一致性命题的地理轴实例
- [[工程知识/后端系统：在并发、失败与变化中维持服务/基础设施/DNS解析的全链路：递归缓存与失效传播.md]] —— CDN 调度依赖的 DNS 层及其 TTL 拖累
- [[工程知识/后端系统：在并发、失败与变化中维持服务/基础设施/代理与网关的谱系：正向反向与sidecar各自解决什么.md]] —— CDN 节点本质是反向代理集群
