---
title: TLS握手与证书链：信任是怎么一层层背书的
type: concept
status: active
updated: 2026-09-22
review_after: 2027-09-22
change_rate: low
confidence: high
tags:
  - security
  - networking
  - distributed/communication
sources:
  - "https://www.rfc-editor.org/rfc/rfc8446"
  - "https://www.rfc-editor.org/rfc/rfc5280"
---

# TLS握手与证书链：信任是怎么一层层背书的

## 要解决的问题

TLS 要解决的问题，是在不安全的网络上安全地交换密钥：双方从未见过面，中间人可以截获、篡改、重放任何报文，凭什么双方能协商出一个只有彼此知道的对称密钥？答案是两段式设计——非对称加密解决"初次见面如何安全商量密钥"，证书链解决"我怎么知道你是你说的你"。QUIC 把传输握手与加密合流到用户态（见 [[工程知识/性能工程：从用户等待到资源瓶颈/存储与网络/QUIC把传输握手与加密合流到用户态.md]]）压缩的是这段流程的耗时，本文展开的是这段流程本身的机制。

## 两段式：密钥交换与身份证明

**密钥交换（ECDHE）**。客户端与服务端各自生成临时椭圆曲线密钥对，交换公钥，各自算出相同的预主密钥。临时密钥对每次连接都换（E 的含义），抓包存档的密钥换不出历史会话的内容——这解决了 RSA 直接交换密钥时代"服务器私钥泄露等于历史全部泄露"的问题。密钥交换完成后，双方用预主密钥派生出四个会话密钥（两个方向各自的加密密钥与 MAC 密钥）。

**身份证明（证书链）**。服务端发证书，证书内容是"我是 example.com，公钥是这个"，签发者 CA 用自己的私钥对这段声明签名。客户端验证：用 CA 的公钥验签 → 验证通过则信任声明。问题是客户端凭什么信任 CA？答案是链式背书：中间 CA 被根 CA 签名，根 CA 被自己签名，根证书预装在操作系统/浏览器里。信任从预装的根出发，沿链逐级验证到叶子证书。RFC 5280 定义了这套 X.509 体系，RFC 8446 的 TLS 1.3 把整个握手压缩到一次往返。

两段的分工：密钥交换建立**保密通道**（没人能偷看），证书链建立**身份确认**（对面不是我假扮的）。只有前者没有后者，中间人可以自签证书冒充任何站点——保密地和一个骗子说话，比公开地和一个骗子说话更危险。

## 背景与代价

思想背景：这套体系的前身是 1976 年 Diffie-Hellman 的密钥交换（数学上第一次证明不见面也能共享秘密）与 1978 年 RSA 论文（数论构造的非对称加密）。证书链的思想源头是社会学的背书链——你信银行，银行给企业担保，你就信这家企业。PKI 把社会信任结构数学化了。

最精巧的一笔是**信任锚的本地化**。根证书预装在本地系统而不是全球注册表，各操作系统/浏览器维护自己的根列表（Mozilla 的根列表事实标准化了整个行业）。代价要摆明：证书链的运维成本持续存在——证书要续期（Let's Encrypt 用 90 天有效期强制自动化，ACME 协议把续期变成无人值守的例行任务）、CA 要被审计（DigiNotar 2011 年被入侵后浏览器厂商联合吊销其根，CA 体系有一次整体信任重置的前科）、证书透明度日志（CT log）作为公开审计层补在链后面。信任不是建好就完事的基础设施，是要持续花钱维护的社会契约。

## 边界

- **TLS 1.3 删掉了协商降级的老路**。1.2 时代客户端可以声明只支持弱套件，中间人篡改协商让连接降到弱加密。1.3 的握手签名覆盖了协商内容，降级攻击被结构性堵死。但老客户端还在跑 1.2，混合环境的弱套件仍是攻击面。
- **证书验证的坑在链验证逻辑而不在算法**。主机名校验遗漏、链不完整（服务端只发叶子不发中间证书）、时间窗校验缺失，是 TLS 集成的三大经典 bug——库给了验证函数，业务代码没正确调用。
- **mTLS 是同一套机制的双向使用**。服务间通信要求客户端也出示证书，把"客户端是谁"也从 IP/Token 升级为密码学证明。服务网格把 mTLS 下沉到基础设施层统一实施。
- **证书不证明服务可信**。它只证明"持有者控制这个域名"。钓鱼站可以合法买证书，浏览器锁图标只说明连接加密，不说明对面是好人——把加密当信誉是常见的认知错位。

## 验证

- **握手观测**：`openssl s_client -connect host:443 -tls1_3` 看真实握手，预期：ClientHello/ServerHello 一次往返完成密钥交换（1.3），证书链完整送达（叶子+中间证书）。链不完整时 openssl 报 verify error，这是服务端配置遗漏的硬证据。
- **链验证演练**：`openssl verify -CAfile ca.pem -untrusted intermediate.pem leaf.pem`，预期链逐级通过。故意去掉中间证书重跑，预期失败——验证逻辑对不完整链必须拒绝，这是中间人攻击的防线。
- **到期预警验证**：监控证书剩余有效期，预期续期流程在到期前 30 天完成轮换。Let's Encrypt + ACME 的自动化预期零人工介入；靠日历提醒的人工续期一定会在某个加班夜漏掉。

## 相关

- [[工程知识/性能工程：从用户等待到资源瓶颈/存储与网络/QUIC把传输握手与加密合流到用户态.md]] —— TLS 握手成本的传输层优化
- [[工程知识/后端系统：在并发、失败与变化中维持服务/服务设计/HTTP语义的分层：方法、状态码与头部的契约角色.md]] —— TLS 之下的 HTTP 语义层
- [[工程知识/软件构建：让变化可以理解、验证与交付/开发工具/依赖供应链需要锁定来源与构建输入.md]] —— 信任链思想在软件供应链的同构应用
- [[工程知识/后端系统：在并发、失败与变化中维持服务/基础设施/服务网格控制面与数据面：mTLS与流量治理.md]] —— mTLS 赖以成立的证书机制
