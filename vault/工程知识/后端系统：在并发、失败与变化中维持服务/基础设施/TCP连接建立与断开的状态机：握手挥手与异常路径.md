---
title: TCP连接建立与断开的状态机：握手挥手与异常路径
type: concept
status: active
updated: 2026-09-22
review_after: 2027-09-22
change_rate: low
confidence: high
tags:
  - tcp
  - networking
  - distributed/communication
sources:
  - "https://www.rfc-editor.org/rfc/rfc9293"
  - "https://www.rfc-editor.org/rfc/rfc1122"
---

# TCP连接建立与断开的状态机：握手挥手与异常路径

## 要解决的问题

TCP 连接的建立与断开要解决的问题，是在不可靠的 IP 网络上让两端对"连接状态"达成一致。IP 层不保证任何东西（丢包、乱序、重复都可能），两台机器凭什么同时认为"我们有一条连接"？答案是状态机加三次握手：每一端维护自己的状态（CLOSED/SYN_SENT/ESTABLISHED/FIN_WAIT/CLOSED），通过交换报文驱动状态迁移。TIME_WAIT 是连接收尾的代价与设计（见 [[工程知识/性能工程：从用户等待到资源瓶颈/存储与网络/TIME_WAIT是连接收尾的代价与设计.md]]），本文展开整台状态机，TIME_WAIT 只是它最出名的一个状态。

## 三次握手：为什么不是两次

建立连接交换 SYN、SYN+ACK、ACK 三个报文。前两个交换初始序列号（ISN），第三个确认。看起来第三次 ACK 没带数据"浪费"，但它要解决的问题有两个：一是让服务端确认"客户端能收到我的包"（两次握手下，服务端无法区分"客户端活着"与"客户端的旧包延迟到达"），二是同步双向的接收窗口协商。RFC 9293 定义的 ISN 随机化解决旧连接的幽灵报文问题——序列号空间错开，旧连接的迟到报文落不进新连接的接收窗口。

**背景本质**：三次是"双向都确认收发能力"的数学下限。零次双方什么都不知道，一次确认一个方向，两次只确认一个方向的双向能力，三次两个方向都确认。追问"为什么不是四次"——第四次可以捎带在第一个数据包里（ACK 与数据合发），省一次纯往返。

## 四次挥手与异常路径

断开比建立多一次报文（FIN、ACK、FIN、ACK），因为 TCP 全双工：一端发完数据要关（FIN），另一端可能还有数据要发，所以另一端的 ACK 与 FIN 分开发送。这就出现了 CLOSE_WAIT（收到 FIN、自己还没发 FIN）状态——对端已关，本地应用还在持有连接。CLOSE_WAIT 堆积是应用层忘记 close 的信号，这是线上排查的常见场景。

异常路径是状态机的另一半，常被忽略：

- **RST 的三个触发**：端口未监听（连接请求打到不存在的服务）、进程崩溃（内核替它发 RST）、队列溢出（backlog 打满时 SYN 被 RST 或丢弃）。RST 不经过协商直接毁连接，两端状态立即回到 CLOSED。
- **半开连接**：一端重启后不知道连接存在（内存里的状态没了），另一端还认为连接活着。下一个包到达重启端会触发 RST，连接被强制收尾。keepalive 探测是主动发现半开连接的手段。
- **同时打开与同时关闭**：两端同时发 SYN（或同时发 FIN）时状态机走对称路径（SYN_SENT → SYN_RCVD），这是教科书冷门但真实存在（两端互调时偶发）。

## 背景与代价

这套状态机的思想背景是 1974 年 Cerf-Kahn 的互联网论文：在不可靠网络上构建可靠字节流的协议设计。它替代的是更早的电路交换思路——电信网靠物理电路保证连接，TCP 靠两端状态一致"仿真"出连接。连接是软件状态，不是物理事实，这是 TCP 最深刻的抽象。

代价要摆明：状态是有成本的。每个 ESTABLISHED 连接占内核内存（套接字缓冲区、控制块，量级几十 KB），每个 TIME_WAIT 连接占表项（约 60 秒）。连接数上限由此而来——状态机的内存占用是 C10K 问题的根源之一，后来的事件驱动（epoll）与连接复用（HTTP keep-alive、连接池）都在摊薄这份状态成本。

## 边界

- **TIME_WAIT 的设计不是缺陷**：它等 2MSL 是为了 (a) 最后的 ACK 丢了还能重发 ACK 响应对端的 FIN 重传 (b) 旧连接的幽灵报文自然消亡。要压缩它见 [[工程知识/性能工程：从用户等待到资源瓶颈/存储与网络/TIME_WAIT是连接收尾的代价与设计.md]]。
- **状态机不解释应用层语义**：连接 ESTABLISHED 不代表服务能处理请求（进程活着但队列满）。健康检查要解决"连接活与业务活"的区分，见 [[工程知识/后端系统：在并发、失败与变化中维持服务/分布式可靠性/健康检查只能提供有时效的失败证据.md]]。
- **握手耗时可绕过**：TCP+TLS 的多次往返在弱网下显著拖慢首请求。QUIC 把传输握手与加密握手合流到用户态（见 [[工程知识/性能工程：从用户等待到资源瓶颈/存储与网络/QUIC把传输握手与加密合流到用户态.md]]），0-RTT 连重传都省了。

## 验证

- **抓包验证**：`tcpdump -i any 'tcp port 80'` 抓一次真实连接的完整握手挥手，预期：SYN、SYN-ACK、ACK 的序列号推进、挥手四次报文的 FIN/ACK 交替。对不上就停下查：抓包点是否在 NAT/防火墙后（中间盒可能改写）。
- **状态观测**：`ss -tan` 看线上服务的连接状态分布，预期：ESTABLISHED 占绝大多数，少量 TIME_WAIT，CLOSE_WAIT 接近零。CLOSE_WAIT 持续增长是应用泄漏的硬证据。
- **异常路径演练**：kill -9 服务进程，从客户端发请求，预期：收到 RST 且客户端状态直接 CLOSED，不是走挥手。

## 相关

- [[工程知识/性能工程：从用户等待到资源瓶颈/存储与网络/TIME_WAIT是连接收尾的代价与设计.md]] —— 状态机收尾状态的单点深挖
- [[工程知识/性能工程：从用户等待到资源瓶颈/存储与网络/QUIC把传输握手与加密合流到用户态.md]] —— 握手成本的用户态重构
- [[工程知识/后端系统：在并发、失败与变化中维持服务/分布式可靠性/健康检查只能提供有时效的失败证据.md]] —— 连接活与业务活的区分
- [[工程知识/后端系统：在并发、失败与变化中维持服务/基础设施/拥塞控制的四代演进：从AIMD到BBR的思路转变.md]] —— 同协议的连接生死视角，与本文的网络生死视角互补
- [[工程知识/后端系统：在并发、失败与变化中维持服务/基础设施/长连接网关的工程要点：心跳推送与连接迁移.md]] —— 长连接赖以存活的传输层状态机
- [[工程知识/后端系统：在并发、失败与变化中维持服务/基础设施/QUIC之后的传输层：HTTP3与连接迁移的工程红利.md]] —— 被 QUIC 绕开的 TCP 状态机及其代价
- [[工程知识/后端系统：在并发、失败与变化中维持服务/基础设施/socket读写缓冲区：拥塞窗口与应用层背压的连接点.md]] —— 连接生命周期与流量控制的分工
