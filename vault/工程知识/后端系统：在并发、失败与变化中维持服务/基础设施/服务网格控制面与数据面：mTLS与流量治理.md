---
title: 服务网格控制面与数据面：mTLS与流量治理
type: concept
status: active
updated: 2026-09-22
review_after: 2027-09-22
change_rate: low
confidence: high
tags:
  - service-mesh
  - security
  - distributed/communication
sources:
  - "https://istio.io/latest/docs/ops/deployment/architecture/"
  - "https://www.envoyproxy.io/docs/envoy/latest/intro/arch_overview/arch_overview"
---

# 服务网格控制面与数据面：mTLS与流量治理

## 要解决的问题

服务网格要解决的问题，是**把服务间通信的治理从应用代码里搬出去**：重试、超时、熔断、加密、观测，这些每个服务都要做一遍，各语言实现一遍，版本漂移、行为不一。网格的答案是分成两块：数据面（每实例旁边的 sidecar 代理，拦截进出流量执行规则）+ 控制面（下发规则的中央大脑）。应用零改造获得治理能力，代价是链路上多了必经的代理层。代理与网关的谱系（见 [[工程知识/后端系统：在并发、失败与变化中维持服务/基础设施/代理与网关的谱系：正向反向与sidecar各自解决什么.md]]）给了 sidecar 形态，本文写网格在形态之上加的东西：控制面与 mTLS。

## 机制

**控制面：规则的存储与下发**。控制面（Istio 的 istiod）聚合三样东西：服务目录（哪些服务、哪些实例，从 Kubernetes API 或注册中心同步）、规则（路由、重试、超时、mTLS 策略，从 CRD 或配置同步）、证书（为每个服务身份签发证书）。通过 xDS 协议把最终形态推给所有 sidecar。控制面是大脑但不在数据路径上——它挂了，已下发的规则继续生效，只是不能改规则。

**数据面：sidecar 执行规则**。每个 Pod 里的 sidecar（Envoy）拦截该实例的全部进出流量：出向流量被 iptables 劫持到 sidecar，sidecar 按规则路由到目标 sidecar，再转给目标应用。链路变成 应用→sidecar→sidecar→应用，两跳本地加一跳网络。治理规则（重试、熔断、鉴权）在 sidecar 里执行，应用只管业务逻辑。

**mTLS：服务间身份的密码学化**。控制面给每个服务（身份来自 Kubernetes ServiceAccount）签发证书，sidecar 之间的连接全部 TLS 加密且双向验证（我要证明我是 order 服务，你也要证明你是 payment 服务）。服务间通信从"IP 可达即可通"升级为"证书身份验证才通"，网络位置不再是信任凭据——Pod 被攻破不能横向移动，内网抓包看不到明文。TLS握手与证书链（见 [[工程知识/后端系统：在并发、失败与变化中维持服务/基础设施/TLS握手与证书链：信任是怎么一层层背书的.md]]）的机制在网格里被控制面自动化了：签发、轮换、验证全部托管，证书有效期短到（24h 级）泄漏窗口小。

## 背景与代价

思想背景：网格的前身是微服务的"胖客户端 SDK"（Netflix Hystrix/Ribbon、Finagle）：治理逻辑进 SDK，每语言一套。多语言团队维护 N 套 SDK 的成本催生了"SDK 下沉为基础设施"的路线：2016 年 Envoy 开源，2017 年 Istio 把"sidecar + 控制面"组合定型。它继承的是 kernel bypass 与中断吸收的思想（把高频动作下沉到专门层），继承方式是把网络治理从应用进程搬到独立进程。

最精巧的一笔是**控制面不碰数据路径**。规则下发后数据面自治运行，控制面故障不影响在跑的流量。这个设计让网格的可用性模型简单：控制面 HA 是配置可用性问题（改不了规则），数据面（sidecar）才是流量可用性问题。代价要摆明：每个请求多两跳本地代理（sidecar 进出各一跳），P99 延迟增加毫秒级到十毫秒级（取决于 sidecar 性能与规则复杂度）；每实例一个 sidecar 的资源乘法（内存几 MB 到几十 MB × 实例数）；控制面本身是新组件（istiod 的可用性、版本升级、与 Kubernetes 版本的兼容矩阵）。"网格值不值"的争论全在代价与收益的换算，小集群（几十个服务）常常不划算。

## 边界

- **sidecar 故障即实例故障**。应用与 sidecar 同 Pod，sidecar 失效则实例的进出流量全断。sidecar 的资源 limit（内存不够被 OOM kill）是实例级事故，监控要按 Pod 粒度盯 sidecar 容器的指标。
- **mTLS 不覆盖入口流量**。网格管的是东西向（服务间），南北向（外部进来的）流量由网关层处理（Ingress），TLS 在网关终结。端到端加密要求（外部到 Pod 全程密文）需要额外的设计（网关透传 TLS 或应用层再加密），不是网格默认给的。
- **规则下发有延迟，故障切换要靠本地**。控制面推规则到全网格生效要秒级，实例下线后 sidecar 感知（通过健康检查）也要秒级。秒级故障窗口内的请求会打到坏实例，sidecar 的重试与熔断是第一道防线（本地毫秒级反应），见 [[工程知识/后端系统：在并发、失败与变化中维持服务/分布式可靠性/重试的正确姿势：退避、抖动与重试预算.md]]。
- **网格的观测是全链路但不是全真相**。sidecar 看得到请求级指标（成功率、延迟、目标），看不到业务语义（这笔请求是转账还是查询）。业务层监控（订单量、支付成功率）仍要应用自己埋点，网格指标是流量真相，业务真相要另配。

## 验证

- **mTLS 生效验证**：网格内两服务通信，在源站 sidecar 抓包，预期看到 TLS 握手（不是明文 HTTP）；`istioctl proxy-config listener <pod>` 看监听器配置，预期 mTLS 模式（STRICT）生效。直连 Pod IP 绕过 sidecar 的请求，预期被拒（mTLS STRICT 拒绝明文），这是"网络位置不是信任凭据"的实测。
- **控制面故障演练**：kill istiod，预期：已下发规则继续生效，流量正常；但改一条路由规则，预期下发失败（控制面不可用）。流量与配置的故障域分离，是这个架构的承诺，演练核实。
- **sidecar 注入验证**：`kubectl get pod -o jsonpath='{.spec.containers[*].name}'`，预期每个 Pod 有应用容器 + istio-proxy 两个容器。漏注入的 Pod 裸奔在网格外（无 mTLS 无治理），接入率是网格的关键指标（istioctl proxy-status 看 sidecar 同步状态）。

## 可运行示例与案例回流：网格的账

```text
网格接管一次调用的推演账（通用形态，sidecar 模式）：

  数据面：业务容器完全无感，流量被 iptables 劫持进 sidecar
    每跳新增两过 sidecar（入+出）：单跳延迟 +0.5-2ms，内存每实例 +50-100MB
  控制面：下发配置（路由规则、mTLS 策略、限流）到全部 sidecar
    下发延迟账：万级实例全量生效要分钟级 → 灰度期间新旧规则并存
  mTLS 的账：证书轮换由控制面托管（默认 24h），业务零改造
    但 sidecar 故障 = 业务故障（旁路变拦路）
  流量治理：按 header/权重切流 → 金丝雀发布从负载均衡层挪进网格层
```

案例回流：sidecar 单点的实录——业务升级时重启 sidecar 与业务容器同批进行，重启窗口内该实例全部流量被劫持失败（数据面接管后 sidecar 就是网络本体）；改为业务就绪检查通过后再注入流量、sidecar 独立升级顺序后消失。控制面下发风暴的实录——一次改路由规则误配全量下发，万级 sidecar 同时重载配置打满控制面，健康检查连锁超时（配置变更限速分批 + 按命名空间灰度下发后可控）。资源税的实录——千级实例集群网格内存税 15%（每实例 80MB sidecar），小服务收益低过税；轻量服务改用边界网关统一治理。网格的通信治理边界见 [[工程知识/后端系统：在并发、失败与变化中维持服务/基础设施/服务网格统一通信治理但不负责业务正确性.md]]、sidecar 在网关谱系中的位置见 [[工程知识/后端系统：在并发、失败与变化中维持服务/基础设施/代理与网关的谱系：正向反向与sidecar各自解决什么.md]]。

## 相关

- [[工程知识/后端系统：在并发、失败与变化中维持服务/基础设施/代理与网关的谱系：正向反向与sidecar各自解决什么.md]] —— sidecar 形态与治理半径
- [[工程知识/后端系统：在并发、失败与变化中维持服务/基础设施/TLS握手与证书链：信任是怎么一层层背书的.md]] —— mTLS 赖以成立的证书机制
- [[工程知识/后端系统：在并发、失败与变化中维持服务/服务设计/RPC框架的通用骨架：序列化连接管理与超时传递.md]] —— 网格接管治理前的 RPC 骨架形态
