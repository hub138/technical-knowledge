---
title: 服务降级是有损服务，损什么损多少要变成业务决策
type: concept
status: active
updated: 2026-09-21
review_after: 2027-03-21
change_rate: medium
confidence: high
tags:
  - backend/degradation
  - resilience
  - tradeoff
sources:
  - "https://sre.google/sre-book/handling-overload/"
  - "https://landing.google.com/sre/workbook/chapters/handling-overload/"
---

# 服务降级是有损服务，损什么损多少要变成业务决策

系统面对过载或依赖故障时，只有两条路：全量拒绝（整体不可用）或主动降级（保主路径、砍次要功能）。降级的本质是**用功能完整性换可用性**——推荐列表换成兜底榜单、实时库存显示变成"有货"占位、个性化关闭改用通用策略。这不是技术决策而是业务决策：损哪个功能、损多久、损到什么程度，答案在业务侧，技术侧的责任是把选项与代价摆出来，并把执行做成可配置。

降级与限流的关系要先分清：限流（见 [[工程知识/后端系统：在并发、失败与变化中维持服务/任务与并发/限流熔断与隔离控制故障半径]]）控制**进来多少**，降级控制**进来的怎么处理**。两者配合使用：限流保系统不死，降级保核心路径在限流后仍有意义。

## 降级阶梯

| 级别 | 动作 | 用户感知 | 触发条件 |
| --- | --- | --- | --- |
| L1 缓存兜底 | 新鲜度降级：实时数据换缓存数据 | 轻微（数据稍旧） | 依赖超时率上升 |
| L2 功能裁剪 | 砍非核心计算（推荐、榜单、个性化） | 部分功能变简单 | 资源利用率阈值 |
| L3 数据降级 | 详细页变摘要、列表变短 | 明显变简 | 容量缺口大 |
| L4 拒绝次要流量 | 非核心场景直接拒绝 | 次要场景不可用 | 保命 |

阶梯的意义是**损失的可控递进**：能停在 L1 不进 L2，每升一级都是独立的业务确认点（预授权过的规则，不是临时拍板）。没有阶梯的降级只有"全开/全关"，过载时要么不敢降（拖死核心）、要么一降到底（损毁体验）。

## 每个降级开关的三要素

一个可用的降级开关要写清楚三件事，缺一不可：

1. **触发条件**：指标阈值（依赖错误率 > 10%、P99 > 2s）加持续时间（避免抖动来回切换），触发条件必须可观测、可提前演练。
2. **恢复条件**：阈值回落多少、观察多久才恢复。恢复条件要比触发保守（滞回），否则在临界点震荡。
3. **损失声明**：降级期间哪些数据是旧的/简的/错的，影响哪些报表与结算。损失声明是业务对账的依据——降级期间的数据质量变化若不声明，下游会把它当真实数据处理（静默失真的变体，见 [[工程知识/缺陷分析：从个案到体系/正确性/静默失真的检测靠对账与不变量而不是报错]]）。

## 设计原则：降级路径不能依赖被降级的依赖

降级链最常见的自杀设计：降级开关依赖的配置中心、兜底数据源、通知通道，本身又依赖正在故障的那个依赖。降级路径要在设计时**全链路检查一遍**：兜底缓存能独立服务、开关状态本地可读、降级页不加载故障组件。混沌工程是验证这一点的标准手段（见 [[工程知识/软件构建：让变化可以理解、验证与交付/测试与交付/混沌工程验证系统是否真的能处理故障]]）。

## 边界

降级不适用于**数据正确性敏感**的核心路径：支付、账务、合规场景没有"有损"选项，过载时的正确答案是排队、限流、拒绝，而不是给一个可能错的数据。判据回到可撤回性：错的推荐可以事后修正，错的扣款不能。

降级预案的维护成本常被低估：依赖与功能每季度都在变，降级开关会腐化（触发条件里的指标改名、兜底数据源下线）。所以降级开关要有定期演练机制——每个开关每季度真实触发一次（低峰期），失效开关在演练中暴露，好过在事故中暴露。

## 验证

演练验证：对依赖注入故障（超时/错误率），观察 L1→L2 的自动切换是否符合触发条件、用户侧实际表现是否与损失声明一致、恢复是否走滞回。用混沌工程的方式定期重跑。

降级数据的下游验证：降级期间采样数据，确认下游消费方按损失声明处理（标记为降级数据）而非当真实数据入库。

## 相关

关系：[[工程知识/后端系统：在并发、失败与变化中维持服务/任务与并发/限流熔断与隔离控制故障半径]] · [[工程知识/软件构建：让变化可以理解、验证与交付/测试与交付/混沌工程验证系统是否真的能处理故障]] · [[工程知识/缺陷分析：从个案到体系/正确性/静默失真的检测靠对账与不变量而不是报错]]

- [[工程知识/后端系统：在并发、失败与变化中维持服务/服务设计/缓存改变读写路径、一致性与故障模式]] —— L1 缓存兜底的一致性代价
- [[工程知识/后端系统：在并发、失败与变化中维持服务/基础设施/容量规划与压测要用同一个负载模型说话]] —— 降级触发阈值的容量依据
- [[工程知识/AI 系统工程：从模型能力到生产能力/推理服务与平台/推理服务的核心矛盾是延迟、吞吐、显存与质量]] —— AI 服务里质量换吞吐的同构取舍
- [[工程知识/后端系统：在并发、失败与变化中维持服务/服务设计/流量入口只负责路由与连接，不负责业务正确性]] —— 降级执行的位置分层

- [[工程知识/后端系统：在并发、失败与变化中维持服务/服务设计/容量估算的通用骨架：从峰值QPS到机器数的推导演练.md]] —— 估算失守、过载发生时的降级阶梯
- [[工程知识/软件构建：让变化可以理解、验证与交付/测试与交付/灰度与功能开关的边界：交付节奏与配置漂移.md]] —— 运维开关背后的降级决策
