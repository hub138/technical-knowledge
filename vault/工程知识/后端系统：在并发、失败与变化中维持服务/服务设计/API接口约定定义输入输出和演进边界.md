---
title: API接口约定定义输入输出和演进边界
type: playbook
status: active
updated: 2026-09-23
review_after: 2027-03-23
change_rate: medium
confidence: high
tags:
  - backend/api
  - api
  - compatibility
sources:
  - "https://www.rfc-editor.org/rfc/rfc9457"
  - "https://spec.openapis.org/oas/v3.1.0"
  - "https://semver.org/"
  - "https://cloud.google.com/apis/design"
---

# API接口约定定义输入输出和演进边界

接口约定不是文档，是**调用方与提供方之间可以被机器校验的契约**：输入长什么样、输出保证什么、错了怎么表达、以及哪些改动算破坏。缺了它，任何一次"很小的字段调整"都要靠人肉通知所有调用方，而调用方只能通过线上报错发现。

约定的价值不在写清楚，而在**能被自动校验**：能跑一次兼容检查就判定某次改动是否破坏，契约才真的约束得住演进。

## 约定的四块内容

| 块 | 要写清什么 | 校验方式 |
|---|---|---|
| 输入 | 字段、类型、是否必填、取值约束、未知字段策略 | JSON Schema / OpenAPI 校验 |
| 输出 | 字段稳定性承诺（哪些保证一直有）、空值语义、分页与排序 | 契约测试、快照比对 |
| 错误 | 状态码分类 + 稳定 `code`，Problem Details 结构 | 错误用例覆盖 |
| 演进 | 版本号策略、弃用流程、破坏性改动的通告期 | 兼容检查 + 变更门禁 |

输入侧的**未知字段策略**常被忽略：严格拒绝未知字段能让拼写错误立刻暴露，代价是调用方升级受限；忽略未知字段则相反。选哪个要写进约定，不能两种行为混在不同的接口上。

## 三类改动与破坏性判定

- **向后兼容**（可直接发）：新增可选字段、新增接口、新增枚举值但调用方必须容忍未知值、放宽校验。
- **潜在破坏**（需要灰度与双读）：收紧校验、改变默认值、调整排序稳定性、字段语义微调。
- **破坏性**（需新版本或协商）：删除或重命名字段、改类型、改必填性、把成功改成失败、收窄枚举。

判据是**调用方代码会不会在不知情的情况下行为改变**：新增字段安全，因为老代码不读它；删除字段不安全，因为老代码可能读它。枚举值尤其危险——服务端新增枚举值对声明了封闭集合的客户端就是破坏，所以约定里要写"客户端必须容忍未知枚举值"。

## 版本与弃用的节奏

- 版本只在大版本边界引入（URL 前缀 `v1`/`v2` 或头部协商），不要为每个小改动加版本——版本越多，维护矩阵越难收敛。
- 弃用要有可观测的度量：记录每个被弃用字段的调用量与调用方标识，`Deprecation` / `Sunset` 头部给出时间线（RFC 8594 的 `Sunset`、RFC 9745 的 `Deprecation`）。
- 移除前先过通告期，通告期内该字段仍然工作，只是响应里带告警头部；没有调用量再移除。

## 错误模型

错误也是契约的一部分，变动它等于变动接口。稳定结构：

```json
{
  "type": "https://api.example.com/problems/insufficient-stock",
  "title": "库存不足",
  "status": 409,
  "detail": "sku A-1 当前可用 2，请求 5",
  "instance": "/v1/orders/42",
  "request_id": "8f2c..."
}
```

要点：`status` 与 HTTP 状态码一致（分类交给协议层，见 [[工程知识/后端系统：在并发、失败与变化中维持服务/服务设计/HTTP语义的分层：方法、状态码与头部的约定角色]]），`type` 是稳定的机器可读标识——调用方**只能**按 `type` 分支，不能按 `title` 或 `detail` 匹配文案，否则文案本地化就是一次破坏性改动。

## 边界

- 契约描述**结构**，不描述业务规则的正确性：字段范围能校验，"这个订单能不能成立"要靠业务测试。
- 契约测试（[[工程知识/软件构建：让变化可以理解、验证与交付/测试与交付/接口约定测试的适用边界：消费者驱动与提供者验证]]）覆盖结构，不覆盖语义、性能与数据状态。
- 契约不能替代领域边界：两个服务共享一张表时，再严格的接口约定也挡不住隐式耦合，边界划分见 [[工程知识/后端系统：在并发、失败与变化中维持服务/服务设计/微服务划分的判据：从团队认知到数据边界]]。

## 验证路径

```bash
# 1) 结构校验：用 OpenAPI 描述跑一次请求/响应的 schema 校验
npx @stoplight/spectral-cli lint openapi.yaml

# 2) 兼容性检查：对比新旧描述，破坏性改动必须非零退出
npx openapi-diff old.yaml new.yaml --fail-on-incompatible

# 3) 线上抽样：真实流量里的响应是否仍满足契约（抽样 1% 即可暴露漂移）
#    判据：schema 校验失败率应为 0；出现失败说明实现已偏离契约
```

落地判据：任何一次接口改动，CI 里的兼容检查必须给出明确结论；破坏性改动要么走新版本，要么拿出"调用量为 0"的实测证据。两条都不满足就合并，等于把发现问题的成本转嫁给调用方的生产环境。
