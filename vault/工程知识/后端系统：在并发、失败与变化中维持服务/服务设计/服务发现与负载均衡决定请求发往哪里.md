---
title: 服务发现与负载均衡决定请求发往哪里
type: concept
status: active
updated: 2026-09-15
review_after: 2027-03-15
change_rate: medium
confidence: high
tags:
  - backend/networking
  - distributed-systems
  - routing
sources:
  - "https://www.rfc-editor.org/rfc/rfc9110"
  - "https://kubernetes.io/docs/concepts/services-networking/service/"
  - "https://kubernetes.io/docs/concepts/services-networking/endpoint-slices/"
---

# 服务发现与负载均衡决定请求发往哪里

调用方只知道**逻辑服务名**（`order-service`），但请求必须发往一个**当前可达、就绪、有容量**的实例。这个过程分两部分，出错后果不同：**服务发现**回答"现在有哪些实例"（错了就发到已下线的实例），**负载均衡**回答"选哪一个"（错了就流量倾斜、部分实例过载）。

关键前提是：**实例列表永远是陈旧的。** 设计上必须假设它会陈旧，并让系统能容忍——下面所有设计都建立在这个前提上。

还要区分三个容易混为一谈的信号：**可达**（端口能连）、**就绪**（应用能响应探针）、**可服务**（业务能正确完成）。一个实例可以"TCP 可达但完全不可服务"——连接池耗尽、模型没加载完都会这样，而这正是发布期间请求失败的常见原因。

出错的后果对照：

| 环节 | 回答 | 出错的后果 |
| --- | --- | --- |
| **服务发现** | 现在有哪些实例？ | 请求发到已下线的实例 → 连接失败 |
| **负载均衡** | 选哪一个？ | 流量倾斜 → 部分实例过载 |

## 要解决的问题：发布时 5% 的请求失败

一个服务做滚动发布，用户侧监控显示：**发布期间约 5% 的请求 500**，发布结束后恢复。

排查发现**四个问题叠加**：

| 现象 | 真因 |
| --- | --- |
| 请求发到已停止的实例 | **注册信息陈旧**：实例已下线，但调用方缓存里还有 |
| 请求发到正在启动的实例 | **没区分 liveness 和 readiness**：进程起来了但配置还没加载完 |
| 个别实例 CPU 打满 | **负载均衡按轮询**，不感知实例实际负载 |
| 故障被放大 | **重试没有总 deadline**，一个慢实例引发连锁重试 |

**四件事分别对应服务发现、健康检查、负载均衡、重试设计。**

## 从 0 开始：服务发现

### 两种模式

**客户端发现**：

```
调用方 → 查注册中心 → 得到实例列表 → 自己选 → 直连
```

优点：少一跳，延迟低，LB 策略由客户端控制。
缺点：**每个语言都要实现一套**。

**服务端发现**（更常见）：

```
调用方 → 负载均衡器（k8s Service / Nginx / Envoy）→ 选一个实例 → 转发
```

优点：调用方无感，语言无关。
缺点：多一跳；LB 本身要高可用。

均衡器在拓扑里的位置与两侧的网络边界，一张图看清：

![Wikimedia Commons 图：基于 NAT 的负载均衡集群——上方是 Terminal 1..n 客户端经 Switch 接入，中间绿色方块是 Loading Balancer，下方两台服务器在 Private Network 一侧，(Semi) Public Network 与 Private Network 的分界线穿过均衡器](https://upload.wikimedia.org/wikipedia/commons/1/1f/Load_Balancing_Cluster_%28NAT%29.svg)

来源：Wikimedia Commons「Load Balancing Cluster (NAT)」。均衡器骑在公网与私网的分界线上，客户端只看到它的地址，后端真实地址被藏在 Private Network 一侧——「多一跳」与「调用方无感」这两个特征同时就是这幅拓扑的形状；NAT 模式下回的包要再经均衡器改地址，这条回程路径也是它成为单点与瓶颈的来源。

**k8s 的做法**：Service 提供稳定的虚拟 IP，kube-proxy 或 Ingress 做转发。**调用方只看到 Service 名。**

### 注册与注销：最容易出问题

```
实例启动 → 注册 → 定期心跳 → 优雅下线时注销
```

**三个坑**：

| 坑 | 后果 |
| --- | --- |
| 靠心跳超时才注销 | 实例已失效但还在列表里（默认可能 30 秒以上） |
| 进程被 `kill -9` | 来不及注销，只能等心跳超时 |
| 网络分区 | 实例活着但注册中心联系不上 → 被误判失效 |

**设计结论：调用方必须容忍列表里有已失效的实例。**

这就要求：

- **连接失败要快速失败**（短超时，不要等 30 秒）
- **要有重试**（但必须幂等）
- **要有主动剔除**：连续失败 N 次就临时拉黑该实例（outlier detection）

### liveness vs readiness：必须区分

| 探针 | 回答 | 失败时 |
| --- | --- | --- |
| **liveness** | 进程还活着吗？ | **重启容器** |
| **readiness** | 能处理请求吗？ | **从负载均衡摘除**（不重启） |

**最经典的踩坑：readiness 检查下游依赖。**

```
❌ readiness = "数据库能连上吗"
   → 数据库抖动 3 秒 → 所有实例 readiness 失败 → 全部被摘除
   → 服务整体不可用（本来只是降级）

✅ readiness = "自身是否就绪"
   → 数据库不可用时仍接收请求，返回降级结果或缓存
```

**这条规则值得反复强调**：探针只检查**自身**，不检查下游。

### 可达 ≠ 可服务

**这是最小实验要验证的核心**：

```
端口能连上（TCP 可达）
  ≠ 应用能处理请求（HTTP 200）
  ≠ 业务能正确响应（依赖可用、缓存已预热）
```

| 信号 | 说明什么 |
| --- | --- |
| TCP 连接成功 | 端口在监听 |
| HTTP 探针 200 | 应用进程响应了 |
| **业务成功率** | **真正能服务** |

**一个实例可以"TCP 可达但完全不可服务"**：连接池耗尽、模型未加载完、缓存未预热都会这样。

**所以需要**：

- **慢启动**：新实例渐进加流量（不做一次全量）
- **异常剔除**：观测实际成功率，而不只看探针
- **主动健康检查**：调用方自己统计失败率

## 从 0 开始：负载均衡

### 五种策略

| 策略 | 做法 | 适用 | 问题 |
| --- | --- | --- | --- |
| **轮询** | 依次分配 | 实例同构、请求均匀 | 实例性能不同时会倾斜 |
| **最少连接** | 选当前连接数最少的 | 请求耗时差异大 | 需要共享状态 |
| **一致性哈希** | 按 key 哈希 | 需要会话粘性/缓存亲和 | 实例变化时部分 key 迁移 |
| **加权** | 按权重 | 实例配置不同 | 权重需人工调 |
| **P2C** | 随机选两个，选较好的 | **通用推荐** | 需要能感知负载 |

**P2C（Power of Two Choices）值得单说**：

```
随机挑 2 个实例 → 选其中"当前请求数较少"的那个
```

**为什么有效**：两次随机的信息量足够避免极端倾斜，而且**不需要全局状态**（每个调用方本地判断即可）。

**实测效果**：P2C 比纯轮询在高负载下 P99 明显更低——它能自动避开正在变慢的实例。

### 长连接让负载均衡失效

```
每次新建连接 → 握手 + TLS → 延迟高、端口耗尽
保持长连接  → 延迟低，但连接建立后流量就固定在某个实例上了
```

**这是 gRPC / HTTP2 场景的核心矛盾。**

**解法**：

| 方案 | 做法 |
| --- | --- |
| **客户端侧 LB** | 客户端持连接池，自己按 P2C 分发（gRPC 的做法） |
| **限制连接寿命** | 定期重建，让 LB 重新分配 |
| **L4 代理** | 每次新连接重新选（但长连接就失效了） |

**gRPC 场景下，服务端发现 + 客户端 LB 通常比 L4 代理更有效。**

### 超时预算：必须分层

- 用户可接受：3 秒
- 网关超时：2.5 秒
- A 到 B：1.5 秒
- B 到 C：800 毫秒
- 数据库查询：300 毫秒

**每层必须小于上层**，留出处理时间。

**错误做法**：所有层都设 3 秒 → 最坏情况总耗时是各层累加 → 远超用户可接受时间。

**必须有总 deadline**（逐层减小，不做每跳独立超时）：请求带一个 deadline 一路传递下去，任何一层发现 deadline 已过就直接失败，不再继续调用。

### 重试的三条纪律

1. **只重试幂等的操作**（不幂等的重试会重复副作用）
2. **指数退避 + 抖动**（避免同步重试风暴）
3. **重试预算**：重试量超过总请求的 10% 就停止重试——这说明系统已过载，继续重试会雪上加霜

**还要：记录重试代际**（retry generation），防止重试链无限嵌套。

## AI 服务的特殊性

**LLM 请求不能按"HTTP 请求数"均衡。**

```
请求 A：100 个 token  → 占用 GPU 0.5 秒
请求 B：32000 个 token → 占用 GPU 30 秒（含 prefill + KV cache）
```

**按请求数轮询会把长请求集中到同一个副本** → 那个副本过载，其他空闲。

**LLM 路由应该考虑**：

| 维度 | 说明 |
| --- | --- |
| **输入/输出长度** | 预估计算量 |
| **KV cache 占用** | 显存是硬约束 |
| **前缀缓存命中** | 命中可大幅降低 prefill 成本 |
| **模型版本 / LoRA** | 不同副本可能加载不同适配器 |
| **GPU 型号** | 异构集群下性能差异大 |
| **优先级 / 租户** | 需要隔离高优先级流量 |

**这就是"token 感知的路由"**，比传统 LB 复杂得多。详见 [[工程知识/AI 系统工程：从模型能力到生产能力/推理服务与平台/模型服务架构从单进程到分布式副本]]。

## 验证：怎么测

**关键实验**：让一个实例**TCP 可达但不可服务**（连接池耗尽或停止加载模型），比较三种配置的表现：

| 配置 | 预期 |
| --- | --- |
| 只做端口探活 | ❌ 请求持续被送进这个实例 |
| 加 readiness | ✅ 但在探针更新前仍有窗口 |
| 按队列/token 负载路由 | ✅ 主动避开 |

**记录**：

- 请求是否仍被送入无容量实例
- 摘除的传播时间（从故障到不再发请求）
- 重试次数和是否重复副作用
- 是否在 deadline 内失败

**其他要注入的**：

```yaml
- 半故障实例（50% 请求慢 10 倍）
- 陈旧端点（直接 kill -9）
- 连接耗尽
- DNS 延迟 / 缓存过期
- 单节点丢失
- 拓扑变化（跨区延迟）
```

## 常见错误

**错误 1：readiness 检查下游依赖**

数据库抖动 → 全站实例被摘除。**探针只检查自身。**

**错误 2：以为负载均衡能感知负载**

默认轮询不感知。**用 P2C 或最少连接。**

**错误 3：长连接让 LB 失效**

gRPC 连接建立后流量固定。**用客户端侧 LB。**

**错误 4：重试没有总 deadline**

重试累加超过用户耐心。**设总 deadline，逐层减小。**

**错误 5：所有操作都重试**

不幂等的重试会重复副作用。**只重试幂等的。**

**错误 6：滚动更新不等 readiness**

新实例未就绪就接流量。**必须先 readiness 通过，再摘旧实例。**

**错误 7：不优雅关闭**

直接 kill → 在途请求失败。**先摘除、等在途请求完成、再关闭。**

**错误 8：AI 服务按请求数均衡**

长请求集中到单个副本。**按 token/负载路由。**

关系：[[工程知识/后端系统：在并发、失败与变化中维持服务/分布式可靠性/部分失败决定分布式系统的设计]] · [[工程知识/后端系统：在并发、失败与变化中维持服务/分布式可靠性/健康检查只能提供有时效的失败证据]] · [[工程知识/后端系统：在并发、失败与变化中维持服务/任务与并发/限流熔断与隔离控制故障半径]]
- [[工程知识/后端系统：在并发、失败与变化中维持服务/服务设计/HTTP语义的分层：方法、状态码与头部的约定角色.md]] —— 请求去向与语义约定的分界
- [[工程知识/后端系统：在并发、失败与变化中维持服务/基础设施/DNS解析的全链路：递归缓存与失效传播.md]] —— DNS 的最终一致不满足实时路由时的替代
- [[工程知识/后端系统：在并发、失败与变化中维持服务/基础设施/代理与网关的谱系：正向反向与sidecar各自解决什么.md]] —— 反向代理执行的"决定"动作
- [[工程知识/后端系统：在并发、失败与变化中维持服务/服务设计/健康检查的三层语义：存活就绪与深度.md]] —— 摘除决策的下游机制

## 边界

负载均衡不修复实例本身的慢与坏：它按健康证据分流，健康检查的时效决定了摘除的滞后窗口——窗口内的失败要靠调用方超时与重试兜住，均衡策略不承担兜底责任。

## 效果与代价

成熟的发现与均衡层的收益是发布与故障的流量动作可编排（摘流、灰度、回滚都变成流量指令），代价是发现链路本身要维护（注册中心的高可用、健康检查的误判治理）；裸直连模式省掉这层，代价是每次发布与摘除都要改配置重启——规模上去之后，这层的基础设施成本必然要付，付得越晚迁移越痛。