---
title: 分布式限流的四种算法：计数漏桶令牌与滑动窗口
type: concept
status: active
updated: 2026-09-22
review_after: 2027-09-22
change_rate: medium
confidence: high
tags:
  - "backend/scalability"
  - "resilience"
  - "backend/scalability"
sources:
  - "https://en.wikipedia.org/wiki/Token_bucket"
  - "https://stripe.com/blog/rate-limiters"
  - "https://kubernetes.io/docs/concepts/policy/resource-quotas/"
---

# 分布式限流的四种算法：计数漏桶令牌与滑动窗口

## 要解决的问题

过载保护的入口层：流量超过容量时按规则拒绝（保护后端）或平滑（整形流量）。四种算法各有形状：固定窗口计数（简单有边界突刺）、滑动窗口（平滑有内存代价）、漏桶（整形到恒速）、令牌桶（允许突发）。要解决的问题是**按场景选对算法**：API 网关的配额、下游保护的整形、公平性防个别调用方无法获得服务，每种需求对应不同的算法形状。这是限流熔断与隔离（见 [[工程知识/后端系统：在并发、失败与变化中维持服务/任务与并发/限流熔断与隔离控制故障半径.md]]）的算法展开，与背压（见 [[工程知识/后端系统：在并发、失败与变化中维持服务/服务设计/背压的传递链路：从下游饱和到上游减速.md]]）构成“拒绝与减速”的两种过载响应。

## 机制

**固定窗口计数（fixed window）**。时间切窗口（每分钟一个），窗口内计数达上限即拒。实现最简（Redis INCR + EXPIRE），但边界突刺：两分钟交界处（前一窗末 + 后一窗首）可放过 2× 上限的量（59 秒 100 个 + 61 秒 100 个 = 3 秒内 200 个），对突发敏感的下游是暗坑。

**滑动窗口（sliding window）**。任意时刻回看最近 W 时间内的计数（真实窗口），无边界突刺。实现代价：精确滑窗要记每个请求的时间戳（ZSET 类结构，内存随 QPS 涨）；近似滑窗（把大窗切小格，如 1 分钟切 6 个 10 秒格）内存省但精度降（格间仍有小突刺）。分布式实现的一致性代价：多实例的计数同步（Redis 集中计数 vs 本地计数 + 周期同步）。

**漏桶（leaky bucket）**。请求进桶，桶以恒速漏出（处理速率恒定），桶满即拒。形状：强整形（输出速率严格恒定），适合保护脆弱下游（下游只能承受恒速）。代价：不允许任何突发（健康流量也被削峰），桶深是缓冲上限（排队延迟进用户路径）。

桶的进出速率关系在图上很直观：

![Wikimedia Commons 图：漏桶（leaky bucket）示意图——上方是不规则的请求流入，中间桶体以固定水位表示缓冲，底部以恒定速率漏出；桶满时超出部分被拒](/static/figures/leakybucket.png)

来源：Wikimedia Commons「Leaky bucket」条目图。上端不规则的流入与下端恒定的漏出之间的差就是「整形」的含义：出口速率与入口突发无关，桶深只决定能缓冲多少——「排队延迟进用户路径」对应的是桶里积压的那部分水位，桶满即拒的判定发生在水位触顶的那一刻。

**令牌桶（token bucket）**。恒速产令牌入桶（桶满丢弃），请求取令牌（无令牌即拒）。形状：允许突发（桶内积攒的令牌一次性取用），平均速率受控（产令牌速率）。适合"平均低但偶发突发"的流量（API 调用方的正常画像）。漏桶与令牌桶是镜像：漏桶整形输出（出口恒速），令牌桶容忍入口突发（入口可突发）。

**分布式的一致性与故障模式**。集中式（Redis）：精确但 Redis 是新依赖（Redis 挂了限流怎么办——fail-open 放行 vs fail-close 拒绝，可用性与保护的权衡）。本地式（每实例独立限流，配额 N/实例数）：无依赖但配额切分静态（负载不均时有的实例饿有的实例闲）。分层式（网关粗限 + 服务细限）：每层管自己的尺度（网关管租户级，服务管自身保护级）。

**公平性维度**。限流键的选择（全局 vs 按用户 vs 按租户）是公平性设计：全局限流保护系统但单租户洪峰殃及全体（Noisy Neighbor），按租户限流隔离爆炸半径但键空间爆炸（百万租户的计数管理）。优先级与权重（付费用户高配额）是商业维度的公平性。

## 背景与代价

思想背景：四种算法都源于网络工程（QoS 领域）：漏桶（1990s ATM 网络的流量整形）、令牌桶（Cisco 的 committed access rate）先用于路由器，应用层限流（API 时代）把它们搬来。Stripe 的限流器博客（2017）是工程视角的经典（他们选滑动窗口 + Redis 的决策过程公开）。分布式限流的难点（一致性 vs 可用性）在 2020s 的多活架构讨论中深化（限流状态本身不能是单点）。

最精巧的一笔是**令牌桶的“突发预算”设计**。纯速率限制（每秒 N 个）对真实流量太苛刻（健康用户也有秒级突发），令牌桶把“长期平均”与“短期突发”分开管：平均速率 = 产令牌速度（长期约定），突发预算 = 桶容量（短期容忍）。这一笔承认流量的突发性是常态（TCP 的 BDP、API 的批处理调用），用两个参数替代一个参数，形状灵活度上一个台阶。漏桶没有这个自由度（恒速整形），所以漏桶适合下游脆弱场景（严格的恒速保护），令牌桶适合用户侧配额（容忍正常突发）。代价要摆明：分布式限流的精度与依赖成正比——精确限流（集中 Redis）引入单点与延迟（每次请求一次 Redis 往返，P99 加几毫秒），本地限流无依赖但配额静态（实例间负载不均时误差大）；fail-open/fail-close 的选择没有免费答案（放行 = 过载风险，拒绝 = 可用性事故），按业务定（对外 API 接近 open 保口碑，内部保护接近 close 保稳定）；四种算法的内存与 CPU 形状不同（滑窗的 ZSET 随 QPS 膨胀，令牌桶的懒计算最省），选型要算资源账不只算语义账。

## 边界

- **算法选择先问“保护谁”**。保护下游不被打挂（下游脆弱 → 漏桶整形）、保护自身容量（容量硬顶 → 令牌桶/滑窗）、配额管理（多租户公平 → 滑窗 + 租户键）。一个系统常要多层限流（网关层配额 + 服务层保护），每层不同算法。
- **fail-open 的默认值要显式决策**。Redis 故障时放行是多数系统的隐式选择（可用性优先），但这个决策要显式（写进设计文档与 runbook），并配监控（Redis 故障期间的真实流量记录，事后校准容量假设）。
- **固定窗口的边界突刺要对齐下游承受力**。选固定窗口（简单）前算一下：2× 上限的突发下游扛得住吗（下游容量余量 >2× 时可以，否则换滑窗）。
- **限流的键空间爆炸要有治理**。百万级键的 Redis 内存与过期管理（TTL 策略、冷键清理），键设计（user_id vs IP vs API key）影响公平性与成本，键的粒度是设计决策不是实现细节。

## 验证

- **算法形状对照**：四种算法在同一突发负载（1 秒 200 请求，限速 100/分钟）下的接受/拒绝时序。预期：固定窗口边界双倍放行、滑窗平滑拒绝、漏桶恒速处理、令牌桶放行突发后限速。形状差异的直接可视化。
- **Redis 故障演练**：集中式限流依赖故障（断 Redis），观测 fail-open/fail-close 的行为。预期：fail-open 全放行（保护失效、可用性保住）、fail-close 全拒（保护住、可用性事故）。权衡的实证。
- **多实例配额实验**：本地限流的 N 实例部署，负载不均（热点 key 打单实例）时观测实际配额利用率。预期：静态切分下利用率低（饿的实例配额闲置），集中式无此问题。切分误差的量化。

## 可运行示例与案例回流：限流的账

```text
四种算法在同一流量下的推演账（通用形态，限 100 次/秒）：

  固定窗口计数：秒边界双倍突发 → 0.999s 和 1.001s 各放 100 次，瞬时 200
  滑动窗口：任意 1 秒窗口内 ≤ 100 → 边界突发消失，精度随桶数增加
  漏桶：恒定速率放行 → 削平突发，但突发友好场景（如批量查询）被误伤
  令牌桶：桶容量允许攒 burst → 平均线稳定、瞬时弹性，最常用

  分布式账：计数器放单点 Redis → Redis 抖动 = 限流失效或全量拒绝
    集群限流要按实例配额二次分桶（总配额按副本数摊，本地先行）
热点账：全局限流键太粗（全接口一个键）→ 低频接口被高频接口长期压制
```

案例回流：固定窗口边界的实录——秒杀开抢瞬间 QPS 翻倍打挂库存服务（0.999/1.001 双窗口各放满配额）；换滑动窗口后边界突发消失。Redis 单点的实录——限流计数器全压在一个 Redis 上，Redis 主从切换的 20 秒里限流服务全量报错（集群限流要本地先行 + 中心校准的分层结构）。键太粗的实录——全接口共用一个全局限流键，大促时报表查询把交易下单的配额吃光（按接口重要性分键 + 权重配额）。兜底口径见 [[工程知识/后端系统：在并发、失败与变化中维持服务/服务设计/服务降级是有损服务，损什么损多少要变成业务决策.md]]、全局限流值的容量换算见 [[工程知识/性能工程：从用户等待到资源瓶颈/存储与网络/节流参数与容量匹配：全局限流值要按单点容量乘副本数算.md]]、入口层的限流位置见 [[工程知识/后端系统：在并发、失败与变化中维持服务/服务设计/流量入口只负责路由与连接，不负责业务正确性.md]]。

## 相关

- [[工程知识/后端系统：在并发、失败与变化中维持服务/任务与并发/限流熔断与隔离控制故障半径.md]] —— 限流在过载防御中的位置
- [[工程知识/后端系统：在并发、失败与变化中维持服务/服务设计/背压的传递链路：从下游饱和到上游减速.md]] —— 拒绝与减速的两种响应
- [[工程知识/后端系统：在并发、失败与变化中维持服务/服务设计/连接池的容量数学：池大小与排队等待的权衡.md]] —— 池作为隐式限流器
