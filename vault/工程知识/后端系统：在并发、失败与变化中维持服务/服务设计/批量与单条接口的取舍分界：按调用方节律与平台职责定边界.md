---
title: 批量与单条接口的取舍分界：按调用方节律与平台职责定边界
status: active
type: pattern
updated: 2026-09-23
change_rate: low
confidence: high
review_after: 2029-09-23
tags:
  - backend/api
  - backend/scalability
  - defect-analysis
sources:
  - "https://docs.github.com/en/rest/using-the-rest-api/getting-started-with-the-rest-api?apiVersion=2022-11-28"
  - "https://martinfowler.com/articles/richardsonMaturityModel.html"
---

# 批量与单条接口的取舍分界：按调用方节律与平台职责定边界

> **一句话总结**：单条接口的 N 次调用在客户端堆出循环与延迟，服务端却在感知不了一把调用的事务与审计语义——提供批量接口的判据按调用方节律与服务端职责推，批量的事务边界、部分失败与大小上限三件事在接口约定里显式化。

## 要解决的问题

平台接口有一类高频争论：**要不要为某个调用方加批量接口**。不加的代价真实存在：客户端发起 N 次单条调用，网络往返 × N、延迟叠加 × N，调用方在应用层拼装"事务感"（第 50 条失败后自己回滚前 49 条？回滚不了）；加的代价同样真实：批量接口引入新的语义面——部分失败怎么办、批量大小上限在哪、事务边界到哪条记录。**接口设计的判断在这组代价间摇摆，摇摆的根源是没有判据**。本库后端域"幂等性的完整谱系"讲过调用语义的精确性；本篇给批量接口的决策骨架：**按调用方节律（是一把动作还是流式动作）与服务端职责（事务、配额、审计谁来做）推边界，把批量的三个语义问题在接口约定里显式化**。

## 机制：两把尺子的量法

**第一把尺：调用方的节律**。调用方发起的这组操作是"一把"还是"一股"——**一把**指逻辑上同时发生的动作（导入一个含 200 行的文件、一次给 50 个用户授权），中断后整组作废重来是合理的语义；**一股**指持续发生的动作（消息处理循环里逐条上报），天然逐条、没有"整组"语义。一把的动作缺批量接口时，调用方用循环模拟"一把"，代价是 N 倍往返与残缺的事务感；一股的动作套批量接口，服务端被迫定义"批多大、超时多久"的伪节律。**节律判断错，批量接口要么没人用、要么用错**。

**第二把尺：服务端的职责边界**。这组操作的原子性应该由谁保证——如果业务语义要求"要么全成要么全败"（一批配置项必须整体生效），原子性是服务端职责，批量接口 + 单事务是正确形态；如果调用方自己定义成败（每条独立记账，失败单独重试），逐条调用 + 客户端编排反而干净。**"一把 + 服务端原子"的组合才必须批量接口，两个条件缺一个，单条循环都成立**。

判据的反面同样有用：**不加批量接口的理由清单**——调用方节律是一股、服务端无需原子性、单条已配重试与幂等、批量的部分失败语义说不清楚。四条里有两条成立，批量接口的需求多半是伪需求（调用方真正缺的是连接复用与并发，按本库后端域"连接池的容量数学"解决）。

## 看完能判断：批量接口的三个约定问题

决定提供批量接口后，三个语义问题在接口约定里显式化，缺一不可：

**事务边界**：全批单事务（全成全败，失败返回第一个错的序号）还是逐条独立（返回每条的成败明细）——两种都合法，**按业务不变量选**："配置项整体生效"选前者，"导入数据允许部分成功"选后者。不声明的批量接口会把它自己的默认行为强加给所有调用方。

**部分失败的返回形态**：逐条独立的批量必须返回**每条的定位信息**（序号、业务键、失败原因），调用方能精确重试失败子集；只返回"整体失败/成功"的批量接口把重试粒度逼回整批，部分成功的数据在重试时要靠幂等性挡重复（幂等键的约定随之必要）。

**大小上限与分批规则**：批量上限按服务端的事务时长与内存画像定（不是拍一个数），超限返回明确错误码；超过上限的调用方分批——**分批的边界破坏事务语义，全成全败型批量在文档里必须写明"超过 N 条请走异步任务接口"**，用任务接口承接大容量而不是放宽上限。

## 边界

三条边界防止防线走形。

**批量接口不改变配额与限流的计量**：一批 100 条按 100 条计量（或批量接口单独计价），按"一次调用"计量会被刷——计量口径与上限在接口约定里对齐，限流按"逻辑操作数"而不是"请求数"。

**异步任务接口是批量的近亲但语义不同**：超过同步上限的批量走任务接口（提交 → 轮询/回调 → 结果明细），任务是"一把"的大号形态，它有自己的幂等与断点续跑语义（本库正确性域"可恢复批处理的幂等与断点续跑"）——把大批量硬塞进同步接口与把小批量塞进任务接口是同一种判断错误。

**REST 语义的批量不必削足适履**：批量端点的资源建模按"批"本身建模（POST /users:batch 或独立资源），与单条端点并存——强行用 PATCH 数组模拟批量会丢掉部分失败的明细语义。

## 验证

可复现的验证路径：

1. 节律核对：调用方场景清单逐条标注"一把/一股 + 原子性归属"，批量接口的需求只对"一把 + 服务端原子"成立——清单进设计文档；
2. 部分失败验证：构造含非法条目的批量请求（逐条独立型），预期返回逐条明细、非法条目定位精确、合法条目已生效；重试失败子集预期成功且已生效条目不重复（幂等键生效）；
3. 上限验证：超限请求预期收到明确错误码与分批指引；全成全败型批量在事务超时场景下预期整批回滚；
4. 断言锚点：**约定三要素（事务边界、明细返回、上限）在接口文档齐备**，缺任一的批量接口不发布。

```text
节律断言 = 一把 + 服务端原子 才有批量
明细断言 = 部分失败可定位可重试
上限断言 = 超限错误码明确，分批指引可达
```

批量接口的判断链很短：节律定需求、职责定形态、三要素定约定——三步推完，加不加批量从争论变成核对。

## 相关

- [[工程知识/后端系统：在并发、失败与变化中维持服务/任务与并发/幂等性的完整谱系：从操作语义到删除接口的天然陷阱]] —— 批量重试的幂等键约定来源
- [[工程知识/后端系统：在并发、失败与变化中维持服务/服务设计/连接池的容量数学：池大小与排队等待的权衡]] —— "一股"调用方的 N 次往返优化路径（连接复用与并发）
- [[工程知识/软件构建：让变化可以理解、验证与交付/架构与代码/API演进的兼容性纪律：废弃流程与版本策略]] —— 批量接口约定自身的演进纪律
- [[工程知识/数据系统：在并发与故障中保存事实/事务与存储/事务隔离决定并发读写能观察到什么]] —— 批量单事务的隔离背景
