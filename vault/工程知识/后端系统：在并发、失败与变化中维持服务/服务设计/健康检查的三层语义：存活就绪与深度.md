---
title: 健康检查的三层语义：存活就绪与深度
type: concept
status: active
updated: 2026-09-22
review_after: 2027-09-22
change_rate: medium
confidence: high
tags:
  - reliability/health-check
  - operations
  - resilience
sources:
  - "https://kubernetes.io/docs/tasks/configure-pod-container/configure-liveness-readiness-startup-probes/"
  - "https://sre.google/sre-book/monitoring-distributed-systems/"
  - "https://martinfowler.com/articles/patterns-of-distributed-systems/health-check.html"
---

# 健康检查的三层语义：存活就绪与深度

## 要解决的问题

“服务健康吗”这一个问句至少混了三层完全不同的问题：进程还活着吗（liveness）、能接流量了吗（readiness）、依赖都正常吗（deep/dependency health）。混用的代价：用一个“总健康”端点做全部决策——编排器看它重启进程（实际是依赖故障，重启雪上加霜）、负载均衡看它摘节点（实际是启动中，误摘）。要解决的问题是**把健康检查按语义分成三层**，每层有不同的被问者（谁问）、不同的判断（测什么）、不同的后果（答错怎么办）。这是服务发现与负载均衡（见 [[工程知识/后端系统：在并发、失败与变化中维持服务/服务设计/服务发现与负载均衡决定请求发往哪里.md]]）的摘除决策依据，与限流熔断（见 [[工程知识/后端系统：在并发、失败与变化中维持服务/任务与并发/限流熔断与隔离控制故障半径.md]]）构成过载防御的搭档（熔断按错误率摘除，健康检查按状态摘除）。

## 机制

**三层语义**。
- 存活（liveness）：进程还能干活吗（事件循环活着、没死锁）。测法：进程内自检（内存端点、心跳线程）。答错后果：重启（编排器杀掉重启）。Kubernetes 的 liveness probe。注意：liveness 测“自己”，不测依赖（依赖挂了重启自己没用）。
- 就绪（readiness）：能接流量吗（初始化完成、关键依赖可用、队列没满）。测法：初始化标志 + 关键依赖的轻探测。答错后果：摘出负载均衡（不重启，等恢复）。Kubernetes 的 readiness probe。
- 深度（deep health）：全依赖树的状态（数据库、缓存、下游服务逐个探）。测法：逐依赖探（带超时与缓存）。被问者：人与监控（排障用），不做自动决策（依赖故障 ≠ 自己该重启/摘除，深度检查是诊断信息不是决策信号）。

**混淆的三种事故形态**。
- liveness 里测依赖：数据库抖动 → 全实例 liveness 失败 → 全实例被重启 → 冷启动风暴 + 数据库连接风暴（重启后重连）→ 数据库更抖。正反馈失控螺旋。
- readiness 里做重活：就绪探测里逐个探全部依赖（每个 200ms 超时）→ 探测慢 → 编排器判“不就绪”摘流量 → 实际服务正常。误摘事故。
- 深度检查挂到负载均衡：依赖故障 → 深度失败 → 全部节点摘除 → 服务整体下线（单点依赖故障放大成全服务下线）。爆炸半径失控。

**检查的工程细节**。
- 探测的成本要小（liveness 每几秒一次，探测本身不能成为负载）。
- 探测的超时要短（超时的探测 = 探测自身故障，决策依据丢失）。
- 探测结果要缓存（依赖探测 500ms 结果缓存 5s，避免每次探测全打依赖）。
- 语义要暴露给问者（HTTP 码区分：200 活/就绪，503 不就绪，探深度给结构化 JSON）。编排器与 LB 各取所需。

## 背景与代价

思想背景：健康检查从 LB 的存活探测（1990s LVS/F5 的后端探活）演化，Kubernetes（2015+）把 liveness/readiness 二分法写进编排标准（startup probe 是 2020 补充，解决慢启动应用的误杀）。SRE 手册（2016）明确“健康检查要便宜、可聚合、语义明确”。深度检查的定位讨论（是否进决策路径）是 2018+ 的共识收敛：依赖状态做诊断，不做摘除依据（摘除决策只看“我能不能服务”，不看“为什么不能”）。

最精巧的一笔是**readiness 与 liveness 的后果分离**。同为“不健康”，后果截然不同：不就绪摘流量（保护用户，不伤自己），不存活重启（修复自己，不管流量）。分离后两类故障各自走对路：启动慢/依赖抖动走摘除（恢复后自动回来），死锁/内存崩走重启（换新实例）。混在一起时后果错配（依赖故障被重启、死锁被摘除留着耗资源），错配的连锁（重启风暴）比原故障更伤。这一笔的本质是**把“故障的修复动作”与“故障的类型”对齐**，与熔断器的半开设计（探测恢复再放流量）同构：恢复路径是探测出来的，不是猜出来的。代价要摆明：三层端点的维护成本（三套探测逻辑、依赖清单的持续同步——新依赖上线忘了进深度检查，深度信息残缺；依赖下线没清理，深度检查永久红）；探测的调优是持续工程（间隔、超时、阈值的三元组按应用特性调，默认值只是起点）；“深度检查不进决策”的纪律要守（人性倾向“看到红就想自动化”，把深度红接进自动摘除是事故配方，深度红的正确响应是告警人看）。

## 边界

- **liveness 只测进程自身，依赖一律不进**。依赖故障时 liveness 保持绿（自己仍在运行），让 readiness 摘流量。这条红线防的是重启风暴的正反馈。
- **readiness 的探测要轻、要快、要缓存**。就绪判断的 SLA 是秒级（摘除要快），探测里做重活（逐依赖真连）让摘除决策比故障还慢。就绪探测 = 标志位 + 最多一个关键依赖的缓存探测。
- **深度检查只给人看，不接自动决策**。它的价值是排障（一眼看到哪个依赖红了），接进自动摘除会放大单点故障。监控告警按深度分片（依赖红告警给对应 Owner，健康页聚合展示）。
- **启动期的专用通道（startup probe）**。慢启动应用（JVM 预热、缓存回填）用 startup probe 保护（启动期间不跑 liveness，避免没启动完就被判定为异常）。启动完成前 readiness 不就绪（不接流量），启动完成后切正常探测。

## 验证

- **依赖故障演练**：下游依赖注入故障，观测三层检查的行为。预期：深度红（诊断正确）、readiness 不就绪（摘流量正确）、liveness 保持绿（不重启正确）。三层语义分明的直接证据。
- **重启风暴复现**：故意把依赖探测放 liveness（错误配置），注入依赖抖动。预期：实例批量重启、连接风暴、依赖加剧抖动（正反馈螺旋）。反模式的实证（教学价值）。
- **摘除与恢复时延实测**：实例故障到 LB 摘除的时延、恢复到重新接入的时延。预期：摘除时延 < 探测间隔 + 超时（秒级），恢复时延受就绪探测节奏约束。决策链路的时延预算证据。

## 可运行示例与案例回流：探针的账

```text
三层探针错配的推演账（通用形态）：

存活（liveness）答"进程要不要重启"：只查进程自己（死锁、无响应）
    错配账：把下游依赖塞进存活检查 → 下游抖动触发全量重启风暴
  就绪（readiness）答"能不能接流量"：查自身关键路径（线程池、缓存预热）
    错配账：启动预热 60 秒但就绪检查 1 秒通过 → 流量打进未预热实例全部超时
  深度检查答"业务是否真的健康"：带真实依赖读（拨测类），频率低、报警而非摘流
  发布联动账：就绪不通过 → 摘流量但不重启；存活失败 → 重启（重启是最后手段）
```

案例回流：下游依赖进存活检查的实录——缓存集群抖动 30 秒，存活探针失败触发全实例重启，恢复时间从 30 秒变 10 分钟（重启风暴叠加）；存活只查进程自身、依赖降级走业务逻辑后同类故障消失。预热缺失的实录——新实例启动 3 秒过就绪，本地缓存全空，前 2 分钟慢查询打挂数据库；就绪检查挂上预热完成标记后平稳。就绪不摘流的实录——实例无响应但探针端口还在应答（应用线程池满、探针独立线程），流量继续打进已经失效的实例；就绪检查要占业务线程池或查队列水位。摘流量与重启的顺序配合见 [[工程知识/性能工程：从用户等待到资源瓶颈/存储与网络/滚动重启的摘流量顺序：先停新请求，再让旧请求做完.md]]、对外部依赖的探测纪律见 [[工程知识/性能工程：从用户等待到资源瓶颈/存储与网络/依赖服务的健康检查要带拨测与退避：探测结果决定流量去留.md]]。

## 相关

- [[工程知识/后端系统：在并发、失败与变化中维持服务/服务设计/服务发现与负载均衡决定请求发往哪里.md]] —— 摘除决策的下游机制
- [[工程知识/后端系统：在并发、失败与变化中维持服务/任务与并发/限流熔断与隔离控制故障半径.md]] —— 错误率侧的摘除搭档
- [[工程知识/后端系统：在并发、失败与变化中维持服务/服务设计/背压的传递链路：从下游饱和到上游减速.md]] —— 过载防御的链路视角
- [[工程知识/后端系统：在并发、失败与变化中维持服务/服务设计/优雅停机的完成语义：排空连接与任务交接.md]] —— readiness 摘除是排空前半步
