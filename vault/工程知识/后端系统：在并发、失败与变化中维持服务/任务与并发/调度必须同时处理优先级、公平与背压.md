---
title: 调度必须同时处理优先级、公平与背压
type: concept
status: active
updated: 2026-08-31
review_after: 2027-02-28
change_rate: stable
confidence: high
tags:
  - algorithms/heap
  - software/scheduling
  - concurrency
sources:
  - "https://docs.python.org/3/library/heapq.html"
  - "[[工程知识/后端系统：在并发、失败与变化中维持服务/分布式可靠性/部分失败决定分布式系统的设计]]"
---

# 调度必须同时处理优先级、公平与背压

堆解决的是“快速取出当前最小/最大优先级”，调度系统还要解决相同优先级、并发、饥饿、取消、持久化和多节点归属。

同一个优先级队列在低负载时看起来足够，但在高优先级持续到达时，低优先级任务可能永远不被执行；无界队列则把调度问题推迟成内存耗尽和 deadline 违约。调度器真正承诺的是“谁在什么资源和截止时间内获得一次执行机会”，因此优先级、公平和背压必须一起设计。

## 数据结构边界

二叉堆的插入和弹出通常为 `O(log n)`，查看堆顶为 `O(1)`。Python `heapq` 是 min-heap，不是线程安全队列，也不会自动给不可比较对象稳定排序。

```python
from dataclasses import dataclass, field
from itertools import count
import heapq

seq = count()
queue = []

@dataclass(order=True)
class Entry:
    priority: int
    sequence: int
    task_id: str = field(compare=False)

heapq.heappush(queue, Entry(priority=10, sequence=next(seq), task_id="t1"))
```

`sequence` 既避免同优先级时比较 task 对象，也提供稳定 FIFO tie-break。进入堆后不要原地修改参与比较的字段；更新优先级通常采用新 entry + 旧 entry tombstone，弹出时跳过失效项。

## 从堆到调度器

| 问题 | 仅靠 heap 能否解决 | 工程机制 |
| --- | --- | --- |
| 同优先级稳定顺序 | 否 | 单调 sequence/timestamp |
| 多线程安全 | 否 | 锁或 `queue.PriorityQueue` |
| 任务取消/改优先级 | 否 | entry id、tombstone、版本 |
| 低优先级饥饿 | 否 | aging、公平队列、配额 |
| 多租户隔离 | 否 | tenant queue、并发/资源配额 |
| 进程崩溃恢复 | 否 | 持久队列、lease、visibility timeout |
| 多节点重复执行 | 否 | owner、attempt、幂等、fencing token |
| 远程命令生命周期 | 否 | [[工程知识/后端系统：在并发、失败与变化中维持服务/任务与并发/任务生命周期必须覆盖进程、日志、超时与清理]] |

## 调度策略

- **Strict priority** 延迟敏感任务最优，但可能让低优先级永久饥饿。
- **Weighted fair queue** 按租户/类别给权重，在优先级与公平之间取舍。
- **Deadline/EDF** 适合有明确截止时间的任务，但需要可预测执行时间和超载策略。
- **Aging** 随等待时间提高有效优先级，减少饥饿。
- **Admission control** 在入队前检查资源；无界队列只是把故障延迟到内存或 deadline。

优先级是业务策略，不应由调用方任意填写。服务端根据身份、任务类型、deadline 和配额计算或约束，避免高优先级滥用。

## 验证

- 相同优先级的稳定顺序、取消和改优先级；
- 大量高优先级流量下的低优先级最大等待时间；
- worker 死亡、lease 过期和重复投递的幂等性；
- 队列长度、等待时间分位数、执行时间、丢弃/取消/重试率；
- 多租户资源上限与优先级权限。

关联：[[工程知识/后端系统：在并发、失败与变化中维持服务/基础设施/基础设施选型先比较责任边界与故障模型]]、[[工程知识/后端系统：在并发、失败与变化中维持服务/分布式可靠性/部分失败决定分布式系统的设计]]、[[工程知识/AI 系统工程：从模型能力到生产能力/安全与治理/执行证据必须独立于AI结论]]。
