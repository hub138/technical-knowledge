---
title: 幂等性的完整谱系：从操作语义到删除接口的天然陷阱
type: concept
status: active
updated: 2026-09-22
review_after: 2027-09-22
change_rate: low
confidence: high
tags:
  - backend/transactions
  - idempotency
  - distributed-systems
sources:
  - "https://www.rfc-editor.org/rfc/rfc9110#name-idempotent-methods"
  - "https://stripe.com/docs/api/idempotent_requests"
---

# 幂等性的完整谱系：从操作语义到删除接口的天然陷阱

## 要解决的问题

分布式系统里重试是家常便饭：超时后重发、消息队列 at-least-once 投递、用户双击提交。重试引发的问题只有一个：同一操作被执行两次。幂等性要解决的问题就是让重复执行产生与一次执行相同的效果。超时、重试与幂等共同定义调用语义（见 [[工程知识/后端系统：在并发、失败与变化中维持服务/分布式可靠性/超时、重试与幂等共同定义调用语义.md]]），本文把其中"幂等"一角展开成完整谱系。

## 两个层次：操作语义层与接口设计层

"幂等"在两个层次被使用，混用会带来设计错误。

**操作语义层**（数学定义）：f(f(x)) = f(x)，一次执行与多次执行效果相同。这是属性，不是实现。POST 创建订单天然不幂等（两次创建两个订单），PUT 全量更新天然幂等（两次写入同一状态）。RFC 9110 对 HTTP 方法幂等性的定义就在这一层。

**接口设计层**（工程实现）：通过给操作附加唯一键（idempotency key），把天然不幂等的操作变成幂等的。Stripe 的做法是代表：客户端生成 UUID，服务端记住"这个键已处理过，结果是 X"，重复请求直接返回上次结果。

两层的关系是：**键是外部世界给操作盖的章，不是操作本身的属性**。键的正确姿势：第一次请求带键，服务端把键与处理结果在同一事务里落库，重复请求查键直接返回。键与业务操作必须在同一事务边界内，否则崩溃后键丢了，重复请求会二次执行。

## 四类接口的天然属性

| 接口 | 天然幂等 | 机制 | 重试后的风险 |
| --- | --- | --- | --- |
| GET/PUT/DELETE | 是 | 语义本身保证 | 无 |
| POST 创建 | 否 | 两次创建两个实体 | 重复下单 |
| POST 执行命令 | 否 | 两次执行两次副作用 | 重复扣款 |
| 带幂等键的 POST | 工程上是 | 键去重 | 键过期后重试二次执行 |

这张表的本质：幂等是接口语义的属性，不取决于重试几次。天然幂等接口（PUT）不需要键，天然不幂等接口（POST 创建）必须有键。这也解释了 DELETE 为什么幂等：删除不存在的资源返回 404，删除已删除的资源还是 404，效果与执行一次相同。

## 背景与代价

幂等键的思想背景是用确定性替换随机性。没有键时，服务端面对重复请求只能猜（重发还是新请求）；有键时，判断变成确定性查询（这个键见过没有）。它替代的两种更早做法：客户端手动确认（用户体验差）与服务端启发式去重（按请求体匹配，误判率高）。

最精巧的一笔是键与结果同事务落库：去重表和业务表在同一事务里写，"记住键"与"完成业务"原子发生。代价要摆明：每笔写操作多一次键查表与键写入；去重表按业务量增长，键不能无限保留，过期窗口必须与业务重试窗口期匹配，这是持续的维护成本。

## 边界

- **键的过期窗口是设计坑**。Stripe 键保留 24 小时，窗口外重试同一键会被当新请求。支付类敏感业务要自定窗口，并配合对账兜底（见 [[工程知识/缺陷分析：从个案到体系/正确性/静默失真的检测靠对账与不变量而不是报错.md]]）。
- **并发重试的竞态**。两个同键请求并发到达，都在写入前去重表，双双查不到对方，双双执行。要配唯一索引或分布式锁兜底，锁的正确用法见 [[工程知识/后端系统：在并发、失败与变化中维持服务/分布式可靠性/租约必须配合FencingToken阻止过期持有者.md]]。
- **DELETE 幂等有前提**：删除无副作用。若删除触发退款、级联清空子资源，幂等性不再成立，DELETE 退化为需加键的普通操作。
- **异步消息同构**。at-least-once 投递下消费端去重表与幂等键同构（键 = 消息 dedup id 或业务键）。

## 验证

- **重复请求验证**：同一键连发两次，预期第二次返回第一次的结果且业务表只多一条记录。对不上就停下查：键落库的事务边界是否完整、并发兜底是否生效。
- **窗口外重试验证**：键过期后重试按新请求处理，预期对账环节能发现并修正双执行。
- **重放验证**：旧键重放被去重表拦截，预期拦截有日志可查。

## 相关

- [[工程知识/后端系统：在并发、失败与变化中维持服务/分布式可靠性/超时、重试与幂等共同定义调用语义.md]] —— 幂等在调用语义三件套里的位置
- [[工程知识/后端系统：在并发、失败与变化中维持服务/分布式可靠性/Saga把长事务拆成补偿链，代价是失去隔离性.md]] —— 补偿操作要求幂等，谱系是其前置
- [[工程知识/数据系统：在并发与故障中保存事实/事务与存储/事务隔离决定并发读写能观察到什么.md]] —— 并发同键请求的隔离问题
- [[工程知识/后端系统：在并发、失败与变化中维持服务/分布式可靠性/重试的正确姿势：退避、抖动与重试预算.md]] —— 重试安全的前置条件
- [[工程知识/后端系统：在并发、失败与变化中维持服务/服务设计/HTTP语义的分层：方法、状态码与头部的契约角色.md]] —— 方法幂等语义的谱系展开
