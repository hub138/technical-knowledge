---
title: 分布式锁的正确与错误用法：fencing与租约
type: concept
status: active
updated: 2026-09-22
review_after: 2027-09-22
change_rate: medium
confidence: high
tags:
  - distributed-systems
  - concurrency
  - correctness
sources:
  - "https://martin.kleppmann.com/2016/02/08/how-to-do-distributed-locking.html"
  - "https://redis.io/docs/latest/develop/use/patterns/distributed-locks/"
  - "https://www.usenix.org/legacy/publications/library/proceedings/osdi04/tech\_docs/full\_papers/mccord\_pal/mccord\_pal\_html/"
---

# 分布式锁的正确与错误用法：fencing与租约

## 要解决的问题

多实例服务共享资源（一份数据、一个外部调用配额、一个任务队列）需要互斥，单机锁（mutex）管不到跨进程。分布式锁要解决的问题是**跨机器的互斥怎么做才正确**——这里的“正确”很硬：进程暂停（GC、VM 冻结）、时钟漂移、网络分区都会让“我持有锁”的信念过期。Martin Kleppmann 与 Redis 作者 antirez 的著名论战（2016）把问题钉死：Redlock 算法在主从切换下不安全，多数“分布式锁”用法其实是“减少低效的efficiency锁”，不是“保证正确性的correctness锁”。分清这两类是本文的核心。

## 机制

**效率锁与正确性锁的分别**。
- 效率锁（efficiency lock）：目标是避免重复劳动（两个实例都跑同一个定时任务，浪费但不出错）。锁偶尔失效可接受（多跑一次幂等任务无害）。实现要求低：单 Redis 实例的 SET NX EX 够用。
- 正确性锁（correctness lock）：目标是互斥必须绝对成立（两个实例同时写同一行数据 = 腐败）。锁失效不可接受。实现要求高：fencing token + 下游验证，或共识系统（ZooKeeper/etcd）+ fencing。

**租约（lease）：锁的到期设计**。持有者可能死掉（crash 后锁永不释放 = 死锁），租约解决：锁带 TTL，到期自动释放，持有者在 TTL 内续租。代价：持有者在 TTL 内完成工作的假设可能破产——GC 暂停/网络延迟让“租约还剩 1 秒”的工作做了 3 秒，锁已被别人拿走，原持有者的写操作变成带外写（这是 Kleppmann 论证的核心风险）。

**fencing token：正确性锁的最后一道防线**。锁服务每次授锁发单调递增的 token（33、34、35…），资源方（数据库/存储）只接受比已见过最大 token 更新的写。即使持有者暂停后“复活”，它的 token（33）已过期（存储见过 34），写被拒。fencing 把“锁的正确性”从锁服务移到资源方——存储必须参与验证，只靠锁服务的承诺不够。

**ZooKeeper/etcd 的会话锁**。基于共识系统：客户端会话（session）在 ZooKeeper 上建临时顺序节点，最小序号者获锁；会话断（crash/网络）节点自动删除，锁释放。共识系统保证线性一致（无脑裂），比 Redis 主从可靠。但仍要 fencing：ZK 的会话超时后客户端可能还在跑（暂停复活问题同样存在），存储侧验证不可省。

## 背景与代价

思想背景：分布式锁的工程史就是“用不安全的组件搭安全结构”的尝试史。Redis SETNX 锁（2011+）因简单普及，Redlock（antirez 2014）试图多实例多数派抗主从切换。Kleppmann 的批评（2016，DDIA 作者）用两个模型（进程暂停、时钟漂移）证明 Redlock 不满足正确性，论战以“Redlock 可作效率锁、正确性要 fencing”收场。Chubby（Google，2006 论文）是工程师视角的经典（粗粒度锁 + 会话 + sequencing token），ZooKeeper 的 recipes 继承其思想。

最精巧的一笔是**fencing token 把正确性责任从锁服务移交给存储**。锁服务（无论 Redis 还是 ZK）对“持有者此刻是否还在跑”永远无法保证（暂停复活是物理世界的事实），唯一能拦住过期写的是看到写请求的一方（存储）。单调 token + 存储侧“只接受更新 token”的规则，把互斥的正确性锚定在数据的安全防线（存储的事务与验证），而非易错的协调服务。这一笔的深层意义：分布式系统的正确性要落在“数据落地处”的防线，不是“协调处”的承诺。代价要摆明：fencing 要求存储配合（数据库要存 token 或支持条件写），不支持 token 验证的存储（多数外部 API：第三方支付、对象存储）做不了完整 fencing，这类场景的正确性锁只能靠“可补偿设计”（幂等键、对账），见幂等设计（见 [[工程知识/数据系统：在并发与故障中保存事实/数据管道与流处理/数据编排要保证依赖、重试与幂等.md]]）。ZK/etcd 的运维成本（三节点起步的共识集群）对多数团队是大锤（效率锁场景用 Redis 单点足够）；租约 TTL 的调优是持续工程（太短 = 正常工作被截断，太长 = crash 后的死等），按任务时长分布 P99 × 安全系数设置。

## 边界

- **先问“锁失效会怎样”再选实现**。答案“浪费但不坏” → 效率锁，Redis SETNX TTL 即可。答案“数据腐败” → 正确性锁，fencing 或共识系统 + fencing，或干脆改设计（把互斥需求消掉：单队列消费、数据库唯一约束、幂等设计）。
- **“用 ZK 就安全”是错觉，会话复活问题依旧**。ZK 解决脑裂（锁不会被两人同时持有），不解决持有者暂停后过期写（那是 fencing 的活）。正确性锁的完整形态：共识锁服务 + fencing token + 存储验证。
- **第三方 API 场景的正确性靠幂等不靠锁**。外部支付、发短信等不可 fencing 的资源，分布式锁只是“减少并发”的软手段，真正保证唯一的是幂等键（业务侧唯一 ID，下游去重）。锁 + 幂等双保险，不要单押锁。
- **看门狗续租不是安全机制**。Redisson 的看门狗（自动续租）让锁“看起来一直有效”，但它延长的是失效概率不是消除（暂停复活时看门狗同样暂停）。看门狗优化效率锁的可用性，对正确性锁无补。

## 验证

- **暂停复活实验**：持锁进程注入长 GC 暂停（或 SIGSTOP），暂停期间锁被他人获取，恢复后原进程继续写。预期：无 fencing 时写成功（带外写发生，互斥破产的实证）；有 fencing 时写被拒。fencing 必要性的直接证据。
- **主从切换实验**：Redis 主从切换窗口（failover 期间），两个客户端先后获锁。预期：Redlock/单 Redis 出现双持锁（切换窗口的不安全窗口暴露），ZK 无此问题。实现可靠性的分界实证。
- **TTL 与任务时长失配实验**：TTL 10s、任务 P99 15s 的负载，观测锁过期后并发进入。预期：TTL < 任务时长时互斥频繁破产（租约假设的边界），TTL 设置要按任务分布调。

## 相关

- [[工程知识/数据系统：在并发与故障中保存事实/数据管道与流处理/数据编排要保证依赖、重试与幂等.md]] —— 锁失效的兜底：幂等设计
- [[工程知识/后端系统：在并发、失败与变化中维持服务/任务与并发/队列和流系统用时间与容量换取解耦.md]] —— 消解互斥需求的替代结构
- [[工程知识/缺陷分析：从个案到体系/并发/案例三：并发两面——写回调卡死与心跳竞态.md]] —— 并发时序病理的案例解剖
- [[工程知识/性能工程：从用户等待到资源瓶颈/处理器与内存/无锁数据结构的证据门槛：CAS与内存序.md]] —— 单机 CAS 与分布式锁是互斥+fencing 的两个尺度
- [[工程知识/缺陷分析：从个案到体系/并发/时间型缺陷的判定树：过期竞态与时钟依赖.md]] —— 租约失效是时间缺陷最大族，fencing 是结构解
