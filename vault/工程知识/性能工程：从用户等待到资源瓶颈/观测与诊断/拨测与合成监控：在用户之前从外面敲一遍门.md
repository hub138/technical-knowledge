---
title: 拨测与合成监控：在用户之前从外面敲一遍门
status: active
type: pattern
updated: 2026-09-23
change_rate: low
confidence: high
review_after: 2029-09-23
tags:
  - observability
  - monitoring
  - reliability
sources:
  - "https://prometheus.io/docs/prometheus/latest/configuration/configuration/#http_get_config"
  - "https://www.sre.google/sre-book/monitoring-distributed-systems/"
---

# 拨测与合成监控：在用户之前从外面敲一遍门

> **要点**：指标与日志是服务从内部看的自述，拨测是探针从外部敲门——外部视角能发现"组件全绿但用户进不来"的失效，拨测的设计按真实用户行为取样、按告警有据定频率、与内部指标分层互相补盲。

## 要解决的问题

监控体系有一个结构性盲区：**所有内部指标都从服务自己的位置采集，服务的入口失效（DNS 解析、负载均衡、证书过期、网关路由、防火墙规则）不经过服务本身**，组件指标全绿而用户侧 100% 失败的形态真实存在——证书过期、LB 健康检查误摘、DNS 记录错误，这些失效点上服务进程毫发无损。本库可观测性域"指标、日志、Trace与剖析各答一问，合起来才构成证据"确立了证据体系的内部分工；本篇补外部的那个问号：**"用户能不能进来"由拨测回答**——探针按真实用户的路径周期性访问，外部视角的失效在这里变成可见的失败记录。

## 机制：外部视角看什么、怎么敲

**拨测的三层深度**，各答不同的问题：

**可用性层（端口与握手）**：探针按周期做 TCP 连接或 HTTP GET，回答"门开着吗"——最便宜、频率最高，覆盖入口链路的存活。它发现不了"门开了但服务错了"。

**功能层（关键路径回放）**：探针执行一段固定的用户旅程（打开首页、登录、提交订单的只读影子操作），断言响应内容与状态码，回答"进去之后路通吗"——这是合成监控的核心：**用最小的合成流量覆盖最关键的用户路径**，路径选取按"失效后业务损失最大"排序，不追求覆盖全部功能。

**质量层（性能水位）**：拨测同时记录响应时间分位数，从外部看端到端延迟——内部指标测组件耗时，外部拨测测用户全程（含网络、CDN、边缘），两者的差值就是"用户多付的等待"，它是 CDN 与网络路径问题的敏感信号（本库性能工程域"端到端网络延迟来自排队、协议、传输与处理"的分段视角在外部视角的对应物）。

**探针位置决定发现能力**：单点拨测只能证明"探针那里能进来"——多地域探针（至少覆盖用户的主要网络运营商/地域）才能区分"全域失效"与"局部运营商失效"；探针位置的选取按用户分布，**从探针那里看不见的失效，对那部分用户就是真实失效**。多探针的收益是失效的地域定位与去噪能力（多数派判据的根基），代价是探针基础设施的维护与合成流量的成本——这笔投入对用户分布跨地域的业务收益远大于代价，对单地域内网系统则单探针加证书检查已经够用，按用户分布定投入规模。

## 看完能判断：拨测设计与告警的纪律

**频率与成本的平衡**：功能层拨测每次都是真实请求，频率过高给生产加压、过低则失效发现延迟长——按"告警确认窗口"定频率：想让 P95 的发现延迟在 1 分钟内，多探针错峰的周期约 1-5 分钟；可用性层可以更密，功能层保持稀疏。**频率的依据是发现延迟要求，拍脑袋的频率要么贵要么钝**。

**告警的判据**：单次失败不告警（网络抖动正常存在），连续 N 次跨多数探针失败才告警——"多数探针 + 连续性"两个条件滤掉探针自身的抖动；单探针失败且他探正常，指向探针侧网络问题，降级为记录。**告警判据里必须有"探针一致性"这个维度**，否则拨测自己变成告警噪音源。

**与内部监控的分工边界**：拨测回答"入口与关键路径通不通"，内部指标回答"组件内部健康吗"——**拨测全绿而内部告警，怀疑拨测覆盖不全；内部全绿而拨测告警，怀疑入口链路失效**。两条视角互为对方的盲区探测器，任何一条单独上线都是残缺的。

## 边界

三条边界防止防线走形。

**拨测流量与真实流量隔离**：合成请求要带可识别标识（专用 UA、测试账号），计入独立的统计桶——混入真实流量会污染业务统计与限流决策，测试账号要与真实账号的权限隔离防越权。

**拨测不替代端到端业务对账**：功能层拨测验证"路径走得通"，验证不了"数据算得对"——正确性靠对账体系（本库缺陷分析域"静默失真的检测靠对账与不变量"），拨测的断言深度止步于路径正确。

**证书与域名的失效要显式拨测**：证书剩余有效期、域名解析结果是入口链路的高发失效点，单独建低频检查（每日）——功能层拨测撞上证书失效才发现，发现延迟等于拨测周期，专门的证书拨测把发现提前到"还剩 N 天"。

## 验证

可复现的验证路径：

1. 失效注入验证：测试环境依次注入入口层失效（停 LB、错误 DNS 记录、过期证书），预期拨测在对应层失败而组件指标全绿——外部视角盲区覆盖能力的实证；
2. 噪音验证：制造单探针网络抖动，预期不触发告警（多数探针条件生效）；制造全域失效，预期告警及时触发；
3. 断言锚点：**多地域探针覆盖主要用户分布、告警判据含探针一致性维度、证书与域名有独立检查**；三者齐备后"组件全绿用户失败"形态的失效必然被拨测捕获。

```text
注入断言 = 入口失效仅拨测可见
去噪断言 = 单探针抖动不告警，全域失败必告警
覆盖断言 = 探针地域 × 用户分布核对表齐备
```

拨测是监控体系里唯一站在用户位置的眼睛——三层深度、多探针、一致性判据配齐，外部失效从"用户投诉才知道"变成"用户之前已告警"。

## 相关

- [[工程知识/性能工程：从用户等待到资源瓶颈/观测与诊断/指标、日志、Trace与剖析各答一问，合起来才构成证据]] —— 内部证据体系的分工，拨测是外部视角的第五种证据
- [[工程知识/性能工程：从用户等待到资源瓶颈/存储与网络/端到端网络延迟来自排队、协议、传输与处理]] —— 拨测延迟与组件延迟的差值解读依据
- [[工程知识/后端系统：在并发、失败与变化中维持服务/服务设计/健康检查的三层语义：存活就绪与深度]] —— 内部健康检查与外部拨测的分层对照
- [[工程知识/性能工程：从用户等待到资源瓶颈/观测与诊断/连通性诊断的分层顺序：从链路到应用逐层收窄，不跳层]] —— 拨测失败后的收窄诊断路径
