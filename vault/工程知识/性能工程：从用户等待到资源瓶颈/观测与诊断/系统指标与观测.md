---
title: 系统指标、结构化事件与观测方法
type: playbook
status: active
updated: 2026-09-16
review_after: 2027-02-28
change_rate: medium
confidence: high
tags:
  - "performance/observability"
  - "linux/metrics"
  - "software/resilience"
sources:
  - "https://opentelemetry.io/docs/concepts/observability-primer/"
  - "https://www.honeycomb.io/what-is-observability"
  - "https://man7.org/linux/man-pages/man1/sar.1.html"
  - "https://man7.org/linux/man-pages/man1/iostat.1.html"
  - "[[工程知识/性能工程：从用户等待到资源瓶颈/观测与诊断/性能问题定位]]"
---

# 系统指标、结构化事件与观测方法

指标用于发现“什么时候、哪个资源域发生异常”，trace 用于串起请求路径，profile 用于解释时间或资源花在哪里，日志用于保留离散事件和上下文。任何一种都不能单独完成根因证明。

例如 p99 上升但 CPU 不变，可能是锁、网络、磁盘、GC 或队列等待；只有把 RED 的用户影响与 USE 的资源饱和、trace 的等待段和 profile 的调用路径对齐，才能缩小根因范围。观测系统本身也要暴露采样缺失和 unknown，而不是把空值当成零。

## 四类证据

| 证据 | 最擅长回答 | 不能单独回答 |
| --- | --- | --- |
| Metrics | 趋势、容量、SLO、告警 | 单个请求为何失败 |
| Traces | 一次请求在哪一段等待 | 函数内部 CPU/分配细节 |
| Profiles | CPU/off-CPU/内存在哪个调用路径 | 业务输入和跨服务因果 |
| Logs/Events | 特定状态变化和错误上下文 | 全量趋势与统计分布 |

## 为什么要记录结构化事件

文本日志适合人顺着读，却不适合回答“哪些 VIP 用户在缓存未命中时出现数据库慢查询”。把一次请求的关键上下文放进同一条结构化事件，才能按字段聚合、切片和关联：

```json
{
  "trace_id": "8f14e45f",
  "service": "order-api",
  "endpoint": "POST /orders",
  "user_tier": "vip",
  "duration_ms": 1832,
  "db_duration_ms": 1780,
  "db_query_count": 12,
  "cache_hit": false,
  "status": 200
}
```

这类“宽事件”不是要求把所有数据都塞进日志，而是让一次请求拥有稳定字段：入口、出口、关键业务分支、错误、租户、版本和 trace。高基数字段放事件或 trace，不能直接当 metric label，否则会导致指标系统基数爆炸。

跨 HTTP、RPC、消息和异步任务传播 W3C Trace Context；任一跳丢失 `trace_id`，调用链就会在最需要解释的位置断开。采样优先保留错误和慢请求，再按比例保留正常请求；采集缺失必须显示为 unknown，不能伪装成零。

## USE 与 RED

- **USE（资源视角）**：Utilization、Saturation、Errors；依次检查 CPU、内存、磁盘、网络、连接池、线程池和队列。
- **RED（服务视角）**：Rate、Errors、Duration；从用户请求出发，再下钻到资源。

资源利用率不是瓶颈的充分条件。CPU 100% 可能仍达到 SLO；现代并行 SSD 的 `%util` 也不代表性能极限。必须同时看排队、吞吐和尾延迟。

## 常用 Linux 入口

```text
总体: uptime, vmstat, pidstat
CPU: mpstat, perf, /proc/stat
内存: free, vmstat, /proc/meminfo, PSI
存储: iostat -x, pidstat -d, fio（验证）
网络: ss, ip -s link, ethtool, nstat, tcpdump
历史: sar（确认采样配置和口径）
```

`sar` 适合查看 sysstat 已采集的历史切片，但它无法补回未采集的 trace/profile，也会因采样间隔遗漏短尖峰。命令示例必须和 sysstat 版本、单位、采样间隔、时区、设备映射一起记录。

## 调查顺序

1. 固定问题窗口、时区、版本、请求类型和基线。
2. 用 RED 判断用户影响，用 USE 找资源域；检查队列而非只看利用率。
3. 用 trace 对齐上下游和重试；用 profile/eBPF 深挖 CPU、锁、I/O 或分配。
4. 提出可被最小实验区分的假设，一次只改变一个变量。
5. 验证业务 SLO、资源代价和错误是否共同改善，并保留回滚阈值。

## 数据质量

- counter 先求 rate，并处理进程重启/回绕；gauge 不应被随意累计。
- histogram bucket 设计应覆盖 SLO，平均延迟不能代表尾部。
- 标签维度控制基数；用户 ID、URL 全路径和异常文本不能直接作为 metric label。
- 主机/容器/进程/请求使用稳定 resource attributes 关联。
- 采集缺失与数值为零是不同状态；报告显式显示 unknown。

## 关联

- 总体方法：[[工程知识/性能工程：从用户等待到资源瓶颈/观测与诊断/性能问题定位]]
- CPU/函数：[[工程知识/性能工程：从用户等待到资源瓶颈/观测与诊断/性能剖析与火焰图]]
- 存储：[[工程知识/性能工程：从用户等待到资源瓶颈/存储与网络/存储IO性能取决于访问模式、队列与持久化语义]]
- 网络：[[工程知识/性能工程：从用户等待到资源瓶颈/存储与网络/端到端网络延迟来自排队、协议、传输与处理]]
- AI/Agent 轨迹：[[工程知识/AI 系统工程：从模型能力到生产能力/质量与运营/可靠生成需要结构、证据、拒答与回归]]
- 内存：[[工程知识/性能工程：从用户等待到资源瓶颈/观测与诊断/进程内存异常：堆转储与句柄清单的现场诊断]]

## 验证

1. 数据类型核对：对 counter 求 rate 并处理进程重启与回绕，对 gauge 不做累计；核对后的曲线与业务事件时间点对齐。
2. 分桶覆盖：检查 histogram 的分桶边界是否覆盖 SLO 阈值，确认尾部延迟位于可观测的分桶内；均值正常时单独核对尾部。
3. 标签基数：统计每个指标的标签组合数量，预期不包含用户标识、完整路径与异常文本；基数变化引起采集成本上升时从标签维度收敛。
4. 缺失与零值：主动停止一类采集，确认看板显示为未知而不是零，恢复后状态回到正常。
5. 断言锚点：指标类型与聚合方式对应，分桶覆盖 SLO，标签基数受控，缺失状态与零值可区分。

机制与本质：指标体系的可用性取决于数据类型的语义与标签基数的控制，本质问题在于采集缺失与数值为零是两种状态，混为一谈时会把采集故障读成业务空闲。

## 边界

- 平均值掩盖分布：延迟与排队时长必须看分位数与直方图分桶，均值正常时尾部仍可能已越过门槛。
- 标签基数决定采集成本：把用户标识、完整路径或异常文本直接作为标签，会把存储与查询成本推高到不可用。
- 采集缺失与数值为零是两种状态：区分不开时会把采集故障当成业务空闲。
- 指标只能回答是否异常：定位到具体函数、具体连接或具体请求需要剖析、日志与链路数据配合。
- 采样与聚合有粒度损失：采样率、聚合窗口与分桶边界决定哪些异常能被看见，越过粒度的事件不会出现在结果里。

## 要解决的问题

服务出现延迟恶化时，看板上各层指标都处在正常区间，团队成员无从判断从哪里入手。本篇回答：每一类指标各自回答什么问题、分位数与分桶要按什么依据设计、标签基数为什么需要控制、采集数据缺失怎样与数值为零区分，以及指标之外的证据从哪里取。

