---
title: 优化收益递减决定何时该停手
type: concept
status: active
updated: 2026-09-21
review_after: 2027-03-21
change_rate: medium
confidence: high
tags:
  - design
  - workflow
sources:
  - "https://www.brendangregg.com/blog/2007-04-16/optimizing-organizations.html"
  - "https://www.usenix.org/legacy/event/usenix05/tech/general/krishnamurthy.html"
---

# 优化收益递减决定何时该停手

性能优化有一条和经济学一样的规律：**收益递减**。第一个优化通常吃掉最大的瓶颈（从 10s 到 2s），第二个吃掉次大的（2s 到 1.2s），第三、四个开始与复杂度成本打平，之后每一步的努力换来的改善按数量级缩小。优化没有天然的终点，**停手判据必须显式设定**，否则优化本身变成新的工程风险：代码复杂化、可维护性下降、为 5% 的收益引入新缺陷（优化引入回归的实例见 [[工程知识/缺陷分析：从个案到体系/正确性/案例三十四：省下的句柄与多出的删除——一次优化引入的回归]]）。

优化的总账不是只有延迟：`净收益 = 性能改善的业务价值 − 复杂度成本 − 缺陷风险 − 优化本身的工时`。四个减项里只有第一项可见，其余三项都是隐藏负债——这正是优化容易做过头的结构性原因。

## 三个停手判据

| 判据 | 问的问题 | 量化的样子 |
| --- | --- | --- |
| 目标达成 | 业务指标（转化、成本、SLO）已达标？ | P99 < 200ms 的 SLO 已绿 |
| 瓶颈转移 | 剩余瓶颈在哪、它值多少？ | 新瓶颈改善的边际收益 < 工时成本 |
| 负债临界 | 复杂度增加开始伤维护？ | 优化后代码评审通过率下降、新人上手变难 |

目标判据优先：优化的目标从业务倒推（延迟低到不影响转化、成本低到预算内），达标后继续优化是"为优化而优化"。瓶颈转移判据是技术面的：按剖析数据排序（方法见 [[工程知识/性能工程：从用户等待到资源瓶颈/观测与诊断/性能剖析与火焰图]]），当前瓶颈修完后的下一个瓶颈改善空间值不值得一轮工时，数字上就能比。

## 收益递减的量化

以缓存层优化为例的三轮递减：加缓存（命中率 0%→70%，延迟 800ms→120ms）、调缓存策略（70%→90%，120ms→90ms）、换缓存结构与预热（90%→95%，90ms→85ms）。第三轮 5ms 的收益要付出预热的复杂度与失效路径的额外风险——按每毫秒的工时算，第三轮的成本效率是第一轮的百分之一量级。**判断发生在第三轮之前**：把三轮的预估收益与成本摆出来，第三轮大概率不该做。

尾延迟的特殊性要单独说：P99 与 P999 的优化是另一条递减曲线（GC 停顿、锁竞争、后台任务抖动各自的尾巴），修 P50 到 P99 的收益可能大于修 P99 到 P999。修哪个尾巴按业务人群算：P99 影响 1% 请求，值不值，看这 1% 是不是关键用户（尾延迟的机制见 [[工程知识/性能工程：从用户等待到资源瓶颈/操作系统与运行时/垃圾回收与内存分配决定延迟尾部]]）。

## 过度优化的形态

三种常见的"过"：

1. **微观优化常态**。手写循环替代标准库、位运算替代乘法——在现代编译器与流水线下多数无效，除非剖析数据证明这里是热点（有效执行的判断见 [[工程知识/性能工程：从用户等待到资源瓶颈/处理器与内存/CPU性能来自有效执行与数据供给]]）。
2. **为想象负载优化**。预估"未来可能百万 QPS"而提前做的分布式化，在真实量级到来前都是负债（容量问题的正确姿势见 [[工程知识/后端系统：在并发、失败与变化中维持服务/基础设施/容量规划与压测要用同一个负载模型说话]]）。
3. **单点优化破坏整体**。某层吞吐提上去，把压力转移给下游没有容量的层，系统整体反而更脆——优化要回到端到端视角（端到端的延迟分解见 [[工程知识/性能工程：从用户等待到资源瓶颈/存储与网络/端到端网络延迟来自排队、协议、传输与处理]]）。

## 优化前的对账清单

动手前先回答：瓶颈有剖析证据吗（不是直觉）？目标是业务倒推的吗？这个优化的回退方案是什么？复杂度增加多少？四个问题答不清就动手，是优化的"未测先飞"。清单与通用的技术方案约束书写法一致（见 [[工程知识/软件构建：让变化可以理解、验证与交付/架构与代码/技术方案先写清约束、取舍与验证]]）。

## 边界

递减规律有例外：**量级型优化**（算法换代、O(n²) 到 O(n log n)）不是递减曲线上的一点，是新曲线的起点。所以"收益递减"判据适用于常数因子优化（缓存、调参、局部改写），算法与架构级的重新选择要先评估是否换了曲线——剖析数据里"占比最高的那块"如果用当前思路怎么优化都压不下去，就是该考虑换曲线的信号。

## 验证

验证的锚点：停手时机要与递减曲线对账——预写优化序列的收益应在某轮之后显著收窄，若实测每轮收益基本持平，与预期对不上，说明还没到拐点。

每次优化记录三件事：优化前后的指标对比（同一负载、同一测量方法，误差控制见 [[工程知识/性能工程：从用户等待到资源瓶颈/观测与诊断/基准测试必须先定义负载和测量误差]]）、付出的复杂度（代码行数、新增依赖、评审轮次）、有没有引入回归（全量测试 + 回归监控）。

优化决策复盘：把一个已完成的优化序列摆出来，标注每轮的收益与成本，检验停手点是否与递减曲线的拐点一致。停在拐点前的，说明该做的没做完；停在拐点后的，说明过度了——两种偏差都校准下一轮的预估。

## 要解决的问题

优化做到后面每一步换来的改善按数量级缩小，而代码复杂度与回归风险持续累积，缺少一个明确的停手依据，于是优化本身变成新的工程风险。本篇回答：收益随轮次变化的规律是什么、停手判据怎样在开始之前设定、常数因子优化与算法换代如何区分，以及每一轮优化要留下哪些记录用于校准判断。

## 相关

关系：[[工程知识/性能工程：从用户等待到资源瓶颈/观测与诊断/性能剖析与火焰图]] · [[工程知识/性能工程：从用户等待到资源瓶颈/观测与诊断/基准测试必须先定义负载和测量误差]] · [[工程知识/缺陷分析：从个案到体系/正确性/案例三十四：省下的句柄与多出的删除——一次优化引入的回归]]

- [[工程知识/后端系统：在并发、失败与变化中维持服务/基础设施/容量规划与压测要用同一个负载模型说话]] —— 目标判据的容量侧输入
- [[工程知识/AI 系统工程：从模型能力到生产能力/推理服务与平台/推理服务的单位请求成本从token定价推出来]] —— 成本优化的同构停止条件
- [[工程知识/软件构建：让变化可以理解、验证与交付/架构与代码/技术方案先写清约束、取舍与验证]] —— 优化方案的事前对账
- [[工程知识/性能工程：从用户等待到资源瓶颈/性能工程：从用户等待到资源瓶颈]] —— 域内优化的整体地图
- [[工程知识/性能工程：从用户等待到资源瓶颈/观测与诊断/性能回归门禁：把性能预算写进CI.md]] —— 优化停手点决定门禁预算线的松紧
