---
title: 监控体系按四个问题分层：坏了没、坏了哪、为什么坏、会再坏吗
status: active
type: concept
updated: 2026-09-23
change_rate: low
confidence: high
review_after: 2029-09-23
tags:
  - os
  - observability
  - methodology
sources:
  - "https://sre.google/sre-book/monitoring-distributed-systems/"
  - "https://opentelemetry.io/docs/"
---

# 监控体系按四个问题分层：坏了没、坏了哪、为什么坏、会再坏吗

> **一句话总结**：监控的四个层次各答一个问题——告警答"坏了没"（ symptom 指标 + 阈值）、指标面板答"坏了哪"（组件逐层下钻）、日志与剖析答"为什么坏"（事件与采样证据）、SLO 报告答"会再坏吗"（预算消耗趋势）；四层混在一张面板里，告警噪声与定位盲区同时出现。

## 背景与本质：监控不是一堆面板的堆叠

监控体系要解决的问题是：故障发生时按顺序回答四个问题，且每层的证据不依赖下一层——先确认"坏了"（用户可感知的症状），再定位"坏在哪"（组件边界），再回答"为什么坏"（根因证据），最后回答"会不会再坏"（趋势与预算）。四个问题对应四类工具：告警、指标面板、日志与剖析、SLO 报告——**混层的代价是双输**：把根因证据做成告警（告警量爆炸、值班疲劳），把症状告警做成面板（故障发生时没人盯着面板、发现靠用户投诉）。本库"指标、日志、Trace 与剖析各答一问"讲过证据类型分工；本篇讲监控体系的分层建设：**每层的建设要点、层间的下钻路径、告警设计的判据**。

## 机制：四层的建设要点

**第一层：告警（坏了没）**。告警只挂用户可感知的症状指标：可用性（错误率/成功率）、延迟（P99 超预算）、正确性（数据新鲜度落后、对账不平）——**容量与资源指标（CPU 80%、磁盘 70%）默认不直接告警**，它们是症状的先行指标，挂告警会带来大量"资源高但服务正常"的噪声；资源告警收敛为趋势告警（增长斜率异常）或预算告警（预计 N 小时内打满）。**告警的三要素**：一个动作（响了要做什么，没有动作的告警是日志）、一个证据链接（点开能看到症状曲线与影响范围）、一个恢复判据（怎么算好了）。

**第二层：指标面板（坏了哪）**。按调用链逐层组织：入口 → 服务 → 依赖（数据库、缓存、下游 RPC）→ 资源（CPU/内存/IO）——**每层同样的四指标骨架**（本库 USE/RED 方法）：流量、错误、延迟、饱和度。面板的组织逻辑与调用链一致，定位的路径就是从入口层往下点：入口错误率上升 → 哪个服务 → 该服务的哪个依赖 → 依赖的资源水位。**面板与告警同源**（告警点开的证据链接就是对应面板），两处指标口径不一致是排障时的经典内耗。

**第三层：日志与剖析（为什么坏）**。指标说"这个服务延迟涨了"，根因在这一层找：日志看异常事件与错误签名（本库日志双防御线篇的签名聚合），Trace 看慢在哪一段（跨服务的时间分解），剖析看资源耗在哪（CPU 火焰图、内存分配）——**这一层的特点是按需加载**（平时不扫全量日志，出事时按面板定位的范围查询），成本控制靠采样与保留期。

**第四层：SLO 报告（会再坏吗）**。错误预算的消耗速率与趋势：本季度预算已用 60%、按当前速率季度末耗尽——**回答的是容量与可靠性的规划问题**（要不要降级非核心功能、要不要加机器、变更节奏要不要放缓）。本库"数据新鲜度是一组配额"的时限预算、容量域的"留白"原则在这一层汇总成数字。

## 看完能判断：层间下钻与告警判据

**下钻路径的设计**：告警 → 面板 → 日志/Trace → SLO 归因，每一跳要有链接（告警里的面板链接、面板里的日志查询模板）——**下钻是手动跳转不是自动关联**（自动根因分析的成本高、误报率高，手动跳转 + 组织良好的面板是性价比解），跳转路径的顺畅度决定排障速度。

**告警的四个判据**：症状优先（用户可感知的才告警）、有动作（响了要做什么）、带证据（点开能看到）、可恢复（怎么算好）——**四条判据过滤后的告警量通常降一个数量级**，剩下的每条都值得人看。

**两个反模式**：面板堆叠症（几十张面板没有组织逻辑，故障时全靠猜）；告警疲劳（大量资源类告警把值班训练成"无视告警"，真症状被淹没）——**两个反模式是同一个根源**：没按四层分工，把该做面板的做成了告警、该做告警的做成了面板。

## 边界

三条边界防止防线走形。

**四层是建设分工，故障时顺序可变**。有经验的值班从日志/Trace 反推（看到错误签名直接定位），不必机械走四层——**分层的价值在建设期**（每类工具的建设目标清晰），使用期按熟练度自由组合。

**资源告警不直接挂，但资源面板常备**。容量问题（磁盘将满、连接数逼近上限）的发现路径是趋势面板 + 预算告警，与症状告警分开管理——**两套告警的分界是"用户可感知"**，不是重要性。

**监控自身的可用性单独保障**。监控组件与业务组件同机部署时，业务故障可能带走监控（最需要的时刻没有证据）——监控自身的资源配额、存储保留期、拨测兜底（本库"拨测与合成监控"的外部视角）要单独配置。

## 验证

可复现的验证路径：

1. 分层审计：对当前监控体系逐条核对四层分工，预期每条告警能答"坏了什么 + 该做什么"、每张面板能答"坏了哪"、日志/Trace 覆盖"为什么坏"、SLO 报告回答"会再坏吗"——不达标的条目即改造清单；
2. 下钻演练：注入一次延迟故障，预期值班沿告警 → 面板 → Trace 的路径在分钟级定位到组件，跳转链接全程无断点；
3. 噪声验证：统计一周告警分布，预期症状告警占比高于资源告警、每条告警的处置动作可回溯；
4. 断言锚点：**告警四判据（症状/动作/证据/恢复）在告警规范里、面板与调用链同构、监控自身有独立保障与拨测**——三件齐备，监控从"面板堆"变成四问分层的证据体系。

```text
分层断言 = 四问各有对应的工具层
告警断言 = 每条告警有动作与证据
下钻断言 = 告警到日志的跳转无断点
```

监控体系的建设顺序按四层展开：先定告警的判据（症状优先）、再按调用链组织面板、再按需加载日志与剖析、最后用 SLO 把可靠性变成可规划的预算——四层各答一问，故障响应才从"翻面板碰运气"变成"沿证据链走路"。

## 相关

- [[工程知识/性能工程：从用户等待到资源瓶颈/观测与诊断/指标、日志、Trace与剖析各答一问，合起来才构成证据]] —— 证据类型的分工原理，本篇是监控体系的分层展开
- [[工程知识/性能工程：从用户等待到资源瓶颈/观测与诊断/日志是诊断的证物也是故障源：磁盘打满与错误风暴两条防御线.md]] —— 第三层日志的防线设计
- [[工程知识/性能工程：从用户等待到资源瓶颈/观测与诊断/拨测与合成监控：在用户之前从外面敲一遍门.md]] —— 外部视角的兜底观测
- [[工程知识/数据系统：在并发与故障中保存事实/可靠性与运维/数据新鲜度是一组配额：每个消费链路声明自己的时限]] —— 第四层预算化思路的同类机制
