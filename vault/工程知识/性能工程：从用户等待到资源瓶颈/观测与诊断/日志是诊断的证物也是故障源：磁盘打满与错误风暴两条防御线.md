---
title: 日志是诊断的证物也是故障源：磁盘打满与错误风暴两条防御线
status: active
type: pattern
updated: 2026-09-23
change_rate: low
confidence: high
review_after: 2029-09-23
tags:
  - operations
  - observability
  - defect-analysis
sources:
  - "https://www.freedesktop.org/software/systemd/man/latest/journald.conf.html"
  - "https://logback.qos.ch/manual/appenders.html"
---

# 日志是诊断的证物也是故障源：磁盘打满与错误风暴两条防御线

> **一句话总结**：日志系统的自洽要求是"诊断工具自己不闯祸"——磁盘防御线给日志配额与轮转（打满前先降级），风暴防御线给重复错误做聚合限流（同样的错误一秒打一次），两条线都失效时日志从证物变成事故的放大器。

## 要解决的问题

日志有两次身份反转：平时是诊断证物，出事时自己变成事故源——**磁盘被日志打满**（错误风暴时每秒百万行的重复错误把磁盘写满，服务从"有 bug"变成"完全不可用"），**日志系统本身崩溃**（日志框架在磁盘满时的行为未定义，有的抛异常把业务线程带崩）。上游 BookKeeper 的一个复现改进是垃圾回收线程记录所有错误（把静默失败变成可见信号）——**可见性是对的，但没有配额的可见性会反噬**：错误风暴时全量记录恰好把服务压垮。本篇讲两条防御线：**配额与轮转的磁盘防御、聚合限流的风暴防御**，让日志"该在的时候在、不该多的时候不多"。

## 机制：磁盘防御线——配额、轮转与降级

**配额先于写入**：日志目录的磁盘配额单独划（logrotate 的 maxsize、systemd-journald 的 SystemMaxUse、应用框架的 maxSize）——**配额按磁盘的一定比例（一般 10-20%）与保留时长双重约束**，两个条件任一触发即轮转。配额缺失时轮转形同虚设：单文件轮了，总量无人管，打满只是时间问题。

**轮转的三要素**：按大小或时间切分、保留 N 份、过期删除——三要素缺一，要么单文件无限膨胀，要么旧日志堆积。**轮转与写入的原子性**：轮转瞬间正在写的文件被改名，进程的文件句柄还指向旧 inode（磁盘空间不释放）——**copytruncate 或信号重开（USR1/SIGUSR2）是标准解**，用错方式是"轮转后磁盘依然满"的经典原因。

**配额触顶时的降级行为要预设**：磁盘到配额上限时日志框架怎么办——丢弃新日志（保服务）、覆盖最旧（保新鲜）、阻塞业务线程等空间（事故放大器）——**三选一必须显式配置**，默认行为常常是第三种（最坏选择）。日志丢了可以接受，业务线程被日志阻塞不可接受，这个优先级要写进配置。

## 机制：风暴防御线——聚合与限流

**重复错误的聚合**：同一错误一秒打千次时，第 2 到第 1000 次是噪声——**聚合按错误签名（异常类型 + 位置 + 关键参数）分组，组内限流**（如"同一签名每秒最多 1 条 + 计数器"，窗口结束输出"过去 N 秒发生 M 次"）。主流日志框架都有此机制（logback 的 DuplicateMessageFilter、各语言的 rate-limited logger），**没配就等于没有**。

**聚合限流的自反性检查**：被限流的错误在聚合计数里可见——**"限流器生效"本身要留痕**，否则限流把真问题藏住（每秒 1 条的采样让人误以为问题轻微）；限流的收益是日志体量可控、磁盘防御线不承压，代价是日志细节被采样稀释——两者靠计数器补偿：计数与告警联动，签名出现频率异常（从未见过的新错误签名）立即告警，不限流告警。

**错误与堆栈的成本核算**：堆栈打印是日志行膨胀的主源（一个异常几十行）——风暴场景下堆栈按签名只打首次，后续计数；**根因堆栈与业务堆栈分离**（根因全打、业务包装层一行带过），日志的信噪比与体量同时改善。

## 看完能判断：两级配置的检查单

**磁盘防御线四查**：日志目录有独立配额吗？轮转三要素（切分、保留、过期）齐全吗？轮转方式与进程的重开机制匹配吗（copytruncate 或信号）？配额触顶的降级行为显式配置了吗（丢弃/覆盖，绝不阻塞）？

**风暴防御线四查**：重复错误有签名聚合吗？聚合窗口的采样留痕吗（计数可见）？新错误签名有即时告警吗？堆栈有根因/业务的分层策略吗？

**两线联动的终验**：错误风暴演练——注入高频错误，预期日志配额不触顶（限流先起作用）、配额触顶时服务不受阻（降级生效）、聚合计数准确反映真实频次——**演练通过，日志才算从"潜在事故源"转正为"可靠证物"**。

## 边界

三条边界防止防线走形。

**日志配额不替代磁盘监控**：配额管日志自己的体量，磁盘的其他消费者（数据、临时文件）监控照旧——**磁盘水位监控与日志配额是两道独立的门**，任何一道单独存在都拦不住所有场景。

**聚合限流不做在错误的层**：按日志行限流（框架层）与按错误签名限流（应用层）效果不同——行级限流可能把不同错误一起掐掉，签名级限流保住多样性——**签名级是首选**，行级只作兜底。

**静默失败与风暴限流的分界**：GC 线程"错误全记录"的原则针对的是平时被吞的失败（可见性优先）；风暴限流针对的是已知重复的噪声（体量优先）——**两者的适用场景不同**，平时全记、风暴限流，一个配置两种策略。

## 验证

可复现的验证路径：

1. 配额验证：注入高频错误直到配额触顶，预期降级行为按配置执行（丢弃新日志）、业务线程无阻塞、服务指标无抖动；
2. 轮转验证：触发轮转，预期磁盘空间释放（句柄重开生效）、无"删除后空间不释放"；
3. 聚合验证：同一错误每秒 500 次，预期日志行速率被限到配置值、窗口计数准确（日志显示"过去 60 秒 30000 次"）；
4. 断言锚点：**日志配额与轮转配置在代码库可查、降级行为有显式配置、签名聚合与计数告警在管、风暴演练做过**——四件齐备，日志从双向风险变成单向证物。

```text
配额断言 = 触顶降级不阻塞业务
轮转断言 = 句柄重开空间即释放
聚合断言 = 限流生效且计数可见
```

日志系统的治理目标是自洽：它用来诊断别人，自己先要经得起错误风暴与磁盘压力的考验。两条防御线配齐，"出事时日志最先坏"的悖论才会消失。

## 相关

- [[工程知识/性能工程：从用户等待到资源瓶颈/观测与诊断/指标、日志、Trace与剖析各答一问，合起来才构成证据]] —— 日志在证据体系中的位置
- [[工程知识/性能工程：从用户等待到资源瓶颈/操作系统与运行时/脏页回写在性能与崩溃丢失窗口间权衡.md]] —— 日志写入的内核路径
- [[工程知识/缺陷分析：从个案到体系/硬件故障/静默数据损坏靠校验与副本投票检出：硬件说成功不等于数据还对.md]] —— 可见性原则的缺陷分析视角
- [[工程知识/后端系统：在并发、失败与变化中维持服务/基础设施/socket读写缓冲区：拥塞窗口与应用层背压的连接点.md]] —— 写入路径背压的同类机制
