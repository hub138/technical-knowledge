---
title: 排队论给容量一个可推导的模型
type: concept
status: active
updated: 2026-09-21
review_after: 2027-03-21
change_rate: medium
confidence: high
tags:
  - performance/queueing-theory
  - capacity
  - latency
sources:
  - "https://en.wikipedia.org/wiki/Little%27s_law"
  - "https://web2.ucm.es/info/bdec/profesor/jd_lopez/pdfs/queueing.pdf"
  - "https://www.brendangregg.com/usemethod.html"
---

# 排队论给容量一个可推导的模型

容量问题（"能扛多少""加机器能改善多少""延迟为什么会突然涨"）没有排队模型时只能靠压测试错——试一次贵一次，且试出来的数字不可迁移（换负载形态就失效）。排队论把这类问题变成可推导的：到达率、服务率、并行度三个参数进模型，延迟与队列长度的期望值出来。模型是简化（真实系统不是 M/M/1），但**简化模型的价值在于方向性结论不需要精确**。

两个最常用的结论先摆出来：

**Little 定律**：`L = λ × W`（系统内平均请求数 = 到达率 × 平均停留时间）。它恒成立（不依赖分布假设），用途极广：反推容量（实测 L 与 W，算出 λ 上限）、估算队列长度（λ 乘 W 的积就是同时要服务的量）、验证观测一致性（三个量测出来不满足公式，说明有一个测错了——这本身就是观测的 sanity check）。

**利用率-延迟曲线**：M/M/1 模型里 `W = 1/(μ-λ)`（平均停留时间 = 1/(服务率-到达率)），利用率 ρ = λ/μ 趋近 1 时延迟发散——90% 利用率的排队延迟是 50% 利用率的 9 倍。**容量规划留余量的数学根据就是这条曲线的形状**：线性扩容的思维（90% 用满 = 还有 10% 余量）在这条曲线面前是错的，10% 的余量对应的延迟恶化是数倍的。

## 三个模型覆盖大多数场景

| 模型 | 场景 | 关键结论 |
| --- | --- | --- |
| M/M/1 单队单服务台 | 单实例、串行瓶颈 | 延迟在 ρ→1 发散 |
| M/M/c 多服务台 | 实例池、连接池 | 并行度提高延迟发散点右移 |
| 排队网络（串联） | 多级流水线 | 最慢一级决定吞吐上限 |

M/M/c 的实践含义：连接池从 10 扩到 20 不是简单的吞吐翻倍——池小于到达所需并发时排队在池门口（连接等待），池大于服务端处理能力时排队挪到服务端内部（线程切换开销上升）。**最优池大小存在一个点，两侧性能都变差**，这个点用排队模型能逼近，用压测能校准（压测方法见 [[工程知识/后端系统：在并发、失败与变化中维持服务/基础设施/容量规划与压测要用同一个负载模型说话]]）。

串联排队网络的含义：流水线吞吐上限 = 最慢一级的服务率，加快其他级无效。这与"端到端延迟来自多层排队"的分解一致（见 [[工程知识/性能工程：从用户等待到资源瓶颈/存储与网络/端到端网络延迟来自排队、协议、传输与处理]]），瓶颈判定用 USE 方法（见 [[工程知识/性能工程：从用户等待到资源瓶颈/观测与诊断/系统指标与观测]]）。

## 用模型回答三个容量问题

**问题一：加机器还是优化代码？** 用 Little 定律算出当前单机有效并发 L = λ×W，对比服务端真实并行度。差距大说明排队在等资源（加机器有效）；差距小且延迟仍高说明单请求本身慢（优化服务时间才有效）。这个判断把"加机器"从领导直觉变成有推导的选择。

**问题二：突发流量会怎样？** 突发是到达率瞬时抬升，模型上队列长度按指数积累（ρ 短暂超过 1 时队列线性增长，增速 = λ-μ）。持续时间 T 的突发需要缓冲容量 (λ-μ)×T——这就是"削峰填谷要多少队列容量"的算法。真实系统的突发形态（整点脉冲、缓存失效风暴）偏离指数假设，量级估算仍适用，精确值靠回放压测。

**问题三：为什么延迟突然涨了三倍？** 排队模型的第一嫌疑是利用率爬过拐点——流量涨 15% 但利用率从 80% 到 92%，延迟就是三倍（曲线形状决定）。第二嫌疑是服务时间分布变长尾（某类请求变慢拖住服务台），表现是平均利用率不高但 P99 恶化——这是 M/M/1 模型外的情况，用剖析找长尾来源（见 [[工程知识/性能工程：从用户等待到资源瓶颈/观测与诊断/性能剖析与火焰图]]）。

## 边界

模型假设的三个偏离要心里有数：到达不是泊松（真实流量有自相关与脉冲）、服务时间不是指数（多峰分布）、系统有反馈（过载时降级与限流改变到达形态）。偏离的方向性影响：脉冲式到达的队列比模型预测更长（预测值是下界），反馈控制让实际系统比模型更韧（预测值偏悲观）。**模型给的是数量级与方向，决策要压测校准**——这一主从关系在容量规划篇已定，这里给的是它的数学面。

模型也不能替代对负载语义的理解：同一个 QPS 数字，读多写少与写多读少的容量结论完全不同（写路径的锁与刷盘改变 μ），负载构成的分析见 [[工程知识/后端系统：在并发、失败与变化中维持服务/基础设施/容量规划与压测要用同一个负载模型说话]] 的负载模型四要素。

## 验证

Little 定律自检：从线上取同时在线请求数（连接数或 inflight 计数）、吞吐、平均延迟三个实测值，验证 L ≈ λ×W。偏差大说明观测有错（inflight 没数对、延迟测量点不一致）——这个自检本身是观测体系健康度的检查项（观测证据的分工见 [[工程知识/性能工程：从用户等待到资源瓶颈/观测与诊断/指标、日志、Trace与剖析各答一问，合起来才构成证据]]）。

拐点实测：阶梯压测画利用率-延迟曲线，与 M/M/1 形状对比。实测拐点位置早于模型（60% 就陡升）通常意味着锁竞争或 IO 等待把有效并行度压低了——模型与实测的差值就是"锁与 IO 吃掉的并行度"的量化。

## 相关

关系：[[工程知识/后端系统：在并发、失败与变化中维持服务/基础设施/容量规划与压测要用同一个负载模型说话]] · [[工程知识/性能工程：从用户等待到资源瓶颈/存储与网络/端到端网络延迟来自排队、协议、传输与处理]] · [[工程知识/性能工程：从用户等待到资源瓶颈/观测与诊断/系统指标与观测]]

- [[工程知识/性能工程：从用户等待到资源瓶颈/观测与诊断/性能剖析与火焰图]] —— 长尾来源的下钻工具
- [[工程知识/性能工程：从用户等待到资源瓶颈/观测与诊断/优化收益递减决定何时该停手]] —— 容量投入的停止判据
- [[工程知识/后端系统：在并发、失败与变化中维持服务/任务与并发/调度必须同时处理优先级、公平与背压]] —— 队列管理在调度层的展开
- [[工程知识/缺陷分析：从个案到体系/资源与容量/案例六：容量与泄漏——连接泄漏和 SST 堆积]] —— 容量失效的实例形态

- [[工程知识/后端系统：在并发、失败与变化中维持服务/服务设计/容量估算的通用骨架：从峰值QPS到机器数的推导演练.md]] —— 把排队参数变成机器数的完整推导演练
- [[工程知识/AI 系统工程：从模型能力到生产能力/推理服务与平台/推理批处理的两难：吞吐与延迟的调度天平.md]] —— 调度天平的通用理论
- [[工程知识/后端系统：在并发、失败与变化中维持服务/服务设计/连接池的容量数学：池大小与排队等待的权衡.md]] —— 池数学的理论基础
