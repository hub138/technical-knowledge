---
title: 网络故障注入先打点对账：丢包重排限速要验证到应用的行为
status: active
type: playbook
updated: 2026-09-23
change_rate: low
confidence: high
review_after: 2029-09-23
tags:
  - net
  - observability
  - testing
sources:
  - "https://wiki.linuxfoundation.org/networking/netem"
  - "https://github.com/chaosblade-io/chaosblade"
---

# 网络故障注入先打点对账：注入值与应用感知值对不上，注入就是无效实验

> **要点**：网络故障注入（丢包、延迟、重排、限速）的有效性取决于两端对账——注入端确认规则真的命中目标流量（tc filter 的统计计数在涨），应用端确认感知到的劣化与注入值一致（RTT 抬升幅度、重传率）；两端对不上（规则没命中、方向反了、被 offload 绕过）时，实验的"通过"是假通过。打点对账是注入实验的第一步，行为验证是第二步。

## 要解决的问题：注入没生效的实验是空转

故障注入实验的隐性失败模式：规则加了、流量没走到（tc 规则加在 eth0，服务流量走的是 veth 或bond 的另一条路径）；方向加反了（想注入下行丢包，规则只命中上行）；网卡 offload（GRO/TSO）把包合并后 netem 的 per-packet 逻辑失真；限速值远高于应用流量实际速率，注入等于没注。**这四类失效的共同点**：实验流程走完了、报告写了"系统表现良好"——表现良好的真实原因是系统根本没经历故障。本库"混沌工程验证系统是否真的能处理故障"篇讲过注入验证的通用纪律；本篇聚焦网络注入特有的**生效性对账**——先证明注入真的发生，再谈系统表现。

## 机制：两端对账与四类失效

**注入端对账：规则命中率的直接证据**。tc/netem 的规则配统计计数：`tc -s qdisc show dev eth0` 看 sent/ dropped 计数，`tc -s filter show` 看每条 filter 的命中包数——**注入期间计数在涨**是规则生效的第一证据；丢包注入时 dropped 计数的增量与注入速率一致（注入 5% 丢包，dropped/total ≈ 5%）。**iptables 注入**（REJECT/DROP 类）同理用 `iptables -L -v -n` 的 pkts 计数。**计数不涨的三种原因**：规则绑错接口（服务流量在 veth/bond 上）、优先级被更前面的规则截胡（filter 顺序）、流量走了其他路径（本机回环、IPVS 的另一侧）——计数是排查这些的第一现场。

**应用端对账：感知值与注入值的一致性**。注入 100ms 延迟后，应用的下游调用 RTT 应从基线抬升约 100ms（打点系统的 P99 曲线直接可见）；注入 5% 丢包后，TCP 连接的重传率应出现对应抬升（`ss -ti` 的 retrans 字段或 eBPF/导出的 TCP 指标）。**两端一致**说明实验真实发生且被系统经历；**应用端无变化**回注入端查（规则没命中）；**注入端在丢包但应用无感知**查 offload（GRO 合包后 per-packet 丢包率被稀释，关 offload 或按流注入重试）。

**四类失效的判定表**：
- 规则没命中 → 注入端计数不涨，查接口与 filter 顺序；
- 方向反了 → 单向注入时应用端无感，查 netem 挂在 ingress 还是 egress（ingress 要用 ifb 虚拟设备转发）；
- offload 稀释 → 注入端丢包计数正常但应用重传率低，查 `ethtool -k` 的 GRO/GSO 状态；
- 限速虚设 → 注入 100Mbps 但应用峰值只有 10Mbps，行为无变化——**限速档位要从应用实际流量倒推**，注入值低于业务峰值才有观察意义。

**行为验证的三层观察**：注入生效确认后，观察三层——传输层（重传率、RTT、零窗事件）、服务层（调用错误率、超时率、熔断器状态）、业务层（用户请求成功率、降级行为触发）。**注入实验的结论只从三层一致的行为里得出**："TCP 重传了但调用没报错"是传输层扛住了（要看吞吐代价），"熔断器打开且业务走了降级"是设计预期达成，"三层全部静默"要怀疑观察面缺打点，别急着当系统无恙。

## 看完能判断：注入实验的报告底线

**一份可信的注入报告至少含四项**：注入参数（哪条链路、什么规则、什么档位）、生效证据（注入端计数与应用端感知曲线）、观察窗口内三层行为的时间线、撤销后基线恢复确认。**缺生效证据的报告**是本篇的第一否决项——"我们注入了 5% 丢包，系统没问题"这句话在计数核对之前不成立。**实验的粒度**：单服务验证（netem 挂本机出口）、链路验证（两端之间注入，ifb 处理 ingress）、全链路验证（网关层整形）——粒度越靠后环境越真，定位越难，**从单服务开始逐级放大**，每级都有对账再进下一级。

**打点的前置性**：注入实验前先确认 RTT、重传率、错误率的打点已存在且粒度够（秒级采样）——注入是瞬态实验，分钟级采样会把 2 分钟的注入窗口糊掉，**没有秒级打点就先补打点再做注入**。

## 边界

三条边界防止防线走形。

**注入的爆炸半径要圈住**：filter 按 IP/端口圈定目标流量（只注测试服务的段），全接口全端口的注入会波及同机其他服务与监控本身——监控通道被注入打断时，实验既危险又不可观察，**监控流量加 filter 豁免**是注入环境的第一条规则。

**netem 的排队延迟有上限**：深队列大延迟会先撑爆 qdisc 的 buffer（`limit` 参数），丢包形态变成缓冲溢出丢而非注入丢——延迟与限速组合注入时核对 limit 与速率的匹配（buffer 容纳 delay × rate 的积）。

**生产注入需要独立的审批与回滚通道**：产线环境的注入（混沌演练的生产阶段）按爆炸半径分层审批，回滚命令与注入命令同置（一键撤销），演练窗口避开业务高峰——本库"退役演练进例行"的同款纪律：演练的价值在于反复做，前提是每次都可控可撤。

## 验证

可复现的验证路径：

1. 生效对账复现：本机 netem 注入 50ms 延迟 + 5% 丢包，`tc -s` 计数与应用 RTT/重传率两端对账，预期两端一致；
2. 失效模式复现：把规则加到无流量的接口上重跑实验，预期注入端计数不涨、应用无感——亲手见过失效模式，报告里的生效证据才知道怎么读；
3. offload 稀释复现：开 GRO 注入 per-packet 丢包，对比关 GRO 后的应用重传率，预期差异明显；
4. 断言锚点：**注入实验报告必含生效证据、监控流量有豁免、撤销与注入同置**——三件齐备，网络注入从"仪式性故障演练"变成有对账的可信实验。

```text
生效断言 = 注入端计数与应用感知对上
半径断言 = 目标圈定监控豁免
可撤断言 = 一键撤销与基线恢复
```

网络故障注入的可信度建立在打点对账上：注入端计数证明规则命中，应用端感知证明系统经历，三层行为观察给出结论——跳过对账的注入实验，报告里所有的"表现良好"都只是"没测到"的礼貌说法。

## 相关

- [[工程知识/软件构建：让变化可以理解、验证与交付/测试与交付/混沌工程验证系统是否真的能处理故障.md]] —— 注入验证的通用纪律
- [[工程知识/性能工程：从用户等待到资源瓶颈/存储与网络/弱网测试要把网络当变量：延迟、抖动、丢包三轴注入到超时边界.md]] —— 三轴注入的参数层
- [[工程知识/性能工程：从用户等待到资源瓶颈/存储与网络/网卡丢包先看环形缓冲区溢出：队列、中断与软中断的接力.md]] —— 丢包的产线侧判定
- [[工程知识/软件构建：让变化可以理解、验证与交付/开发工具/Linux调查先收集事实再改变状态.md]] —— 先看计数再下结论
