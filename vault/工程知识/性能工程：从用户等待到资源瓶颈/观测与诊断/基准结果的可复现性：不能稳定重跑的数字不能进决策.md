---
title: 基准结果的可复现性：不能稳定重跑的数字不能进决策
status: active
type: pattern
updated: 2026-09-23
change_rate: low
confidence: high
review_after: 2029-09-23
tags:
  - performance/benchmark
  - hardware
  - systems/performance
sources:
  - "https://dl.acm.org/doi/10.1145/3579370"
  - "https://dl.acm.org/doi/10.1145/2854038"
  - "https://www.spec.org/docs/cweb.html"
---

# 基准结果的可复现性：不能稳定重跑的数字不能进决策

> **一句话总结**：硬件层的一切性能数字都带着环境指纹（频率、负载、状态、随机性），跑一次拿到的数字没有证据资格——能以可声明的方式重跑并给出差异范围的数字才能进决策。

## 要解决的问题

硬件层的性能数字有一个与软件层不同的特点：**它把环境状态和随机性一起包进了结果**。软件层的性能问题（一个函数的耗时）尚且受环境干扰，硬件层的数字直接由频率、温度、磁盘磨损、页缓存状态共同决定。市面 Benchmark 的设计读法一文解决了"基准测什么才可靠"的问题；本篇解决另一半："这个数字本身可信吗，重跑还会是这个数吗"。

不稳定数字的真实成本，全部发生在决策链上：采购选型按厂商发布的一组跑分做决定，跑分是理想环境下的最好成绩，到货后实测打了七折——**数字在生产环境不可复现，选型就从证据变成了赌博**；超分超卖按实验室峰值定容量，峰值是一次性事件，线上持续负载下性能回落，SLA 失守。这些事故的共同结构：**单次测量 + 未声明环境 + 无重跑记录，被当成了稳定事实**。

## 机制：数字为什么不能稳定重跑

一次测量结果 = 被测对象的行为 × 环境状态 × 随机性。硬件层三个乘数都不稳定：

**频率与热状态的波动**：CPU 从基准频率到睿频差可达一倍，睿频持续多久由功耗与温度预算决定。基准测试刚开机时跑在睿频上，十分钟后降回基准——**同一个测试，时间窗不同，结果不同**。"峰值性能"与"持续性能"是两个数，测到哪一个取决于测试时长。

**邻居与后台的干扰**：共享环境下（云主机、同宿主机其他租户），邻居的负载直接影响你的缓存与带宽占用；本机后台任务（日志回写、监控采集、定时任务）在测量窗口里插入干扰。裸机独占能消除邻居，消除不了系统自身的后台活动。

**磁盘与文件系统的状态**：写性能测试跑在全新空盘上，与跑在用了三个月、SSD 缓存已磨损、文件系统碎片累积的盘上，数字可以差数倍。页缓存状态同理——第一次读冷数据走磁盘，第二次读同一数据走缓存，**"冷跑"与"热跑"是两个不同的测量**。

**编译与参数的隐式差异**：重跑时的编译选项、库版本、内核参数与上一次不同，数字自然不同——这类差异可声明、可控制，与前几类"不可控但可声明"的环境因素共同构成重跑协议的内容。

这些波动的处理方式由数字的用途决定：决策需要的是**可复现声明下的结果区间**，单点数字只在"恰好处于稳定状态"时偶然成立。

## 修复与防线

**测量前置状态声明**：每次基准记录环境快照——CPU 型号与 governor、内存配置、磁盘型号与已用量、内核版本、后台负载、测量时长与次数。声明的意义在纵向对比：两份数字的可比性由环境差异决定，环境未声明的对比没有意义。

**多轮跑取分布**：单次测量升级为 N 轮（预热轮丢弃、正式轮取分布），报告中位数与分位数，不报告最好成绩——分布的宽度本身就是结果稳定性的度量，宽分布说明环境干扰大，此时要回头控环境而不是引数字。

**峰值与持续分开测**：短时长测峰值（睿频窗口内）、长时长测持续（热稳定后），两个数都报告且用途分开——容量决策用持续值，瞬时突发评估用峰值。**用哪个数的决定权留给消费方，但两个数都要给**。

**关键决策双环境验证**：采购、架构选型这类高成本决策，实验室数字之外在类生产环境复测——同一套脚本、同样的负载模型，数字偏差超过预期就要查环境差异，查不出来就按保守值决策。

**数字与脚本一起入库**：基准脚本版本化，结果记录附带脚本版本与参数——三个月后的复测能对上"当时怎么测的"，数字才具备纵向可比性。

## 边界

三条边界防止防线走形。

**可复现不等于数字固定**：目标是"差异可解释、范围可声明"，重跑结果在声明的范围内波动即算复现——追求每次完全相同的数字会把测量引向过度简化的环境，失去代表性。

**基准环境与生产环境的取舍**：完全模拟生产的基准环境成本高且仍非生产；规范做法是双轨——可控环境跑可比数字（版本间对比），生产环境跑真实流量画像（容量校准），两份数字回答不同问题。

**随机性控制适度即可**：固定随机种子、固定数据规模消除可消除的随机性；硬件层的物理随机性（热噪声级别的干扰）消除不了，靠分布统计吸收——区分"可控制的随机"与"只能统计的随机"。

## 验证

可复现的验证路径：

1. 建立基线：同一环境跑基准 N 轮，记录环境快照与结果分布；
2. 稳定性验证：隔日重跑，结果分布与基线重叠、差异在声明范围内——数字获得"可复现"资格；
3. 敏感性验证：改变一个声明过的环境变量（governor 从 performance 切到 powersave），观察数字偏移幅度，确认环境敏感度与预期一致；
4. 断言锚点：**任何进决策的基准数字都有环境快照、N 轮分布与重跑记录**三者齐备；缺任何一项的数字不进评审材料。

```text
基线 = 环境快照 + N 轮分布（中位数 + P90）
复现断言 = 隔日重跑分布 ⊆ 基线声明范围
决策断言 = 进评审的数字三件套齐备
```

"数字 + 环境 + 分布"三件套是基准证据的最低完整形态——市面 Benchmark 的设计读法保证测得对，本篇保证测得稳，两者合起来数字才有资格进决策。

## 相关

- [[工程知识/性能工程：从用户等待到资源瓶颈/观测与诊断/市面Benchmark的设计读法：宏微与中间层基准]] —— 前置篇：基准测什么才可靠，本篇解决结果可复现性
- [[工程知识/性能工程：从用户等待到资源瓶颈/处理器与内存/大页与TLB：地址翻译的隐藏成本]] —— 微架构层面的测量对象，其效果同样依赖环境状态
- [[工程知识/数据系统：在并发与故障中保存事实/可靠性与运维/数据库基准必须描述负载与资源边界]] —— 数据库基准对环境描述的姊妹纪律
- [[工程知识/性能工程：从用户等待到资源瓶颈/观测与诊断/性能回归门禁：把性能预算写进CI]] —— 把可复现测量工程化进持续集成
- [[工程知识/性能工程：从用户等待到资源瓶颈/观测与诊断/基准报告要绑定机器画像：没有环境描述的数字不可比较.md]] —— 可复现的第一步是环境可描述：画像采集与比较条件
