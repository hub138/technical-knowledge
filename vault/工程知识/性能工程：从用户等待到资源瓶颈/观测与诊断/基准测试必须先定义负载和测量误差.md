---
title: 基准测试必须先定义负载和测量误差
type: playbook
status: active
updated: 2026-09-03
review_after: 2027-03-03
change_rate: medium
confidence: high
tags:
  - performance/benchmark
  - capacity
  - methodology
sources:
  - "https://github.com/GoogleBenchmark/gbench"
  - "https://www.spec.org/"
  - "https://www.brendangregg.com/methodology.html"
---

# 基准测试必须先定义负载和测量误差

基准是一个实验，不是一个数字。请求/数据分布、并发、到达模式、缓存、预热、硬件、版本、采样、运行时抖动和结果统计都会改变结论；没有这些上下文的“快 30%”不可复用。

基准的产物应是“在何种负载和误差范围内，哪个变量造成了什么变化”，而不是一个脱离环境的排行榜。对 AI 服务还要把质量、token 分布、上下文命中和单位成本纳入同一实验。

## 实验模板

```text
问题与目标 -> 代表性负载 -> 环境与版本 -> 预热/稳态
          -> 基线 -> 单变量改变 -> 重复与置信区间
          -> p50/p95/p99、吞吐、错误、资源、成本
          -> 解释、回归与上线阈值
```

吞吐测试要说明是否排队、批处理或丢弃超时请求；延迟测试要说明客户端/服务端边界、冷启动和长尾。平均值不能替代分位数，单次运行不能证明稳定收益。

## 常见偏差

- 合成数据过于均匀，隐藏热点、长尾和真实大小。
- 预热不一致或缓存状态不同，测到初始化而非稳态。
- 优化一个微基准，却改变了端到端排队、网络、质量或成本。
- 只看 CPU/GPU 利用率，不看用户 SLO 和错误。

## 可运行示例：一份误差可见的基准报告模板

同一个"QPS 12000"的结论，误差披露前后是两份不同的报告：

```text
负载定义（先于一切数字）：
  请求构成：80% 读（点查 95% + 范围查 5%）、20% 写（单行 upsert）
  键分布：Zipf 0.9（热 key 集中度对齐生产抓包）
  数据量：1 亿行 × 200B，热数据占比 40%
  并发：200 连接，预热 10 分钟（缓存形态达到稳态）

测量结果（带误差与分位）：
  吞吐：11,950 ± 320 ops/s（三轮中位数，轮间差 2.7%，判据 <5%）
  延迟：P50 2.1ms / P95 8.4ms / P99 22ms
        （P99 的置信区间 ±3ms——只有 1% 样本支撑，波动天然大）
  资源：CPU 78%、磁盘读 210MB/s、网络 84MB/s

结论（只在边界内成立）：
  "该配置下 80/20 Zipf 负载、1 亿行规模，吞吐约 1.2 万 ops/s，
   瓶颈在 CPU；键分布改均匀分布或热数据占比过半，数字要重测。"
```

跑一遍的收获：误差披露把"测出来 12000"升级成"测出来 12000±320 且三轮稳定"——没有误差的数字在下一轮复测就对不上，对不上就没人再信任何数字。P99 的置信区间天然比 P50 宽一个量级（样本占比差 50 倍），这是"只报 P99 不报样本量"的报告不可信的数学原因。

## 量化锚点与案例回流

稳定性的判据账：轮间差 <5% 是基准可用的最低门槛（超过说明预热不足或有后台噪声——compaction、checkpoint、日志压缩都是常见源），测三轮取中位数是纪律底线。预热的账：缓存冷热差 2-5 倍（内存库冷启动甚至差一个数量级），跳过预热等于把"冷态性能"当"稳态性能"报告。案例回流：数据库域的同款纪律见 [[工程知识/数据系统：在并发与故障中保存事实/可靠性与运维/数据库基准必须描述负载与资源边界.md]]（负载与资源边界三问），两篇是同一方法在两层存储的各自展开；负载定义缺失的实录后果见 [[工程知识/缺陷分析：从个案到体系/资源与容量/案例六：容量与泄漏——连接泄漏和 SST 堆积.md]]——容量假设建立在无边界基准上，量产后假设失效。

## 验证

验证的锚点：基准结论要与离散程度对账——预写多轮重复的离散度应在可接受范围内，单轮结果与预期对不上时先扩大样本，采信最快的一次没有意义。

至少重复多轮并报告中位数、分位数和离散程度；用真实回放与合成负载交叉验证；固定随机种子/版本/拓扑；在目标硬件和压力区间测量饱和点。AI 推理还要固定输入/输出 token 分布、模型质量和 KV cache 状态。

关系：[[工程知识/性能工程：从用户等待到资源瓶颈/性能工程：从用户等待到资源瓶颈]] · [[工程知识/性能工程：从用户等待到资源瓶颈/观测与诊断/排队论给容量一个可推导的模型]] · [[工程知识/数据系统：在并发与故障中保存事实/可靠性与运维/数据库基准必须描述负载与资源边界]]
- [[工程知识/性能工程：从用户等待到资源瓶颈/观测与诊断/市面Benchmark的设计读法：宏微与中间层基准.md]] —— 自做基准讲纪律，读别人的基准讲读法，同方法论两方向
- [[工程知识/性能工程：从用户等待到资源瓶颈/观测与诊断/性能回归门禁：把性能预算写进CI.md]] —— 门禁的基准复用负载与误差纪律

## 机制与本质

基准是一次受控实验，结论由负载定义、测量方法与误差范围三部分共同成立。请求与数据分布决定系统走到哪个状态，并发与到达模式决定队列与竞争程度，预热与轮次决定测量是否位于稳定区间；任何一个上下文缺失，得到的数字都无法迁移到别的环境。

## 要解决的问题

两个环境之间比较吞吐或延迟数字，得出快百分之几十的结论，换到真实负载上差异消失或者方向相反。本篇回答：负载由哪些维度描述、测量误差从哪里来、预热与轮次为什么是纪律要求、中位数与分位数各自表达什么，以及结果要带哪些上下文才算可复用。

