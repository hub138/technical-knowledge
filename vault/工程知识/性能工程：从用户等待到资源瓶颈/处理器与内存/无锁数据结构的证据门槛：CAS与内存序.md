---
title: 无锁数据结构的证据门槛：CAS与内存序
type: concept
status: active
updated: 2026-09-22
review_after: 2027-09-22
change_rate: low
confidence: high
tags:
  - "concurrency"
  - "performance/cpu"
  - "software/engineering"
sources:
  - "https://en.cppreference.com/w/cpp/atomic/memory_order"
  - "https://lwn.net/Articles/543814/"
  - "https://en.wikipedia.org/wiki/Compare-and-swap"
---

## 要解决的问题

锁不是不能用，是慢在两处：上下文切换（等待者睡眠再唤醒，微秒级）与缓存行乒乓（锁竞争把锁字所在行打来打去）。CAS（compare-and-swap）循环承诺免切换的原子更新：失败就重试，不睡眠。但 CAS 循环的"快"要有证据支撑，没有证据的 CAS 换来的常是更慢（自旋烧 CPU、ABA 咬坏数据、内存序错配在弱内存模型机器上随机崩）。问题的本质：**无锁把"等待"从调度器（睡眠）搬到指令层（重试），快慢取决于竞争强度与回退路径，它有明确的适用门槛，不是锁的普适替代品**。

## 机制

**CAS 的协议语义**：一条指令完成"读-比-写"三步原子动作。`compare_and_swap(ptr, expected, desired)`：当前值等于 expected 才写入 desired，返回是否成功。失败的线程重读再试。硬件保证单条 CAS 的原子性（x86 的 `lock cmpxchg`、ARM 的 LL/SC 对），线程级的不变量要靠循环与重试自己维护。

**ABA 问题：值相等不等于没动过**。线程 1 读到 A，被抢占；线程 2 把 A 改 B 再改回 A；线程 1 的 CAS 发现还是 A，成功写入——但中间态 B 已被别人消费或结构已被改形（链表节点被释放又复用，指针同值不同物）。解法：带版本号的 CAS（DoubleWord CAS，指针+计数）、 hazard pointer（延迟复用）、epoch-based reclamation（分代回收）。**ABA 在无 GC 的语言里是数据正确性问题，在带 GC 语言里退化为计数语义问题**（对象不会复用同地址，但业务状态仍可能"回去了"）。

**内存序：原子 ≠ 顺序**。CAS 成功只保证被操作的字段原子，不保证周围的读写顺序。弱内存模型（ARM、POWER）上编译器与 CPU 都会重排。acquire/release 语义：release 写之前的读写不许下沉到写之后，acquire 读之后的读写不许上浮到读之前——两者配对形成"发生先后"（happens-before）边。`memory_order_seq_cst` 全局总序但最贵。经典事故：用 relaxed CAS 更新标志位，数据写入被重排到标志之后，读者看到标志置位但数据未就绪。**内存序是给重排划边界的约定，选错序是并发 bug 的富矿**。

**证据门槛：什么时候无锁真的快**。三个条件同时满足才值得：临界区极短（几条指令，锁的获取释放开销占比过高）；竞争低或竞争模式友好（CAS 失败率低，自旋不烧）；或需要免锁的实时性/免死锁性质（中断上下文不能睡眠拿锁）。竞争激烈时长 CAS 自旋比互斥锁更糟（自旋消耗整核还阻塞总线，futex 在无竞争时只是一条原子指令）。判定靠测量：`lock cmpxchg` 失败率、每操作纳秒数、与 std::mutex 对照，不靠"无锁更快"的直觉。

## 背景与代价

思想背景：CAS 指令源自 IBM System 370（1970s，compare-and-swap 原语），研究界把它推成无同步原语的基础（Herlihy & Shavit 等的无锁算法体系，2000s）。Java 5（2004）的 java.util.concurrent（Doug Lea）把 CAS+内存序做成平台级并发库，"原子类替代锁"进入工业主流。C++11 标准化 memory model（2011）把内存序从方言（各编译器 barrier 内建）收进语言。RCU、hazard pointer 等内存回收方案在 Linux 内核与文献中成熟。

最精巧的一笔是**把同步从"临界区"改写成"协议"**：锁定义一块互斥区域（进区睡觉出区唤醒），无锁把互斥改写成一轮轮协商协议（尝试-失败-重读-再试），不变量由算法结构（而非阻塞）保证。这个视角让无锁队列、Treiber stack（一句 CAS push）成为可能。代价要摆明：无锁算法的设计与验证难度高一个量级（不变量证明、ABA、内存回收三座山，Linus 对无锁链表的批评是行业警钟：多数人写不对）；弱内存模型上的测试地狱（x86 强序机器上"碰巧对"，ARM 上随机坏，TSAN 能测一部分但模型细节仍靠形式化方法 litmus test）；调试与观测难（竞态偶发、core dump 里状态已变）。**无锁是特种兵，不是步兵**：内核 RCU 读侧免锁、JDK 的 ConcurrentHashMap 分段 CAS、无锁 MPMC 队列这些场景验证过它值，业务代码的大多数临界区该用锁。

## 边界

- **单生产者单消费者（SPSC）环形队列是无锁的甜点区**：两个写者各写各的游标（正好用上一条缓存行对齐避免伪共享），协议简单可证明。多生产者（MPMC）复杂度跳档（Helping 协议、批量索引），非热点别碰。
- **内存序错误在强序机器上测不出来**。x86 的 TSO 模型免费给了很多顺序保证，代码迁到 ARM/Graviton 上才爆。跨平台部署的代码内存序必须按最弱模型写（release/acquire 显式配对），litmus 工具（herd、loom）验证。
- **CAS 重试风暴是隐藏的活锁**。高竞争下所有线程同时失败同时重试（同步的失败节拍），吞吐塌到比串行还低。缓解：退避（指数退避+随机抖动）、或放弃改用锁。队列长度/失败率的监控是无锁结构上线的必备观测。
- **"无锁"有分级，宣传语不等于性质**。lock-free（系统级前进保证：至少一个线程在前进）≠ wait-free（每个线程有限步完成，更强也更贵）。多数"无锁"实现只是 lock-free，个别操作仍可能长期停滞。

## 验证

- **CAS vs mutex 对照实验**：短临界区计数器分别用 CAS 循环与 std::mutex 实现，竞争线程数从 1 扫到 32。预期：低竞争 CAS 每操作纳秒级优势明显；高竞争区 CAS 失败率飙升、吞吐反超点出现，mutex（futex 睡眠）在重竞争下更稳。适用门槛的实测证据。
- **内存序 litmus 实验**：Store-Load 重排的经典 message passing 程序（两个标志位 + relaxed/acquire-release/seq_cst 三版），在 ARM 机器上跑百万次迭代。预期：relaxed 版出现乱序观察（r1=1 但 r2=0），acquire-release 版零乱序。内存序必要性的可复现证据。
- **ABA 复现实验**：Treiber stack（无锁后进先出结构，push/CAS pop）不带版本号，并发 pop 同一节点两次（中间被释放复用）。预期：带 GC 语言需对象池复用地址才能复现双弹；带版本号（DCAS/指针计数）后双弹消失。ABA 从传闻变实验证据。

## 相关

- [[工程知识/性能工程：从用户等待到资源瓶颈/处理器与内存/缓存行与伪共享：64字节里的并发性能.md]] —— CAS 竞争把缓存行乒乓放大，游标对齐是无锁队列的标配组合
- [[工程知识/后端系统：在并发、失败与变化中维持服务/任务与并发/分布式锁的正确与错误用法：fencing与租约.md]] —— 单机的 CAS 与跨机的分布式锁是同一问题（互斥+ fencing）的两个尺度
- [[工程知识/性能工程：从用户等待到资源瓶颈/操作系统与运行时/进程线程与系统调用构成执行边界.md]] —— 锁的睡眠路径经过调度器（futex），无锁把等待留在用户态，边界在哪决定开销在哪
- [[工程知识/缺陷分析：从个案到体系/并发/并发缺陷的复现策略：压力放大与确定性调度.md]] —— 内存序与 CAS 是机制背景，本篇是复现侧
- [[工程知识/缺陷分析：从个案到体系/并发/回调必须回到属主线程：完成通知的线程归属纪律]] —— "线程安全的容器加非串行化使用"仍会撕裂上层不变量的反面教材
