---
title: 缓存行与伪共享：64字节里的并发性能
type: concept
status: active
updated: 2026-09-22
review_after: 2027-09-22
change_rate: slow
confidence: high
tags:
  - performance/cpu
  - concurrency
  - systems/memory
sources:
  - "https://en.wikipedia.org/wiki/CPU_cache"
  - "https://www.kernel.org/doc/html/latest/core-api/cachetlb.html"
  - "https://lwn.net/Articles/250967/"
---

## 要解决的问题

多线程程序里，两个线程各写各的变量，逻辑上毫无共享，性能却塌了一截。这些变量在代码层各自独立，在缓存层同住一个 64 字节的缓存行（cache line），硬件为保证一致性把整个缓存行当作失效与同步的最小单位。两个独立计数器各自 +1，缓存一致性协议把“独立写”翻译成“乒乓同步”，性能随核数增加反而下降。问题的本质：**程序的对象布局是按语言语义组织的，硬件的一致性粒度是按缓存行组织的，两层粒度错配**，写写冲突在语义层不存在，在缓存层真实发生。

## 机制

**缓存行：硬件管理的最小单位**。CPU 缓存不以字节为单位管理，而以缓存行为单位（主流 x86/ARM 为 64 字节，部分 Apple Silicon L2 为 128 字节）。一次内存读把整行搬进缓存，一致性协议（MESI 及其变种）以整行为单位维护各核副本状态。一个 64 字节缓存行内放 8 个 long，任何核写其中任何一个，其他核持有的整行副本都进入失效状态，下次读要重新同步。一致性粒度是“行”，**行内的任何写都波及整行**。

**伪共享：错配的具体形态**。两个线程分别频繁写相邻的两个变量（比如各自队列的头指针、各自的统计计数器），变量恰好落在同一缓存行。线程 A 写自己的计数器 → 整行在 A 核为 Modified、在 B 核失效；线程 B 写自己的计数器 → 整行迁到 B 核、A 核失效。变量独立，缓存行乒乓。MESI 在做语义上完全正确的事（保证一致性），代价由共享同一行的无关写者集体承担。

**填充（padding）：把语义独立变成物理独立**。在热点字段间插入冗余字节，让不同的核写不同行。Java 历史上用 `long p1,p2,...` 手工填充，后来 `@Contended` 注解（需 -XX:-RestrictContended）由 JVM 自动填充；C/C++ 用 `alignas(64)` 或手工 padding；Go 1.22 之前 struct 内字段重排不做跨行填充，热点场景需手工。注意填充的方向：只有**写写冲突**的热点才值得填充，只读字段共享一行毫无代价（副本不失效）。

**检测：先测后填**。`perf c2c`（Linux，cache-to-cache 采样 HITM 事件）直接报告伪共享热点行；`perf stat -e cache-misses` 的异常偏高是线索但非定位；macOS 无公开等价工具，靠 Instruments 的 Cache Misses 面板推断。JVM 场景 JMH `-prof perfasm` / `-prof gc` 旁证。判定标准：**多个核对同一行交替写（HITM 集中）且逻辑上变量独立**，才是伪共享；逻辑上真共享的变量同住一行反而是优化（一个一致性事务覆盖两个热点）。

## 背景与代价

思想背景：缓存一致性协议从 DEC Firefly、Intel Pentium 时代演化到 MESI（Illinois，1985）成为事实标准；SMP 多核普及（2005 年前后）后伪共享从教科书概念变成工程问题。Disruptor（LMAX，2011）把缓存行填充做成并发框架的招牌设计（环形队列槽位对齐 128 字节防伪共享），Java 社区因此广泛认识这个问题。Linux 内核的 `____cacheline_aligned` 宏、内核文档 Cache and TLB Flushing 详述了对齐约定。

最精巧的一笔是**把“逻辑独立”翻译成“物理独立”的填充技术**：语义层的并发正确性由锁与原语保证，缓存层的并发效率由布局保证，两层各司其职。Disruptor 的环形缓冲把 sequence 与条目元数据填充隔离，单线程写多线程读的路径上每个消费者游标独占一行，吞吐显著高于传统锁队列。代价要摆明：填充浪费内存（每热点对象多出 56 到 120 字节，结构体数组场景浪费可达数倍）；@Contended 这类自动机制只对 JVM 内部与标注字段生效，跨对象布局无能为力；**过度填充是更常见的错误**——不经测量给所有结构体对齐，内存带宽压力（行数变多）反而损害性能，且多数程序的多数字段没有写写冲突。填充是测量驱动的点手术，是架构级默认动作。

## 边界

- **64 字节是当前主流，是约定不可依赖**。Apple Silicon M 系 L2 行 128 字节，老 ARM 实现各异。跨平台性能代码用运行时探测（`sysconf(_SC_LEVEL1_DCACHE_LINESIZE)`、JVM 的 CacheLineSize）或取保守值 128 填充。
- **只读共享不是问题，填充只对写写热点**。三个线程只读同一行，各核各持副本零同步代价。给只读字段填充是纯浪费。
- **真共享反而该同居**。锁与其保护的数据同住一行（一次一致性事务把锁与数据一起拿到）在特定模式（自旋锁+数据紧邻）下是优化；把锁单独填充反而增加一次行同步。是否填充取决于访问模式的实测，布局没有普适最优。
- **编译器与运行时可能重排你的布局**。C 结构体字段重排、Go 编译器对未用 padding 的消除（unused padding 警告）、JVM 对象头布局，都会让手工填充失效或过度。验证填充生效要看内存布局的实测（`pahole` 报告结构体空洞、JOL 打印对象布局），不能只看源码。

## 验证

- **perf c2c 实验**：多线程各自写相邻计数器，`perf c2c record`/`report`。预期：HITM 事件集中在该计数器地址区间；填充到不同行后 HITM 峰值消失，吞吐按核数接近线性提升。伪共享存在与消除的对照证据。
- **缓存行大小探测**：程序步进写数组，步长从 8 字节到 512 字节扫一遍，测吞吐拐点。预期：拐点出现在 64（x86）或 128（M 系列）附近——缓存行大小可被公开手段复现，不依赖厂商文档。
- **Disruptor 对照读**：LMAX 论文与 Disruptor 源码的 RingBuffer 填充段，读到填充对象与消费者游标的布局意图。第三方高性能库的公开设计证据。

## 相关

- [[工程知识/性能工程：从用户等待到资源瓶颈/处理器与内存/CPU缓存与分支决定有效执行时间.md]] —— 缓存行是缓存层级的基本单位，本篇是一致性粒度的并发代价
- [[工程知识/性能工程：从用户等待到资源瓶颈/处理器与内存/NUMA让跨槽访问付出带宽与延迟代价.md]] —— 伪共享与 NUMA 是多核内存系统的两个维度：行级失效与跨槽供给
- [[工程知识/数据系统：在并发与故障中保存事实/查询与索引/列存与向量化执行：OLAP引擎快的两个来源.md]] —— SIMD 通道数受缓存行供给约束，列存的行级布局是同一粒度思想在数据系统的应用
- [[工程知识/性能工程：从用户等待到资源瓶颈/处理器与内存/SIMD与数据并行：一行代码的多个通道.md]] —— 通道数是并行度上限，布局连续才装得满通道
- [[工程知识/性能工程：从用户等待到资源瓶颈/处理器与内存/无锁数据结构的证据门槛：CAS与内存序.md]] —— CAS 竞争放大缓存行乒乓，游标对齐是无锁队列标配
- [[工程知识/性能工程：从用户等待到资源瓶颈/存储与网络/cache分配策略与命中率：LFU与LRU的实证对比.md]] —— CPU 缓存行是硬件 LRU 实例，本篇是软件缓存策略层，共享局部性逻辑
