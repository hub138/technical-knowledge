---
title: 临时文件要先写进命名约定：可清理性是写入路径的设计要求
status: active
type: pattern
updated: 2026-09-23
change_rate: low
confidence: high
review_after: 2029-09-23
tags:
  - operating-systems/process
  - operations
sources:
  - "https://man7.org/linux/man-pages/man2/rename.2.html"
  - "https://www.sqlite.org/tempfiles.html"
---

# 临时文件要先写进命名约定：可清理性是写入路径的设计要求

> **要点**：临时文件的可靠性要求是"随时可安全删除"——命名带进程与用途标识、启动扫描清理孤儿、写入路径分三步完成（临时名 → fsync → 原子改名）；三件事配齐，磁盘打满与残留垃圾从"运维事故"变成"启动日志里的一行清理记录"。

## 要解决的问题

临时文件的问题在"逃逸"：进程崩溃时写了一半的文件没人认领（孤儿），越积越多直到磁盘打满——**打满的时机恰好在最需要磁盘的时候**（写入高峰，日志与数据同时膨胀）。上游 BookKeeper 的一个复现改进是修复"SST 文件在 locations 目录不被清理"的缺陷（垃圾文件堆积的存储版）：**写下去容易，收回来要靠设计**。本篇把临时文件的可清理性展开：**命名约定的字段、启动清理的扫描逻辑、文件就位的三步**——目标只有一个：任何时刻手动或自动删除全部临时文件，服务无损。

## 机制：命名、清理与文件就位

**命名约定带三类字段**：进程标识（pid 或实例 id，认领用）、用途标识（purpose：sort-temp、merge-temp、session-upload，分类统计与定向清理用）、随机后缀（防并发冲突：同进程两个协程写同类临时文件不互踩）——**命名即元数据**，约定定好后清理脚本不用读文件内容就能分类。

**启动扫描清理孤儿**：进程启动时扫描自己的临时目录，删除"命名匹配 + 不属于当前运行实例"的文件——**清理的判据来自命名约定**（无约定的目录只能按 mtime 猜，误删风险高）。两类工具补位：systemd-tmpfiles 按年龄清理（system 侧），应用框架的启动钩子清自己的（应用侧）；两层各管一段，注册表（tmpfiles.d 配置）与代码约定一致。

**原子就位三步**：写入 `name.tmp-<pid>-<rand>` → fsync 数据 → `rename()` 到正式名——**rename 在同一文件系统内是原子操作**，读方要么看到旧文件要么看到新文件，永远看不到半成品；SQLite 的 journal、LSM 引擎的 SST 就位都是这个模式。**跳过 fsync 直接 rename 的隐患**：崩溃后 rename 生效但数据未持久，正式名指向空文件或旧内容——本库"存储引擎的文件一致性"的元数据核对在这里就是核对这一步有没有做全。

**权限与目录隔离**：临时文件放独立目录（不与正式数据混放），目录权限按最小授权（本服务可写、其他不可读——临时数据也可能敏感）；`TMPDIR` 重定向是容器环境的第一课（容器可写层与挂载卷的容量边界不同）。

## 看完能判断：清理的安全判据

**清理的三个安全判据**：正在写的临时文件不能删（按 mtime + 打开状态判断，`lsof` 或 fuser 核对）、跨进程共享的临时目录不按名字猜（按约定前缀）、删除动作要留日志（清了什么、多大、为什么——磁盘类事件的取证起点）。**自动清理与手动清理都要过这三条**，差异只在触发时机。

**两个典型的失败模式**：临时文件写进正式数据目录（混放后清理脚本不敢删，孤儿永久堆积）；临时文件命名不带 pid（崩溃后无人认领，mtime 猜测误删活文件）——**两个模式都违反命名约定**，约定是清理的地基。

## 边界

三条边界防止防线走形。

**可清理性有前提：内容可再生**。临时文件的语义是"丢了能重来"（排序中间结果、会话草稿）——不可再生的中间状态（用户上传的原始数据）要先持久化再处理，临时区只放加工产物；"临时区里的东西删了就没了"是设计错误在先。

**rename 的原子性有边界：同文件系统**。跨设备 rename 退化为复制 + 删除（非原子，中断留下两半）——临时目录与最终位置必须在同一文件系统，`TMPDIR` 指到另一块盘时要么改挂载、要么显式复制。

**清理频率与 IO 成本的平衡**：启动扫描一次的成本可控；运行中周期扫描（每分钟扫大目录）的 IO 与锁竞争要评估——大目录场景用"写入时登记 + 定期核对"降低扫描面，与全量扫描的收益差按目录规模算。

## 验证

可复现的验证路径：

1. 孤儿复现：进程运行中 kill -9，重启后预期启动扫描清掉全部孤儿、日志记录清理清单；
2. 原子性验证：写入路径注入崩溃（kill 在 fsync 前后各一次），预期崩溃后正式名要么完整要么不存在，读方无半成品；
3. 磁盘打满演练：往临时目录灌数据到配额触顶，预期写入降级（拒绝新任务）而正式数据路径不受影响；
4. 断言锚点：**临时目录与正式目录分离、命名约定带进程与用途标识、启动清理在代码路径里、清理动作有日志**——四件齐备，临时文件从"迟早打满磁盘"变成"启动时顺手清掉"。

```text
命名断言 = 临时文件名自含认领信息
就位断言 = fsync 后 rename 才算写完
清理断言 = 孤儿在启动时被清且留痕
```

临时文件的治理思路是把"以后再清理"变成"写入时就决定怎么清理"：命名约定、目录隔离、原子就位三件套，让每一份临时数据在创建时就自带删除条件。

## 相关

- [[工程知识/性能工程：从用户等待到资源瓶颈/操作系统与运行时/脏页回写在性能与崩溃丢失窗口间权衡.md]] —— fsync 与持久化时机的内核层
- [[工程知识/性能工程：从用户等待到资源瓶颈/存储与网络/存储引擎的文件一致性：元数据与数据块的对应关系要可核对.md]] —— 就位后的完整性核对
- [[工程知识/后端系统：在并发、失败与变化中维持服务/任务与并发/后台任务不能依赖终端会话维持生命周期.md]] —— 进程生命周期与文件生命周期的托管
- [[工程知识/数据系统：在并发与故障中保存事实/事务与存储/LSM树用写放大换取顺序写和可扩展性.md]] —— SST 类文件的就位实践
