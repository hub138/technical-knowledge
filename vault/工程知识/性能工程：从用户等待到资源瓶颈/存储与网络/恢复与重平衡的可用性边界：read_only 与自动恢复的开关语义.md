---
title: 恢复与重平衡的可用性边界：read_only 与自动恢复的开关语义
status: active
type: pattern
updated: 2026-09-23
change_rate: low
confidence: high
review_after: 2029-09-23
tags:
  - networking
  - operations
  - defect-analysis
sources:
  - "https://github.com/apache/bookkeeper"
  - "https://www.sre.google/sre-book/managing-critical-state/"
---

# 恢复与重平衡的可用性边界：read_only 与自动恢复的开关语义

> **要点**：存储节点的"只读降级"与"自动恢复"是两个独立开关，组合出四种运维形态；缺陷形态是"磁盘有空间却卡在只读"与"该自动恢复时不动手"——开关语义、状态机判定条件、人工接管路径三件事在文档与演练里显式化，恢复从碰运气变成按图作业。

## 要解决的问题

存储节点有一组防自伤的降级机制：磁盘满转只读、副本损坏拒绝服务、检测到异常状态停止写入。这些机制的意图是**用可用性换数据安全**，但机制的判定条件与解除路径常出偏差——上游 BookKeeper 的一个复现缺陷是：磁盘有可用空间，节点却停留在 read_only 状态不再恢复服务（状态判定的解除条件与进入条件不对称）；另一个方向的问题是自动恢复（auto-recovery）该触发时未触发，副本缺失长期存在。**运维要回答的问题是"这个节点现在为什么不服务、谁有权把它拉回来"**——开关语义混乱时，这个问题的答案靠猜。

## 机制：两开关四形态与状态机的不对称

**两个独立开关**：read_only（本节点是否拒绝写入，磁盘满等自保条件触发）与 auto-recovery（集群是否自动补副本/重平衡）。开关分离的收益是故障调查期可以冻结现场而不中断副本保护，代价是两个开关的组合状态空间要靠文档与演练维持（组合错了就是一次事故）：组合出的四种形态各有适用场景——**正常服务 + 自动恢复**（常态）、**只读 + 自动恢复**（磁盘满，节点退出写入但副本照补）、**正常服务 + 恢复暂停**（计划内维护，避免恢复流量干扰）、**只读 + 恢复暂停**（故障调查期，冻结现场）。

**状态机的不对称是缺陷高发区**：进入只读的判定（磁盘水位过线）与退出只读的判定（水位回落 + 显式解除 + 引用刷新）常由不同代码路径实现，退出路径缺少"水位已回落"的复查、或缺少解除后的状态广播——上游复现的"磁盘有空间却卡只读"正是退出路径的判定缺失。**判定条件对称性检查（进入条件取反是否等于退出条件）应进存储系统设计评审**。

**自动恢复的触发链**：副本缺失检测（元数据里的副本集合 vs 实际存活节点）→ 恢复任务生成 → 限速执行——链上任何一环的静默失败（检测周期性抖动、任务生成后 worker 不可用、限速参数失效）都表现为"该恢复时不动手"。恢复链的每一环要有独立的可观测信号：缺失副本数、在途恢复任务数、恢复吞吐。

## 看完能判断：运维处置的决策树

节点不服务时的排查顺序：

**一查降级原因**：只读状态的原因字段（磁盘满？副本损坏？人工设置？）——原因不明时不解除降级。**二查解除条件**：触发原因是否已消除（空间已清理？损坏副本已修复？），消除后按系统的解除路径操作（显式命令、重启、自动复查），解除后验证状态广播生效（其他节点不再把写入路由过来）。**三查恢复链**：如果本节点数据有副本缺失，核对恢复任务的状态（生成没有？执行没有？速率多少？），卡住的环节对症处理（重启 recovery worker、修限速参数、手动触发）。

**人工接管的边界**：自动恢复暂停期间的人工补副本，完成后要把开关恢复原位——**"处置动作的收尾是恢复常态配置"**，冻结现场忘了解冻、暂停恢复忘了重启，是恢复链失效的常见人为原因。开关的当前值与预期值要有巡检比对（配置漂移检测，本库软构域"灰度与功能开关的边界"的同构纪律）。

## 边界

三条边界防止防线走形。

**read_only 不等于数据只读**：节点层面拒绝写入时，客户端看到的是部分失败（写不进这台），读路径可能仍可用——把"节点只读"误当"集群只读"会触发不必要的全量回滚。

**自动恢复不是无条件的善**：元数据损坏场景下自动恢复会按错误的副本视图"修复"——把仅存的正确副本覆盖掉。**元数据可疑时先暂停恢复再调查**，这是冻结现场的价值所在。

**状态判定依赖的指标本身会骗人**：磁盘水位按 inode 还是按容量、按实时还是按缓存值——判定条件用的指标口径要与告警口径一致，口径漂移会产生"告警说满、判定说没满"的分裂。

## 验证

可复现的验证路径：

1. 对称性验证：测试环境触发磁盘满只读 → 清理空间 → 预期节点自动或按既定路径恢复写入；解除失败即退出路径有判定缺失（上游复现缺陷的形态）；
2. 恢复链验证：删一个副本节点，预期缺失检测在周期内触发、恢复任务生成、按限速执行；逐环断开（停 worker、错配限速），预期对应环节的监控信号可见——"不动手"可归因到环节；
3. 冻结演练：人工暂停恢复 → 完成处置 → 恢复开关，巡检比对预期捕捉到任何遗漏的开关漂移；
4. 断言锚点：**进入/退出条件对称性在评审记录里核对过、恢复链各环节有独立监控、开关值有常态巡检**。

```text
对称断言 = 水位回落可恢复写入
链条断言 = 缺失检测/任务生成/执行 三信号可见
巡检断言 = 开关漂移被比对捕捉
```

降级开关与自动恢复的语义清晰化，把"节点为什么不服务"从考古题变成决策树——每个分支有判定条件、有解除路径、有验证动作，恢复作业按图执行。

## 相关

- [[工程知识/数据系统：在并发与故障中保存事实/可靠性与运维/复制的三种模式：主从、多主与无主的失败形态]] —— 副本缺失与恢复的动作背景
- [[工程知识/性能工程：从用户等待到资源瓶颈/存储与网络/磁盘空间管理三原则：配额水位与临时目录纪律]] —— 只读降级最常见的触发源（磁盘满）的防线
- [[工程知识/软件构建：让变化可以理解、验证与交付/测试与交付/灰度与功能开关的边界：交付节奏与配置漂移]] —— 开关漂移巡检的同构纪律
- [[工程知识/后端系统：在并发、失败与变化中维持服务/服务设计/健康检查的三层语义：存活就绪与深度]] —— 节点状态对外语义的分层
