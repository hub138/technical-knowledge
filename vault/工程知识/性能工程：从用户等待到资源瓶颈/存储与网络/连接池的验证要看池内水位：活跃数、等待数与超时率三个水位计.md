---
title: 连接池的验证要看池内水位：活跃数、等待数与超时率三个水位计
status: active
type: pattern
updated: 2026-09-23
change_rate: low
confidence: high
review_after: 2029-09-23
tags:
  - networking
  - observability
  - defect-analysis
sources:
  - "https://github.com/brettwooldridge/HikariCP"
  - "https://www.postgresql.org/docs/current/runtime-config-connection.html"
---

# 连接池的验证要看池内水位：活跃数、等待数与超时率三个水位计

> **要点**：连接池的健康状况读三个水位计——活跃连接数（池被用了多少）、等待队列深度（借不到连接的请求有多少）、获取超时率（彻底借不到的比例）；三个水位计的组合形态对应三类病因（池太小、池泄漏、下游变慢），扩容只对第一类有效，先读表再动手。

## 要解决的问题

连接池故障的报错是"获取连接超时"，常见误反应是把池调大——**三类病因里只有一类对调大有反应**：池太小（峰值活跃数顶到 max）、池泄漏（借了不还，活跃数只涨不降）、下游变慢（单次占用时间变长，池没坏但周转慢了）。**扩容对泄漏无效**（更大的池只是推迟打满），对下游变慢有害（更大的并发打到慢下游）。本库服务设计域"连接池参数的联动校验"讲过参数推导；本篇讲运行时的水位诊断：**三个水位计的读法、组合形态与病因对照、每类病因的处理动作**。

## 机制：三个水位计的读法

**活跃连接数（active）**：当前借出未还的连接数——**健康形态是在 max 之下波动**（峰值接近 max 且等待出现，才是池太小的信号）；异常形态是"只涨不降、长期贴着 max"（泄漏的嫌疑），或"低位平稳但超时率高"（下游变慢的信号：连接在池里是空闲的，业务层却报超时——超时发生在别处）。

**等待队列深度（pending）**：正在等连接的请求数——**瞬时波动正常**（突发流量时短暂排队），持续大于零是容量不足或周转慢的信号；等待深度与活跃数的相位关系是判据：活跃贴 max 且等待持续，池是瓶颈；活跃不满且等待持续，连接在个别地方被长时间占用（慢查询、下游 hang）。

**获取超时率（timeout）**：等待超过 maxWait 的请求比例——**它是最终症状**，前两个水位计是先行指标；超时率曲线与等待深度的领先关系（等待先涨、超时后涨）是容量预警的窗口。**告警挂先行指标**（等待深度持续 > 0 达 N 秒），超时率告警作最后防线。

**三者的组合诊断表**：

| 活跃 | 等待 | 超时 | 判断 | 动作 |
| --- | --- | --- | --- | --- |
| 贴 max | 持续 > 0 | 上升 | 池太小 | 扩 max（联动下游连接上限） |
| 只涨不降 | 上涨 | 上升 | 泄漏 | 泄漏检测 + 借还配对审计 |
| 低位平稳 | 上涨 | 上升 | 下游变慢 | 修下游耗时，别扩池 |
| 波动正常 | 瞬时 | 零 | 健康 | 不动作 |

## 看完能判断：三类病因的处理

**池太小（扩容前的两个核算）**：max 的上限是下游连接数预算 ÷ 实例数（下游 `max_connections` 被所有实例分摊，本库"节流参数与容量匹配"的全局核算原则）；扩容后核对单连接占用时间（pool 的连接生命周期参数联动）——**扩容的收益是排队消散、超时回落，代价是下游连接预算被多占**，所以扩容是改一组联动参数，单改 max 是最常见的半截动作。

**池泄漏（检测与审计）**：泄漏检测的两个手段——连接借出时的堆栈记录（leakDetectionThreshold，借出超过阈值打印获取点堆栈）、借还配对审计（borrow/return 计数恒等，缺口即泄漏量）——**泄漏的常见源头**：异常路径漏还（finally 缺失）、事务未关闭、长事务持有（本库"案例四十七"的长事务是连接泄漏的高发形态）。

**下游变慢（不扩池的验证）**：单连接占用时间曲线（connection usage time 的 P99）与下游自身耗时曲线对齐，即可确认瓶颈在下游——**池参数全部不动，修下游**；修完后等待与超时自动回落，池水位是下游健康的传导表。

## 边界

三条边界防止防线走形。

**水位计要有实例维度的聚合**：多实例部署下"平均活跃数"会掩盖单实例泄漏（一实例打满、其他正常，平均值看起来健康）——**聚合看总量、分位看分布，max(instance) 单独成图**。

**池的空闲回收与最小连接数要联动业务节奏**：空闲回收太快（低谷期缩到 min）会让高峰期重建连接（建连成本打在业务头上）——**min idle 按高峰爬坡速度定**，回收窗口长于爬坡时间。

**验证型压测要带故障注入**：正常压测验证不到泄漏与下游变慢（两者都是异常路径的病）——压测方案里带"借出不还"注入与"下游延迟"注入，预期水位计按诊断表的形态变化，才算验证了诊断能力。

## 验证

可复现的验证路径：

1. 病因复现：测试环境分别注入三类场景（小池 + 高并发、借出不还、下游延迟），预期三个水位计按诊断表的形态区分，判断结论与注入一致；
2. 泄漏检出验证：注入泄漏，预期 leakDetection 在阈值时间打印获取点堆栈、borrow/return 缺口计数准确；
3. 联动验证：扩 max 而不动下游预算，预期下游拒绝连接数上升——验证"扩容是联动改组"的判据；
4. 断言锚点：**三个水位计有实例维度监控、等待深度先行告警在管、泄漏检测开启且有审计计数**——三件齐备，"连接超时"从条件反射式扩池变成按水位计读表下药。

```text
水位断言 = 活跃、等待、超时三表齐全
诊断断言 = 三类病因按形态可区分
泄漏断言 = 获取点堆栈可打印可审计
```

连接池的排障读表优先于调参：三个水位计的组合形态直接给出病因分类，扩容只对"池太小"这一类有效——读表的十秒钟，省掉扩容无效后的两小时排查。

## 相关

- [[工程知识/后端系统：在并发、失败与变化中维持服务/服务设计/连接池参数的联动校验：池大小、超时与队列深度是一组约束.md]] —— 参数推导的静态面，本篇是运行时水位面
- [[工程知识/缺陷分析：从个案到体系/资源与容量/案例六：容量与泄漏——连接泄漏和 SST 堆积.md]] —— 连接泄漏与空间泄漏的个案
- [[工程知识/性能工程：从用户等待到资源瓶颈/存储与网络/连接建立失败先看内核队列：SYN 队列与 accept 队列是两个瓶颈.md]] —— 建连侧的队列水位，与池内水位互补
- [[工程知识/性能工程：从用户等待到资源瓶颈/存储与网络/节流参数与容量匹配：全局限流值要按单点容量乘副本数算.md]] —— 全局预算核算的同类方法
