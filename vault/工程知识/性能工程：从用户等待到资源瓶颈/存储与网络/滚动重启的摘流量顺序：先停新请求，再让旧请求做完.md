---
title: 滚动重启的摘流量顺序：先停新请求，再让旧请求做完
status: active
type: pattern
updated: 2026-09-23
change_rate: low
confidence: high
review_after: 2029-09-23
tags:
  - networking
  - operations
  - defect-analysis
sources:
  - "https://kubernetes.io/docs/concepts/workloads/pods/pod-lifecycle/"
  - "https://www.envoyproxy.io/docs/envoy/latest/intro/arch_overview/upstream/load_balancing"
---

# 滚动重启的摘流量顺序：先停新请求，再让旧请求做完

> **要点**：实例从服务摘除的正确顺序是三步——从注册表摘除（新流量不再来）、等在途请求完成（旧流量做完）、超时后退出（残留强杀有兜底）；跳过第二步是重启丢请求的主因，每步的超时要与调用方的重试和超时预算联动。

## 要解决的问题

滚动重启是最高频的运维动作，也是最常见的"零星错误来源"：每次发版出现几笔超时或连接重置——**原因几乎都在摘流量顺序**：进程收到停止信号后立刻退出，负载均衡的注册表还没刷新（新请求继续打过来）、连接池里还有在途请求（旧请求被腰斩）。本库任务与并发域"后台任务的守护链"讲过进程托管；服务设计域"优雅停机的完成语义"讲过排空接口；本篇把**三步顺序的时序、每步超时与调用方预算的联动、长连接与短连接的差异**展开成可直接执行的流程。

## 机制：三步顺序与时间预算

**第一步：从注册表摘除（停止接新）**。停机信号到达后第一动作是向服务注册表（或负载均衡配置）摘除本实例——**摘除生效的传播时间 = 注册表刷新间隔 + 各调用方缓存刷新间隔**，这个传播窗口内新请求还会来（上一轮的注册信息还在缓存里）。**第一步的超时要覆盖这个传播窗口**，否则第二步刚开始就被新请求打断。

**第二步：等在途请求完成（旧流量做完）**。新请求停了，已接入的请求继续处理到完成——**第二步的超时按本服务请求 P99 的若干倍定**（覆盖慢请求），超时到了还没完成的请求按业务语义决定（可重试的让它失败由调用方重试，不可重试的尽力完成）。这一步的实现形态按服务类型：HTTP 服务停止 accept 并等待 handler 返回，RPC 服务关闭监听并等待在途调用，消息消费停止拉取并等待当前消息 ack。

**第三步：超时强杀兜底**。第二步超时后进程退出，残留的连接与任务由内核回收——**第三步由进程管理器执行**（systemd 的 TimeoutStopSec、Kubernetes 的 terminationGracePeriodSeconds），强杀是兜底不是路径：每次走到强杀都说明第二步预算不足，要回头调预算。

**与调用方预算的联动**：摘流量窗口的每一秒都要位于调用方的超时与重试预算内——本实例摘除后，调用方对它的失败请求要能重试到其他实例（重试预算够）或快速失败（超时够短）。**服务端停机预算比调用方超时长、调用方重试预算覆盖失败窗口**，三个数字互相咬合，任意一个错位都让滚动重启出错误。

## 看完能判断：长连接与短连接的差异

**短连接服务（每请求新建连接）**：摘除注册表后新请求自然减少，剩余在途请求有限——三步顺序基本够用，重点在第一步传播窗口的估算（按注册表与客户端缓存的刷新间隔实测）。

**长连接服务（连接常驻）**：注册表摘除不影响已建立的连接——**客户端不主动断开，连接会一直用**。补充动作是服务端主动关连接（发 GOAWAY/关闭通知，让客户端迁移到新实例）或等待客户端按心跳判定。**长连接的排空时间 = 客户端重连周期 × 连接数规模**，预算按这个量级定，比短连接长得多。

**有状态任务（后台任务、批处理）**：在途的是任务——**任务要有检查点**（可中断、可恢复），第二步等任务到检查点后交接状态，不能等任务做完（长任务等到天荒地老）。上游 BookKeeper 的一个同类场景是 bookie 退役时的数据迁移与流量摘除配合（先摘写入流量、等副本修复完成、再下线），同一时序原理：**先停写入、再等存量、最后退出**。

## 边界

三条边界防止防线走形。

**健康检查要反映摘除状态**：注册表摘除后健康检查的语义要一致（摘除即不健康），否则负载均衡按健康检查把流量又分回来——**健康检查与注册表状态同源**，两处各说各话是摘流量失效的常见根因。

**发布批次与容量挂钩**：滚动重启每批摘除 N 个实例，剩余实例承接全部流量——**批次大小按剩余容量够不够过高峰定**（本库容量域"留白"原则），高峰期滚动要缩批次。

**启动失败不能进流量**：新实例启动后先过就绪检查再注册（不是注册后再检查）——**就绪检查挂在注册之前**，失败实例不进流量，滚动不因坏实例扩散错误。

## 验证

可复现的验证路径：

1. 零丢失验证：压测持续打流量时触发滚动重启，预期全程零请求失败（超时或错误计数为零）——这是摘流量顺序的终验标准；
2. 传播窗口实测：摘除注册表后按秒探测新请求停止时间，预期与"注册表刷新 + 客户端缓存"的估算一致，第一步超时据此校准；
3. 长连接迁移验证：长连接场景下发 GOAWAY，预期客户端在重连周期内完成迁移、无请求发送到已关闭连接上；
4. 断言锚点：**三步顺序在部署脚本或框架配置里固化、三个时间预算的数字有文档且联动、零丢失演练进过发布流程**——三件齐备，滚动重启从"零星错误的常态来源"变成"无感操作"。

```text
顺序断言 = 摘新、等旧、兜底三步固定
预算断言 = 停机预算与调用方预算咬合
终验断言 = 滚动全程零请求失败
```

滚动重启的每一次"零星错误"都在说同一件事：摘流量顺序或时间预算有一处错位。三步顺序固化、三个预算联动、零丢失演练常态化，发版的错误率就从"每次几笔"归到零。

## 相关

- [[工程知识/后端系统：在并发、失败与变化中维持服务/服务设计/优雅停机的完成语义：排空连接与任务交接]] —— 排空接口与任务交接的机制面
- [[工程知识/后端系统：在并发、失败与变化中维持服务/任务与并发/后台任务的守护链：进程托管与会话脱离是可靠性的底线配置]] —— 进程托管与强杀兜底的执行者
- [[工程知识/后端系统：在并发、失败与变化中维持服务/基础设施/长连接网关的工程要点：心跳推送与连接迁移]] —— 长连接迁移的专门场景
- [[工程知识/后端系统：在并发、失败与变化中维持服务/服务设计/服务发现与负载均衡决定请求发往哪里]] —— 注册表与缓存刷新的机制层
