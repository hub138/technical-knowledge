---
title: 存储引擎的文件一致性：元数据与数据块的对应关系要可核对
status: active
type: concept
updated: 2026-09-23
change_rate: low
confidence: high
review_after: 2029-09-23
tags:
  - hardware
  - observability
  - defect-analysis
sources:
  - "https://github.com/apache/bookkeeper/blob/master/README.md"
  - "https://rocksdb.org/blog/"
---

# 存储引擎的文件一致性：元数据与数据块的对应关系要可核对

> **要点**：LSM 类存储引擎的活性依赖"数据文件、清单（MANIFEST）、目录视图"三者对得上——任何一个环节的引用与实物脱节，都会形成磁盘泄漏或数据不可见；诊断这类问题的入口是把三者的账目各自导出后交叉核对，修复的入口是先停写入源再清理，顺序颠倒会让泄漏继续生长。

## 背景与本质：谁在记账，谁在存货

存储引擎的文件一致性要解决的问题是：LSM 类存储引擎（RocksDB 是代表，BookKeeper 的 KeyValueStorage、Pulsar 的 offload 存储都是它的用户）把数据组织成三层账目：**数据文件**（SST，真正存键值的不可变块）、**清单**（MANIFEST，记录"当前有效的 SST 是哪些"的元数据日志）、**目录视图**（文件系统里实际存在的文件）。正常路径上三者同步演进：写入产出新 SST 并在清单登记，Compaction 合并旧 SST 后把淘汰者从清单移除，后台清理按清单删除不再被引用的文件。

**泄漏的本质是"引用消失、实物还在"**：清单里已经没有某个 SST 的记录，但文件还在目录里躺着——没有清单引用的文件是孤儿，清理流程只按清单工作，孤儿文件永远不会被删。这个过程慢到不可见（每天几百 MB），直到磁盘水位告警才暴露；暴露时的形态与"写入太多"不同：写入量平稳、Compaction 正常，磁盘却单调上涨。**诊断的第一步是分清这两种上涨**：写入型增长伴随数据量与 Compaction 指标同步上涨，泄漏型增长的驱动项只有时间。

这个机制不绑定特定系统：任何"元数据 + 数据文件"结构的存储（消息队列的段文件、对象存储的分片、Git 的对象库）都有同构风险——引用与实物之间隔着一个可能失败的删除环节，失败被吞掉（清理线程的异常被 catch 后仅记日志），泄漏就开始积累。

## 机制：三个环节各自的失配形态

**Compaction 与清单的不同步**：合并流程选好输入文件、写出新文件、原子切换清单——切换失败（进程被杀、磁盘满）后重启，恢复逻辑要么回滚到合并前的清单（新写的文件成为孤儿），要么推进到新清单（旧输入成为孤儿）。**恢复路径决定了孤儿文件的产生，实现质量决定产生频率**；健康的实现会在启动时做一次孤儿扫描（目录视图与清单 diff），不健康的实现依赖下次 Compaction 恰好覆盖到——泄漏就这样沉淀下来。

**删除引用与删除实物的时序**：清理线程按清单算出"可删除文件列表"，逐个 unlink。这个窗口本身是安全的（清单已无引用，文件可删），但**删除动作失败时的处理决定后果**：unlink 报错（文件被其他进程持有、权限问题）后如果只记日志不重试，这批文件就永远留在"待删除但从未删除"的状态。上游 BookKeeper 的一个复现缺陷是 SST 文件留在 locations 目录没被清理——形态正是"引用没了，目录里的实物还在"。

**外部工具与引擎视图的冲突**：备份工具、巡检脚本、人工误操作直接动了数据目录（复制、移动、恢复旧备份到运行中的目录），引擎的清单与磁盘实物的对应关系被外力打断——这类失配没有引擎内因，诊断时要从"最近是否有引擎外的目录操作"问起。

## 诊断与修复的路径

诊断按"先对账、后归因"的顺序走，对账本身是可复现的核查手段，它的收益是泄漏从猜测变成可数的清单，代价是要在引擎侧开放清单导出的离线通道（本身是一个需要审计的运维入口）：

**第一步：把三类账目导出**。引擎侧拿清单的当前有效文件集合（RocksDB 有 `--manifest_dump` 之类的离线工具，BookKeeper 的 KeyValueStorage 有对应的 admin 接口），文件系统侧拿目录的实际文件列表，监控侧拿磁盘用量按时间曲线——**三份数据是核对的原料，缺一不可**。

**第二步：交叉核对定位失配**。目录里有而清单没有的文件集合 = 孤儿候选；孤儿候选的总大小与磁盘"超出预期的部分"对得上，泄漏假设成立；对不上，回到写入型增长假设。**这一步的输出是可数的：孤儿文件 N 个、共 M GB、最早产生时间 T**——T 的价值在归因（对照 T 附近的版本变更、配置变更、故障记录）。

**第三步：先停源、再清理**。修复顺序不可颠倒：先确认泄漏源已停（升级到修复版、关闭触发泄漏的路径），再清理孤儿文件。**顺序颠倒的后果：一边清理一边产生，清理速度追不上产生速度，且清理动作与引擎的 Compaction 并发时可能误删在途文件**。清理用引擎提供的离线工具或按"不在清单中的文件"规则生成清单，逐个核对后再执行。

**验证**：清理后观察一个完整的 Compaction 周期，磁盘用量回到"与数据量匹配的水平"且不再单调上涨；水位曲线的斜率归零是泄漏修复的判据，单次下降不是。

## 边界

三条边界防止防线走形。

**清单损坏比泄漏更严重**：孤儿文件只浪费空间，清单丢失或损坏则数据不可见——恢复旧备份、手动编辑清单这类操作的风险等级远高于清理孤儿，没有引擎工具支持时不动手。

**快照与引用计数会合法地保留"看似孤儿"的文件**：引擎的快照、检查点、进行中的迭代器都是合法引用，对账工具要按"当前有效引用集合"判定孤儿，按裸清单判定会误删被快照引用的文件，等于数据丢失。

**泄漏的根因修复靠上游版本，清理只是止血**：能定位到具体缺陷（如引用未登记、异常路径漏删）的泄漏，修复动作是升级或打补丁；只清理不修复，问题按同样的节奏复发——止血与根治的区分要写进修复记录。

## 验证

可复现的验证路径：

1. 对账可复现：同一目录跑两次对账，输出一致（孤儿集合稳定）；对账工具对已知健康目录报零孤儿（无误报基线）；
2. 泄漏复现：在测试环境构造清单切换失败（kill 合并中的进程），重启后预期出现孤儿文件——机制的实证，同时验证引擎是否有启动期孤儿扫描；
3. 清理验证：对复现出的孤儿执行清理，预期磁盘回收、引擎读写正常、快照引用的文件未被误删；
4. 断言锚点：**泄漏型增长与写入型增长的区分判据（驱动项是时间还是数据量）在诊断记录里显式写明**，这是这类问题最容易被误判的地方。

```text
对账断言 = 两次输出一致，健康目录零孤儿
复现断言 = 清单切换失败产生孤儿
修复断言 = 停源后清理，斜率归零，快照无损
```

元数据与数据块的对应关系是 LSM 类引擎的地基——对账能力（清单导出、目录 diff、曲线对照）常备，泄漏从"磁盘神秘的病"变成"账目可查的错"。

## 相关

- [[工程知识/数据系统：在并发与故障中保存事实/事务与存储/存储引擎的两大路线：B树与LSM的写读取舍]] —— LSM 的 Compaction 与清单机制背景
- [[工程知识/缺陷分析：从个案到体系/稳定性/案例一：垃圾回收线程的终止——BookKeeper 磁盘写满]] —— 磁盘写满的故障传导链，本篇是它的一种上游病因
- [[工程知识/性能工程：从用户等待到资源瓶颈/存储与网络/磁盘空间管理三原则：配额水位与临时目录纪律]] —— 空间治理的防线层，本篇是泄漏型病例的诊疗层
- [[工程知识/数据系统：在并发与故障中保存事实/可靠性与运维/备份的价值由恢复演练来验证]] —— 引擎外目录操作（备份恢复）的纪律边界
