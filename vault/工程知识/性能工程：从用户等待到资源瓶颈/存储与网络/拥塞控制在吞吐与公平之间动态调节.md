---
title: 拥塞控制在吞吐与公平之间动态调节
type: concept
status: active
updated: 2026-09-21
review_after: 2027-09-21
change_rate: low
confidence: high
sources:
  - "https://www.rfc-editor.org/rfc/rfc5681"
  - "https://www.rfc-editor.org/rfc/rfc9002"
  - "https://calonyu.github.io/dynamic-bbr-b6f5/"
tags:
  - networking
  - tcp
  - performance/network
---

# 拥塞控制在吞吐与公平之间动态调节

同一条链路，一个批量传输把窗口拉满，其他人的交互请求延迟飙升——这不是带宽不够，是拥塞控制没分好。要解决的问题：发送方不知道网络能吃多少、别人在用多少，只能靠丢包或延迟信号探测；探测策略（经典 AI/MD、CUBIC、BBR）决定了吞吐、尾延迟与多流公平性的取舍。

## 机制：从信号到窗口

```text
信号        经典响应                现代响应
丢包        AIMD: 窗口减半再线性爬升  BBR: 不看丢包，看带宽×RTT 积
RTT 上升    （忽略）                BBR/DCTCP: 早于丢包的拥塞信号
ECN 标记    （可选）                DCTCP/BBRv2: 显式反馈细粒度响应
```

AIMD（RFC 5681）：每 RTT 窗口 +1（加性增），丢包则窗口减半（乘性减）。锯齿形窗口能收敛到公平份额，代价是永远在过冲与保守之间震荡。CUBIC（Linux 默认）用三次函数让高 BDP 链路更快回满。BBR 反向思路：主动测量瓶颈带宽与最小 RTT，窗口 = 带宽×RTT，理论上不填满缓冲区，尾延迟与吞吐兼得。

锯齿形的窗口轨迹放到一张图里看，慢启动的指数爬升、拥塞避免的线性爬升与丢包后的减半各自长什么样：

![Wikipedia 条目图：TCP 慢启动与拥塞避免的窗口轨迹——纵轴是拥塞窗口 cwnd（MSS），横轴是 RTT；指数爬升到阈值后转线性增长，超时（蓝点）与重复 ACK（红点）触发窗口减半与慢启动重启，全程呈锯齿形](https://upload.wikimedia.org/wikipedia/commons/2/24/TCP_Slow-Start_and_Congestion_Avoidance.svg)

来源：Wikipedia「TCP congestion control」条目（CC BY-SA）。图上两段爬升斜率的差异就是加性增与慢启动的分工，每次回落的垂直落差就是乘性减——「锯齿形窗口在过冲与保守之间震荡」在图上是一条可以直接量斜率的曲线；BBR 要替代的正是这套以丢包为触发信号的轨迹形状，把探测从「等回落」换成「估计带宽与 RTT 的乘积」。

## 缓冲区填满的代价

丢包驱动算法的隐藏前提是"缓冲区溢出才算拥塞"，于是深度缓冲（bufferbloat）链路上窗口持续增长，排队延迟吃掉几百毫秒交互体验。BBR 类延迟驱动算法不依赖填满缓冲，但多流 BBR 之间没有 AIMD 那样的公平收敛保证——BBRv2 引入 ECN 与丢包响应就是补这个洞。选算法不是选快的，是选这条链路上"吞吐-延迟-公平"三角里你要的位置。

## 案例回流与量化锚点：动态调节的实录

量化锚点：拥塞窗口的动态账——丢一个包，经典 Reno 砍半窗口（吞吐瞬间掉一半），BBR 改为按带宽与 RTT 乘积估计主动调节（不依赖丢包信号）；长肥管道（高带宽 × 高 RTT）上 Reno 的窗口恢复要几十个 RTT，一次丢包的吞吐损失以十秒计，这就是跨洋传输与卫星链路换 BBR 的直接理由。缓冲区膨胀（bufferbloat）的账：深缓冲交换机把拥塞藏成排队延迟（丢包不发生、延迟暴涨），延迟敏感流量全被压制——调节的公平维度不只在连接之间，还在吞吐型与延迟型流量之间（CoDel/FQ-CoDel 用排队时长而非丢包做信号）。案例回流的实录：[[工程知识/性能工程：从用户等待到资源瓶颈/存储与网络/端到端网络延迟来自排队、协议、传输与处理.md]] 的排队段是拥塞在延迟侧的形态（队列涨、丢包不涨），[[工程知识/缺陷分析：从个案到体系/稳定性/案例二十三：一次抖动变成一次重算——SASL 拉取没有重试.md]] 是拥塞抖动传导到应用层的下游后果——网络层一次窗口收缩，应用层一次全量重算，跨层的放大链就是要在网络层把抖动压平的理由。

## 验证

验证的锚点：公平性结论要与双流竞争实验对账——预写两条流同起应各自收敛到半带宽，实测其中一条长期独占，与预期对不上。

- 单流 iperf3 -C bbr 对比 cubic：固定 RTT 与丢包率下吞吐曲线与重传数。
- 双流竞争实验：两条流同起，测各自吞吐占比（公平性）与合计吞吐（效率）；AIMD 类应收敛到各半，BBR v1 在深缓冲下可能挤占 cubic 流。
- tc qdisc netem 注入 1% 丢包或 20ms 额外延迟，观察窗口收缩行为与恢复时间。
- 生产验证看重传率、retransmit 与 p99 延迟的相关性：重传率平稳但 p99 飙升，优先怀疑 bufferbloat 而非带宽不足。

## 边界

- 拥塞控制是端到端传输层行为，代理与服务网格的 L7 限流是另一层控制（[[工程知识/后端系统：在并发、失败与变化中维持服务/任务与并发/限流熔断与隔离控制故障半径]]），两者叠加可能双重惩罚。
- 数据中心内 vs 跨互联网：DC 场景 RTT 微小，DCTCP/BBR 的信号粒度设计完全不同，跨公网结论不能直接搬。
- QUIC 把拥塞控制从内核搬到用户态可插拔（RFC 9002 默认沿用类 TCP 算法），服务端可按链路类型逐连接调参，机制差异见 [[工程知识/性能工程：从用户等待到资源瓶颈/存储与网络/Socket与TCP把连接可靠性分成多层]]。

## 相关

- [[工程知识/性能工程：从用户等待到资源瓶颈/存储与网络/端到端网络延迟来自排队、协议、传输与处理]]：端到端时间线里拥塞是排队段的主导者
- [[工程知识/性能工程：从用户等待到资源瓶颈/观测与诊断/排队论给容量一个可推导的模型]]：排队论是拥塞行为的数学骨架，本文是它在传输层的实例
- [[工程知识/后端系统：在并发、失败与变化中维持服务/服务设计/服务发现与负载均衡决定请求发往哪里]]：连接层分流减少单流竞争
