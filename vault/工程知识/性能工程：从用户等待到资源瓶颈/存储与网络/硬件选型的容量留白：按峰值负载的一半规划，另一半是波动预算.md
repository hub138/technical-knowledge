---
title: 硬件选型的容量留白：按峰值负载的一半规划，另一半是波动预算
status: active
type: pattern
updated: 2026-09-23
change_rate: low
confidence: high
review_after: 2029-09-23
tags:
  - hardware
  - performance/storage
  - systems/capacity
sources:
  - "https://www.snia.org/education/storage-networking-primer/capacity-planning"
  - "https://docs.aws.amazon.com/AWSEC2/latest/UserGuide/ebs-volume-types.html"
---

# 硬件选型的容量留白：按峰值负载的一半规划，另一半是波动预算

> **一句话总结**：硬件层的能力数字（带宽、IOPS、容量）是理想条件下的上限，真实负载永远带着波动与衰减——按峰值规划意味着峰值那天系统就到极限，留白是给波动、衰减与超预期增长买的保险。

## 要解决的问题

硬件选型有一类高频事故：**按实测峰值负载恰好选型，上线后不久就在峰值时刻排队**。数字本身没错——峰值 500 MB/s 的负载选 600 MB/s 的盘，账面有余量。问题在"峰值"这个词的语义：记录到的峰值是**过去**的最大值，未来流量、数据量、并发度都会超过它；同时硬件能力在衰减（盘写入放大增长、网卡降速、内存故障降容）。**一个会涨的需求量，对着一个会跌的供给量，按"刚好相等"配置——交叉点必然到来，只是时间问题。**

这类事故在存储上最常见也最隐蔽：SSD 的标称持续写带宽是全新盘的数字，长期使用后 SLC 缓存耗尽、写入放大上升，持续写能力可跌至标称的一半以下——**按标称选型的盘，用一年后峰值时刻开始排队**，而排查时查监控，"磁盘水位 60%"看起来一切正常，瓶颈卡在 IOPS 与带宽的水下部分。

## 机制：留白的三个组成部分

容量留白不是一个拍脑袋的安全系数，由三块各有依据的组成：

**波动留白（最大头）**：负载的日峰周峰与均值差数倍，记录到的峰值还会被突破（业务增长、促销、热点事件）。留白的量按负载分布定：如果监控显示峰值 P99 是均值的三倍，留白至少要让"未来半年的 P99"仍然跑在硬件能力的六成以内——**留白依据是负载分布的尾部，不是均值**。

**衰减留白**：硬件能力随时间下行——SSD 写放大、盘片坏道重映射消耗备用空间、内存 ECC 降容、网卡降速协商。每一项都有一条衰减曲线，选型时按"生命周期末期的剩余能力"算账，按新盘标称算账等于把衰减风险全留给运维。衰减量的权威依据是厂商的耐久指标（TBW、MTBF）与运行经验。

**错误与重试放大**：硬件层故障不是静默的——重映射、重试、降速运行都会放大实际负载（一次坏块写入在物理层可能变成十次操作）。这部分留白比例小但不可省，它是"同样的逻辑负载，硬件压力随老化上升"的来源。

三块留白合起来，经验区间是硬件能力的四到六成作为常态运行水位——这就是"按峰值的一半规划"的量化含义。水位不是浪费：**硬件能力的最后两成是应急资源**，故障转移、数据重建、备份恢复时短暂用到，平时闲着是设计意图。

## 为什么这个直觉反常识

"买大了浪费钱"的账本算错了一边：硬件闲置的成本是线性的（差价），容量不足的成本是阶梯式的——排队超时、SLA 违约、紧急扩容（临时采购 + 迁移 + 停机窗口），一次事故的代价超过全年差价。**留白买的是把阶梯成本变平的可能性**，这笔账在云时代以预留实例与按需实例的差价形式继续成立。

反直觉的另一半在监控口径：水位监控显示 60% 时，工程直觉是"还有四成余量"；实际余量要按"峰值时刻的带宽/IOPS 余量"算——平均水位与峰值水位的差，正是波动留白被吃掉的部分。**按均值监控的系统，看不到自己离峰值的距离。**

## 修复与防线

**选型计算用三行算式**：需求侧（峰值 × 增长系数），供给侧（标称 × 衰减系数），留白 = 供给侧/需求侧 ≥ 1.5——算式进选型文档，每个系数的依据（监控数据、厂商指标）落在注释里，评审有对象。

**峰值水位监控**：监控口径按峰值时刻的硬件利用率（每小时的 P99），告警线设在硬件能力的六成——均值水位监控留给趋势分析，峰值水位才是容量告警的正确口径。

**定期重估**：负载分布每季度重估一次（P99 峰值、增长趋势），衰减按硬件服役时长推算——两个数一除，剩余留白随时可见，扩容决策在留白耗尽前做出而不是在事故后。

**写入负载画像先于选型**：存储选型前先画负载画像（读写比例、顺序随机、块大小、写入总量与 TBW 的比值）——画像决定衰减曲线的形状，重写入负载的衰减留白要加倍。

## 边界

三条边界防止防线走形。

**留白比例按负载性质调整**：批处理类平稳负载留白可以收窄（波动小），在线交易类宽尾部负载留白放宽——1.5 是经验锚点，负载分布才是依据。

**云环境的留白形态变化**：云盘的 IOPS/带宽是购买的配置项，留白表现为"配置档位留一档"，扩容成本从采购周期变成一次配置变更——留白的经济逻辑不变，形态从"买大"变成"买可升"。

**留白不替代瓶颈定位**：容量留白解决"能力不够用"，解决不了"单条路径的瓶颈"（单连接带宽、单队列深度）——留白是容量问题，排队是结构问题，混在一起会误判。

## 验证

可复现的验证路径：

1. 画像验证：选型前跑写入负载画像（fio 按真实负载参数），确认画像与标称场景的差异幅度；
2. 衰减验证：对服役中的盘重跑同一画像，持续写带宽与选型时的数字对比，衰减幅度进入留白预算内即为健康；
3. 峰值验证：监控的每小时 P99 水位长期低于六成，峰值时刻无排队超时；
4. 断言锚点：**选型文档含三行算式与系数依据；峰值水位告警线常驻；剩余留白每季度可读**。

```text
算式 = (标称 × 衰减) / (峰值 × 增长) ≥ 1.5
监控断言 = 小时级 P99 水位 < 60%，有告警线
复审断言 = 季度重估输出剩余留白数字
```

三行算式 + 两个监控口径 + 一次季度重估，把"买大一点"的直觉变成可审计的决策——留白的具体数字可以争论，留白的必要性没有争论空间。

## 相关

- [[工程知识/性能工程：从用户等待到资源瓶颈/存储与网络/存储IO性能取决于访问模式、队列与持久化语义]] —— 负载画像决定衰减曲线的机制背景
- [[工程知识/性能工程：从用户等待到资源瓶颈/观测与诊断/市面Benchmark的设计读法：宏微与中间层基准]] —— 标称数字与实测画像的方法来源
- [[工程知识/后端系统：在并发、失败与变化中维持服务/服务设计/容量估算的通用骨架：从峰值QPS到机器数的推导演练]] —— 需求侧峰值推导的姊妹篇，本篇补供给侧衰减
- [[工程知识/数据系统：在并发与故障中保存事实/可靠性与运维/数据库基准必须描述负载与资源边界]] —— 容量数字的负载边界描述纪律
