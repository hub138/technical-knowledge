---
title: Socket 与 TCP 把连接可靠性分成多层
type: concept
status: active
updated: 2026-09-03
review_after: 2027-09-03
change_rate: low
confidence: high
tags:
  - "systems/network"
  - "tcp"
  - "backend/architecture"
sources:
  - "https://www.rfc-editor.org/rfc/rfc9293"
  - "https://man7.org/linux/man-pages/man7/socket.7.html"
  - "https://www.rfc-editor.org/rfc/rfc9000"
---

# Socket 与 TCP 把连接可靠性分成多层

它解决的是把“连接建立成功”误认为“请求一定完成”的问题。核心机制是把 DNS、连接、TLS、字节流、应用协议和业务提交分层，并在每层定义 deadline、取消和重试；收益是故障定位和恢复边界清晰，代价是状态与观测字段更多。用半开、RST、部分读、连接池耗尽和取消实验验证资源与副作用。

TCP 提供有序、可靠的字节流和拥塞控制，不提供消息边界、业务超时、请求幂等或“对端一定处理了”。Socket 还受 DNS、路由、防火墙、连接池、内核队列和应用读写循环影响。

## 一次请求经过什么

```text
解析/连接 -> SYN/TLS -> send buffer -> 网络/拥塞
           -> receive buffer -> 应用协议解析 -> 业务处理 -> 响应
```

`read` 可能只返回部分字节；EOF、RST、超时和连接复用分别表达不同事实。HTTP keep-alive、HTTP/2 multiplexing、QUIC/HTTP/3 改变连接和队头阻塞边界，但仍需应用 deadline、取消和重试语义。

## 性能与故障

RTT、拥塞窗口、带宽、丢包、TLS、Nagle、队列和服务器处理时间共同决定延迟。连接建立成功不代表服务健康；半开连接、连接池耗尽、端口耗尽和代理 idle timeout 会制造间歇性错误。

## 可运行示例：一次请求的分层故障定位

"客户端偶尔超时"这一个症状，四层各看一个指标：

```text
应用层（Socket 缓冲区）：
  SO_RCVBUF 满套 → 应用读得慢，内核缓冲积压 → 超时
  看：应用处理延迟 P99（网络正常时也超时 → 先查这里）

TCP 层（重传与拥塞）：
  丢包 → 重传（RTO 1s 起，指数退避） → 该请求延迟+1s 的整数倍
  看：ss -ti 的 retrans 计数、`netstat -s` 的重传段数

内核队列层（连接建立与接收）：
  SYN 队列 / accept 队列溢出 → 握手被丢 → 连接超时（不是读超时）
  看：`netstat -s` 的 "listen queue overflow" 计数

链路层（网卡与缓冲区）：
  环形缓冲区溢出 → 丢包（TCP 层看到的"丢包"源头在这）
  看：ethtool -S 的 rx_dropped / rx_fifo_errors

定位顺序自下而上：链路丢包 → TCP 重传 → 队列溢出 → 应用读慢，
每一层都有专属计数器，症状"超时"在四层是四个不同的病。
```

跑一遍的收获：分层模型的价值在故障定位时兑现——"偶尔超时"不查分层就是玄学，逐层计数器对账后每次都指向唯一一层。分层同时意味着责任分层：应用层调缓冲区参数救不了链路丢包，链路换网卡救不了应用读得慢。

## 量化锚点与案例回流

延迟的量级账：同机房 RTT 百微秒级，一次丢包重传最少加 1 秒（RTO 初值）——丢包率千分之一、每秒万请求的负载下每秒就有十次重传级延迟，P99 的毛刺大多从这里来。队列的量级账：accept 队列溢出计数在清零后再次增长是扩容信号（somaxconn 调大只是推迟），SYN 溢出在公网入口常伴 SYN flood。案例回流：分层定位的实录见 [[工程知识/性能工程：从用户等待到资源瓶颈/存储与网络/网卡丢包先看环形缓冲区溢出：队列、中断与软中断的接力.md]]（链路层的接力链）与 [[工程知识/性能工程：从用户等待到资源瓶颈/存储与网络/连接建立失败先看内核队列：SYN 队列与 accept 队列是两个瓶颈.md]]（内核队列层）；TIME_WAIT 的收尾账见 [[工程知识/性能工程：从用户等待到资源瓶颈/存储与网络/TIME_WAIT是连接收尾的代价与设计.md]]。

## 验证

验证的锚点：连接各段耗时要与注入对照对账——预写注入丢包与延迟后变化应集中在传输段，实测变化出现在处理段，与预期对不上。

用 trace 区分 DNS、connect、TLS、request write、server queue、TTFB 和 body read；注入丢包、延迟、半开、RST、连接复用和服务端超时。验证重试不会重复副作用，长响应取消会释放 socket 和服务端工作。

关系：[[工程知识/后端系统：在并发、失败与变化中维持服务/服务设计/HTTP请求穿过浏览器、网络与服务端边界]] · [[工程知识/后端系统：在并发、失败与变化中维持服务/分布式可靠性/超时、重试与幂等共同定义调用语义]] · [[工程知识/性能工程：从用户等待到资源瓶颈/存储与网络/TLS握手与证书链决定连接建立成本]] · [[工程知识/性能工程：从用户等待到资源瓶颈/存储与网络/TIME_WAIT是连接收尾的代价与设计]] · [[工程知识/性能工程：从用户等待到资源瓶颈/存储与网络/RDMA绕过内核但要付出可运维性代价]]
- [[工程知识/性能工程：从用户等待到资源瓶颈/操作系统与运行时/io_uring与异步IO演进：系统调用省下的开销.md]] —— epoll 复用等待，io_uring 再把提交收割批量化

## 要解决的问题

连接建立成功被当成请求已经完成，超时出现时无法判断问题位于 DNS、TCP 握手、内核队列、链路丢包还是应用读取过慢，重试因此作用在错误的一层上，副作用还可能被重复执行。本篇回答：一次请求依次经过哪些层、每层的可靠性保证到哪里结束、同一个超时症状在四层分别对应哪个计数器、deadline 与取消由哪一层定义语义、以及分层带来的责任边界位于哪里。

