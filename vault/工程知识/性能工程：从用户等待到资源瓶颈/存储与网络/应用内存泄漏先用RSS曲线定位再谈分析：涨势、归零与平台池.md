---
title: 应用内存泄漏先用 RSS 曲线定位再谈分析：涨势、归零与平台池
status: active
type: pattern
updated: 2026-09-23
change_rate: low
confidence: high
review_after: 2029-09-23
tags:
  - os
  - observability
  - defect-analysis
sources:
  - "https://man7.org/linux/man-pages/man1/pmap.1.html"
  - "https://www.brendangregg.com/linuxperf.html"
---

# 应用内存泄漏先用 RSS 曲线定位再谈分析：涨势、归零与平台池

> **一句话总结**：内存泄漏的判定与定位分三步——RSS 曲线确认"真在涨"（区分缓存的平台期与泄漏的斜率）、回落实验确认"泄漏点在哪类分配"（重启归零 + 分模块灰度）、按分配路径选分析工具（运行时分配器缓存是 RSS 不降的主因， jemalloc/tcmalloc 的池开关是第一道验证）；跳过曲线直接上 profiler，分析的往往不是泄漏。

## 要解决的问题

"内存一直涨"的报告里，相当一部分最终归类为"缓存增长，符合设计"——RSS 曲线的平台期（缓存填满后稳定）与泄漏的斜率（永不回头）需要区分。本库资源与容量域"案例六"给过连接泄漏与 SST 堆积的个案（应用层泄漏的形态）；本篇给**判定与定位的通用流程**：先确认泄漏存在（曲线形态），再定位泄漏模块（回落实验），再选对分析工具（分配路径）——三步各有明确的判据与动作，避免"内存涨了就 panic 加内存"或"直接上 profiler 分析半天缓存"。

## 机制：三步判定与定位

**第一步：RSS 曲线确认涨势**。监控面板上看 RSS 的长期曲线（天级以上窗口）——**泄漏的特征**：阶梯状上升（每次流量事件上一级台阶后不回落）、斜率与请求量正相关、OOM 前的加速（换页压力）。**非泄漏的形态**：缓存的平台期（启动后爬升到一个高位持平——本地缓存、页缓存映射）、锯齿（周期性 GC 回落，JVM/Go 运行时的正常呼吸）。**判定的量化标准**：连续 3 天的单调上升 + 阶梯不回落 = 泄漏嫌疑成立；平台期后的波动 = 缓存，转容量评估。

**第二步：回落实验定位模块**。确认涨势后做两类实验：**重启归零验证**（重启后 RSS 回到基线——确认是进程内泄漏，排除系统层假象如页缓存）；**分模块灰度**（按功能模块开关灰度流量：A 模块开启的实例曲线涨、关闭的不涨——泄漏点定位到模块）。**灰度实验的设计要点**：两组实例同配置同流量模型（流量不均会把非泄漏模块误判成嫌疑）、观察窗口覆盖泄漏的触发路径（有的泄漏只在特定请求类型触发，均匀灰度覆盖不到）。**案例六的连接泄漏**在此步的特征：泄漏量与请求量线性相关、关闭连接池复用后加速——分配路径的线索在灰度阶段就已出现。

**第三步：按分配路径选工具**。定位到模块后，按运行时选分析手段：

| 运行时 | 第一工具 | 看什么 |
| --- | --- | --- |
| C/C++ | valgrind massif / jemalloc profile | 分配调用栈的驻留量排名 |
| Go | pprof heap（alloc_space vs inuse_space） | inuse 不降的分配栈 |
| JVM | jmap histo + MAT 支配树 | 对象实例数的增长类 |
| Python | tracemalloc / objgraph | 快照 diff 的增长对象 |

**分析的第一道验证是分配器开关**：glibc malloc 的 arena 碎片、jemalloc 的 dirty page 驻留，都会让 RSS 不降而分配器视角的"泄漏量"远小于 RSS——**`MALLOC_ARENA_MAX=2`、jemalloc 的 `dirty_decay_ms` 调低后的曲线变化**是判断"真泄漏 vs 分配器驻留"的快速实验：开关调参后 RSS 曲线趋平 = 驻留问题（调参解决），继续涨 = 真泄漏（进 profile 分析）。

## 看完能判断：三类误判的排除

**误判一：页缓存算进进程**。`free -h` 的 available 与 buff/cache 的分界——应用读取大文件后页缓存增长，`top` 的 RES 含文件映射部分——**排除动作**：`pmap -x <pid>` 按段看（匿名内存 vs 文件映射），泄漏只在匿名段（[anon]）。

**误判二：监控口径混乱**。RSS、PSS、VSZ 三个口径混用（VSZ 是虚拟地址空间，涨是正常的；PSS 是按共享折算的真实占用）——**判定的统一口径用 PSS 或 RSS 单一化**，监控与告警同口径，换口径的对比全部作废。

**误判三：把 GC 的堆外泄漏当堆内**。JVM 的 DirectByteBuffer、Go 的 cgo 分配不在堆统计里——**堆曲线平稳 + RSS 持续涨 = 堆外嫌疑**，分析对象转向 native 分配（pmap 的 anon 段增长 + valgrind/jemalloc profile）。

**三步流程的顺序不可倒**：跳过曲线确认直接 profiler（分析的可能是缓存）、跳过灰度直接全局分析（堆栈排名被正常流量淹没）——**每一步都为下一步缩小范围**，范围不缩的工具调用是排障时间的黑洞。

## 边界

三条边界防止防线走形。

**泄漏的修复优先于分析完备**：灰度定位到模块后，先做止损（该模块限流或降级）再深挖调用栈——**止损的收益是立即封住风险敞口，代价是功能暂时降级**——**泄漏在产线的每一小时都是风险敞口**，分析完备性给止血让路。

**压测复现要覆盖触发路径**：泄漏往往只在异常路径触发（超时重试、错误分支的分配未释放）——**正常路径压测不复现就加故障注入**（本库健康检查篇的注入思路同源），异常注入后的曲线变化才是泄漏复现的正确姿势。

**驻留调参不是泄漏修复**：arena 与 decay 参数压低 RSS 驻留，分配失控的代码该漏照漏（只是泄漏得更慢）——**调参是缓冲，修复要回到分配调用栈**，把"调参后趋平"当修复完成是复发型事故的起点。

## 验证

可复现的验证路径：

1. 形态判别复现：分别跑缓存型（限容 LRU）与泄漏型（map 只进不出）负载，预期 RSS 曲线呈平台期与阶梯上升两种形态，判定法可区分；
2. 灰度定位复现：两实例同流量不同模块开关，预期涨势随开关分布，定位结论与注入点一致；
3. 驻留实验复现：glibc 多线程高分配场景下设 `MALLOC_ARENA_MAX=2`，预期 RSS 显著下降且曲线趋平；
4. 断言锚点：**RSS 长期曲线在监控（天级窗口）、PSS/RSS 口径统一、分配器驻留的快速实验进排障手册**——三件齐备，"内存一直涨"从焦虑事件变成三步走完的判定流程。

```text
曲线断言 = 平台期与斜率先区分
灰度断言 = 模块开关定位分配源
驻留断言 = 分配器参数先排除再分析
```

内存泄漏的排查效率取决于顺序：曲线确认存在、灰度定位模块、工具分析调用栈、分配器实验排除驻留——每步为下一步收窄范围，顺序对了，泄漏定位的常规耗时从数天缩到数小时。

## 相关

- [[工程知识/缺陷分析：从个案到体系/资源与容量/案例六：容量与泄漏——连接泄漏和 SST 堆积.md]] —— 应用层泄漏的个案形态
- [[工程知识/性能工程：从用户等待到资源瓶颈/操作系统与运行时/虚拟内存与文件系统把地址和持久化分层.md]] —— RSS/PSS/页缓存的机制层
- [[工程知识/性能工程：从用户等待到资源瓶颈/存储与网络/df 与 du 的差值就是被占住的已删文件：磁盘占用异常的三个来源.md]] —— 存储侧的同类差值排障
- [[工程知识/性能工程：从用户等待到资源瓶颈/观测与诊断/基准报告要绑定机器画像：没有环境描述的数字不可比较.md]] —— 口径统一的同类纪律
