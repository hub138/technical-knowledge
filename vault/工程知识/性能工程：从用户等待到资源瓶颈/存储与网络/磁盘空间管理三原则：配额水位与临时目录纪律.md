---
title: 磁盘空间管理三原则：配额水位与临时目录纪律
status: active
type: pattern
updated: 2026-09-23
change_rate: low
confidence: high
review_after: 2029-09-23
tags:
  - hardware
  - resource-capacity
  - operations
sources:
  - "https://man7.org/linux/man-pages/man8/df.8.html"
  - "https://man7.org/linux/man-pages/man8/du.8.html"
  - "https://man7.org/linux/man-pages/man5/tmpfiles.d.5.html"
---

# 磁盘空间管理三原则：配额水位与临时目录纪律

> **要点**：磁盘写满的事故链是"无配额的写入者 × 无水位的监控 × 无纪律的临时目录"，三原则在写满发生前把三个环节各自管住——空间是共享资源，共享资源不设边界必然耗尽。

## 要解决的问题

磁盘空间是运维事故里形态最经典的一类：**平时无事，某个写入者失控后全机共沉沦**。本库稳定性域"案例一：垃圾回收线程的终止——BookKeeper 磁盘写满"已解剖过写满后的故障传导（日志写不进、数据库崩溃、服务雪崩）；本篇把防线前移，回答"怎么让磁盘写满不发生"这个运维普遍面临的问题——三条原则各管一个环节：**配额管写入者的边界，水位管空间的可见性，临时目录纪律管最容易失控的那类写入**。本库缺陷分析域"资源泄漏的三段式排查法"解决泄漏类缺陷的排查，本篇解决存储空间这一特定资源的系统性治理，两篇共同覆盖"资源被悄悄吃光"的故障家族。

## 机制：三个环节的失效形态

**无配额的写入者**：一台机器上跑着日志、数据库、应用缓存、任务产物等多种写入，任何一种失控（日志轮转配置丢失、任务产物堆积、调试输出未关）都会吃掉别人的空间。失控的放大器是**共享文件系统的"先到先得"**：没有边界时，写得快的进程获胜，写日志的把数据库的空间挤掉，故障从"某个组件失控"升级为"全机不可用"。配额（用户级 quota、目录级限额、容器存储限制、日志轮转上限）把每个写入者的最大占用显式化，**失控被限制在配额内，不外溢**。

**无水位的监控**：磁盘从 80% 到 100% 可以只差一次大文件写入（数据库大事务、全量备份、日志风暴），按天看水位的监控在几小时内错过整个窗口。水位的两个口径都要有：**使用率（df 的容量水位）与 inode 水位**——海量小文件（会话文件、任务产物）先把 inode 耗尽，容量看着还有余量，写入照样失败，df 正常而 touch 报错的形态就是 inode 耗尽。

**无纪律的临时目录**：临时文件是磁盘事故的高发源头，失效形态三种——异常退出留下的孤儿临时文件（清理逻辑没跑到）、"临时"文件从未被清理（写入时没设计清理路径）、清理周期远长于产生周期（产生快于清理，净增长为正）。**临时目录的失控概率与其"无主"程度成正比**：谁都能写、没人负责清理的目录，必然走向膨胀。

## 修复与防线

**写入者配额化**：日志按轮转策略限量（logrotate 的 size 上限 + 保留份数）、数据库按表空间配额、容器按存储限制、共享用户按磁盘 quota——**每个长期写入者都有声明的上限**，上限值按"该写入者合理的最大空间"定，超限即该写入者自己的问题，告警指向它而不是全机。

**双水位监控**：容量与 inode 两条水位线（80% 警告、90% 严重）进监控告警；大文件写入密集的机器加"增速"监控（小时级增量），增速异常先于水位到达报警——水位管余量，增速管趋势，趋势告警比水位告警早数小时。

**临时目录三纪律**：写入时用带唯一名的文件并在 finally/退出钩子里清理（孤儿文件的源头治理）；目录按 tmpfiles.d 或等价机制定期清理（清理机制与进程存续解耦）；**超大临时文件不进默认临时目录**（进专门的带配额与监控的大文件区）——三条让"临时"名副其实。

**写满演练**：定期在测试环境填满磁盘（fallocate 大文件），核对告警按预期触发、关键服务按降级路径运行——写满的预案（本库稳定性域"磁盘写满的连锁反应"）没演练过就只是文档。

## 边界

三条边界防止防线走形。

**配额不是越紧越好**：数据库、消息队列等组件需要空间弹性（峰值与回收周期），配额按其峰值画像定并留余量，过紧的配额把组件的正常增长变成故障——配额的依据是写入者的画像，统一系数会给慢写入者松、快写入者紧。

**清理不能只靠 cron**：定时清理与写入侧清理钩子是互补的两层——只有 cron，异常退出产生的孤儿文件在两次清理之间堆积且可能被后续同名写入掩盖；只有写入侧钩子，进程被 kill -9 时钩子不会执行。**两层都要**。

**空间问题有非磁盘形态**：tmpfs 的"磁盘"是内存（/tmp 挂 tmpfs 的机器写满 /tmp 等于吃光内存）、overlayfs 的配额在镜像层——不同文件系统形态的空间语义不同，治理规则按挂载形态核对。

## 验证

可复现的验证路径：

1. 配额验证：给测试写入者配额后制造超限写入，预期该写入者报错受控（fail-fast）、同机其他写入不受影响；
2. 双水位验证：fallocate 填满测试分区，容量告警触发；用 stress 或循环创建小文件耗尽 inode，inode 告警触发而容量告警不触发——两条水位各自独立生效；
3. 孤儿文件验证：kill -9 一个写临时文件的进程，临时目录按 tmpfiles.d 周期清理后孤儿文件消失；
4. 断言锚点：**每个长期写入者有配额声明、双水位告警常驻、临时目录有周期清理机制**；三者齐备后写满类事故的复发面收敛到"未配额的新写入者"，新写入者上线时的配额评审堵住最后的口子。

```text
配额断言 = 超限写入者受控，他人不受影响
水位断言 = 容量与 inode 告警各自独立触发
清理断言 = 孤儿文件在周期内消失
```

空间治理的三原则把"磁盘写满"从随机事故变成有边界、有可见性、有纪律的受管资源——事故不发生，排查链（本库稳定性域）就永远只是预案。验证的预期对照提前写明：配额超限时预期写入者自身受控报错而同机他人不受影响，对不上（他人空间被挤占）即配额没有作用在正确的层级；双水位的注入演练预期两类告警各自独立触发，对不上（inode 耗尽无告警）即监控只看了容量口径，这是最常见的漏报形态。治理的收益是写满类事故的复发面收敛，代价是每个写入者的配额评审与水位告警的维护投入，两者都在上线清单里显式核算。

## 相关

- [[工程知识/缺陷分析：从个案到体系/稳定性/案例一：垃圾回收线程的终止——BookKeeper 磁盘写满]] —— 故障传导链的实例解剖，本篇是它的前置防线
- [[工程知识/缺陷分析：从个案到体系/资源与容量/资源泄漏的三段式排查法：预筛确认与归因]] —— 泄漏类缺陷的排查方法，与本篇的治理规则互补
- [[工程知识/软件构建：让变化可以理解、验证与交付/开发工具/词汇即接口：Linux与容器命令的词源与设计]] —— df/du/logrotate 等命令的设计语义
- [[工程知识/后端系统：在并发、失败与变化中维持服务/任务与并发/后台任务的守护链：进程托管与会话脱离是可靠性的底线配置]] —— 长期任务的日志归属与轮转配置
