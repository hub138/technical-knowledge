---
title: fd 泄漏先用 lsof 对账：每类套接字都有名有姓才算干净
status: active
type: mechanism
updated: 2026-09-23
change_rate: low
confidence: high
review_after: 2029-09-23
tags:
  - os
  - operating-systems/process
  - defect-analysis
sources:
  - "https://man7.org/linux/man-pages/man8/lsof.8.html"
  - "https://man7.org/linux/man-pages/man2/fcntl.2.html"
---

# fd 泄漏先用 lsof 对账：每类套接字都有名有姓才算干净

> **要点**：文件描述符是进程对内核对象的引用票据，泄漏的形态是"票据发出去了没人还"——`too many open files` 报错是终场哨，判定要在哨响之前做：`lsof -p` 按类型对账（socket、文件、epoll、eventfd 各多少、归谁），涨势与业务量对不上的类型就是嫌疑人；fd 上限要用软硬两层设置，告警挂在软限的七成。

## 要解决的问题：涨势对不上业务的 fd 就是泄漏

fd 泄漏的经典场景：连接池的连接借出不还、日志句柄滚动后旧句柄没关、每请求新开 socket 不关闭、epoll fd 与 eventfd 在热更新路径上累积。**泄漏与高占用的区分**：fd 用量高但随业务量等比波动（高峰高、低谷回落）是正常；用量单调上涨且低谷不回落，涨势与业务量对不上——这就是泄漏，涨到软限报 EMFILE，进程丧失一切需要 fd 的能力（建连、读文件、收信号管道）。本库"资源与容量"域讲过连接泄漏的个案；本篇给 fd 维度的**通用对账方法**——不定位到具体泄漏点也能先圈定类型与涨速，为代码定位缩小范围。

## 机制：对账、上限与告警三层

**第一层：lsof 按类型对账**。`lsof -p <pid>` 列出进程全部 fd，按 TYPE 分组统计：IPv4/IPv6 socket（连接类，对应连接池与客户端）、REG（文件类，日志与数据文件）、unix socket、epoll/eventfd/pipe（内核机制对象）。**对账的预期**：每类的数量能对上业务事实——socket 数 ≈ 连接池大小 × 目标数 + 活跃请求，REG 数 ≈ 日志文件数（滚动的旧文件如果还开着，就是日志句柄泄漏的特征：同名日志多个 fd 指向已滚走的 inode），epoll/eventfd 数应恒定（每线程或每 loop 一个，随重启次数上涨就是热更新泄漏）。**对不上账的类型就是嫌疑人**，进一步用 `ss -tanp` 看socket 状态分布（CLOSE_WAIT 堆积 = 对端关了我没关，读路径忘了处理关闭事件）。

**第二层：涨速与上限的账**。抓两个时间点的 fd 数（间隔小时级），算出涨速（个/小时），与软限（ulimit -n）对比得出"还剩多少小时"——这个账决定处置节奏：涨速低（天级耗尽）走修复排期，涨速高（小时级耗尽）先重启止血再修。**上限的设置**：systemd 的 `LimitNOFILE`、容器的 `ulimits`、进程内 setrlimit 三处要一致，软限（告警线）与硬限（天花板）分层——软硬同值会让"先报错先降级"失去空间，软限设为预期的 2 倍余量、硬限再翻一倍是常见起点。

**第三层：告警挂在趋势上**。fd 数进指标（进程级 gauge），告警两条：绝对值超软限七成（预警）与线性上涨趋势（连涨 N 个采样点且低谷不回落，确诊级）——绝对值告警对慢泄漏不敏感（从 1 万涨到 1.2 万不触发阈值），**趋势告警抓的是"该回落时没回落"**。容器环境的 fd 计入 cgroup 的 files 维度，节点级与容器级双层盯。

## 看完能判断：泄漏嫌疑的定位路径

**症状"EMFILE 报错"**：先 `ls /proc/<pid>/fd | wc -l` 确认用量顶到限，再 lsof 分类型——socket 型大头（CLOSE_WAIT 多）查连接关闭路径（HTTP 客户端连接是否 `release` 回池、响应体是否读完才归还）；REG 型大头且多个 fd 指向同一文件的旧 inode，查日志库的滚动配置（滚动后旧句柄未重开）；epoll/eventfd 型随部署次数上涨，查热加载路径。**三问定位法**：一问类型分布（哪类在涨）、二问状态分布（socket 卡在哪个状态）、三问增长拐点（涨势从哪个版本/部署开始——`ls -l /proc/<pid>/fd` 的时间戳与版本发布时间对齐）。三问的答案组合直接指向代码位置，修复后用同样三问验证回落。

**修复的验证**：压测回放（按产线峰值的请求形态跑 24 小时），fd 曲线预期随负载波动且低谷回落到基线——**回落的低谷值**是泄漏修复的验收线（修复前低谷单调上抬，修复后低谷恒定）。

## 收益与代价

fd 对账的收益是泄漏在耗尽前数天到数周被趋势告警接住（EMFILE 的全面瘫痪变成计划内重启与修复），代价是三处上限配置与指标告警的初始建设（一次性）与 lsof 对账的排障时间（每次泄漏半小时级）；不做的代价不对称——一次 EMFILE 的损失（服务不可用时长 × 修复定位时间）远超数年的建设成本。

## 边界

三条边界防止防线走形。

**fd 上限不是泄漏的解药**：调大上限（65535 → 100 万）只是延后耗尽时刻，泄漏照旧——上限按真实需求定（连接规模 × 安全系数），调大后内存与内核对象成本随之上涨，**用调大上限掩盖泄漏**是把故障从今天推到资源耗尽的某天。

**fd 泄漏不止 socket**：文件、epoll、inotify、timerfd 都在票据系统里——对账要覆盖全部类型，只盯连接数会漏掉 inotify watch 泄漏这类"监控自己的代码把自己拖垮"的形态。

**子进程继承 fd 要显式防**：fork/exec 的子进程默认继承全部 fd（close-on-exec 标志没开的），后台任务的守护链（本库"后台任务的守护链"）里父进程的 fd 会被一堆子进程共同持有——**CLOACK/CLOEXEC 纪律**在创建 fd 的代码里统一开，审计用 `lsof` 看子进程列表与 fd 复用情况。

## 验证

可复现的验证路径：

1. 泄漏复现：写一个"每请求新开 socket 不关"的小服务，压测下观察 fd 曲线单调上涨、EMFILE 触发、lsof 分类中 socket 型独大——复现即验证判定法；
2. 对账验证：连接池服务正常态的 lsof 分类统计与配置对账（socket 数 ≈ 池大小 × 目标数），数字对上说明对账方法可用；
3. 告警验证：注入 fd 缓慢上涨（每分钟 +10），预期趋势告警先于 EMFILE 触发；
4. 断言锚点：**fd 数有指标与趋势告警、软硬上限分层且三处设置一致、泄漏处置有 lsof 对账路径**——三件齐备，fd 从"报错了才知道"变成"涨势不对就有人管"。

```text
对账断言 = 每类fd有名有姓对上业务
上限断言 = 软硬分层三处一致
趋势断言 = 告警抓该回落而未回落的低谷
```

fd 泄漏的治理是一套对账术：lsof 把每张票据的名字念出来，涨速与上限算出剩余时间，趋势告警在耗尽之前拉响——`too many open files` 从突然的全面瘫痪，变成三问定位路径上的一个已知章节。

## 相关


- [[工程知识/缺陷分析：从个案到体系/资源与容量/案例六：容量与泄漏——连接泄漏和 SST 堆积.md]] —— 连接泄漏的个案层
- [[工程知识/后端系统：在并发、失败与变化中维持服务/任务与并发/后台任务的守护链：进程托管与会话脱离是可靠性的底线配置.md]] —— 子进程 fd 继承的守护链
- [[工程知识/性能工程：从用户等待到资源瓶颈/操作系统与运行时/进程线程与系统调用构成执行边界.md]] —— fd 所在的系统调用边界
- [[工程知识/软件构建：让变化可以理解、验证与交付/开发工具/Linux调查先收集事实再改变状态.md]] —— 先看事实再动手的纪律
- [[工程知识/性能工程：从用户等待到资源瓶颈/存储与网络/应用内存泄漏先用RSS曲线定位再谈分析：涨势、归零与平台池.md]] —— 泄漏排查的另一半：RSS 曲线的平台期（缓存填满后稳定）与泄漏的斜率（永不回头）要分开看，先定性再谈分析
