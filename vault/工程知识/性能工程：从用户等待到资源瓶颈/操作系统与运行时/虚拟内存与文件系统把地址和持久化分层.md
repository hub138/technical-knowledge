---
title: 虚拟内存与文件系统把地址和持久化分层
type: concept
status: active
updated: 2026-09-03
review_after: 2027-09-03
change_rate: low
confidence: high
tags:
  - systems/memory
  - systems/filesystems
  - linux
sources:
  - "https://docs.kernel.org/admin-guide/mm/concepts.html"
  - "https://man7.org/linux/man-pages/man2/mmap.2.html"
  - "https://man7.org/linux/man-pages/man2/fsync.2.html"
---

# 虚拟内存与文件系统把地址和持久化分层

它解决的是应用地址、物理内存、文件缓存和持久化被混为一谈，导致错误的容量和崩溃恢复判断。核心机制是页表、page cache、脏页回写和 fsync 分别管理映射、缓存与耐久性；收益是隔离和吞吐，代价是缺页、回写尖峰和复杂恢复语义。用冷/热缓存、磁盘满、重启和断电模拟验证可见性与恢复。

进程看到的是虚拟地址空间，内核通过页表把它映射到物理页、文件映射或共享页。文件系统把文件名映射到 inode、块和缓存；`write` 返回只说明数据进入内核路径，不等于已经持久化到设备。

## 性能与正确性

- page fault、TLB miss、内存回收和 swap 会把 CPU 计算变成等待。
- page cache 让文件读写看似很快，但脏页回写和 `fsync` 会产生延迟尖刺。
- `mmap` 共享地址空间和文件页，减少拷贝但引入缺页、截断、并发和错误处理边界。
- `fsync`、目录同步、文件系统和设备缓存共同决定崩溃后哪些写入可见；应用不能只看返回码。

## 常见错误

把 RSS 当作“真实使用内存”、把 page cache 当作泄漏、把 `close` 当作持久化、把磁盘写入吞吐当作 fsync 延迟，都会导致错误容量和恢复判断。

## 最小映射实验

对同一文件分别使用 buffered I/O、`mmap` 和 direct/sync 写入，先测冷缓存再测热缓存，并在写入返回后模拟进程崩溃或重启。比较 page fault、dirty/writeback、fsync 延迟、可见数据和恢复结果，明确每种路径提供的是缓存性能还是持久性保证。

## 可运行示例：一次缺页的完整旅程

用 `/proc` 的缺页计数器看虚拟内存的真实动作：

```bash
grep -E "min_flt|maj_flt" /proc/self/status
# 次缺页（min_flt）：页在内存，只补页表——微秒级
# 主缺页（maj_flt）：数据在磁盘，要发起 IO——毫秒级，差三个数量级

# 观察一次顺序读的缺页过程
dd if=/path/to/bigfile of=/dev/null bs=1M count=100
# 回收页缓存后再读（测试机专用，生产禁用）：echo 3 > /proc/sys/vm/drop_caches
```

mmap 与 read 的分野在同一台机器上可复现：mmap 随机读命中页缓存时省去拷贝（次缺页代价），未命中时每个缺页一次 IO（主缺页代价）；read 系统调用批量预读（默认 128KB），顺序场景反而更稳。差异的根源就是"地址空间"与"文件缓存"合流的那个页。

## 量化锚点：页大小与 TLB

x86-64 默认 4KB 页，1GB 数据 = 26 万个页表项；2MB 大页把条目压到 512 个，TLB 命中率敏感的负载（大内存随机访问）用大页可省 10-30% 的访问时间。代价是分配与回收的粒度变粗——数据库类常驻进程开透明大页（THP）反而引入延迟毛刺（khugepaged 的整理停顿），禁用 THP 是数据库部署清单的常规项，`/sys/kernel/mm/transparent_hugepage/enabled` 一查便知。

## 验证

验证的锚点：分层结论要与冷热对照对账——预写首次访问应出现主缺页、二次访问命中缓存，实测页错误数与预期对不上就说明换入换出发生在别的层。

观察 minor/major faults、RSS/匿名页/缓存页、dirty/writeback、IO wait、fd 和文件系统队列；分别测试冷/热缓存、随机/顺序、同步/异步写、磁盘满、重启和断电模拟。数据库和 RAG 索引的持久化、快照和恢复必须基于这些语义。

关系：[[工程知识/性能工程：从用户等待到资源瓶颈/存储与网络/存储IO性能取决于访问模式、队列与持久化语义]] · [[工程知识/数据系统：在并发与故障中保存事实/可靠性与运维/备份恢复是可验证的时间边界]]

## 要解决的问题

应用看到的地址、物理内存、文件缓存与持久化存储被混为一谈，容量评估与崩溃恢复的结论因此与实际情况不符。本篇回答：页表、文件缓存、脏页回写与持久化刷写各自管理什么、写入之后数据在什么条件下才算持久、缺页与回写尖峰为什么会出现在延迟曲线上，以及验证可见性与恢复要构造哪些场景。

## 相关

- [[工程知识/性能工程：从用户等待到资源瓶颈/操作系统与运行时/零拷贝把数据通路的CPU让给业务|零拷贝把数据通路的CPU让给业务]]——讲 sendfile/mmap 一系如何消掉通路上的重复搬运，是页缓存角色的下游
- [[工程知识/性能工程：从用户等待到资源瓶颈/处理器与内存/大页与TLB：地址翻译的隐藏成本.md]] —— 大页是页表粒度的配置维度，翻译成本在这里专门展开
