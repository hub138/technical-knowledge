---
title: 脏页回写在性能与崩溃丢失窗口间权衡
type: concept
status: active
updated: 2026-09-22
confidence: high
change_rate: low
review_after: 2029-09-22
tags:
  - linux/io
  - operating-systems/process
  - performance/storage
  - storage
sources:
  - "https://www.kernel.org/doc/html/latest/admin-guide/mm/watermark.html"
  - "https://www.kernel.org/doc/Documentation/sysctl/vm.txt"
  - "https://lwn.net/Articles/572916/"
---

# 脏页回写在性能与崩溃丢失窗口间权衡

写文件 `write(2)` 返回成功不代表数据在盘上，只代表进了页缓存（page cache，内核把磁盘内容缓存在内存里的机制）。要解决的问题：什么时候把这些脏页（已修改未落盘的缓存页）真正写回磁盘，这个决策同时决定写吞吐与断电丢失多少数据，两个目标方向相反。

## 机制：回写触发条件与参数表

回写由后台线程（writeback 线程）按三条触发线执行：

| 触发线 | 默认参数 | 含义 |
| --- | --- | --- |
| 脏页比例 | vm.dirty_ratio（20） | 脏页占内存超 20%，进程写被阻塞强制回写 |
| 时间上限 | vm.dirty_expire_centisecs（3000，即 30 秒） | 脏页最长停留 30 秒后必回写 |
| 周期扫描 | vm.dirty_writeback_centisecs（500，5 秒） | 后台线程每 5 秒醒来巡检 |

权衡的三种典型调法：数据库与消息队列这类自己管理落盘的软件倾向 `dirty_ratio` 调小、缩短窗口（它们自己 fsync，内核回写越少干预越好）；大文件批处理倾向调大窗口攒顺序写（吞吐优先，接受更大丢失窗口）；默认值是两端的折中。收益与代价的换算关系：窗口越大写吞吐越高（攒出大块顺序 IO），但断电时丢掉的最近写入越多——这不是可调优的矛盾，是必须选择的取舍。

fsync 的角色：应用层强制把指定文件的脏页与元数据推到盘上，是数据库 WAL（预写日志）可靠性的基石。fsync 成功返回后，崩溃不丢该数据；代价是绕开页缓存优化、每次都真实落盘，IOPS 数量级下降。什么场景必须 fsync：数据丢失不可接受的写入（事务日志、元数据变更）；什么场景可以不 fsync：可重新生成的数据（缓存、临时文件、可重算的中间产物）。

## 验证

```bash
# 观察脏页水位与回写速率
grep -E 'Dirty|Writeback' /proc/meminfo
# 攒脏页观察回写行为（写 4GB 不 fsync）
dd if=/dev/zero of=/tmp/bigfile bs=1M count=4096 &
watch -n1 'grep -E "Dirty|Writeback" /proc/meminfo'
# 对比 fsync 成本：攒批 fsync 与每块 fsync
python3 -c "
import os, time
f = open('/tmp/fsynctest', 'wb')
t0 = time.time()
for i in range(1000): f.write(b'x' * 4096); os.fsync(f.fileno())
print('per-block fsync:', time.time() - t0)"
```
预期：只写不 fsync 时 Dirty 快速上升、按回写参数回落；每块 fsync 的耗时比攒批高一到两个数量级。数字对不上时先确认盘是不是带易失写缓存的（`hdparm -W` 查看写缓存状态），硬件层缓存会让 fsync 的保证打折。内核文档 watermark 与 vm.txt 是这些参数语义的权威来源。

## 边界

本篇讲 OS 页缓存层的回写决策。文件系统在崩溃时的元数据一致性（journal、copy-on-write 两类方案）见 [[工程知识/数据系统：在并发与故障中保存事实/事务与存储/崩溃一致性给文件系统补上POSIX未定义部分|崩溃一致性给文件系统补上POSIX未定义部分]]，该篇讲的是 fsync 之上的应用层事务问题。存储介质层的写入代价（SSD 先擦后写）见 [[工程知识/性能工程：从用户等待到资源瓶颈/存储与网络/SSD的写放大来自介质要先擦后写|SSD的写放大来自介质要先擦后写]]。页缓存与虚拟内存的分层关系见 [[工程知识/性能工程：从用户等待到资源瓶颈/操作系统与运行时/虚拟内存与文件系统把地址和持久化分层|虚拟内存与文件系统把地址和持久化分层]]。IO 队列与持久化语义见 [[工程知识/性能工程：从用户等待到资源瓶颈/存储与网络/存储IO性能取决于访问模式、队列与持久化语义|存储IO性能取决于访问模式、队列与持久化语义]]。数据库怎么在 fsync 之上组事务，见 [[工程知识/数据系统：在并发与故障中保存事实/可靠性与运维/InnoDB用日志连接事务、恢复与复制|InnoDB用日志连接事务、恢复与复制]]。

## 相关
- [[工程知识/性能工程：从用户等待到资源瓶颈/操作系统与运行时/cgroups把机器切成可调度的资源单元|cgroups把机器切成可调度的资源单元]]——回写速率与配额相互作用：cgroup 的 blkio 限制会反过来约束回写带宽

- [[工程知识/性能工程：从用户等待到资源瓶颈/操作系统与运行时/虚拟内存与文件系统把地址和持久化分层|虚拟内存与文件系统把地址和持久化分层]]——页缓存的分层位置
- [[工程知识/性能工程：从用户等待到资源瓶颈/存储与网络/存储IO性能取决于访问模式、队列与持久化语义|存储IO性能取决于访问模式、队列与持久化语义]]——回写落盘的队列视角
- [[工程知识/数据系统：在并发与故障中保存事实/事务与存储/崩溃一致性给文件系统补上POSIX未定义部分|崩溃一致性给文件系统补上POSIX未定义部分]]——同一枚硬币的另一半
- [[工程知识/后端系统：在并发、失败与变化中维持服务/分布式可靠性/电源与时钟：UTC与单调钟的工程含义.md]] —— 电源故障与存储一致性的另一投影
