---
title: 进程线程与系统调用构成执行边界
type: concept
status: active
updated: 2026-09-03
review_after: 2027-09-03
change_rate: low
confidence: high
tags:
  - systems/os
  - processes
  - concurrency
sources:
  - "https://man7.org/linux/man-pages/man2/fork.2.html"
  - "https://man7.org/linux/man-pages/man2/execve.2.html"
  - "https://man7.org/linux/man-pages/man7/signal.7.html"
---

# 进程线程与系统调用构成执行边界

进程是地址空间、文件描述符、凭据和资源限制的隔离单位；线程共享进程资源，但拥有自己的调用栈、寄存器和调度状态；系统调用是用户态请求内核执行文件、网络、内存、进程和时间操作的边界。理解这三者，才能解释后台任务、容器、超时、信号和资源清理。

## 生命周期

```text
fork/clone -> 继承或共享资源 -> exec 替换地址空间
           -> run/blocked/stopped -> exit -> wait/reap
```

父子进程、进程组、会话和信号决定终端断开、超时取消、子进程清理和僵尸进程行为。`kill` 发送信号不等于进程立即停止；优雅终止需要处理信号、关闭连接、保存状态和等待子进程。

## 资源边界

文件描述符指向打开文件描述（偏移和状态可共享），不是“文件名计数”；管道、socket、epoll、共享内存和锁拥有不同阻塞/关闭语义。线程共享堆和 fd，数据竞争与一处未关闭资源会影响整个进程。

## 量化锚点：切换成本的数量级

上下文切换的直接成本（寄存器与页表切换）约 1-5 微秒，间接成本（缓存与 TLB 污染）可达 10-50 微秒——高并发服务每秒百万次切换时，仅切换就吃掉 10-50% 的 CPU。协程的价值就是把切换压回 0.1-0.2 微秒（用户态无页表切换）；系统调用的成本（模式切换）约 0.1-1 微秒，批量化手段（io_uring 把多次提交合并、writev 合并写）在 syscall 密集负载下可省 30% 以上。数字的用法是定数量级，实测用 `perf stat` 的 context-switches 计数对账。

## 案例回流：fd 与守护链的衔接

执行边界漏资源是缺陷域的高发区：[[工程知识/性能工程：从用户等待到资源瓶颈/操作系统与运行时/fd 泄漏先用 lsof 对账：每类套接字都有名有姓才算干净]] 讲的 fd 泄漏，本质是"打开的系统调用没有配对的关闭"跨过边界漏走；[[工程知识/后端系统：在并发、失败与变化中维持服务/任务与并发/后台任务的守护链：进程托管与会话脱离是可靠性的底线配置]] 讲的守护链，本质是进程生命周期的所有权纪律。两者都是执行边界这条主线在资源维度的展开。

## 验证

验证的锚点：资源收敛要与信号注入对账——预写终端断开与 SIGTERM 后内存、文件描述符与子进程数应回到基线，实测缓慢增长，与预期对不上。

用 `ps`、`/proc`、`lsof`、`strace/dtruss` 或语言 trace 观察父子关系、fd、信号、系统调用和等待；注入终端断开、SIGTERM、超时、OOM、子进程崩溃和 fd 耗尽，确认状态和资源最终收敛。

关系：[[工程知识/后端系统：在并发、失败与变化中维持服务/任务与并发/任务生命周期必须覆盖进程、日志、超时与清理]] · [[工程知识/软件构建：让变化可以理解、验证与交付/语言与运行时/Python并发先区分协程、线程、进程与解释器]]

- [[工程知识/性能工程：从用户等待到资源瓶颈/处理器与内存/无锁数据结构的证据门槛：CAS与内存序.md]] —— futex 无竞争时退化为一条原子指令，等待才进内核
- [[工程知识/性能工程：从用户等待到资源瓶颈/操作系统与运行时/io_uring与异步IO演进：系统调用省下的开销.md]] —— syscall 是执行边界上的税，io_uring 把税摊到批次粒度

## 机制与本质

进程提供地址空间、描述符表、凭据与资源限制四类隔离单位，线程在同一地址空间内共享这些资源而各自持有调用栈与调度状态，系统调用是用户态请求内核代办文件、网络、内存、进程与时间操作的入口。三者划定了执行与资源之间的边界，后台任务的存活范围、容器的隔离范围、超时的作用位置与资源清理的责任归属都由这条边界决定。

## 要解决的问题

后台任务在会话结束后消失、容器的资源限制没有生效、超时之后子进程仍在运行、描述符耗尽但看不出是谁持有的，这些现象都涉及执行与资源之间的边界。本篇回答：隔离单位各自包含哪些资源、线程与进程在共享与独占上差在哪里、系统调用承担什么职责，以及定位这类问题要看哪些记录。

