---
title: 数据编排要保证依赖、重试与幂等
type: playbook
status: active
updated: 2026-09-03
review_after: 2027-03-03
change_rate: medium
confidence: high
tags:
  - "data/orchestration"
  - "data/pipelines"
  - "data/reliability"
sources:
  - "https://airflow.apache.org/docs/apache-airflow/stable/core-concepts/dags.html"
  - "https://docs.dagster.io/getting-started/concepts"
  - "https://beam.apache.org/documentation/programming-guide/"
---

# 数据编排要保证依赖、重试与幂等

## 核心要点

| 维度 | 回答 |
| --- | --- |
| 要解决的问题 | 多个数据任务有依赖、分区和时间窗口，迟到、崩溃或重复触发会导致产物缺失、重复写入或覆盖新结果。 |
| 本质 | 编排器维护带输入快照和产物版本的状态图；重试只有在同一输入下可安全重放时才有意义。 |
| 做法 | 用数据集、分区、窗口、代码 revision 和输入快照定义任务身份，临时写入后原子提交，显式记录 attempt、回填和失败状态。 |
| 效果与代价 | 任务可重跑、补数、恢复和审计；代价是版本存储、调度状态、资源竞争和幂等设计复杂度。 |
| 边界 | 调度成功不等于业务指标正确或下游已消费；无限重试、跳过和部分成功不能替代数据质量与发布门禁。 |
| 验证 | 注入 worker 崩溃、依赖迟到、重复触发、空分区、schema 变化和回填并发，核对产物版本、下游可见性和重建能力。 |

数据任务是带输入版本、分区、时间窗口和产物的有向图。调度器负责依赖、触发、重试、并发和状态；它不负责证明业务指标正确或下游已经消费新数据。

一个任务显示成功，可能只是文件写完，尚未通过质量门禁或被下游读取；如果重试没有绑定输入快照，还可能用新数据覆盖旧版本。编排的核心是让“这次运行处理了什么、产出了什么、谁可以看到”可追踪。

这也是为什么数据平台的绿色任务状态不能直接当成指标可信：任务完成、质量通过、索引发布和下游可见是不同状态，必须在图中分别表示。

## 任务身份

```text
dataset + partition/window + code revision + input snapshot -> output version
```

重试必须幂等：写临时产物后原子提交、使用分区/版本键、记录 attempt 和输入快照。回填会与实时任务竞争资源；暂停、跳过、失败、部分成功和手工重跑必须是显式状态。

## 可运行示例：一个会重复执行的调度任务

同一段写库任务，有幂等键与没有幂等键在重试下的不同结局：

```python
# 任务：把昨天的订单汇总写入 ads 表（每天调度，失败自动重试 3 次）

# 无幂等：重试后 ads 表出现两份 9-22 的汇总（INSERT 直接追加）
#   count(*) where dt = '2026-09-22' → 2 行，下游 SUM 翻倍

# 有幂等：任务身份 = (task_name, partition_date)，写入前先删后插或 INSERT OVERWRITE
DELETE FROM ads.daily_summary WHERE dt = '{partition}';
INSERT INTO ads.daily_summary SELECT ... WHERE dt = '{partition}';
#   重试 N 次结果一致——幂等的定义不是"不重复执行"，是"重复执行无副作用"
```

跑一遍的收获：幂等键的选择决定重试的安全性——按分区重算的任务天然可幂等（覆盖写），增量追加类任务要靠业务键去重（external_id 唯一约束）。编排器给的重试是杠杆，幂等是支点，缺支点的重试把小故障放大成数据事故。

## 量化锚点与案例回流

重试的退避账：指数退避（1s → 2s → 4s → 8s，上限 5 次）覆盖 99% 的瞬时故障，总等待 15 秒；不退避的立即重试 3 次对瞬时故障的修复贡献接近零，反而把下游打得更痛（重试风暴的数学：下游恢复到 80% 容量时，积压的重试集中涌入，利用率瞬间回到 100%，再次过载）。退避要加抖动（±20%），否则同批任务的整齐重试等于一次同步洪峰。与 [[工程知识/数据系统：在并发与故障中保存事实/数据管道与流处理/CDC把数据库变化传播为可重放事实]] 的衔接：CDC 的位点重放与调度的分区重算共用同一条幂等纪律——重放安全来自"任务身份 + 覆盖语义"；[[工程知识/AI 系统工程：从模型能力到生产能力/Agent与工作流/Agent状态机与任务恢复：进度是状态不是聊天记录]] 的外部动作幂等键是同构设计在 Agent 域的形态。

## 验证

验证的锚点：编排可靠性要与重复触发对账——预写 worker 崩溃后重跑不应覆盖出错误产物、任务可从任意版本重建，实测出现半成品，与预期对不上。

注入 worker 崩溃、依赖迟到、重复触发、上游空数据、schema 变化、分区缺失、重试和回填并发；检查是否重复/覆盖错误产物、是否可从任意版本重建、下游是否看到一致快照。AI/RAG 索引构建也应使用相同的任务身份和发布门禁。

关系：[[工程知识/数据系统：在并发与故障中保存事实/数据管道与流处理/CDC把数据库变化传播为可重放事实]] · [[工程知识/后端系统：在并发、失败与变化中维持服务/任务与并发/任务生命周期必须覆盖进程、日志、超时与清理]]
- [[工程知识/后端系统：在并发、失败与变化中维持服务/任务与并发/分布式锁的正确与错误用法：fencing与租约.md]] —— 锁失效的兜底：幂等设计
- [[工程知识/后端系统：在并发、失败与变化中维持服务/服务设计/优雅停机的完成语义：排空连接与任务交接.md]] —— 消息交接的幂等兜底
- [[工程知识/后端系统：在并发、失败与变化中维持服务/服务设计/事件驱动架构的解耦代价：最终一致与可追溯性.md]] —— 消费侧的重试与幂等
