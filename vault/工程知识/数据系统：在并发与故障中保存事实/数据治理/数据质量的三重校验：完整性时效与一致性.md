---
title: 数据质量的三重校验：完整性时效与一致性
type: concept
status: active
updated: 2026-09-22
review_after: 2027-09-22
change_rate: low
confidence: high
tags:
  - data/quality
  - data/governance
  - correctness
sources:
  - "https://en.wikipedia.org/wiki/Data_quality"
  - "https://greatexpectations.io/"
---

# 数据质量的三重校验：完整性时效与一致性

## 要解决的问题

数据管道的产出被下游当事实消费，但数据在采集、传输、转换、存储的每一环都可能坏：丢数据（完整性）、慢数据（时效性）、错数据（一致性）。三重校验要解决的问题，是**给数据质量一个可执行的验收体系**，让“数据是好的”从感觉变成可度量、可报警、可复盘的工程判断。静默失真的检测靠对账与不变量而不是报错（见 [[工程知识/缺陷分析：从个案到体系/正确性/静默失真的检测靠对账与不变量而不是报错.md]]）写的是通用检测思想，本文把三重校验落到数据管道的具体形态。

## 机制

**完整性：该来的都来了**。校验维度：行数对账（上游事件数 vs 下游落地数，传输丢包/过滤规则的意外剔除都体现在差值）；主键唯一与非空（重复消费、幂等失效的直接信号）；外键存在性（维度表缺行导致 join 丢行）。完整性问题的隐蔽性：丢 1% 的事件不报错，报表数字略小，无对照就无感知。

**时效性：数据新到能用**。校验维度：端到端延迟（事件产生到可查询的时延，超过 SLA 报警）；分区就绪（批处理分区在约定时间前可读，迟到分区阻塞下游全部任务）；水位线推进（流处理的 event time 水位停滞说明迟到或反压）。时效性问题的隐蔽性：慢一天的数据照样自洽（内部一致），下游拿它做当天决策就是错的。

**一致性：数据对得上**。校验维度：口径对账（同一指标在两个系统算出来不等——sum/avg 逻辑差异、时区边界、null 处理）；快照对账（源库总量 vs 数仓总量，批间增量对得上总量变化）；业务不变量（金额恒等式：订单总额 = 明细求和 + 优惠 - 退款，违反即有环节错）。一致性问题的隐蔽性：单独看每个系统都对，对照才见错。

**校验的三个落点**。事前（写路径）：入口断言（schema 校验、值域检查、主键冲突拒收），把坏数据挡在门外，数据治理把质量责任放到写路径上（见 [[工程知识/数据系统：在并发与故障中保存事实/数据治理/数据治理把质量责任放到写路径上.md]]）。事中（管道）：对账任务（每个作业后跑断言，失败阻断下游），失败要止损（坏数据污染下游比数据晚到更糟）。事后（消费侧）：指标监控（核心指标的方差/环比突变报警）。三层防线分别对应“防止进入、及时发现、影响面控制”。

## 背景与代价

思想背景：数据质量的概念体系从制造业的质量管理（Juran 的 fitness for use，1980s）借来，2010s 随大数据管道复杂化成为独立工程学科。Google 与 Uber 曾在公开技术分享中介绍过各自的数据质量平台，开源侧的 Great Expectations、dbt test 把断言式校验做成声明式配置。变化的是工具形态，不变的是三问：该来的来了吗、新吗、对吗。

最精巧的一笔是**业务不变量作为一致性校验的高地**。口径对账与快照对账验证“数据与数据”的和谐，业务不变量验证“数据与现实”的和谐（金额恒等式、状态机合法转换、时间戳因果）。不变量是业务知识的编码——写下“退款金额 ≤ 订单金额”这条断言，就是写下对业务的理解；断言失败即业务理解与数据现实脱节。这一笔让数据质量从数据工程问题升维到业务知识工程。代价要摆明：三重校验本身也是管道，也有成本——对账任务消耗计算资源（全量对账贵，抽样对账有漏检）、报警风暴（敏感阈值产生噪音，团队报警疲劳后忽略真报警）。校验体系自身也要治理：断言覆盖率（关键表有多少断言）、报警信噪比、断言失败的处理 SLA，这些元指标常被漏掉。

## 边界

- **三重校验不覆盖正确性的全部**。数值精度（浮点累积误差，见 [[工程知识/数据系统：在并发与故障中保存事实/数据建模与SQL/消失的数值：浮点格式与精度陷阱.md]]）、时区与夏令时（时间口径的一致性陷阱）、语义漂移（上游字段含义悄然变化，值域检查不报）。补位：schema 演进管理、语义层（指标定义集中化，见 [[工程知识/数据系统：在并发与故障中保存事实/数据治理/数据质量与血缘让指标可以被解释.md]]）。
- **抽样对账的漏检率要心里有数**。99% 抽样漏掉恰好 1% 的环节数据（按分区抽样漏分区级故障）。分层抽样（大分区全查、小分区抽检）与全量对账的周期组合（日抽样 + 周全量）是成本与覆盖的折中。
- **“准时”与“新鲜”是两个指标**。分区就绪（批处理可用性）与数据新鲜（事件时间滞后）语义不同：分区准时但内容旧（上游断流后批处理照跑，产出空分区或旧数据）是真实故障形态。两个指标分开监控，空分区检测（行数为 0 的分区报警）是必备断言。
- **断言失败的处理要预演**。断言失败阻断下游（数据晚到比坏数据传播好），但阻断本身也是生产事故（下游报表停更）。失败处理的决策树：自动阻断哪些（关键表）、告警人工决策哪些（次要表）、重跑与修复路径（从哪个位点重放，见 CDC（见 [[工程知识/数据系统：在并发与故障中保存事实/数据管道与流处理/CDC把数据库变化传播为可重放事实.md]]）的位点机制）。

## 验证

- **完整性对账实测**：上游埋点事件数与下游表行数对账（分组统计），人为丢一批事件（停消费者 5 分钟），预期：差值报警触发；恢复重放后差值归零。差值与故障时长的关系曲线是阈值设定依据。
- **时效 SLA 实验**：标注事件时间与落地时间的差值分布（P50/P99），预期：分布双峰（正常秒级 + 异常小时级），异常峰即 SLA 违反样本；上游反压（限速）重跑，预期 P99 劣化。分布数据是 SLA 承诺的依据。
- **不变量断言实验**：金额恒等式断言跑历史 30 天数据，预期：发现既有历史脏数据（历史违约率非零），修复后新数据违约率为 0。历史违约率的清零曲线是断言上线的效果证据。

## 相关

- [[工程知识/缺陷分析：从个案到体系/正确性/静默失真的检测靠对账与不变量而不是报错.md]] —— 静默失真检测的通用思想
- [[工程知识/数据系统：在并发与故障中保存事实/数据治理/数据治理把质量责任放到写路径上.md]] —— 写路径防线的治理视角
- [[工程知识/数据系统：在并发与故障中保存事实/数据治理/数据质量与血缘让指标可以被解释.md]] —— 口径与血缘的指标可解释性
- [[工程知识/AI 系统工程：从模型能力到生产能力/数据与MLOps/数据飞轮的工程闭环：日志回流清洗与标注.md]] —— 清洗漏斗的质量校验同源思想
