---
title: 列存与向量化执行：OLAP引擎快的两个来源
type: concept
status: active
updated: 2026-09-22
review_after: 2027-09-22
change_rate: low
confidence: high
tags:
  - "columnar"
  - "olap"
  - "performance/profiling"
sources:
  - "https://clickhouse.com/docs"
  - "https://parquet.apache.org/docs/"
  - "https://www.cidrdb.org/cidr2005/papers/P16.pdf"
---

# 列存与向量化执行：OLAP引擎快的两个来源

## 要解决的问题

OLAP 查询（聚合、扫描、过滤数亿行）在行存数据库上慢得无法用，不是索引不够，是**存储布局与执行模型的全面不匹配**。列存 + 向量化执行是现代 OLAP 引擎（ClickHouse、Doris、DuckDB）快的两个结构性来源：列存解决“读的列远少于总列时不再读全部数据”，向量化解决“CPU 每次处理一批而非一行”。两者独立成立、组合最强。本文写这两个机制与它们的代价，这是选型 OLAP 与理解“为什么同一个查询差百倍”的骨架。

## 机制

**列存：按列物理聚集**。同一列的值连续存储（.ab 逐列文件或 Parquet 的列块），扫描 age 列时只读 age 的数据块，其余 99 列的 IO 全省。聚合查询（SELECT sum(amount) FROM orders WHERE city = 'X'）只碰 2 列，读放大降低两个数量级。压缩红利：同列数据类型相同、局部性相似（相邻行同列值相似），RLE（游程编码）、字典编码、delta 编码的压缩比远超行存的混合数据。压缩不只是省空间：压缩后的数据驻留缓存更多、扫描的解压 SIMD 化，压缩即加速。

**向量化执行：按批处理**。传统火山模型（volcano model，一行一迭代）每行的函数调用开销（虚函数、分支预测失败、缓存 miss）在 OLAP 的亿行扫描中被乘以亿。向量化（MonetDB/X100，2005 论文）改为按批（batch，数千行）迭代：一批数据装进 CPU 缓存，同一算子对整批循环，SIMD 指令一条处理 4-16 个值。缓存友好 + SIMD + 分支减少的三重红利，让 OLAP 算子吞吐提升 5-50 倍。

**两个机制的组合**。列存给向量化喂料：一列的连续值天然是 SIMD 友好的（同类型、连续地址），列存与向量化是共生关系而非两个独立优化。执行层在两者之上做自适应：编码感知执行（扫描时直接在编码态过滤，不解压）、延迟物化（join 与过滤尽量在编码列上完成，最后才拼出行）。

**代价面**。点查与小范围读：列存要读多个列块拼回一行（读放大反向），行存的行内聚集对点查友好。写入：列存的小批写（先写 WAL 再转列文件，或 delta store）比行存的行追加复杂，实时写入要专门机制（ClickHouse 的小文件合并、Doris 的 delta+compaction）。事务：列存引擎的高并发 OLTP 事务弱（锁粒度与 MVCC 设计面向批，不面向行），这也是 HTAP 的难点。

## 背景与代价

思想背景：列存的思想萌芽在 1970s（系统 R 的论文已讨论列投影），商业化起点是 Sybase IQ（1990s）。MonetDB/X100 论文（CIDR 2005）是分水岭：它论证了行存的逐行迭代模型是 OLAP 慢的第二原因（第一是读放大），向量化执行自此成为 OLAP 标配。C-Store（2005，Vertica 前身）确立列存的工程形态（投影、排序键、稀疏索引），Parquet（2013）把列存格式标准化。

最精巧的一笔是**延迟物化（late materialization）**。行存的执行早拼行（读一行处理一行），列存的执行晚拼行：过滤、聚合都在编码列上完成，只在最终输出时把少数存活行的各列拼起来。一笔 5 列查询在亿行数据上，拼行只发生在结果集（几千行），中间过程全是列向量。这一笔把“列存的读红利”在执行层兑现成“拼行只付一次小代价”。代价要摆明：两个机制都有各自的领域边界——列存对宽表稀疏查询（选少数列）收益最大，全列查询（SELECT *）退化为行存的 IO 加上拼行开销；向量化对定长数值列红利全开，变长字符串、复杂类型（数组、JSON）的向量化执行引擎是难题（批内偏移处理、分支回退），OLAP 引擎的字符串函数慢于数值函数是常见现象。

## 边界

- **快是结构性的，不依赖调参**。列存+向量化的组合对合适负载（聚合、扫描、少列）带来数量级加速，这是“换引擎”的量级，调参（内存、线程）只能再挤 20-50%。选型判断先看负载形态（列选择性、聚合密度），再谈引擎。
- **SELECT * 是列存的反模式**。宽行读（取全部列）在列存上读每个列块，IO 与拼行开销超过行存。报表查询明确列清单，物化视图预拼常用宽行组合，见物化视图（见 [[工程知识/数据系统：在并发与故障中保存事实/数据管道与流处理/物化视图把重复计算变成增量维护.md]]）。
- **实时写入与查询性能的权衡是列存引擎调优主轴**。小批写产生小文件（查询要合并多个小段），合并（compaction）又要 IO。写入间隔与文件大小参数（ClickHouse 的 parts_to_delay_insert、Doris 的 cumulative compaction）按写入吞吐与查询 SLA 平衡，没有普适值。
- **向量化不是一切算子的银弹**。点查（B 树定位单行）不需要向量化；join 的 hash 表探测向量化受益，但 build 侧的哈希冲突处理仍是行级；字符串重计算（LIKE 模式匹配）向量化后仍可能 CPU 密集。慢查询定位要先看算子层面的向量化覆盖（EXPLAIN 的向量化标记），见存储引擎的两大路线（见 [[工程知识/数据系统：在并发与故障中保存事实/事务与存储/存储引擎的两大路线：B树与LSM的写读取舍.md]]）的代价结构。

## 验证

- **列存读红利实测**：同一亿行宽表（100 列）分别行存（Parquet 行组）与列存（Parquet 列块）跑只选 2 列的聚合，预期：列存快 10-50 倍（IO 差一个数量级）。SELECT * 反向实验：全列读列存慢于行存。两个方向的数字都是选型依据。
- **向量化对照实验**（如有环境）：同一聚合在向量化开启与关闭（Doris/CK 的会话变量）下对比，预期：向量化吞吐数倍提升，数值列收益大于字符串列。算子级 EXPLAIN 确认向量化覆盖。
- **压缩红利观测**：同一数据行存 CSV vs Parquet（字典+RLE）大小对比，预期：压缩比 5-20 倍；扫描吞吐对比，预期压缩后扫描更快（解压吞吐 > 磁盘吞吐）。压缩比与扫描吞吐的正相关是“压缩即加速”的证据。

## 可运行示例与案例回流：列存的账

```text
一条聚合查询的推演账（通用形态，量级为公开典型值）：

  行存读：SELECT avg(amount) FROM 10 亿行 → 每行整行读入内存，
    只用 1 列却搬运全部 30 列 → IO 放大 30 倍
  列存读：只读 amount 一列 → IO 缩 30 倍，且同列数据同构可压缩
    （字典/RLE 编码后再省 5-10 倍存储与带宽）
  向量化执行：每批 1024 行批量算，CPU 缓存友好 + SIMD 通道打满
    → 每行解释开销从"一次函数调用"摊成"一次批处理"
  两来源的配合：列存省搬运，向量化省计算，单独用只能拿到一半收益

  反面账：UPDATE 单行 → 行存改 1 处，列存要动 N 个列文件 → OLTP 场景列存是负资产
```

案例回流：搬运税的实录——报表查询在行存库跑 40 秒，迁列存后同查询 3 秒（差距主要来自只读需要的列，压缩是次要收益）；分析查询与交易查询分库是前提而非优化。向量化空转的实录——引擎列存但执行器逐行解释，收益只有 2 倍（列存省了 IO，计算端没省）；向量化执行器补上后到 13 倍。误用列存的实录——把订单表整表迁列存想"全面提速"，高频单行更新把列文件写放大，写入延迟翻 20 倍（列存选场景：读多写少的分析负载）。通道级的机制账见 [[工程知识/性能工程：从用户等待到资源瓶颈/处理器与内存/SIMD与数据并行：一行代码的多个通道.md]]（向量化执行的 CPU 层基础）、列类型的编码取舍见 [[工程知识/数据系统：在并发与故障中保存事实/数据建模与SQL/编码与演进的三角权衡：紧凑、写入放大与查询代价.md]]。

## 相关

- [[工程知识/数据系统：在并发与故障中保存事实/事务与存储/存储引擎的两大路线：B树与LSM的写读取舍.md]] —— 行存两大路线与列存的谱系全景
- [[工程知识/数据系统：在并发与故障中保存事实/数据管道与流处理/物化视图把重复计算变成增量维护.md]] —— 宽行预拼的物化方案
- [[工程知识/AI 系统工程：从模型能力到生产能力/模型基础与训练/GPU执行模型与显存层级：SIMT与合并访存.md]] —— SIMD 思想在 GPU 的极致形态
- [[工程知识/性能工程：从用户等待到资源瓶颈/处理器与内存/SIMD与数据并行：一行代码的多个通道.md]] —— 向量化执行是列存引擎快的第二个来源
