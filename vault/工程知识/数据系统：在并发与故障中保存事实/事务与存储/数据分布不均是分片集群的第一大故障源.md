---
title: 数据分布不均是分片集群的第一大故障源
type: concept
status: active
updated: 2026-09-22
confidence: high
change_rate: low
review_after: 2029-09-22
tags:
  - sharding
  - defect-analysis
  - distributed-systems
  - data/storage
sources:
  - "https://www.mongodb.com/docs/manual/core/sharding-balancer/"
  - "https://kafka.apache.org/documentation/#design_horizontalpartitions"
  - "https://www.usenix.org/system/files/conference/nsdi16/nsdi16_paper_kulkarni.pdf"
---

# 数据分布不均是分片集群的第一大故障源

分片集群上线时按容量规划了 N 个分片，三个月后 80% 流量打在一个分片上：热点 key、单调递增的分片键、时区性的访问模式，都会把"均匀分布"的假设打破。要解决的问题：把数据倾斜（skew）当作分片集群的第一故障源来对待，识别它的形态、检测它的信号、给出修复与预防路径。

## 机制：倾斜形态与检测信号表

| 倾斜形态 | 成因 | 检测信号 | 修复路径 |
| --- | --- | --- | --- |
| 热点 key 倾斜 | 少量 key 承载高频访问（明星商品、大 V） | 单分片 QPS/其它分片均值 > 10x | 应用层散列加盐（salt 前缀） |
| 单调键倾斜 | 分片键单调递增（时间戳、自增 ID） | 写入永远集中在尾分片 | 键加散列前缀、范围分片改哈希分片 |
| 大 key 倾斜 | 单 key 数据量超大（百万级 hash field） | 单分片存储量/均值 > 10x | 分开 key、冷热分离 |
| 时区倾斜 | 用户地理分布不均 | 分片 QPS 的昼夜周期与地理分布相关 | 按地理域分组再分片 |

检测纪律：监控每个分片的 QPS、存储量、连接数三个维度（任一维度倾斜都算），设告警阈值为"最大分片/中位数分片 > 3x"——比均值更抗大 key 干扰。收益是早发现早散列；代价是散列加盐破坏了 key 的可读性与范围查询能力，加盐前先确认访问模式真的没有范围需求。Kafka 的分区设计文档与 MongoDB balancer 文档分别给出了流式与文档型两种系统的一手设计依据。

## 案例回流与量化锚点：分布不均的实录

量化锚点：分布不均的两本账——存储不均（一个分片占 40% 数据，扩容加节点它不动，写入热点使它的磁盘先满）；负载不均（明星用户所在分片 QPS 是均值的 10 倍，其他分片空闲，瓶颈分片的 P99 决定全集群的 P99）。热点键的典型来源：时间戳当分片键首列（所有写入永远集中在最后一个分片，顺序写热点）、大客户 ID（单一租户占 30% 流量）。案例回流的实录：[[工程知识/数据系统：在并发与故障中保存事实/事务与存储/分片键设计以查询与均衡为双重约束.md]] 是本篇的姊妹篇——分片键的选型决策决定不均是否发生，本篇讲不均发生后的识别与处置（监控按分片看写入 QPS 与磁盘增速，尾部离散度超 2 倍即告警）；[[工程知识/缺陷分析：从个案到体系/资源与容量/案例六：容量与泄漏——连接泄漏和 SST 堆积.md]] 的容量假设失效实录里，分片不均是"按节点数算容量却算不准"的直接原因——均值没有意义，尾部分片才是容量真相。

## 验证

```bash
# 对自己的分片集群跑分布体检
redis-cli --cluster info / CLUSTER SLOTS | 检查 slot 分布
# MongoDB: 每分片 chunk 数与数据量
sh.status(); db.collection.getShardDistribution()
# Kafka: 分区消息速率
kafka-consumer-groups.sh --describe --group g
```
预期：健康集群最大分片/中位数分片比值在 1.5x 以内；超出即触发治理流程。数字对不上预期（某分片持续超阈）时，先查分片键的写入模式（单调性、热点集中度），再查是否迁移速度跟不上倾斜速度（balancer 滞后也是一类故障）。

## 边界

本篇讲分布不均的故障形态与治理。分片键的选型方法论（查询局部性与均衡的权衡）见 [[工程知识/数据系统：在并发与故障中保存事实/事务与存储/分片键设计以查询与均衡为双重约束|分片键设计以查询与均衡为双重约束]]。再平衡时怎么把移动量压到平均槽位见 [[工程知识/数据系统：在并发与故障中保存事实/事务与存储/一致性哈希把再平衡的数据移动量压到平均槽位|一致性哈希把再平衡的数据移动量压到平均槽位]]。热点在缓存层的对应形态与防御见 [[工程知识/后端系统：在并发、失败与变化中维持服务/服务设计/缓存穿透击穿与雪崩是三种不同的失效模式|缓存穿透击穿与雪崩]]。缓存的读写路径与一致性代价见 [[工程知识/后端系统：在并发、失败与变化中维持服务/服务设计/缓存改变读写路径、一致性与故障模式|缓存改变读写路径、一致性与故障模式]]。

## 相关

- [[工程知识/数据系统：在并发与故障中保存事实/事务与存储/分片键设计以查询与均衡为双重约束]]——倾斜的源头预防
- [[工程知识/数据系统：在并发与故障中保存事实/事务与存储/一致性哈希把再平衡的数据移动量压到平均槽位]]——再平衡的移动量控制
- [[工程知识/数据系统：在并发与故障中保存事实/可靠性与运维/热点副本与流量倾斜：读写热点要按分片维度分开治理]]——倾斜的治理端：两种形态的诊断信号与手段选择树
