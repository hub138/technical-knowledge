---
title: 崩溃一致性给文件系统补上POSIX未定义部分
type: concept
status: active
updated: 2026-09-22
confidence: high
change_rate: low
review_after: 2029-09-22
tags:
  - data/storage
  - file-systems
  - reliability
  - storage
sources:
  - "https://pdos.csail.mit.edu/6.824/papers/crash-consistency.pdf"
  - "https://www.usenix.org/legacy/event/fast05/tech/full_papers/vijayan/vijayan.pdf"
  - "https://lwn.net/Articles/526317/"
---

# 崩溃一致性给文件系统补上POSIX未定义部分

POSIX 只定义了正常执行时 write 的语义，没定义 write 进行到一半断电会发生什么。要解决的问题：一次逻辑写入牵动多个磁盘块（数据块、元数据块、目录项），崩溃可能停在任何两块之间，留下既不是旧状态也不是新状态的中间态。崩溃一致性（crash consistency）就是让文件系统在任何崩溃点都能恢复到一个可解释的状态。

## 机制：两类方案与一张问题表

问题表（一次"往文件追加一行"牵动的写序）：

| 崩溃点 | 已写入磁盘 | 磁盘状态 | 结果 |
| --- | --- | --- | --- |
| 全部之前 | 无 | 旧文件完整 | 丢失追加，可接受 |
| 数据块后、元数据前 | 新数据块 | 新数据指向旧大小 | 空间泄漏（垃圾） |
| 元数据后、数据前 | 新元数据 | 旧大小指向新数据 | 读到垃圾数据，最危险 |
| 全部之后 | 全部 | 新文件完整 | 追加成功 |

两类方案：

| 方案 | 机制 | 代表 | 代价 |
| --- | --- | --- | --- |
| 日志（journaling） | 先把全部写序记录到日志区，再真正写数据 | ext3/ext4、XFS | 日志写一遍数据再写一遍，写入放大两倍（可关闭数据的日志回序） |
| 写时复制（copy-on-write） | 永远写新块，原子切换根指针 | ZFS、Btrfs、APFS | 元数据开销大、随机写变随机读+写、碎片化 |

权衡：日志方案顺序化写路径、恢复快（重放日志），但 fsync 成本高；COW 方案天然快照与校验和、崩溃恢复 O(1)，但碎片化与顺序写退化为随机写。数据库视角看这两类方案：WAL（预写日志）就是应用层的 journaling，见 [[工程知识/数据系统：在并发与故障中保存事实/可靠性与运维/InnoDB用日志连接事务、恢复与复制|InnoDB用日志连接事务、恢复与复制]]。收益：任意崩溃点恢复可解释；代价：写吞吐下降、设计复杂度上升。

## 验证

```bash
# 观察自己文件系统的行为：查挂载选项
mount | grep -E "ext4|xfs|zfs|btrfs"
# 用 crashmonkey / alice 类的探索性测试框架检测真实文件系统的崩溃一致性
# （crashmonkey: https://github.com/utsaslab/crashmonkey）
# 用 Alice 系论文的方法：模糊文件系统操作序与崩溃点，检查恢复后的不变量
```
预期：ext4 data=ordered 模式下"数据先于元数据"保证不会读到垃圾数据，但可能出现零填充（null bytes）——这是 Linux 社区对 "POSIX 未定义" 的实际选择。验证时要对照论文的发现：Alice 论文（FAST'15）系统性检查了 11 个应用在真实文件系统上的崩溃一致性漏洞，包括 sqlite、LevelDB、PostgreSQL，都存在非平凡漏洞。数字对不上或行为异常时，先查你的盘是否启用回写缓存（写缓存易失时，盘自身先于文件系统丢失数据，fsync 的语义再次打折）。

## 边界

本篇讲文件系统层的崩溃一致性。OS 页缓存层的回写决策与 fsync 机制见 [[工程知识/性能工程：从用户等待到资源瓶颈/操作系统与运行时/脏页回写在性能与崩溃丢失窗口间权衡|脏页回写在性能与崩溃丢失窗口间权衡]]，两篇是同一枚硬币的两面：那篇讲"什么时候写"，本篇讲"写了停一半怎么办"。应用层事务怎么用 fsync 组出原子提交，见 [[工程知识/数据系统：在并发与故障中保存事实/可靠性与运维/InnoDB用日志连接事务、恢复与复制|InnoDB用日志连接事务、恢复与复制]]。分布式视角的多数派持久化（Raft 的日志先于提交）见 [[工程知识/后端系统：在并发、失败与变化中维持服务/分布式可靠性/Raft选举与日志复制把共识变成工程实现|Raft选举与日志复制把共识变成工程实现]]。备份与恢复的演练视角见 [[工程知识/数据系统：在并发与故障中保存事实/可靠性与运维/备份的价值由恢复演练来验证|备份的价值由恢复演练来验证]]。

## 相关

- [[工程知识/数据系统：在并发与故障中保存事实/可靠性与运维/InnoDB用日志连接事务、恢复与复制]]——应用层的崩溃一致性实现
- [[工程知识/性能工程：从用户等待到资源瓶颈/操作系统与运行时/脏页回写在性能与崩溃丢失窗口间权衡]]——写路径另一半
