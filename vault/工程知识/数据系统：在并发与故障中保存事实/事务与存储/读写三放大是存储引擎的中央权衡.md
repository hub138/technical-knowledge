---
title: 读写三放大是存储引擎的中央权衡
type: concept
status: active
updated: 2026-09-21
review_after: 2027-09-21
change_rate: low
confidence: high
sources:
  - "https://www.cs.umb.edu/~poneil/lsmtree.pdf"
  - "https://rocksdb.org/docs/"
  - "https://smalldatum.blogspot.com/2019/05/write-amplification-in-b-tree-vs-lsm.html"
tags:
  - data/storage
  - lsm
  - databases
---

# 读写三放大是存储引擎的中央权衡

存储引擎的选型争论，剥掉名词后都是三个数字的权衡：写放大（写 1 字节实际写入磁盘多少字节）、读放大（读 1 次逻辑读实际做几次物理读）、空间放大（存 1 字节逻辑数据实际占多少空间。要解决的问题：三者构成不可能三角——压低任何一个必然抬高另外两个；不看三放大的存储选型，等于只看锚点性能指标做决策。

## 三放大的来源

| 放大 | B+Tree 的来源 | LSM 的来源 |
| --- | --- | --- |
| 写放大 | 页内原地更新 + WAL | memtable 多层 compaction 合并 |
| 读放大 | 树高（3-4 次页读） | 多层 SSTable 探测 + Bloom 滤波 |
| 空间放大 | 页碎片 + 预留空页 | 旧版本 + tombstone 等待 compaction |

LSM 用写放大与空间放大换写吞吐（顺序写）：代价是读路径变贵（层层数）、后台 compaction 与前台争 I/O。B+Tree 用读友好换写代价（每次更新整页写回）。这是 [[工程知识/数据系统：在并发与故障中保存事实/事务与存储/LSM树用写放大换取顺序写和可扩展性]] 的机制层面展开，也是所有"XX 引擎比 YY 快"论战的还原终点。

## 度量先于选型

三放大不是常数，随工作负载漂移：读多写少时 LSM 的空间放大恶化（compaction 变少）；写多读少时 B+Tree 的页内更新热点恶化。选型前用真实负载画像回答：读写比、key 分布（单调/均匀/热点）、value 大小分布、删除比例。画像方法与 [[工程知识/数据系统：在并发与故障中保存事实/可靠性与运维/数据库基准必须描述负载与资源边界]] 一致。

## 可运行示例：同一份数据在两类引擎的三放大实测

一个 KV 工作负载（80 读 20 写），B+ 树与 LSM 各算各的账：

```text
数据：1 亿条 × 1KB，B+ 树（InnoDB 类）：
  写放大：随机写 → 页分裂 → 一次写变 2-4 次页写（+redo log）
  读放大：树高 3-4 层 → 3-4 次 IO（缓存热时 1 次）
  空间放大：页填充率 70-85% → 1.2 倍

LSM（RocksDB 类，4 层，每层 10 倍）：
  写放大：memtable → L0 → compaction 逐层下推，典型 8-15 倍
读放大：memtable + 每层各一个 SST（布隆过滤器全命中时）≈ 5 次探测
  空间放大：compaction 期间新旧数据共存，典型 1.5-2 倍

同一负载的读延迟对账：B+ 树 P99 稳（路径固定），
LSM P99 有毛刺（compaction 峰值时段读被抢 IO）——毛刺即写放大的时间侧形态
```

![Leveled Compaction 的层级结构](https://github.com/facebook/rocksdb/raw/gh-pages-old/pictures/level_structure.png)

上图是 Leveled Compaction 的文件布局：L0 的文件键范围相互重叠，L1 以下每层内部不再重叠，向下合并时同一份数据被重新写一遍，这是写放大在结构上的来源。图取自 [RocksDB 官方仓库 wiki](https://github.com/facebook/rocksdb/wiki/Leveled-Compaction)。

跑一遍的收获：三放大不是三个独立指标，是一笔账的三种记法——LSM 用读放大和毛刺换写吞吐，B+ 树用写放大换读稳定。选型先测负载里哪个放大最贵。

## 量化锚点与案例回流

经验数字的适用边界：点查密集（缓存类）B+ 树胜，顺序写密集（日志、时序）LSM 胜（写放大差距 3-5 倍）；空间放大 2 倍意味着同样 1TB 数据 LSM 要 2TB 盘——容量规划的乘数不能漏。compaction 的预算账：写入 100MB/s 的持续负载，compaction 的后台写要吃 1-3 倍（写放大倍数）即 100-300MB/s 的盘带宽，盘配小了 compaction 追不上写入（stall），这是"写得好好的突然卡住"的标准病因。案例衔接：[[工程知识/数据系统：在并发与故障中保存事实/事务与存储/LSM树用写放大换取顺序写和可扩展性]] 是本篇中央权衡在 LSM 侧的专文；[[工程知识/缺陷分析：从个案到体系/资源与容量/案例六：容量与泄漏——连接泄漏和 SST 堆积]] 的 SST 堆积实录正是 compaction 追不上写入的实证形态。

## 验证

验证的锚点：三放大倍数要与工具输出对账——预写实际写入字节与逻辑写入量之比，compaction 开销突增时该比值与预期对不上，说明负载已经漂移。

- 基准工具输出三放大：RocksDB 的 statistics 或独立工具（如 TestHarness）统计实际写入磁盘字节/逻辑写、逻辑读/实际读次数、磁盘占用/逻辑数据量。
- compaction 监控作为写放大的运行时代理：compaction I/O 字节 / 前台写入字节 的比值趋势，突增意味着负载漂移。
- 空间放大用对账验证：logic_data_size 与磁盘用量之差即碎片+旧版本+tombstone，趋势恶化触发 compaction 调参或归档。

## 边界

- 三放大是引擎层视角，数据库整体视角还要叠加复制放大（每个写要复制 R 份）与 [[工程知识/数据系统：在并发与故障中保存事实/数据管道与流处理/物化视图把重复计算变成增量维护]] 的视图维护放大。
- SSD 的写放大还有介质层（FTL 的 GC 与磨损均衡），与引擎层叠加，全链路写放大 = 引擎 × 介质；介质层无法被引擎消除，只能减少写入总量。
- 三放大无法同时压低的证明属于理论层，工程实践的自由度是调平衡点（compaction 策略、Bloom 误报率、页填充率），不是打破三角。

## 相关

- [[工程知识/数据系统：在并发与故障中保存事实/事务与存储/LSM树用写放大换取顺序写和可扩展性]]：LSM 侧的机制专篇，本文横向比较
- [[工程知识/数据系统：在并发与故障中保存事实/查询与索引/B+Tree索引为访问路径服务]]：B+Tree 侧的机制专篇
- [[工程知识/数据系统：在并发与故障中保存事实/查询与索引/一条SQL如何穿过优化器与存储引擎]]：三放大之上的查询路径全貌
- [[工程知识/性能工程：从用户等待到资源瓶颈/存储与网络/SSD的写放大来自介质要先擦后写|SSD的写放大来自介质要先擦后写]]——讲介质层的先擦后写约束如何叠加在软件层放大之上，是三放大在硬件层的根
