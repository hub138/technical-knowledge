---
title: 非关系数据库按访问模式选择数据模型
type: concept
status: active
updated: 2026-09-03
review_after: 2027-03-03
change_rate: medium
confidence: high
tags:
  - "data/nosql"
  - "databases"
  - "data/modeling"
sources:
  - "https://www.cs.cornell.edu/courses/cs6450/2012sp/papers/dynamo.pdf"
  - "https://redis.io/docs/latest/develop/data-types/"
  - "https://www.mongodb.com/docs/manual/data-modeling/"
---

# 非关系数据库按访问模式选择数据模型

键值、文档、宽列、图和时序系统不是“没有 schema 的数据库”，而是把 schema、查询、扩展和一致性边界放在不同位置。选型先从访问模式、数据关系、更新、事务、规模和运维能力出发，不能从产品标签推导设计。

同一份用户资料，如果主要按完整用户读取，文档模型可能减少连接；如果需要按多种关系、多租户条件和事务约束查询，反规范化的同步成本可能超过收益。模型选择是在访问路径和一致性成本之间做交换，不是把 SQL 换成另一种语法。

## 最小选型实验

用脱敏的真实查询分布建立 key-value、文档或关系模型的对照：固定数据量、热点比例、更新/读取比和故障预算，记录 p95/p99、跨分区请求、重复写、空间、备份恢复和开发复杂度。只有当主要访问路径和失败语义都通过，才把模型写入架构决策。

## 常见取舍

| 模型 | 擅长 | 代价 |
| --- | --- | --- |
| key-value | 已知 key 的低延迟读写 | 复杂查询和关系需要应用维护 |
| document | 聚合读取、局部嵌套变化 | 跨文档事务、重复数据和查询组合 |
| wide-column | 大规模分区读写、时间/键范围 | 查询必须围绕分区键建模 |
| graph | 多跳关系和邻接遍历 | 写入、分片、全局分析和运维复杂 |
| time-series | 时间窗口、压缩和保留策略 | 高基数标签、更新和跨域关联 |

反规范化是以写放大、同步和一致性换取读取路径；最终一致性、冲突解决、TTL、分区和重平衡必须写进业务语义。NoSQL 不能自动替代事务、约束、备份和审计。

## 案例回流：访问模式错配的缺陷实录

选型错误在缺陷域有直接对应物：[[工程知识/缺陷分析：从个案到体系/资源与容量/案例六：容量与泄漏——连接泄漏和 SST 堆积]] 里的 SST 堆积，正是"写多读少的 LSM 类存储被当成了高频点查缓存"的访问模式错配——LSM 的读路径要逐层查，点查密集时读放大失控。同类错配还有：把宽列存当强事务账本用（隔离语义对不上）、把文档库的多文档事务当跨实体一致性用。选型实验跑的每个访问模式，都应该对着缺陷案例里的失败形态过一遍。

## 量化锚点：读放大的账

LSM 类存储的点查成本 = memtable + 每层一个 SST（布隆过滤器命中时跳过），层数 L 的读放大约 L 次 IO；B+ 树类是树高 3-4 次 IO——点查密集（缓存、会话）场景 B+ 树常胜，写吞吐密集（日志、时序）场景 LSM 胜（顺序写 vs 随机写，差距 10 倍级）。罗盘的另一面：LSM 的 compaction 在后台吃 IO 预算，写入峰值撞上 compaction 就是延迟毛刺——[[工程知识/数据系统：在并发与故障中保存事实/事务与存储/LSM树用写放大换取顺序写和可扩展性]] 的账在这里直接兑现。

## 验证

验证的锚点：数据模型要与访问模式对账——预写按真实键分布与热点压测出的延迟与空间占用，用均匀分布假设推出的结论与预期对不上。

用真实 key/查询分布、热点、跨分区、重复/乱序、节点丢失、迁移和过期数据压测；比较一致性、p99、吞吐、空间、备份恢复和开发/值班成本。对 AI 记忆、检索缓存和向量元数据额外测租户/ACL 隔离。

关系：[[工程知识/数据系统：在并发与故障中保存事实/数据系统：在并发与故障中保存事实]] · [[工程知识/后端系统：在并发、失败与变化中维持服务/分布式可靠性/复制分片与再平衡改变数据归属]]

## 机制与本质

非关系数据库的机制层：数据模型把结构约束放在写入侧还是读取侧，决定了哪些查询能在存储层完成、哪些必须由应用自行维护。访问模式在选型之前就已经存在，跨分区查询、多文档事务与关系遍历的成本由模型与分区键共同决定；选定的模型决定了后续哪些需求无法通过扩容解决。

## 要解决的问题

键值、文档、宽列、图与时序系统常被当作没有结构约束的存储，按产品标签选定之后发现查询模式与数据关系无法支持。本篇回答：几类模型各自把结构约束与查询能力放在什么位置、访问模式与数据关系的哪些特征决定模型选择、一致性与扩展能力各自的边界在哪里，以及选型应当从哪些条件出发。

