---
title: LSM 树用写放大换取顺序写和可扩展性
type: concept
status: active
updated: 2026-09-03
review_after: 2027-09-03
change_rate: low
confidence: high
tags:
  - data/storage
  - lsm
  - databases
sources:
  - "https://www.cs.umb.edu/~poneil/lsmtree.pdf"
  - "https://rocksdb.org/docs/"
---

# LSM 树用写放大换取顺序写和可扩展性

要解决的问题是随机更新难以维持高写入吞吐：磁盘的随机写代价远高于顺序写，分析查询又需要数据有序。核心机制是先顺序写日志和内存表，再通过 compaction 合并有序文件；收益是写路径更适合高吞吐介质，代价是写放大、空间放大、读放大和后台合并竞争。验证要覆盖不同读写比例、键分布、compaction backlog 和故障恢复，只看顺序写速度会得出错误结论。

LSM 系统先把写入追加到内存结构和日志，再批量刷成不可变有序文件，通过 compaction 合并重叠范围。它把随机写变成顺序写，却引入读放大、空间放大、后台 I/O 和 compaction 尾延迟。

## 读写路径

```text
write -> WAL -> memtable -> immutable table
read  -> memtable + block cache + 多层 SSTable
merge -> compaction -> 新版本文件
```

Bloom filter、索引块和缓存减少无效读取；分层/大小分级 compaction 在写放大、空间和读放大之间取舍。删除通常先写 tombstone，直到 compaction 确认旧版本不可见。

分层的样子放到一张图里看，L0 的小文件怎么一层层合并成更大的文件：

![Wikipedia 条目图：LSM 树的分层 compaction——Level 0 的多个小文件向下合并，Level 1 的文件标注 sorted 且更大，Level 2 是单个更大的有序文件，箭头表示每层的合并方向](https://upload.wikimedia.org/wikipedia/commons/f/f2/LSM_Tree.png)

来源：Wikipedia「Log-structured merge-tree」条目。层越深文件越少越大、层内有序——这幅形态就是「同 key 多版本散布各层」的来源：读要逐层找，compaction 把上层小文件重写进下层大文件，写放大发生在每一次向下的箭头处；布隆过滤器挡掉的是「这一层根本不含此 key」的探测。

## 适用边界

写密集、顺序追加和横向扩展的工作负载常受益；高比例随机读、严格尾延迟或频繁更新热点可能受 compaction 影响。WAL 刷盘、压缩、文件数量、磁盘带宽和后台线程必须纳入容量计划。

## 可运行示例：一条写入的完整旅程

同一个 key 连续写三次（`put(k=order:42, v=v1)`、`v2`、`v3`）的旅程：

1. 写 WAL（顺序追加，崩溃恢复的保证），一次顺序写
2. 写 memtable（内存跳表，k→v3），零盘 IO
3. memtable 满，flush 成 L0 的 SST 文件，一次顺序写；此时盘上有两个 SST 都含 `order:42`（v1 在旧文件，v3 在新文件）
4. 读路径：memtable → L0 → L1 → ... 逐层找，新层的 key 覆盖旧层，读到 v3，但要探测每一层（布隆过滤器挡掉不含此 key 的层）
5. 后台 compaction：L0 与 L1 合并排序，同一 key 只留最新版，v1、v2 的空间在 compaction 时才真正释放

写路径全程顺序：这是 LSM 对 HDD/追加友好介质的价值来源；代价记在两处——读要逐层探测（读放大），compaction 要重写数据（写放大）。

跑一遍的收获：LSM 的"写快"有精确的来源（全程顺序追加），它的"读慢"同样有精确的来源（同 key 多版本散布各层）——布隆过滤器、缓存、compaction 策略都是在给后者打补丁，补丁的预算就是中央权衡的定价。

## 量化锚点与案例回流

收益的量级：顺序写与随机写在 HDD 上差两个数量级（百 MB/s 对百 KB/s），在 SSD 上差数倍——LSM 在机械盘时代是碾压性选型，在 SSD 时代优势收窄到"写吞吐与空间可扩展性"两项。补丁的账：布隆过滤器把点查的层探测从"每层一次 IO"压到"内存位图判定"（误判率 1% 时每层探测次数降到 1.01 次），compaction 的写放大典型 8-15 倍（与层数和每层倍率直接相关）。案例回流：compaction 追不上写入的实录见 [[工程知识/数据系统：在并发与故障中保存事实/事务与存储/读写三放大是存储引擎的中央权衡.md]] 的预算账——后台写带宽被写入速率吃满时 stall 出现，"写得好好的突然卡住"的标准病因；SST 回收的登记对账见 [[工程知识/性能工程：从用户等待到资源瓶颈/存储与网络/SST文件回收的对账：回收器要与登记表核对，不能只扫目录.md]]——compaction 删旧文件的对账纪律在文件系统层的形态。

## 验证

验证的锚点：存储引擎结论要与真实负载对账——预写按实际读写比例与键分布压测出的三放大倍数，用空库基准推断的值与预期对不上，不能作为依据。

按读写比例、key 分布、value 大小、更新/删除比例和磁盘类型压测；观察 p50/p99、写/读/空间放大、compaction backlog、WAL 恢复和重启时间。不要用空数据库 benchmark 推断生产表现。

关系：[[工程知识/数据系统：在并发与故障中保存事实/可靠性与运维/数据库基准必须描述负载与资源边界]] · [[工程知识/性能工程：从用户等待到资源瓶颈/存储与网络/存储IO性能取决于访问模式、队列与持久化语义]] · [[工程知识/数据系统：在并发与故障中保存事实/事务与存储/读写三放大是存储引擎的中央权衡]]

## 相关

- [[工程知识/性能工程：从用户等待到资源瓶颈/存储与网络/SSD的写放大来自介质要先擦后写|SSD的写放大来自介质要先擦后写]]——讲闪存介质层的写放大机制，与本篇软件层顺序写策略互补成完整写路径
- [[工程知识/数据系统：在并发与故障中保存事实/事务与存储/SSD与HDD的IO特性差异：随机写与写放大.md]] —— 软件层写放大与硬件层相乘
- [[工程知识/数据系统：在并发与故障中保存事实/事务与存储/存储引擎的两大路线：B树与LSM的写读取舍.md]] —— LSM 写放大的深水区与本篇的对照谱系
