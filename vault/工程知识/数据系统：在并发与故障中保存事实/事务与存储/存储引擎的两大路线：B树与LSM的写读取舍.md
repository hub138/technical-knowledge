---
title: 存储引擎的两大路线：B树与LSM的写读取舍
type: concept
status: active
updated: 2026-09-22
review_after: 2027-09-22
change_rate: low
confidence: high
tags:
  - lsm
  - database/storage
  - performance
sources:
  - "https://www.semver.org/"
  - "https://rocksdb.org/docs/"
  - "https://en.wikipedia.org/wiki/Log-structured_merge-tree"
---

# 存储引擎的两大路线：B树与LSM的写读取舍

## 要解决的问题

磁盘上的数据结构要同时服务写、读、空间三件事。B 树（原地更新）与 LSM（追加再合并）是两条根本不同的路线：**B 树用读性能换写吞吐，LSM 用写吞吐换读性能**，这个交换比不是常数，随读写比、点查与扫描比、空间预算变化。选错路线的代价是数量级的（写重负载用 B 树撑不住，读重负载用 LSM 读放大高）。本文写两条路线的机制、代价结构与选型判据，它们是数据库、缓存、消息队列存储层的共同底座。

## 机制

**B 树：原地更新的有序树**。多叉平衡树，页为节点（16KB 上下），根到叶 3-4 层即覆盖亿级行。写路径：找到叶子页 → 原地改 → 若页满则分裂，向上传播。读路径：树高对数，点查 3-4 次 IO 定位。写代价藏在原地更新：随机写（页分散在盘上）+ 页分裂的连锁（写放大 WAF：一次逻辑写可能触发页分裂传播、供未来读的预读失效）。B+树索引为访问路径服务（见 [[工程知识/数据系统：在并发与故障中保存事实/查询与索引/B+Tree索引为访问路径服务.md]]）写的是索引结构，本文写它作为存储路线的代价结构。

**LSM：追加+分层合并**。写路径：写 memtable（内存跳表）→ 满了刷成 SSTable（不可变、有序文件）→ 后台 compaction 把多层 SSTable 合并去重。读路径：memtable → L0（可能多个重叠文件）→ 逐层向下找，每层多个文件都可能命中同一键的旧版本——读放大（RAF）是 LSM 的命门：最坏情况查遍全部层。存储引擎的两大路线之外，LSM树用写放大换取顺序写和可扩展性（见 [[工程知识/数据系统：在并发与故障中保存事实/事务与存储/LSM树用写放大换取顺序写和可扩展性.md]]）已把 LSM 的写放大机制写透，本文补两条路线的对照谱系与选型。

**写读代价的结构性差异**。B 树：写随机（原地改，任意页）、读对数（树导航）。LSM：写顺序（memtable 吸收全部随机性，写入磁盘全是顺序大块）、读多路（层间+层内查找）。磁盘的物理特性（顺序快随机慢，见 [[工程知识/数据系统：在并发与故障中保存事实/事务与存储/SSD与HDD的IO特性差异：随机写与写放大.md]]）决定了：HDD 时代 LSM 的顺序写红利巨大；SSD 时代随机写不再那么贵，但大块顺序写仍是 SSD 的 FTL 友好模式（GC 效率高、WA 低），LSM 红利缩小但不消失。

**compaction 的调优三角**。LSM 的空间/写放大/读放大三者不可同时最小（这组三角关系见存量 LSM 篇（上文链接））。调优即选角：leveled compaction（RocksDB 默认：层间重叠小、读放大小，写放大高）；tiered（层内先攒再合，写放大小、读放大大）；FIFO（只按时间丢，纯日志场景）。现代引擎把三者做成参数谱（RocksDB 的 compaction priority、Cassandra 的 STCS/LCS/TWCS）。

**事务与崩溃恢复的路线差异**。B 树配 WAL（redo log）：写日志页缓存→WAL 先写入磁盘→数据页后台刷。LSM 本身就是日志结构（WAL→memtable→SSTable），崩溃恢复重放 WAL 到 memtable，不需要独立的 redo。两者都靠 WAL 先写原则保证持久性，但 LSM 的追加写让“页半写”（torn page，一页写一半断电）风险集中在 SSTable 的 CRC 校验上，B 树的原地写则要 doublewrite（InnoDB 的双写缓冲）防页损坏。

## 背景与代价

思想背景：B 树（1970，Bayer 与 McCreight）是为主存小、磁盘慢的时代设计的：树高即 IO 次数，多叉把树高压到 3-4。LSM（1996，O'Neil 与 Chen）来自日志结构文件系统（LSF）思想：磁盘的顺序吞吐远超随机，把随机写重写成顺序大块。两条路线在 2010s 融合：B 树系吸收 LSM 手法（InnoDB 的 change buffer 吸收随机写），LSM 系吸收 B 树手法（分区 SSTable、布隆过滤器压读放大）。

最精巧的一笔是**memtable 把随机性全部吸收到内存**。磁盘上没有一次随机写——全部写以 memtable 为缓冲转成顺序 SSTable。这一笔的前提是内存装得下热数据的工作集，代价是热数据溢出时 memtable 刷盘风暴（写停滞、write stall）。代价要摆明：LSM 的写吞吐红利不是免费的，它是向读、向空间、向后台 CPU 借来的。背景 compaction 与前台读写争 IO/CPU（大 compaction 期间 P99 劣化是 LSM 系的通病）；删除是墓碑（tombstone，标记不物理删），读要跨墓碑，彻底删要等 compaction——法规删除（GDPR 类）在纯 LSM 上是难题（要停写全量重写）。

## 边界

- **“LSM 快”默认指写吞吐，读没这么幸运**。布隆过滤器把不存在键的查找挡在多数层外，但存在键的最坏查找仍是全层扫描。点查重、扫描轻的负载（用户画像查询、配置读取）B 树的稳定读延迟更合适；写重读轻（日志、事件、时序）LSM 红利全开。范围扫描两者都行，LSM 的 SSTable 顺序性对大范围扫描甚至更快。
- **compaction 调优没有离线最优解，只有负载画像**。同一份参数在写重与读重负载下表现反转。上线路径：默认参数 → 观测实际 RAF/WAF/空间比 → 按三角取舍调整 → 回归 P99。参数文档（RocksDB tuning guide）给的是起点不是终点。
- **空间放大是 LSM 被低估的代价**。多版本并存（旧版本等 compaction 去重）让 LSM 实际磁盘占用显著高于逻辑数据量，监控磁盘容量要按“逻辑数据 × 空间放大系数”预估，容量规划打不足会撞 write stall。
- **路线选择影响上层一切**。事务实现（B 树的 MVCC 在页内、LSM 的 MVCC 在版本链）、备份（B 树物理备份 vs LSM 的 SSTable 快照）、EC 表达（LSM 的 SSTable 天然不可变、适合对象存储）。换引擎 = 换生态，选型时把运维工具链一并计入，见数据质量的三重校验（见 [[工程知识/数据系统：在并发与故障中保存事实/数据治理/数据质量的三重校验：完整性时效与一致性.md]]）的口径治理。

## 验证

- **写读代价对照实验**：同一数据集（1 亿键值对）分别用 B 树引擎（如 SQLite/MySQL）与 LSM 引擎（RocksDB 配默认 leveled）跑 100% 写、100% 点查、70/30 混合三组负载。预期：写负载 LSM 吞吐数倍于 B 树；点查负载 B 树延迟更稳（P99）；混合负载差异收窄。数字对不上先查 compaction 是否成为瓶颈（CPU/IO 观测）。
- **写停滞复现**：RocksDB 写入速率超限（p999 write stall 计数）压测，预期：write stall 计数上升、写入延迟出现长尾；调大 memtable 与后台 flush 线程后重测，预期 stall 缓解。stall 与参数的响应曲线是容量规划依据。
- **空间放大观测**：写入 100GB 逻辑数据，观察实际磁盘占用随时间演化（compaction 前后），预期：占用显著高于 100GB（空间放大 1.5-3x），compaction 后回落但稳定高于逻辑值。真实占用/逻辑数据的比值是容量系数。

## 相关

- [[工程知识/数据系统：在并发与故障中保存事实/事务与存储/LSM树用写放大换取顺序写和可扩展性.md]] —— LSM 写放大的深水区
- [[工程知识/数据系统：在并发与故障中保存事实/查询与索引/B+Tree索引为访问路径服务.md]] —— B 树索引的机制细节
- [[工程知识/数据系统：在并发与故障中保存事实/数据治理/数据质量的三重校验：完整性时效与一致性.md]] —— 引擎选择影响的数据口径
- [[工程知识/数据系统：在并发与故障中保存事实/查询与索引/列存与向量化执行：OLAP引擎快的两个来源.md]] —— 行存两大路线与列存的谱系全景
