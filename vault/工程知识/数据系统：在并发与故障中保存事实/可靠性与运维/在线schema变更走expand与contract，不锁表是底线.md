---
title: 在线schema变更走expand与contract，不锁表是底线
type: concept
status: active
updated: 2026-09-21
review_after: 2027-03-21
change_rate: medium
confidence: high
tags:
  - database/migration
  - database/operations
sources:
  - "https://www.percona.com/blog/online-schema-change-tools/"
  - "https://dev.mysql.com/doc/refman/8.0/en/innodb-online-ddl-operations.html"
  - "https://github.com/github/gh-ost/"
---

# 在线schema变更走expand与contract，不锁表是底线

表结构变更（加列、改类型、加索引）在百万行以上的表上不能直接执行：原生 DDL 可能长时间持锁，锁住的是线上全部读写。在线变更的方法论是 **expand-contract 两阶段**：先扩张（新旧结构并存，双写或触发器同步），再收缩（确认无旧读者后删旧结构）。中间用不锁表的工具做数据搬迁，把"一次危险的 DDL"变成"一组可暂停、可回滚的步骤"。

锁为什么不可接受：变更期间的锁与业务流量竞争，长事务持锁能让主从延迟堆积、连接池耗尽——这不是慢，是连锁故障。案例十一（上游修好了你这边没有）从另一个方向说明 schema 变更的风险：变更修复在不同发行分支上不同步，环境间的结构漂移本身就是缺陷来源。

## 四步流程

| 步骤 | 动作 | 关键检查 |
| --- | --- | --- |
| 1 扩张 | 新增列/表（只加不改），旧结构不动 | 加列是 DDL 里最安全的操作（元数据变更） |
| 2 双写 | 应用同时写新旧两份 | 双写的事务边界：同事务内，避免半写 |
| 3 回填 | 工具分批搬迁存量数据 | 批次大小随复制延迟动态调整 |
| 4 收缩 | 确认无旧读者后删旧列 | 删除前用访问审计确认零依赖 |

第 3 步回填是耗时主体，工程工具（gh-ost、pt-online-schema-change）的机制是**影子表加binlog 回放**：建新结构影子表、增量数据从 binlog 持续拷贝、拷完原子切换改名。它不锁原表的原因是读写一直走原表，切换瞬间才换指向。工具的选择与限制（复制延迟阈值、磁盘空间要双份表）在官方文档里各有明确边界。

第 4 步收缩最容易被跳过——"新列已上线，旧列留着以后再说"，然后旧列永久驻留、双写永久化、 nobody 敢删。收缩要有明确触发：读路径审计（连续 N 天无访问）后才删，审计方法与影响面估算同构（见 [[工程知识/缺陷分析：从个案到体系/影响判断/影响面估算先于修复——从改一行到伤多少服务]]）。

## 变更类型的危险分级

| 变更 | 危险度 | 原因 |
| --- | --- | --- |
| 加可空列 | 低 | 元数据操作，不重写数据 |
| 加索引 | 低-中 | 在线建索引，但消耗 IO 与复制带宽 |
| 改列类型 | 高 | 全表重写，兼容性要双阶段 |
| 改列语义（单位/编码） | 最高 | 数据本身要变换，"结构没变数据变了" |
| 删列/删表 | 中（时机错则高） | 不可逆，依赖审计要先做 |

语义变更（单位从分改成元）是最危险的一类：schema 工具帮不了忙，因为它看到的数据"类型兼容"，必须走完整 expand-contract 且中间态里两套语义并存（列名带单位后缀），确认全部读路径切换后才清理。

## 边界

在线变更的前提是**复制架构健康**：binlog 格式支持、主从延迟有余量、磁盘能容纳双份表。延迟敏感期（大促前）不做变更，变更窗口与业务日历对齐。

多实例与多环境的结构一致性要靠**声明式迁移脚本**（版本化的 migration 文件，每个环境按序执行），而不是手工在各环境执行。迁移脚本的版本治理与 API 演进的兼容性要求同构（见 [[工程知识/软件构建：让变化可以理解、验证与交付/架构与代码/API与Schema演进必须兼容新旧消费者]]）：脚本是代码，要过评审、要可回滚（每个 migration 配 down 脚本或明确标注不可逆）。

## 验证

验证的锚点：变更安全要与演练测量对账——预写四步各自耗时与主从延迟曲线应在阈值内，超出预期的批次大小要调小重跑，不能带阈值超标上线。

变更演练：在复制了线上数据规模的 staging 表上完整走一遍四步，测量每步耗时与主从延迟曲线。延迟超过阈值的批次大小要调小。

收缩审计验证：删列前开审计（general log 或 performance_schema 的表访问统计）跑一个完整业务周期，确认零访问再执行删除。

回滚验证：对每个 migration 预演回滚路径——expand 阶段的回滚是"停双写删新列"（低成本），contract 阶段的回滚是"重新启用旧列"（要求旧列还没删）。把可回滚窗口的边界写进变更方案。

## 要解决的问题

百万行以上的表执行原生结构变更时长时间持锁，锁住的是线上全部读写，而变更步骤在失败之后难以回退。本篇回答：扩张与收缩两阶段各自做什么、新旧结构并存期间读写怎样兼顾、数据搬迁在不锁表的前提下如何暂停与回滚，以及确认旧读者已消失的依据是什么。

## 相关

关系：[[工程知识/软件构建：让变化可以理解、验证与交付/架构与代码/API与Schema演进必须兼容新旧消费者]] · [[工程知识/缺陷分析：从个案到体系/影响判断/影响面估算先于修复——从改一行到伤多少服务]] · [[工程知识/数据系统：在并发与故障中保存事实/可靠性与运维/MySQL空间、容量与在线运维]] 的运维边界

- [[工程知识/数据系统：在并发与故障中保存事实/可靠性与运维/MySQL空间、容量与在线运维]] —— 在线运维的空间与容量前提
- [[工程知识/数据系统：在并发与故障中保存事实/可靠性与运维/InnoDB用日志连接事务、恢复与复制]] —— 变更工具依赖的 binlog 机制
- [[工程知识/软件构建：让变化可以理解、验证与交付/测试与交付/配置与特性开关分离部署与发布]] —— 双写开关的实现载体
- [[工程知识/缺陷分析：从个案到体系/兼容与升级/案例七：兼容与性能——滞后的依赖与放大的日志]] —— 结构漂移的代价实例
