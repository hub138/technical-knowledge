---
title: 数据库基准必须描述负载与资源边界
type: playbook
status: active
updated: 2026-08-31
review_after: 2027-02-28
change_rate: medium
confidence: high
sources:
  - "https://dev.mysql.com/doc/refman/8.4/en/performance-schema.html"
  - "https://dev.mysql.com/doc/refman/8.4/en/optimization.html"
  - "https://www.tpc.org/tpc_documents_current_versions/pdf/tpc-c_v5.11.0.pdf"
tags:
  - "database/mysql"
  - "database/benchmark"
  - "database/mysql"
---

# 数据库基准必须描述负载与资源边界

基准测试的价值是比较一个明确的变化，不是生成一个脱离负载的“数据库分数”。任何结果都必须同时说明工作负载、数据规模、并发、缓存状态、硬件、版本和正确性检查。

## 指标分层

| 层 | 指标 | 用来回答 |
| --- | --- | --- |
| 用户体验 | p50/p95/p99 延迟、错误率 | 用户是否感到变慢？尾部是否恶化？ |
| 吞吐 | QPS/TPS、提交事务数、队列等待 | 单位时间完成多少有效工作？ |
| 数据库 | rows examined、buffer pool 命中、锁等待、redo/checkpoint、复制延迟 | 数据库在哪一层受限？ |
| 系统 | CPU、内存带宽、磁盘延迟/队列、网络重传 | 是否是资源边界而非 SQL 本身？ |
| 成本 | 每千次请求资源和费用、恢复时间 | 改善是否值得？ |

不要只看 Threads_connected、平均延迟或 Seconds_Behind_Master 一个数。先用 SHOW PROCESSLIST、慢日志、Performance Schema、执行计划和系统指标对齐同一时间窗口，再判断根因。

## 基准设计

1. 明确目标：例如“p99 小于 200 ms 且错误率低于 0.1%”，而不是“尽可能高的 QPS”。
2. 固定版本、schema、索引、数据分布、客户端、连接池、隔离级别和参数。
3. 预热与正式阶段分开，记录缓存、checkpoint、后台刷盘和复制是否稳定。
4. 一次只改变一个关键变量；至少运行多轮并报告波动范围，而非只挑最好一次。
5. 同时校验事务比例、结果正确性、约束、数据量和副作用，防止“跑得快但做错事”。
6. 将原始报告和脚本放在来源层，把可复用的结论写回本页或对应主题页。

## 现场排查顺序

~~~text
用户延迟/错误
 -> 请求与 SQL 样本
 -> 执行计划和实际行数
 -> 锁/事务/连接池
 -> Buffer Pool、redo、磁盘和复制
 -> CPU、内存、网络和调度
 -> 单变量实验与回归
~~~

## TPC-C 的正确用法

TPC-C 是特定在线事务负载和合规规则的基准，不等于所有业务的性能。使用 tpcc-mysql、BenchmarkSQL 等工具时，先核对工具实现是否符合目标规范，再记录事务混合、规模、预热、测量窗口和一致性检查。原始学习稿中的比例、延迟和数据规模只作为线索，不能直接当作官方门槛。

## 关联页面

- [[工程知识/数据系统：在并发与故障中保存事实/查询与索引/一条SQL如何穿过优化器与存储引擎]]
- [[工程知识/数据系统：在并发与故障中保存事实/查询与索引/B+Tree索引为访问路径服务]]
- [[工程知识/数据系统：在并发与故障中保存事实/事务与存储/事务隔离决定并发读写能观察到什么]]
- [[工程知识/性能工程：从用户等待到资源瓶颈/观测与诊断/性能问题定位]]
- [[工程知识/软件构建：让变化可以理解、验证与交付/测试与交付/测试策略从风险选择证据]]

## 可运行示例：同一个基准两种结论

TPC-C 类基准（订单-库存事务）在两种资源边界下跑，结论完全相反：

```text
同一硬件（32C/128G/NVMe），同一 TPC-C 装载（100 仓库）：

跑法 A：无限内存池（buffer pool = 数据全量，热数据全在内存）
  → tpmC 45 万，瓶颈在 CPU —— 结论：这库 CPU 效率高

跑法 B：真实边界（buffer pool = 16G，数据 100G，热数据 60% 在内存）
  → tpmC 18 万，瓶颈在 IO —— 结论：这库 IO 调度好

差异的根源：跑法 A 测的是"内存数据库的 CPU 路径"，
跑法 B 才是生产形态（热数据从来不是 100%）。两个数字都真实，都不可比。
```

跑一遍的收获：基准报告的每一行数字必须能回答三件事——负载是什么（事务构成、读写比、行数分布）、资源边界是什么（内存池、并发连接数、磁盘型号）、预热了多久（缓存冷热差 2-5 倍）。答不出这三件事的数字没有比较价值。

## 量化锚点与案例回流

可复现性的账：同一基准两次跑的差异应 <5%（超过就是边界没锁住——后台 compaction、checkpoint、autovacuum 都是噪声源），测三轮取中位数是最低纪律。容量类数字的换算：tpmC 到业务的换算要乘事务复杂度差（TPC-C 一个事务 5 张表、电商下单常 10+ 张），"tpmC 45 万等于支撑日均 4 亿单"这类直线换算是错的，正确用法只做同构对比（同负载模型下 A 与 B 比）。案例衔接：[[工程知识/数据系统：在并发与故障中保存事实/可靠性与运维/恢复点目标要先算账：RPO 是数据的时间边界，不是备份频率的同义词]] 的算账纪律与本篇同源——都是"承诺之前先定边界"；[[工程知识/缺陷分析：从个案到体系/资源与容量/案例六：容量与泄漏——连接泄漏和 SST 堆积]] 是边界没锁住时容量假设失效的实录。

机制与本质：基准数字的价值取决于负载与资源边界是否被完整描述，本质问题在于后台任务与缓存状态这类未锁定的条件会把两次运行的差异做到超过测量本身。

## 验证

1. 负载描述：基准报告的每一行数字都能回答负载构成（事务类型、读写比、行数分布）、资源边界（内存池、连接数、磁盘型号）与预热时长三项，答不出的数字不进入比较。
2. 可复现性：同一基准连续跑三轮，记录两次之间的差异，预期在 5% 以内；超出时先定位 compaction、checkpoint、autovacuum 等后台任务。
3. 单变量对照：只改动一项条件（如内存池大小）再跑一次，记录瓶颈从 CPU 转移到 IO 的完整过程，确认差异可归因到该变量。
4. 正确性断言：基准过程中同时校验数据正确性，避免吞吐数字建立在不一致的中间状态上。
5. 断言锚点：报告含负载与资源边界描述，波动范围有记录，比较围绕单一变更，正确性检查与性能数字一并输出。

## 效果与代价

把负载、规模、并发、缓存状态与版本一起记录，换来的是两次测试之间的差异可以归因到某一个变量，容量结论只在描述清楚的边界内成立。代价是基准脚本与报告需要连同数据分布和预热条件一并留存，正确性断言与波动范围也要进入报告，否则数字仍然不可比。

## 要解决的问题

数据库性能数字被当作可迁移的结论使用，换到真实负载之后差异消失或者方向相反，无法判断两次测试之间究竟变了什么。本篇回答：工作负载由哪些维度描述、数据规模与并发与缓存状态怎样影响结果、硬件与版本为什么要一起记录、正确性检查为什么不能省略，以及比较要围绕哪一个明确的变更。

