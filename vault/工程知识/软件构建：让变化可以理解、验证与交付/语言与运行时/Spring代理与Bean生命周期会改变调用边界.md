---
title: Spring 代理与 Bean 生命周期会改变调用边界
type: concept
status: active
updated: 2026-08-31
review_after: 2027-02-28
change_rate: medium
confidence: medium
sources:
  - "https://docs.spring.io/spring-framework/reference/core/beans.html"
  - "https://docs.spring.io/spring-framework/reference/data-access/transaction.html"
  - "https://docs.oracle.com/en/java/"
tags:
  - "java"
  - "spring"
  - "backend/architecture"
---

# Spring 代理与 Bean 生命周期会改变调用边界

它解决的是对象创建、配置、事务和横切逻辑在大型 Java 服务中重复实现且容易失控的问题。核心机制是容器管理 Bean 生命周期并通过代理织入调用边界；收益是依赖和事务治理集中，代价是隐式调用路径、启动顺序和代理限制增加。验证要覆盖同类内部调用、异步边界、事务提交后的副作用和版本迁移。

## 核心模型

Spring 的价值在于依赖注入、生命周期管理、配置和横切能力；它不替代领域边界、事务设计和可观测性。Bean 生命周期通常经历实例化、依赖注入、Aware/后处理器、初始化、使用和销毁，代理可能改变调用路径和事务边界。

## 事务边界

- 事务应围绕一个完整的持久化不变量，而不是包住网络调用或长计算。
- `@Transactional` 依赖代理调用；同类内部直接调用可能绕过代理，事务不生效。
- 明确传播、隔离、只读、超时和回滚异常；数据库隔离级别仍由数据库实际配置决定。
- 异步、事件和事务提交后的副作用要通过 outbox、消息确认或补偿机制协调。

## 配置和发布

- 配置按环境分层，密钥进入密钥管理系统，不写入仓库和日志。
- 兼容升级遵循“先兼容读写，再迁移数据，最后删除旧路径”；锁定 JDK、Spring、构建插件和驱动版本。
- 健康检查区分存活与就绪；启动、关闭、线程池、连接池和数据库迁移都要有超时。

## 同类内部调用为什么绕过代理

`@Transactional` 织入的是**代理对象**上的方法调用。Spring 在创建 Bean 时，如果该类需要事务增强，容器实际注入的是代理实例；外部通过代理调用方法时，事务拦截器先开启事务再执行目标方法。

但同一个类内部用 `this.method()` 调用，走的是**对象自身的引用**，不经过代理，拦截器不会触发。这不是 Spring 的缺陷，而是 Java 方法调用的语义——代理只在从外部进入时才有机会介入。

最小复现：

```java
@Service
public class OrderService {

    // 外部调用：经过代理，事务生效
    @Transactional
    public void placeFromOutside() {
        repository.save(...);
        throw new RuntimeException("rollback");  // 回滚
    }

    // 同类内部调用：绕过代理，事务不生效
    public void place() {
this.placeFromOutside();   // 事务未开启，数据已写入数据库
    }
}
```

调用 `placeFromOutside()` 抛异常会回滚；调用 `place()` 则不会——两者执行的是同一个方法体，差别只在于**入口是否经过代理**。

三种修法，代价不同：注入自身代理（`@Lazy private OrderService self;` 后调 `self.placeFromOutside()`，简单但引入自引用）；把方法移到另一个 Bean（边界最清晰，代价是多一个类）；改用 AspectJ 编织（不依赖代理，代价是构建复杂度）。

## 示例与版本的使用原则

旧课程和示例工程不作为当前 API 或生产配置。重写示例时先锁定 JDK、Spring、构建插件和数据库驱动版本，再以当前官方文档验证 Bean 生命周期、代理事务和迁移行为；默认密码、绝对路径、旧注解组合和日志配置不复制到新项目。

## 量化锚点与案例回流：代理边界的账

量化锚点：代理带来的调用开销与行为差异的量级——JDK 动态代理走反射调用，单次开销微秒级（相对业务逻辑可忽略，但在每秒百万次的极热点路径上可观，CGLIB 字节码子类调用更快）；代理拦截器链每多一层（事务、日志、安全各一层），调用栈多一层穿透。生命周期时序的量化：`@PostConstruct` 在依赖注入完成后、代理最终形态确定前执行，`ApplicationReadyEvent` 在全部 Bean 就绪后——两个时点之间所有 Bean 的初始化都可能穿插。

案例回流：内部调用绕过代理的实录——`this.doQuery()` 从同类内部调用带 `@Transactional` 的方法，事务不生效（代理包裹的是外部入口，内部 this 直达原始对象），数据不一致在并发下现形，修复为注入自身代理或改用另一个类。初始化时序的实录——`@PostConstruct` 里启动后台线程读其他 Bean 的配置，读到的是默认值（对方 Bean 的配置绑定在其 `@PostConstruct` 里更晚执行），启动完成事件后再启动线程才稳定。代理失效的家族实录与 [[工程知识/软件构建：让变化可以理解、验证与交付/架构与代码/AST提供语法结构，语义仍需符号与运行证据.md]] 的语法与语义分叉同构——源码看到的调用关系（语法）与运行时实际走的对象（语义）之间隔着代理与生命周期两层改写，运行证据是唯一裁判。

## 边界

代理机制的适用前提是**调用从容器管理的边界进入**。以下情况它不再成立：对象是 `new` 出来而非容器创建（无代理）；方法是 `private`、`static` 或 `final`（CGLIB 无法覆盖）；自调用（如上一节）；以及 Bean 尚在初始化阶段就被提前引用（代理可能未织入完成，表现为部分增强丢失）。

`@Transactional` 也不能替代事务设计：它只说明"这段方法在一个事务里"，不说明事务边界是否覆盖了完整的持久化不变量。把网络调用或长计算包进事务，会让连接池在慢调用上堆积——这是常见且难以在测试环境暴露的失效模式。

## 验证

验证的锚点：代理边界要与回滚事实对账——预写同类内部调用后检查数据库是否真的回滚，只测外部入口而内部调用未回滚，与预期对不上。

- 同类内部调用后检查数据库是否真的回滚，而不是只测外部入口。
- 检查 `@Transactional` 方法的 `private`/`final` 修饰与自调用路径。
- 事务提交后再触发的副作用（消息、缓存、外部 API）单独验证，确认失败时有补偿或 outbox。
- 启动与关闭阶段检查线程池、连接池、数据库迁移的超时行为，确认就绪探针在迁移完成前不通过。

## 关联

关系：[[工程知识/软件构建：让变化可以理解、验证与交付/架构与代码/分层与依赖方向决定可测试性和演进成本]] · [[工程知识/后端系统：在并发、失败与变化中维持服务/服务设计/API接口约定定义输入输出和演进边界]]

- [[工程知识/软件构建：让变化可以理解、验证与交付/软件构建：让变化可以理解、验证与交付]]
- [[工程知识/数据系统：在并发与故障中保存事实/数据系统：在并发与故障中保存事实]]
- [[工程知识/数据系统：在并发与故障中保存事实/事务与存储/事务边界应覆盖完整业务用例]] —— 事务该包住什么
- [[工程知识/后端系统：在并发、失败与变化中维持服务/服务设计/流量入口只负责路由与连接，不负责业务正确性]] —— 调用边界的另一半
- [[工程知识/软件构建：让变化可以理解、验证与交付/测试与交付/发布门禁必须绑定真实证据]] —— 启动与关闭的验证归属

## 要解决的问题

加了事务注解的方法在同类内部被调用之后没有回滚，数据已经写入；横切逻辑要靠注解维持，调用路径却被代理改写，排查时看到的调用栈与源码不一致；配置、生命周期与事务治理集中在容器里，对象的创建顺序和代理是否织入完成却难以从代码上看出来。本篇回答：容器管理 Bean 生命周期的哪些阶段、代理在什么位置介入调用、哪些调用形式会绕过代理、事务边界应当包住什么、以及启动关闭阶段的超时与探针如何验证。

