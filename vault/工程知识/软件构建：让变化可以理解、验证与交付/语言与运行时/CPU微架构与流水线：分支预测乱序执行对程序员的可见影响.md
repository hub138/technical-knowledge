---
title: CPU微架构与流水线：分支预测乱序执行对程序员的可见影响
type: concept
status: active
updated: 2026-09-22
review_after: 2027-09-22
change_rate: low
confidence: high
tags:
  - hardware
  - performance/cpu
sources:
  - "https://www.intel.com/content/www/us/en/developer/articles/technical/intel-sdm.html"
  - "https://developer.arm.com/documentation"
  - "https://en.wikipedia.org/wiki/Hennessy%E2%80%93Patterson"
---

# CPU微架构与流水线：分支预测乱序执行对程序员的可见影响

## 要解决的问题

流水线与乱序执行要解决的问题，是**让快的部件不等慢的部件**。一条指令的执行被切成五段（取指、译码、执行、访存、写回），不同指令的不同段可以重叠：第 1 条在执行时第 2 条在译码、第 3 条在取指。理想情况吞吐翻数倍。但两个东西会打断重叠：分支（下一条指令是谁取决于本条结果，预测错了已取的指令全作废）与慢指令（cache miss 要等几百周期，后面的指令全堵着）。分支预测与乱序执行分别救这两个坑：前者猜方向让流水线继续取指，后者把后面的独立指令提到前面先执行。它们对程序员的可见影响，是代码的**实际性能与代码顺序脱钩**——写得顺的代码不一定跑得快，数据布局与分支形态比算法复杂度常数更重要。CPU缓存与分支决定有效执行时间（见 [[工程知识/性能工程：从用户等待到资源瓶颈/处理器与内存/CPU缓存与分支决定有效执行时间.md]]）写的是怎么测，本文写机制本身与它的可见后果。

## 机制

**流水线：重叠执行与断流代价**。五段流水线让 IPC（每周期指令数）逼近 1（标量）或更高（超标量，多发射）。断流（stall）的两个来源：数据相关（后一条要前一条的结果，必须等）与控制相关（分支没解析，不知道取谁）。断流的代价是清空与重填流水线，十几到几十周期。

**分支预测：猜错了全作废**。分支预测器（现代 CPU 的预测准确率 95%+，多级历史表 TAGE 类）提前猜分支方向与目标。猜对，流水线无感；猜错（mispredict），清空流水线重新取指，代价 15-20 周期。可见影响：高度可预测的分支几乎免费，不可预测的数据依赖分支是吞吐杀手。排序后二分查找败给线性扫（小数组）的经典反直觉案例，根源就是分支预测——线性扫的分支模式固定（全顺序），预测器全中；二分的分支看数据（每次走向不同），预测器频繁失手。

**乱序执行：把等待变成机会**。指令窗口（ROB，reorder buffer，数百条目）里，引擎扫描哪些指令的输入已就绪，就绪就发到执行单元，不管它在程序顺序里的位置。慢指令（cache miss）在等内存时，后面几十条独立指令已经执行完。可见影响一：**内存访问的重排**。CPU 不保证按程序顺序访存——写 a 后写 b，别的核可能先看到 b 后看到 a。多核可见性靠内存屏障（memory barrier）显式约束，这直接解释了并发编程为什么需要屏障与原子指令：语言层面的 happens-before 语义（见 [[工程知识/软件构建：让变化可以理解、验证与交付/语言与运行时/内存模型决定并发读写何时可见.md]]）往下追到微架构，就是乱序引擎的这个行为。

**投机执行与它的幽灵**。分支预测猜方向，流水线就投机执行猜中的路径，猜错回滚。Spectre/Meltdown（2018）证明投机执行的回滚不干净：投机路径上碰过的缓存行留下了侧信道痕迹，可被测量反推出秘密数据。这是"性能优化的可见影响"里最戏剧的一例——乱序引擎为了快留下的物理痕迹变成了安全边界洞，软件层要用屏障与猜测屏障堵。

## 背景与代价

思想背景：流水线思想来自 1960s 的 CDC 6600（Thornton 的 scoreboard 记分板：乱序的雏形）与 IBM 360/91（Tomasulo 算法，1967：寄存器重命名让乱序实用化）。RISC 运动（1980s）把指令切成等长段，流水线如虎添翼。深流水线（Pentium 4 的 31 级）在 2000s 撞墙（功耗与 mispredict 代价），行业转向多核，单核微架构在宽发射（6-8 宽）与大 ROB（500+ 条目）上继续。

最精巧的一笔是**寄存器重命名**。程序里的变量名是逻辑的，物理寄存器更多。写 a 时引擎映射到新物理寄存器，旧值还在（别的读者用），WAR/WAW 假相关被消除，真相关（RAW）才等待。这一笔让"乱序"不用改程序语义（程序顺序的表象保留，提交按序），只重排执行时机。代价要摆明：预测与乱序的收益集中在**可预测、可并行**的负载；指针追逐（pointer chasing）、分支密集的解释器、依赖链长的串行计算，乱序引擎无事可做，IPC 跌回 0.2-0.5。写代码时"给引擎喂可预测的东西"（数据布局连续、分支模式稳定、依赖链短）是微架构友好的基本功，数据布局的微架构友好实践见 [[工程知识/性能工程：从用户等待到资源瓶颈/处理器与内存/CPU缓存与分支决定有效执行时间.md]] 的测量部分。

## 边界

- **IPC 高不等于效率高**。乱序引擎会把忙等循环也跑出高 IPC（每周期都在执行自旋指令）。性能判断要结合 stall 原因（perf stat 的 branch-misses、cache-misses），见测量方法论（见 [[工程知识/性能工程：从用户等待到资源瓶颈/处理器与内存/CPU缓存与分支决定有效执行时间.md]]）。IPC 单指标会骗人。
- **分支预测是统计数据不是承诺**。预测器在负载变化（分支模式突变）时要几十条分支才重新训练，负载切换瞬间（请求类型变化）的吞吐凹陷是预测器失手的可见信号。benchmark 的分支行为与线上不同（输入分布不同），实验室数据外推要打折扣。
- **乱序窗口有限**。ROB 几百条目，miss 后能遮蔽的独立指令就这么多。多个 miss 叠加（指针追逐链）时窗口耗尽，流水线彻底停滞。MLP（memory level parallelism）是这个场景的解药：软件层预取或显式并行让多个 miss 重叠。
- **微架构行为不构成编程约定**。预测器实现、重排策略、窗口大小每代 CPU 都变，同一段代码在不同代的性能排序可能反转。依赖微架构细节的优化（手工对齐、时序假设）是脆弱的，写进文档要标注"微架构相关，换 CPU 需重测"。

## 验证

- **分支预测代价实测**：对同一数组做"先排序后查找"与"不排序直接查找"，条件分支命中率天差地别。预期：小数组（几千元素内）线性扫快于二分，perf stat 的 branch-misses 线性扫低一个数量级。数字对不上就查编译器是否优化掉了分支（-O2 可能向量化改写循环）。
- **乱序遮蔽实测**：串行依赖链（b 依赖 a，c 依赖 b）与独立计算（a/b/c 互不依赖）同指令数对比，预期：独立的吞吐数倍于依赖链。这是乱序引擎"提独立指令到前面"的最直接证据。
- **访存重排实测**：两个核分别写 x、y，另一核观察到的写入顺序与程序顺序不符（需要精心构造的乱序窗口，如 IRIW 案例的简化版）。做不出来不代表重排不存在——多数架构上要特定指令组合才触发，可用 litmus test 工具（herd7）在模型层面验证。

## 相关

- [[工程知识/性能工程：从用户等待到资源瓶颈/处理器与内存/CPU缓存与分支决定有效执行时间.md]] —— 测量这套机制的方法论
- [[工程知识/软件构建：让变化可以理解、验证与交付/语言与运行时/内存模型决定并发读写何时可见.md]] —— 微架构重排在语言层的约定化
- [[工程知识/性能工程：从用户等待到资源瓶颈/处理器与内存/NUMA让跨槽访问付出带宽与延迟代价.md]] —— 跨槽访存的代价表与伪共享的区分
- [[工程知识/AI 系统工程：从模型能力到生产能力/模型基础与训练/GPU执行模型与显存层级：SIMT与合并访存.md]] —— CPU 与 GPU 两条优化路线的对照
