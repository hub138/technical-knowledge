---
title: API 与 Schema 演进必须兼容新旧消费者
type: playbook
status: active
updated: 2026-09-03
review_after: 2027-03-03
change_rate: medium
confidence: high
tags:
  - software/architecture
  - api
  - migrations
sources:
  - "https://martinfowler.com/articles/evodb.html"
  - "https://protobuf.dev/programming-guides/proto3/"
  - "https://json-schema.org/specification"
---

# API 与 Schema 演进必须兼容新旧消费者

## 核心拆解

| 维度 | 回答 |
| --- | --- |
| 要解决的问题 | 线上新旧客户端、worker、缓存和回滚版本会并存，一次性替换字段或语义会让部分消费者无法读取或产生错误解释。 |
| 本质 | 把不可逆变更拆成可观测的兼容阶段，让数据和代码在迁移窗口内同时满足新旧契约。 |
| 做法 | 先扩展可选字段/双读，再迁移消费者和数据，确认覆盖后停止旧写入并删除旧路径；记录消费者、停止条件和回滚组合。 |
| 效果与代价 | 支持灰度、暂停和回滚，降低发布爆炸半径；代价是双读/双写、迁移状态、短期重复逻辑和兼容测试成本。 |
| 边界 | schema 校验通过不代表业务状态兼容；数据库事务不能覆盖远端 API，字段删除、默认值和排序语义改变仍可能破坏消费者。 |
| 验证 | 保留旧客户端/worker 做契约回归，测试部分发布、回填中断、双写不一致、重试和回滚后新旧版本的交互。 |

线上变化通常不是一次性替换：旧客户端、旧 worker、缓存、消息、数据库和回滚版本会与新代码并存。兼容设计把迁移拆成可观察的小步，避免“发布成功但旧消费者无法读取”的事故。

兼容性的真实对象不是字段表，而是消费者对默认值、错误、排序、幂等和状态转移的全部假设；只验证 schema 能解析，仍可能让旧 worker 误处理新状态。

## 最小演进实验

让旧客户端、旧 worker 和回滚版本同时运行，依次执行 expand、回填中断和 contract；记录读写结果、错误分类、重复副作用和回滚后的可见数据。

## Expand / Migrate / Contract

1. **Expand**：增加可选字段、新表/索引或双读路径，保持旧读写可用。
2. **Migrate**：灰度新消费者、回填数据、双写或事件重建，比较事实和延迟。
3. **Contract**：确认所有消费者已迁移，停止旧写入，最后删除字段/路径。

字段删除、类型收窄、枚举重编号、默认值变化、排序/分页语义改变和消息顺序改变都可能是破坏性变更。数据库事务不能覆盖远端 API，schema 通过也不能证明业务状态兼容。

## 验证

保留旧版本客户端/worker 做契约回归；测试部署顺序、部分发布、重试、回滚、数据回填中断和双写不一致。每次变更记录消费者、迁移进度、停止条件和删除时间，不靠“应该没人用了”。

关系：[[工程知识/后端系统：在并发、失败与变化中维持服务/服务设计/API契约定义输入输出和演进边界]] · [[工程知识/软件构建：让变化可以理解、验证与交付/测试与交付/发布门禁必须绑定真实证据]]
- [[工程知识/软件构建：让变化可以理解、验证与交付/架构与代码/API演进的兼容性纪律：废弃流程与版本策略.md]] —— schema 兼容是机制面，废弃流程是纪律面
