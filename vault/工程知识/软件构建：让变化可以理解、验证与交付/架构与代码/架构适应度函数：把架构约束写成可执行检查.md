---
title: 架构适应度函数：把架构约束写成可执行检查
type: playbook
status: active
updated: 2026-09-22
review_after: 2027-09-22
change_rate: slow
confidence: high
tags:
  - architecture
  - software-quality
  - quality
  - continuous-integration
  - delivery
sources:
  - "https://www.thoughtworks.com/radar/techniques/architecture-fitness-functions"
  - "https://www.archunit.org/userguide/html/000_Index.html"
  - "https://nealford.com/books/buildingevolutionaryarchitectures/"
---

## 要解决的问题

架构图上画的分层（controller 不直连 dao、模块间单向依赖），三个月后代码里就有人绕过。code review 能挡住看得见的违规，挡不住悄悄长出来的环形依赖、跨层调用、被随手加进核心模块的第三方依赖。问题不是缺规范，是规范停在文档里：**规范不可执行，违规就不可发现，架构腐化是常态而非事故**。适应度函数把"架构必须保持的性质"（分层、依赖方向、模块边界、技术选型边界）写成自动化检查，跑在 CI 里，违规即红灯。问题的本质：把架构从"文档与评审约束"升级为"可执行约束"，让架构决策与代码改动之间有持续验证的闭环。

## 机制

**三类检查物**。结构检查（依赖与分层）：ArchUnit（Java）、import-linter（Python）、dependency-cruiser（JS/TS）解析代码的 import/依赖图，断言"web 层不依赖 dao 层""模块间无环""共享内核只被白名单引用"。这些工具的规则即断言（noCycles、layeredArchitecture、classes()...shouldNotDependOnClassesThat），DSL 直接表达架构约束。指标检查：模块耦合度（传入传出依赖数）、包间循环、变更耦合（git 历史上两个模块频繁同改，提示边界划错）。选型边界检查：依赖白名单（这个服务不许引 Kafka 客户端）、技术雷达的 ring 约束（评估、试用、采用、 hold），用 lint 到 CI 的门禁实现。

**执行位置决定约束强度**。本地 lint（即时反馈、可跳过）→ pre-commit（强制、可绕过）→ CI 门禁（强制、难绕过）→ 生产运行时检查（适应度函数的运行时形态：混沌工程注入验证弹性、可观测性 SLO 即运行时适应度）。同一个约束写在不同位置，语义从"建议"升到"合同"。

**反馈的三个设计点**。失败信息要能定位到违规点与规范出处（哪条规则、哪个模块、为什么有这条规则）；豁免机制要显式（annotation suppress、白名单文件，豁免记录进注释与到期时间）；规则演进与架构演进同步（架构决策变了，适应度函数先改，再放行代码——顺序不能反）。

## 背景与代价

思想背景：Fitness Function 借自遗传算法（衡量基因适应度的函数），Neal Ford、Rebecca Parsons、Patrick Kua 在《Building Evolutionary Architectures》（2017）引入软件架构，思想源头可追到 Michael Nygard 的 "ultra-large scale" 与治理自动化讨论。工具生态随后落地：ArchUnit（2017+，Java 社区）、dependency-cruiser、import-linter，ThoughtWorks 技术雷达持续跟踪。

最精巧的一笔是**把架构治理从"人的评审"改写成"机器的持续检查"**：评审是采样（只看提交到面前的），适应度函数是全量（每次 CI 全代码库检查）；评审的强度随人疲劳衰减，机器不衰减。这一笔让"演进式架构"成立——允许架构变，但每次变都被适应度函数验证"想保的性质没丢"。代价要摆明：**规则全集是第二份架构描述，有维护成本与漂移风险**（架构改了规则没改，门禁变成阻碍；反过来规则松了门禁失效）；过度约束把本可局部决定的依赖关系全部锁死，小团队的灵活被门禁吃掉；结构性检查有盲区（它查依赖图，不查"模块职责是否还在膨胀"，语义腐化依赖评审）；import 解析对动态语言的天花板（Python 的运行时导入、JS 的动态 require，静态检查有覆盖缺口，需要运行时验证补位）。**适用判定：多人协作的中大型代码库、架构边界即团队边界（康威定律生效处）收益最大；单人原型与探索期项目，门禁收益不抵摩擦**。

## 边界

- **先立规则再自动化，不是反过来**。规则从现有架构提取（依赖图现状即基线），先锁"不允许变差"（ratchet 模式：现状之上的新增违规零容忍，存量逐步清），再收紧到目标态。一上来写理想态规则，全库飘红，团队学会的第一件事是绕过门禁。
- **适应度函数验证架构性质，不验证业务正确性**。业务逻辑的测试归测试金字塔，两者混写会让规则库变成第二个测试套件，职责不清、维护翻倍。
- **粒度对齐架构决策的粒度**。决策是"分层"就查分层，是"服务边界"就查跨服务调用面（API 合同），是"数据所有权"就查数据库访问白名单。粒度错位（用类级规则表达服务级决策）规则爆炸且没人看得懂。
- **跨仓库架构（微服务）需要平台级检查**。单仓库工具只看单库，跨库约束（A 服务只许通过网关调 B）落在平台层：服务网格的策略、API 网关的配置校验、依赖清单的集中审计。单库工具挑不动跨库的担子。

## 验证

- **规则有效性实验**：在 ArchUnit 写分层规则，故意提交一个跨层调用（controller 直连 dao）。预期：CI 红灯，错误信息定位到具体类与规则名。约束可执行性的直接证据。
- **依赖图基线提取**：对存量代码库跑 dependency-cruiser 的依赖图导出，与架构图人工对照。预期：图上有架构图没有的边（现实与文档的漂移量），漂移点即需要决策的存量违规。规则从现状提取的方法验证。
- **ratchet 模式实测**：survival rate 配置（只对新代码生效），观察 4 周存量违规数的走向。预期：新增违规为零、存量缓慢下降。渐进收紧策略的行为证据。

## 相关

- [[工程知识/软件构建：让变化可以理解、验证与交付/测试与交付/静态分析的证据边界：它证明什么不证明什么.md]] —— 静态分析提供证据，适应度函数是它在架构域的定向应用
- [[工程知识/软件构建：让变化可以理解、验证与交付/测试与交付/灰度与功能开关的边界：交付节奏与配置漂移.md]] —— 门禁挡违规提交，灰度挡事故发布，两道闸位置不同
- [[工程知识/软件构建：让变化可以理解、验证与交付/架构与代码/购买与自建的决策框架：每条约束都摆上桌面.md]] —— 选型边界是适应度函数的一类检查物，决策框架是它的上游
- [[工程知识/软件构建：让变化可以理解、验证与交付/架构与代码/错误处理的分类学：错误类型与恢复策略的映射.md]] —— 异常只留给编程错误的约定可入适应度检查
- [[工程知识/软件构建：让变化可以理解、验证与交付/架构与代码/领域建模的落地路径：从事件风暴到聚合边界.md]] —— 跨聚合不得共事务的边界纪律可写成适应度检查
- [[工程知识/后端系统：在并发、失败与变化中维持服务/服务设计/微服务拆分的判据：从团队认知到数据边界.md]] —— 拆分后的边界纪律可用适应度函数持续检查
