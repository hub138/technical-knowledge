---
title: 设计模式解决的是变化点的隔离，不是代码复用
type: principle
status: active
updated: 2026-09-15
review_after: 2027-03-15
change_rate: low
confidence: high
tags:
  - backend/design-patterns
  - backend/architecture
  - skill/mastery
sources:
  - "https://refactoring.guru/design-patterns"
  - "https://martinfowler.com/articles/patterns-of-distributed-systems/"
---

# 设计模式解决的是变化点的隔离，不是代码复用

例如，一个订单服务最初直接调用 `WechatPay.pay()`。半年后要接支付宝，再过半年要接企业账期。每加一种支付方式，就要改 `OrderService` 里的那段 `if`，连带重跑所有订单测试。

这就是变化点没有隔离。**模式不是为了让代码"更优雅"，是为了让新增一种支付方式时，已有的订单逻辑一行都不用改。**

## 背景：变化是唯一确定的事

软件里唯一不会变的是"需求会变"。但变化不是均匀的——总有几个地方特别容易变，其他地方常年不动：

| 系统 | 高频变化点 | 常年稳定 |
| --- | --- | --- |
| 电商 | 支付方式、优惠规则、物流商 | 订单状态机、库存扣减语义 |
| 内容平台 | 推荐策略、审核规则 | 内容存储、发布流程 |
| SaaS | 计费模型、集成的第三方 | 用户身份、权限模型 |

**模式的作用就是在这两者之间划一条线**，让变化点被关在线的一侧。

判断该不该用模式，只问一句：**这个地方未来会以什么方式变？** 如果答不上来，就不要用——猜错的变化点比不用模式更糟，因为它引入了额外的抽象层却没换来任何隔离。

## 方案：先识别变化点，再选模式

不要从"我要用策略模式"开始，从"这里会怎么变"开始：

| 如果变化的是… | 用什么隔离 | 代价 |
| --- | --- | --- |
| 一个算法/规则的多种实现 | 策略 + 依赖注入 | 多一层接口，调用方要拿到正确的实现 |
| 对象怎么被创建 | 工厂 / 抽象工厂 | 构造逻辑藏起来，排查时多一跳 |
| 一个事件有哪些后续动作 | 观察者 / 发布订阅 | 执行顺序不再直观，调试要查订阅表 |
| 要给一批操作都加上同样的处理 | 装饰器 / 中间件 | 调用链的层数增加，调用栈变深 |
| 外部系统的模型和你的不一致 | 适配器 / 防腐层 | 多一层转换代码，要同步维护 |
| 流程骨架固定、步骤可变 | 模板方法 / 钩子 | 父类成为新的耦合点 |
| 行为随状态改变，且状态较多 | 状态机 | 需要显式建模状态和迁移 |
| 操作要支持撤销、排队、重放 | 命令 | 每个动作变成对象，类数量增加 |

## 效果与代价

**收益**

- **开闭原则的实施**：新增实现不改已有代码，回归范围从"整个服务"缩到"新加的那一个类"
- **可测试性**：变化点被接口化后，测试可以注入 mock，不必依赖真实的第三方
- **团队并行**：不同人可以在各自的实现上工作，不用抢同一个文件的锁

**代价（真实存在，别忽略）**

- **间接层成本**：每个模式都至少多一层。5 个模式叠在一起，一个请求要走 8 层才能到业务逻辑，新人看代码会崩溃
- **过度设计的风险**：为"以后可能会变"提前抽象，是最大的浪费。**YAGNI 是对的模式使用纪律**——等到第二次真的要加同类变化时再抽象，因为只有到那时你才知道真正的变化轴在哪
- **调试变难**：`策略 + 装饰器 + 观察者` 组合后，一个 bug 的定位路径可能是"找到装饰器→确认策略被谁注入→查观察者回调顺序"

## 从什么地方做

**第一步：列出变化历史，而不是预测未来**

不要问"以后可能怎么变"，去翻 git log：过去一年这个模块改了几次？每次改的是什么？**过去的变化模式是未来最好的预测器。**

**第二步：给变化点命名**

找到高频改动的地方后，明确写出来："支付方式会变"、"审核规则会变"。能命名清楚才说明真的理解了变化轴。

**第三步：只在第二次重复时抽象**

第一次需求：直接写。第二次同类需求：这时候才抽象，因为你已经看到两个具体案例，知道真正的差异点在哪。**第三次：验证抽象是否正确。**

**第四步：用测试锁定边界**

抽象之后，写一组测试只针对接口（"任意支付方式都能完成支付"），另一组针对具体实现。新增实现时只加后一种测试。

## 判断抽象对没对

抽象错了的典型症状：**每次新增实现，都要改接口本身**。

比如 `PaymentStrategy` 加了支付宝后，发现需要传 `channelId`，于是给所有实现都加了参数——这说明接口抽象时漏了概念。**正确的做法是承认当初的抽象不完整，重构它**，而不是继续往里塞参数。

好的抽象应该是：**新增一种实现时，只需新增文件，不改任何已有代码**。

相关：[[工程知识/软件构建：让变化可以理解、验证与交付/架构与代码/策略与依赖注入把变化关在边界外]] · [[工程知识/软件构建：让变化可以理解、验证与交付/架构与代码/分层与依赖方向决定可测试性和演进成本]]
- [[工程知识/软件构建：让变化可以理解、验证与交付/架构与代码/领域建模的实施路径：从事件风暴到聚合边界.md]] —— 聚合是变化点隔离思想在业务建模层的放大

验证的锚点：隔离效果要与新变化对账——下一类变化到来时，看改动是否被圈在既定的变化点内（改动文件数与波及面与预期对账），对不上的变化点说明隔离位置选错，要重新划定，继续加补丁只会加深问题。

## 可运行示例与案例回流：变化点隔离的账

```text
一个折扣计算模块，三种形态面对同一变化（新增会员折扣类型）：

形态 A（switch 平铺）：
  if type == "full" ... elif type == "percent" ... elif ...
  新增会员折扣：改核心函数 → 全量回归 → 影响面 = 所有折扣逻辑

形态 B（策略模式，变化点已隔离）：
  每种折扣一个策略类，注册进折扣工厂
  新增会员折扣：加一个类 + 注册一行 → 影响面 = 新类自身
  核心计算流程零改动 → 回归范围缩小到折扣计算单元

形态 C（过度设计）：
  为"未来可能的变化"预设三层抽象（工厂之上再套建造者）
  新增会员折扣：穿过三层抽象各改一遍 → 比形态 A 还重
  → 为不存在的变化付的复杂度是负资产
```

案例回流：变化点选错的实录——把"支付渠道"当变化点做了策略隔离，实际频繁变化的是"风控规则"（每季度改），渠道三年没动过；结果是稳定的部分被抽象包裹（多余间接层），多变的部分散在核心流程里（每次改风控都全量回归）——变化点要按实际变更历史选（git log 里哪个目录改得最勤），按架构美感选必错位。过度设计的实录同源：预设六种未来变化建了六层抽象，三年后只有一种发生，五层抽象成为每次改动的穿行税（与 [[工程知识/软件构建：让变化可以理解、验证与交付/架构与代码/策略与依赖注入把变化关在边界外.md]] 的注入点判据衔接：隔离点要在第二个真实变化出现时再抽象，第一个变化出现时先平铺）。

## 验证

最小验证：在已抽象的变化点上新增一种实现，断言改动只涉及新文件与装配位置，已有代码零改动。若新增实现需要修改接口本身，判定抽象不完整并重新定义接口。

成本核对：统计从入口到业务逻辑经过的层数，超过预先设定的层数上限时复核每一层对应的变化点是否真实存在。

## 机制与本质

模式的作用是把一处会重复变化的位置从调用路径中取出来，使新增一种情况对应新增一个实现。识别变化点依据的是历史改动记录，而每次新增实现时是否需要改动接口，是判断抽象是否准确的直接依据；间接层的成本随抽象数量累积，因此抽象的位置与数量都需要依据。

## 要解决的问题

每次新增一种支付方式或审核规则，都要改动同一个函数里的条件分支，并重跑该函数的全部测试，改动之间互相牵连。本篇回答：变化点怎样从历史改动中识别、抽象在什么时机进行、抽象是否准确的判断依据是什么，以及间接层带来的成本如何控制。

