---
title: 策略与依赖注入把变化关在边界外
type: pattern
status: active
updated: 2026-09-15
review_after: 2027-03-15
change_rate: stable
confidence: high
tags:
  - backend/design-patterns/strategy
  - backend/dependency-injection
  - skill/mastery
sources:
  - "https://martinfowler.com/articles/injection.html"
  - "https://blog.ploeh.dk/2010/02/02/RefactoringtoAggregateServices/"
---

# 策略与依赖注入把变化关在边界外

## 背景：那段越写越长的 if

一个定价服务，最初只有一个规则：

```go
func Price(o Order) Money {
    return o.BasePrice
}
```

三个月后变成：

```go
func Price(o Order) Money {
    p := o.BasePrice
    if o.UserLevel == VIP { p = p * 0.9 }
    if o.Coupon != nil { p = p - o.Coupon.Amount }
    if isPromotionPeriod(time.Now()) { p = p * 0.8 }
    if o.Channel == "app" { p = p - 5 }
    return p
}
```

问题不是代码长，是**每加一条规则都要改同一个函数，所有定价的测试都要重跑**。而且 `Price` 现在依赖了用户等级、优惠券、时间、渠道——它知道得太多了。

## 方案：把每条规则变成可替换的实现

策略模式的核心动作只有一个：**把"怎么做"从调用方移到被注入的实现里**。

```go
type PriceRule interface {
    Apply(o Order, current Money) Money
    Name() string
}

func Price(o Order, rules []PriceRule) Money {
    p := o.BasePrice
    for _, r := range rules {
        p = r.Apply(o, p)
    }
    return p
}
```

装配在组合根完成（应用启动时），业务代码只依赖接口：

```go
rules := []PriceRule{
    VIPDiscount{}, CouponDeduct{}, PromotionDiscount{}, AppRebate{},
}
```

这样新增一条规则 = 新增一个实现了 `PriceRule` 的文件 + 在装配处加一行。**`Price` 函数本身零改动。**

## 依赖注入：谁来提供实现

策略定义了"怎么变"，依赖注入解决"谁来决定用哪个实现"。三种方式：

| 方式 | 怎么做 | 适用 | 陷阱 |
| --- | --- | --- | --- |
| 构造器注入 | 依赖通过构造函数传入 | 必需依赖，**默认选择** | 构造参数可能变多 |
| 方法参数注入 | 每次调用时传入 | 依赖每次不同（如请求上下文） | 调用方要自己找依赖 |
| 容器/框架注入 | Spring、Guice 等自动装配 | 大型应用减少样板代码 | 依赖关系变隐式，难以静态分析 |

**优先用构造器注入**。原因很实际：依赖关系显式写在签名里，看构造参数就知道这个类依赖什么；测试时直接传 mock，不需要启动容器。

服务定位器（`ServiceLocator.Get<IPayment>()`）要避开——它把依赖从签名里藏起来，调用方不知道自己到底需要什么，测试也必须先配置全局状态。

## 效果与代价

**收益**

- `Price` 函数的测试从"覆盖所有规则组合"变成"验证规则被按序应用"——用一组假规则就能测
- 每条规则可以独立测试，互不干扰
- 规则可以在运行时按配置启用/禁用（灰度、AB 实验）

**代价**

- **规则之间可能有隐式顺序依赖**：`先打折再减券` 和 `先减券再打折` 结果不同。接口上看不出顺序要求，要靠文档或显式建模（比如给规则加 `Order()` 字段）
- **类数量增加**：每条规则一个类。4 条规则 = 4 个文件，看起来比一个 20 行函数"重"
- **过度使用的信号**：如果某个策略永远只有一个实现，且没有测试替身的需求，那这个接口就是纯负担

## 从什么地方做

1. **找到变化轴** —— 翻 git log，看这个模块过去半年改的是什么。是"新增规则"还是"改某个规则的参数"？前者适合策略，后者直接改就行
2. **先写两个具体实现再抽接口** —— 只有一个实现时抽象，多半会猜错接口
3. **定义接口时问"实现方需要什么"** —— 如果实现方需要访问数据库、配置等，通过构造器注入给它，不要让 `Apply` 方法参数越来越长
4. **装配放在一处** —— 所有的 `rules := []PriceRule{...}` 集中在一个文件/包里，不要在业务代码里零散地 new
5. **用测试锁定不变的部分** —— 写测试验证"任意规则组合都能被按序应用"，这样以后加规则不会破坏框架

## 常见错误

**错误 1：策略里塞满了不同维度的变化**

一个 `PriceRule` 既处理折扣又处理税费又处理运费——这不是策略，这是把原来的大函数拆成了三个大函数。应该按变化轴分开：`DiscountRule`、`TaxRule`、`ShippingRule`。

**错误 2：用字符串/enum 分发替代多态**

```go
switch rule.Type {
case "vip": ...
case "coupon": ...
}
```

这又回到了原点。每个分支还是会碰同一个 switch。**判断标准：新增一种类型时，你要改几个地方？如果超过一个，就是没隔离。**

**错误 3：接口方法过多**

`PriceRule` 被塞进 `Apply`、`Validate`、`Describe`、`Serialize` 四个方法，于是每个实现都要写一堆自己不需要的方法。应按**接口隔离原则**拆成多个小接口，让实现只依赖它需要的。

相关：[[工程知识/软件构建：让变化可以理解、验证与交付/架构与代码/设计模式解决的是变化点的隔离]] · [[工程知识/软件构建：让变化可以理解、验证与交付/架构与代码/分层与依赖方向决定可测试性和演进成本]]

相关（容器实现）：[[工程知识/软件构建：让变化可以理解、验证与交付/语言与运行时/Spring代理与Bean生命周期会改变调用边界]] —— 依赖注入在容器中落地时的代理与生命周期约束
