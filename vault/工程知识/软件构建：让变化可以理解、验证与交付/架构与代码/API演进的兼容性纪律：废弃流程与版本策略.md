---
title: API演进的兼容性纪律：废弃流程与版本策略
type: principle
status: active
updated: 2026-09-22
review_after: 2027-09-22
change_rate: low
confidence: high
tags:
  - backend/api
  - compatibility
  - architecture
sources:
  - "https://semver.org/lang/zh-CN/"
  - "https://google.aip.dev/162"
  - "https://docs.microsoft.com/en-us/azure/architecture/best-practices/api-design"
---

## 要解决的问题

接口一改，所有调用方一起崩。加个字段没人反对，删个字段全球追责。API 是生产者与消费者之间的合同，改合同比改代码难一个量级：代码的调用关系编译器兜底，API 的消费关系散落在别人的系统里，你没有他们的代码。问题的本质：**API 演进是跨组织的变更管理，兼容性是合同条款，废弃是要走流程的社会工程**。没纪律的 API 会在"不敢改"与"随便改"之间摆荡：前者冻结成化石，后者炸遍下游。

## 机制

**兼容的两个方向**。向后兼容（新增不破坏旧消费者）：加字段（可选字段带默认值）、加端点、加响应字段——旧消费者忽略新东西，继续工作。向前兼容（旧生产者不破坏新消费者）：新消费者容忍字段缺失（可选语义、默认值兜底）——这要求消费端对未知/缺失有宽容协议（Protobuf 的 unknown field 保留、JSON 的宽松解析）。**破坏性变更**（删字段、改类型、改语义、改必填性）是合同违约，需要版本升级或废弃流程。

**语义化版本是合同号**。MAJOR.MINOR.PATCH：MAJOR 含破坏性变更（消费者必须改代码），MINOR 向后兼容的新增（消费者可选用），PATCH 修复。SemVer 是模块/包层面的约定，对 HTTP API 常用 URL 版本（/v1/）或 header 版本（Accept: application/vnd.x.v2+json）承载 MAJOR 号。**版本号是承诺的载体：升号即承诺，消费者按号决策升级节奏**。

**废弃是流程，是删字段前的一段路**。成熟 API 平台的废弃纪律（Google AIP-162 为代表）：标记（Deprecated 头/字段标注/文档声明）→ 通知（消费方监控废弃告警，changelog 与邮件）→ 宽限期（时长写明，6 个月到数年，给下游排期）→ 迁移指引（新旧映射表、自动化迁移工具）→ 下线（到期后返回 410 Gone 或直接删除）。**每一步都有观测**：废弃字段的调用率下降曲线决定下线安全性，调用率不降说明下游没动，下线就是事故。

**变更的语义红线比结构红线更隐蔽**。结构兼容（字段还在）不等于语义兼容：字段的含义变了（米改厘米）、取值域变了（枚举加值）、错误码语义变了（404 从"无此资源"变"无权限伪装"）、时间戳格式变了（秒改毫秒）、分页语义变了（offset 改 cursor）。**结构由 schema 校验兜底，语义靠文档约定与消费者测试兜底**——接口约定测试在 CI 里跑真实消费场景，语义漂移才会被抓住。

## 背景与代价

思想背景：SemVer（Tom Preston-Werner，2013 标准化）把散乱的版本约定收成一份规格；Google AIP（API Improvement Proposals）与其内部 API 演进纪律（2016 年后公开）代表平台级实践；HTTP 生态的 deprecation 标准化（RFC 8594 Deprecation header，Sunset header）在协议层补位。REST 与 RPC 两大生态都沉淀了废弃流程模板（Stripe 的 API 版本策略是商业 API 的标杆：版本固定、迁移窗口数年）。

最精巧的一笔是**把"删除"改写成"有观测的退役流程"**：化石 API 的问题不是技术，是没人知道删了会炸谁。废弃流程给删除动作装上仪表（调用率监控）与缓冲（宽限期），把不可逆的社会工程变成有数据的工程决策。代价要摆明：多版本并存的成本（每个 MAJOR 版本是一份完整代码路径，bug 要修 N 处，测试矩阵翻倍；Stripe 数年窗口的代价是全家桶维护）；**宽容协议的复杂度转嫁**（unknown field 保留、可选语义让消费端代码满是 null 分支，复杂性从生产者转到消费者）；版本策略的过度设计（内部 API 抄商业 API 的数年窗口，没必要——内部 API 的消费方可强制同步升级，窗口几天到几周就够）。**适用判定：外部/开源 API 纪律越重越好，内部 API 纪律按组织半径缩放**——半径内靠协调，半径外靠合同。

## 边界

- **破坏性变更不总是要升 MAJOR**。安全漏洞的字段删除、法务要求的清理（GDPR 数据字段），可以走"短窗口强制下线"，但要在 release note 与告警里写明理由。纪律服务于工程，不是教条。
- **gRPC/Protobuf 的兼容语义自带一部分**。字段编号保留不复用（deleted field reserved）是协议层纪律，比 JSON API 少一档结构风险，但语义红线照样要人守。
- **版本共存期的新功能双写**。v2 出来后 v1 还活着时，新功能做不做 v1？纪律：v1 进入维护态（只修 bug 不加功能），逼升级的引力来自新功能的排他。双写全部功能等于把 MAJOR 升级变成空话。
- **消费者侧的兼容责任**。生产者守纪律，消费者也要守：不解析未声明的字段（或显式容忍 unknown）、不依赖响应字段的顺序、错误处理不 match 具体错误文案。脆弱消费者让最好的生产者纪律也防不住。

## 验证

- **接口约定测试跨版本实验**：消费者测试套件对 v1 与 v2 各跑一遍（同一套业务场景断言）。预期：结构兼容的变更（加字段）在两版本都绿；语义变更（改枚举含义）在接口约定测试里红——接口约定测试抓住结构之外的红线。语义红线的可执行验证。
- **废弃调用率观测实验**：标记废弃字段后接入调用率监控（按消费者维度），跑一个宽限期。预期：调用率曲线分形态（自觉消费者快速归零、滞留消费者不动、僵尸消费者无人认领），曲线决定下线决策与沟通对象。退役流程仪表化的实测。
- **SemVer 违规扫描**：用 API diff 工具（openapi-diff、buf breaking）对比两个版本的真实 schema。预期：工具报出"删字段/改必填"类 breaking 变更，与 release 声明的版本号对照，可发现"MINOR 号里藏 MAJOR 变更"的违诺。版本承诺的可审计证据。

## 可运行示例与案例回流：演进的账

```text
一次字段废弃的三步推演账（通用形态）：

  第一步 加而不删：新字段 `owner_v2` 与旧字段 `owner` 并存发布（新旧消费者都能用）
  第二步 停写观察：服务端停止写 `owner`，监控其读取量 →
    三个消费方已迁移，一个内部脚本还在读 → 通知迁移
  第三步 删：读取量归零后从 Schema 删除 → 完成一轮演进
  版本策略账：URL 大版本（/v2/）留给破坏性变更；
    小演进走兼容规则（只加可选字段）
  兼容测试账：CI 里用新旧两版消费者回放真实流量样本，跑不兼容即拦
```

案例回流：直接删字段的实录——服务端一次性删掉响应里的 `total_count`，三个内部调用方当场报错（通知发了但没验证读取量归零）；改为"停写观察-归零-再删"流程后同类故障消失，迁移方也因此有明确截止信号。静默改语义的实录——字段名不变、枚举值从 `"ACTIVE"` 扩到 `"INACTIVE, PENDING"`，老消费者 switch 进入 default 分支静默归零处理（加枚举也是不兼容变更，消费者要按未知值报错，吞掉等于掩盖）。版本泥球的实录——URL 版本从 v1 堆到 v7，每版全量复制代码，修 bug 要改 7 处；大版本只收破坏性变更 + 内部按兼容规则演进后收敛回两个版本。约定的系统性视角见 [[工程知识/软件构建：让变化可以理解、验证与交付/架构与代码/API与Schema演进必须兼容新旧消费者.md]]、消费者的容错视角见 [[工程知识/软件构建：让变化可以理解、验证与交付/架构与代码/静态分析发现结构问题但不能证明行为正确.md]]（行为正确性要靠回放验证）。

## 相关

- [[工程知识/软件构建：让变化可以理解、验证与交付/架构与代码/API与Schema演进必须兼容新旧消费者.md]] —— 存量篇展开 schema 兼容机制，本篇补废弃流程与版本策略的纪律面
- [[工程知识/软件构建：让变化可以理解、验证与交付/测试与交付/接口约定测试的适用边界：消费者驱动与提供者验证.md]] —— 接口约定测试是兼容性纪律的可执行化身
- [[工程知识/软件构建：让变化可以理解、验证与交付/架构与代码/错误处理的分类学：错误类型与恢复策略的映射.md]] —— 410 Gone 与 4xx 族的语义稳定性是错误分类的接口侧约定
- [[工程知识/软件构建：让变化可以理解、验证与交付/开发工具/依赖升级的半衰期策略：跟随与锁定的平衡.md]] —— 升级策略是 API 纪律的消费侧
