---
title: 适配器与防腐层隔离外部模型
type: pattern
status: active
updated: 2026-09-15
review_after: 2027-03-15
change_rate: stable
confidence: high
tags:
  - backend/design-patterns/adapter
  - backend/acl
  - backend/integration
  - skill/mastery
sources:
  - "https://refactoring.guru/design-patterns/adapter"
  - "https://martinfowler.com/bliki/BoundedContext.html"
---

# 适配器与防腐层隔离外部模型

## 背景：第三方 SDK 的类型渗进了业务代码

接了微信支付，代码里到处是：

```go
func PlaceOrder(o Order) error {
    req := &wxpay.UnifiedOrderRequest{   // 第三方类型
        OutTradeNo:  o.ID,
        TotalFee:    int(o.Amount * 100),
        Body:        o.Desc,
        TradeType:   "JSAPI",
        SpbillCreateIP: o.ClientIP,
    }
    resp, err := wxpayClient.UnifiedOrder(req)
    if resp.ReturnCode == "SUCCESS" && resp.ResultCode == "SUCCESS" {
        // ...业务代码
    }
}
```

半年后要接支付宝，发现：

- `wxpay.UnifiedOrderRequest` 出现在 15 个文件里
- 金额单位是"分"，而业务代码用的是 `Money` 类型，转换散落各处
- 判断成功要检查 `ReturnCode` 和 `ResultCode` 两个字段——**这个规则被复制了 8 次**
- 更麻烦的是：微信的 `OutTradeNo` 长度限制 32 位，我们的订单 ID 是 UUID（36 位），这个约束被硬编码在了业务代码里

**第三方模型渗进了领域层。** 换供应商的成本等于重写。

## 方案：在系统边界上做翻译

### 适配器：解决接口不匹配

把第三方接口包装成自己的接口：

```go
// 我们的领域接口
type PaymentGateway interface {
    Charge(ctx Context, req ChargeRequest) (ChargeResult, error)
}

type ChargeRequest struct {
    OrderID     OrderID
    Amount      Money      // 带货币单位
    Description string
}

// 微信适配器：翻译发生在这一层
type WechatAdapter struct{ client *wxpay.Client }

func (a *WechatAdapter) Charge(ctx Context, r ChargeRequest) (ChargeResult, error) {
    resp, err := a.client.UnifiedOrder(&wxpay.UnifiedOrderRequest{
        OutTradeNo:  truncate32(string(r.OrderID)),   // 第三方约束在这里处理
        TotalFee:    r.Amount.ToFen(),                // 单位转换在这里
        Body:        r.Description,
        TradeType:   "JSAPI",
    })
    if err != nil {
        return ChargeResult{}, classifyError(err)     // 错误也翻译成领域错误
    }
    // 第三方的"双字段成功判断"只在这一处
    if resp.ReturnCode != "SUCCESS" || resp.ResultCode != "SUCCESS" {
        return ChargeResult{}, ErrPaymentRejected
    }
    return ChargeResult{TransactionID: resp.TransactionID}, nil
}
```

### 防腐层（ACL）：比适配器更彻底

适配器解决"接口形状不同"，**防腐层解决"概念模型不同"**。

差别在这：

| | 适配器 | 防腐层 |
| --- | --- | --- |
| 解决 | 函数签名不匹配 | **概念模型污染** |
| 做法 | 包装调用 | 定义自己的模型，双向翻译 |
| 范围 | 单个接口 | 整个外部系统的边界 |

防腐层的关键是：**业务代码里完全不出现第三方的类型和术语。**

```go
// 领域模型：用我们自己的语言
type Payment struct {
    OrderID   OrderID
    Amount    Money
    Status    PaymentStatus  // Pending / Succeeded / Failed / Refunded
}

// 防腐层负责双向翻译
//   我们的 PaymentStatus ←→ 微信的 trade_state（含 9 种状态）
//   我们的 Money         ←→ 微信的 TotalFee（分，int）
```

**翻译表要显式写出来**，这是防腐层最有价值的产出：

```go
var statusMapping = map[string]PaymentStatus{
    "SUCCESS":    StatusSucceeded,
    "REFUND":     StatusRefunded,
    "NOTPAY":     StatusPending,
    "CLOSED":     StatusFailed,
    "REVOKED":    StatusFailed,
    "USERPAYING": StatusPending,
    "PAYERROR":   StatusFailed,
    // 未知的第三方状态 → 明确标记为 Unknown，而不是猜
}
```

### 放在哪一层

```
领域层：   Payment、Order、Money        ← 不含任何第三方类型
   ↑
防腐层：   PaymentGateway 接口 + 翻译    ← 接口定义在领域，实现在基础设施
   ↑
第三方：   wxpay.Client、alipay.Client   ← 被关在最外层
```

**接口定义在领域侧（或独立的 port 包），实现放在基础设施层。**

## 效果与代价

**收益**

- **换供应商成本可控**：新增一个适配器，业务代码零改动
- **第三方约束被集中处理**：`truncate32`、单位转换、状态映射只在一处
- **可测试**：业务代码用假的 `PaymentGateway` 测试，不需要真的调第三方
- **领域语言纯净**：业务代码里只有 `Payment`、`Money`，没有 `OutTradeNo`

**代价**

- **多一层翻译代码**：每个外部系统一个适配器 + 一套映射表
- **翻译可能丢信息**：第三方的 9 种状态压缩成我们的 4 种，**细节会丢失**。要在转换时保留原始值（存 raw 字段）以备排查
- **同步维护**：第三方 API 升级时，适配器和映射表都要跟着改
- **过度使用的风险**：内部系统之间也加防腐层，会把简单调用复杂化

## 从什么地方做

**第一步：找出业务代码里的第三方类型**

搜 import 里的第三方包，看它们的类型有没有出现在 service/领域层。**出现即污染。**

**第二步：定义自己的领域接口**

站在业务这边问："我需要支付网关做什么？" 写下最小接口（通常 2-4 个方法）。

**第三步：写适配器，把所有翻译集中进去**

单位、ID 长度、状态映射、错误分类——**全部放在适配器里，一处都不要漏到外面。**

**第四步：保留原始响应**

```go
type Payment struct {
    // ...领域字段
    RawResponse string   // 原始的第三方响应，用于排查
}
```

翻译丢信息是必然的，保留原始值能救回来。

**第五步：对未知状态显式处理**

第三方返回了没见过的状态 → **标记为 Unknown + 告警**，而不是默认映射成 Failed 或 Succeeded。

## 常见错误

**错误 1：把第三方 DTO 直接当领域对象用**

`Order` 就是 `wxpay.Order` —— 换供应商时全盘重写。**必须有自己的模型。**

**错误 2：翻译逻辑散落各处**

`int(amount*100)` 出现在 8 个文件里。**单位转换只能在适配器里出现一次。**

**错误 3：静默吞掉未知状态**

`default: StatusFailed` —— 第三方新增了一种"处理中"的状态，被当成失败。**未知要显式标记。**

**错误 4：防腐层里写业务逻辑**

适配器里开始判断"VIP 用户打九折"——这是领域逻辑，混进防腐层后会难以测试和复用。**防腐层只做翻译。**

**错误 5：对所有外部系统都做防腐层**

内部微服务之间、同团队维护的系统，**直接调用 + 共享契约就够了**。防腐层的成本只在应对"不受控的外部变化"时才值得。

相关：[[工程知识/软件构建：让变化可以理解、验证与交付/架构与代码/设计模式解决的是变化点的隔离]] · [[工程知识/软件构建：让变化可以理解、验证与交付/架构与代码/分层与依赖方向决定可测试性和演进成本]]
