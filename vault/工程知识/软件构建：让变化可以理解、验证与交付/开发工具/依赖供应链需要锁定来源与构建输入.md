---
title: 依赖供应链需要锁定来源与构建输入
type: playbook
status: active
updated: 2026-09-03
review_after: 2026-12-03
change_rate: medium
confidence: high
tags:
  - software/dependencies
  - supply-chain
  - reproducibility
sources:
  - "https://slsa.dev/spec/v1.0/"
  - "https://docs.npmjs.com/cli/v11/using-npm/package-lock-json"
  - "https://maven.apache.org/guides/introduction/introduction-to-dependency-mechanism.html"
---

# 依赖供应链需要锁定来源与构建输入

## 核心要点

| 维度 | 回答 |
| --- | --- |
| 要解决的问题 | 构建结果会受传递依赖、镜像、脚本、工具链和仓库状态影响；仅记录“安装成功”无法复现、审计或安全回滚。 |
| 本质 | 把源码之外的每一个可影响产物的输入固定成可追溯身份，并在隔离环境中验证其行为和来源。 |
| 做法 | 使用 lockfile、版本/哈希/签名、可信 registry、工具链清单、SBOM 和构建证明；升级先评估许可证、漏洞、兼容和回滚。 |
| 效果与代价 | 产物可重建、风险可定位、升级可回退；代价是锁文件维护、存储证明、扫描和供应商治理成本。 |
| 边界 | 锁版本不能证明依赖无漏洞或行为适合业务；缓存命中、扫描绿色和包能安装都不是完整安全结论。 |
| 验证 | 在干净环境构建两次，比较产物与输入；注入恶意脚本、缓存污染、源不可用和升级回滚，检查门禁证据。 |

依赖管理解决的不只是“把包下载下来”，还包括版本解析、传递依赖、许可证、漏洞、构建脚本、镜像、缓存和可重现性。一个短暂的网络成功不能成为产物身份。

一个 lockfile 相同的构建仍可能因 registry、平台 wheel、编译器、环境变量或安装脚本不同而产生不同产物；供应链治理的目标是知道每个输入是谁、从哪里来、做过什么，而不只是让命令今天跑通。

## 构建输入

```text
源码 revision + lockfile + registry/mirror
      + toolchain + compiler flags + generated files + environment
```

锁文件固定解析结果，但仍需校验仓库来源、完整性、签名/证明和平台差异。依赖升级要区分安全补丁、行为变化和破坏性版本，记录直接/传递依赖和回滚组合。

## 风险与门禁

- 安装/构建脚本可能执行代码；在隔离环境和最小权限下运行。
- 依赖混淆、恶意接管、typosquatting 和缓存污染需要私有源策略和名称保护。
- 漏洞扫描有误报/漏报，不能替代版本、调用路径和可利用性分析。
- 生成代码、基础镜像、模型权重和插件也属于供应链输入。

## 可运行示例：一次锁文件对账

lock 文件与依赖声明的对账，把"构建可复现"从口号变成命令：

```bash
# 声明层：requirements.txt 写的是范围（requests>=2.28）
# 锁定层：requirements.lock 写的是精确值 + 哈希
grep -A1 "name = "requests"" requirements.lock
# name = "requests"
# version = "2.31.0"
# hash = "sha256:942c5a7585f9d4e..."   ← 锁的是字节，不只是版本号

# 构建时校验（哈希不符就地失败，fast-fail）
pip install --require-hashes -r requirements.lock
# 哈希不符 → "THESE PACKAGES DO NOT MATCH THE HASHES" → 构建终止

# SBOM 生成与对账（构建产物带了哪些传递依赖，一份清单）
pip freeze --all > sbom.txt   # 生产用 cyclonedx/syft 生成标准格式
```

跑一遍的收获：锁文件回答"哪天构建都得到同一份字节"，SBOM 回答"这份产物里有什么"——两者合起来才能在漏洞通告发出时，用一条 grep 回答"我们中招没有"。

## 量化锚点与案例回流

时间窗的账：漏洞从公开到在野利用的中位时间以天计（公开基准：CVSS 9+ 的漏洞平均 5 天内出现利用代码），而"升级一个传递依赖"在无锁定无 SBOM 的仓库里要人工确认影响链（小时到天级）——供应链工具的投入就是把确认时间从小时压到分钟。投毒的现实量级：主流注册表每年下架的恶意包以千计（typosquatting、账号劫持两类占大头），防御的性价比排序：锁定哈希（零成本，恒久有效）> 传递依赖最小化 > 自动化漏洞扫描 > 构建隔离。案例衔接：[[工程知识/缺陷分析：从个案到体系/安全/案例四十四：投毒不是写错，是写给你看——依赖投毒的三个真实剧本]] 是本篇机制在缺陷域的完整展开；构建输入锁定的姊妹篇是 [[工程知识/软件构建：让变化可以理解、验证与交付/测试与交付/CI流水线把反馈速度和发布证据连接起来]] 的门禁证据链——锁定与证据共同回答"这次发布里到底是什么"。

## 验证

验证的锚点：构建可复现要与产物比对对账——预写同一来源两次构建应产出相同产物，出现差异，与预期对不上时先确认不可复现的输入是否已被登记。

新依赖通过许可证、来源、哈希、维护状态、漏洞和最小行为测试；构建两次比较产物或记录不可避免差异。升级后跑 API、性能、安全、兼容和回滚测试，保留上一份可部署 lockfile/镜像。

关系：[[工程知识/软件构建：让变化可以理解、验证与交付/软件构建：让变化可以理解、验证与交付]] · [[工程知识/软件构建：让变化可以理解、验证与交付/开发工具/依赖升级按风险分层：验证阶梯、节奏策略与 CVE 响应]] · [[工程知识/AI 系统工程：从模型能力到生产能力/安全与治理/模型与数据供应链需要可验证来源]]
- [[工程知识/软件构建：让变化可以理解、验证与交付/架构与代码/购买与自建的决策框架：每条约束都摆上桌面.md]] —— 购买路线的供应链治理
- [[工程知识/后端系统：在并发、失败与变化中维持服务/基础设施/TLS握手与证书链：信任是怎么一层层背书的.md]] —— 信任链在供应链的同构应用
- [[工程知识/软件构建：让变化可以理解、验证与交付/开发工具/依赖升级的半衰期策略：跟随与锁定的平衡.md]] —— 供应链篇锁来源，本篇定版本节奏，锁定的两个维度
