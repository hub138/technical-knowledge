---
title: 维护者信任是供应链信任的地基：贡献者治理与维护者变更审计
type: playbook
status: active
updated: 2026-09-22
review_after: 2026-12-22
change_rate: medium
confidence: high
tags:
  - "software/dependencies"
  - "supply-chain"
  - "supply-chain"
sources:
  - "https://openssf.org/"
  - "https://github.com/ossf/scorecard"
  - "https://github.com/advisories"
---

# 维护者信任是供应链信任的地基

## 核心要点

| 维度 | 回答 |
| --- | --- |
| 要解决的问题 | 依赖信任评估只覆盖“包里是什么”，漏掉了“包由谁、怎么产生”。xz 后门证明：当维护者本身就是攻击者，锁版本、锁 hash、锁来源的防线全部失守，因为每一个“锁”都在他手里。 |
| 本质 | 把上游项目的“人”和“流程”纳入依赖评估对象：贡献者集中度、维护者准入与变更、release 权限分离、构建可复现性。 |
| 做法 | 依赖准入时跑 scorecards 并人工复核低分项；把“维护者变更”列入例行审计信号；release 权限与 commit 权限分离；关键依赖要求可复现构建。 |
| 边界 | 信任评估也有边界：scorecards 高分不等于无后门（社会工程学不进静态指标），贡献者分散不等于无卧底（xz 潜伏两年多）。评分是降低概率，不是消除风险。 |
| 效果与代价 | 对关键依赖维持一份“人的画像”：谁在维护、怎么准入、release 态度如何。代价：评估成本从“扫描一次”升级为“持续观察”，仅对关键依赖值得。 |
| 验证 | 对依赖清单跑一次 scorecards；挑 3 个关键依赖做维护者变更回溯（近一年 release 是否有无名新维护者接手）；检查这些项目的构建是否可复现。 |

## 机制：信任的四根柱子与各自失效模式

开源供应链的信任可以分成四根柱子，每根都有独立的失效模式：

**柱一：身份信任（这个贡献者是谁）**。失效模式：细粒度 commit 权限缺失——所有 maintainer 共享一个粗粒度 CODEOWNERS，一人被盗全仓库沦陷；贡献者匿名度异常——无名新账号突然获得关键路径写权限。观察信号：贡献者集中度（一人包办全部关键 commit）、账号年龄与贡献历史的断层。

**柱二：流程信任（改动怎么变成 release）**。失效模式：release 权限集中——release 经理与主要贡献者是同一人，社会工程学只需攻破一人；tag 与构建产物分离——tag 指向的 commit 与发布 tarball 内容不一致（xz 模式）。观察信号：release 是否走 CI 构建（而非本机构建）、tag 与 CI 产物是否 bit-for-bit 一致。

**柱三：产物信任（你拿到的东西是什么）**与柱二衔接：可复现构建是产物信任的硬证据，任何人能从同一源码构建出相同产物，则“构建者私自注入”无处藏身。失效模式：构建不可复现（时间戳、随机种子、路径依赖进产物），注入可以藏在构建噪声里。

**柱四：时间信任（这套信任能维持多久）**。失效模式：burnout 与静默放弃——维护者耗尽热情后账号闲置，等待被接管（xz 前传：原维护者 burnout 被投毒者利用）；项目转入维护模式后贡献者流失，攻击者以“接手维护”名义入场。观察信号：issue 响应时长、commit 频率的长期趋势、bus factor。

## 为什么 scorecards 是必要但不充分的

OpenSSF scorecards 把上述柱子的多数信号做成了自动化检查（Maintainer-Num、CI-Installed、SAST、Binary-Artifacts 等）。它的价值是把"人的观察"变成"可比较的分数"，让依赖准入有客观起点。不充分之处有二：

1. **静态指标看不见潜伏**。xz 的 Jia Tan 贡献了两年多合法代码、通过了所有社区流程，scorecards 在后门注入前会给 xz 打高分。社会工程学的本质是利用流程的合法性，静态评分对此失明。
2. **低分项目的合法解释**。单维护者项目（bus factor=1）可能是作者本人坚持的艺术品（如 SQLite），不必然是风险。评分要触发人工判断，不能替代判断。

## 防线分层：按依赖的关键度升格治理

| 依赖级别 | 判断标准 | 治理动作 |
| --- | --- | --- |
| 基础库（xz 级） | 几乎所有下游都会拉取（压缩、加密、网络协议库） | scorecards 年审 + 维护者变更例行审计 + 可复现构建验证 |
| 关键依赖 | 直接影响产品行为与数据语义 | scorecards 准入 + 协作记录留存（release 节奏、维护者名单存档） |
| 普通依赖 | 可替换、影响面小 | 扫描器例行扫描即可，不做人审 |

这个分层与依赖升级分层（见 [[工程知识/软件构建：让变化可以理解、验证与交付/开发工具/依赖升级按风险分层：验证阶梯、节奏策略与 CVE 响应]]）同构：治理强度跟着爆炸半径走。

## 与依赖供应链理论篇的分工

[[工程知识/软件构建：让变化可以理解、验证与交付/开发工具/依赖供应链需要锁定来源与构建输入]] 锁的是“物”（版本、hash、签名、构建输入），本篇补的是“人”（谁维护、怎么准入、release 怎么产生）。两篇合起来才是一套完整的供应链信任评估：锁物不锁人，xz 后门教你做人；锁人不锁物，包内容偷换防不住。

## 验证

- **scorecards 批量跑**：对依赖清单里全部直接依赖跑 scorecards（`scorecard --repo=github.com/xxx/xxx`），低分项人工复核。预期：多数项目 CI/分支保护/依赖更新检查得分正常，维护者集中度与 Binary-Artifacts 是常见扣分项。
- **维护者变更回溯**：挑 3 个关键依赖，读近一年 release 的 commit 历史与维护者名单变化。预期：能说清每个项目“现在谁在维护、维护者怎么产生的”，说不清的就是治理缺口。
- **可复现构建抽查**：对最关键的一个依赖做源码构建 vs 发布产物 diff。预期：多数项目可通过；通不过且无解释的项目，其产物信任为零，按基础库标准它必须修。

关系：[[工程知识/软件构建：让变化可以理解、验证与交付/开发工具/依赖供应链需要锁定来源与构建输入]] · [[工程知识/软件构建：让变化可以理解、验证与交付/开发工具/依赖升级按风险分层：验证阶梯、节奏策略与 CVE 响应]] · [[工程知识/缺陷分析：从个案到体系/兼容与升级/案例四十三：升级把前提带走了——依赖行为变更引发的下游失效]] · [[工程知识/缺陷分析：从个案到体系/安全/案例四十四：投毒不是写错，是写给你看——依赖投毒的三个真实剧本]]