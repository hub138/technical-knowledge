---
title: 静态分析的证据边界：它证明什么不证明什么
type: concept
status: active
updated: 2026-09-22
review_after: 2027-09-22
change_rate: medium
confidence: high
tags:
  - software/static-analysis
  - software/testing
  - correctness
sources:
  - "https://dl.acm.org/doi/10.1145/3236024.3236089"
  - "https://hal.inria.fr/hal-01091186/document"
  - "https://clang.llvm.org/docs/analyzer"
---

# 静态分析的证据边界：它证明什么不证明什么

## 要解决的问题

静态分析（不运行程序，从代码结构推断性质）在 CI 里承诺“零成本发现 bug”：不写测试就能查空指针、未初始化、类型错配。但它的告警有真有假（false positive 让人麻木，false negative 让人裸奔），它的“证明”有强有弱（bug finding 模式 vs sound 验证模式）。要解决的问题是**把静态分析定位成证据的一种，明确它能证明什么、不能证明什么**，与动态测试（见 [[工程知识/软件构建：让变化可以理解、验证与交付/测试与交付/测试策略从风险选择证据.md]]）互补而不是互替。这决定 CI 策略（哪些告警设门禁、哪些只报告）与工具选型（bug finder vs verifier）。

## 机制

**两种语义，两种承诺**。
- Bug finding（如 linter、Clang SA 的默认模式）：目标抓常见 bug 模式，允许漏报（不承诺全），追求低误报。证据强度：提示性（“这里可能有 bug”），不是证明。
- Sound 验证（如形式化验证、抽象解释的 prover 模式）：承诺“验证通过 = 无此类错误”（所有路径枚举或保守近似），代价是误报（sound 但 imprecise 会报不存在的 bug）。证据强度：证明性（数学上成立），但只对验证的属性成立。

**停机问题划下的硬边界**。任意程序的非平凡性质判定（会不会空指针、会不会死锁）不可判定（Rice 定理），任何静态分析必须在漏报与误报之间选边：全 sound（不漏）必引入近似（误报），全 precise（不误）必漏。这是数学边界不是工具缺陷，工具的改进只是在边界上移动参数，不可能突破。工程含义：没有“既不误报也不漏报”的分析器，声称有的都在撒谎。

**分析的谱系与各自的证据强度**。AST 规则匹配（最浅：命名规范、简单模式）→ 数据流分析（变量定值-使用链，未初始化检测）→ 控制流分析（路径敏感，空指针路径）→ 符号执行（路径精确但路径爆炸）→ 抽象解释（不动点迭代，sound 但粗）。谱系越深证据越强，工程成本（时间、误报）也越大。CI 的典型配置：AST 层设门禁（快、准），数据流层报告（慢、误报要人看），符号执行层按需（安全关键代码）。

**静态与动态的互补分工**。静态的证据是“代码结构蕴含的性质”（全部执行路径上的性质，但近似），动态的证据是“实际执行过的路径”（精确但只覆盖走过的路）。分工：静态管结构类缺陷（类型、空指针、资源泄漏的形状）、动态管行为类缺陷（逻辑错、性能、集成问题）。测试金字塔（单元为主）与静态门禁（lint 为门禁）是两条证据链的合理配比。

## 背景与代价

思想背景：静态分析与编译器优化同源（1970s 数据流分析为优化服务，后来独立成缺陷检测），抽象解释（Cousot 1977）给 sound 近似数学基础。工业里程碑：Coverity/Polyspace 类工具进航空航天与汽车（安全关键代码的强制验证），ASTrat 类 linter 成为开源 CI 标配。误报治理的转折是机器学习排序（2018+，按告警是 true positive 的概率排序，让人先看最像真的），把“误报淹没开发者”缓解成“排序后的优先级队列”。

最精巧的一笔是**抽象解释的“sound 但不精确”分层设计**。它不追求精确回答（这条路通向路径爆炸），而是把程序状态抽象成保守近似（如“这个变量可能是 0 也可能不是”），在抽象域上迭代到不动点，数学上保证结论对原程序成立（sound），代价是结论可能太保守（把安全代码也报了）。Astrée 验证 Airbus 飞控代码（C 代码无运行时错误证明）是工程顶点：不是抓 bug，是证明无 bug。代价要摆明：静态分析的 ROI 曲线不是线性的——linter 层的成本极低收益稳定（每代码行抓 0.1-1 个真 bug），数据流层成本升（配置与误报治理），符号执行层的成本陡增（路径爆炸的取舍、专家配置），sound 验证层只对安全关键代码划算（Airbus 的例子是每行代码几十美元的验证成本）。CI 的分层门禁策略要按这个曲线定：不是“越多静态分析越好”，是“每层证据的边际成本与边际收益对齐”。误报治理本身是持续成本：规则集的维护（语言版本升级、框架模式的更新）、告警基线的管理（存量告警豁免机制），无治理的静态分析三个月后退化为噪音源。

## 边界

- **静态分析不能证明“程序正确”**，只能证明“程序无某类特定错误”（sound 模式）或“发现了一些疑似问题”（bug finding 模式）。“验证通过”的语义要写进 CI 文档：通过 = 无此类错误，不是无 bug。
- **误报不是缺陷是参数**。每个 sound 分析器都有 soundness/precision 旋钮（抽象域的粗细），误报率 5% 与 50% 是同一个工具的不同配置。团队要主动选参数（按误报承受力），不是被动接受默认。
- **跨边界分析是误报重灾区**。动态语言（JS/Python）的运行时类型、反射、eval 让静态信息稀薄，分析器被迫保守（误报飙升）。动态语言的静态分析价值集中于 lint 层，深度分析要配类型注解（TypeScript、mypy 的路线）。
- **告警修复的优先级要与风险对齐**。静态告警的风险排序（可达性 × 后果严重度）要接测试策略（见 [[工程知识/软件构建：让变化可以理解、验证与交付/测试与交付/测试策略从风险选择证据.md]]）：高危路径的告警先修，不可达代码的告警可豁免。告警一视同仁的修复策略浪费预算。

## 验证

- **证据强度分层实验**：同一代码库跑 linter、数据流、符号执行三层，统计各层告警的确认率（true positive rate）。预期：linter 高（>80%）、数据流中（40-70%）、符号执行低但单个告警价值高。分层证据强度的分布是门禁配置的量化依据。
- **误报基线演进**：新告警零基线（new-code-only 门禁）上线，存量告警豁免表维护，统计豁免表的增长与确认率。预期：新告警门禁让人工审核量下降一个数量级，豁免表增长缓慢（规则成熟后收敛）。基线机制有效性的证据。
- **sound 验证试点**：安全关键模块（认证、支付核心路径）跑 abstract interpretation 类工具，对比 bug finding 模式的发现。预期：sound 模式给出“无越界/无除零”的证明性结论（bug finding 给不了），误报集中在并发与外部调用边界。两种语义的承诺差异实证。

## 相关

- [[工程知识/软件构建：让变化可以理解、验证与交付/测试与交付/测试策略从风险选择证据.md]] —— 证据组合中的静态证据位置
- [[工程知识/软件构建：让变化可以理解、验证与交付/测试与交付/覆盖率的三种读法：行分支与路径覆盖的证据强度.md]] —— 动态证据的覆盖强度谱
- [[工程知识/性能工程：从用户等待到资源瓶颈/观测与诊断/指标、日志、Trace与剖析各答一问，合起来才构成证据.md]] —— 运行时证据的互补面
- [[工程知识/软件构建：让变化可以理解、验证与交付/架构与代码/架构适应度函数：把架构约束写成可执行检查.md]] —— 静态分析供证据，适应度函数是它在架构域的定向应用
- [[工程知识/缺陷分析：从个案到体系/配置/配置类缺陷的静态防线：schema校验与默认值审计.md]] —— 配置 schema 校验是静态分析思想在配置域的定向应用
