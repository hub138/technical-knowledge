---
title: 混沌工程验证系统是否真的能处理故障
type: playbook
status: active
updated: 2026-09-03
review_after: 2027-03-03
change_rate: medium
confidence: high
tags:
  - testing/chaos
  - software/resilience
  - operations
sources:
  - "https://principlesofchaos.org/"
  - "https://sre.google/sre-book/handling-overload/"
  - "https://kubernetes.io/docs/tasks/debug/debug-cluster/"
---

# 混沌工程验证系统是否真的能处理故障

## 核心要点

| 维度 | 回答 |
| --- | --- |
| 要解决的问题 | 正常路径绿色不能说明系统能应对进程崩溃、网络分区、依赖超时、过载或数据故障；未验证的恢复逻辑往往只在事故中第一次执行。 |
| 本质 | 在可观测稳态上注入有界故障，用关键不变量、SLO 和恢复证据检验系统的真实行为。 |
| 做法 | 先写假设、稳态指标、注入范围、爆炸半径、自动停止、回滚和预期不变量，再在隔离环境或小流量灰度执行。 |
| 效果与代价 | 暴露恢复、降级、幂等和观测缺口；代价是实验风险、环境准备、数据隔离和值班成本。 |
| 边界 | 混沌实验不是随机破坏生产，也不能覆盖未建模风险；服务返回 200 不代表数据、任务状态和权限仍然正确。 |
| 验证 | 保存注入命令、时间线、trace、日志和恢复结果；将关键故障模型加入新版本、推理后端和数据管道回归。 |

混沌实验以可观测的稳定状态为基线，注入受控故障，验证系统是否保持关键不变量并在预算内恢复。它不是随机破坏生产，更不是替代单元/集成测试；实验对象、范围、停止条件和回滚必须明确。

## 可注入的故障

进程崩溃、节点/网络分区、延迟/丢包、磁盘满、CPU/内存压力、依赖 5xx/超时、消息重复/乱序、时钟变化、GPU 驱逐和凭据失效。AI 任务还要注入模型/工具不可用、RAG 索引旧代际和中途取消。

## 实验约定

```text
假设 + 稳态指标 + 注入范围 + 最大爆炸半径
     + 自动停止 + 预期不变量 + 恢复证据
```

成功不是“服务还返回 200”，而是核心请求、数据一致性、权限、任务状态、重试预算、SLO 和恢复时间符合预期；副作用不能重复，失败不能覆盖已确认事实。

## 可运行示例：一个带假设的实验全程

对支付服务注入"依赖超时 2 秒"故障，完整实验记录：

```text
稳态假设（先明确写下，可证伪）：
  支付依赖慢 2s 时：下单成功率降幅 <2%，P99 <5s，无错误级联

爆炸半径控制：
  注入对象：单个可用区的支付客户端超时（非全量）
  止动开关：一键撤销注入（<5s 生效），观察者持有人工止动权

实验记录：
  t+0    注入生效（60% 请求超时重试，重试风暴现形：下游 QPS ×1.8）
  t+40s  下游线程池耗尽，未注入区域也开始超时 ← 假设已被证伪
  t+50s  人工止动，注入撤销

产出（实验的全部价值）：
  发现 1：重试无退避 → 超时被放大成重试风暴（修复：退避+抖动）
  发现 2：线程池无舱壁隔离 → 故障跨区域传染（修复：按依赖分池）
  发现 3：止动流程 50s（超标，目标 10s 内）
  → 三项进入修复队列，修复后重跑同一实验验证，实验收尾
```

跑一遍的收获：混沌工程的产出是对故障处理能力的证据（处理了、没处理、处理得慢），价值全在"假设被证伪后产出的修复项"——注入本身什么也不证明，证明的是注入后系统的行为与假设的差距。

## 量化锚点与案例回流

频率与强度的账：注入强度从小比例起步（1% 请求延迟、单实例故障），生产环境实验的故障注入面不超过一个可用区（爆炸半径用流量比例圈）；实验频率的公开经验是月度级（配合发布节奏），稳态指标在实验间的漂移本身就是告警。修复验证的闭环账：实验产出的修复项，重跑同一实验是唯一验收方式（"修了"与"扛得住"之间隔着重跑），验收通过才归档为稳态能力。案例回流：故障传染的实录见 [[工程知识/缺陷分析：从个案到体系/稳定性/案例三十六：写被自己的回调堵住——挂起的 add 回调.md]]（一个挂起堵住整条路径，正是舱壁缺失的形态）；重试风暴的机制账见 [[工程知识/数据系统：在并发与故障中保存事实/数据管道与流处理/数据编排要保证依赖、重试与幂等.md]] 的退避与抖动段；实验的计划性与 [[工程知识/软件构建：让变化可以理解、验证与交付/测试与交付/CI流水线把反馈速度和发布证据连接起来.md]] 的证据链同源——混沌实验是流水线证据在"故障日"的形态。

## 验证

先在隔离环境和小流量灰度，保存注入命令、时间线、trace、日志、状态和恢复结果。每个新版本、基础设施、推理后端和数据管道的关键故障模型至少有一条自动回归；实验失败应产生可执行修复，不要只记录“已知风险”。

关系：[[工程知识/后端系统：在并发、失败与变化中维持服务/分布式可靠性/部分失败决定分布式系统的设计]] · [[工程知识/软件构建：让变化可以理解、验证与交付/测试与交付/发布门禁必须绑定真实证据]]
相关：[[工程知识/性能工程：从用户等待到资源瓶颈/观测与诊断/网络故障注入先打点对账：注入值与应用感知值对不上，注入就是无效实验.md]] —— 网络注入的生效性对账
