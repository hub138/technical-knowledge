---
title: 调试从假设到最小复现
type: playbook
status: active
updated: 2026-09-15
review_after: 2027-09-03
change_rate: stable
confidence: high
tags:
  - software/debugging
  - testing
  - diagnosis
sources:
  - "https://go.dev/doc/diagnostics"
  - "https://lldb.llvm.org/use/tutorial.html"
  - "https://martinfowler.com/articles/2021-test-shapes.html"
---

# 调试从假设到最小复现

## 先看两种做法

线上偶发超时，一天几十次。

**做法 A**：翻日志，看到一堆 `context deadline exceeded`，判断"下游太慢"，加超时时间。第二天照旧，只是变慢了些。

**做法 B**：先想办法让它按需复现——写个脚本并发打 200 请求，5 分钟内必现一次。然后每次只改一个变量：先怀疑连接池，把池从 10 调到 100，还复现，排除；再怀疑 GC，打开 `GODEBUG=gctrace=1`，发现每次卡顿都紧跟一次 STW，锁定。

两种做法的区别不在聪明程度，在**有没有一条会变红的回路**。做法 A 从头到尾没有任何东西能为结论"变红"——它只是在合理的解释里挑了一个。

调试的目标不是找到一个听起来合理的原因，是**让某个假设在最小输入和可观测证据下被证伪或支持**。

## 没有复现，就没有因果

这是最容易被跳过的一步。没有稳定复现时：

- 修复无法区分**因果**、**巧合**和**环境变化**
- 无法确认修复真的修好了，还是这次运气好
- 下次再现有，不知道是不是同一个问题

所以第一件事是**建立能对用户症状变红的回路**：最小输入、明确断言、可重复环境、分层观测。

对偶发问题，不追求 100% 复现，但要**记录复现率**：同样跑 200 次，从"偶发"变成"10 次里现 1 次"，就已经可以用来验证修复了。

## 循环

```text
症状 → 复现 → 3-5 个可证伪假设 → 定向探针
     → 最小修复 → 原场景回归 → 删除临时诊断
```

几个容易做错的地方：

- **假设要可证伪**。"下游慢"不是假设，"连接池耗尽导致排队"才是——后者能通过打印池的等待队列长度来证实或推翻。
- **一次只改一个变量**。同时调池大小和超时时间，好了也不知道是哪个起作用。
- **探针要有指向性**。日志、断点、trace、core dump、属性测试、故障注入各回答不同问题。全量加日志不算观测策略，那只是把问题往后推。

优先在正确的调用链上测试：并发问题需要多个执行者，跨服务问题需要真实边界，性能问题需要基线和 profile。

## 收缩到最小

从原始失败开始，逐步删除输入、并发、依赖和配置，直到症状刚好消失，**再恢复最后一个变量**——那一步删掉的东西就是相关的。

记录复现概率、版本、环境和期望断言。这套最小复现不只是给你自己用：它是回归测试的原料，也是别人复核你结论的唯一依据。

## 收尾

修复后要在三个地方各跑一次：

1. **原始场景** —— 确认症状真的消失
2. **最小场景** —— 确认修复没有依赖原场景的某些偶然条件
3. **相邻边界** —— 确认不是把问题转移到另一条路径

最后删掉临时诊断代码，保留失败样本、环境版本、输入和时间窗口。

对非确定性问题，提高复现率并记录概率。**不把一次偶然成功写成修复。**

关系：[[工程知识/性能工程：从用户等待到资源瓶颈/观测与诊断/性能问题定位]] · [[工程知识/软件构建：让变化可以理解、验证与交付/测试与交付/CI流水线把反馈速度和发布证据连接起来]]
