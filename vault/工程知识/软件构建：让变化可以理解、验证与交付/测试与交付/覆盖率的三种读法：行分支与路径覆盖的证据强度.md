---
title: 覆盖率的三种读法：行分支与路径覆盖的证据强度
type: concept
status: active
updated: 2026-09-22
review_after: 2027-09-22
change_rate: medium
confidence: high
tags:
  - software/testing
  - software/static-analysis
  - correctness
sources:
  - "https://ieeexplore.ieee.org/document/4515251"
  - "https://coverage.readthedocs.io/en/latest/"
---

# 覆盖率的三种读法：行分支与路径覆盖的证据强度

## 要解决的问题

“覆盖率 90%”是测试报告里最常见的数字，但它掩盖了三种完全不同的承诺强度：行覆盖（这行执行过）< 分支覆盖（每个条件的两条路都走过）< 路径覆盖（所有执行序列都走过）。同一数字 90% 在三种读法下的证据强度相差数量级。要解决的问题是**把覆盖率从单一数字还原成证据强度的谱系**，让“90% 行覆盖”与“90% 路径覆盖”不可通约这件事显形，CI 阈值与质量讨论才有共同语言。这是测试策略从风险选择证据（见 [[工程知识/软件构建：让变化可以理解、验证与交付/测试与交付/测试策略从风险选择证据.md]]）的量化支柱，与静态分析的证据边界（见 [[工程知识/软件构建：让变化可以理解、验证与交付/测试与交付/静态分析的证据边界：它证明什么不证明什么.md]]）构成证据谱的两端。

## 机制

**三种读法的定义与强度差异**。
- 行覆盖（statement coverage）：每行代码至少执行一次。最弱读法：执行过 ≠ 验证过（执行到这行但断言没检查它的结果，覆盖白覆盖）。if 不写 else 时 100% 行覆盖可以漏掉整个 else 分支。
- 分支覆盖（branch coverage）：每个判定的每条边都走过（if 的真与假、switch 的每个 case）。中等强度：条件组合间的交互漏掉（A and B 的四象限只测了两象限）。
- 路径覆盖（path coverage）：所有可能的执行路径都走过。最强读法：但路径数随分支数指数增长（10 个二分支 = 1024 条路径），全路径覆盖对真实程序不可达（组合爆炸的物理约束）。MC/DC（modified condition/decision coverage，航空标准 DO-178 的要求）是折中：每个条件都能独立影响判定结果，把指数压成线性。

**覆盖率高低的证据含义**。覆盖率是必要条件不是充分条件：低覆盖率 = 某些代码从未被任何测试验证过（证据缺口明确），高覆盖率 = 所有代码被“执行过”（但不保证被“断言过”）。断言密度（每覆盖行的断言数）是覆盖率的补集指标：90% 覆盖 + 低断言密度 = 跑过但没检查，证据强度虚。变异测试（mutation testing）是更强读法：改一处代码看测试是否失败，测试杀掉的变异比例 = 测试的真实检错力。它是覆盖率的证据升级（从“执行过”到“能检错”），但成本高（全量变异跑 N 倍测试时间），按风险模块选用。

**复合条件与短路求值的陷阱**。 的分支覆盖只要求整条判定走真假两边，不要求 a、b 单独的组合（a=true/b=false 与 a=false/b=true 是不同路径）。复合条件覆盖（condition coverage）补这个洞，MC/DC 进一步要求独立性（固定其他条件，翻转单一条件能翻转判定）。这是航空与汽车安全标准选 MC/DC 的原因：指数路径里挑出真正影响行为的线性子集。

**增量覆盖率与门禁设计**。全量阈值的博弈（老代码拉低均值，新代码搭便车）用增量门禁解：新代码必须 100% 覆盖（diff-coverage），存量渐改。门禁分层：行覆盖做 CI 门禁（便宜快），分支覆盖做发布门槛（较贵），路径/变异按模块风险（贵，只用于核心）。

## 背景与代价

思想背景：覆盖率指标诞生于 1960s（Miller 与 Maloney 的早期工作），行/分支/路径的谱系在 1970s 交代清楚，军用与航空标准（DO-178、ISO 26262）把 MC/DC 写进法规层。工业界的大讨论是覆盖率阈值之争（2010s）：Google 的 60% 与 Microsoft 的 80% 各自成立（语言、领域、风险分布不同），共识收敛为“阈值无普适值，按风险分布与回归成本定”。变异测试（1971 DeMillo 提出）的复兴是工具成熟（PIT、Stryker）把成本从不可用压到可选。

最精巧的一笔是**MC/DC 把指数路径压成线性证据**。全路径覆盖不可达（组合爆炸），只测真值表的对角线（每个条件独立翻转）却能捕捉大部分行为缺陷。这一笔的洞察：缺陷的分布不是均匀的，多数缺陷由单一条件的错误翻转引发，独立翻转覆盖恰好对应这个分布。代价要摆明：覆盖率的三个读法都不能替代断言质量——覆盖率的证据上限是“代码被走过”，变异测试才到“测试能检错”，而真正的行为正确性要靠断言与预期结果（断言弱，覆盖率是虚证据）。覆盖率门禁的博弈行为（为凑数写无断言测试、把难测代码标 ignore）是管理问题，指标只能量化它不能消除它。变异测试的成本曲线陡（全库变异 = 测试时间 × 变异点数），只对高风险模块值得；增量变异（只变异新代码）是成本平衡点。覆盖率的健康用法：作回归网的守门员（覆盖率下降报警，见回归网思想（见 [[工程知识/缺陷分析：从个案到体系/复现与回归/从复现到回归——让证据走完一圈.md]]）），不作质量分数的排行榜。

## 边界

- **覆盖率是必要条件的证据，不是质量分数**。高覆盖不等于高质量（断言弱则覆盖虚高），低覆盖明确是问题（未验证代码）。把覆盖率当 KPI 考核会激励凑数行为，正确用法是门禁与缺口分析。
- **三种读法的选择按风险分层**。CI 快速反馈用行覆盖，发布门槛用分支覆盖，安全关键模块用 MC/DC 或变异测试。全库统一最高读法是浪费（贵且无必要），统一最低读法是裸奔（漏条件组合）。
- **不可达代码的处理是数据卫生**。废弃代码（被删功能的残留）拉低覆盖率且永远无法覆盖，该清理；平台特异代码（OS 分支）按平台分报覆盖率，不混在一起。
- **覆盖率报告要配合缺口分析使用**。数字之后的动作是看未覆盖的模块分布（风险高的模块未覆盖是红灯，样板代码未覆盖是噪音），报告的可视化按风险排序而不是按目录。

## 验证

- **三种读法对照实验**：同一模块分别统计行/分支/MC-DC 覆盖率，再注入缺陷（fault injection）看测试能否检出。预期：行覆盖高但分支覆盖低的模块，缺陷检出率显著低（条件组合漏测的直接证据）。读法强度与检出率的相关性。
- **变异测试基线**：核心模块跑变异测试，统计变异杀除率与覆盖率的相关性。预期：90% 覆盖的模块变异杀除率 60-80%（覆盖与检错力的落差），断言弱的模块杀除率断崖。覆盖虚高的实证。
- **增量门禁实验**：diff-coverage 门禁上线前后，新代码的缺陷逃逸率（上线后 bug）对比。预期：逃逸率下降（未覆盖代码上线减少）。门禁有效性的业务证据。

## 相关

- [[工程知识/软件构建：让变化可以理解、验证与交付/测试与交付/测试策略从风险选择证据.md]] —— 覆盖率在证据体系中的位置
- [[工程知识/软件构建：让变化可以理解、验证与交付/测试与交付/静态分析的证据边界：它证明什么不证明什么.md]] —— 证据谱另一端的静态证据
- [[工程知识/缺陷分析：从个案到体系/复现与回归/从复现到回归——让证据走完一圈.md]] —— 覆盖率作为回归网守门员
- [[工程知识/缺陷分析：从个案到体系/并发/并发缺陷的复现策略：压力放大与确定性调度.md]] —— 交错是路径爆炸的极端形态，覆盖率证据分析在此放大
- [[工程知识/缺陷分析：从个案到体系/复现与回归/回归风险评估的通用骨架：从变更面到测试选择.md]] —— 覆盖率读法是测试选择的证据强度依据
- [[工程知识/软件构建：让变化可以理解、验证与交付/开发工具/依赖升级的半衰期策略：跟随与锁定的平衡.md]] —— 升级自动化的胆量由测试置信度决定，覆盖率是地基
