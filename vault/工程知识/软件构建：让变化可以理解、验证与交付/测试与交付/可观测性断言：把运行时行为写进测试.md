---
title: 可观测性断言：把运行时行为写进测试
type: concept
status: active
updated: 2026-09-22
review_after: 2027-09-22
change_rate: medium
confidence: high
tags:
  - observability
  - software/testing
  - debugging
sources:
  - "https://opentelemetry.io/docs/"
  - "https://martinfowler.com/articles/observability-versus-monitoring.html"
---

# 可观测性断言：把运行时行为写进测试

## 要解决的问题

传统测试断言的是返回值（调用 f(x) 得到 y），但很多正确性不在返回值里：这个请求是否走了缓存、慢查询有没有发生、重试了几次、日志有没有打出错误、资源有没有泄漏。这些运行时行为靠“跑完看监控”是人肉验证，不可重复也不进 CI。可观测性断言要解决的问题是**把运行时行为的期望写成机器可执行的断言**：测试不仅断言输出，还断言“执行过程中发生了什么”。这是把可观测性（见 [[工程知识/性能工程：从用户等待到资源瓶颈/观测与诊断/指标、日志、Trace与剖析各答一问，合起来才构成证据.md]]）的信号体系接进测试体系的桥，与测试策略从风险选择证据（见 [[工程知识/软件构建：让变化可以理解、验证与交付/测试与交付/测试策略从风险选择证据.md]]）的“证据从哪来”问题衔接。

## 机制

**三类信号的断言形态**。
- 指标断言：测试跑完后查询 metric（如 http_requests_total{status="500"} == 0，cache_hit_ratio > 0.9）。适用：计数与比率类期望（无错误、命中率高）。实现：测试进程内埋点直读（micrometer 类 registry 断言）或测试后查询监控系统（Prometheus 的 PromQL in test）。
- 日志断言：期望某日志行出现（或严禁出现）。适用：关键路径的日志证据（降级触发时打 WARN、审计事件落地）。实现：日志 appenders 拦截（测试内捕获）或结构化日志的断言库。陷阱：日志文案变化导致断言脆（断言结构化字段而非自由文本）。
- Trace 断言：期望调用链的形状（span 数量、顺序、父子关系、duration 上限）。适用：链路级期望（RPC 链路必经鉴权 span、慢链路无 N+1 查询）。实现：OTel SDK 的 in-memory exporter 在测试里断言 span 树。

**实现层次：进程内优先**。可观测性断言的实现分两档：进程内（测试与被测服务同 JVM/同进程，直读 SDK 的内存 exporter/registry，快且确定）与黑盒（测试完查询外部 Prometheus/ELK，慢且有采集延迟的时序问题）。优先进程内：断言的是自己的行为，不需要外部系统的间接性。黑盒档留给需要真实基础设施的集成测试。

**断言漂移与噪声治理**。可观测性断言的新风险：断言本身会漂移（埋点改名、span 结构重构后断言全红）与噪声（非确定行为：异步任务的日志时序、GC 触发的 duration 抖动）。治理：断言的结构化锚定（锚定 span.name 与 attribute 而非文案）、非确定的容忍窗口（duration 断言给区间而非点值）、断言分层（核心行为必断言，次要行为可选断言）。

**与契约测试的分工**。契约测试（见 [[工程知识/软件构建：让变化可以理解、验证与交付/测试与交付/契约测试的适用边界：消费者驱动与提供者验证.md]]）验证接口形状（请求/响应兼容），可观测性断言验证执行行为（调用链上发生了什么）。互补：契约管“接口对不对”，可观测性断言管“过程对不对”。一个完整的服务级测试金字塔：单元（逻辑）+ 契约（接口）+ 可观测性断言（行为）+ 少量端到端（业务流）。

## 背景与代价

思想背景：可观测性断言的实践源头混沌工程（chaos engineering，Netflix 2011+）：故障注入后的系统行为（降级是否触发、流量是否切走）靠断言监控信号验证，这是“运行时行为写进测试”的第一次大规模工程化。OpenTelemetry（2019+）统一了信号模型（metric/log/trace 的标准 SDK），让断言有稳定的编程接口。测试端的产品化（Honeycomb/Lightstep 的 CI 集成、OTel 的 assertion 库）是 2023+ 的事。

最精巧的一笔是**“用 in-memory exporter 把 Trace 变成测试可以检查的返回值”**。OTel 的 SDK 架构天然支持 exporter 可插拔：生产环境导出到收集器，测试环境换 in-memory exporter，测试代码直接遍历 span 树断言形状。这一笔把“黑盒的运行时行为”变成“白盒的结构化数据”，Trace 从诊断工具升级为测试证据源。代价要摆明：可观测性断言是第三种测试成本——埋点维护（断言锚定 span 名与 attribute，埋点重构即断言碎，与业务代码耦合度上升）、非确定性（异步与并发的行为断言天然 flaky，要容忍窗口与重试逻辑）、调试成本（断言失败时要读 trace 才知道哪层错，比栈跟踪难读）。适用边界：只对“行为即正确性”的场景值得（降级、重试、审计、安全链路），普通 CRUD 的返回值断言仍是大头，别为了时髦给全部测试加可观测性断言。

## 边界

- **进程内断言优先，黑盒断言按需**。同进程直读 SDK 快且确定，黑盒查询外部系统有采集延迟与时序噪声。只有测试真实基础设施（真实的 Prometheus、真实的日志管道）时才用黑盒档。
- **断言锚定结构而非文案**。日志文案、span 名的重构频率高，断言锚定结构化字段（event.type、span attribute）才稳定。文案断言是维护负担的制造机。
- **行为断言的 flaky 治理要前置**。异步行为的断言（“日志最终出现”）要有等待与超时机制（awaitility 类），duration 断言给区间。无治理的行为断言会把 CI 的红噪声推向不可用。
- **不要把可观测性断言当覆盖率替代品**。它验证的是关键路径的行为证据，代码覆盖仍是覆盖证据（见 [[工程知识/软件构建：让变化可以理解、验证与交付/测试与交付/覆盖率的三种读法：行分支与路径覆盖的证据强度.md]]），两者维度不同不能互替。

## 验证

- **降级行为断言实验**：依赖故障注入（mock 依赖超时），断言降级日志 + fallback 指标出现。预期：断言精确捕捉降级行为（返回值断言看不出降级是否真发生）。行为证据的直接演示。
- **span 树断言实验**：OTel in-memory exporter 断言典型链路的 span 形状（服务 A→B→C 的父子关系与顺序）。预期：链路重构（B 内部改异步）后断言红，捕捉到行为变更。链路行为回归的检测演示。
- **断言稳定性实验**：同一测试跑 100 次统计 flaky 率。预期：无容忍窗口的 duration 断言 flaky 率 >10%，加区间后 <1%。非确定性治理必要性的量化。

## 相关

- [[工程知识/性能工程：从用户等待到资源瓶颈/观测与诊断/指标、日志、Trace与剖析各答一问，合起来才构成证据.md]] —— 信号体系与测试体系的桥
- [[工程知识/软件构建：让变化可以理解、验证与交付/测试与交付/测试策略从风险选择证据.md]] —— 行为证据在测试策略中的位置
- [[工程知识/软件构建：让变化可以理解、验证与交付/测试与交付/契约测试的适用边界：消费者驱动与提供者验证.md]] —— 接口证据与行为证据的分工
- [[工程知识/性能工程：从用户等待到资源瓶颈/观测与诊断/性能回归门禁：把性能预算写进CI.md]] —— 同是把观测变断言：性能门禁在 CI 侧，可观测断言在运行时侧
