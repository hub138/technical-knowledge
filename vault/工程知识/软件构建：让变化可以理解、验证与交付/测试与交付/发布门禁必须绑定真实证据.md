---
title: 发布门禁必须绑定真实证据
status: active
type: principle
updated: 2026-09-03
change_rate: medium
confidence: high
review_after: 2027-03-03
tags:
  - software/ci-cd
  - quality
  - testing
sources:
  - "https://opentelemetry.io/docs/concepts/signals/traces/"
  - "https://sre.google/workbook/canarying-releases/"
---

# 发布门禁必须绑定真实证据

一个步骤成功，不能自动扩大成整个目标成功。格式检查通过不等于编译通过，编译通过不等于测试通过，测试进程退出 0 不等于目标代码被覆盖，部署完成也不等于用户任务正确。

发布门禁的对象是可验证事实，不是流程勾选：每条结论都要绑定源码 revision、命令、环境、退出码、产物和断言粒度；证据不足时保持 unknown，而不是为了绿色发布猜一个成功。

## 机制（要解决的问题）：事实的粒度不能被放大

| 已确认事实 | 可以结论 | 不能结论 |
| --- | --- | --- |
| 命令被模型输出 | 生成过命令文本 | 命令实际执行过 |
| 命令退出 0 | 该进程按自身语义成功退出 | 目标行为正确 |
| Maven validate 通过 | validate 生命周期完成 | compile/test 完成 |
| 测试套件通过 | 运行的断言均通过 | 目标分支或修复被覆盖 |
| coverage 命中目标行 | 此次运行经过该位置 | 所有语义和边界正确 |
| 部署控制器 Ready | 资源满足就绪条件 | 业务 SLO 与数据正确 |

报告只能引用与声明同粒度或更强的证据。

## 一次可审计验证

```yaml
verification:
  target:
    repository: repo-id
    revision: immutable-sha
    dirty: false
  execution:
    argv: [...]
    cwd: ...
    environment_fingerprint: ...
    started_at: ...
    exit_code: 0
    stdout_ref: artifact://...
    stderr_ref: artifact://...
  coverage:
    target_symbol: package.Type.method
    evidence_ref: artifact://...
  result:
    assertion: "回归用例触发目标代码且结果符合预期"
    verifier: deterministic-test
```

日志、报告和 coverage 都应绑定同一源码 revision、构建产物和 attempt，不能拿旧报告证明新代码。

## 失败状态不可折叠

至少区分：

- 产品断言失败；
- 编译或类型失败；
- 依赖、网络或权限造成的环境阻断；
- 测试基础设施失败；
- 超时、取消或资源耗尽；
- 未执行、未收集证据或证据不完整。

“测试不适用”是需要证据的语义判断，不能用于覆盖依赖下载失败；unknown 也不能被投影成 pass。

## 已确认事实单调累积

后续 attempt 失败不能抹掉先前已经确认的事实。例如 attempt 1 已稳定复现缺陷，attempt 2 因网络失败没有完成修复验证，最终报告应同时保留“缺陷已复现”和“修复验证被环境阻断”。事件日志追加事实，投影层按类型计算当前状态，避免最后一次文本覆盖全部历史。

## 发布门禁

发布前将证据分成独立护栏：

- 功能：核心任务、历史失败与相邻边界；
- 兼容：API、schema、数据迁移和旧客户端；
- 性能：代表性负载下的 p50/p95/p99、吞吐和资源；
- 安全：身份、租户、越权、敏感数据和供应链；
- 可运营：日志、trace、告警、取消、回滚与恢复。

灰度期间比较版本，而不是只看新版本绝对值。任一关键护栏超过阈值即停止或回滚；不要用综合分数让性能提升抵消正确性或安全回归。

## AI 的正确位置

AI 可以理解项目、选择工具、适配测试、解释失败并提出下一步；框架负责隔离、真实执行、证据采集、结构校验、版本绑定和状态一致性。详细模型见 [[工程知识/AI 系统工程：从模型能力到生产能力/安全与治理/执行证据必须独立于AI结论]]。

相关：[[工程知识/软件构建：让变化可以理解、验证与交付/测试与交付/测试策略从风险选择证据]] · [[工程知识/AI 系统工程：从模型能力到生产能力/Agent与工作流/AI辅助研发必须形成证据闭环]]

- [[工程知识/缺陷分析：从个案到体系/复现与回归/回归风险评估把修复的爆炸半径圈出来]] —— 门禁前的半径评估

## 效果与代价

证据门禁的收益是发布质量可审计（每次发布带的证据可回查，事故复盘能定位到门禁缺口），代价是门禁建设与维护的工程投入、以及证据采集对发布速度的轻度拖累；跳过门禁的代价不对称——一次未带证据的坏发布，其召回成本与信任损失远超日常的采集开销。

