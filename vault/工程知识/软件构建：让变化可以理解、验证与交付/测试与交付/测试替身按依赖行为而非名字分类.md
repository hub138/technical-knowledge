---
title: 测试替身按依赖行为而非名字分类
type: concept
status: active
updated: 2026-09-21
review_after: 2027-09-21
change_rate: low
confidence: high
sources:
  - "https://martinfowler.com/articles/mocksArentStubs.html"
  - "https://testdouble.com/"
  - "https://github.com/testdouble/contributing-tests/wiki/Detect-impact"
tags:
  - software/testing
  - testing
  - quality
  - code-quality
---

# 测试替身按依赖行为而非名字分类

单元测试要隔离依赖，于是有人 mock 数据库、有人 stub 支付接口、有人 fake 整个仓储层——名词满天飞，分歧却在更深处：替身返回什么、断言什么、能不能复用。要解决的问题：替身选错型，测试要么验证了不存在的交互（交互断言过强，重构即碎），要么放过了真实缺陷（状态断言缺位，mock 绿了线上炸了）。本文按替身的**行为约定**分类，而非传话筒式的名词分类；分对类型的收益是测试既验证真实行为又给重构留空间，分错类型的代价是测试套件与实现绑定过紧。

## 四种替身的行为定义

| 替身 | 行为 | 验证什么 |
| --- | --- | --- |
| dummy | 占位，从不被使用 | 不验证任何东西 |
| stub | 返回预置值 | 被测单元的状态输出 |
| spy | 记录调用供事后断言 | 被测单元对外的副作用 |
| fake | 有真实简化实现 | 被测逻辑与轻量实现的组合 |

（mock 这个词在社区有两层含义：狭义 = spy + 交互期望，广义 = 一切替身。本文用行为定义避开名词之争，与 [[工程知识/软件构建：让变化可以理解、验证与交付/测试与交付/测试策略从风险选择证据]] 的弱证据/强证据表同构。）

## 交互断言的过度指定

"验证 orderService.create 被调用了一次"是交互断言。它的脆弱性：实现从一次调用改成两次批量调用，行为没变（订单都创建了），测试红了——这叫过度指定（over-specification）。判据：断言的交互是需求的**本质**（"必须发送恰好一封确认邮件"）还是**实现细节**（"调用了一次 mailer"）。前者合法，后者把测试与实现绑得过紧。

## fake 的信任问题

fake 是有简化实现的替身（内存数据库、内存队列）。它的风险：与真实实现行为漂移。防线是与真实实现跑同一套接口约定测试（contract test）：同一组输入，断言 fake 与真实组件行为一致。没有接口约定测试的 fake 是第二个待测系统，见 [[工程知识/软件构建：让变化可以理解、验证与交付/测试与交付/测试策略从风险选择证据]] 的"关键依赖用真实组件"行。

## 可运行示例：同一个依赖，四种替身的取舍

被测对象是订单服务，依赖是支付网关，四种替身各管一种测试问题：

```python
# 桩（Stub）：给定返回，不验证交互 —— 测"拿到支付结果后系统怎么反应"
gateway = Stub(charge=lambda order: {"status": "paid", "txn": "T1"})
result = order_service.submit(order, gateway)
assert result.state == "confirmed"

# 假对象（Fake）：可用实现，轻量捷径 —— 测"流程在真实语义下能否走通"
gateway = InMemoryGateway()   # 内存记账，扣款语义真实存在
result = order_service.submit(order, gateway)
assert ledger.balance_of(order.user) == before - order.amount

# 模拟（Mock）：预编交互期望 —— 只在"交互本身是需求"时用
gateway = Mock()
order_service.submit(order, gateway)
gateway.assert_called_once_with(charge=order)   # 需求就是"钱只扣一次"

# 间谍（Spy）：只记录调用，事后断言 —— 审计类需求
events = Spy()
order_service.submit(order, events)
assert events.recorded("order.confirmed")
```

跑一遍的收获：选型判据只有一个——这次测试要验证的是状态变化还是交互发生。验证状态用桩和假对象（断言返回值与账目结果），验证交互才用模拟和间谍；把"调没调、调几次"写成期望是最容易过度指定的形态（被测对象的结构一改就红，红得毫无意义）。

## 量化锚点与案例回流

维护成本的账：交互断言类用例的重构脆弱性远高于状态断言类——被测对象的结构重构后 mock 密集套件的假警报率是状态断言套件的数倍，每个假警报都要人工判一次"红得对不对"，这就是过度指定的日常代价。假对象的信任账：InMemory 实现与真实实现的语义分叉是事故源（内存版扣款永远成功，真实版会余额不足），所以 fake 也要有对账测试对着真实实现定期跑。案例回流：替身语义分叉的实录见 [[工程知识/缺陷分析：从个案到体系/稳定性/案例二十三：一次抖动变成一次重算——SASL 拉取没有重试.md]]——测试环境的客户端替身不重试、生产客户端重试，同一段业务代码跑在两种语义上，这正是 fake 信任问题在缺陷域的表现。替身在流水线的分工见 [[工程知识/软件构建：让变化可以理解、验证与交付/测试与交付/回归测试的耗时分层：快速反馈层先跑，全量层收尾.md]]——快层用替身换速度，全量层用真实依赖换可信。

## 验证

验证的锚点：替身分类要与重构不变量对账——预写一次纯重构后交互断言过强的测试应碎掉，全都通过，与预期对不上，说明这些测试没有触及调用行为。

- 重构不变量测试：做一次纯重构（不改行为），交互断言过强的测试会碎——碎的那批就是过度指定。
- 约定一致性：fake 与真实组件跑同一套输入集，diff 输出与状态变化。
- 替身密度审查：一个测试里替身数量与被断言对象数量的比例，替身多于断言对象时测试在验证替身自己。

## 边界

- 替身选择是测试策略的下游决策：先定风险与证据等级（[[工程知识/软件构建：让变化可以理解、验证与交付/测试与交付/测试策略从风险选择证据]]），再选替身型，顺序不能反。
- 并发与时序依赖不适合 stub：竞态、超时、重试路径需要真实异步组件或故障注入（[[工程知识/软件构建：让变化可以理解、验证与交付/测试与交付/混沌工程验证系统是否真的能处理故障]]）。
- AI 生成测试的替身选择常系统性偏向交互断言（容易生成、容易过），审查时按本表逐个核对断言类型。

## 相关

- [[工程知识/软件构建：让变化可以理解、验证与交付/测试与交付/测试策略从风险选择证据]]：替身选择的上层策略，本文细化它的替身分类行
- [[工程知识/软件构建：让变化可以理解、验证与交付/测试与交付/调试从假设到最小复现]]：测试失败后的调查路径，与替身分类互补
- [[工程知识/软件构建：让变化可以理解、验证与交付/架构与代码/分层与依赖方向决定可测试性和演进成本]]：依赖方向决定哪里能插替身，架构与测试的连接点
- [[工程知识/软件构建：让变化可以理解、验证与交付/测试与交付/接口约定测试的适用边界：消费者驱动与提供者验证.md]] —— 替身按约定行为实现的思想源头
