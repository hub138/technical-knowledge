---
title: 测试替身按依赖行为而非名字分类
type: concept
status: active
updated: 2026-09-21
review_after: 2027-09-21
change_rate: low
confidence: high
sources:
  - "https://martinfowler.com/articles/mocksArentStubs.html"
  - "https://testdouble.com/"
  - "https://github.com/testdouble/contributing-tests/wiki/Detect-impact"
tags:
  - software/testing
  - testing
  - quality
  - code-quality
---

# 测试替身按依赖行为而非名字分类

单元测试要隔离依赖，于是有人 mock 数据库、有人 stub 支付接口、有人 fake 整个仓储层——名词满天飞，分歧却在更深处：替身返回什么、断言什么、能不能复用。要解决的问题：替身选错型，测试要么验证了不存在的交互（交互断言过强，重构即碎），要么放过了真实缺陷（状态断言缺位，mock 绿了线上炸了）。本文按替身的**行为契约**分类，而非传话筒式的名词分类；分对类型的收益是测试既验证真实行为又给重构留空间，分错类型的代价是测试套件与实现焊死。

## 四种替身的行为定义

| 替身 | 行为 | 验证什么 |
| --- | --- | --- |
| dummy | 占位，从不被使用 | 不验证任何东西 |
| stub | 返回预置值 | 被测单元的状态输出 |
| spy | 记录调用供事后断言 | 被测单元对外的副作用 |
| fake | 有真实简化实现 | 被测逻辑与轻量实现的组合 |

（mock 这个词在社区有两层含义：狭义 = spy + 交互期望，广义 = 一切替身。本文用行为定义避开名词之争，与 [[工程知识/软件构建：让变化可以理解、验证与交付/测试与交付/测试策略从风险选择证据]] 的弱证据/强证据表同构。）

## 交互断言的过度指定

"验证 orderService.create 被调用了一次"是交互断言。它的脆弱性：实现从一次调用改成两次批量调用，行为没变（订单都创建了），测试红了——这叫过度指定（over-specification）。判据：断言的交互是需求的**本质**（"必须发送恰好一封确认邮件"）还是**实现细节**（"调用了一次 mailer"）。前者合法，后者把测试焊死在实现上。

## fake 的信任问题

fake 是有简化实现的替身（内存数据库、内存队列）。它的风险：与真实实现行为漂移。防线是与真实实现跑同一套契约测试（contract test）：同一组输入，断言 fake 与真实组件行为一致。没有契约测试的 fake 是第二个待测系统，见 [[工程知识/软件构建：让变化可以理解、验证与交付/测试与交付/测试策略从风险选择证据]] 的"关键依赖用真实组件"行。

## 验证

- 重构不变量测试：做一次纯重构（不改行为），交互断言过强的测试会碎——碎的那批就是过度指定。
- 契约一致性：fake 与真实组件跑同一套输入集，diff 输出与状态变化。
- 替身密度审查：一个测试里替身数量与被断言对象数量的比例，替身多于断言对象时测试在验证替身自己。

## 边界

- 替身选择是测试策略的下游决策：先定风险与证据等级（[[工程知识/软件构建：让变化可以理解、验证与交付/测试与交付/测试策略从风险选择证据]]），再选替身型，顺序不能反。
- 并发与时序依赖不适合 stub：竞态、超时、重试路径需要真实异步组件或故障注入（[[工程知识/软件构建：让变化可以理解、验证与交付/测试与交付/混沌工程验证系统是否真的能处理故障]]）。
- AI 生成测试的替身选择常系统性偏向交互断言（容易生成、容易过），审查时按本表逐个核对断言类型。

## 相关

- [[工程知识/软件构建：让变化可以理解、验证与交付/测试与交付/测试策略从风险选择证据]]：替身选择的上层策略，本文细化它的替身分类行
- [[工程知识/软件构建：让变化可以理解、验证与交付/测试与交付/调试从假设到最小复现]]：测试失败后的调查路径，与替身分类互补
- [[工程知识/软件构建：让变化可以理解、验证与交付/架构与代码/分层与依赖方向决定可测试性和演进成本]]：依赖方向决定哪里能插替身，架构与测试的连接点
- [[工程知识/软件构建：让变化可以理解、验证与交付/测试与交付/契约测试的适用边界：消费者驱动与提供者验证.md]] —— 替身按契约行为实现的思想源头
