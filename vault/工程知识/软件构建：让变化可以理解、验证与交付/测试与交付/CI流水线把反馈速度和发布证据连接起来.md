---
title: CI 流水线把反馈速度和发布证据连接起来
type: playbook
status: active
updated: 2026-09-03
review_after: 2027-03-03
change_rate: medium
confidence: high
tags:
  - software/ci-cd
  - testing
  - delivery
sources:
  - "https://docs.github.com/en/actions"
  - "https://sre.google/sre-book/release-engineering/"
  - "https://slsa.dev/spec/v1.0/"
---

# CI 流水线把反馈速度和发布证据连接起来

它解决的是变更在开发机“看起来没问题”，却无法在干净环境重现、审计或安全发布的问题。核心做法是按风险编排静态、单元、集成、性能和发布检查，并保存 revision、环境、命令、退出码和产物；收益是反馈和发布证据连续，代价是运行时间、维护矩阵和 flaky 治理。通过缓存污染、runner 崩溃、依赖不可用和部分发布实验验证。

CI/CD 不是把命令排成 YAML，而是把一次变更从源码身份带到可验证产物、环境和回滚。快反馈检查发现明显错误，慢反馈检查证明集成、性能、安全和发布行为；两者需要按风险分层。

## 一条可信流水线

```text
变更 -> 静态/格式 -> 单元 -> 构建产物
-> 集成/接口约定 -> 安全/性能 -> 发布候选
     -> 灰度观测 -> 扩大或回滚
```

每一步保存 revision、环境、依赖、命令、退出码、日志和产物。缓存只加速可重建结果，不能隐藏输入变化；并行任务要避免共享可变目录和相互清理。

## 质量门禁

门禁要对应风险：schema 兼容、迁移、并发、权限、依赖漏洞、性能回归和 AI 模型/索引质量不能被一个绿色单测代替。失败分类要区分代码、测试、环境、网络、权限和数据问题，重试预算不能覆盖失败事实。

## 量化锚点：反馈时长的预算表

流水线各阶段的时长预算决定开发者行为：单次提交的反馈在 10 分钟内，开发者会等结果再切任务；超过 30 分钟，开发者批量提交、问题定位单元变大，缺陷修复成本随定位延迟上涨。预算的常见分法：静态检查与单测 5 分钟内（并行化）、集成测试 15 分钟内（按变更影响面裁剪）、全量回归只在合并前跑。提速的手段按收益排：测试并行分片（收益线性）、依赖缓存（首次构建省 50-80%）、影响面分析跳过无关测试（大库收益显著）。

## 案例回流：门禁证据的衔接

流水线的门禁环节与 [[工程知识/软件构建：让变化可以理解、验证与交付/测试与交付/发布门禁必须绑定真实证据]] 一脉相承：流水线不只是跑测试，是给发布决策供证据（哪个 commit、哪些测试过、什么环境验过）。反面教材在缺陷域有实录：[[工程知识/缺陷分析：从个案到体系/配置/案例四：配置不生效——recover 速率与重复日志]] 类的配置漂移问题，正是流水线里缺"配置生效值核对"这一步证据的后果——门禁链条上一环缺失，问题就会在产线现形。

## 验证

验证的锚点：流水线可信度要与故障注入对账——预写注入不稳定用例、缓存污染与超时后重跑应可解释，实测重跑结论不一致，与预期对不上。

故意制造 flaky、依赖不可用、缓存污染、取消、超时、runner 崩溃和部分发布；检查重跑是否可解释、产物是否绑定正确 revision、报告是否能支撑发布结论。周期性在干净环境重建，验证流水线不是依赖开发机状态。

关系：[[工程知识/软件构建：让变化可以理解、验证与交付/测试与交付/发布门禁必须绑定真实证据]] · [[工程知识/软件构建：让变化可以理解、验证与交付/开发工具/依赖供应链需要锁定来源与构建输入]]

## 边界

- 流水线的可信度取决于执行环境与开发机状态分离：依赖本地缓存或本地服务时，干净环境重建会失败。
- 缓存只加速可重建的结果：把输入变化一起缓存会掩盖真实的构建差异。
- 并行任务之间不能共享可变目录：共享时相互清理与相互覆盖会让失败无法定位到具体任务。
- 门禁覆盖的范围由检查项决定：单测通过不覆盖 schema 兼容、迁移、权限与性能回归。
- 重试会掩盖失败事实：区分不了代码问题与环境问题时，重试把真实缺陷留在流水线之外。

## 机制与本质

流水线把一次变更的多个判断点串成一条链，每个判断点输出的是绑定到具体源码版本的证据，链的整体价值来自证据的可追溯性。反馈时长由最慢的判断点决定，因此阶段划分与并行化决定开发者是否在提交后等待结果；缓存与影响面分析作用于路径长度，不改变判断的严格程度。

## 要解决的问题

变更在本地通过全部检查，进入干净环境却重现不了，或者发布之后无法说明当时验证的是哪个版本、跑过哪些检查。本篇回答：一次变更从源码身份到发布候选要经过哪些判断点、每一段需要保存什么证据、反馈时长怎样影响提交行为，以及哪些失败不能被重试掩盖。

