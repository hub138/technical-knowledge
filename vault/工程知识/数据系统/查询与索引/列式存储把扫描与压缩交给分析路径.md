---
title: 列式存储把扫描与压缩交给分析路径
type: concept
status: active
updated: 2026-09-03
review_after: 2027-09-03
change_rate: stable
confidence: high
tags:
  - data/analytics
  - columnar
  - olap
sources:
  - "https://parquet.apache.org/docs/"
  - "https://clickhouse.com/docs/en/engines/table-engines/mergetree-family/mergetree"
  - "https://arrow.apache.org/docs/format/Columnar.html"
---

# 列式存储把扫描与压缩交给分析路径

它解决的是分析查询只需要少数列，却被行式存储迫使读取整行和大量无关字节的问题。核心机制是同列布局、编码压缩、分区和谓词下推；收益是扫描带宽和 CPU 降低，代价是单行更新、事务交互、小文件合并和数据延迟更难。用真实列选择性、更新比例、并发和缓存状态比较，而不是只看压缩比。

OLTP 常按行读取和更新单个实体；OLAP 常扫描少数列、聚合大量记录。列式格式把同列值放在一起，便于压缩、向量化和谓词下推，但不适合频繁单行更新和强事务交互。

## 主要机制

- 字典、RLE、位图和编码利用同列相似性降低存储与带宽。
- 分区、排序键、min/max 统计和 zone map 跳过无关数据。
- 向量化执行按批处理列，减少解释和函数调用开销。
- 增量写入通常先进入小文件/内存表，再合并；小文件和合并 backlog 会损害查询。

## 数据架构边界

数据仓库、湖表和实时 OLAP 的事务、schema、更新、时间旅行和一致性承诺不同。把主库 CDC 原样堆到分析系统不等于得到正确数仓；必须定义删除、迟到、重复、重放和指标口径。

## 最小扫描实验

用同一数据分别做少列聚合、全列扫描、单行更新和高并发写入，比较扫描字节、CPU、压缩后空间、合并 backlog、p95 和结果新鲜度。列式方案只有在主要读取模式的扫描收益超过更新和合并代价时才合适，不能用单一压缩比代表系统收益。

## 验证

用真实扫描列数、选择性、分区命中、并发和数据更新比例测试；记录扫描字节、压缩比、CPU、内存、合并、p95 和结果一致性。比较列式与行式方案时固定数据布局和缓存状态。

关系：[[工程知识/数据系统/数据管道与流处理/CDC把数据库变化传播为可重放事实]] · [[工程知识/数据系统/数据系统：在并发与故障中保存事实]]
