---
title: 数据编排要保证依赖、重试与幂等
type: playbook
status: active
updated: 2026-09-03
review_after: 2027-03-03
change_rate: medium
confidence: high
tags:
  - data/orchestration
  - data/pipelines
  - reliability
sources:
  - "https://airflow.apache.org/docs/apache-airflow/stable/core-concepts/dags.html"
  - "https://docs.dagster.io/getting-started/concepts"
  - "https://beam.apache.org/documentation/programming-guide/"
---

# 数据编排要保证依赖、重试与幂等

## 核心拆解

| 维度 | 回答 |
| --- | --- |
| 要解决的问题 | 多个数据任务有依赖、分区和时间窗口，迟到、崩溃或重复触发会导致产物缺失、重复写入或覆盖新结果。 |
| 本质 | 编排器维护带输入快照和产物版本的状态图；重试只有在同一输入下可安全重放时才有意义。 |
| 做法 | 用数据集、分区、窗口、代码 revision 和输入快照定义任务身份，临时写入后原子提交，显式记录 attempt、回填和失败状态。 |
| 效果与代价 | 任务可重跑、补数、恢复和审计；代价是版本存储、调度状态、资源竞争和幂等设计复杂度。 |
| 边界 | 调度成功不等于业务指标正确或下游已消费；无限重试、跳过和部分成功不能替代数据质量与发布门禁。 |
| 验证 | 注入 worker 崩溃、依赖迟到、重复触发、空分区、schema 变化和回填并发，核对产物版本、下游可见性和重建能力。 |

数据任务是带输入版本、分区、时间窗口和产物的有向图。调度器负责依赖、触发、重试、并发和状态；它不负责证明业务指标正确或下游已经消费新数据。

一个任务显示成功，可能只是文件写完，尚未通过质量门禁或被下游读取；如果重试没有绑定输入快照，还可能用新数据覆盖旧版本。编排的核心是让“这次运行处理了什么、产出了什么、谁可以看到”可追踪。

这也是为什么数据平台的绿色任务状态不能直接当成指标可信：任务完成、质量通过、索引发布和下游可见是不同状态，必须在图中分别表示。

## 任务身份

```text
dataset + partition/window + code revision + input snapshot -> output version
```

重试必须幂等：写临时产物后原子提交、使用分区/版本键、记录 attempt 和输入快照。回填会与实时任务竞争资源；暂停、跳过、失败、部分成功和手工重跑必须是显式状态。

## 验证

注入 worker 崩溃、依赖迟到、重复触发、上游空数据、schema 变化、分区缺失、重试和回填并发；检查是否重复/覆盖错误产物、是否可从任意版本重建、下游是否看到一致快照。AI/RAG 索引构建也应使用相同的任务身份和发布门禁。

关系：[[工程知识/数据系统/数据管道与流处理/CDC把数据库变化传播为可重放事实]] · [[工程知识/后端与分布式系统/任务与并发/任务生命周期必须覆盖进程、日志、超时与清理]]
